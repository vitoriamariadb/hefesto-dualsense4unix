# CONFERIDO-EM — a segunda porta do portão FOTOS-DA-VERSAO-01.
#
# A régua é `tests/unit/test_as_fotos_acompanham_a_versao.py`, e a primeira
# porta dela é a topologia: as fotos desta pasta têm de vir DEPOIS da última
# mexida em `src/…/app`, `src/…/gui` ou `scripts/gui-captura`.
#
# Só que a primeira porta pede um COMMIT de foto como prova de um GESTO. Quando
# o gesto não produz bytes — mudou-se código de tela e nenhuma imagem mudou —
# não há o que commitar, e o portão fica vermelho para sempre. Este arquivo é a
# saída, e ela não afrouxa nada:
#
#     rodou o retratista e as imagens MUDARAM   -> commite as imagens
#     rodou o retratista e nada mudou           -> declare aqui o commit
#
# Uma linha por conferência. O primeiro campo é o SHA COMPLETO (40) do commit de
# tela conferido; o resto da linha é para quem for auditar. `#` é comentário.
# A declaração envelhece sozinha: qualquer mexida nova na tela gera outro SHA,
# nenhuma linha daqui o nomeia, e o portão reabre.
#
# NÃO REFOTOGRAFE SÓ PARA GERAR BYTES. A foto carrega o estado VIVO da máquina
# de quem a tira — a linha "Controles detectados" e a cor do plástico lida de
# cada controle. Commitar a sua troca a mesa dela pela sua, calada.

2899ef0d97497b9eb3b353cd8515d61d4812c344  03/09/2026  retratista rodado com o `fatos_do_mapa.py` de antes e de depois deste commit, mesma máquina e mesma mesa: as 14 fotos byte a byte IDÊNTICAS. O commit mexeu em `app/fatos_do_mapa.py` (a tabela que a aba Emulação e a seção Controles leem), e moveu ZERO pixel.

HEAD  03/09/2026  retratista rodado com a interface de hoje. TRÊS fotos mudaram, e as três foram olhadas antes de decidir. As DUAS de Configurações entram no commit: elas mostram uma CURA — o Jogador 2 e o Jogador 3, os dois por Bluetooth, traziam uma caixa vazia dizendo "Escreva a cor" e agora dizem "Nova Pink (lido)" e "Midnight Black (lido)", com a bolinha da cor. É o `secao_controles.py` que parou de recusar a pergunta a quem está no rádio (`transporte != "usb": continue`). A aba usa uma mesa de fixture de quatro jogadores, então nada da máquina viva vaza nela. A de Emulação NÃO entra: a única diferença é a linha "Controles detectados", que dizia "2 controles físicos, 2 gamepads virtuais — 8 nós" e passaria a dizer "1 controle físico — 2 nós". Isso é a mesa de agora, não o produto — commitá-la trocaria a mesa dela pela de quem rodou, calada, que é exatamente o que esta régua avisa.

901e6b066f21a47447de1ab2697ccb9952d393ce  04/09/2026  retratista rodado na mesma máquina. DUAS fotos mudaram e as duas foram OLHADAS antes de decidir. A de Navegação ENTRA: a mudança é de CONTEÚDO — a linha do L3 dizia "Abrir teclado na tela" e passou a dizer "Abrir e fechar teclado na tela", que é o que o botão faz. A de Emulação NÃO entra, pelo mesmo motivo da declaração acima: a única diferença é "Controles detectados", de "2 controles físicos, 2 gamepads virtuais — 8 nós" para "1 controle físico, 1 gamepad virtual — 3 nós". Isso é a mesa DE AGORA, não o produto. Medido por GdkPixbuf: a de Navegação difere em 11 linhas de 1080 (y 553-563), e nenhuma outra foto moveu um pixel.
