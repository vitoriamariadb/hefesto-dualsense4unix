# Agente: v2:1a03c6a56688602ec810a4d58b04598cd42f83ce97679260afdfdda1d7185694

## report

MAPA DA NUMERAÇÃO GLOBAL DE PLAYERS (player_slot) + P3 + P6 + guard anti-dup — dados verificados AO VIVO em 2026-07-18 ~20:40.

== EXISTEM 4 DOMÍNIOS DE NUMERAÇÃO INDEPENDENTES (nenhum sincroniza com o que o jogo vê) ==

(A) `player_slot` dos DualSense — src/hefesto_dualsense4unix/daemon/subsystems/identity.py, classe ControllerIdentityRegistry (linhas 96-388). MAC normalizado (12-hex, `_canonical` l.172-184) → menor slot livre, atribuído LAZY na 1ª consulta do provider de cor (`slot_for` l.186-237, assign=True vem de make_auto_output_provider l.391+ dentro do reconcile do backend = ordem de ADOÇÃO). Persistido por boot em ~/.config/hefesto-dualsense4unix/controllers.json (load l.300-346 valida boot_id; sync_connected l.253-289 roda no tick ~2s do poll loop — lifecycle.py l.1619-1646 e l.1676-1678). Slot vira RESERVA no disconnect (D2) e só expira quando a sessão esvazia. Vpad NUNCA ganha slot (`_VPAD_MAC_PREFIX="02fe"` l.78, rejeitado em slot_for l.205-210 e sync l.269-270). Exposto no IPC por ipc_handlers.py `_player_slot_for` l.894-913 (assign=False) → campo `player_slot` dos controllers (l.813). ESTADO VIVO: controllers.json = {"<MAC-DS-BRANCO>": 1 (branco USB), "<MAC-DS-ROXO>": 2 (roxo BT)}; journal 19:59:48 slot=1 branco, 20:00:47 slot=2 roxo.

(B) `player_index` do co-op — daemon/subsystems/coop.py. Primário=1 fixo; secundários ganham o menor índice ≥2 (`_next_player_index` l.312-322, com REUSO de índice de quem saiu). É ESTE número que: nomeia o vpad ("Hefesto Virtual DualSense P{N}", integrations/uhid_gamepad.py l.479) e o MAC forjado (l.269: `02:fe:00:00:00:{player:02x}`); acende os player-LEDs BRANCOS dos DualSense via sysfs (`_apply_coop_player_leds` l.500-537, nós `*:white:player-N` casados por MAC); e alimenta `resolve_player_numbers` l.777-814 ("número que o JOGO vê" — na prática, o número que o daemon ACHA que o jogo vê). player_indexes() l.133-153.

(C) `player_slot` dos EXTERNOS (Nintendo/8BitDo) — ipc_handlers.py `_external_inventory` l.156-192: slot = `dualsense_count + index + 1` (l.185). `dualsense_count` = nº de handles ADOTADOS com connected=True em describe_controllers (handler `_handle_controller_list` l.1041-1049; backend_pydualsense.py l.1546+ — só DualSense físicos, vpads nunca entram). `index` = posição em `discover_external_gamepads()` (core/evdev_reader.py l.243-311), que ordena por NÚMERO DO EVENT NODE (l.278 `sorted(list_devices(), key=_event_num)`) — ou seja, ordem de plug/reconexão (BT que reconecta ganha eventN alto e vai pro fim). SEM registry, SEM reserva por MAC, SEM persistência: recalculado do zero A CADA chamada IPC `controller.list {external:true}`. Quem chama: a GUI, a cada ~4 s (app/actions/status_actions.py `_maybe_fetch_externals` l.433-459). O LED físico é escrito como EFEITO COLATERAL de cada poll (l.189-191 → core/external_leds.py apply_player_number l.153-167; barra verde via write_player_number l.65-83 nos nós /sys/class/leds/<inst>:green:player-{1..4}/brightness; modo DS4-BT via write_lightbar_slot l.130-150). Escrita silenciosa (contextlib.suppress, ZERO telemetria — nada no journal). GUI fechada = LED nunca atualiza. GUI espelha o mesmo número (app/actions/external_controllers.py external_slot l.139-146, slot_of l.149-156).

(D) O que o JOGO/SDL vê — fora do controle do daemon. SDL2 enumera drivers na ordem HIDAPI PRIMEIRO, evdev depois; dentro do HIDAPI, ordem de hid_enumerate = hidraw ascendente; hotplug é apendado por ordem de chegada. ESTADO VIVO dos hidraw: hidraw2=Pro Controller USB, hidraw3=DualSense branco físico, hidraw6=vpad P1 (0df2), hidraw7=roxo BT físico, hidraw8=vpad P2 (0df2), hidraw9=8BitDo BT — todos com ACL uaccess p/ vitoriamaria (getfacl confirmado), i.e. o SDL HIDAPI consegue abrir TODOS. Com o wrapper (IGNORE=0x054c/0x0ce6, launch_env.py l.66), os DOIS físicos somem e a ordem SDL vira: #0 Pro USB (hidraw2), #1 vpad P1 (hidraw6), #2 vpad P2 (hidraw8), #3 8BitDo (hidraw9) — SE tudo já estava conectado no launch. Qualquer reconexão BT (8BitDo caiu AGORA durante a análise: event266 sumiu de /sys — muro conhecido do hid-nintendo BT) reordena por chegada.

== P3: por que LED=2 no 8BitDo mas jogo=player 1 ==
Mecanismo comprovado: o slot do externo é função do ESTADO DO DAEMON NO INSTANTE DO POLL da GUI, não da enumeração do jogo. Evidência ao vivo da instabilidade: hoje o LED do 8BitDo foi escrito como 2 (madrugada: journal 00:53-01:33 mostra só 1 DualSense adotado ds_count=1 + 8BitDo como 1º externo → 2) e AGORA está com os 4 LEDs acesos = slot 4 (lido de /sys/class/leds/0005:057E:2009.000C:green:player-{1..4}/brightness = 1,1,1,1; o Pro USB 0003:057E:2009.0003 = 1,1,1,0 = slot 3). Já o jogo ordena por índice SDL dos devices VISÍVEIS: com IGNORE escondendo os físicos, o 8BitDo pode ser #0 (player 1) se: (i) o jogo abriu antes de os vpads subirem (vpads são recriados em cada transição — journal 20:15:11-20:15:30 mostra teardown+respawn de P1/P2), (ii) o Pro USB não estava plugado no launch, ou (iii) hotplug pós-launch reordenou. Não há NENHUM mecanismo que leia a ordem SDL/Steam ou a influencie (o daemon não controla hidraw index nem ordem de criação dos externos — eles são do kernel). Divergência estrutural: (1) ds_count conta físicos ADOTADOS que o jogo NEM VÊ (escondidos pelo IGNORE, substituídos por vpads em outra posição); (2) index dos externos = ordem de event node ≠ ordem hidraw/hotplug do SDL; (3) slot recalculado a cada 4 s sem identidade (replug troca números entre Pro e 8BitDo); (4) domínios A/B/C podem discordar ENTRE SI (identity slot ≠ coop player_index se controles replugam em ordem diferente — registry reserva, coop reusa).

== Vpad sobe / IGNORE: o daemon renumera? ==
NÃO. Nenhum dos domínios A/B/C observa o vpad ou o wrapper: identity registry rejeita 02fe; ds_count ignora vpads (describe_controllers só tem handles físicos); a subida/queda do vpad só regrava launch_env (launch_env.py materialize_launch_env l.261-330; default.env AO VIVO: PROTON_ENABLE_HIDRAW=1 + IGNORE + shader cache, backends=['uhid','uhid']). O deslocamento dos externos na ordem SDL acontece (físicos somem, vpads entram em hidraw 6/8), mas o daemon é cego a isso.

== P6: Motion Sensors como js2/js4/js6/js8 ==
AO VIVO: js0=Pro USB(event6), js1=branco(event8), js2=branco Motion(event9), js3=vpad P1(event26), js4=vpad P1 Motion(event27), js5=roxo(event259), js6=roxo Motion(event260), js7=vpad P2(event262), js8=vpad P2 Motion(event263). 8BitDo BT NÃO tinha js. Os 4 nós Motion têm properties=0x40 (INPUT_PROP_ACCELEROMETER) e udev: ID_INPUT_ACCELEROMETER=1 SEM ID_INPUT_JOYSTICK (verificado em event9, event27, js4) → o backend udev do SDL2 já os IGNORA; jogos SDL modernos NÃO os enumeram. O jsN é criado pelo joydev do KERNEL (que não consulta udev nem pula acelerômetros) — polui apenas a API legada /dev/input/js* (engines antigas/SDL1/scanners de js). A regra assets/78-dualsense-motion-not-joystick.rules (INSTALADA em /etc/udev/rules.d/) zera ID_INPUT_JOYSTICK só para os nomes Sony físicos (l.11-12) e NÃO casa "Hefesto Virtual DualSense P1 Motion Sensors" — mas é redundante: o builtin input_id já classifica por INPUT_PROP_ACCELEROMETER. Supressão real do jsN: impossível via ENV (joydev é kernel); opções: (a) udev MODE="0000"/GROUP root nos jsN cujo pai casa ATTRS{name}=="*Motion Sensors*" (esconde de scanners js), (b) nada a fazer no uhid — quem cria o input de motion é o hid-playstation ao bindar no descriptor Edge, inerente ao mascaramento. Custo real de P6 p/ jogos SDL: ZERO; o problema visível é o deslocamento de índices js (o físico branco=js1 e vpad=js3 com "buracos" de motion entre eles) só em jogos js-API.

== Guard anti-dup + por que P2 persiste ==
Guards existentes: (1) BUG-COOP-BOOT-PRIMARY-DUP-01 — coop.py l.242-252: adia spawn de secundário enquanto o MAC do primário não resolveu (evita vpad para o PRÓPRIO controle do P1 no boot); (2) grab EVIOCGRAB do evdev físico do primário enquanto o vpad existe — gamepad.py `_set_controller_grab` l.101-124 (journal 20:15:28: grab=True ok=True state=held); secundários idem via EvdevReader grab (coop spawn); (3) dedup_status/DEDUP-06 — gamepad.py l.236-285 (só REPORTA se o IGNORE seria seguro); (4) wrapper hefesto-launch (assets/hefesto-launch.sh, allowlist l.84-92) + launch_env. POR QUE P2 (plug direto SEM wrapper) duplica: a env SDL_GAMECONTROLLER_IGNORE_DEVICES só existe DENTRO do processo lançado pelo wrapper — jogo lançado sem ele não recebe nada (design fail-safe documentado em launch_env.py l.1-35: "duplicado > zero controles"). O grab (2) esconde só o canal EVDEV; o canal HIDRAW fica aberto: /dev/hidraw3 (branco físico) tem ACL de usuária → o SDL HIDAPI (driver PS5, prioridade sobre evdev) abre o físico E o vpad (hidraw6) e cria 2 controllers que se movem juntos (o daemon espelha o físico no vpad) = "mexe player 3 e 4". É a conclusão da memória "udev-puro REFUTADO (HIDAPI escapa)" — não há guard no canal hidraw hoje (nenhum revoke de ACL do físico enquanto o vpad está de pé). Nota: o roxo BT não duplica visivelmente porque em BT o firmware ocioso emudece o hidraw p/ HIDAPI, e no co-op ele é grabado + tem vpad próprio.

== Observações colaterais úteis à frota ==
- A escrita do LED externo depende da GUI aberta (poll de 4 s) — o daemon sozinho nunca numera externo; applet/CLI só se chamarem controller.list external:true.
- describe_controllers ds_count conta roxo BT "connected" mesmo quando o jogo não o vê — mais uma fonte de divergência slot×jogo.
- 8BitDo BT caiu DURANTE esta análise (event266/hidraw9 sumiram ~20:45) — confirma o hid-nintendo BT instável; o próximo poll da GUI vai renumerar (Pro vira o único externo → slot 3) e o LED do 8BitDo, ao reconectar, pode receber outro número de novo.

## key_files

- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/ipc_handlers.py:156-192, 894-913, 1004-1050 — _external_inventory: slot do externo = dualsense_count+index+1 (l.185) + escrita do LED como efeito colateral do poll (l.189-191, silenciosa); _player_slot_for (registry, assign=False); _handle_controller_list calcula ds_count (l.1044-1049).
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/subsystems/identity.py:78, 96-388 — ControllerIdentityRegistry: player_slot dos DualSense por MAC, lazy, reserva de sessão, persistência controllers.json por boot; vpad 02fe rejeitado (l.205-210). Externos NÃO passam por aqui — ausência de identidade é a raiz da instabilidade do número deles.
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/external_leds.py:65-167 — Escrita do LED de player dos externos: write_player_number (barra verde 1..N), write_lightbar_slot (modo DS4 BT), apply_player_number decide o modo; tudo best-effort sem log.
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/evdev_reader.py:243-311 — discover_external_gamepads: ordem = número do event node (l.278) → índice do slot; dedup por uniq; exclui virtuais e DualSense.
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/subsystems/coop.py:133-153, 242-252, 312-322, 500-537, 777-814 — 3º domínio de numeração: player_index (1=primário, ≥2 reusável) — nomeia vpad P{N}/MAC 02:fe:..:0N, acende white:player-N dos DualSense, e resolve_player_numbers; guard BUG-COOP-BOOT-PRIMARY-DUP-01 (l.242-252).
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/subsystems/gamepad.py:101-124, 236-285 — _set_controller_grab (EVIOCGRAB só cobre evdev — hidraw escapa = P2) e dedup_status/DEDUP-06 (guard que só reporta, não bloqueia).
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/launch_env.py:58-106, 261-330 — compose_env/materialize: IGNORE=0x054c/0x0ce6 só via wrapper; sem wrapper = sem dedup (P2). Regravado nas transições do vpad — não renumera nada.
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/assets/hefesto-launch.sh:35-109 — Wrapper: allowlist e gate de vida; a env só existe no processo lançado por ele.
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/actions/status_actions.py:433-459 — GUI faz o poll controller.list {external:true} a cada ~4 s — é o ÚNICO gatilho da escrita de LED externo; GUI fechada = LED congelado.
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/actions/external_controllers.py:139-176 — external_slot/slot_of/button_labels_for: a GUI espelha o player_slot do daemon (mesma conta ds_count+index+1).
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/assets/78-dualsense-motion-not-joystick.rules:11-12 — P6: só casa nomes Sony físicos, não o vpad 'Hefesto Virtual * Motion Sensors' (redundante na prática: INPUT_PROP_ACCELEROMETER já faz o input_id classificar); não impede o jsN do joydev (kernel).
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/assets/79-external-controller-leds.rules — Regra que torna graváveis os nós green:player-N (057E) e a lightbar DS4 — instalada e funcionando (LEDs escritos ao vivo).
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/integrations/uhid_gamepad.py:269, 479 — Vpad nomeado pelo player_index do co-op (P1/P2) e MAC 02:fe:00:00:00:0N — domínio B na cara do jogo.
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/led_control.py:125-137 — player_slot_color: paleta canônica 1=azul 2=vermelho 3=verde 4=rosa usada pela lightbar automática e pelo modo DS4 dos externos.

## hypotheses

- P3-H1 (CONFIRMADA no mecanismo): não existe NENHUM elo entre o player_slot do daemon e o índice SDL do jogo — o slot do externo é ds_count(físicos adotados, mesmo os escondidos pelo IGNORE) + posição por número de event node, recalculado a cada poll de 4s da GUI (ipc_handlers.py:185-191); o jogo ordena pelos devices VISÍVEIS na ordem HIDAPI-hidraw/hotplug. Evidência viva: LED do 8BitDo foi 2 de madrugada (ds_count=1) e está 4 agora (ds_count=2 + 2º externo), sem o jogo mudar nada.
- P3-H2 (provável no episódio relatado): no momento do launch do jogo o 8BitDo era o 1º device SDL visível (vpads ainda subindo — são derrubados/recriados a cada transição, journal 20:15:11-30; ou Pro USB plugado depois) → SDL #0 = player 1; o LED 2 veio do poll da GUI num instante em que só 1 DualSense estava adotado e o 8BitDo era o 1º externo.
- P3-H3 (estrutural, agrava): externos não têm identidade/reserva (diferente do ControllerIdentityRegistry dos DualSense) — replug/reconexão BT muda o event node e TROCA os números entre Pro Controller e 8BitDo; e o LED só é reescrito se a GUI estiver aberta.
- P2-H1 (CONFIRMADA por construção): sem wrapper não há IGNORE no env do jogo (decisão fail-safe 'duplicado > zero controles', launch_env.py) e o EVIOCGRAB só cobre evdev — o SDL HIDAPI abre /dev/hidraw3 do físico (ACL uaccess) e o vpad hidraw6 ao mesmo tempo = 2 controllers espelhados (players 3 e 4). Não existe guard no canal hidraw (ex.: revogar ACL do hidraw físico enquanto o vpad está vivo).
- P6-H1 (CONFIRMADA): js2/js4/js6/js8 são criados pelo joydev do kernel (não pula INPUT_PROP_ACCELEROMETER e não consulta udev) — mas os nós Motion NÃO têm ID_INPUT_JOYSTICK (verificado), logo SDL2/jogos modernos já os ignoram; poluição real só na API js legada. A regra 78 não casa os nomes 'Hefesto Virtual * Motion Sensors' porém é redundante (builtin input_id já classifica). Supressão possível: udev MODE=0000 nos jsN de pais '*Motion Sensors*'; impossível evitar a criação do input de motion no vpad uhid (é o hid-playstation bindando no descriptor Edge).

## open_questions

- Ordem SDL exata NO JOGO da sessão relatada: precisa de captura no ambiente do jogo (SDL_JOYSTICK_DEBUG ou testcontroller do SDL dentro do runtime) para cravar se o 8BitDo era #0 por hotplug ou por launch precoce — o host não guarda essa ordem.
- Steam Input estava fora do caminho nesses jogos? (PSSupport=2 desativado historicamente, mas o Switch Pro Controller tem toggle próprio — se o Steam Input embrulha o 8BitDo, a ordem é do Steam, não do SDL).
- Se a numeração deve seguir o JOGO: a única fonte honesta seria o daemon reordenar seus slots pela ordem hidraw/hotplug dos devices visíveis (modelar o algoritmo do SDL) OU parar de prometer 'o número que o jogo vê' e tratar o LED como identidade da GUI apenas — decisão de produto pendente.
- O LED do 8BitDo em modo Switch BT exibe write_player_number como os 4 LEDs cumulativos (1..N acesos) — conferir se o firmware 8BitDo mostra isso como 'player 4' ou padrão próprio (a usuária reportou 'player 2' quando o daemon havia escrito slot 2 = LEDs 1+2 acesos; um observador pode ler '2 LEDs' como player 2 mas '4 LEDs' como outra coisa).
- P2: vale criar guard no canal hidraw (revogar ACL do hidraw do físico enquanto o vpad uhid correspondente está vivo, com restore no teardown)? Resolveria plug-direto sem wrapper, mas conflita com Modo Nativo (jogo precisa do hidraw físico) e com o próprio daemon (pydualsense usa o hidraw) — exige gate cuidadoso.
- Por que o 8BitDo BT (event266) não tinha handler jsN enquanto vivo (os demais gamepads têm)? Irrelevante p/ SDL, mas indica que o joydev pode ter atingido alguma condição — não investigado (o device caiu durante a análise, muro hid-nintendo BT conhecido).
