# Agente: v2:589c7cb503b71c5821eed6f38993a465b155d73c781cf07859409e8bf46b6158

## report

DIAGNÓSTICO AO VIVO 2026-07-18 ~20:30-20:47 (read-only; daemon PID 2469 rodando desde 19:59:46, código = worktree sprint/harmonia-uhid @62483b2, venv editable — daemon E GUI rodam o código do branch).

== TOPOLOGIA CONFIRMADA AGORA ==
- hidraw2 = Nintendo Pro USB (0003:057E:2009.0003, event6/js0, driver nintendo, uniq <MAC-NINTENDO>)
- hidraw3 = DualSense BRANCO USB (0003:054C:0CE6.0004, input9, event8/js1, uniq <MAC-DS-BRANCO>) — físico do vpad P1
- hidraw7 = DualSense ROXO BT (0005:054C:0CE6.000A, input43, event259/js5, uniq <MAC-DS-ROXO>)
- hidraw6 = vpad P1 uhid (0003:054C:0DF2.000D, input64, event26/js3; Motion=input65/js4)
- hidraw8 = vpad P2 uhid (0003:054C:0DF2.000E, input68, js7; Motion=input69/js8) — criado 20:15:30 p/ o roxo (co-op)
- hidraw9 = 8BitDo BT (0005:057E:2009.000C) — MORTO no kernel desde ~20:23 (ver abaixo): event266/input50 FOI EMBORA, nós de LED sumiram; hidraw9 segue aberto (fds stale) por steam/winedevice.
- Holders (lsof): daemon 2469 = uhid(2x) + hidraw3 + hidraw7 + event8 + event259 (grab held). steam 6034 = hidraw2,3,7,8,9 (NÃO tem hidraw6!). winedevice 7347 = TODOS os hidraw 0-9 (inclusive físicos 3 e 7 e ambos os vpads 6 e 8). NINGUÉM além do daemon lê evdev/js.
- Steam: SteamController_PSSupport=0 (localconfig.vdf:1106) — Steam Input PS desligado; quem expõe os físicos ao jogo é o caminho hidraw do Proton (winedevice), que o PROTON_ENABLE_HIDRAW=1 DO NOSSO launch_env habilita e que o SDL_GAMECONTROLLER_IGNORE_DEVICES NÃO filtra.
- launch_env (regravado 20:37:59): default.env e steam_app_1599660.env ambos com PROTON_ENABLE_HIDRAW=1 + SDL_GAMECONTROLLER_IGNORE_DEVICES=0x054c/0x0ce6, backends=['uhid','uhid'].

== P1 LIGHTBAR VERDE-LIMÃOAZUL (branco USB) — RACE DE 3 ESCRITORES, PROVADO ==
- sysfs input9:rgb:indicator = "0 0 153" (AZUL slot-1 da paleta automática, brilho 0.6) e input43 = "153 0 0" (vermelho slot-2, bate com o roxo OK). Perfil ativo 'vitoria' quer lightbar [129,61,156] (roxa) — a cor automática por slot VENCE o perfil (make_auto_output_provider, daemon/subsystems/identity.py:391-431, via _merged_desired_for_key).
- Escritor periódico observado: mtime de input9/brightness+multi_intensity avança a CADA 30s exatos (20:35:37→20:36:07), valor sempre 0 0 153, SEM linha de log correspondente. Fonte: reconnect_loop com fallback RECONNECT_ONLINE_CHECK_INTERVAL_SEC=30.0 (daemon/connection.py:30,339) chama controller.connect() como tick de hotplug; connect() SEMPRE termina em reassert_resolved_outputs() (COR-WAKE-01, core/backend_pydualsense.py:774-790 — "re-resolve a cor em TODA reconciliação") → set_rgb azul → kernel hid_playstation monta um output report próprio → lightbar pisca AZUL.
- Escritor rival (verde-limão): o jogo/lado Windows via winedevice (hidraw3 aberto; PROTON_ENABLE_HIDRAW=1) escrevendo report 0x02 direto no físico — o IGNORE do SDL não alcança esse caminho. Verde forte = cor de player-index do SDL/jogo p/ 3º-4º controle (consistente com P2: "mexe player 3 e 4"). O daemon NÃO briga via hidraw (prepareReport limpa flags 0x04|0x10 quando _suppress_leds, backend_pydualsense.py:306-335), então a alternância é exatamente jogo(hidraw, verde)  daemon(sysfs 30s, azul).
- Nota: os nós rgb:indicator estão 0666 (regra 77) — qualquer processo também poderia escrever, mas os 2 escritores acima explicam o sintoma.

== P1 RUMBLE NÃO CHEGA NO BRANCO USB ==
- Nenhum erro de FF no journal (nem game_rumble_target_failed nem game_rumble_failed). IPC state_full: rumble_ff {plays:19, vpads:2} — agregado, SEM split por vpad (uhid_gamepad.ff_play_count existe mas o handler soma).
- Caminho correto existe: UHID_OUTPUT→pump_ff (integrations/uhid_gamepad.py:811-884)→rumble_sink→apply_game_rumble(target_uniq=primary_uniq) (daemon/subsystems/gamepad.py:371-389, 159-208)→set_rumble_for no handle USB.
- Hipótese líder: o jogo NÃO manda FF ao vpad P1 — manda ao FÍSICO branco via winedevice/hidraw3 (o jogo o enxerga como controle próprio pelo caminho Proton-hidraw). Só que o report_thread do daemon escreve output report no hidraw3 quando o report muda OU a cada OUT_REPORT_KEEPALIVE_SEC=0.5s (backend_pydualsense.py:262-296) com motores=0 (passthrough) → ZERA o rumble do jogo em ≤0.5s. É a mesma física do bug "rumble zerado a cada meio segundo" documentado em FEAT-NATIVE-OUTPUT-MUTE-01 (backend_pydualsense.py:247-252) — só que aqui o mute NÃO está ativo porque o modo não é Nativo. O roxo BT vibra porque o jogo fala com o vpad P2 (plays=19) e/ou porque em BT a disputa não zera igual.
- Agravante mid-game: às 20:15:11-20:15:30 os DOIS vpads foram destruídos e recriados (toggle manual na GUI — manual_lock 30s visível em profile_mode_skipped_manual_lock 20:15:40) → handles SDL/wine do jogo p/ o vpad P1 antigo morreram no meio da partida; Steam hoje NÃO tem hidraw6 aberto (só o winedevice reabriu).
- Achado colateral: journal 19:59:48 "profile_rumble_policy_applied mult=0.7 policy=max" e state_full rumble_mult_applied=0.7 com policy=max — max deveria ser 1.0 (core/rumble.py:79-81); rumble do jogo está sendo multiplicado por 0.7 indevidamente.

== P2 CONTROLE DUPLICADO SEM WRAPPER ==
- Evdev do físico está grabbed (primary_grab_state=held) → duplicação NÃO vem de evdev; vem de hidraw: sem wrapper não há IGNORE, o SDL do jogo vê físico 0ce6 (hidraw3, ACL uaccess) + vpad 0df2 (hidraw6); e com Proton o winedevice expõe AINDA os dois por outro caminho. Com 2 vpads sempre-Edge ativos (P1+P2), 1 físico branco vira potencialmente 3 entidades → "aparece player 1 mas mexe player 3 e 4" fecha com vpadP1(forward)+físico ambos visíveis.

== P3 NUMERAÇÃO 8BITDO (LED 2/4 vs jogo player 1) ==
- O inventário externo do daemon (ipc_handlers._external_inventory:156-193 + evdev_reader.discover_external_gamepads:243-311) devolve HOJE só o Nintendo USB com player_slot=3 (ds_count=2 + index 0 + 1). O 8BitDo BT está FORA da lista porque o kernel o matou (ver abaixo) — antes disso seria slot 4 (event6 < event266 na ordem de enumeração). Estado real dos LEDs: Nintendo USB green:player-1..3=1, player-4=0 (padrão "player 3" ); 8BitDo BT green:player-1..4 TODOS =1 (padrão "player 4", não 2!).
- Causa-raiz da dessincronia: o player_slot do daemon é derivado de ds_count+ordem-evdev e escrito no LED, mas o JOGO numera pela ordem de enumeração da SDL/Steam/wine (vpads 0df2 + externos + duplicatas hidraw) — não existe nenhum acoplamento entre as duas numerações. Com IGNORE ativo os físicos Sony somem do SDL mas o 8BitDo (057e) fica, e a ordem do jogo fica: 8BitDo=1 (BT conectou antes dos vpads renascerem às 20:15) etc.
- BÔNUS/CAUSA DE MORTE AO VIVO: dmesg 20:23:09+ = storm `nintendo 0005:057E:2009.000C: joycon_enforce_subcmd_rate: exceeded max attempts` (contínuo, vários/seg) e o input50/event266 foi DESREGISTRADO. Suspeito forte: _external_inventory chama apply_player_number(hidraw, slot) (ipc_handlers.py:188-192 → core/external_leds.py:write_player_number) EM TODA chamada de controller.list{external:true}; a GUI (aberta 20:15) faz esse fetch com TTL de só 4s (app/actions/status_actions.py:446) e cada fetch escreve 4-5 nós de LED sem comparar estado → 1+ subcomando BT/s no firmware clone do 8BitDo → estouro do rate-limit do hid-nintendo → driver desiste (a morte começou 8min após a GUI abrir). ATENÇÃO: meu próprio probe IPC de diagnóstico também disparou 1 escrita dessas (inevitável — a leitura tem efeito colateral de escrita, o que por si já é um bug de design).

== P4 FICHA DO EXTERNO SEM DROPDOWN ==
- O dropdown NUNCA foi construído: 9532eb1 entregou DETECÇÃO+ORIENTAÇÃO textual. app/actions/external_controllers.py:216-244 (mode_guidance) devolve texto; app/gui_dialogs.py:294-306 renderiza como Gtk.Label read-only (+ linha "O jogo vê como" via detail_rows:253). Racional no código: o modo Nintendo/Xbox é modo de HARDWARE (combo ao ligar), não togglável por software. A GUI roda o código atual do branch (venv), então não é versão velha — é expectativa vs design. Gap real possível: um seletor de COMO O JOGO VÊ (renomear/re-expor via vpad ou xpad) não existe.

== P5 DIÁLOGO DO WRAPPER CLARO ==
- O diálogo é Gtk.MessageDialog criado em app/actions/launch_wrapper_dialog.py:357-386, toplevel separado. O tema Drácula é aplicado por CSS com classe .hefesto-dualsense4unix-window adicionada SÓ à janela principal (app/theme.py:22-64; app/app.py:192) — o MessageDialog não ganha a classe, e o fallback gtk-application-prefer-dark-theme não cobre o tema claro do COSMIC → fundo claro. Mesma família de bug atinge a ficha do externo (gui_dialogs também não aplica a classe).

== P6 MOTION SENSORS VIRAM jsN ==
- Confirmado: js2 (accel do branco, input10), js4 (accel vpad P1, input65), js6 (accel roxo, input44), js8 (accel vpad P2, input69) — todos ID_INPUT_ACCELEROMETER=1, criados pelo joydev do kernel. Cada vpad uhid replica o descriptor completo do DualSense → hid_playstation registra Motion+Touchpad também no vpad, DOBRANDO o ruído (2 vpads = +2 js de motion). SDL ignora accelerometers, mas a API js legada e o winedevice não. Mitigação clássica: regra udev que remove/marca os nós de accelerometer (ID_INPUT_JOYSTICK=0) ou joydev blacklist — não existe regra nossa para isso hoje.

== ESTADO IPC/CONFIG ==
- daemon.state_full: 2 controllers (slots 1/2, lightbar sysfs azul/vermelha), coop enabled players=2, gamepad_emulation uhid dualsense dedup_ok=true, rumble_policy=max custom_mult=0.7 applied=0.7, passthrough=true, window_detect_last_class=Main.py (a própria GUI!).
- Autoswitch flapping no journal 20:15:40-20:15:51: vitoriasackboy_nativo a cada troca de foco GUI/jogo (wm_class Main.py e Hefesto-Dualsense4Unix contam como janela) — cada flip mexe em perfil/rumble policy; com mode.kind=gamepad não recria vpad, mas é ruído constante.
- 20:15:11.463 warning evdev_read_lost EBADF em event259 durante teardown do co-op (fd fechado antes da thread parar) — bug menor de teardown.
- controllers.json: {<MAC-DS-BRANCO>:1, <MAC-DS-ROXO>:2}; profiles: vitoria (any, prio 5, lightbar roxa, rumble max), sackboy_nativo (steam_app_1599660, prio 80, mode gamepad dualsense coop, suppress_desktop_emulation=true).

## key_files

- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/connection.py:20-37, 324-352 — RECONNECT_ONLINE_CHECK_INTERVAL_SEC=30.0 — o tick que dispara connect() e o reassert de cor a cada 30s (o lado 'azul' da alternância da lightbar do branco)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/backend_pydualsense.py:262-296, 306-335, 660-790, 854-1010, 1240-1310 — report_thread com keepalive 0.5s que zera rumble escrito por terceiros no hidraw USB; prepareReport limpa flags de LED (_suppress_leds); connect() chama reassert_resolved_outputs() em todo tick; _refresh_sysfs_leds/priming; set_rumble/_write_partial_output
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/subsystems/gamepad.py:159-208, 371-389, 598-627 — apply_game_rumble (FF do vpad→físico, target por primary_uniq), make_primary_rumble_sink, dispatch_gamepad/pump_ff — o caminho que FUNCIONA e que o jogo aparentemente não usa p/ P1
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/ipc_handlers.py:156-193, 1004-1051 — _external_inventory: numeração global ds_count+index+1 E escrita de LED (apply_player_number) EM TODA leitura — efeito colateral que bombardeia o 8BitDo BT de subcomandos
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/external_leds.py:34-85 — write_player_number escreve os 4 nós green:player sem comparar estado atual — cada poll da GUI = 4-5 subcomandos BT no hid-nintendo
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/actions/status_actions.py:336-346, 444-450, 469-486 — GUI polla controller.list{external:true} com TTL de apenas 4s (linha 446) — a cadência do bombardeio de LED; abertura da ficha do externo
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/evdev_reader.py:243-311 — discover_external_gamepads — filtro/dedup do inventário (o 8BitDo sumiu daqui porque o kernel desregistrou o input dele)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/subsystems/identity.py:391-431 — make_auto_output_provider — cor automática por slot (azul 0,0,153 no branco) que VENCE a lightbar do perfil ativo
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/actions/launch_wrapper_dialog.py:357-393 — Gtk.MessageDialog do wrapper sem a classe CSS do tema — P5 (fundo claro)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/theme.py:22-64 — apply_theme escopa o Drácula à .hefesto-dualsense4unix-window (só a janela principal recebe a classe em app/app.py:192)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/actions/external_controllers.py:197-244 — input_mode/mode_guidance — o 'modo Xbox/Nintendo' é texto de orientação, não dropdown (P4)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/gui_dialogs.py:260-323 — renderização da ficha do externo (labels read-only; também sem tema escuro)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/integrations/uhid_gamepad.py:127-149, 505-527, 811-890 — gate de valid_flags do rumble no report 0x02, contadores ff_play_count/output_count POR VPAD (não expostos no IPC — necessários p/ fechar o split dos 19 plays)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/launch_env.py:262-306 — materialização do PROTON_ENABLE_HIDRAW=1 + IGNORE — o par que abre o caminho winedevice→físico que o IGNORE não filtra
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/rumble.py:51-91 — _effective_mult: policy max deveria dar 1.0, mas o vivo mostra mult=0.7 aplicado com policy=max (journal 19:59:48 + state_full)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/subsystems/coop.py:202-330, 469-520 — ciclo de spawn/teardown dos vpads do co-op (recriação mid-game às 20:15 invalidou handles do jogo; evdev_read_lost EBADF no teardown)

## hypotheses

- P1-lightbar (CONFIRMADO nos dois lados): race entre (a) o jogo/winedevice escrevendo lightbar no hidraw3 do branco (PROTON_ENABLE_HIDRAW=1 expõe o físico apesar do SDL IGNORE; verde = cor de player-index do 3º/4º controle) e (b) o reassert de cor do daemon a cada 30s — reconnect_loop (connection.py:30,339) → connect() → reassert_resolved_outputs() (backend_pydualsense.py:774-790) → sysfs azul slot-1 (0,0,153; mtime do nó avança exatamente a cada 30s). Cura provável: parar de reassertar quando um jogo está com o hidraw do físico aberto, e/ou tirar o físico do alcance do Proton (hidraw perm) quando o vpad está ativo.
- P1-rumble (mais provável): o jogo manda FF ao FÍSICO branco pelo caminho Proton-hidraw (winedevice tem hidraw3 aberto) e o report_thread do daemon o ZERA em ≤0.5s (OUT_REPORT_KEEPALIVE_SEC=0.5 com motores=0 em passthrough, backend_pydualsense.py:262-296) — mesma física do bug curado no Modo Nativo pelo output-mute, que aqui não está ativo. Alternativa/combinada: o jogo perdeu o handle do vpad P1 quando os vpads foram recriados mid-game às 20:15:28 (Steam não tem hidraw6 aberto), então o FF de P1 nunca chega ao vpad; os 19 plays agregados seriam todos do vpad P2 (roxo vibra). Colateral certo: policy=max aplica mult 0.7 (deveria ser 1.0, core/rumble.py:79).
- P2-duplicado (quase certo): sem wrapper não há IGNORE e o SDL do jogo vê o físico 0ce6 (hidraw3, ACL uaccess) + vpad P1 (hidraw6) + vpad P2; com Proton o winedevice expõe físicos e vpads por um segundo caminho que o IGNORE nunca filtra. Grab de evdev só esconde o evdev — hidraw é o vazamento. 1 físico = até 3 entidades visíveis → 'aparece player 1 mas mexe 3 e 4'.
- P3-numeração (estrutural): player_slot do daemon = ds_count + ordem de enumeração evdev (ipc_handlers.py:156-193) e é gravado no LED, mas o jogo numera pela ordem de enumeração SDL/Steam/wine — zero acoplamento. O 8BitDo estava numerado slot 4 (4 LEDs verdes acesos agora), não 2. Sincronizar exigiria ou adotar os externos em vpads (ordem controlada) ou desistir de prometer paridade LED=jogo.
- P3-morte-do-8BitDo (forte, ao vivo): a escrita de LED em TODA listagem de externos (apply_player_number chamado dentro de _external_inventory, com a GUI pollando a cada 4s desde 20:15) mandou subcomandos BT contínuos ao firmware clone → dmesg 20:23:09+ 'joycon_enforce_subcmd_rate: exceeded max attempts' em loop → hid-nintendo desregistrou o input (event266 sumiu). O 'muro do kernel' conhecido é AGRAVADO por nós: leitura de inventário tem efeito colateral de escrita.
- P4-ficha (fato, não bug de runtime): o seletor de modo nunca foi um dropdown — mode_guidance (external_controllers.py:216) + label read-only (gui_dialogs.py:294-306); modo é de hardware. O gap é de produto/UX (expectativa de um controle interativo).
- P5-tema (confirmado por código): CSS Drácula escopado à classe .hefesto-dualsense4unix-window aplicada só à janela principal (theme.py:64, app.py:192); Gtk.MessageDialog do wrapper (launch_wrapper_dialog.py:365) nasce sem a classe e herda o claro do sistema.
- P6-motion-js (confirmado): joydev cria jsN para os nós de accelerometer do hid_playstation, e cada vpad uhid replica Motion/Touchpad do descriptor — 2 vpads dobram o ruído (js2/js4/js6/js8 hoje). Falta regra udev/flag que tire ID_INPUT_JOYSTICK dos accelerometers ou descriptor sintético do vpad sem a coleção de motion.
- Transversal: autoswitch trata a própria GUI (wm_class Main.py/Hefesto-Dualsense4Unix) como janela de jogo e flipa perfis vitoriasackboy_nativo a cada mudança de foco (journal 20:15:40-51) — ruído de estado durante gameplay; e o toggle manual de emulação recria os vpads mid-game, invalidando handles do jogo (o que a Steam mostra: hidraw6 não está mais aberto por ela).

## open_questions

- Split dos 19 rumble_ff.plays entre vpad P1 e vpad P2: os contadores por-vpad (uhid_gamepad.ff_play_count) existem mas não são expostos no IPC — expor por player fecharia em definitivo se o jogo mandou ALGUM FF ao vpad P1.
- Quem exatamente escreve o verde-limão no hidraw3 (winedevice/jogo vs steam client): exigiria sniff de hidraw (usbmon/tracepoint) que não cabia no read-only; a inferência (PSSupport=0 + winedevice com hidraw3 aberto + IGNORE só cobre SDL) é forte mas não é captura direta.
- Por que policy=max aplica mult 0.7 (profile_rumble_policy_applied mult=0.7 policy=max às 19:59:48; state_full rumble_mult_applied=0.7): rastrear quem propaga custom_mult por cima do RUMBLE_POLICY_MULT no profiles/manager e no ipc_rumble_policy.
- A user relatou LED 'player 2' no 8BitDo, mas o sysfs mostra o padrão de player 4 (4 LEDs verdes acesos): a numeração que ela viu era de um momento anterior (ds_count menor) ou o firmware clone exibe diferente? Se o daemon renumera conforme controles chegam, o LED do 8BitDo muda ao longo da noite — instabilidade percebida como 'errado'.
- O storm joycon_enforce_subcmd_rate começou às 20:23:09 (GUI aberta 20:15, poll 4s): correlação forte mas não prova — reproduzir com a GUI fechada (nenhuma escrita de LED) diria se o hid-nintendo sobrevive mais tempo com o 8BitDo em BT.
- O jogo (Sackboy) estava rodando via wrapper nesta sessão? O env do processo do jogo (se ainda vivo) confirmaria se o IGNORE estava na env efetiva — não inspecionei o env do processo do jogo (winedevice 7347 é o device manager, não o jogo).
- Vpads recriados às 20:15 mid-game: a Steam nunca reabriu o hidraw6 (vpad P1) — verificar se o SDL da Steam re-enumera uhid recriado com o MESMO MAC 02:fe:00:00:00:01 ou se ignora por identidade repetida.
