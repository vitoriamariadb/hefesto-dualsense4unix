# Agente: v2:0a5bfb92eb5795642eda6c5f5c3decbccf37667c7328e12b29a1d17e9152e521

## report

MAPA INSTALL/UNINSTALL + UDEV — repo vs máquina (2026-07-18, ~20h30)

== 1. REGRAS UDEV DO REPO (11 arquivos em assets/) ==
Canônicas (instaladas pelo scripts/install_udev.sh, SEM flag): 70-ps5-controller (hidraw DualSense USB/BT, MODE 0660 + uaccess), 71-uhid (uaccess /dev/uhid), 71-uinput (uaccess /dev/uinput), 72-ps5-controller-autosuspend (power/control=on), 76-dualsense-touchpad-libinput-ignore, 77-dualsense-leds (lightbar/player-LED sysfs), 78-dualsense-motion-not-joystick, 79-external-controller-leds (LEDs Nintendo/8BitDo + lightbar DS4 via DRIVERS=="playstation").
Fora do conjunto: 73-ps5-controller-hotplug + 74-...-bt = DESCONTINUADAS (install_udev.sh faz `sudo rm -f` delas ~linhas 74-78; install.sh:524-527 as pula) — os assets seguem no repo como arquivos mortos; 75-ps5-controller-disable-usb-audio = OPT-IN (`install_udev.sh --disable-usb-audio`), alternativa mutuamente exclusiva ao quirk snd_usb_audio.

== 2. DIFF REPO vs INSTALADO ==
As 8 canônicas estão em /etc/udev/rules.d/ BYTE-IDÊNTICAS ao repo (diff real rodado; mtime 2026-07-18 01:37 = último ciclo install). /etc/modules-load.d/hefesto-dualsense4unix.conf idêntico (uinput+uhid). 73/74/75 corretamente AUSENTES. ZERO drift de regras.
Units systemd --user: hefesto-dualsense4unix.service (enabled), hefesto-steam-input-guard.path/.timer (enabled) + .service (com __SCRIPT__ resolvido p/ o repo — idêntico ao template), hefesto-dualsense4unix-storm-watch.service (enabled) — TODAS idênticas aos assets. Unit gui-hotplug NÃO instalada (prompt default n — consistente com 73/74 removidas).
Wrapper ~/.local/share/hefesto-dualsense4unix/bin/hefesto-launch idêntico a assets/hefesto-launch.sh (asset inalterado desde f2e2aad). Applet /usr/local/bin/hefesto-dualsense4unix-applet presente (23 MB, jul 17 02:08). WirePlumber drop-in 51 presente (~/.config/wireplumber/wireplumber.conf.d/51-hefesto-dualsense-no-default-source.conf); 52/53 ausentes (esperado).
Quirks: /etc/modprobe.d/hefesto-dualsense-storm.conf presente E ativo no runtime (/sys/module/snd_usb_audio/parameters/quirk_flags = 054c:0ce6:ignore_ctl_error|ctl_msg_delay_1m,054c:0df2:...). Cmdline TEM usbcore.quirks=054c:0ce6:gn,054c:0df2:gn (via Aurora, não pelo install — o 3b opt-in nem foi preciso).
launch_env vivo (~/.local/state/hefesto-dualsense4unix/launch_env/): default.env + steam_app_1599660.env regravados 20:15 com PROTON_ENABLE_HIDRAW=1 + SDL_GAMECONTROLLER_IGNORE_DEVICES=0x054c/0x0ce6 + __GL_SHADER_DISK_CACHE*; estado "native=False emulacao=True mascara=dualsense backends=['uhid','uhid']" (DOIS vpads: P1 e P2).
VEREDITO: instalação 100% em sincronia com o repo. Nenhum passo do install está dessincronizado na máquina agora.

== 3. uaccess / ACL / MODE (a mecânica real) ==
/usr/lib/udev/rules.d/73-seat-late.rules:16 (`TAG=="uaccess" ... RUN{builtin}+="uaccess"`) é quem vira TAG em ACL — só regras numeradas <73 pegam ACL. Nossas 70/71/71/72 estão OK: /dev/uhid e /dev/uinput = 0660 root:root + ACL user:vitoriamaria:rw (getfacl confirmado). hidraw3 (DualSense branco USB), hidraw7 (roxo BT), hidraw2/9 (Pro Controller USB/BT) = 0660 + ACL.
Regras 77 e 79 são numeradas >73 → o TAG+="uaccess" delas é MORTO (nunca vira ACL); quem funciona de fato é o fallback RUN+=/bin/chmod 0666 — confirmado ao vivo: todos os nós de LED (input9/43/64/68 rgb:indicator + white:player-N do DualSense; 0003:057E:2009.0003 e 0005:057E:2009.000C green:player-N/blue:player-5 do Nintendo USB e do 8BitDo BT) estão -rw-rw-rw-. Ou seja: a 79 ESTÁ funcionando (LED write do daemon provado), mas por chmod, não por ACL. Nota: 0666 nesses nós de LED significa que QUALQUER processo pode escrever a lightbar (múltiplos escritores possíveis — evidência para a hipótese de race do P1).
LACUNA da regra 70: o hidraw do PRÓPRIO vpad (hidraw6/8, HID 0003:054C:0DF2 sob /devices/virtual/misc/uhid) NÃO casa nenhuma linha nossa (ATTRS{idVendor} sobe até USB — o vpad não tem pai USB; KERNELS=="0005:..." só cobre BT). O acesso do jogo ao hidraw do vpad vem do pacote de TERCEIRO steam-devices: /usr/lib/udev/rules.d/71-sony-controllers.rules, linha `KERNEL=="hidraw*", KERNELS=="*054C:0DF2*", MODE="0660", TAG+="uaccess"` (casa o nome do pai HID "0003:054C:0DF2.000D", e <73 → ACL, confirmado). Numa distro SEM steam-devices, o hidraw do vpad ficaria inacessível → sem rumble na máscara PS. Fix barato: acrescentar `KERNELS=="0003:054C:0DF2.*"` (ou match por DRIVERS=="playstation") na 70.

== 4. hidraw 0666 (pergunta específica) ==
hidraw0/1 = Compx 2.4G Wireless Receiver (25a7:fa07, dongle de mouse) e hidraw4/5 = BY Tech Gaming Keyboard (258a:010c) estão crw-rw-rw- SEM ACL. A origem do 666 é /etc/udev/rules.d/60-openrgb.rules linha 10: `KERNEL=="hidraw*", MODE="0666"` — regra COBERTORA para TODOS os hidraw. Cabeçalho do arquivo: "Fonte-de-verdade: ~/.config/zsh/scripts/60-openrgb.rules ... mantido pelo ritual-aurora-self-heal" → é TERCEIRO (toolchain pessoal Aurora/OpenRGB), NÃO é do hefesto. Os controles escapam do 666 porque regras posteriores reatribuem MODE (última atribuição vence): nossa 70 (0660+uaccess) e as steam-devices 71-sony/71-nintendo (0660+uaccess). Falha de segurança: SIM, é falha — hidraw world-writable num receiver de teclado/mouse permite a qualquer processo local ler/injetar reports HID crus (superfície de keystroke-injection) — mas é falha do 60-openrgb.rules (Aurora), não nossa; o upstream do OpenRGB recomenda regras por VID/PID, não blanket. Nossas regras não pioram nem curam isso (só blindam os controles). Também de terceiros/Aurora: 99-usb-kill-autosuspend.rules e 99-storage-no-link-pm.rules ("Ritual da Aurora v3.3").

== 5. O QUE O INSTALL FAZ (mapa por passo, install.sh) ==
Flags: linhas 108-131. Passo 3 udev (501-563, via scripts/install_udev.sh; lista canônica derivada de assets com skip 73/74/75 em 524-527; triggers pós-reload incl. --subsystem-match=misc p/ uhid/uinput e leds --action=add). 3b quirk cmdline usbcore (566-583, OPT-IN --with-usb-quirk). 3c quirk snd_usb_audio (585-612, DEFAULT, com post-check do .conf). 4 ícone/.desktop/launcher + wlrctl/XWayland em COSMIC (615-755). 4b glyphs; 4b-2 WRAPPER hefesto-launch (763-784, DEFAULT sem flag, sem sudo, + mkdir launch_env). 4c perfis; 4d i18n. 5 symlink ~/.local/bin. 6 unit do daemon via CLI `daemon install-service` (820-853, autostart = prompt default y). 7 hotplug-gui (854-894, prompt default n). 7b storm-watch (896-928, --with-storm-watch OU --yes). 8 AppIndicator GNOME. 9 applet COSMIC (963-1002, DEFAULT-ON em COSMIC, opt-out --no-cosmic-applet). 10 WirePlumber drop-in 51 (1003-1024, DEFAULT, opt-out --keep-dualsense-mic). 11 Steam Input PSSupport off + GUARD path/timer (1025-1064, DEFAULT, opt-out --keep-steam-input). 11b migração das Launch Options venenosas → wrapper (1066-1092, DEFAULT; só toca linhas com assinatura nossa).
UNINSTALL (uninstall.sh): simétrico — remove por default as 11 regras (incl. 73/74/75 se existirem) + modprobe storm conf (linhas 305-335), guard (189-193), storm-watch (175-186), applet (241-252), drop-ins WP 51/52/53 (253-283), glyphs/i18n; --keep-udev preserva (78).

== 6. REGRA DE OURO "install SEM FLAGS" — o que ainda exige flag ==
Nenhum fix RECENTE (ondas UHID/DEDUP/8BIT-02/lightbar-reset-0x08) exige flag: regra 79, wrapper, migração 11b, snd quirk, WP fix, Steam-Input-off, guard e applet (em COSMIC) são todos DEFAULT. Continuam gated (por decisão documentada, não esquecimento): (a) regra 75 áudio-off → install_udev.sh --disable-usb-audio (alternativa ao 3c; corretamente ausente na máquina); (b) quirk de cmdline → --with-usb-quirk (a máquina já o tem via Aurora); (c) storm-watch → --with-storm-watch ou --yes (está enabled na máquina); (d) hotplug-gui → prompt default n / --enable-hotplug-gui; (e) autostart → prompt default y / --enable-autostart; (f) XWayland → prompt em COSMIC / --force-xwayland. Armadilha menor: install_udev.sh PRESERVA uma 75 pré-existente mesmo sem flag (linhas 84-88) — não afeta a máquina (75 ausente).

== 7. EVIDÊNCIAS PARA OS PROBLEMAS DO DIA (domínio install/udev) ==
P6 (Motion viram jsN): mapa vivo: js0=Pro Controller USB; js1=DualSense branco USB; js2=Motion dele; js3=vpad P1; js4=Motion do vpad P1; js5=DualSense roxo BT (nome "DualSense Wireless Controller" SEM prefixo Sony!); js6=Motion dele; js7=vpad P2; js8=Motion do vpad P2. A regra 78 casa por ATTRS{name} apenas os nomes USB oficiais ("Sony Interactive Entertainment DualSense [Edge] Wireless Controller Motion Sensors") — NÃO casa o BT (sem prefixo) nem os vpads ("Hefesto Virtual DualSense PN Motion Sensors"). PORÉM, ao vivo os 4 nós de Motion (event9/260/27/263) já têm ID_INPUT_ACCELEROMETER=1 e NENHUM ID_INPUT_JOYSTICK — o kernel hid-playstation seta INPUT_PROP_ACCELEROMETER e o builtin input_id do udev classifica sozinho (a 78 é hoje redundante nesse kernel). O que a 78 NUNCA vai curar: os nós /dev/input/js2/4/6/8 são criados pelo joydev do KERNEL independente de ENV udev — só jogos que enumeram /dev/input/js* cru (API joystick legada) os veem; SDL2 (caminho udev) os ignora. Cura possível: regra jsN MODE 0000/remoção de symlink para inputs aceleômetro, não ENV.
P2 (duplicado sem wrapper): o passo 11b só MIGRA linhas venenosas antigas; NÃO existe passo que insira `hefesto-launch %command%` em jogos novos/sem opção — e o daemon autostart mantém o vpad Edge sempre vivo → jogo lançado sem wrapper vê físico js1 + vpad js3 (ambos ID_INPUT_JOYSTICK). É o comportamento esperado do design atual (o popup "SIM 1x/jogo", task #7 da onda, nunca foi construído).
P1 (lightbar verde-limãoazul no branco USB): a 77 deixa a lightbar sysfs 0666 (qualquer escritor); o jogo enxerga SÓ os vpads (IGNORE_DEVICES esconde 054c/0ce6) e pinta o vpad P1 via hidraw6 (SDL default P1=azul) → chega ao daemon por UHID_OUTPUT; se o daemon repassa ao físico, briga com a cor-de-slot que ele mesmo pinta via sysfs = alternância. Race é de código do daemon, não de regra; udev só habilita os dois caminhos. Perms do hidraw3 (0660+ACL) não bloqueiam rumble — a falha de vibração do branco é domínio do daemon (FF forward do vpad P1), não de permissão.
P3 (8BitDo player 2 no LED, player 1 no jogo): LEDs 0005:057E:2009.000C green:player-* graváveis (0666, regra 79 OK). Numeração jogo=ordem SDL/Steam ≠ slot global do daemon — domínio daemon/SDL, sem papel do udev. Obs: durante a varredura o 8BitDo BT não tinha jsN (idle/desconectado?).

## key_files

- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/scripts/install_udev.sh:27-117 — Conjunto canônico das 8 regras + rm das 73/74 descontinuadas + 75 opt-in (--disable-usb-audio) + modprobe uinput/uhid + triggers (misc/leds/input)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/install.sh:108-131, 501-563, 566-612, 763-784, 820-928, 963-1092 — Flags; passo udev; quirks 3b/3c; wrapper hefesto-launch (default); units daemon/hotplug/storm; applet; WirePlumber; Steam-Input off + guard; migração 11b das Launch Options
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/uninstall.sh:78, 175-193, 241-283, 305-339 — Simetria: remove todas as regras (incl. 73/74/75) + storm conf + guard + storm-watch + applet + drop-ins WP; --keep-udev
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/assets/78-dualsense-motion-not-joystick.rules:14-15 — P6: match por ATTRS{name} NÃO cobre BT ('DualSense Wireless Controller Motion Sensors', sem prefixo Sony) nem vpads ('Hefesto Virtual DualSense PN Motion Sensors'); hoje mascarado pelo input_id (INPUT_PROP_ACCELEROMETER), mas js2/4/6/8 são joydev do kernel e ENV udev não os remove
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/assets/70-ps5-controller.rules:6-13 — LACUNA: não casa o hidraw do vpad uhid (0003:054C:0DF2 sem pai USB); acesso do jogo depende do steam-devices de terceiro (71-sony-controllers.rules KERNELS *054C:0DF2*)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/assets/77-dualsense-leds.rules:27-28 — TAG uaccess morto (>73); o que vale é o RUN chmod 0666 — lightbar sysfs world-writable = múltiplos escritores possíveis (evidência p/ race do P1)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/assets/79-external-controller-leds.rules:31-36 — LEDs Nintendo/8BitDo + lightbar DS4 (DRIVERS playstation); funcionando ao vivo via chmod 0666 (uaccess >73 também morto aqui)
- /etc/udev/rules.d/60-openrgb.rules:10 — TERCEIRO (Aurora self-heal/OpenRGB): KERNEL==hidraw* MODE=0666 cobertor — é ELE quem deixa hidraw0/1 (mouse Compx 25a7:fa07) e hidraw4/5 (teclado BY Tech 258a:010c) em 666; falha de segurança do Aurora, não do hefesto
- /usr/lib/udev/rules.d/71-sony-controllers.rules — steam-devices (terceiro): é quem dá ACL/0660 ao hidraw6/8 do vpad Edge (KERNELS *054C:0DF2*) — dependência não declarada da nossa regra 70
- /usr/lib/udev/rules.d/73-seat-late.rules:16 — Mecanismo TAG uaccess→ACL; só regras <73 pegam ACL (confirma /dev/uhid+/dev/uinput OK e uaccess morto em 77/79)
- /home/vitoriamaria/.local/state/hefesto-dualsense4unix/launch_env/default.env — Env vivo do wrapper (20:15): IGNORE_DEVICES=0x054c/0x0ce6 esconde AMBOS os físicos; backends=['uhid','uhid'] = vpads P1+P2 ativos
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/assets/73-ps5-controller-hotplug.rules — Asset MORTO no repo (install_udev.sh o remove da máquina desde 2026-06-23); candidato a deleção junto com 74
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/scripts/install-host-udev.sh:80-101 — Paridade Flatpak/.deb do mesmo conjunto canônico (com o mesmo comentário da regra <73)

## hypotheses

- P6: os js2/js4/js6/js8 dos Motion Sensors são nós do joydev (kernel) que NENHUMA env de udev remove; a regra 78 é hoje redundante (input_id já marca ID_INPUT_ACCELEROMETER via INPUT_PROP do hid-playstation) e ainda por cima seu match por nome falha para BT (nome sem prefixo 'Sony Interactive Entertainment') e para os vpads ('Hefesto Virtual DualSense PN Motion Sensors') — o ruído restante afeta só jogos que enumeram /dev/input/js* cru; cura seria regra em js* (MODE 0000) para inputs-acelerômetro, não ENV. Evidência: udevadm de event9/260/27/263 = ACCELEROMETER=1 sem JOYSTICK em todos.
- P2: comportamento esperado do design atual do install — o passo 11b só migra Launch Options já envenenadas (assinatura nossa) e NÃO injeta 'hefesto-launch %command%' em jogos novos; com o daemon autostart (vpad sempre-Edge vivo), jogo sem wrapper vê físico js1 + vpad js3 = duplicado. O popup por-jogo (task #7 da onda) nunca foi construído. Evidência: install.sh:1066-1092 + steam_launch_options.py só-migração + daemon enabled.
- P1 (lightbar): race de dois escritores habilitada pelas permissões — a 77 deixa a lightbar sysfs 0666 e o jogo pinta o vpad P1 (SDL P1=azul) via hidraw6→UHID_OUTPUT, que o daemon possivelmente repassa ao físico, brigando com a cor-de-slot que o próprio daemon escreve via sysfs; alternância verde-limãoazul bate com isso. Não há drift de instalação envolvido; a correção é no código do daemon (arbitragem de escritor de LED). O rumble ausente no branco USB tampouco é permissão (hidraw3 = 0660+ACL ok) — é o forward de FF do vpad P1 no daemon.
- hidraw 666: hidraw0/1 (mouse Compx) e hidraw4/5 (teclado BY Tech) estão 0666 por causa do 60-openrgb.rules linha 10 (blanket hidraw), arquivo de TERCEIRO mantido pelo ritual-aurora-self-heal — falha de segurança do Aurora/OpenRGB (injeção/leitura HID por qualquer processo local), NÃO do hefesto; os controles escapam porque regras posteriores (70 nossa, 71-sony/71-nintendo do steam-devices) reatribuem MODE 0660+uaccess.
- Instalação SEM drift: as 8 regras canônicas, modules-load, units (daemon/guard/storm-watch), wrapper, applet, drop-in WP 51 e quirk snd estão byte-idênticos ao repo e ativos; 73/74/75 corretamente ausentes; nenhuma correção recente exige flag (75/usb-quirk/storm-watch/hotplug-gui são opt-in por decisão documentada). Riscos latentes de portabilidade: regra 70 não cobre o hidraw do vpad uhid (hoje coberto pelo steam-devices) e o TAG uaccess das 77/79 é no-op (>73, vive do chmod 0666).

## open_questions

- A regra 70 deve ganhar match próprio para o hidraw do vpad uhid (ex.: KERNELS=="0003:054C:0DF2.*" ou DRIVERS=="playstation") para não depender do pacote steam-devices em outras distros?
- P6: adotar regra js* (MODE 0000 ou GROUP restrito) para nós joydev de inputs-acelerômetro — quais jogos da mantenedora usam a API js legada e seriam de fato afetados? E a 78 deve ser estendida (nomes BT/vpad) ou aposentada como redundante?
- O daemon repassa escrituras de lightbar recebidas por UHID_OUTPUT do vpad para o controle físico? (segundo escritor da race do P1 — fora do meu escopo de install, precisa confirmação no código do daemon/vpad)
- O 8BitDo BT (modo Switch) não tinha nó jsN durante a varredura (só os LEDs 0005:057E:2009.000C existiam) — dormiu por idle do hid-nintendo ou desconectou? Afeta a reprodutibilidade do P3.
- Vale remover do repo os assets mortos 73/74 (descontinuados desde 2026-06-23) para eliminar o skip-list em install.sh:524-527 e o rm compensatório no install_udev.sh?
