# Agente: v2:388ecd5e1ecd7b52f9d8f3351b216b4031d68ed8505e0f19eee5edfee4d081c3

## report

MAPA DA SUÍTE E DOS GATES (verificado AO VIVO 2026-07-18; suíte rodada com o daemon de produção ativo e ele SOBREVIVEU — tripwire GATE-STALE-TEST-01 funcionando).

== 1. GATE COMPLETO LOCAL (comandos exatos, todos executados hoje) ==
cd /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix
1) .venv/bin/ruff check src/ tests/                        -> "All checks passed!" (ruff 0.15.20 no .venv, pin em pyproject.toml linhas ~40-46, CHORE-CI-PIN-RUFF-01; 2 warnings benignos de noqa em tests/unit/test_validar_acentuacao_glyphs_defense.py:286-287)
2) .venv/bin/python -m mypy src/hefesto_dualsense4unix     -> "Success: no issues found in 141 source files" (strict=true em pyproject [tool.mypy]; escopo = src apenas, tests NÃO passam por mypy)
3) .venv/bin/python -m pytest tests/ -q -rs                -> 3136 passed, 0 failed, 0 SKIPPED em 165s (2m45s). Config: pyproject [tool.pytest.ini_options] asyncio_mode="auto", testpaths=["tests"]. O gate correto é a suíte INTEIRA (tests/, não tests/unit) CONTANDO skips: 0 skips com GTK real é o esperado — qualquer skip local = red flag (regra dos 22 skips falsos).
4) bash scripts/check_anonymity.sh                         -> "OK: anonimato preservado."
5) .venv/bin/python scripts/check_version_consistency.py   -> "OK: 6 alvo(s) versionado(s) em 3.14.0."
6) .venv/bin/python scripts/validar-acentuacao.py --all    -> HOJE EXIT 1 com 45 violações, TODAS pré-existentes no main (provado via git show main: — requirements.txt:1, tests/unit/test_xdg_paths.py:48/68/77, test_profile_manager.py:148/160, test_applet_paridade_modo.py:58, test_coop_default_on_migration.py:135, test_mode_transition_um_dono.py:244 + ~16 em docs/process/sprints versionados). NÃO tratar como gate duro local; regra prática (memória do projeto): linhas TOCADAS limpas.
7) opcional (réplica do runtime-smoke da CI, socket IPC isolado = seguro com daemon vivo): ./run.sh --smoke && ./run.sh --smoke --bt
Venv: usar .venv (Python 3.12.3). Existe também um venv/ legado na raiz — run.sh usa .venv. NUNCA ./run.sh --daemon --force (mata o daemon de produção) e nunca pkill -f com pattern que casa o próprio shell.

== 2. CI (.github/workflows/ci.yml) ==
Jobs: anonymity (scripts/check_anonymity.sh) · acentuacao (validar-acentuacao.py --all — ver questão aberta) · version-check (scripts/check_version_consistency.py) · version-sync (__init__.py vs pyproject) · lint-test (matriz py 3.10/3.11/3.12: `ruff check src/ tests/` + `pytest tests/unit -v --cov`) · typecheck (`mypy src/hefesto_dualsense4unix`) · runtime-smoke (run.sh --smoke usb/bt × gnome/cosmic + assert de backend + grep anti-Traceback no log) · build-wheel + smoke-multi-distro (debian-12 gate duro; fedora-40/arch experimental continue-on-error).
Triggers: push em MAIN, PR e workflow_dispatch — push em sprint/* NÃO roda CI; no fork não auto-dispara (commit 2dc384e). release.yml roda: check_anonymity + `ruff check src/ tests/` + `pytest tests/unit -v` num ubuntu-latest SEM PyGObject/pycairo (só pip install -e ".[dev]" + libhidapi/libudev/libxi) — ESTE é o ambiente headless que custou 4 rounds de release.
IMPORTANTE: CI roda só tests/unit — tests/core/test_sysfs_leds.py (rota sysfs da lightbar!) roda APENAS no gate local `pytest tests/`.

== 3. OS 22 SKIPS FALSOS ==
História: o gate "2410 verde" tinha 22 skips falsos que esconderam 1 vermelho (docs/process/sprints/2026-07-16-sprint-autoswitch-e-launch-options.md:205-206 e 432-433). CURADOS no commit bc68718 (Fase 1, "hermeticidade da suíte: tripwire no quit_app, 22 skips falsos curados"). Causa do mascaramento: GATE-SKIP-MASK-01 — stubs de gi em sys.modules mutavam o gi REAL (sobrescreviam GLib.idle_add/require_version) e faziam testes de GUI skipar como "sem GTK" (explicado em tests/unit/test_rumble_actions.py:22-27). Regra vigente (2026-07-16-sprint-vpad-sempre-edge.md:362): "gate completo verde CONTANDO os skips: nenhum teste novo pode skipar em CI" — e localmente com GTK real o baseline é 0 skips (confirmado hoje: 3136/0/0).
Aliada: test_quit_app_stops_daemon.py já MATOU o daemon da máquina dela numa auditoria; hoje tem fixture autouse _isola_runtime_e_kill (linhas 28-55) com os.kill-tripwire — provado seguro hoje (daemon ativo após a suíte).

== 4. GUARDAS gi/cairo — o que a CI headless NUNCA executa (só o gate local cobre) ==
Skips de módulo inteiro (importorskip): test_input_actions.py:18 (gi, 15 testes) · test_mouse_actions_gui_sync.py:20 (gi, 15) · test_harmonia_mouse_um_dono.py:23 (gi, 22) · test_status_tinting_widgets.py:31 (cairo, 9) · test_status_cards.py:37 (cairo, 38) · test_status_buttons_glyphs.py:30 (cairo, 11) · test_input_actions_gtk.py:42 (pytestmark skipif GTK_AVAILABLE, 3).
Skips parciais: test_lightbar_auto_colors.py:400 (skipif Gdk.RGBA — 1 de 26; a CI headless tem um Gdk PARCIAL sem RGBA, ver linhas 385-397) · test_launch_wrapper_dialog.py:691 classe TestDialogoGtkReal (exige display real) e :755 TestFiacaoNoApp (exige GdkPixbuf) — de 63 testes · test_segmented_selector.py:170 (skipif _gtk_pronto) · test_quit_app_stops_daemon.py:61 + test_app_scroll_wrap.py:93 (skip se gi/GdkPixbuf ausente) · test_theme_css.py:46,52 (skip sem GTK, 10 testes) · test_button_glyph.py:89 (skip sem GTK3).
Total da faixa cega headless: ~120-140 testes de GUI.

== 5. SKIPS QUE PODEM ESCONDER REGRESSÃO NAS ÁREAS DA ONDA (P1-P6) ==
- LIGHTBAR (P1 verde-limão/azul): test_lightbar_auto_colors.py:400 (botão Apagar com Gdk.RGBA — CI não vê); tests/core/test_sysfs_leds.py FORA da CI (rota sysfs de LED); test_status_cards.py + test_status_tinting_widgets.py (tinting/cores dos cards — cairo, CI não vê). test_lightbar_reset.py (8) e test_lightbar_persist.py rodam em todo lugar (herméticos).
- WRAPPER/DIÁLOGO (P2/P5 tema claro): test_launch_wrapper_dialog.py TestDialogoGtkReal (:691) constrói o MessageDialog REAL — é exatamente onde um assert de tema/dark do diálogo viveria e a CI nunca roda; test_theme_css.py (CSS provider) idem. test_hefesto_launch_wrapper.py (8) e test_launch_env.py (18) são herméticos e rodam na CI.
- GUI da FICHA EXTERNA (P4 dropdown de modo sumido): test_external_controllers.py roda em TODO lugar (hermético, 32 testes) — classes TestModo (:163-194, input_mode/guidance Nintendo vs Xbox, "O jogo vê como") e TestFicha (:149-161, detail_rows) — nota: cobre a CAMADA PURA (inventário/strings), NÃO a construção GTK do popup; se o dropdown sumiu na GUI real, é exatamente o tipo de furo que a camada pura não pega. test_multi_controller_ui.py e test_home_controller_cards.py usam Gtk fake (rodam headless).
- NUMERAÇÃO (P3): test_external_controllers.py TestSlotGlobalDosBotoes (:205-221, slot global continua dos DualSense) + test_external_leds.py (20) + test_external_gamepads_inventory.py (10) + test_coop_player_leds.py (10) — todos herméticos; nenhum testa a ORDEM DE ENUMERAÇÃO da Steam/SDL (P3 é invalidável só ao vivo).
- RUMBLE (P1): test_subsystem_rumble.py (15), test_vpad_ff_passthrough.py (23), test_rumble_policy.py, test_rumble_persistent.py, test_rumble_mult_um_dono.py, test_led_and_rumble.py — todos herméticos, rodam na CI. Sem furo de skip aqui.

== 6. CUIDADOS PARA NÃO QUEBRAR A CI HEADLESS COM TESTES NOVOS DE GUI ==
1) NUNCA `import gi` no topo do módulo sem guarda — na CI de release (sem PyGObject) vira ERRO DE COLETA, não skip, e derruba `pytest tests/unit -v` do release.yml (a armadilha dos 4 rounds).
2) Padrão preferido: teste que RODA headless — stub de gi via `_install_gi_stubs()` copiando tests/unit/test_rumble_actions.py:22-40 (GATE-SKIP-MASK-01: se o PyGObject REAL existe, usa o real e NÃO muta com stub) ou `monkeypatch.setitem(sys.modules, "gi", fake)` + import lazy `from gi.repository import ...` DENTRO da função testada (padrão documentado em tests/unit/test_home_controller_cards.py docstring).
3) Se o teste exige GTK REAL de verdade: `pytest.importorskip("gi")` (módulo) ou skipif com probe de ATRIBUTO — a CI tem Gdk PARCIAL em sys.modules (vazamento de stubs de outros testes), então testar `hasattr(Gdk, "RGBA")`, não só o import (modelo: test_lightbar_auto_colors.py:385-397). Para widgets que desenham: `pytest.importorskip("cairo")`. Para a App inteira: probe de GdkPixbuf (test_launch_wrapper_dialog.py:738-753).
4) Regra dos 22: teste novo NÃO PODE skipar na CI — se a asserção GTK-real for essencial, espelhar com um teste stub-level que rode headless (par TestDialogoGtkReal/TestMixinCacheEAntiSpam do launch_wrapper_dialog é o modelo).
5) Gate local sempre `pytest tests/ -rs` (suíte inteira SEM filtro) e conferir "0 skipped" na linha final.
6) Markers requires_gtk/requires_display são citados no smoke-multi-distro (`-m "not requires_gtk and not requires_display"`) mas NÃO estão registrados no pyproject nem usados em teste algum — se criar markers novos, registrar em [tool.pytest.ini_options].
7) Hermeticidade: conftest.py (tests/conftest.py) já força FAKE=1 + XDG isolado + HEFESTO_DUALSENSE4UNIX_SKIP_PRESET_SEED=1; XDG_RUNTIME_DIR NÃO é isolado de propósito (single_instance) — teste novo que toque pid/socket real precisa da blindagem estilo test_quit_app_stops_daemon.py:28-55.

## key_files

- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/pyproject.toml:40-46, 96-105, 107-131, 133-135 — Pin ruff==0.15.20 (CHORE-CI-PIN-RUFF-01), config ruff (select E/F/W/I/N/UP/B/SIM/RUF, line-length 100), mypy strict escopo src, pytest asyncio_mode=auto + testpaths=[tests]
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/.github/workflows/ci.yml:13-17 (anonymity), 19-28 (acentuacao --all), 70-105 (lint-test: ruff+pytest tests/unit), 107-125 (mypy), 127-215 (runtime-smoke run.sh --smoke usb/bt x gnome/cosmic) — Definição completa da CI; só roda em push no main/PR/dispatch — sprint/* não dispara
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/.github/workflows/release.yml:39-45 — Gate do release: check_anonymity + ruff check src/ tests/ + pytest tests/unit -v em ubuntu HEADLESS sem PyGObject/pycairo — o ambiente que pega import gi sem guarda como erro de coleta (armadilha dos 4 rounds)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/scripts/check_anonymity.sh:9-28 — Regex FORBIDDEN + whitelist de paths (docs/process/**, docs/history/**, tests/fixtures/** excluídos) — rodar antes de qualquer commit da onda
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/tests/conftest.py:17-77 — Hermeticidade base da suíte: FAKE=1, XDG isolado por teste (BUG-TEST-CONFIG-LEAK-01), SKIP_PRESET_SEED=1; XDG_RUNTIME_DIR deliberadamente NÃO isolado (single_instance)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/tests/unit/test_quit_app_stops_daemon.py:10-16, 28-55 — GATE-STALE-TEST-01: fixture autouse _isola_runtime_e_kill com os.kill-tripwire — este arquivo já matou o daemon real dela; hoje provado seguro (suíte rodou com daemon vivo e ele sobreviveu)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/tests/unit/test_rumble_actions.py:22-40 — Padrão CANÔNICO para teste de GUI novo rodar headless: _install_gi_stubs com GATE-SKIP-MASK-01 (nunca mutar o gi real — a causa dos 22 skips falsos)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/tests/unit/test_lightbar_auto_colors.py:385-400 — Guarda por ATRIBUTO (hasattr Gdk.RGBA) porque a CI headless tem Gdk PARCIAL em sys.modules — modelo para guardas novas; 1 teste da área lightbar que a CI nunca roda
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/tests/unit/test_launch_wrapper_dialog.py:680-704 (TestDialogoGtkReal, exige display), 738-758 (TestFiacaoNoApp, exige GdkPixbuf) — Únicos testes que constroem o diálogo GTK REAL do wrapper (área do P5 tema claro) — CI headless nunca os executa; asserts novos de tema devem entrar aqui + espelho stub-level
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/tests/unit/test_external_controllers.py:149-161 (TestFicha), 163-194 (TestModo input_mode/guidance), 205-221 (TestSlotGlobalDosBotoes) — Cobertura hermética das áreas P3/P4 (ficha, modo Nintendo/Xbox, slot global) — camada pura apenas, não a montagem GTK do popup
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/tests/core/test_sysfs_leds.py — Roda no gate local (pytest tests/) mas NÃO na CI (que roda tests/unit) — furo de CI na área sysfs-LED/lightbar (P1)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-16-sprint-autoswitch-e-launch-options.md:205-206, 432-433 — Registro original dos 22 skips falsos + aviso do test_quit_app matando o daemon; regra: suíte completa SEM filtro contando skips
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/run.sh:60-100 — Smoke com socket IPC isolado (hefesto-dualsense4unix-smoke.sock) = seguro com daemon vivo; NUNCA --daemon --force na máquina dela
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/scripts/validar-acentuacao.py — Gate de acentuação; --all HOJE exit 1 com 45 violações pré-existentes no main — usar por-linha-tocada, não como gate duro local

## hypotheses

- A faixa cega headless (~120-140 testes de GUI guardados por gi/cairo/display) é o mecanismo pelo qual P4 (dropdown de modo sumido na ficha) e P5 (diálogo do wrapper com tema claro) podem passar CI verde: a camada GTK-real dessas telas só é exercitada no gate local com GTK — evidência: test_launch_wrapper_dialog.py:691/755 e test_theme_css.py:46-52 skipam na CI de release, e test_external_controllers.py TestModo cobre só a camada pura de strings, não a montagem do popup GTK.
- tests/core/test_sysfs_leds.py fica fora da CI (que roda apenas tests/unit) — a rota sysfs de LED/lightbar (área do P1 verde-limãoazul) tem cobertura garantida SÓ pelo gate local `pytest tests/`; um fix de lightbar validado apenas por CI teria furo.
- P3 (8BitDo LED player 2 mas jogo vê player 1) é por construção INVISÍVEL à suíte: TestSlotGlobalDosBotoes (test_external_controllers.py:205-221) valida o slot global do DAEMON, e nenhum teste modela a ordem de enumeração Steam/SDL — o gate para P3 é humano/ao vivo, não pytest.
- validar-acentuacao.py --all está VERMELHO no branch (exit 1, 45 violações todas pré-existentes no main) — se algum agente da onda tratar isso como gate duro vai travar à toa; e inversamente, ao abrir PR para main o job acentuacao da CI tende a REPROVAR por dívida que não é nossa (provável que a CI simplesmente não rode no fork desde que a dívida entrou — commit 2dc384e indica que push no fork não auto-dispara).
- O risco número 1 de quebrar release com os fixes de GUI da onda é import de gi no topo de teste novo sem guarda (erro de COLETA em `pytest tests/unit -v` do release.yml, não skip) — exatamente a armadilha que custou 4 rounds do v3.14.0; o antídoto verificado é o padrão _install_gi_stubs de test_rumble_actions.py:22-40 (GATE-SKIP-MASK-01).

## open_questions

- Como o job acentuacao da CI está verde no main se `validar-acentuacao.py --all` reprova com 45 violações pré-existentes lá (requirements.txt:1, tests/unit/test_xdg_paths.py:48 etc. confirmados via git show main:)? Hipótese: a CI do fork não auto-dispara em push (commit 2dc384e) e o job nunca rodou desde que a dívida entrou — conferir o último run real antes do próximo PR/push ao main, ou limpar as 45 na onda.
- O gate 'suíte completa' das próximas sprints deve ser `pytest tests/` (3136, inclui tests/core/test_sysfs_leds.py) ou paridade com CI `pytest tests/unit`? Recomendo padronizar tests/ (3136) como número de referência do gate humano.
- Qual o papel do diretório venv/ na raiz (além do .venv que run.sh e o gate usam)? Se for lixo legado, candidato a remoção fora desta onda.
- Os markers requires_gtk/requires_display citados no smoke-multi-distro não existem em teste algum nem estão registrados — filtro morto; vale registrar/usar ou remover do ci.yml quando tocarmos em CI.
