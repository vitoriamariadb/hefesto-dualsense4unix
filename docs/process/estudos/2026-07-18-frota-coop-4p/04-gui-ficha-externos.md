# Agente: v2:f7a294ed82f3c07d721897557b3f88b1406c5b4109e070c3173265045eade13a

## report

MISSÃO P4 (ficha do controle externo sem o "dropdown" de modo Nintendo/Xbox) — DIAGNÓSTICO FECHADO.

VEREDITO CENTRAL: o dropdown NUNCA EXISTIU. O commit 9532eb1 não construiu um seletor interativo — construiu DETECÇÃO read-only + texto de orientação. A própria mensagem do commit diz: "É modo de HARDWARE (combo no controle), não toggle de software". E o design doc do 8BIT-02 (docs/process/sprints/2026-07-16-sprint-8bitdo-e-outros-controles.md:284) veta explicitamente: "ZERO knobs proprietários para outras marcas. Sem dropdowns/popups (bug de foco do cosmic-comp — usar cards e botões segmentados)". Linha 20 do mesmo doc: "8BIT-05 (decisão de modo) segue com a Vitória — custo zero de código". A ficha é 100% read-only por pré-requisito de produto (mesmo doc, seção 8BIT-02, pré-requisito 1). Trocar o modo do 8BitDo/Pro Controller é combo físico ao ligar o controle — software não consegue.

O QUE A FICHA REALMENTE MOSTRA (e a condição exata de visibilidade):
1. src/hefesto_dualsense4unix/app/gui_dialogs.py:283 — loop de detail_rows(entry) renderiza as linhas rótulo:valor em Gtk.Grid.
2. src/hefesto_dualsense4unix/app/actions/external_controllers.py:257-259 — dentro de detail_rows(), `guia = mode_guidance(entry); if guia is not None: rows.append(("O jogo vê como", guia[0]))` → linha "O jogo vê como: Nintendo (modo Switch)".
3. gui_dialogs.py:298-306 — `guia = mode_guidance(entry); if guia is not None:` → parágrafo dim-label com a orientação (texto longo sobre trocar pro modo Xbox via combo no controle). ESTA é a única "UI de modo" que existe — um Gtk.Label cinza com wrap, sem nenhum widget interativo.
4. Condição raiz: external_controllers.py:215-244 mode_guidance() retorna não-None sse input_mode() (linhas 196-212) devolve "nintendo" (vid=="057e" OU driver em {"nintendo","hid-nintendo"}) ou "xbox" (vid=="045e" OU driver em {"xpad","microsoft"}). Não há gate de transporte (BT vs USB), de OUI nem feature flag.

PROVA DE QUE O CÓDIGO ESTÁ VIVO E FUNCIONA PARA O PRO CONTROLLER POR CABO: executei as funções puras no venv do repo com a entry real (vid=057e, pid=2009, bus=usb, driver=nintendo): input_mode="nintendo", mode_guidance!=None, e detail_rows devolve exatamente: Controle=Pro Controller (modo Switch) / Como conectou=Cabo (USB) / O jogo vê como=Nintendo (modo Switch) / Driver do Linux=nintendo / Nome do sistema=Pro Controller / Gerenciado por=Linux + Steam (o Hefesto não mexe nele). nintendo_bt_warning=None (só dispara com bus BT — external_controllers.py:187). Ou seja: a ficha do screenshot DEVE conter a linha "O jogo vê como" e o parágrafo cinza de orientação; o que ela não tem (nem nunca teve) é um dropdown/toggle. Se o screenshot não mostra nem a linha "O jogo vê como", ver open_questions (única brecha: entry sem vid/driver — refutada pelo inventário, evdev_reader.py:295-304 sempre preenche vid/pid/driver).

VERSÃO EM EXECUÇÃO NÃO É O PROBLEMA: GUI PID 10464 = /usr/bin/python3.12 com VIRTUAL_ENV=/home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/.venv; o site-packages do venv tem _editable_impl_hefesto_dualsense4unix.pth (install editable) → importa o src/ do repo em HEAD 62483b2 (working tree clean), que contém 9532eb1 e e855f0d. Daemon PID 2469 idem (.venv/bin/python).

O QUE CADA COMMIT MUDOU NA ÁREA:
- 9532eb1: criou input_mode()/mode_guidance() em external_controllers.py; inseriu a linha "O jogo vê como" em detail_rows() e o parágrafo de orientação em gui_dialogs.py:298-306; adicionou o slot global no topo da ficha (gui_dialogs.py:259-265); e o TEMA: gui_dialogs.py:248-249 `dialog.get_style_context().add_class("hefesto-dualsense4unix-window")`.
- e855f0d: SÓ rotulagem/LED, não tocou a ficha nem gui_dialogs.py. Adicionou _BRAND_BY_OUI (e417d8→8BitDo, external_controllers.py:57-59), _oui_of() (68-78), brand_of() (101-114, OUI vence VID p/ desmascarar 8BitDo em modo DS4), e fez short_button_label/button_labels_for usarem brand_of; resto foi core/external_leds.py (lightbar por slot no modo DS4) e ipc_handlers.py.

TEMA DO POPUP ("tema no popup" do 9532eb1): theme.py:58-62 registra theme.css SCREEN-WIDE (Gtk.StyleContext.add_provider_for_screen, PRIORITY_APPLICATION), mas TODOS os seletores de fundo/cor do theme.css são escopados sob .hefesto-dualsense4unix-window (66 ocorrências; theme.css:4-7 define o fundo Drácula #282a36). Diálogo SEM a classe herda o claro do COSMIC mesmo com gtk-application-prefer-dark-theme=True (theme.py:40-44 — o commit 9532eb1 prova que prefer-dark sozinho não bastou). A ficha externa TEM a classe (gui_dialogs.py:248-249) → escura, OK. BONUS/P5: o diálogo do wrapper para jogo não mapeado (_build_wrapper_dialog, src/hefesto_dualsense4unix/app/actions/launch_wrapper_dialog.py:357-386) cria Gtk.MessageDialog SEM add_class("hefesto-dualsense4unix-window") → é exatamente por isso que ele abre com background claro do sistema (causa-raiz do P5; fix de 1 linha, espelhar gui_dialogs.py:248-249).

"Gerenciado por: Linux + Steam": hardcoded incondicional como última linha de detail_rows — external_controllers.py:266: ("Gerenciado por", "Linux + Steam (o Hefesto não mexe nele)"). Não há lógica; é o contrato read-only do 8BIT-02 (o Hefesto não adota externos).

Testes que travam esse comportamento: tests/unit/test_external_controllers.py:164-194 (input_mode nintendo/xbox/outro; mode_guidance None p/ desconhecido; rows["O jogo vê como"]=="Nintendo (modo Switch)").

## key_files

- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/gui_dialogs.py:221-322 (ficha); 248-249 (classe de tema); 283 (grid detail_rows); 298-306 (condicao `if guia is not None` do bloco de modo) — show_external_controller e a UNICA UI da ficha secreta — prova que nao existe nenhum widget de dropdown, so labels; contem a condicao de visibilidade do bloco de modo e o fix de tema do 9532eb1
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/actions/external_controllers.py:196-212 (input_mode), 215-244 (mode_guidance), 247-267 (detail_rows; 257-259 linha 'O jogo ve como'; 266 'Gerenciado por' hardcoded), 57-59+101-114 (OUI/brand_of do e855f0d) — Logica pura que decide se o bloco de modo aparece (vid 057e/045e ou driver nintendo/xpad) — sem gate de transporte, sem flag; verificada ao vivo devolvendo o bloco para o Pro Controller USB
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-16-sprint-8bitdo-e-outros-controles.md:20 (8BIT-05 humano), 262-266 (ficha 100% read-only), 284-286 (veto a dropdowns/knobs) — Decisao de design documentada: sem dropdowns na ficha (cosmic-epoch#2497), modo e escolha de hardware da mantenedora
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/theme.py:40-44 (prefer-dark insuficiente no COSMIC), 58-63 (provider screen-wide + classe na janela) — Mecanismo do tema: CSS e global mas escopado pela classe .hefesto-dualsense4unix-window — dialogo sem a classe fica claro
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/gui/theme.css:4-7 e 66 ocorrencias da classe — Todos os seletores de fundo/cor exigem .hefesto-dualsense4unix-window — explica dialogo claro sem a classe
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/actions/launch_wrapper_dialog.py:357-386 — P5 (bonus): _build_wrapper_dialog NAO adiciona a classe de tema — causa-raiz do dialogo do wrapper abrir claro; fix de 1 linha espelhando gui_dialogs.py:248-249
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/actions/status_actions.py:411-431 (botoes externos), 470-486 (_on_external_clicked abre a ficha com contextlib.suppress(Exception)) — Caminho de abertura da ficha; o suppress engoliria excecao silenciosamente (nao e o caso aqui, mas e o unico ponto de falha silenciosa do fluxo)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/evdev_reader.py:243-311 — discover_external_gamepads sempre preenche vid/pid/bus/driver — refuta a hipotese de entry incompleta na ficha
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/tests/unit/test_external_controllers.py:164-194 — Testes que travam input_mode/mode_guidance e a linha 'O jogo ve como' — comportamento atual e o especificado

## hypotheses

- H1 (praticamente certa): NÃO é bug — o 'dropdown' nunca foi construído; 9532eb1 entregou detecção read-only + texto de orientação (mode_guidance), e o design doc 8BIT-02 (2026-07-16-sprint-8bitdo-e-outros-controles.md:284) veta dropdowns/knobs ('ZERO knobs proprietários... Sem dropdowns/popups', bug de foco cosmic-epoch#2497); a troca de modo é combo de HARDWARE no controle (8BIT-05 = decisão humana, 'custo zero de código'). A ficha do Pro Controller por cabo MOSTRA a linha 'O jogo vê como: Nintendo (modo Switch)' + parágrafo cinza — que visualmente não parece um seletor, daí a expectativa frustrada da mantenedora.
- H2 (gap de UX real, acionável): o parágrafo de orientação usa classe dim-label e texto corrido (gui_dialogs.py:301-306), fácil de ler como rodapé decorativo; se o produto quiser um 'seletor' visível sem violar o veto a dropdowns, o padrão da casa é botões segmentados read-only (como o seletor do topo) marcando o modo detectado — mas isso é feature nova, não regressão.
- H3 (remota, só se o screenshot não mostrar NEM a linha 'O jogo vê como'): entry chegando sem vid/driver ao _on_external_clicked — refutada pelo inventário (evdev_reader.py:295-304 sempre preenche vid/pid/driver) e pela verificação ao vivo das funções puras no venv; manteria só se o daemon em execução fosse pré-8BIT-01, o que não é (PID 2469 roda do venv editable no HEAD).

## open_questions

- O screenshot de hoje mostra ou nao a linha 'O jogo vê como: Nintendo (modo Switch)' e o paragrafo cinza de orientacao? Se SIM (esperado), o gap e so de expectativa (texto vs seletor interativo) e a acao e decisao de produto; se NAO, investigar a entry recebida por _on_external_clicked ao vivo (unico caminho seria entry sem vid/driver, ja refutado no codigo).
- A mantenedora quer um 'seletor' VISUAL do modo (read-only, botoes segmentados marcando Nintendo|Xbox como o resto da GUI) mesmo sabendo que a troca real e combo fisico no controle? Isso seria feature nova compativel com o veto a dropdowns do 8BIT-02, nao um fix.
- P5: confirmar com o agente do wrapper que o fix e adicionar add_class('hefesto-dualsense4unix-window') no Gtk.MessageDialog de launch_wrapper_dialog.py:365 (mesma cura do 9532eb1) — e varrer os demais dialogos de gui_dialogs.py (confirm_delete_profile etc., linhas 150-213) que tambem nao tem a classe.
