# Agente: v2:1ffe0aa0ad7940c30e6700ea175662b2d14aae73965d564d92852093e8a092ea

## report

INVENTÁRIO COMPLETO DOS ESCRITORES DE LIGHTBAR/LED + DIAGNÓSTICO DA RACE DO P1 (branco USB verde-limão  azul)

== EVIDÊNCIA AO VIVO (coletada agora, só leitura) ==
1. /sys/class/leds/input9:rgb:indicator (uniq <MAC-DS-BRANCO> = BRANCO USB): multi_intensity = "0 0 153", brightness=255 → a ÚLTIMA escrita via classe LED foi AZUL slot-1 escalado (0,0,255)*0.6. A barra física está verde-limão ⇒ o verde NÃO veio da classe LED/sysfs; veio de escrita HIDRAW CRUA (que nunca atualiza a classe — documentado em sysfs_leds.py:80-105, STATUS-01).
2. /sys/class/leds/input43:rgb:indicator (<MAC-DS-ROXO> = ROXO BT): "153 0 0" = vermelho slot-2 * 0.6. Consistente com o relato "roxo perfeito, lightbar vermelha".
3. ~/.config/hefesto-dualsense4unix/controllers.json: {"<MAC-DS-BRANCO>": 1, "<MAC-DS-ROXO>": 2} → BRANCO = slot 1 (cor canônica AZUL 0,0,255), ROXO = slot 2 (VERMELHO). Perfil ativo com brilho 0.6 explica o 153 (sackboy_nativo.json: lightbar_brightness 0.6; perfis vitoria/sackboy/navegacao NÃO têm verde — nenhum perfil é a fonte do verde).
4. fuser nos hidraw: /dev/hidraw3 (BRANCO físico) está aberto por TRÊS processos: 2469 (daemon hefesto), 6034 (steam client) e 7347 (winedevice.exe = stack HID do Proton do jogo). /dev/hidraw6 (vpad P1) aberto por winedevice.exe. /dev/hidraw7 (ROXO BT) também por daemon+steam+winedevice.
5. Journal de hoje: profile_activated flapando via autoswitch (20:15:11 vitoria → 20:15:40 sackboy_nativo → 20:15:45 vitoria → 20:15:51 sackboy_nativo; 20:21:03...) e backend_hotplug_reconcile trigger=input_dir_change (20:15:14, 20:15:29, 20:15:31, 20:38:45...). lightbar_reset_enviado SÓ para o roxo BT (<MAC-DS-ROXO>), nunca para USB (correto por design).

== ESCRITORES INTERNOS (daemon) ==
W1. reassert_resolved_outputs — backend_pydualsense.py:1600-1631. Via SYSFS (node.set_rgb). Escreve o merge (explícito > paleta AUTO > global). GATILHOS PERIÓDICOS: (a) FIM DE TODO connect() (backend:790) — e o reconnect_loop chama controller.connect() EM TODA ITERAÇÃO: online = a cada 30s (connection.py:30 RECONNECT_ONLINE_CHECK_INTERVAL_SEC; corpo do loop connection.py:239) e ANTECIPADO a cada mudança de /dev/input (fatias de 2s, connection.py:39 + 310-317); (b) fim de TODA ativação de perfil (profiles/manager.py:227-229) — e o autoswitch está flapando (journal); (c) "Aplicar" da GUI (daemon/ipc_draft_applier.py:158). Para o branco escreve AZUL (0,0,153). USB e BT igualmente.
W2. Paleta automática por slot (COR-03) — core/led_control.py:123-128 (_PLAYER_SLOT_COLORS: 1=AZUL (0,0,255), 2=vermelho, 3=VERDE (0,255,0), 4=rosa) + daemon/subsystems/identity.py:391-431 (make_auto_output_provider, escala pelo brilho D11). Não escreve hardware diretamente — alimenta W1/W3/W5 via _merged_desired_for_key (backend:518-551). Slot 1 do branco = AZUL, não verde.
W3. apply_output_defaults (broadcast da ativação de perfil) — backend:1391-1398 via _for_each_led→sysfs. Escreve a cor GLOBAL do perfil em TODOS os controles a cada profile_activated (autoswitch incluso); ~ms depois o W1 (manager:227) repinta com a paleta. Gera um blink cor-do-perfil→azul a cada flap.
W4. Priming de nó novo — backend:958-979 dentro de _refresh_sysfs_leds: nó sysfs novo/recriado recebe desired.led ou KERNEL_DEFAULT_BLUE (0,0,128) (backend:65). Via sysfs, no connect() (backend:774).
W5. _reapply_desired/_write_partial_output — backend:1225-1281: handle recém-aberto (hotplug-IN) recebe o merge; sysfs preferido, fallback hidraw pydualsense.
W6. set_led (perfil/GUI/IPC led.set) — backend:1294-1303; a GUI só chama em gesto do usuário (app/actions/lightbar_actions.py:268, 333) — não periódico. Applet não escreve LED.
W7. report_thread pydualsense — _PinnedPyDualSense.prepareReport backend:305-339, via HIDRAW; SUPRIMIDO para o branco (coberto por sysfs ⇒ _suppress_leds=True limpa flags 0x04|0x10, backend:328) e SEMPRE suprimido em BT (backend:941-943, LIGHTBAR-BT-NEVER-01). NÃO é escritor aqui — descartado.
W8. Reset LED state 0x08 — core/lightbar_reset.py:41-77, HIDRAW, SÓ BT, só na ADOÇÃO de handle novo (backend:760-768). Não escreve cor nenhuma (todos os campos zerados, só flag1=0x08 devolve o claim ao host). Nunca roda no USB — descartado como fonte.
W9. Co-op _apply_coop_player_leds — daemon/subsystems/coop.py:500-537: SÓ player-LEDs brancos (set_players), nunca RGB. Descartado.
W10. external_leds — core/external_leds.py:130-151 (write_lightbar_slot com player_slot_color): só externos (Nintendo barra verde / 8BitDo modo DS4). Não alcança DualSense. Descartado.
W11. Vpad UHID: uhid_gamepad._handle_output (integrations/uhid_gamepad.py:856-878) SÓ parseia rumble (valid_flag0 & 0x03, linhas 870-874); lightbar que o jogo manda ao hidraw do vpad é DESCARTADA — o daemon NÃO replica LED do vpad pro físico. Descartado como rota do verde.

== ESCRITORES EXTERNOS (fora do daemon) ==
W12. Kernel hid_playstation: azul default (0,0,128) no probe/resume por caminho interno que não atualiza a classe (backend:59-65). Só em connect/resume.
W13. Steam client (PID 6034): tem /dev/hidraw3 e /dev/hidraw7 abertos (fuser). Steam Input com PS support escreve lightbar por hidraw.
W14. winedevice.exe (PID 7347 — o jogo via Proton): tem /dev/hidraw3 (FÍSICO BRANCO!), hidraw6 (vpad P1), hidraw7 (roxo) e hidraw2 (Nintendo) abertos. Causa: o wrapper compõe PROTON_ENABLE_HIDRAW=1 (daemon/launch_env.py:95-102) — isso expõe TODOS os hidraw ao hid.sys do wine; SDL_GAMECONTROLLER_IGNORE_DEVICES esconde o físico SÓ da SDL, não do wine/libScePad. O jogo (Sackboy, port PS com suporte nativo a DualSense) escreve report 0x02 com bytes de lightbar DIRETO no hidraw3.

== A RACE DO P1, EXPLICADA ==
(a) QUEM ESCREVE O VERDE-LIMÃO: o JOGO (winedevice.exe/libScePad via PROTON_ENABLE_HIDRAW), por hidraw cru no físico branco. O jogo enxerga o físico como SEU player 3 (o P2 relata exatamente isso: "aparece player 1 mas mexe player 3 e 4" — físico js1 + vpad js3 visíveis) e pinta a cor PS de P3 = VERDE (verde puro no LED do DualSense lê como limão). Prova: classe LED = azul (0 0 153) enquanto a barra está verde ⇒ escrita hidraw que não passa pela classe; winedevice segura o hidraw3. Steam (W13) é escritor alternativo/possível do mesmo verde (precisa de snoop para distinguir).
(b) QUEM ESCREVE O AZUL: o DAEMON — paleta automática slot 1 do branco = (0,0,255)×0.6 = (0,0,153), via sysfs (W1). controllers.json confirma slot 1.
(c) O TIMER DA ALTERNÂNCIA: reconnect_loop → connect() → reassert_resolved_outputs a cada ≤30s online (connection.py:30) + a cada mudança de /dev/input (2s de latência) + a cada flap do autoswitch (vários no journal, wm_class "unknown"/steam/Hefesto alternando). Ciclo: jogo mantém verde → daemon repinta azul no tick → jogo reescreve verde no próximo output dele → "alterna e volta".
(d) POR QUE SÓ O USB (branco) E NÃO O BT (roxo): COLISÃO DE CORES que esconde a mesma race no roxo — o daemon pinta o roxo de VERMELHO (slot 2 da paleta) e o JOGO também o pinta de VERMELHO (é o player 2 DO JOGO, cor PS P2 = vermelho; o rumble do jogo chega nele, prova de que o output do jogo alcança o roxo). Dois escritores, mesma cor ⇒ race invisível. No branco: daemon diz slot 1 (azul), jogo diz player 3 (verde) ⇒ race visível. A numeração do daemon e a do jogo DIVERGEM — o mesmo desalinhamento raiz do P3.

== CAUSA-RAIZ ESTRUTURAL ==
O daemon se considera dono da lightbar do físico (paleta por slot, reassert convergente) ao mesmo tempo em que o wrapper entrega o hidraw do MESMO físico ao jogo (PROTON_ENABLE_HIDRAW=1 é global ao processo wine; IGNORE_DEVICES não cobre wine). Não existe gate "jogo em sessão wrapper ⇒ físico-do-vpad é do jogo" análogo ao output_mute do Modo Nativo (que já resolve isso para o Nativo — backend:1197-1204, 1256-1260). Direções de cura para os fix-agents: (1) durante sessão do hefesto-launch, mutar/pular o reassert de lightbar do físico adotado pelo vpad (mesmo gate do _output_mute, mas por-controle); (2) alinhar a paleta do daemon à numeração que o jogo vê (curaria por colisão, frágil); (3) esconder o físico do wine (não há mecanismo limpo).

## key_files

- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/backend_pydualsense.py:790, 1600-1631, 1391-1398, 958-979, 941-943, 305-339, 518-551, 1161-1216, 1241-1281, 65 — Concentra quase todos os escritores: reassert_resolved_outputs (o timer do AZUL), apply_output_defaults (blink da cor do perfil), priming KERNEL_DEFAULT_BLUE, supressão do report_thread, merge de 3 camadas e rotas sysfs/hidraw
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/connection.py:25-39, 237-246, 310-317, 324-350 — Cadência do reassert: connect() roda TODA iteração do reconnect_loop — 30s online (RECONNECT_ONLINE_CHECK_INTERVAL_SEC) + antecipação por mudança de /dev/input (2s) = o período da alternância azul
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/led_control.py:123-137 — _PLAYER_SLOT_COLORS: 1=azul (0,0,255) — a cor que o daemon insiste no branco; 3=verde (0,255,0) — a mesma convenção PS que o jogo usa para o SEU player 3
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/launch_env.py:59-102, 292-299 — PROTON_ENABLE_HIDRAW=1 expõe o hidraw do FÍSICO ao wine (winedevice.exe) — IGNORE_DEVICES só esconde da SDL; é a porta de entrada do escritor verde
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/subsystems/identity.py:186-237, 391-431 — Registro MAC→slot (controllers.json: branco=1, roxo=2) e provider da paleta consultado em todo reassert; a numeração daqui diverge da do jogo (raiz comum com P3)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/sysfs_leds.py:80-105, 137-150 — STATUS-01: escrita hidraw NÃO atualiza a classe LED — é o que prova que o verde veio de hidraw cru (classe lê azul 0 0 153 com a barra verde)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/integrations/uhid_gamepad.py:856-878 — _handle_output só parseia rumble; lightbar mandada pelo jogo ao vpad é descartada — o daemon NÃO replica LED do vpad pro físico (rota descartada do verde)
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/core/lightbar_reset.py:41-77 — Reset 0x08: só BT, só na adoção, payload sem cor — descartado como participante da race do USB
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/profiles/manager.py:204-229 — Cadeia da ativação (autoswitch incluso): broadcast do global (blink) seguido de reassert da paleta — dispara a cada flap de perfil visto no journal
- /home/vitoriamaria/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/daemon/subsystems/coop.py:500-537 — Co-op escreve SÓ player-LEDs (set_players), nunca RGB — descartado como escritor de lightbar

## hypotheses

- H1 (ALTA, ~85%): race dois-escritores hidraw-vs-sysfs — o JOGO (winedevice.exe PID 7347, hidraw aberto no físico via PROTON_ENABLE_HIDRAW=1 do wrapper, launch_env.py:95-102) pinta o branco de VERDE (cor PS do player 3 — o jogo vê físico js1 + vpad js3, trata o físico como P3/P4) por hidraw cru; o daemon repinta AZUL slot-1 (0,0,153) via sysfs no reassert_resolved_outputs a cada ≤30s (connection.py:30→backend:790), a cada mudança de /dev/input e a cada flap do autoswitch. Evidência: multi_intensity="0 0 153" (azul) com barra fisicamente verde = escrita hidraw fora da classe LED; fuser mostra winedevice+steam no hidraw3; controllers.json slot branco=1; journal com flaps e reconciles.
- H2 (MÉDIA, alternativa dentro da H1): o escritor do verde é o STEAM client (PID 6034, também segura hidraw3) via Steam Input PS support recolorindo pela ordinal DELE — mesma forma de race; distinguir de winedevice exige snoop de hidraw ou fechar Steam Input.
- H3 (explica o 'só no USB'): no ROXO BT a MESMA race existe mas é invisível por colisão de cor — daemon slot 2 = vermelho E jogo player 2 = vermelho (o rumble do jogo chega no roxo, prova que o output dele alcança o controle). Não é propriedade do transporte BT.
- H4 (contribuinte menor do 'alterna'): cada profile_activated do autoswitch (flapando por wm_class unknown/steam) faz apply_output_defaults escrever a cor GLOBAL do perfil (roxa/azulada) em broadcast (backend:1391-1398) e só depois o reassert repinta a paleta — blinks azulados extras entre os verdes.
- H5 (DESCARTADAS com evidência): report_thread pydualsense (suprimido: _suppress_leds=True para coberto-sysfs e sempre-BT, backend:941-943); Reset 0x08 (só BT, só adoção, não escreve cor, nunca roda no USB); vpad UHID replicando LED (uhid_gamepad._handle_output:856-878 descarta tudo que não é rumble); cor de perfil verde (nenhum perfil ativo tem verde: vitoria [129,61,156], sackboy_nativo [80,60,220], navegacao [40,80,180]); GUI/applet (led_set só em gesto).

## open_questions

- Quem exatamente emite o verde — winedevice.exe (libScePad do Sackboy) ou o Steam client (Steam Input PS support)? Ambos seguram /dev/hidraw3; distinguir exige snoop (strace -e write nos PIDs 7347/6034 apontado ao fd do hidraw3) ou teste de exclusão (desligar PS support no Steam / rodar sem PROTON_ENABLE_HIDRAW).
- Qual é o RGB exato do verde-limão que o jogo escreve (verde puro 0,255,0 percebido como limão, ou um lime real tipo 128,255,0)? Um dump do report 0x02 no hidraw3 responderia e confirmaria a convenção de cor por player do jogo.
- A ordem que faz o jogo tratar o físico branco como player 3/4 é estável (vpad Edge + 8BitDo + Nintendo na frente?) — a mesma enumeração desalinhada é a causa do P3; mapear a ordem SDL/wine do jogo fecharia os dois problemas com um fix só.
- O reassert deveria ser suprimido quando outro processo segura o hidraw do físico (sessão wrapper ativa)? Hoje não há gate por-controle análogo ao output_mute do Modo Nativo — decisão de design pendente para os fix-agents.
- O Steam também escreve no roxo BT com report 0x31 bem-formado (pós nosso Reset 0x08)? Se sim, o roxo tem TRÊS escritores em vermelho coincidente — relevante se a paleta um dia mudar de cores.
