# Liga os que a onda 1 deixou sem dono, agora que input/select chegam ao Python

Workflow `wf_c13e3fc4-cd3`, rodado em 01/09/2026.

Este arquivo é a MATERIALIZAÇÃO do que os agentes devolveram. O transcrito
vivo mora em `~/.claude/projects/`, sob o nome ANTIGO da pasta
(`…-dev`), e some quando a sessão for limpa. O que está aqui, fica.


---

## Agente 1

{
  "aba": "06-navegacao.html",
  "ligados_agora": 1,
  "total_da_aba": 6,
  "testes_verdes": true,
  "ainda_sem_dono": [
    "navegacao-interna — navegar a janela do Hefesto com o controle nao tem metodo no daemon (nenhum dos 39) e o `core/disputa_de_botao.py` que as sprints de 27/08 citam NAO existe no disco (conferido com ls).",
    "modo-steam — nao ha metodo de Modo Steam no daemon; nenhum dos 39 de daemon/ipc_server.py.",
    "vel-touch-menos / vel-touch-mais — o cursor do touchpad sai do MESMO `mouse_speed`: `factor = TOUCHPAD_SENSITIVITY * (self.mouse_speed / DEFAULT_MOUSE_SPEED)` em integrations/uinput_mouse.py:446, e TOUCHPAD_SENSITIVITY e constante de modulo (:91). Nao ha segundo numero a ajustar; ligar os dois botoes ao mesmo `speed` poria dois numeros diferentes na tela mandando no mesmo valor.",
    "rolagem-dedos-menos / rolagem-dedos-mais — rolagem por dois dedos no touchpad nao existe no produto: `_emit_scroll(rx, ry)` (uinput_mouse.py:412-428) le so o analogico direito.",
    "acao-do-gesto — FATO CORRIGIDO. O inventario da primeira leva dizia 'os cinco combos moram em key_bindings do perfil'. NAO moram: sao callbacks montados em codigo (daemon/subsystems/hotkey.py:86 build_ps_solo, :414 build_next_bridge). O unico pedaco ajustavel e `config.ps_button_action`, e metodo de IPC nenhum o escreve.",
    "padrao-da-aba — a frase do proprio botao promete a aba INTEIRA ('as opcoes de ativacao, os 5 gestos e as 21 linhas das duas telas'). So as duas velocidades tem rota (mouse.emulation.set speed-only). Os 5 gestos sao codigo, as 21 linhas nao persistem, e Modo Steam / Navegacao Interna nao tem metodo. Um 'Voltar ao padrao' que devolve dois numeros de cinco promessas responde calado sobre as outras tres.",
    "guardar-definicoes — FATO CORRIGIDO, e o novo e pior. HA metodo: profile.apply_draft tem secao `keyboard.key_bindings` (daemon/ipc_draft_applier.py:646-673). Mas (a) ele empurra ao device VIVO e nunca grava em disco — some no proximo profile.switch; (b) sem `_keyboard_device` ele volta calado (:668-669) enquanto `_apply_section` (:125) ainda poe 'keyboard' em `applied`, ou seja o daemon responde que aplicou sem ter aplicado; (c) alcanca 9 das 21 linhas (as do DEFAULT_BUTTON_BINDINGS) — as outras 12 sao os tres mapas fixos de uinput_mouse.py:93,99,105 (BUTTON_TO_UINPUT, DPAD_TO_KEY, EDGE_KEY_MAP), sem campo em perfil nenhum.",
    "padrao-definicoes — idem ao contrario (`key_bindings: null` devolve o DEFAULT_BUTTON_BINDINGS via profiles/manager.py:1584), com o mesmo silencio sem device e a mesma ausencia de persistencia.",
    "guardar-remapeamento — o remapeamento botao-por-botao nao tem sequer campo no schema de perfil (grep em profiles/schema.py), quanto mais metodo de IPC.",
    "padrao-remapeamento — idem, ao contrario.",
    "guardar-ponto — 'Estilo de Jogo' nao existe em campo, widget ou preset nenhum do produto; quem ja mediu isso e app/actions/perfis_web.py, no cabecalho, para a aba Perfis. O `point_and_click` que existe e um PERFIL em disco (profiles/loader.py:95), nao um estilo, e gravar perfil nao tem metodo de IPC.",
    "mouse.emulation.restore — CONTINUA sem virar gesto, e a segunda leva achou uma razao maior que a primeira. Alem de o handler dizer o lugar dele ('entra na transicao de modo, nunca em um botao solto', ipc_handlers.py:5011) e de ele devolver a preferencia PERSISTIDA (que nao e 'de fabrica'), ele LIGA o mouse: restore_mouse_preference (lifecycle.py:1385-1408) chama set_mouse_emulation(pref,...) e, com a preferencia nunca gravada, `pref = True` por default (:1403). Isso move o cursor DELA e derruba o gamepad virtual (:1359) — e o poria na mesma prateleira do gesto `modo`, que ja esta em hefesto_vivo.PERIGOSOS. Criar um gesto perigoso NOVO fora daquela lista nao e possivel daqui: hefesto_vivo.py nao e territorio desta aba.",
    "as tres pop-ups (os <a> que as abrem, os 'Cancelar', os 'x' e os dois 'Voltar ao padrao' que so abrem a pergunta) — DEIXADOS SEM GESTO de proposito: sao navegacao de tela por ancora e `<label for>`, nao IPC. As 49 listas dentro delas continuam sem `data-gesto` pelo mesmo motivo dos 'Guardar' acima: o ponto de gravacao e o Guardar, e ele nao tem onde gravar."
  ],
  "mordida": "TRES mordidas, cada uma num ponto diferente da cura, com as mensagens copiadas da reprovacao.\n\n1) Troquei a chamada do gesto por `pass` (o defeito classico: gesto registrado que nao faz nada). Reprovou nas duas provas do teclado:\n\"AssertionError: 06-navegacao.html:teclado nao chamou NADA. E o defeito que esta regua existe para pegar: o gesto registrado que nao faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro.\"\n(assert [] where [] = PonteDeMentira.chamadas)\n\n2) Inverti o bool do mapa `_ESCOLHA` (\"desligada\": False -> True), que e o defeito que a prova de um lado so nao pegaria. Reprovou:\n\"AssertionError: teclado: mandou enabled=True, esperava False\" — 1 failed, 51 passed.\n\n3) Subi o PISO_DA_ABA para 7 com 6 gestos. Reprovou:\n\"AssertionError: 06-navegacao.html tem 6 gestos com dono e o piso e 7.\"\n\nDepois das tres, arquivo restaurado: 52 passed em test_os_botoes_tem_dono.py, 64 passed somando test_o_despachante_serve_as_dez.py, `ruff check layout/_ferramentas/pacotes/a06_navegacao.py` limpo, `validar-acentuacao.py --check-file` rc=0 nos dois arquivos meus, e `scripts/portoes.sh --rapido` TODOS VERDES — 23 portoes.\n\nPROVA DA OPCAO SEM DONO (a regua nao sabe cobrar \"nao chamou nada\", entao provei a mao, sem abrir janela): com a ponte dublada, \"So fora do jogo\" levanta RuntimeError e `chamadas == []`; \"Bananas\" levanta ValueError e `chamadas == []`; as duas opcoes reais mandam `chamar('keyboard.emulation.set', enabled=True/False)`.\n\nPROVA DA PINTURA (idem, sem janela): sem o bloco `keyboard_emulation` no estado a chave `teclado-estado` NAO e emitida (nada e afirmado); com enabled=True sai 'Ligada — atalhos e teclado na tela' e com False sai 'Desligada' — e conferi que as duas frases existem VERBATIM como `<option>` em layout/06-navegacao.html, que e a pagina que o produto abre. Sem essa conferencia o `el.value = texto` do piloto esvaziaria o `<select>` a cada tique.\n\nNAO rodei hefesto_vivo.py nem abri janela, conforme a ordem.",
  "achados": [
    "O UNICO GESTO NOVO E `teclado`, e o que o segurava nunca foi o daemon. `keyboard.emulation.set` estava la desde EMULACAO-NO-JOGO-01; o que faltava era o canal — o ouvinte do piloto so mandava `texto`, que num `<select>` e a lista INTEIRA concatenada. Com o `change` + `valor` de f53361db a lista ligou em quatro linhas. Arquivos tocados: /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/pacotes/a06_navegacao.py e /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/aba06.py (mais mockup/06-navegacao.html e layout/06-navegacao.html, regerados e publicados).",
    "'SO FORA DO JOGO' NAO EXISTE DO OUTRO LADO, E O CANDIDATO OBVIO FAZ O CONTRARIO. O que o produto tem e o 'modo jogo': `daemon.emulation.suppress` -> `set_emulation_suppressed` (daemon/lifecycle.py:1855-1892) e uma inversao de bool INCONDICIONAL que suspende mouse E teclado AGORA, no desktop, com ou sem jogo aberto. O unico caminho que liga a supressao sozinho quando um jogo comeca e `apply_profile_suppression`, a partir de `suppress_desktop_emulation` do perfil — e metodo de IPC nenhum grava perfil. Pendurar a opcao no `emulation.suppress` faria a tela dizer 'so fora do jogo' e o teclado morrer DENTRO do desktop, no mesmo clique. Fica sem dono, recusando dizendo.",
    "DOIS FATOS ERRADOS NO INVENTARIO QUE EU MESMO ESCREVI NA PRIMEIRA LEVA, substituidos no arquivo. (a) 'nao ha metodo de IPC que escreva key_bindings' e FALSO: `profile.apply_draft` tem secao `keyboard.key_bindings` (daemon/ipc_draft_applier.py:646-673). (b) 'os cinco combos moram em key_bindings do perfil' e FALSO: `key_bindings` sao os nove BOTOES do DEFAULT_BUTTON_BINDINGS (core/keyboard_mappings.py:41-64) e combo nenhum; os combos sao callbacks em codigo. O cabecalho do modulo tambem dizia que os cinco gestos moram no perfil — substituido.",
    "DEFEITO DO PRODUTO, ACHADO AO MEDIR O `guardar-definicoes`, e ele nao e desta aba: `profile.apply_draft` responde `applied: ['keyboard']` sem ter aplicado nada. `_apply_keyboard` faz `return` mudo quando `self.daemon._keyboard_device is None` (ipc_draft_applier.py:668-669) e `_apply_section` (:125) so olha se houve excecao para pendurar a secao em `applied`. Como a emulacao de teclado dela nasce desligada, esse e o caso COMUM — quem chamar o 'Aplicar' do rodape com a secao keyboard recebe um verde que nao vale nada. E a forma exata do defeito que esta casa chama de ausencia de noticia lida como sucesso.",
    "A RECUSA DE UM GESTO NAO APARECE NA TELA. `hefesto_vivo._gesto` manda a excecao para o stderr (`[gesto falhou] ...`, hefesto_vivo.py:519-521) e nada mais; o `[gesto sem dono]` idem (:497). Isso vale para as dez abas, e morde qualquer `<select>` ligado: a lista fica parada na opcao recusada e quem escolheu conclui que pegou. Por isso dei ao `<select>` um endereco de PINTURA (`data-campo=\"teclado-estado\"` + `data-hef-alvo=\"valor\"`): o tique de 500 ms reescreve o `value` com o que o daemon diz, e a opcao sem dono volta sozinha. Hoje e o unico canal de recusa visivel desta aba — e um lugar obvio para quem cuidar do piloto: um toast na janela resolveria para as dez.",
    "`value` EM `<option>` NAO E INVISIVEL AO PORTAO DO DESENHO. `check_o_desenho_aprovado.INVISIVEIS` lista `data-campo`, `data-gesto`, `data-hef-alvo` e mais quinze, mas nao `value` — entao dar chave as opcoes viraria divergencia de desenho em toda aba com lista. E por isso que o gesto casa por TEXTO, e casa pela PRIMEIRA palavra ('ligada' / 'desligada' / 'so', distintas entre si) para sobreviver a uma reescrita do que vem depois do travessao. Quem ligar `<select>` nas outras abas vai bater nisto.",
    "AS 21 LINHAS DO DESENHO NAO MOSTRAM OS BINDINGS REAIS — e isso torna o 'Guardar' pior do que so nao-persistente. As tres regioes do touchpad aparecem como 'Botao esquerdo / Botao direito / F11' e o DEFAULT_BUTTON_BINDINGS tem KEY_BACKSPACE / KEY_ENTER / KEY_DELETE; o clique do analogico direito aparece como 'Botao do meio' e o default e `__CLOSE_OSK__`. Um 'Guardar' ligado hoje gravaria o que ela nunca viu. E conteudo do DESENHO, dela — nao mexi.",
    "SEM PORTAO DE MODO NO GESTO DO TECLADO, ao contrario do gesto `modo` vizinho, e e medido: o portao de la existe porque ligar o MOUSE derruba o vpad (lifecycle.py:1359). `set_keyboard_emulation` (lifecycle.py:1448-1495) nao toca no vpad, e com o gamepad despachando o teclado nem e consultado (lifecycle.py:4742, `if not gamepad_dispatched`). Copiar o portao bloquearia, dentro do jogo, o unico interruptor que existe para calar o Alt+Tab do R1 — que e a queixa dela de 29/07 que o metodo nasceu para curar.",
    "O QUE ESTE BOTAO AINDA NAO DIZ: o handler manda a interface repassar que desligar tira tambem o teclado na tela do L3/R3 e as tres regioes do touchpad (ipc_handlers.py:5060). Nao ha canal de aviso no piloto — mesmo buraco do achado da recusa invisivel.",
    "PUBLICACAO: rodei `aba06.py` e `check_o_desenho_aprovado.py --publicar 06`. O portao respondeu '1 pagina(s) - 0 mudou/mudaram de fato', porque as duas marcacoes novas estao em INVISIVEIS: o desenho nao andou um pixel, e mockup/06 e layout/06 continuam com a mesma soma do que se ve. Antes de publicar conferi que o gerador e deterministico (regerei sem editar nada e o md5 nao mudou).",
    "COORDENACAO: um commit concorrente varreu meus arquivos em voo. O `ac253c6f` ('test(suite): 14.104 passam...') levou layout/_ferramentas/aba06.py, mockup/06-navegacao.html e layout/06-navegacao.html no meio do meu trabalho — as edicoes estao integras e ja em historia, mas quem rodou um `git add -A` alcancou territorio de outra aba. Nada quebrou; fica o registro porque a regra de territorio exclusivo so vale se o `git add` respeitar a mesma fronteira."
  ]
}


---

## Agente 2

{
  "aba": "02-controles.html",
  "ligados_agora": 1,
  "total_da_aba": 3,
  "testes_verdes": true,
  "ainda_sem_dono": [
    "Giroscópio (data-sensor=\"giroscopio\") — não há método de sensor no daemon. Medido de novo em 01/09 com pacotes/daemon.metodos(): 39 métodos, zero `sensor.*`, `gyro.*` ou `motion.*`. O daemon/sensor_hub.py só LÊ — 15 funções (`leitura`, `reconciliar`, `_abrir_*`, `_descobrir_*`), nenhuma liga ou desliga. profiles/schema.py:902 diz \"FORA POR AUSÊNCIA, NÃO POR DECISÃO\", dona ONDA-CONTROLES-07; `grep ProfileSensorsConfig src/` devolve UMA linha, e é esse próprio comentário. daemon/ipc_draft_applier.py não tem uma ocorrência de giro/sensor/accel — o rascunho também não os conhece. FICA SEM data-gesto (aba02.py:822-823): cai em `clique`, que não tem dono, e o piloto recusa dizendo o nome.",
    "Acelerômetro (data-sensor=\"acelerometro\") — o mesmo caso do Giroscópio, mesma linha do schema, mesma ausência de método. NÃO é \"o par do Giroscópio que você já ligou\": o Giroscópio NUNCA foi ligado (o a02_controles.py da primeira leva tinha DOIS gestos, `mudo` e `rota`, e nenhum de sensor).",
    "Mapa do Controle — `<a class=\"btn\" href=\"mapa-do-controle.html\">` em layout/02-controles.html:1409. Navegação, não IPC. Deixado SEM gesto de propósito, como o enunciado mandou.",
    "Calibrar Sensores de Movimento — `<a class=\"btn\" href=\"calibrar-sensores.html\">` em layout/02-controles.html:1407. Mesmo caso: navegação.",
    "Deslizante de volume do MICROFONE — NÃO EXISTE como elemento clicável. `type=\"range\"` aparece ZERO vez nos 16 HTML de layout/ (medido com grep -c em todos). O desenho tem `<span class=\"trilho\"><span class=\"cheio\" style=\"width:80%\">` (layout/02-controles.html:1620-1622): sem `value`, sem `data-*`, logo o `ev.target.closest(...)` do ouvinte (hefesto_vivo.py:204-206) nem dispara. O DAEMON ATENDE (`mic.volume.set`, e ponte.py:80 tem `mic_volume_set`) — o que falta é do lado do DESENHO, e desenho é dela.",
    "Deslizante de volume do ALTO-FALANTE — idêntico, layout/02-controles.html:1652-1654. `speaker.set {volume}` existe e ponte.py:81 tem `speaker_set`; o elemento é que não existe.",
    "Todo o som do PC (data-rota=\"pc\") — continua com gesto que RECUSA DIZENDO (a02_controles.py, gesto `rota`), não com dono. Metade dele é `pactl set-default-sink` (app/audio_saida.RotaDeSaida.mandar_para_o_controle:820), que não é IPC. Inalterado nesta leva."
  ],
  "mordida": "Sete mordidas, todas com a mensagem copiada da reprovação. NO GERADOR (`python3 aba02.py`, régua `_conferir`): (1) devolvi o `data-campo` ao container → \"ERRO em 02-controles — decisão dela desfeita: - o `data-campo` voltou ao container dos modos do microfone: a pintura vai apagar os dois botões\"; (2) tirei o `data-gesto` dos dois botões → \"- os modos do microfone perderam o `data-gesto` — chegam como 'clique'\"; (6) tirei o endereço da bateria → \"- a bateria perdeu o endereço `bateria` — o número volta a ser o do desenho\"; (7) tirei o `data-hef-alvo` da barra → \"- a barra da bateria perdeu o `data-hef-alvo=largura`: a pintura vai escrever o número DENTRO da barra, em vez de dar-lhe a largura\". NO PACOTE (pytest test_os_botoes_tem_dono.py): (3) troquei a chamada por `ok, motivo = True, \"\"` → \"AssertionError: 02-controles.html:mic-modo não chamou NADA. É o defeito que esta régua existe para pegar: o gesto registrado que não faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro.\"; (4) troquei `machine_declare` por `machine_declar` → DUAS reprovações, \"AssertionError: 02-controles.html:mic-modo chamou ['machine_declar'], esperava ['machine_declare']. A ORDEM importa.\" e \"AssertionError: a02_controles.py chama `ponte.machine_declar()` e a ponte não tem essa função.\" A MORDIDA (5) NÃO MORDEU e está declarada: renomear só `bateria-barra` no pacote passa, porque a aba casa 10 endereços contra um piso de 9 — por isso a régua da bateria foi escrita no GERADOR (mordidas 6 e 7), onde morde. E a mordida que prova o defeito curado: com o `data-campo` de volta no container, o Chrome headless mede `[data-mic-modo]` antes 4, depois 0.",
  "achados": [
    "O ENUNCIADO DESTA LEVA TINHA TRÊS FATOS ERRADOS, e conferi cada um contra a fonte antes de agir. (a) \"os deslizantes são <input type=range>\" — são ZERO: `grep -c 'type=\"range\"' layout/*.html` dá 0 nos DEZESSEIS arquivos, aba 02 inclusive. São `<span class=\"trilho\">` decorativos. (b) \"o Giroscópio, que você já ligou\" — nunca foi ligado: o a02_controles.py de HEAD tem exatamente dois gestos, `mudo` e `rota`. (c) \"o Nativo é o par do Virtual que você já ligou (mic.set com escopo)\" — o Virtual também nunca foi ligado, e `mic.set` NÃO tem escopo: `daemon.parametros('mic.set')` devolve ('muted', 'uniq'), só isso.",
    "O DEFEITO MAIS CARO DA ABA, e ele explica por que Virtual/Nativo \"ficaram sem dono\": ELES NÃO EXISTIAM QUANDO ELA CLICAVA. O pacote emitia a chave `mic-modo`, e o endereço `data-campo=\"mic-modo\"` estava no `<span class=\"rota mic-modo\">` que ENVOLVE os dois botões — não numa folha. O `escrever` do piloto (hefesto_vivo.py:100-116) faz `el.textContent = t`, com `t` virando `—` para valor vazio. MEDIDO no Chrome headless sobre a página publicada, com os dois valores que o pacote emitia (`''` e `'sem posse'`): `[data-mic-modo]` antes 4, depois 0. O primeiro tique da pintura trocava os quatro botões por um travessão. Curado nos dois lados (a chave saiu do pacote, o atributo saiu do gerador), com régua em cada um. LIÇÃO QUE VALE PARA AS DEZ ABAS: endereço de pintura só pode morar em FOLHA; um container endereçado é um container que some.",
    "O VIRTUAL/NATIVO TEM DONO, e a minha afirmação da primeira leva caiu. Eu escrevera \"grep por mic virtual em src/ devolve ZERO — só a sprint\", e a segunda metade continua verdadeira (`integrations/microfone_do_dualsense.py` não existe; a ONDA-CONEXOES-06 não virou código). A CONCLUSÃO é que estava errada: procurei a PALAVRA \"virtual\", que é vocabulário do desenho, e não a COISA. A coisa é a ponte de microfone por Bluetooth (QUATRO-MICROFONES-01, 22/08, decisão dela \"por controle\"), viva desde então: integrations/dualsense_bt_audio.py cria a fonte PipeWire própria (Opus tunelado no HID) — que é literalmente o title do botão Virtual —, o dono do estado é `ControleDeclarado.microfone` (utils/maquina.py:563), quem escreve é `machine.declare` (ipc_handlers.py:5258, e ponte.py:88 já expõe `machine_declare`), e quem sobe a ponte é o `bt_mic` do daemon, que RELÊ a fonte a cada varredura (bt_mic.py:60) — logo a escolha vale sem reiniciar o daemon. O gesto vivo na GUI estável é secao_controles.py:1113.",
    "REUSO EM VEZ DE REESCRITA, e ele trouxe a frase de recusa pronta: `pode_ligar_o_mic` e `dica_do_microfone` (app/actions/config/secao_controles.py:520 e :537) são funções de MÓDULO, puras, sobre um objeto de dados — o aviso do guia sobre `app/actions/*` é sobre os MIXINS GTK, e o `from gi.repository import Gtk` daquele arquivo mora dentro dos construtores de widget. Medido: o import roda sem display. A regra do produto é \"só no rádio\", e no cabo o Virtual recusa com a frase dele, palavra por palavra: \"Só vale no rádio. Pelo cabo o microfone deste controle é uma placa de som USB e não passa por esta ponte — ele já funciona sem ela.\"",
    "PROVA DE PONTA A PONTA, sem abrir janela na tela dela (Chrome headless, `/usr/bin/google-chrome`): carreguei a página PUBLICADA, instalei o BOOTSTRAP do piloto importado de hefesto_vivo.py (não copiado), pintei com o pacote REAL e CLIQUEI nos botões de verdade, capturando a mensagem do ouvinte. Resultado: antes da pintura 4 botões, DEPOIS DA PINTURA 4 botões (era 0); a bateria saiu dos 100% do mockup para os 37% do estado. O clique em Virtual chega como `gesto='mic-modo' micModo='virtual'` e, entregue ao gesto, no cabo RECUSA com a frase do produto e no rádio chama `machine_declare({'controles': {'aabbcc000001': {'microfone': True}}})`; o Nativo chama o mesmo com `microfone: None`. O script está em /tmp/claude-1000/-mnt-Apate-Desenvolvimento-hefesto-dualsense4unix-dev/4d211abb-52de-46ad-83fc-f5e1943569e6/scratchpad/prova_clique.py.",
    "A BATERIA ERA A PINTURA DO MOCKUP PARA SEMPRE. O pacote emitia `bateria` desde que nasceu e a página não tinha onde recebê-la — a régua do casamento a listava entre os órfãos. O número e a barra ficavam nos 100%/64% que o gerador desenhou, com o controle dela em qualquer carga. Dei dois endereços (`data-campo=\"bateria\"` na folha do número, `data-campo=\"bateria-barra\" data-hef-alvo=\"largura\"` na barra) e o pacote passou a emitir também o número CRU para a largura (um \"95%\" ali viraria `width: 95%%`). A cobertura REAL da aba foi de 8 para 10 endereços casados — e o antigo nono era o `mic-modo` destrutivo, ou seja, a régua contava um defeito como cobertura.",
    "O `machine.declare` NÃO TEM ECO no `state_full`, e o handler diz por quê (ipc_handlers.py:5301): \"aquilo é o tique de 20 Hz, e a declaração muda por gesto dela, não por quadro\". Declarado em SEM_ECO. CONSEQUÊNCIA NA TELA, e é dívida declarada, não descuido: depois de clicar \"Nativo\" o botão \"Virtual\" continua aceso até o gerador rodar de novo, porque a pintura do piloto só sabe escrever texto, largura, fundo e `value` (hefesto_vivo.py:100-116) — ela não sabe acender uma CLASSE. Ligar isso é do PILOTO, que não é território desta aba. Vale para todo botão de escolha exclusiva das dez.",
    "UM FATO ERRADO NO TEXTO APROVADO DELA, que eu NÃO corrigi porque texto de mockup é dela. O title do botão Nativo diz \"Pelo rádio isso depende do perfil que o adaptador negociou\". O dualsense_bt_audio.py mede o contrário, com fonte citada (Luiz Von Dentz, bluez/bluez#892): o DualSense NÃO implementa A2DP/HFP/HSP — o SDP dele só anuncia HID (0x1124) e PnP (0x1200). Pelo rádio, sem a ponte, NÃO HÁ microfone nenhum; não é \"depende do adaptador\". A frase certa seria algo como \"pelo rádio, sem o Hefesto, não há microfone: o DualSense não fala o perfil de áudio do Bluetooth\". Deixo a proposta e a medição na mesa.",
    "VERMELHO QUE NÃO É MEU, e conferi isolando: `ruff check layout/_ferramentas/pacotes/` acusa E402 em `pacotes/__init__.py:271` (o bloco `from . import (a01_jogar, ...)` sem `# noqa: E402`). Provado anterior ao meu trabalho: `ruff --isolated --select E402` sobre `git show HEAD:layout/_ferramentas/pacotes/__init__.py` acusa a mesma linha. Não toquei — é território de ninguém nesta leva. NÃO entra no portão: o comando do CI é `ruff check src/ tests/` (portoes.sh:96, ci.yml:483), e ele está verde.",
    "OUTRO VERMELHO QUE NÃO É MEU: `test_o_casamento_das_dez.py::test_a_aba_casa_pelo_menos_o_piso[01-jogar.html]` reprova com \"casa 7 endereços e o piso medido é 9\", vazios `['aviso-selo', 'aviso-texto', 'pendente', 'pendente-alvo']`. ISOLEI: restaurei a minha aba 02 ao estado de HEAD e o 01-jogar reprova IGUAL (e o 02 passa a reprovar junto, o que mede que a minha cura é o que o deixa verde). É o agente da aba 01 trabalhando agora — o `a01_jogar.py` tem mtime de 10:23 com conteúdo igual ao de HEAD.",
    "ESTADO FINAL VERDE: `ruff check layout/_ferramentas/pacotes/a02_controles.py` limpo; `pytest tests/unit/test_os_botoes_tem_dono.py` 54 passam; as cinco réguas vizinhas (botões, casamento[02], despachante, gancho, abas-vivas) passam; `bash scripts/portoes.sh --rapido` TODOS VERDES — 23 portões; `check_o_desenho_aprovado.py` diz \"o produto não está atrás do desenho dela\" (13 páginas). Arquivos tocados, e só estes quatro: /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/pacotes/a02_controles.py, /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/aba02.py, /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/mockup/02-controles.html e /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/02-controles.html (regerado por `python3 aba02.py` e publicado por `check_o_desenho_aprovado.py --publicar 02`). Só atributos e comentários mudaram no HTML — nenhum pixel."
  ]
}


---

## Agente 3

{
  "aba": "10-perfis.html",
  "ligados_agora": 7,
  "total_da_aba": 10,
  "testes_verdes": true,
  "ainda_sem_dono": [
    "recarregar — NÃO HÁ O QUE CHAMAR, e é medido: a dica dela diz \"Relê a lista do disco. Não descarta o que está no editor ao lado\", e a lista JÁ é relida do disco a cada tique de 500 ms — `a10_perfis.py:112` chama `load_all_profiles()` dentro de `pacote()`, que o piloto invoca em `hefesto_vivo.py:620` a cada `TIQUE_MS = 500` (`hefesto_vivo.py:63`). Não há IPC atrás dele e não há trabalho a refazer. Ligá-lo a um `load_all_profiles()` extra seria um botão que finge trabalho já feito. O que falta não é motor: é o botão SAIR do desenho, e isso é decisão dela.",
    "editor.estilo — NÃO EXISTE EM LUGAR NENHUM, e o próprio produto já o declara assim: `app/actions/perfis_web.py:145` (`GESTOS_SEM_MOTOR[\"editor.estilo\"]`) diz \"não existe campo de Estilo de Jogo no perfil, nem preset que o resolva\", e `perfis_web.py:122` aponta a ONDA-PERFIS-04 como dona. Conferido em 01/09: não há campo em `profiles/schema.py` (`Profile`), não há chave em `profiles/simple_match.py:157` (`SIMPLE_MATCH_PRESETS`, sete presets, nenhum de estilo) e os quinze estilos do desenho (`aba10.py:361`) não têm arquivo atrás. NÃO liguei, e a tela deixou de mentir sobre ele: com `data-hef-alvo=\"valor\"` e valor vazio o seletor abre EM BRANCO em vez de dizer \"Luta\" para todo perfil. RESSALVA HONESTA: a recusa do piloto sai em `stderr` (`hefesto_vivo.py:527`), não na tela — para ELA, clicar neste seletor ainda é silêncio. É o único dos doze nessa situação."
  ],
  "mordida": "ONZE MORDIDAS, cada cura arrancada e a reprovação copiada. As quatro primeiras contra a régua oficial (`tests/unit/test_os_botoes_tem_dono.py`), as demais contra a régua de disco que escrevi para esta leva (24 casos, pasta de perfis DE VERDADE num diretório temporário, dublê de ponte igual ao da régua oficial: /tmp/claude-1000/-mnt-Apate-Desenvolvimento-hefesto-dualsense4unix-dev/4d211abb-52de-46ad-83fc-f5e1943569e6/scratchpad/provar.py).\n\n[1] `ativar` deixa de chamar a ponte → \"AssertionError: 10-perfis.html:ativar não chamou NADA. É o defeito que esta régua existe para pegar: o gesto registrado que não faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro.\"\n[2] PISO_DA_ABA sobe para 11 → \"AssertionError: 10-perfis.html tem 10 gestos com dono e o piso é 11. / assert 10 >= 11\"\n[3] METODOS ganha `profile.delete` (que o daemon não atende) → \"AssertionError: estes métodos não existem no daemon: ['profile.delete'].\"\n[4] tiro `data-hef-alvo=\\\"valor\\\"` do `<select>` \"Funciona em\", regenero e meço no Chrome → \"editor.ambiente: opcoes 5 → 0\" (e o \"Estilo de Jogo\", já curado, fica em 15). Devolvida a cura: 5 e 15.\n[5] tiro o `editado=alvo` de `pacote()` → \"AssertionError: o editor pintou 'Elden Ring' e o escolhido e 'Orpheus' — o `editado=` nao chegou em pacote_da_aba\"\n[6] `_uma_vez_so` passa a repintar sempre → \"AssertionError: editor.nome foi repintado no 2o tique — apagaria o que ela digita\"\n[7] o `remover` deixa de armar (`armado = True`) → três casos caem: \"AssertionError: remover NAO recusou\" — apagaria no PRIMEIRO clique.\n[8] o `_gravar` do renomear vira `pass` → \"AssertionError: ['Elden Ring', 'Universal']\" (o perfil não foi renomeado) e \"AssertionError: []\" (não avisou o daemon).\n[9] volto o impasse dos dois campos (o \"Nome do Jogo\" recusa em vez de gravar a regra) → \"ValueError: o nome do jogo so entra em Jogo/Jogo da Steam\", com os dois caminhos fechados.\n[10] tiro a guarda \"ela já escolheu\" da inferência do campo Jogo → \"AssertionError: trocou a escolha DELA: Jogo da Steam\" — digitar um número num perfil que ela pôs em \"Jogo\" viraria perfil da Steam pelas costas dela.\n[11] tiro a guarda do clique (`_so_mudou` → `True`) → \"AssertionError: editor.jogo agiu num CLIQUE: [('profile_switch', ('Universal',), {}), ('chamar', ('launch_env.refresh',), {})]\" — um clique para pôr o cursor gravaria no disco.\n\nDepois de cada mordida o arquivo foi restaurado e reprovado verde. Estado final: 54 passam na régua oficial, 24/24 na régua de disco, 23 portões `--rapido` TODOS VERDES, `ruff check layout/_ferramentas/pacotes/a10_perfis.py` limpo, acentuação limpa nos dois arquivos, `check_o_desenho_aprovado.py` OK e `mockup/10-perfis.html` idêntico a `layout/10-perfis.html`.",
  "achados": [
    "A PRIMEIRA PINTURA APAGAVA OS DOIS `<select>` DO EDITOR — e este é o achado que destravou a leva inteira. Medido em Chrome de verdade (headless, sem janela na tela dela), injetando o `BOOTSTRAP` do piloto sobre o `layout/10-perfis.html` PUBLICADO: `editor.ambiente` 5 opções → 0, `editor.estilo` 15 opções → 0, num único tique. A causa é `escrever()` em `layout/_ferramentas/hefesto_vivo.py:114`: sem `data-hef-alvo`, o alvo padrão é `texto`, e `select.textContent = \"Jogo da Steam\"` destrói os filhos. A aba Gatilhos já tinha pago isto e curado (`aba03.py:502`); a Perfis não. Curado nos quatro campos do editor em `layout/_ferramentas/aba10.py`.",
    "OS DOIS `<input>` MOSTRAVAM O MOCKUP PARA SEMPRE. Mesmo instrumento, mesma tela: depois de pintar \"Elden Ring\"/\"1245620\", o `value` continuava \"Mortal Kombat\"/\"Mortal Kombat 1\" — a pintura escrevia `textContent`, que num campo de texto não aparece. Era a segunda metade da causa que a primeira leva nomeou. Sem curar isso, ligar o campo Nome seria ela RENOMEAR UM PERFIL OLHANDO O NOME DE OUTRO.",
    "O EDITOR PINTAVA O PERFIL ERRADO. `pacote()` chamava `perfis_web.pacote_da_aba(...)` sem `editado=`, e a função cai no perfil ATIVO (`app/actions/perfis_web.py:426`). Então `selecionar` mudava o alvo dos botões e o editor ao lado continuava mostrando outro perfil. Enquanto nenhum campo tinha gesto era só desalinho; com o Nome ligado, seria estrago. Curado em `a10_perfis.py:126`.",
    "UM FATO DO PRODUTO ESTÁ ERRADO PELA METADE, e é o que deu dono ao `detectar` e ao `novo`. `app/actions/perfis_web.py:115` afirma: \"NÃO TEM DONO. O daemon LÊ a janela em foco e o IPC NÃO PUBLICA o título nem a classe.\" MEDIDO em 01/09 contra o daemon `dev` desta árvore, via `ipc_bridge.daemon_state_full()`: das 49 chaves, SETE são de detecção de janela — `window_detect_backend`, `window_detect_healthy`, `window_detect_last_class`, `window_detect_current_class`, `window_detect_useful_age_sec`, `window_detect_seeing`, `window_detect_reason`. A CLASSE é publicada; só o TÍTULO não é. E `profiles_actions._aplicar_nascimento_com_jogo:3088` já lia exatamente `window_detect_last_class` desde o PERFIL-NASCE-CERTO-01. A frase está no `src/` (fora do meu território) e precisa ser corrigida lá: a metade certa é \"o título não é publicado\".",
    "EU CRIEI UM IMPASSE E O MEDI ANTES DE ENTREGAR. Com um perfil em \"Todos\": escolher \"Jogo\" no seletor recusava por falta de nome (`MSG_JOGO_SEM_NOME`) e digitar o nome recusava por o seletor estar em \"Todos\" — os dois caminhos fechados, o perfil preso em \"Todos\" para sempre. Cura: digitar no \"Nome do Jogo\" grava a REGRA INTEIRA, e `normalize_appid` decide entre \"Jogo\" e \"Jogo da Steam\" (só dígitos, ou um endereço da loja, é Steam). Não é invenção: `profiles_actions.py:3128` faz exatamente isso — `_select_radio(\"steam_game\")` E preenche o campo, no mesmo gesto. E a inferência só age quando o seletor NÃO estava numa das duas: a escolha explícita dela nunca é trocada.",
    "CLICAR DENTRO DE UM CAMPO DISPARA O GESTO, com o valor que já estava lá. Achado no Chrome, coletando o `postMessage`: mexer nos quatro campos produziu 13 mensagens, QUATRO delas `evento=click` (`editor.jogo evento=click valor='1245620'` só de eu clicar para pôr o cursor). O ouvinte escuta `click` E `change` (`hefesto_vivo.py:188-200`). Sem guarda, clicar no \"Nome do Jogo\" de um perfil em \"Todos\" gravaria a regra nova em disco por causa de um clique que não mudou nada. Curado com `_so_mudou(o)` nos três campos — `!= \"click\"` e não `== \"change\"`, para um clique de régua (dicionário sem `evento`) continuar valendo.",
    "A REGRA DE 500 MS TORNARIA OS CAMPOS INTOCÁVEIS. Com `data-hef-alvo=\"valor\"`, a pintura faz `el.value = t` sempre que difere, e o tique é de 500 ms — na segunda tecla que ela digita, o campo já difere do disco e meio segundo depois volta ao valor do perfil. Cura: `_uma_vez_so` pinta os três campos UMA VEZ por perfil escolhido, e volta a pintar quando ela sai da aba e volta (detectado pelo BURACO no tique: intervalo > 2 s significa que a página foi embora). Sem essa segunda guarda, a página recarregada mostraria de novo o \"Mortal Kombat\" do mockup.",
    "FATO SUBSTITUÍDO DA PRIMEIRA LEVA: \"numa árvore de teste a pasta de perfis é vazia\". A medição estava certa e a conclusão errada. `perfil.lista()` devolve 0 porque lê a pasta sem semear; `load_all_profiles()` — que é a que `pacote()` chama — SEMEIA os presets de fábrica (`loader._maybe_seed_presets:485`, uma vez por processo). Medido no mesmo lar de mentira do `conftest.py`: `perfil.lista()` → 0, `load_all_profiles()` → 9 perfis (Ação, Aventura, Corrida, Esportes, FPS, Navegação, fallback, meu_perfil, point_and_click). Corrigido nos dois lugares onde a frase aparecia em `a10_perfis.py`. A razão REAL de os nove gestos não terem prova declarada é outra: o `ctx` da régua é fixo em `active_profile=\"regua\"` e nenhum perfil se chama assim.",
    "AS RECUSAS NÃO CHEGAM NA TELA DELA. `hefesto_vivo.py:527` imprime `[gesto falhou]` em `stderr`, no terminal. O docstring do `_gesto` promete \"RECUSA o resto com o motivo NA TELA\" e isso não é o que o código faz. Consequência prática: a dica do Remover promete \"Pergunta antes\" e esta janela não tem diálogo — o `on_profile_remove` da janela estável abre um `gui_dialogs.confirm_delete_profile` GTK MODAL (`profiles_actions.py:3167`), e daqui não dá, porque os gestos rodam EM THREAD (`hefesto_vivo.py:520`). A pergunta foi então para o único pedaço de tela que já existe e que ela está olhando: o RÓTULO do próprio botão, que vira \"Remover “<perfil>”? Clique de novo\" por oito segundos. O desenho não muda — o mockup continua escrevendo \"Remover\".",
    "O `remover` tem TRÊS guardas, e o armamento é POR PERFIL: escolher outra linha e clicar não aproveita a confirmação da anterior (seria a pior forma de perder o perfil errado). Recusa apagar o perfil ATIVO. E tudo o que estes gestos apagam tem volta: `delete_profile` arquiva a última versão em `profiles/.historico/<slug>/` antes do `unlink` (PERFIL-SEM-RASTRO-01, `loader.py:1601`) — verificado em disco nos testes, inclusive para o arquivo antigo de um RENOMEAR.",
    "NÃO HÁ `rename` NO PRODUTO. `profiles/loader.py` tem `save_profile`, `delete_profile`, `load_profile` e `restaurar_do_historico`, e nenhum renomeia. O `editor.nome` faz a mesma dupla que o Salvar da janela estável (gravar o nome novo, apagar o antigo), nesta ordem — invertida, uma falha no meio perderia o perfil. Recusa nome vazio e recusa nome que já é de OUTRO perfil (o `save_profile` grava por SLUG: renomear \"Elden Ring\" para \"Pragmata\" gravaria por cima do Pragmata dela, calado).",
    "A METADE DO `detectar` QUE CONTINUA SEM DONO, e ela é dívida declarada: a dica promete \"funciona com jogo de qualquer lugar, não só da Steam\". O detector entrega uma **wm_class**, e o produto só sabe guardá-la como `MatchCriteria(window_class=…)` — uma regra que este editor NÃO SABE MOSTRAR (`perfis_web.py:172`), então o perfil abriria travado, com a frase de usar a linha de comando. Gravá-la a partir de um botão seria empurrar o perfil dela para fora da tela; escrevê-la como `process_name` seria pior, porque é outro dado (o basename de `/proc/PID/exe`) e casaria por acaso. O gesto RECUSA DIZENDO a classe que viu. Mesma dívida no `novo`: ele nasce catch-all sem jogo Steam em foco, que é exatamente o que `_aplicar_nascimento_com_jogo` faz.",
    "O `novo` também não sobe a prioridade acima dos catch-all como a janela estável faz — `_prioridade_acima_dos_catch_all` mora num mixin GTK que depende de widget e não é reusável daqui. O perfil nasce com a prioridade padrão do esquema. Está escrito no docstring do gesto.",
    "TERRITÓRIO E VIZINHOS: `ruff check layout/_ferramentas/pacotes/` tem UM erro, E402 em `pacotes/__init__.py:271`, que está assim no HEAD (commit 88b08e0a) e é de outro agente — `a10_perfis.py` sozinho passa limpo. Na suíte de vizinhos (261 testes que tocam `layout/_ferramentas`) há 3 vermelhos, e PROVEI que nenhum é meu revertendo o meu arquivo ao HEAD e vendo os mesmos 3: `test_identidade_das_duas_casas` (2, sobre a logo SVG) e `test_o_casamento_das_dez[01-jogar.html]` (\"casa 7 endereços e o piso medido é 9\"). O `10-perfis.html` passa nessa régua.",
    "AVISO PARA QUEM COORDENA: o commit ac253c6f (\"test(suíte): 14.104 passam\") levou o meu `layout/_ferramentas/aba10.py` e os dois HTML (`layout/10-perfis.html` e `mockup/10-perfis.html`) para dentro do HEAD junto com o trabalho de outro agente — um `git add -A` no meio da leva. Eles estão corretos e os dois HTML são idênticos entre si, mas a mensagem daquele commit não descreve o que entrou. O que ainda está solto na árvore é só `layout/_ferramentas/pacotes/a10_perfis.py`."
  ]
}


---

## Agente 4

{
  "aba": "08-conexoes.html",
  "ligados_agora": 4,
  "total_da_aba": 8,
  "ainda_sem_dono": [
    "luz-nao-acende (2 elementos) — NAO E IPC e nao tem funcao chamavel daqui: e `Disconnect` do BlueZ por D-Bus, em `src/hefesto_dualsense4unix/integrations/gesto_de_reconexao.py`, que roda `busctl`. NOTA: 'nao e IPC' deixou de ser motivo suficiente nesta leva — o `examinar-portas` tambem nao e IPC e foi ligado. O que falta aqui e um dono chamavel, nao o socket.",
    "mic-escopo (2 selects) — CONTRADICAO, e o ouvinte de `change` NAO a desfaz. E `mic_button_toggles_system`, UM por maquina (`src/hefesto_dualsense4unix/daemon/lifecycle.py:272`), aplicado pelo rascunho do perfil (`ipc_draft_applier.py:592`). A tela oferece POR CONTROLE: ligar faria o segundo card sobrescrever a escolha do primeiro, calado. Declarada em `src/hefesto_dualsense4unix/gui/aba_conexoes.py:100` SEM_FONTE, linha `controle.*.mic.escopo`, dona MIGRA-CONEXOES-06 §0.7, palavra dela.",
    "teto-da-vibracao (2 selects) — CONTRADICAO, idem. A tela oferece teto POR CONTROLE e o produto aplica `min` global (`src/hefesto_dualsense4unix/core/rumble.py`); o `min` e justamente o que impede um 'teto' de AUMENTAR a forca. `gui/aba_conexoes.py:100` SEM_FONTE, linha `controle.*.vibracao.teto`: sobrepor mudaria o DAEMON, nao a tela. Dona MIGRA-CONEXOES-11 §0.5, palavra dela.",
    "escolher-aparelho (7 botoes) — a lista e a constante `CENSO` do gerador (`layout/_ferramentas/aba08.py`), nao o censo do barramento dela. O pacote nao repinta a pop-up 'Mapear Entradas'.",
    "escolher-entrada (11 quadrados) — os quadrados saem de `FACES`/`QUEM_ESTA`, constantes do gerador. Declarar `mapa.portas` a partir deles escreveria no `maquina.json` dela o desenho de uma bancada de exemplo.",
    "nova-entrada (2 botoes) — mesma razao: a face e do mockup.",
    "novo-hub (1) — mesma razao, e o hub ainda pergunta em que entrada esta.",
    "tirar-daqui (1) — mesma razao: o alvo e um quadrado do mockup.",
    "nova-extensao (1) — mesma razao: a mae e um quadrado do mockup.",
    "nova-face (1) — O MOTIVO MUDOU HOJE e esta corrigido no pacote. O antigo ('o ouvinte manda `texto`, que num <input> e vazio') CADUCOU: o `valor` chega. O que impede agora e outra coisa, medida: `MapaDaMesa.faces` e uma LISTA (`src/hefesto_dualsense4unix/utils/maquina.py:494`) e `fundir_declaracao` (`maquina.py:640`) TROCA lista inteira em vez de fundir — criar uma face e reescrever as que ja existem. E as faces na tela sao do mockup (`FACES`), que o pacote nao repinta: a face nasceria em disco e nao apareceria, entao o segundo clique dela criaria a segunda."
  ],
  "testes_verdes": true,
  "mordida": "QUATRO mordidas, com a mensagem exata da reprovacao. (1) `mic-existe` trocado por `pass` -> pytest: \"AssertionError: 08-conexoes.html:mic-existe nao chamou NADA. E o defeito que esta regua existe para pegar: o gesto registrado que nao faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro. / assert []\". (2) 'Desligado' gravando `False` em vez de `None` -> \"AssertionError: mic-existe: passou ({'controles': {'aabbcc000001': {'microfone': False}}},) a machine_declare, esperava ({'controles': {'aabbcc000001': {'microfone': None}}},)\" — 1 failed, 53 passed. (3) filtro de `ordens_dispensadas` arrancado de `_itens_da_tela()` -> a prova reprova com \"AssertionError: a linha NAO calou\" e a tira volta a 6 achados com 'Mudanca recomendada' depois do clique no circulo-barrado: e literalmente o \"botao que grava e nao cala\" que a primeira leva mediu como razao para nao ligar. (4) guarda de faixa do `_slot` arrancada, com dois radios na mesa e clique no terceiro bloco -> \"IndexError: tuple index out of range\"; COM a guarda: \"RuntimeError -> vizinho-o-que-e: esta linha esta vazia — a tela tem o lugar e a sua mesa tem 2 item(ns) aqui agora\".",
  "achados": [
    "A ABA JOGAR CHAMA UMA FUNCAO PRIVADA DESTA, E O RENOMEIO A QUEBROU EM SILENCIO. `layout/_ferramentas/pacotes/a01_jogar.py:73` faz `from . import a08_conexoes; a08_conexoes._exame()` dentro de um `except Exception: return []`. Eu dividi `_exame()` em `_conferencias()` + `_itens_da_tela()` e o `AttributeError` virou LISTA VAZIA: a aba Jogar parou de emitir `aviso-selo` e `aviso-texto`, sem uma linha de erro. Quem acusou foi `tests/unit/test_o_casamento_das_dez.py` ('01-jogar.html casa 7 enderecos e o piso medido e 9'), e eu cheguei a atribuir a falha a outro agente ate reverter meus arquivos ao HEAD e ver o teste passar. CURADO: `_exame()` voltou como nome publico, com docstring dizendo que E CONTRATO entre abas. Duas licoes: (a) `except Exception` largo transforma renomeio em ausencia de dado; (b) o territorio 'so o meu aNN_*.py' nao e verdade — outra aba importa o meu.",
    "O MOTIVO QUE EU ESCREVI PARA `examinar-portas` NA PRIMEIRA LEVA ERA MEIA VERDADE, e a metade que faltava era a que importava. Eu escrevi 'o exame ja roda a cada tique — o botao pediria de novo o que a aba refaz duas vezes por segundo'. Rodavam TRES conferencias das cinco, e ZERO ordens de servico. As outras nao cabem no tique: `pareamentos` forka `busctl` (teto de 5 s, `exame_da_mesa.ESPERA_DO_BUSCTL_S`) e `mesa_de_radio.ler_a_mesa` proibe tique no proprio docstring. MEDIDO nesta maquina: `ler_a_mesa()` 0,001 s; `exame(leitura_das_ordens=...)` 0,02 s mas com fork. O botao passou a fazer o que o `title` dele promete, e foi ISSO que deu sujeito ao circulo-barrado.",
    "`mic-existe` NAO E `mic.set`, e o meu relatorio anterior errou o dono. `mic.set` e o MUDO no firmware: acende a luz vermelha e o PipeWire continua publicando a fonte. A tela promete 'Desligado, nenhum programa o enxerga' — isso e a PONTE, `ControleDeclarado.microfone` no `maquina.json` (`src/hefesto_dualsense4unix/utils/maquina.py:563`), decisao dela de 22/08 ('por controle'), consumida por `daemon/subsystems/bt_mic.py:127`. E vale NA HORA: `_handle_machine_declare` (`ipc_handlers.py:5340`) sobe/desce o subsystem no mesmo pedido.",
    "OS QUATRO RADIOS VIZINHOS DA TELA ERAM DO MOCKUP, E AGORA SAO OS DELA. `ler_a_mesa()` devolve 4 radios reais nesta maquina — `3554:fa09`, `25a7:fa07`, `046d:08e5`, `2357:012d` — exatamente os 4 blocos que o desenho tem. Sem isso, o `<select>` so podia gravar em `maquina.json` um radio de bancada de exemplo. O que muda um botao dessa familia de 'nao da' para 'da' e a TELA deixar de ser exemplo, nao o gesto.",
    "A REGUA `test_os_botoes_tem_dono` NAO ALCANCA TRES DOS QUATRO, e eu declarei isso no pacote em vez de fingir. `examinar-portas` nao chama a ponte (e sysfs + busctl); `ignorar` e `vizinho-o-que-e` dependem de leitura do `/sys` DELA. Dar prova a eles ali exigiria um teste unitario varrendo o barramento da maquina que o roda — verde nesta bancada, vermelho em qualquer CI. Provei a parte por fora, com leitura real e ponte duble: o circulo-barrado gravou `{'mesa': {'ordens_dispensadas': {'teclado_so_no_hub': {'quando': '2026-09-01', 'arranjo': '3-1.1.2'}}}}` sobre uma ordem REAL desta maquina, `ordens_novas` passou a devolver [] e `ordens_caladas` ['teclado_so_no_hub'], e com um arranjo diferente a ordem VOLTA; o `vizinho-o-que-e` traduziu 'Caixa de som' -> `caixa_de_som` sobre `046d:08e5`; e as tres cargas foram gravadas de verdade por `gravar_maquina_com_descartes` num XDG_CONFIG_HOME temporario (gravou=True, descartes=()), que e a MESMA chamada do handler do daemon. NADA foi escrito no `maquina.json` dela.",
    "`ruff check layout/_ferramentas/pacotes/` JA ESTAVA VERMELHO NO HEAD, e nao e meu: `layout/_ferramentas/pacotes/__init__.py:271` da E402 (o bloco `from . import a01_jogar, ...` que o commit 93ef6c66 acrescentou depois do `sys.path.insert`, com `# noqa: F401` mas sem `# noqa: E402`). Provado com o arquivo intacto (`git status` diz unmodified). Fora do meu territorio — quem puder, acrescente `E402` ao noqa. Os meus dois arquivos passam limpos.",
    "`tests/unit/test_identidade_das_duas_casas.py` reprova em dois testes de LOGO (`test_a_logo_de_dev_nao_depende_do_que_o_librsvg_ignora`, `test_a_logo_de_dev_e_visivelmente_outra`). Provado que nao e meu: reprova igual com os meus quatro arquivos revertidos ao HEAD, e nenhum arquivo de logo esta modificado na arvore.",
    "A TELA ESCONDE O QUE SOBRA, e nao ha onde dizer. O desenho tem 5 linhas de exame e 4 blocos de vizinho; se a mesa dela render mais (tres ordens abertas, sete radios), o excedente nao aparece. Nenhum clique age sobre o alvo errado — o `data-v` so vai ate 4 e o `_slot` confere a faixa — mas e o mesmo buraco que `gui/aba_conexoes.sobraram` mede na janela estavel. Fica escrito em comentario no `pacote()` para quem desenhar o proximo.",
    "O `VIZINHOS` do gerador estava escrito DUAS vezes — em `layout/_ferramentas/aba08.py` e em `gui/aba_conexoes.RESPOSTAS_DO_VIZINHO`, identicas byte a byte. Passou a derivar da segunda. Enquanto fossem iguais ninguem veria problema; no dia em que uma ganhasse uma opcao, a tela ofereceria uma resposta que o gesto recusa. Zero mudanca visual (portao `desenho-aprovado` verde).",
    "O GERADOR MUDOU SO EM ENDERECO: `data-v` por linha no circulo-barrado e nos selects de vizinho, `data-campo`+`data-hef-alvo=\"valor\"` no `mic-existe` e nos vizinhos. Todos estao na lista de INVISIVEIS do `scripts/check_o_desenho_aprovado.py:98`, entao `mockup/` e `layout/` continuam identicos ao que ela aprovou — o portao confirma ('0 mudou/mudaram de fato'). O `casamento.py` mediu a aba subindo de 5 para 8 enderecos casados (`mic-existe`, `vizinho-nome`, `vizinho-tipo` novos)."
  ]
}
