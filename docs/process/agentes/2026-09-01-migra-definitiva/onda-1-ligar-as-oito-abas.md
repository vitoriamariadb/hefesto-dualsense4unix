# Liga os botões de oito abas, um agente por aba, território exclusivo

Workflow `wf_6fe75b1f-b27`, rodado em 01/09/2026.

Este arquivo é a MATERIALIZAÇÃO do que os agentes devolveram. O transcrito
vivo mora em `~/.claude/projects/`, sob o nome ANTIGO da pasta
(`…-dev`), e some quando a sessão for limpa. O que está aqui, fica.


---

## Agente 1

{
  "aba": "09-sistema.html — pacote /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/pacotes/a09_sistema.py, gerador /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/aba09.py",
  "ligados": 1,
  "metodos": [
    "machine.declare — NOVO, o gesto `perfil-da-mesa` (os três botões do Perfil de Bateria). Via p.machine_declare({\"orcamento\": {\"teto\": …}}), a MESMA função que o \"Aplicar\" do rodapé usa (app/actions/footer_actions.py:356). A tradução botão→disco sai de secao_orcamento.TETO_POR_PERFIL:137, nunca digitada: tudo_ligado→'balanceado', bateria_longa→'economia', eu_escolho→None.",
    "daemon.resume — JÁ ESTAVA (gesto `retomar`), não toquei.",
    "daemon.reload — JÁ ESTAVA (gesto `atualizar`), não toquei."
  ],
  "sem_dono": [
    "reiniciar: é `systemctl --user restart` em subprocess, não IPC — daemon_actions.py:2277 (on_daemon_service_restart, subprocess.run em thread worker).",
    "desligar: é `self._run_systemctl_async(\"stop\")`, não IPC — daemon_actions.py:2234. E a régua PROÍBE ligá-lo: tests/unit/test_os_botoes_tem_dono.py:test_um_botao_sem_dono_devolve_none afirma que ele tem de continuar sem dono.",
    "autostart: é `systemctl --user enable/disable`, não IPC — daemon_actions.py:2398 (on_daemon_autostart_toggled).",
    "refazer-consertos: roda `bash scripts/disable_steam_input.sh --apply-quiet` e `bash scripts/fix_wireplumber_default_source.sh --install` por subprocess, não IPC — daemon_actions.py:1218.",
    "refazer-proton: abre diálogo GTK e mexe no `config.vdf` da Steam em disco, não IPC — daemon_actions.py:1793 (on_proton_lock).",
    "procurar-camadas: censo do `system.reg` de cada prefixo Proton em disco + diálogo GTK, não IPC — emulation_actions.py:2075 (on_camadas_engasgo).",
    "restaurar-de-fabrica: copia o asset `meu_perfil.json` e recarrega o DraftConfig, com diálogo de confirmação, não IPC — footer_actions.py:1477 (on_restore_default).",
    "ver-detalhes: é `journalctl --user -u <unit> -n 80` por subprocess, não IPC — daemon_actions.py:2387 chama _journalctl_tail, definido em daemon_actions.py:2767. Não existe método de log entre os 39 que daemon.metodos() lê do ipc_server.py.",
    "ver-plugins: ESTE É DE OUTRA ESPÉCIE, e não o liguei por outro motivo. O daemon ATENDE `plugin.list` e `plugin.reload` (ipc_server.py:184-185; handlers em ipc_handlers.py:5348 e 5361). O que falta é o CAMINHO DE VOLTA: o gesto do piloto devolve None (hefesto_vivo.py:_gesto, ~linha 320) e não há por onde escrever a lista na página. Um gesto que chamasse `plugin.list` e jogasse o resultado fora seria o botão \"Ver os plugins carregados\" que não mostra plugin nenhum — o botão que responde calado, exatamente o que a instrução manda evitar. E `plugin.reload` levanta ValueError('plugins não habilitados neste daemon') quando o subsistema está desligado, que é o padrão; o `p.chamar` engoliria isso num False e o piloto imprimiria \"aplicado\"."
  ],
  "testes_verdes": true,
  "mordida": "Duas mordidas, as duas reprovaram.\n\n[1] Troquei o corpo do `perfil-da-mesa` (as três linhas do machine_declare + o raise) por `pass  # MORDIDA` e rodei `HEFESTO_VARIANTE=dev .venv/bin/python -m pytest tests/unit/test_os_botoes_tem_dono.py -q`. Saiu 2 failed, 16 passed, e a mensagem exata foi:\n\n  FAILED tests/unit/test_os_botoes_tem_dono.py::test_o_gesto_chama_a_funcao_certa[a09_sistema-perfil-da-mesa0]\n  AssertionError: 09-sistema.html:perfil-da-mesa não chamou NADA. É o defeito que esta régua existe para pegar: o gesto registrado que não faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro.\n  assert []\n   +  where [] = <tests.unit.test_os_botoes_tem_dono.PonteDeMentira object at 0x7ff19fadb1d0>.chamadas\n\n[2] Essa mordida só prova que o gesto CHAMA. Mordi também o PAYLOAD, para ver se a régua mede o VALOR: troquei `{\"orcamento\": {\"teto\": teto}}` por `{\"orcamento\": {\"teto\": \"max\"}}` — um valor que o schema aceita, de propósito. Reprovou nas duas provas:\n\n  AssertionError: perfil-da-mesa: passou ({'orcamento': {'teto': 'max'}},) a machine_declare, esperava ({'orcamento': {'teto': 'economia'}},)\n  AssertionError: perfil-da-mesa: passou ({'orcamento': {'teto': 'max'}},) a machine_declare, esperava ({'orcamento': {'teto': None}},)\n\nDesfeitas as duas: `diff` do arquivo contra a cópia intacta guardada no scratchpad diz IDÊNTICO, e a régua voltou a 18 passed, 6 skipped.\n\nEstado final do meu território: `.venv/bin/ruff check layout/_ferramentas/pacotes/a09_sistema.py` → All checks passed; as 8 provas da aba09 (`-k \"a09_sistema or inventario or ponte_nao_tem or nao_atende\"`) → 8 passed; `scripts/check_o_desenho_aprovado.py` → \"OK: o produto não está atrás do desenho dela\". O único vermelho do arquivo de teste na rodada inteira é `a02_controles-mudo0`, do agente da aba 02, que está com 220 linhas novas não commitadas — não é meu.",
  "achados": [
    "O ENUNCIADO DA TAREFA CARREGAVA UM FATO ERRADO, e ele decidia o método. O prompt diz \"autoswitch_lock_set está na ponte (o 'perfil-da-mesa')\". Não é. `autoswitch.lock` congela a TROCA AUTOMÁTICA DE PERFIL — é um booleano; handler em src/hefesto_dualsense4unix/daemon/ipc_handlers.py:2377, ponte em src/hefesto_dualsense4unix/app/ipc_bridge.py:198. O `perfil-da-mesa` são TRÊS botões de escolha única com os rótulos de `ROTULOS_DOS_PERFIS` (\"Tudo ligado / Bateria longa / Eu escolho\"), sob o título \"Perfil de Bateria\", e o dono declarado pelo produto é `secao_orcamento._ao_escolher:468`, que monta `{\"orcamento\": {\"teto\": …}}` → `machine.declare`. Três escolhas não cabem num booleano. Liguei o que o produto diz, não o que o prompt disse. E `autoswitch.lock` NÃO TEM BOTÃO nesta aba: a linha \"Trocar de perfil ao abrir o jogo\" (data-id `hefesto-troca-de-perfil`) é linha de ESTADO, sem interruptor, e o valor dela sai de `window_detect_healthy`, não de `autoswitch_locked`.",
    "O `data-perfil` DOS TRÊS BOTÕES ERA UM ENDEREÇO MORTO — layout/_ferramentas/aba09.py:624, antes da minha mudança. O ouvinte de clique do piloto encaminha uma lista FIXA de campos ao Python (layout/_ferramentas/hefesto_vivo.py:191-207: gesto, modo, forca, player, lado, campo, hef, hex, sensor, rota, mudo, micModo, v, controle, texto) e `perfil` não está nela. Os três botões pareciam endereçados e chegariam do outro lado indistinguíveis — o mesmo clique. Troquei por `data-v`, que o piloto já capta e que a aba Conexões já usa (layout/_ferramentas/aba08.py:1791), e NÃO guardei os dois: um atributo morto ao lado do vivo é a próxima pessoa lendo o errado. Atualizei junto a régua interna do gerador (aba09.py:1253), que casava por `data-perfil` e teria ficado VERDE sobre a mudança sem notar. Gerei e publiquei: uma linha mudou em mockup/09-sistema.html e o `--publicar 09` a levou a layout/09-sistema.html.",
    "O CONTRATO DO PRODUTO ERRA SOBRE O `ver-detalhes`, e o erro está em DOIS lugares do mesmo arquivo. `src/hefesto_dualsense4unix/gui/aba_sistema.py`, no GESTOS[\"ver-detalhes\"], afirma que `on_daemon_view_logs` \"mostra OUTRA coisa: o `systemctl status`, não as últimas linhas de registro que o desenho pediu\"; e o SEM_FONTE[\"registro-texto\"] repete: \"o que existe é `on_daemon_view_logs`, que mostra `systemctl status` da unidade\". O código não faz isso: daemon_actions.py:2393 chama `self._journalctl_tail(SERVICE_NORMAL, lines=80)`, e `_journalctl_tail` (daemon_actions.py:2767) roda `journalctl --user -u <unit> -n 80 --no-pager` — exatamente as 80 linhas que o desenho pediu (\"Joga as últimas 80 linhas do registro técnico no painel ao lado\"). O botão faz o que promete; é o registro que diz que não. Não corrigi porque `src/` está fora do meu território — mas a decisão que o SEM_FONTE atribui a ela (MIGRA-SISTEMA-10, \"ou o painel passa a dizer que é o systemctl status, ou nasce um método de log\") está formulada sobre um fato falso. Continua verdade que não há IPC de log, e por isso o botão fica sem gesto.",
    "O COMENTÁRIO DO PRÓPRIO PACOTE CONTAVA SETE E SÃO OITO. O a09_sistema.py dizia \"OS SETE QUE NÃO SÃO IPC\" e listava reiniciar, desligar, autostart, refazer-consertos, refazer-proton, procurar-camadas, restaurar-de-fabrica — o `ver-detalhes` ficou de fora da lista sem ficar de fora do problema. Reescrevi o bloco com os oito, cada um com o arquivo e a linha onde medi, e com o `ver-plugins` separado, porque a razão dele é outra (o daemon atende; falta o caminho de volta).",
    "O DUBLÊ DA RÉGUA FORÇA CÓDIGO DEFENSIVO NO GESTO. `PonteDeMentira.__getattr__` (tests/unit/test_os_botoes_tem_dono.py) devolve a dupla `(True, None)` só quando o nome termina em `_set` E contém `identity`; para todo o resto devolve `True`. Mas `ipc_bridge.machine_declare:861` devolve `(ok, motivo)` — e o motivo importa: `versao_desconhecida` quer dizer \"não gravei nada, os bytes ficaram intactos\", ou seja, o botão aceso na tela passaria a mentir. Escrever `ok, motivo = p.machine_declare(...)` rebentaria com TypeError NA RÉGUA e funcionaria na mão dela. Resolvi dentro do meu território com `_ok_e_motivo()`, que aceita tupla ou bool, e documentei por quê no docstring. É contorno de DUBLÊ, não de produto: quando a PonteDeMentira aprender quais funções devolvem `(ok, motivo)`, esta função sai.",
    "OS DOIS GESTOS QUE JÁ ESTAVAM LIGADOS ENGOLEM A RECUSA DO DAEMON. `retomar` e `atualizar` fazem `p.chamar(\"daemon.resume\")` / `p.chamar(\"daemon.reload\")` e descartam o retorno; `ponte.chamar` devolve `bool` e o piloto imprime `→ aplicado` sempre que o gesto não levanta. Logo, daemon offline ou recusa do handler saem na tela como sucesso — a mesma doença que a régua e o guia perseguem. O conserto existe e está ao lado: `ponte.chamar_detalhado`, que devolve `(ok, motivo)` traduzido. NÃO MEXI, porque a instrução foi \"não os toque, apenas acrescente\" — fica registrado para quem tiver a palavra sobre eles.",
    "PROVEI A DECLARAÇÃO PARCIAL CONTRA O SCHEMA, sem daemon: `MaquinaConfig.model_validate({\"orcamento\": {\"teto\": X}})` aceita 'balanceado', 'economia' e None — é a mesma linha que o `gravar_maquina_com_descartes:772` roda antes de gravar. E `fundir_declaracao({\"mesa\": …, \"orcamento\": {\"teto\": \"economia\"}}, {\"orcamento\": {\"teto\": None}})` devolve a mesa intacta com o teto em None: o \"Eu escolho\" apaga o teto sem apagar o que as outras seções declararam. Por isso a prova do `eu_escolho` manda o `None` PRESENTE e não a chave ausente — omiti-la deixaria o teto velho em disco com o botão aceso dizendo que não há teto (fundir_declaracao, utils/maquina.py:650)."
  ]
}


---

## Agente 2

{
  "aba": "05-vibracao.html",
  "ligados": 3,
  "metodos": [
    "rumble.policy_set (via ponte.rumble_policy_set_checked)",
    "rumble.set (via ponte.rumble_set_checked)",
    "rumble.stop (via ponte.rumble_stop e ponte.rumble_stop_checked)",
    "rumble.passthrough (via ponte.rumble_passthrough)",
    "controller.target.set (degrau 3, p.chamar — o ipc_bridge nao cita 'target' uma unica vez)"
  ],
  "sem_dono": [
    "lado — os 4 interruptores de motor esquerdo/direito (data-papel=\"lado\"): NAO existe endereco por LADO em lugar nenhum do produto. O daemon nao tem metodo (censo: rumble.set le so weak/strong), e a unica camada por peca e set_rumble_scales, que e UM float por uniq — core/backend_pydualsense.py:3820 e _escalar_rumble:3797 multiplicam os DOIS motores pelo mesmo fator. A dica publicada promete o contrario ('desligue um lado e o jogo deixa de fazer aquele punho tremer — o outro continua', aba05.py:657) e essa promessa nao tem dono.",
    "motor — as 4 barras de valor (data-papel=\"motor\", data-lado=e|d): a barra e leitura, nao cursor. O ouvinte unico do piloto manda so os data-* do elemento (hefesto_vivo.py:202-207), nunca a posicao do clique, entao nao ha numero a mandar ao rumble.set. Ligar isto exigiria um cursor no desenho e um campo novo no ouvinte — os dois fora do meu territorio.",
    "forca sem degrau — as 2 barras 'Personalizado' (data-papel=\"forca\" SEM data-forca): rumble.policy_custom exige 'mult' (daemon.parametros -> ('mult',)) e um clique em barra sem cursor nao traz numero. O gesto forca EXISTE e RECUSA dizendo a frase inteira; nao responde calado.",
    "desenho (4) e identidade (2) — nao sao botoes, mas o ouvinte os capta por data-papel. Ficam sem gesto de proposito: o piloto imprime '[gesto sem dono] 05-vibracao.html · desenho', que e o estado honesto."
  ],
  "testes_verdes": true,
  "mordida": "DUAS mordidas, ambas desfeitas e reconferidas. (1) Arranquei o alvo: em _mirar() troquei p.chamar(\"controller.target.set\", index=_indice(ctx, uniq)) por 'pass  # MORDIDA'. A regua reprovou DOIS gestos, com estas mensagens exatas: \"AssertionError: 05-vibracao.html:testar chamou ['rumble_set_checked', 'rumble_stop', 'rumble_passthrough'], esperava ['chamar', 'rumble_set_checked', 'rumble_stop', 'rumble_passthrough']. A ORDEM importa.\" e \"AssertionError: 05-vibracao.html:parar chamou ['rumble_stop_checked', 'rumble_passthrough'], esperava ['chamar', 'rumble_stop_checked', 'rumble_passthrough']. A ORDEM importa.\" — 2 failed, 2 passed. (2) Como a primeira mordida deixou 'forca' verde, mordi tambem: troquei a chamada por 'ok, motivo = True, None  # MORDIDA'. Mensagem exata: \"AssertionError: 05-vibracao.html:forca nao chamou NADA. E o defeito que esta regua existe para pegar: o gesto registrado que nao faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro.\" — 1 failed. Restaurado do backup em /tmp/claude-1000/-mnt-Apate-Desenvolvimento-hefesto-dualsense4unix-dev/4d211abb-52de-46ad-83fc-f5e1943569e6/scratchpad/a05_intacto.py; depois: ruff 'All checks passed!' e 4 passed nos casos a05 (33 passed / 3 skipped / 0 failed no arquivo inteiro). ATENCAO: o orquestrador (ou um hook) rodou 'git add' enquanto a mordida (1) estava no disco e STAGEOU o arquivo mordido. Refiz o 'git add' do meu arquivo depois de restaurar; conferido com 'git show :layout/_ferramentas/pacotes/a05_vibracao.py | grep -c MORDIDA' -> 0.",
  "achados": [
    "A VIBRACAO NAO TEM ENDERECO NO IPC, e isso muda todo gesto desta aba. Medido com pacotes/daemon.parametros: rumble.set('weak','strong'), rumble.stop(), rumble.policy_set('policy'), rumble.passthrough('enabled') — nenhum recebe uniq. Quem escolhe o controle e o ALVO DE OUTPUT, que o handler congela junto do par (daemon/ipc_handlers.py:4294, rumble_active_uniq = uniq_do_alvo_de_output) e que o rumble.stop tambem le (:4367). Sem alvo o padrao e BROADCAST (ipc_handlers.py:4133). Por isso 'Testar' e 'Parar' mandam controller.target.set ANTES — sem esse passo os quatro controles tremeriam e a coluna, que e o endereco desta aba, estaria mentindo. E o mesmo par de passos da janela estavel (status_actions.py:2452 + rumble_actions.py:1019), so que sem seletor.",
    "A FORCA E DA MESA, NAO DA COLUNA — e o produto ja sabia. app/actions/rumble_actions.py:911 escreve com todas as letras: 'nao ha IPC de politica por unidade, e inventar um seria mecanismo novo'. O desenho endereca por coluna ('O endereco do ajuste e a coluna, nao a fita', MIOLO do aba05.py) e clicar 'Economia' na coluna do P2 muda os QUATRO. Liguei assim mesmo porque o clique chega ao aparelho e muda a intensidade de verdade; o que falta e o escopo, e o que vale por peca so chega pelo perfil (set_rumble_scales). Isto precisa da palavra dela.",
    "DEFEITO VIVO NA PINTURA DESTA ABA, que eu NAO consertei: o pacote emite mesa['forca'] = st['rumble_policy'] e o piloto pinta todo [data-papel=\"forca\"] (hefesto_vivo.py:132). Sao os 8 botoes de degrau MAIS as 2 barras 'Personalizado'. Com o daemon vivo, os rotulos 'Economia'/'Balanceado'/'Maximo'/'Auto' viram todos a mesma palavra (ex.: 'balanceado'), e a barra que devia mostrar 150% mostra 'max'. Nao mexi porque trocar as chaves do pacote derrubaria o piso de 9 enderecos casados que test_o_casamento_das_dez.py:49 exige, e isso e decisao de quem coordena, nao minha.",
    "O PAR DE TESTE 160/220 NAO TEM DONO. Ele e literal dentro de app/actions/rumble_actions.py:1012-1013 (on_rumble_test_500ms) e aparece em prosa em outros seis arquivos (subsystems/rumble.py:183, ipc_handlers.py:4323, lifecycle.py:220, backend_pydualsense.py:537...). Declarei PAR_DE_TESTE no meu modulo com a citacao da origem — sao duas copias, e a segunda nasce declarada para a proxima pessoa achar com um grep. Se alguem quiser um dono unico, o lugar e src/, fora do meu territorio.",
    "O DUBLE DA REGUA REPROVA POR FORMA, NAO POR DEFEITO. PonteDeMentira.__getattr__ (tests/unit/test_os_botoes_tem_dono.py:96) devolve tupla SO quando o nome termina em '_set' e contem 'identity'; para toda funcao *_checked ele devolve bool. Um gesto que faz 'ok, motivo = p.rumble_set_checked(...)' — que e o certo, e o que a janela estavel faz — rebenta com TypeError na regua. Curei no meu lado com o normalizador _resposta(), que aceita as duas formas de retorno do ipc_bridge. Se outra aba usar funcoes *_checked, vai bater no mesmo muro.",
    "CONTRADICAO DE TEXTO AINDA DE PE sobre a duracao do 'Testar'. Decisao dela em 30/08: 'ali vai ser so Testar; se o user quiser parar vai clicar em Parar' — e o MESMO comentario do gerador registra que 'o meio segundo continua sendo o que o gesto manda ao daemon; o que sai e a PROMESSA na tela' (aba05.py, bloco .acoes-col), e a dica publicada diz 'faz aquele controle tremer meio segundo'. Segui os dois textos publicados (500 ms, com stop+passthrough no fim, exatamente o _rumble_test_stop de rumble_actions.py:1214-1215). Mas o par Testar/Parar fica quase sem funcao se o teste se apaga sozinho: nao da para clicar 'Parar' dentro de meio segundo. Vale perguntar a ela.",
    "'PARAR' AQUI FAZ O QUE NA JANELA ESTAVEL SAO DOIS BOTOES. Na janela estavel, 'Parar' = rumble_stop_checked (fixa (0,0) e o laco re-afirma o silencio) e existe um SEGUNDO botao, 'Devolver ao jogo' = rumble_passthrough(True) (rumble_actions.py:1111). Esta aba nao tem o segundo. Se 'Parar' so travasse o silencio, o controle ficaria mudo para o jogo sem caminho de volta na tela — estado morto, que a casa proibe. A dica publicada ja promete os dois ('corta a vibracao dele agora E DEVOLVE A MAO AO JOGO'), entao juntei os dois passos num gesto so.",
    "O INDICE DO ALVO NAO E O UNIQ, e o FALSO da regua nao o traz. controller.target.set recebe 'index' (0-based, posicao em list(self._handles), core/backend_pydualsense.py:5107), nunca uniq — ipc_handlers.py:4137 rejeita qualquer outra coisa. O FALSO de test_os_botoes_tem_dono.py:41 nao tem a chave 'index', entao _indice() cai na posicao do controle em ctx.conectados — o mesmo recurso que o proprio daemon usa em _numero_de_exibicao (ipc_handlers.py:526). Com o daemon de verdade o index vem na entrada e e ele que vale (conferido com um controle de index=3).",
    "FORA DO MEU TERRITORIO, mas medido agora e vermelho: 'ruff check layout/_ferramentas/pacotes/' reprova com F821 'Undefined name _escolhido' em a10_perfis.py:57 (outro agente, meio de edicao). Meu arquivo passa sozinho ('All checks passed!'). Durante a leva o a01_jogar.py:103 (I001) e o a02_controles.py:165 (F841, um 'pass # MORDIDA' esquecido no lugar, que derrubava o gesto 'rota' da regua) tambem estiveram vermelhos e ja fecharam."
  ]
}


---

## Agente 3

{
  "aba": "01-jogar.html",
  "ligados": 5,
  "metodos": [
    "native.mode.set",
    "gamepad.emulation.set",
    "mouse.emulation.restore",
    "coop.sync",
    "identity.renumber"
  ],
  "sem_dono": [
    "modo-steam (chip \"Steam Input\") — nenhum dos 39 metodos do ipc_server.py liga Steam Input, e o degrau nao e IPC: UseSteamControllerConfig so sobrevive com a Steam FECHADA (a Steam regrava o localconfig.vdf ao sair — integrations/steam_input_ponte.py), entao ligar exige fechar a Steam, reabrir a Steam e reabrir o jogo (integrations/ponte_escada.py, secao OS DOIS TRAMOS). MARQUEI o chip com data-gesto=\"modo-steam\" e NAO registrei @gesto: assim o piloto recusa dizendo o nome (\"[gesto sem dono] 01-jogar.html · modo-steam\") em vez de o clique sumir calado.",
    "mascara (os tres chips DualSense / Xbox 360 / Nintendo Pro dentro de CADA cartao) — a mascara do gamepad virtual e UMA para a maquina, nao uma por controle: gamepad.emulation.set recebe 'flavor' e nao recebe 'uniq' (daemon/ipc_handlers.py:5060, sem uniq na assinatura; daemon.parametros devolve ('enabled','flavor')). E \"Nintendo Pro\" nao e mascara do produto: o portao de entrada RECUSA em voz alta tudo o que nao for dualsense/xbox e sinonimos (ipc_handlers.py:5090-5117, \"nome desconhecido e erro, nao default\"). Nao ha external_mask no IPC. Marcados com data-gesto=\"mascara\" e sem @gesto — so nos cartoes CONECTADOS; os dois lugares vazios ficaram sem endereco de proposito (sem controle nao ha sequer o que pedir).",
    "Aplicar / Salvar Perfil / Importar / Exportar (rodape) — nao sao da aba 01: saem de layout/_ferramentas/topo.html, que e o esqueleto das dez, e ja ganharam dono em layout/_ferramentas/pacotes/rodape.py por outro agente (commit daf3e42d). Fora do meu territorio, nao toquei.",
    "A fita \"Selecionar: Todos / P1...\" do cabecalho — tambem e do topo.html. O metodo que a serviria (controller.target.set) e citado no meu enunciado, mas ele NAO e da aba 01."
  ],
  "testes_verdes": true,
  "mordida": "MORDIDA 1 (a prescrita) — em /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/pacotes/a01_jogar.py troquei o corpo de _aplicar() de `p.chamar(metodo, **params)` por `pass`. A regua reprovou os CINCO gestos de modo de uma vez. Mensagem exata: \"AssertionError: 01-jogar.html:modo-navegacao nao chamou NADA. E o defeito que esta regua existe para pegar: o gesto registrado que nao faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro.\" / \"assert []\" / \"+  where [] = <tests.unit.test_os_botoes_tem_dono.PonteDeMentira object>.chamadas\" — sumario \"5 failed, 28 passed, 3 skipped in 0.96s\", com FAILED em a01_jogar-hefesto0, hefesto1, modo-dualsense, modo-xbox e modo-navegacao. DESFEITO, e a suite voltou verde.  |  MORDIDA 2 (porque a primeira nao provava tudo) — a primeira mostra que o gesto chama ALGO; queria provar que a regua tambem pega chamar a COISA ERRADA, ja que os cinco planos saem da mesma funcao. Troquei `_aplicar(p, _plano_do_chip(\"xbox\"))` por `_plano_do_chip(\"dualsense\")` no gesto modo-xbox. Mensagem exata: \"AssertionError: modo-xbox: mandou flavor='dualsense', esperava 'xbox'\" / \"assert 'dualsense' == 'xbox'\" — sumario \"1 failed, 32 passed, 3 skipped in 0.95s\". DESFEITO. Estado final conferido: ruff em layout/_ferramentas/pacotes/ \"All checks passed\"; pytest tests/unit/test_os_botoes_tem_dono.py \"39 passed, 2 skipped\"; check_o_desenho_aprovado.py nao lista mais 01-jogar.html (a unica atrasada e 06-navegacao.html, de outro agente).",
  "achados": [
    "A PONTE NAO TEM FOLGA DE TEMPO, e isso limita o que estes botoes podem prometer. layout/_ferramentas/pacotes/ponte.py:108 faz `_b._safe_call(metodo, params)` SEM timeout, herdando o default de 250 ms — que desde o BUG-IPC-READ-NO-TIMEOUT-01 cobre tambem a LEITURA da resposta (src/hefesto_dualsense4unix/app/ipc_bridge.py:63-83). Mas trocar de modo cria uinput e faz grab, e o produto declara 2,0 s para isso (app/actions/mode_transition.py:37, MODE_IPC_TIMEOUT_S, cujo comentario diz: \"sem folga o toast dizia 'Falha' com o modo JA aplicado\"); profile.switch teve a mesma cicatriz e ganhou 3,0 s (ipc_bridge.py:49, PROFILE_SWITCH_TIMEOUT_S). Consequencia medida no codigo: p.chamar(\"gamepad.emulation.set\", ...) pode voltar False com o modo aplicado. Por isso _aplicar NAO levanta com o retorno — levantar reintroduziria o defeito exato que o MODE_IPC_TIMEOUT_S curou. A cura e `chamar` aceitar timeout=, e ela mora em ponte.py, fora do meu territorio. Deixei escrito em a01_jogar.ACHADO_DO_TIMEOUT.",
    "O VOCABULARIO QUE A ABA JA USAVA NAO ALCANCA O CLIQUE. Os chips de modo tem data-degrau=\"dualsense|xbox|steam|navegacao\" desde 31/08, mas o ouvinte unico do piloto (layout/_ferramentas/hefesto_vivo.py:192-207) so transporta um conjunto FECHADO de atributos e data-degrau nao esta nele: o clique chegava ao Python com o nome 'clique' e sem dizer em qual chip. Por isso a chave foi para DENTRO do proprio data-gesto (\"modo-<chave>\") em vez de inventar um atributo — data-degrau continua sendo a identidade da fileira e o endereco da pintura.",
    "ARMADILHA PARA QUEM LIGAR A PROXIMA ABA: data-v E transportado pelo piloto (hefesto_vivo.py:203) e o COMO-LIGAR-UMA-ABA.md o lista entre os que ele capta — mas data-v NAO esta na lista INVISIVEIS de scripts/check_o_desenho_aprovado.py:99-112. Marcar botao com data-v faz o portao do desenho acusar divergencia VISUAL onde so ha endereco, e a reacao natural seria desligar o portao. O mesmo vale para data-hex, que a aba 04 ja usa. Os seguros sao os nomeados naquela regex (data-gesto, data-modo, data-degrau, data-mascara, data-papel, data-hef...).",
    "PRE-EXISTENTE, e nao e meu: layout/_ferramentas/aba01.py emite tres marcadores `# noqa-acento` PARA DENTRO do HTML gerado (linhas 430, 443 e 1029 do gerador, dentro de comentarios CSS e de um <li>). Provei que e anterior a mim regerando o gerador do HEAD para um diretorio de desvio (HEFESTO_BANCADA) e comparando com git show HEAD:layout/01-jogar.html: as tres linhas ja saiam diferentes. Ou seja, o gerador foi editado e nunca rodado — exatamente o buraco que onde._DESVIO nasceu para pegar. Os marcadores so precisam existir no .py: scripts/validar-acentuacao.py:450 lista EXTENSOES_ALVO e .html NAO esta la. Sao invisiveis na tela (estao dentro de comentarios), mas foram junto na publicacao de 01 porque nao consegui separa-los sem reescrever prosa que nao e minha.",
    "O NOME DO BOTAO DIZ O CONTRARIO DO QUE ELE FAZ, e o produto ja tinha decidido isso: \"Reconectar Controles\" NAO reconecta rádio. integrations/gesto_de_reconexao.py:15-19 e explicito — \"Nao reconectar e decisao dela (...) o botao PS e dela; reconectar() nao existe aqui de proposito. Um Connect nosso devolveria o controle sem o gesto fisico\". O que o botao e, de verdade, esta na propria legenda da aba (aba01.py:1028: \"'Reconciliar jogadores' virou 'Reconectar Controles'\") e no botao antigo home_actions._on_home_reconciliar_clicked:3112 — coop.sync e DEPOIS identity.renumber, encadeados. Liguei nessa ordem, e a recusa do renumber com jogo aberto NAO vira erro (os jogadores ja voltaram no passo 1 — mesma regra do reported_step_index).",
    "OUTRO METODO CUJO NOME ENGANA, e ele estava na minha lista de candidatos: controller.target.set parece o seletor de mascara por controle e NAO e — ele define o ALVO das acoes de output (lightbar, gatilho, player-LED, rumble) para um indice de controle (daemon/ipc_handlers.py:4124-4150). Quem o usaria e a fita \"Selecionar:\" do topo.html, nao os chips desta aba. daemon.emulation.suppress, tambem da minha lista, e o toggle do \"modo jogo\" do long-press do PS (ipc_handlers.py:5227) — nada nesta aba mapeia nele. Nenhum dos dois foi usado.",
    "AS PROVAS FICARAM LITERAIS DE PROPOSITO. A tentacao era monta-las chamando o mesmo _plano() que o gesto chama (\"nao digitar o que tem dono\"), e isso custaria duas coisas: (1) a regua viraria tautologia — os dois lados perguntariam ao mesmo plan_mode_transition, e o chip Xbox pedindo mascara dualsense daria VERDE (foi exatamente o que a MORDIDA 2 pegou); (2) PROVAS e lido no import, e monta-lo com _plano() puxaria painel -> home_actions -> GTK para dentro do import das dez abas, quebrando a pureza que pacotes/__init__ declara. O preco esta declarado no arquivo: se o produto mudar a sequencia de um modo, esta lista reprova — e essa e a noticia certa.",
    "Outro agente COMMITOU o meu gerador no meio do trabalho (commit daf3e42d, do rodape, via git add -A): git diff HEAD de layout/_ferramentas/aba01.py voltou vazio. Conferi que as 8 marcas data-gesto estao tanto no HEAD quanto na arvore. Nao commitei nada."
  ]
}


---

## Agente 4

{
  "aba": "08-conexoes.html",
  "ligados": 4,
  "metodos": [
    "controller.target.set",
    "machine.declare"
  ],
  "testes_verdes": true,
  "sem_dono": [
    "luz-nao-acende (2 botões, \"A luz não acende\"): é `Disconnect` do BlueZ pelo D-Bus, não IPC — integrations/gesto_de_reconexao.py roda `busctl` e o daemon não tem método para isso. Confirmado contra daemon.metodos(): não há nada de bluetooth ali.",
    "examinar-portas (\"Examinar Portas\"): o exame é integrations/exame_da_mesa (sysfs + busctl), não IPC. E ele JÁ roda a cada tique dentro de a08_conexoes._exame() — o botão pediria de novo o que a aba refaz 2x por segundo.",
    "ignorar (5 botões ⊘): dispensar grava mesa.ordens_dispensadas[chave]={quando,arranjo} (secao_exame.py:995), e a chave/arranjo são de um ordens_da_mesa.Ordem. As linhas do exame desta aba são exame_da_mesa.Item, e só têm .ordem quando a linha É uma ordem — as três que _exame() traz hoje não são. Gravar com arranjo=\"\" PASSA no esquema e não cala nada: ordens_da_mesa.py:850 compara o arranjo guardado com o de agora e nunca casaria. Botão que grava e não cala é pior que botão que recusa.",
    "mic-existe (2 selects): TEM dono no daemon — mic.set, via ipc_bridge.mic_set(muted, uniq), o mesmo que controller_card.py:3562 chama. Não ligado porque é <select> e o piloto só ouve `click` (hefesto_vivo.py:188). O clique que chega é o de ABRIR o popup, com o valor ANTIGO.",
    "mic-escopo (2 selects): é mic_button_toggles_system, e o produto guarda UM por máquina (daemon/lifecycle.py:272, aplicado por ipc_draft_applier.py:592), não por controle como a tela oferece. Contradição já declarada em gui/aba_conexoes.SEM_FONTE. Também é <select>.",
    "teto-da-vibracao (2 selects): a tela oferece teto POR CONTROLE e o produto aplica `min` global (core/rumble.py); gui/aba_conexoes.SEM_FONTE diz que sobrepor mudaria o daemon, não a tela. Também é <select>.",
    "vizinho-o-que-e (4 selects): TEM dono — machine.declare gravando mesa.radios[vid:pid].tipo (secao_mesa._ao_declarar_o_radio:1486). É o ÚNICO desta aba que só espera um ouvinte de `change` no hefesto_vivo.py, que está fora do meu território.",
    "escolher-aparelho (7 botões mm-ap): a lista de aparelhos é a constante CENSO do gerador, não o censo do barramento dela. O pacote não repinta nada disso.",
    "escolher-entrada (11 botões mm-sq): os quadrados saem de FACES/QUEM_ESTA, constantes do gerador. Declarar mapa.portas a partir deles escreveria no maquina.json DELA o desenho de uma bancada de exemplo.",
    "nova-entrada (2), novo-hub (1), tirar-daqui (1), nova-extensao (1): mesma razão — o alvo de cada um é um quadrado/face do mockup, não da mesa dela.",
    "nova-face (1): o nome sai de um <input>, e o ouvinte manda `texto` = textContent do BOTÃO, nunca o `value` do campo (hefesto_vivo.py:208). O gesto chegaria sem o nome.",
    "Renomear (adaptador): não existe mais como botão — virou duplo clique em contenteditable (decisão dela, 31/08). E não é IPC: é org.bluez.Adapter1.Alias, integrations/apelido_do_dongle.renomear_o_dongle:481, por busctl.",
    "Aplicar / Salvar Perfil / Importar / Exportar (4 botões do rodapé): moram em layout/_ferramentas/fim.html, que é UM para as dez abas — fora do meu território. Não têm data-gesto nenhum, então hoje o clique não produz nem recusa."
  ],
  "mordida": "Troquei `p.chamar(\"controller.target.set\", index=indice)` do gesto `alvo` por `pass`, rodei `HEFESTO_VARIANTE=dev .venv/bin/python -m pytest tests/unit/test_os_botoes_tem_dono.py -q -k \"a08_conexoes and alvo\"` e a régua REPROVOU com: «AssertionError: 08-conexoes.html:alvo não chamou NADA. É o defeito que esta régua existe para pegar: o gesto registrado que não faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro. assert [] + where [] = <tests.unit.test_os_botoes_tem_dono.PonteDeMentira object at 0x7f5b1fbf9520>.chamadas» (tests/unit/test_os_botoes_tem_dono.py:205 — 1 failed, 48 deselected). Desfeito e conferido byte a byte contra a cópia de antes (`diff` sem saída); 49 passed de novo.",
  "achados": [
    "layout/_ferramentas/aba08.py:1293 — os blocos de controle desta aba NÃO tinham `data-controle`. Era o único dos seis pacotes por controle sem ele (aba01/03/04/06 já usam), e a consequência ia além do gesto: o `colunas` que a08_conexoes.pacote emite (via, bateria, ponte, fragil) nunca teve onde cair, porque a pintura procura `[data-controle=\"pN\"]`. Acrescentei `data-controle=\"{pref}\"`; o casamento da 08 segue em 5 (o piso), porque os quatro campos por controle continuam órfãos por falta de `data-campo` — isso fica como dívida de quem pintar a aba.",
    "layout/_ferramentas/hefesto_vivo.py:188 — o piloto único instala UM ouvinte, e é de `click`. Isso desliga SEIS campos desta aba de uma vez (os três selects por controle e os quatro dos vizinhos), e um deles — `vizinho-o-que-e` — tem dono pronto no daemon (machine.declare, mesa.radios[vid:pid].tipo). É o maior bloqueio único da 08 e a cura é uma linha fora do meu território: um `change` ao lado do `click`, mandando `alvo.value`.",
    "src/hefesto_dualsense4unix/daemon/ipc_handlers.py:4130 — `controller.target.set` pede `index`, a POSIÇÃO em `controllers`, e não o número do jogador. O próprio produto separa as duas coisas de propósito (status_actions._controller_target_rows:1579 ordena pelo número de identidade e CARREGA o index da enumeração, com o comentário «a usuária clicaria no chip do 1 e editaria outro controle»). Meu `_indice()` lê `entry['index']` e só cai na posição da lista quando o daemon não o publicou; sem index e sem posição, RECUSA em vez de mirar o 0.",
    "layout/_ferramentas/aba08.py:1296 — o `title` das etiquetas promete «A fita do topo passa a apontar para ele», e a fita NÃO se move. O piloto único chama `mesa_viva.mesa_do_estado(st, …)` sem o argumento `alvo` (hefesto_vivo.py:476), então a fita aponta sempre para a posição 1. O efeito do daemon que o gesto entrega é real (lightbar/gatilho/rumble/mic passam a mirar só aquele controle); a promessa da fita é do piloto. Está escrito no docstring do gesto.",
    "mockup/08-conexoes.html — a dica do select do microfone diz «Se o microfone deste controle existe. Desligado, nenhum programa o enxerga». O que o produto tem é `mic.set`, o MUDO no firmware (ipc_handlers.py:4755): a fonte de captura continua existindo no PipeWire, só fica muda. A frase promete mais do que o método faz — não liguei o botão (é select), mas a frase caduca independente disso.",
    "layout/_ferramentas/aba08.py:1905 — os botões «Sim/Não/Não sei» das duas perguntas da sala nascem com o estado escrito à mão no gerador («Sim» marcado na altura, nenhum na visada). O clique agora GRAVA de verdade no maquina.json, mas a tela não relê o disco: o pacote não emite nada para esses dois campos. Ela pode responder «Não» e ver «Sim» aceso. Cura: o pacote emitir `altura_da_antena`/`linha_de_visada` de `utils.maquina.carregar_maquina()`.",
    "tests/unit/test_os_botoes_tem_dono.py:PonteDeMentira — o dublê devolve `(True, None)` só para nomes que casam `endswith('_set') and 'identity' in nome`; para todo o resto devolve `True`. `machine_declare` devolve `(ok, motivo)` de verdade (ipc_bridge.py:861), então desempacotar às cegas levantaria TypeError DENTRO do teste. Escrevi `_resposta()` que tolera as duas formas, com o porquê no docstring — mas o instrumento é que está estreito: quem puder mexer em tests/ devia ensinar o dublê a devolver a dupla para as funções `_checked`/`_detalhado`/`machine_declare` também."
  ]
}


---

## Agente 5

{
  "aba": "03-gatilhos.html",
  "ligados": 2,
  "metodos": [
    "trigger.set",
    "trigger.reset"
  ],
  "testes_verdes": true,
  "sem_dono": [
    "guardar (\"Guardar esse efeito\", um por coluna): NÃO existe método de daemon que guarde um efeito com nome — conferido nos 39 de daemon.metodos(). O `profile.apply_draft` APLICA um rascunho ao vivo, não grava (ipc_handlers.py:903-925); gravar perfil é I/O de disco DENTRO do processo da GUI (app/actions/footer_actions.py:838 on_save_profile → save_profile), fora do IPC. E o destino que o desenho promete — \"Meus efeitos\" — não existe em lugar nenhum de src/ (grep por efeitos_salvos/custom_effects/saved_effect: zero). Ficou com data-gesto=\"guardar\" e SEM @gesto, para o piloto o recusar dizendo o nome (a mesma convenção dos sete da aba Sistema).",
    "As duas opções de \"Meus efeitos\" (\"Recuo do MK — pesado no fim\", \"Freio do carro — trava tardia\"): nasceram com value=\"\" e o gesto `pronto` RECUSA DIZENDO. São ficção do mockup — não há store de efeito nomeado no produto.",
    "A opção \"— Nenhum —\" do efeito pronto: value=\"custom\", que é o token do próprio produto para \"nenhuma curva pronta\" (FEEDBACK_POSITION_LABELS[\"custom\"]). O gesto recusa: não há curva a mandar, e mandar \"de volta ao normal\" confundiria este campo com o \"Desligado\" do campo de cima.",
    "As barras de ajuste (.barra, data-ajuste=\"e-0\"…): são MOSTRADOR, não entrada — o desenho as pinta como trilho com bolinha, sem input. Não há o que ligar até ela decidir onde as dez posições se desenham (a própria legenda da aba diz que isso está em aberto)."
  ],
  "mordida": "Troquei, no gesto `modo`, a linha `ok, motivo = _desfecho(p.trigger_reset_detalhado(lado, uniq=uniq))` por `ok, motivo = True, \"\"` (a chamada ao daemon arrancada, o resto do gesto intacto) e rodei `HEFESTO_VARIANTE=dev .venv/bin/python -m pytest tests/unit/test_os_botoes_tem_dono.py -q -k a03`. A régua reprovou com a mensagem exata: «AssertionError: 03-gatilhos.html:modo não chamou NADA. É o defeito que esta régua existe para pegar: o gesto registrado que não faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro. assert [] +  where [] = <tests.unit.test_os_botoes_tem_dono.PonteDeMentira object>.chamadas» — «FAILED tests/unit/test_os_botoes_tem_dono.py::test_o_gesto_chama_a_funcao_certa[a03_gatilhos-modo1] — 1 failed, 4 passed, 44 deselected». Desfeita e reconferida: 49 passed. ATENÇÃO: entre a mordida e o desfazer alguém rodou `git add -A` e o ÍNDICE ficou com a versão mordida — corrigido com `git add` dos meus quatro arquivos; `git diff` e `git diff --cached` agora batem com a árvore.",
  "achados": [
    "A PINTURA DESTA ABA IA APAGAR OS CAMPOS DE ESCOLHA — o defeito mais grave que achei, e é anterior aos gestos. Os quatro <select> tinham `data-campo` e NÃO tinham `data-hef-alvo`; o `escrever()` do piloto (layout/_ferramentas/hefesto_vivo.py:104-116) usa o alvo padrão `texto`, ou seja `select.textContent = \"Rígido\"` — o que REMOVE as 19 opções do modo e as 8 do efeito pronto e põe um nó de texto no lugar, nas quatro colunas, na PRIMEIRA pintura. Curado: `data-hef-alvo=\"valor\"` nos quatro (aba03.py, função `bloco`), com régua nova no `_conferir` que exige tantos `data-hef-alvo=\"valor\"` quantos `<select>`.",
    "O <select> NÃO ATRAVESSA A PONTE DO PILOTO, e por isso os dois gestos que escrevi ainda não podem disparar de um clique humano. O ouvinte é só de `click` (hefesto_vivo.py:189) e o `manda()` (hefesto_vivo.py:203-209) não manda `value` de elemento nenhum — só os `data-*`. O piloto ANTERIOR desta casa tinha o ramo que falta, e está escrito: layout/_ferramentas/sistema_viva.py:333-336, `if(b.tagName === 'SELECT'){ b.addEventListener('change', ()=>manda({gesto:nome, valor:b.value})); }`. O piloto único não o trouxe. É uma linha em hefesto_vivo.py — FORA do meu território, e afeta pelo menos a 06-navegacao (60 <select>) e a 01-jogar (2). Os gestos já aceitam as duas chaves (`modo`/`v` de hoje e `valor` do change), e há prova declarada para a segunda porta, então o dia em que o ramo voltar eles funcionam sem ninguém lembrar de mexer aqui.",
    "As opções não carregavam o contrato de disco — só o rótulo. `trigger.set` quer `Rigid` e a opção dizia `Rígido`. Curado: `value=\"<name>\"` em cada uma das 19, DERIVADO de trigger_specs.PRESETS pela ORDEM (aba03.py, CHAVE_DO_MODO), com guarda de tamanho. Casar por RÓTULO era impossível: a tabela MODOS da aba diz \"Arco de flecha\" e \"Disparo\", e o produto diz \"Arco de flecha (Bow)\" e \"Disparo (Weapon)\" — as duas decisões dela de 07/08/2026 que esta página nunca acompanhou. DUAS VERDADES VIVAS, e a escolha entre elas é dela.",
    "\"Efeito pronto\" TINHA dono e o pacote dizia que não. Os cinco nomes do desenho (Rampa crescente, Rampa decrescente, Plateau central, Stop hard, Stop macio) são, letra por letra, cinco dos seis `FEEDBACK_POSITION_LABELS` de src/hefesto_dualsense4unix/profiles/trigger_presets.py:55-63, e cada um resolve para dez intensidades 0..8. O pacote pintava ali o RÓTULO DO MODO (`fora[\"pronto\"] = spec.label`, com o comentário \"até existir um segundo eixo, o pronto É o modo\") — o segundo eixo existia desde antes da aba. Corrigido na origem: `pronto` passou a ser a CHAVE, RECONHECIDA comparando a curva salva com FEEDBACK_POSITION_PRESETS (`_pronto_da_curva`), e `custom` quando não é nenhuma.",
    "DECISÃO QUE É DELA, e eu tomei uma provisória: o gesto `pronto` TROCA O MODO para \"Curva de força\" (MultiPositionFeedback). Razão medida: uma curva de dez posições só existe nesse modo, e na GUI estável a linha de preset nem aparece nos outros 17 (triggers_actions._update_preset_row_visibility, `_MODES_COM_PRESET`). O desenho a mostra para os 19. A alternativa que NÃO fiz era calada (mandar dez intensidades num modo sem posições, que o `build_from_name` recusaria sem a tela explicar). Precisa da palavra dela.",
    "O desenho oferece só a lista de curvas de FEEDBACK. O produto tem uma SEGUNDA lista, `VIBRATION_POSITION_LABELS` (Pulso crescente, Machine gun, Galope, Senoide, Vibração final), para o modo \"Vibração por posição\" — e o desenho não tem onde pô-la (na GUI estável o combo é repopulado por modo, `_populate_preset_combo`). Deixei o gesto só na lista de feedback; a de vibração fica sem tela.",
    "\"Desligado\" NÃO é `trigger.set` com modo Off — é `trigger.reset`, e o nome não denuncia. `_handle_trigger_set` termina em `mark_manual_trigger_active(\"trigger\")` (ipc_handlers.py:1240) e `_handle_trigger_reset` em `clear_manual_trigger_active(\"trigger\")` (ipc_handlers.py:1309): mandar Off pelo `set` ARMARIA a trava que pausa a troca automática de perfil. É a R-19, e o gesto `modo` desvia para o reset — com prova declarada, então trocar de volta reprova.",
    "A lista posicional na ordem do spec JÁ É o formato do fio para os três modos \"especiais\" — conferido contra triggers_actions._send_trigger_named:626-657. MultiPositionFeedback tem pos_0..pos_9 (= strengths), MultiPositionVibration tem frequency,pos_0..pos_9 (= [freq,*strengths]) e Custom tem mode,force_0..force_6 (= [mode,*forces]). Ou seja: `preset_to_positional_params(spec, {})` cobre os 19 e não há caso especial a reescrever no gesto.",
    "O modo \"Montar do zero\" (Custom) tem SETE FORÇAS ZERO de padrão (trigger_specs.py, `_pos`/força 0). Escolhê-lo aplica um efeito sem zona ativa — o firmware honra fazendo nada. É o mesmo defeito que a TRIGGER-CANON-01 curou nos dois MultiPosition (dez zeros → _RAMPA_PADRAO) e que ficou de pé neste. Não mexi: é src/, fora do meu território.",
    "`ipc_bridge.trigger_set(side, mode, params)` NÃO aceita `uniq` — só `trigger_set_checked` e `trigger_set_detalhado` aceitam. Numa aba de quatro colunas, usar a versão do enunciado mandaria o gatilho em BROADCAST e escreveria nos quatro controles, que é exatamente o defeito ABAS-06 de 25/07 descrito no docstring do `trigger_reset_detalhado`. Usei a `_detalhado` nas duas rotas, que é também a porta que a GUI estável migrou a usar (ELO-MUDO-01/T3): só ela junta as DUAS formas de recusa — o erro JSON-RPC e a recusa que vem DENTRO de uma resposta bem-sucedida (`_recusa_no_corpo`).",
    "A `PonteDeMentira` da régua devolve `True` (não tupla) para tudo que não seja `identity*_set` — tests/unit/test_os_botoes_tem_dono.py:96. Um `ok, motivo = p.trigger_set_detalhado(...)` rígido rebentaria com TypeError na régua. Escrevi um `_desfecho()` que normaliza as três formas de resposta da ponte (bool, 2-tupla, 3-tupla) — o que é verdade sobre a ponte real, não só sobre o dublê. Registro porque a próxima aba com função `_checked`/`_detalhado` vai esbarrar no mesmo, e o arquivo de teste é território de ninguém."
  ]
}


---

## Agente 6

{
  "aba": "02-controles.html",
  "ligados": 2,
  "metodos": [
    "mic.set",
    "speaker.set"
  ],
  "testes_verdes": true,
  "sem_dono": [
    "Giroscópio (data-sensor=\"giroscopio\", aba02.py:822) — não há método de sensor nos 39 do ipc_server (conferido com daemon.metodos(): só mic.set e mic.volume.set casam 'mic|sensor|gyro|motion'). O daemon/sensor_hub.py só LÊ (leitura/reconciliar/_abrir_readers); não há ligar/desligar. E o perfil não guarda: profiles/schema.py:902 lista giroscopio e acelerometro como \"FORA POR AUSÊNCIA, NÃO POR DECISÃO\", com dona declarada (ONDA-CONTROLES-07) e uma ProfileSensorsConfig que ainda não existe — grep em src/ devolve UMA linha, e é o próprio comentário.",
    "Acelerômetro (data-sensor=\"acelerometro\", aba02.py:823) — idem, e é o MESMO nó evdev do giroscópio (separados só pelo código do eixo), logo o buraco é um só. ATENÇÃO ao motivo errado que quase entrou aqui: NÃO é que o acelerômetro não funcione. docs/data/mapa-controles.csv:175 diz cabo_aciona=SIM e radio_aciona=SIM desde 29/08/2026 (ONDA-CONTROLES-04) — o produto publica inputs.accel. O que falta é o INTERRUPTOR, não a leitura.",
    "Virtual (data-mic-modo=\"virtual\", aba02.py:1107) — o modo do microfone não virou código. grep -riE \"microfone virtual|mic virtual|fonte virtual de captura|virtual_mic\" em src/ devolve ZERO ocorrências; a única fonte é a sprint docs/process/sprints/2026-08-27-ONDA-CONEXOES-06-o-microfone-muda-de-aba.md:153, que lista \"Criar a fonte virtual de captura\" como trabalho A FAZER, e :157 marca a latência como NÃO MEDIDA. Não há método IPC para escolher o modo.",
    "Nativo (data-mic-modo=\"nativo\", aba02.py:1109) — o irmão do anterior, mesma sprint, mesmo zero em src/. Um seletor de dois estados sem nenhum dos dois implementado: ligar só este acenderia um botão que não muda caminho nenhum.",
    "Todo o som do PC (data-rota=\"pc\", aba02.py:1129) — TEM gesto e ele RECUSA DIZENDO, de propósito. São DUAS camadas e só a segunda é IPC: profiles/schema.py:516 escreve o limite (\"LIMITE DECLARADO: a rota é a CAMADA 2 (o firmware). O estado 'Todo o som do PC' da janela também mexe na CAMADA 1 (o default sink do PipeWire), que é um fato GLOBAL do sistema\"), e controller_card.py:4218 diz quem vence (\"A camada 1 vence a camada 2: volume e rota perfeitos num sink mudo é trabalho invisível\"). A camada 1 é pactl set-default-sink, em app/audio_saida.py:820 (RotaDeSaida.mandar_para_o_controle) — não há método no daemon para isso. Mandar só rota=3 escreveria o byte certo e não moveria uma nota de som, com o botão aceso: era o \"responde calado\". O que falta para ligá-lo é dar à janela nova o dono da camada 1 que a velha injeta no card (controller_card.definir_pedido_de_rota, :4310) — e ele NÃO pode ser chamado de dentro do gesto hoje, porque rodaria pactl set-default-sink de verdade dentro da régua e trocaria a saída de som da máquina dela no pytest.",
    "Calibrar Sensores de Movimento (aba02.py:1490) — é <a href=\"calibrar-sensores.html\">, navegação por load_uri, não IPC. Deixado sem gesto conforme o enunciado: é do piloto.",
    "Mapa do Controle (aba02.py:1491) — <a href=\"mapa-do-controle.html\">, idem.",
    "Os DOIS controles deslizantes de volume (o do microfone e o do alto-falante) — não são botão nem têm endereço: são <span class=\"trilho\"><span class=\"cheio\"> (aba02.py:1091 e :1123), sem data-* nenhum, então o ouvinte do piloto nem dispara. E o produto TEM as duas pontes: ponte.mic_volume_set (0-100, camada PipeWire) e ponte.speaker_set(volume=) (0-255, o byte). É o único item desta lista onde falta ENDEREÇO no desenho, não dono no daemon — mas acrescentar interação que o desenho aprovado não tem é decisão dela, não minha."
  ],
  "mordida": "Duas mordidas, as duas na régua HEFESTO_VARIANTE=dev .venv/bin/python -m pytest tests/unit/test_os_botoes_tem_dono.py.\n\nMORDIDA 1 — arranquei a chamada ao daemon do [mic]: troquei `if not p.mic_set(not agora, uniq=uniq):` por `pass  # MORDIDA` + `if False:`. A régua reprovou:\n\n  FAILED tests/unit/test_os_botoes_tem_dono.py::test_o_gesto_chama_a_funcao_certa[a02_controles-mudo0]\n  AssertionError: 02-controles.html:mudo não chamou NADA. É o defeito que esta régua existe para pegar: o gesto registrado que não faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro.\n  assert []\n   +  where [] = <tests.unit.test_os_botoes_tem_dono.PonteDeMentira object at 0x7b40cd235850>.chamadas\n\nMORDIDA 2 — troquei a constante da rota pelo número errado (o vizinho, que é justamente o \"Todo o som do PC\"): `rota=SAIDA_L_FONE_R_ALTO_FALANTE` virou `rota=3`. Reprovou pelo VALOR, não só pela chamada:\n\n  FAILED tests/unit/test_os_botoes_tem_dono.py::test_o_gesto_chama_a_funcao_certa[a02_controles-rota]\n  assert 3 == 2\n   +  where 3 = <built-in method get of dict object at 0x779f6861b340>('rota')\n   +    where <built-in method get of dict object at 0x779f6861b340> = {'rota': 3, 'uniq': 'aa:bb:cc:00:00:01'}.get\n\nAs duas DESFEITAS por cópia byte a byte do arquivo intacto (`diff` devolve idêntico). Depois: ruff em layout/_ferramentas/pacotes/ passou, a régua fechou 39 passed / 2 skipped, e `scripts/portoes.sh --rapido` deu TODOS VERDES — 23 portões.",
  "achados": [
    "DOIS gestos, TRÊS botões que AGEM — para o número `ligados: 2` não ser lido errado. `PISO_DA_ABA` conta gestos registrados, e o gesto `mudo` atende o [mic] e o [nota] (a página marca os dois com o mesmo data-mudo). Os três que agem: [mic] → mic_set, [nota] → speaker_set(muted), \"Sons do jogo\" → speaker_set(rota=2). O quarto, \"Todo o som do PC\", entra no mesmo gesto `rota` e RECUSA com a razão. Provei os três de ponta a ponta lendo o HTML publicado e montando o `o` como o JS o monta: [mic] com mic_mudo=True devolveu mic_set(False) (alterna certo), [nota] com {volume:101,muted:False} devolveu speaker_set(muted=True, volume=101, uniq=…), \"Sons do jogo\" devolveu speaker_set(rota=2, volume=101, uniq=…), e os quatro sem dono saíram como gesto 'clique' → recusado.",
    "SEM `data-gesto`, TODO botão desta aba se chamava `clique`. hefesto_vivo.py:209 monta o nome como `d.gesto || d.hefGesto || d.papel || doRodape || 'clique'` — os data-sensor/data-rota/data-mudo/data-mic-modo que a aba já tinha são ARGUMENTO, não endereço de quem atende. Sem marcar, os oito botões de som e sensor disputariam UM nome e o gesto teria de adivinhar pelo texto. Marquei só os que têm dono; os cinco sem dono ficaram sem `data-gesto` de propósito, e é isso que os faz caírem no `clique` órfão e serem recusados com o nome na tela.",
    "FATO ERRADO NO PRÓPRIO GERADOR, corrigido em aba02.py (bloco D-A-LEITURA-DO-ACELERÔMETRO-SAI-DA-TELA). Ele afirmava, como uma das três fontes que justificavam tirar a leitura do acelerômetro: \"docs/data/mapa-controles.csv, movimento.acelerometro@dualsense: cabo_aciona=não, radio_aciona=não\". Conferido em 01/09: a linha 175 do CSV diz cabo_aciona=SIM e radio_aciona=SIM desde 29/08/2026 (ONDA-CONTROLES-04, depois de ela dizer \"não era pra ele sair. era pra ele FUNCIONAR\"), e a leitura VOLTOU para a tela — o data-bloco=\"acelerometro\" está lá. Não apaguei o bloco (a decisão foi tomada e revertida, e a conta dos 81px é o que impede refazê-la); acrescentei a nota datada com o número certo e o ponteiro para o dono. Eu quase propaguei esse número para este relatório.",
    "CÓDIGO MORTO EM aba02.py:979, e ele fica MAIS perigoso agora que liguei o [mic]: `mic_trava = \"\" if mic_posse else \" disabled\"` é calculado e NUNCA usado — o botão do microfone (linha 1093) não interpola `{mic_trava}`, ao contrário do irmão do alto-falante, que usa `{alto_trava}` (linha 1125). O parâmetro `mic_posse` de `bloco()` (linha 951) portanto não faz nada. É resto do botão \"Liberar\" que ela mandou tirar em 30/08. Quem ler o gerador conclui que o [mic] tem trava de posse; ele não tem — e o mudo do microfone NÃO precisa dela (só o do alto-falante é recusado pelo daemon sem volume conhecido), então a cura provável é apagar as duas linhas. NÃO mexi: `bloco()` é importado de fora (hefesto_vivo/controles_vivos montam a mesa viva com ele) e tirar parâmetro no meio de uma leva com sete agentes é risco sem necessidade.",
    "O RODAPÉ ERA INVISÍVEL AO PILOTO — e foi curado por outro agente no meio do meu trabalho. Quando medi, os quatro botões do rodapé (.r-aplicar, .r-salvar, .r-importar, .r-exportar, que vêm de layout/_ferramentas/fim.html e estão nas DEZ páginas) não tinham data-* nenhum, então o `closest()` do ouvinte devolvia null e o clique não produzia NEM a linha de \"gesto sem dono\": zero, silêncio total. O commit daf3e42d acrescentou `.r-aplicar,.r-salvar,.r-importar,.r-exportar` ao seletor e o nome sai por classe (hefesto_vivo.py:207). Registro porque o defeito era real e porque a cura NÃO passa por data-gesto no fim.html — quem for auditar o rodapé precisa saber que ele é o quarto vocabulário de endereço da casa.",
    "O `speaker.set` RECUSA `muted` sem volume conhecido, e isso é armadilha para quem ligar botão de mudo: ipc_handlers.py:4682 levanta ValueError (\"mudo como primeira escrita tranca o alto-falante em zero e o próprio mudo não o solta\"). O daemon só sabe o volume depois do PRIMEIRO speaker.set, porque o DualSense não devolve o registrador (ipc_handlers.py:4600) — daí a chave `speaker` só aparecer no state_full a partir daí. O desenho já sabia: o [nota] nasce `disabled` quando `alto_pode` é falso. Meu gesto manda o volume junto QUANDO o daemon o publica, e omite quando não — inventar um número aqui esconderia a recusa. É por isso que a PROVA do [nota] declara `chama` SEM volume: o controle da régua vem com `speaker: {}`, que é o estado real de quem nunca recebeu um speaker.set.",
    "O NOME DO MÉTODO ENGANOU DE NOVO, e desta vez para o outro lado: `speaker.set` parece o método do alto-falante e é também o do MICROFONE por tabela — o `rota` dele escreve o common[7], que carrega a rota de saída (bits 4-5) E o caminho do microfone (o resto). backend_pydualsense.py:377 (`_byte_da_rota`) preserva a outra metade, mas só desde a regressão medida em 02/08/2026, quando pedir rota MATOU o microfone (o parec caiu de 131072 bytes para zero). Nenhum gesto meu escreve o byte inteiro — a rota vai pelo kwarg, que é o caminho que preserva.",
    "A ROTA NÃO FOI DIGITADA: `SAIDA_L_FONE_R_ALTO_FALANTE = 2` vem de core/ds_output_report.py:136, que é módulo puro. O outro lugar onde o número tem nome é app/widgets/controller_card.py:716 (ROTA_DO_CANAL), e aquele importa GTK — pegá-lo de lá puxaria Gtk para dentro da régua."
  ]
}


---

## Agente 7

{
  "aba": "10-perfis.html",
  "ligados": 3,
  "metodos": [
    "profile.switch",
    "launch_env.refresh"
  ],
  "testes_verdes": true,
  "mordida": "DUAS mordidas, porque o gesto novo nao cabe na regua declarativa.\n\n(1) NA REGUA - troquei `if not p.profile_switch(nome): raise ...` por `pass` em `ativar` e rodei `HEFESTO_VARIANTE=dev .venv/bin/python -m pytest tests/unit/test_os_botoes_tem_dono.py -q`. Reprovou em `test_o_gesto_chama_a_funcao_certa[a10_perfis-ativar]`, mensagem exata:\n\n  AssertionError: 10-perfis.html:ativar nao chamou NADA. E o defeito que esta regua existe para pegar: o gesto registrado que nao faz nada passa por qualquer teste de registro, e na tela o clique some sem uma linha de erro.\n  assert []\n   +  where [] = <tests.unit.test_os_botoes_tem_dono.PonteDeMentira object at 0x7a47c58fa7b0>.chamadas\n\nDesfeita: 49 passed.\n\n(2) NA MEDICAO PROPRIA - troquei `restaurar_do_historico(nome)` por `pass` em `voltar-a-de-ontem` e rodei a prova com pasta de perfis de verdade. Mensagem exata:\n\n  AssertionError: o perfil NAO voltou: prioridade 7\n\nE o que ela revelou e pior que o vermelho: com o disco arrancado, as DUAS chamadas ao daemon sairam assim mesmo - `chamadas: [('profile_switch', ('Prova',)), ('chamar', ('launch_env.refresh',))]`. Ou seja, o piloto teria impresso `-> aplicado` sobre um perfil que nunca voltou. Desfeita: `depois: 90`, VERDE.",
  "achados": [
    "ligados: 3, e a contagem honesta e 2+1. Nasceram aqui `selecionar` e `voltar-a-de-ontem`; o terceiro e o `ativar`, que TINHA dono e NAO FUNCIONAVA - ver o achado seguinte. Todos em /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/pacotes/a10_perfis.py.",
    "O `ativar` era um gesto que nenhum clique alcancava. Ele le `o[\"texto\"]`, mas o unico elemento marcado `data-hef-gesto=\"ativar\"` era o BOTAO (layout/_ferramentas/aba10.py:504), cujo textContent e literalmente \"Ativar\" - o gesto pedia ao daemon um perfil chamado \"Ativar\". E a linha da tabela, que carrega o nome, so tinha `data-hef-perfil`, que NAO esta entre os atributos que o ouvinte do piloto casa (layout/_ferramentas/hefesto_vivo.py:190-193): clicar num perfil nao mandava nada a lugar nenhum. Curado marcando a celula do nome com `data-hef-gesto=\"selecionar\"` em layout/_ferramentas/aba10.py:479 e fazendo o botao agir sobre o escolhido.",
    "O `ativar` engolia a recusa do daemon. `profile_switch` devolve False quando ele nao confirmou (src/hefesto_dualsense4unix/app/ipc_bridge.py:271, ATIVAR-NAO-MENTE-01) e o retorno era descartado - o piloto imprimia `-> aplicado` sobre uma troca que nao aconteceu. Agora levanta com o nome do perfil.",
    "O ENUNCIADO DESTA TAREFA CARREGA UM FATO ERRADO. Ele diz \"Os botoes Novo/Remover/Duplicar mexem em DISCO (profiles/loader.save_profile, delete_profile)\". Medido no produto estavel: `on_profile_new` (src/hefesto_dualsense4unix/app/actions/profiles_actions.py:3016) e `on_profile_duplicate` (:3144) NAO tocam o disco - so preenchem o editor; quem grava e `on_profile_save` (:3323). So o Remover apaga (:3162), e mesmo esse atras de um dialogo. A propria dica da tela confirma: \"Copia o perfil inteiro PARA O EDITOR\".",
    "O daemon nao tem `profile.save` nem `profile.delete`. Medido com `pacotes.daemon.metodos()`: 39 metodos, e de perfil so `profile.switch`, `profile.list` e `profile.apply_draft`. Gravar e apagar rodam no processo da janela, direto no disco - por isso todo gesto que escreve tem de avisar o daemon depois: `profile.switch` para reaplicar (o daemon NAO rele JSON de perfil sozinho - src/hefesto_dualsense4unix/app/actions/profiles_actions.py:3508) e `launch_env.refresh` para a antecipacao por appid (src/hefesto_dualsense4unix/app/actions/footer_actions.py:205).",
    "O campo Nome do editor e um `<input>` e a pintura escreve `textContent` nele - invisivel. O piloto unico so escreve `value` quando o elemento traz `data-hef-alvo=\"valor\"` (funcao `escrever` do BOOTSTRAP em layout/_ferramentas/hefesto_vivo.py), e layout/_ferramentas/aba10.py:518 nao traz. Consequencia viva: a aba mostra \"Mortal Kombat\" (o valor do mockup) para sempre, mesmo com perfis reais no disco. NAO CUREI de proposito: dar-lhe o `data-hef-alvo` faria a pintura reescrever o campo a cada 500 ms e ela nao conseguiria digitar nele. A cura e o rascunho do editor, que e a fase 3.",
    "A barra da Prioridade recebe o NUMERO como texto DENTRO dela. `editor.prioridade` e o `<span class=\"cheio\">` (layout/_ferramentas/aba10.py:523) e o pacote emite o inteiro; sem `data-hef-alvo=\"largura\"` a pintura escreve \"90\" dentro da barra. O proprio BOOTSTRAP do piloto nomeia este defeito (\"Sem isso, pintar uma barra escreveria o numero DENTRO dela\"). NAO CUREI: a cura exige converter para porcentagem contra `profiles/schema.PRIORIDADE_MAXIMA = 200`, e 90 viraria 45% - a barra encolheria pela metade para o mesmo numero, que e mudanca de DESENHO e precisa do olho dela.",
    "Medicao dos enderecos da pagina publicada: 17 `data-hef` no HTML, 12 chaves emitidas pelo pacote. DEZ enderecos nao sao pintados por ninguem (`perfis.com-ajuste`, `perfis.lista`, `guarda.nome`, `guarda.id`, `guarda.secao`, `guarda.linhas`, `editor.ambiente`, `editor.jogo`, `editor.estilo`, `editor.prioridade.dica`) - a tabela \"Ajuste proprio\" por controle esta inteiramente estatica na aba viva. E CINCO chaves saem do pacote sem endereco onde pousar (`ativo`, `travado`, `jogo`, `quantos`, `selecionado`).",
    "O `autoswitch.lock` e um metodo vivo do daemon e a aba PINTA o estado dele (`travado`), mas o desenho nao tem interruptor nenhum para ele - zero ocorrencias de trava/lock em layout/_ferramentas/aba10.py. E recurso do produto sem porta na tela nova.",
    "O `mockup/10-perfis.html` e o `layout/10-perfis.html` do HEAD estavam UMA linha atras do proprio gerador: a regeneracao trouxe um `# noqa-acento` que ja estava no `aba10.py` versionado e nao no HTML. Ou seja, um marcador de linter do Python esta vazando para dentro de um comentario CSS da pagina entregue (layout/_ferramentas/aba10.py:174). Agora os dois lados o tem; a limpeza fica para quem cuidar do texto do CSS.",
    "Ruff: /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/aba10.py tem 35 erros de lint PRE-EXISTENTES (E401/E501/I001). Ele nao esta no caminho que a tarefa manda medir (`ruff check layout/_ferramentas/pacotes/`) nem no do CI, e nenhum e meu - mas quem um dia incluir os geradores no lint vai encontra-los.",
    "Fora do meu territorio, e por isso so relato: no momento desta leva `layout/_ferramentas/pacotes/a06_navegacao.py:198` e `:226` reprovam o ruff (RUF002, sinal de menos ambiguo) e `tests/unit/test_os_botoes_tem_dono.py::test_o_gesto_chama_a_funcao_certa[a03_gatilhos-modo1]` esta vermelho. Sao de outros dois agentes em voo; os 13 testes que tocam a aba 10 passam."
  ],
  "sem_dono": [
    "novo: NAO e gravacao em disco. `on_profile_new` (src/hefesto_dualsense4unix/app/actions/profiles_actions.py:3016) so preenche o editor e marca `_new_profile`; quem grava e o Salvar (:3323). Sem rascunho de editor e sem Salvar - o rodape das dez abas e a fase 3 - o clique nao teria onde pousar. O piloto ja o trata como perigoso (layout/_ferramentas/hefesto_vivo.py:283).",
    "duplicar: mesma causa, e a dica DELA na propria tela diz com todas as letras \"Copia o perfil inteiro PARA O EDITOR, com (copia) no nome\" (layout/_ferramentas/aba10.py:562). `on_profile_duplicate` (profiles_actions.py:3144) guarda o perfil-fonte e renomeia o campo; nada vai ao disco. Gravar a copia no clique criaria dois perfis com o MESMO match e a MESMA prioridade disputando as mesmas janelas - o estrago que o dialogo R-10 do Salvar existe para impedir.",
    "remover: `profiles/loader.delete_profile` e puro e reusavel, mas a dica dela promete \"Apaga do disco. PERGUNTA ANTES.\" (layout/_ferramentas/aba10.py:506) e o `on_profile_remove` (profiles_actions.py:3162) o esconde atras de `gui_dialogs.confirm_delete_profile` - BUG-DELETE-NO-CONFIRM-01, remocao e permanente. A janela nova nao tem dialogo nenhum. Liga-lo agora apagaria um perfil (possivelmente o que esta valendo) num clique, desfazendo uma decisao medida desta casa.",
    "recarregar: nao ha IPC atras dele. `on_profile_reload` (profiles_actions.py:3319) so chama `_reload_profiles_store()`, que rele o disco para a lista do GTK - e a pintura da aba nova ja rele o disco A CADA TIQUE de 500 ms (`a10_perfis.pacote` -> `layout/_ferramentas/pacotes/perfil.py:lista()`). Um botao que refaz o que o relogio ja faz responderia calado.",
    "detectar: o daemon RESPONDE - `daemon.state_full` publica `window_detect_last_class`, e `extract_steam_appid` (src/hefesto_dualsense4unix/app/actions/launch_wrapper_dialog.py:77) o traduz em appid. O que falta e o destino: a dica dela promete \"monta a regra\", e montar a regra e gravar o `match` no perfil, o que e o Salvar da fase 3. Ligado agora, encheria um campo que nada grava - promessa que o produto nao cumpre.",
    "editor.nome: e um `<input type=text>` marcado como gesto. O clique so diz que ela clicou no campo; nao carrega o que foi digitado, e o piloto nao ouve `input` nem `change` (layout/_ferramentas/hefesto_vivo.py:187 escuta so `click`). Sem canal de valor nao ha o que mandar ao daemon.",
    "editor.jogo: identico ao anterior - `<input>` sem canal de valor.",
    "editor.ambiente: e um `<select>`. O clique manda `texto: alvo.textContent`, que num select e a concatenacao de TODAS as opcoes cortada em 60 caracteres - nunca a escolhida. Sem ouvinte de `change` no piloto, nao ha como saber o que ela escolheu.",
    "editor.estilo: mesmo caso do `editor.ambiente`. E o unico que teria dono claro no daemon (`profile.apply_draft`, via `p.apply_draft_detalhado` - a dica diz \"pre-aplica um perfil inteiro\"), e e o que mais doi deixar de fora: falta so o canal que diga QUAL das catorze opcoes foi escolhida."
  ]
}


---

## Agente 8

{
  "aba": "06-navegacao.html",
  "ligados": 5,
  "metodos": [
    "mouse.emulation.set",
    "keyboard.emulation.set"
  ],
  "testes_verdes": true,
  "sem_dono": [
    "teclado (Função do teclado): a tela tem TRÊS estados ('Ligada', 'Só fora do jogo', 'Desligada') e o daemon tem um bool — keyboard.emulation.set só lê 'enabled' (ipc_handlers.py:5024). E é <select>: não liga por clique (ver achados).",
    "navegacao-interna: navegar a janela do Hefesto com o controle não tem método nenhum entre os 39 do ipc_server.py.",
    "modo-steam: não existe método de Modo Steam no daemon — nenhum dos 39.",
    "vel-touch-menos / vel-touch-mais (Velocidade de cursor · Touch): o cursor do touchpad SAI do mesmo mouse_speed — factor = TOUCHPAD_SENSITIVITY * (mouse_speed / DEFAULT_MOUSE_SPEED), integrations/uinput_mouse.py:446. Não há segundo número a ajustar.",
    "rolagem-dedos-menos / rolagem-dedos-mais (Velocidade da rolagem · Dois dedos): rolagem por dois dedos no touchpad NÃO EXISTE no produto — _emit_scroll(rx, ry) lê só o analógico direito (uinput_mouse.py:412-428). Zero linhas de código para isso.",
    "acao-do-gesto (as 5 listas dos combos PS+…): moram em key_bindings do perfil, e não há método de IPC que escreva key_bindings.",
    "padrao-da-aba (o 'Confirmar' do 'Voltar ao padrão' da fileira): devolver a aba ao de fábrica mexe em key_bindings e nas opções do perfil — sem método de IPC.",
    "guardar-definicoes / padrao-definicoes (rodapé da tela Definições Controle e Mouse): gravar/zerar as 21 linhas é escrever key_bindings — sem método de IPC.",
    "guardar-remapeamento / padrao-remapeamento (rodapé da tela Remapeamento): o remapeamento botão-por-botão não tem sequer campo no perfil, quanto mais método de IPC.",
    "guardar-ponto ('Guardar no estilo' da tela Point-and-click): o Estilo Point-and-click é um estilo de jogo do perfil; gravá-lo não tem método de IPC. Os dois bignum DESTA tela ficaram SEM marca de propósito — são campos deste formulário, e ligá-los ao mouse.emulation.set mudaria a velocidade VIVA enquanto ela pensa que edita um estilo guardado.",
    "mouse.emulation.restore: o terceiro método que o enunciado me deu, e NÃO virou botão. O próprio handler proíbe: 'entra na transição de modo (app/actions/mode_transition.py), NUNCA em um botão solto' (ipc_handlers.py:5011). Ele devolve a preferência PERSISTIDA, que não é 'o de fábrica' nem 'o que a tela mostra' — pendurá-lo no 'Voltar ao padrão' faria o botão prometer uma coisa e fazer outra.",
    "As 49 listas das três pop-ups (21 Definições + 21 Remapeamento + 7 Point-and-click) ficaram sem marca: são os CAMPOS de um formulário cujo ponto de gravação é o 'Guardar', e é o Guardar que já sai no inventário. Nomear as 49 encheria o relato de 49 linhas dizendo a mesma coisa.",
    "Os três <a> que abrem as pop-ups, os 'Cancelar' e os '×' ficaram sem marca porque FUNCIONAM (:target do CSS, sem script). Marcá-los faria o piloto acusar de 'sem dono' um botão que faz o que promete."
  ],
  "mordida": "Arranquei a SEGUNDA chamada do gesto `modo` (a do teclado), trocando `if not p.chamar(\"keyboard.emulation.set\", enabled=novo): raise ...` por `pass`. A régua reprovou com:\n\n    AssertionError: 06-navegacao.html:modo chamou ['chamar'], esperava ['chamar', 'chamar']. A ORDEM importa.\n    E   assert ['chamar'] == ['chamar', 'chamar']\n    E     Right contains one more item: 'chamar'\n    tests/unit/test_os_botoes_tem_dono.py:213: AssertionError\n    1 failed, 48 passed in 0.95s\n\nDESFEITO, e verde de novo: 49 passed in 0.94s (conferido com `grep -c MORDIDA` = 0).\n\nSegunda mordida, para provar a outra metade da régua — troquei `keyboard.emulation.set` por `keyboard.emulation.enable` em METODOS:\n\n    AssertionError: estes métodos não existem no daemon: ['keyboard.emulation.enable']. O `ipc_server.py` é a fonte — se o nome mudou, mude aqui também; se o método não existe, o botão NÃO tem dono e deve recusar dizendo.\n\nTambém desfeita.",
  "achados": [
    "<select> NÃO SE LIGA NESTA CASA, e isso decide 12 dos 19 controles marcados. O piloto ouve `click` (hefesto_vivo.py:188) e o clique num <select> chega quando a lista ABRE, carregando o valor ANTIGO. Ligar um select por clique aplicaria sempre a escolha anterior. Marquei-os só com nome (para o piloto recusar dizendo qual é) e deixei sem gesto. Enquanto o piloto não ouvir `change`, nenhum campo de escolha desta aba pode agir.",
    "A TELA DESENHA DUAS VELOCIDADES DE CADA LADO E O PRODUTO TEM UMA. 'Velocidade de cursor' mostra Touch + Analógico e 'Velocidade da rolagem' mostra Dois dedos + Analógico — quatro números. O daemon tem `mouse_speed` (um) e `mouse_scroll_speed` (um). O Touch é DERIVADO do mesmo mouse_speed (uinput_mouse.py:446) e a rolagem por dois dedos simplesmente não existe (_emit_scroll só lê o analógico direito, uinput_mouse.py:412). Duas das quatro metades são conta que ninguém faz do outro lado.",
    "AS DUAS DICAS DIZEM A FAIXA ERRADA. D_VEL e D_ROL (layout/_ferramentas/aba06.py:907 e :911) dizem 'De 1 a 10' nas duas linhas. O daemon apara speed em 1..12 (daemon/lifecycle.py:1429) e scroll_speed em 1..5 (lifecycle.py:1431). Nenhuma das duas é 1..10. Não mexi no texto — é desenho aprovado, e a correção é dela.",
    "A PONTE NÃO ENXERGA A RECUSA DO DAEMON, e é justamente o que o BG-02 construiu. `p.chamar` passa por `_safe_call` (app/ipc_bridge.py:103), que devolve ok=True para qualquer RPC bem-sucedido — um corpo `{\"status\": \"failed\", \"bloqueio\": \"sem_device\"}` volta como sucesso. Só `_call_checked_detalhado` (ipc_bridge.py:319) entrega o corpo, e `pacotes/ponte.py` não o expõe. Consequência: o botão pode dizer 'ninguém respondeu', mas NÃO pode dizer por que o Hefesto disse não. A tabela BLOQUEIO_DO_MOUSE_EM_PORTUGUES segue inalcançável desta janela.",
    "NOVE VALORES DO PACOTE NÃO TINHAM ONDE SER PINTADOS. Antes desta leva o 06-navegacao.html só tinha data-campo para conta, conta-b, navega e perfil — os nove valores de `mesa` (vel-cursor, vel-rolagem, rato-ligado, rato-bloqueio, rato-despachando, teclado-ligado, teclado-osk, gestos, gestos-lista) eram calculados e escritos em lugar nenhum. Dei endereço aos DOIS que os meus botões movem (vel-cursor, vel-rolagem) — sem isso o −/+ mudaria o daemon e o número na tela ficaria parado, que é um botão respondendo calado. Os outros SETE continuam sem alvo, e `cobertura.pintados` do pacote conta `len(cards)*2 + 9` como se todos pintassem.",
    "O INTERRUPTOR NÃO TEM COMO SER REPINTADO, e ele nasce INVERTIDO. O `escrever()` do piloto cobre texto, largura, fundo e `value` — não há alvo para `checked`. O desenho traz `<input id=\"st-modo\" checked>` (Ligado) e o daemon dela foi medido com `mouse_emulation.enabled=false` (está no docstring do próprio a06_navegacao.py). Logo tela e daemon começam em desacordo. O gesto decide pelo DAEMON, nunca pela caixinha — senão o primeiro clique faria o contrário do que ela vê. A cura de verdade é um alvo `checked` no piloto, que é território de outra pessoa.",
    "MODE_GATE_HINT MANDA PARA UMA ABA QUE NÃO EXISTE MAIS. A frase do produto (app/actions/mouse_actions.py:33) diz 'Só dá para ligar o mouse em \"Controlar o PC\" (aba Início)'. No desenho das dez abas não há Início — o modo mudou para a aba Jogar. Não pude reusar a frase, e nem o módulo: mouse_actions.py importa GTK no topo e os pacotes são puros. Escrevi uma frase própria apontando a aba Jogar, e deixo registrado que o produto estável tem uma frase que envelheceu.",
    "O PORTÃO DO MODO ESTAVA SÓ NA JANELA VELHA — o daemon não o tem. `set_mouse_emulation(enabled=True)` PARA o gamepad virtual em silêncio (daemon/lifecycle.py:1359), e quem segura isso é a GUI: `blocked = mode != MODE_DESKTOP` (mouse_actions.py:299). Copiei essa política para o gesto usando `mode_of_state` (o dono único da leitura do modo) — sem ela, o interruptor desta aba derrubaria o vpad e os jogadores do co-op no meio da partida. Efeito colateral honesto: com ela em modo Jogar o interruptor RECUSA dizendo, e o desenho não mostra esse estado em lugar nenhum. É decisão dela se fica assim.",
    "O `origin` É PARÂMETRO REAL E O INVENTÁRIO NÃO O VÊ. `daemon.parametros('mouse.emulation.set')` devolve ('enabled', 'speed', 'scroll_speed') porque `pacotes/daemon.py` varre `params.get(\"…\")` no corpo do handler, e o handler lê a origem por `origem_do_pedido(params)` (ipc_handlers.py:46). Quem confiar no inventário omite `origin`, o daemon lê a ausência como 'profile' e o clique dela deixa de ser tratado como gesto humano — que é exatamente o ORIGEM-QUE-MENTE-01 / JOGADOR-3-FANTASMA-01. Mandei `origin=\"manual\"` nos três gestos.",
    "O NÚMERO QUE O −/+ SOMA VEM DO TIQUE ANTERIOR (500 ms, hefesto_vivo.TIQUE_MS): dois cliques dentro do mesmo tique leem o mesmo valor e mandam o mesmo alvo — o segundo não anda. Ler o daemon a cada clique custaria um `daemon.state_full` por clique (e o HARM-15 já registra que ele passa dos 0,25 s de timeout sob carga). Está declarado no código, no bloco de abertura dos gestos.",
    "O TIMEOUT DE `p.chamar` É 0,25 s E LIGAR O MOUSE DEMORA MAIS. `_safe_call` usa o default do bridge (ipc_bridge.py:88); ligar a emulação cria um device uinput e para o vpad, cuja parte bloqueante o próprio daemon documenta em ~4 s. O interruptor pode dizer 'o Hefesto não respondeu' sobre uma chamada que funcionou. Não dá para consertar desta aba — o timeout mora em ponte.py.",
    "NÃO HÁ RÉGUA QUE COBRE O INVENTÁRIO DO QUE FALTA. Nada garante que todo `data-gesto` do HTML ou tem dono ou está explicado no pacote. Conferi à mão nesta aba: 19 marcas na tela = 5 com dono + 14 declaradas em SEM_GESTO, zero órfãs. Vale virar portão, mas o arquivo de teste é território compartilhado e não o toquei."
  ]
}
