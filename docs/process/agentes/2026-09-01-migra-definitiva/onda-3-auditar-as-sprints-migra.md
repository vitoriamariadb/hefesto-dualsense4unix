# Audita, sprint a sprint, quais das dez ondas MIGRA o trabalho de 01/09 fechou

Workflow `wf_6e7a3f12-8a3`, rodado em 01/09/2026.

Este arquivo é a MATERIALIZAÇÃO do que os agentes devolveram. O transcrito
vivo mora em `~/.claude/projects/`, sob o nome ANTIGO da pasta
(`…-dev`), e some quando a sessão for limpa. O que está aqui, fica.


---

## Agente 1

{
  "onda": "MIGRA-LANCADORES",
  "total": 10,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-LANCADORES-02",
      "feito": "O defeito de abertura da sprint (\"o HTML da aba 07 nao esta no repositorio\") MORREU — mas pelo trabalho de 31/08 (a mudanca de novo-layout/ para layout/), nao pelo de 01/09. Medido: `git ls-files layout/07-lancadores.html layout/_ferramentas/aba07.py` devolve os DOIS; `git check-ignore -v` nos dois devolve rc=1 (nao ignorados). O gerador esta em /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/aba07.py (267 linhas) e resolve a raiz por __file__ via onde.py, como a sprint exigia. E ha portao de divergencia: `diff -q mockup/07-lancadores.html layout/07-lancadores.html` diz IDENTICOS.",
      "falta": "OS TRES ENTREGAVEIS DO FRONTMATTER NAO EXISTEM: src/hefesto_dualsense4unix/gui/telas/07-lancadores.html (a pasta gui/telas/ nao existe), scripts/telas/aba07.py (a pasta scripts/telas/ nao existe), tests/unit/test_migra_lancadores_02_a_pagina_viaja.py. E o item que a propria sprint chama de \"a prova de chegada\" — a maquina INSTALADA — esta intacto: `grep -n layout install.sh` devolve ZERO linhas, e pyproject.toml:83-91 (`[tool.hatch.build.targets.wheel] include`) leva so `gui/*.glade`, `gui/assets/*.png` e os `.mo`. Como layout/ mora FORA de src/hefesto_dualsense4unix, a pagina e o piloto nao entram no wheel nem no install — a trava P4 que a sprint nomeia segue aberta, e nao so para a 07: para as DEZ."
    }
  ],
  "abertas": [
    "MIGRA-LANCADORES-01 (o enxerto substitutivo) — ABERTA, os cinco entregaveis ausentes. A casca da Emulacao esta INTACTA no Glade: `grep -noE 'id=\"[a-z_0-9]*emul[a-z_0-9]*\"' src/hefesto_dualsense4unix/gui/main.glade` devolve 28 ids vivos (de :3117 a :3569) e o rotulo da aba segue em main.glade:3653 (`<property name=\"label\">Emulação</property>`); `emulation_actions.py` tem 2197 linhas. Nao existe src/hefesto_dualsense4unix/app/telas/lancadores.py (a pasta app/telas/ so tem __init__.py e vibracao.py). `grep -rn VISIVEL_NA_TIRA src/ layout/ tests/ scripts/` devolve ZERO. `grep -rn 'WebKit|WebView' src/hefesto_dualsense4unix/app/app.py` devolve ZERO — nenhum WebView entrou no Gtk.Notebook. So existe a camada compartilhada com OUTRO nome (src/hefesto_dualsense4unix/gui/ponte_da_tela.py, de 30/08, que trata `load-failed` como o item 3 pede), e ela e entregavel da MIGRA-CONTROLES-01, nao desta.",
    "MIGRA-LANCADORES-03 (cada valor ganha endereco) — ABERTA, zero de zero. Medido em layout/07-lancadores.html E em mockup/07-lancadores.html: `data-lanc` 0, `data-acao` 0, `id=\"lanc-conta\"` 0, `id=\"lancadores\"` 0, `data-estado` 0, `pintarLancadores` 0. No gerador layout/_ferramentas/aba07.py: `data-lanc` 0, `data-acao` 0, `data-campo` 0, `lanc-conta` 0. Os UNICOS dois `data-campo` da pagina sao do cabecalho compartilhado — :787 (`conta`, `conta-b`) e :802 (`perfil`) —, nao da aba. `HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 07-lancadores.html` confirma: html 3, pac 0, casam 0, vazios ['conta','conta-b','perfil'].",
    "MIGRA-LANCADORES-04 (quem esta instalado) — ABERTA. src/hefesto_dualsense4unix/integrations/lancadores_instalados.py AUSENTE; tests/unit/test_lancadores_instalados.py AUSENTE. O grep que a sprint usou como enunciado devolve HOJE exatamente o que devolvia em 29/08: `grep -rn -iE 'heroic|lutris|retroarch|mgba|dolphin' src/` = 5 linhas, TODAS prosa em comentario (profiles/schema.py:1302, daemon/lifecycle.py:2250 e :4160, daemon/subsystems/hotkey.py:56, daemon/subsystems/game_signal.py:97). RetroArch, Dolphin e mGBA: zero ocorrencias.",
    "MIGRA-LANCADORES-05 (os cartoes nascem da maquina) — ABERTA. Nao existe app/telas/lancadores.py nem pacote layout/_ferramentas/pacotes/a07_lancadores.py (o diretorio tem a01..a06 e a08..a10 — a 07 e a unica ausente). Medido no registro vivo: `pacotes.PACOTES.get('07-lancadores.html')` devolve None. Os seis cartoes seguem literais no HTML e o cabecalho segue com o numero digitado — layout/07-lancadores.html:844 `<span class=\"conta\">5 encontrados · 1 com impedimento</span>`, exatamente o literal que a sprint manda derivar.",
    "MIGRA-LANCADORES-06 (o censo que o produto joga fora) — ABERTA, os tres numeros do enunciado intactos. `grep -rn como_dicionario src/`: prontuario_dos_jogos.py:614 (a definicao) tem como unico chamador o :1030, que e o `main()` do proprio modulo. `grep -rn com_ponte_confirmada src/`: so :569 (def), :593 e :623 (usos internos) — nenhum consumidor de tela. `grep -rn levantar_censo src/`: um unico chamador de produto, app/actions/daemon_actions.py:818. Nenhum `data-campo=\"jogos\"` existe para receber a contagem (ver a 03).",
    "MIGRA-LANCADORES-07 (os cinco impedimentos, e o Heroic) — ABERTA. Nenhum dos cinco de `_ESTORVOS` chega a esta tela: nao ha modulo de aba, nao ha `data-campo=\"diz\"` nem `data-estado` (0 ocorrencias), e nao existe filtro de procedencia nenhum. O cartao do Heroic segue sendo o unico laranja da pagina carimbando \"Sem wrapper\" — causa que nasce de prontuario_dos_jogos.py:395 lendo o localconfig.vdf da STEAM. `grep -rn -i heroic src/` continua devolvendo so as 3 linhas de comentario da 04: nao ha uma linha que leia configuracao do Heroic.",
    "MIGRA-LANCADORES-08 (o \"Consertar\") — ABERTA, e o grep e byte a byte o mesmo de 29/08. `grep -rn curar_o_que_e_automatico src/ tests/` devolve as MESMAS quatro linhas: prontuario_dos_jogos.py:56 (docstring), :885 (def), :1019 (o `main()` do proprio modulo) e tests/unit/test_ponte_steam_input_01_*.py:431. ZERO chamadores de producao. E a propria pagina publicada documenta o buraco em layout/07-lancadores.html:994 (e no gerador aba07.py:225): «\"Consertar\" — prontuario_dos_jogos.py:885, sem nenhum chamador em src/». O botao \"Consertar\" existe no desenho e nao tem endereco (0 `data-acao`).",
    "MIGRA-LANCADORES-09 (os botoes que saem da aba) — ABERTA. src/hefesto_dualsense4unix/integrations/abrir_lancador.py AUSENTE. `grep -rn open_or_focus_steam src/ layout/ scripts/` fora do proprio steam_launcher.py devolve UM chamador so — daemon/subsystems/hotkey.py:67-69, o botao PS do daemon; a janela continua sem chamar. Os 10 botoes desta familia existem no HTML e nenhum tem endereco: `grep -oE '<button[^>]*>[^<]*</button>' layout/07-lancadores.html` conta 5 \"Abrir o lançador\", 3 \"Criar perfil para um jogo\", 1 \"Detectar o jogo que está aberto\", 1 \"Aplicar o estilo Retrô/Emulador\" — todos sem `data-acao`/`data-gesto`. O item 6 (o botao roxo nao promete) esta cumprido por ACIDENTE: ele nao chama nada porque nada nesta aba chama nada.",
    "MIGRA-LANCADORES-10 (o selo, e a aba na tira) — ABERTA, e o item 4 esta INVERTIDO no produto. O quarto estado nao existe: layout/_ferramentas/aba07.py:111 segue `SELOS = {\"ok\": \"CHEGAM\", \"warn\": \"NÃO CHEGAM\", \"off\": \"NÃO ACHEI\"}`, e `grep -n 'NÃO SEI'` no gerador e na pagina devolve ZERO. A regua do selo nao nasceu, e a afirmacao da sprint sobre o mapa de canais SOBREVIVE a reconferencia: `cut -d, -f1 docs/data/mapa-controles.csv | grep -ci 'lancador|wrapper|steam'` = 0 chaves em 309 linhas. E o pior: A ABA JA ESTA NA TIRA, sem a palavra dela. `grep -l '07-lancadores.html' layout/*.html | wc -l` = 10 — as dez paginas trazem o `<a class=\"aba\" href=\"07-lancadores.html\">Lançadores</a>` (p.ex. layout/01-jogar.html:1092), que e exatamente o que o item 4 manda NAO existir ate ela decidir vendo. Nao ha teste guardando isso: tests/unit/test_migra_lancadores_10_o_selo_e_a_tira.py AUSENTE."
  ],
  "achados": [
    "O ENUNCIADO DA TAREFA CARREGA UM NUMERO ERRADO: ele diz \"14 sprints distintas\" no indice. Sao DEZ. `grep -cE '^\\| [0-9]{2} \\|' docs/process/sprints/2026-08-29-MIGRA-LANCADORES-INDICE.md` = 10, e `ls docs/process/sprints/*MIGRA-LANCADORES*` = 10 arquivos + o indice. O 14 do documento e outro numero: sao os 14 GESTOS da aba (indice, linha da sprint 03: \"35 valores, 14 gestos, zero enderecos\"). Conferi contando os botoes: `grep -c '<button' layout/07-lancadores.html` = 18, sendo 14 da aba (5 abrir + 3 criar perfil + 2 procurar + 1 ver impede + 1 detectar + 1 consertar + 1 estilo retro) e 4 do rodape compartilhado.",
    "ZERO DAS DEZ FECHOU. O trabalho de 01/09 nao tocou a aba 07 em ponto nenhum — e isso e COERENTE, nao descuido: layout/_ferramentas/hefesto_vivo.py:70-71 declara `#: A aba que NAO tem pacote, por decisao dela — so o botao que leva a ela. SEM_PACOTE = {\"07-lancadores.html\"}`. A onda inteira segue esperando as duas palavras que o proprio indice nomeia como travas.",
    "A REGUA DOS BOTOES DA VERDE SOBRE 14 BOTOES MORTOS, e a forma e a mesma do falso-verde de 29/08. `tests/unit/test_os_botoes_tem_dono.py` enumera os PACOTES (glob `a[0-9][0-9]_*.py`), nao os botoes das paginas — a aba 07 nao tem pacote, logo esta fora do universo da regua. Rodei: `pytest tests/unit/test_os_botoes_tem_dono.py test_o_casamento_das_dez.py test_o_despachante_serve_as_dez.py test_o_rodape_das_dez.py -q` → 82 passed, com os 14 botoes da 07 sem uma linha de Python atras. \"Os 30 portoes estao verdes\" nao diz nada sobre esta aba.",
    "PIOR QUE \"SEM DONO\": OS 14 BOTOES NAO CHEGAM SEQUER A SER RECUSADOS. O ouvinte delegado (hefesto_vivo.py:203-206) so dispara para `[data-gesto],[data-modo],[data-hef-gesto],[data-papel],[data-forca],…` ou `.r-aplicar|.r-salvar|.r-importar|.r-exportar`. A 07 tem ZERO desses atributos nos seus botoes — medido. Logo o clique nao vira mensagem, e a linha `[gesto sem dono]` de hefesto_vivo.py:503-506 nunca sai. Silencio completo, que e o que a docstring do proprio `_gesto` diz existir para impedir. (E mesmo quando sai, ela sai por `print()` no terminal, nao \"com o motivo na tela\" como a docstring promete.)",
    "O CABECALHO E A FITA MENTEM NA 07, E SO NELA. `_tique` retorna em hefesto_vivo.py:634 (`if pacote is None: return True`) ANTES de `pacotes.topo(ctx)` (:646) e de `_fita(ctx.mesa)` (:651). Resultado medido no HTML publicado: layout/07-lancadores.html:787 continua dizendo \"2 controles: 1 USB · 1 BT\", :802 \"Mortal Kombat\", e a fita (:795-797) continua com os dois chips do mockup (P1 Cosmic Red USB, P2 Starlight Blue BT). Numa mesa de UM controle, ela navega de uma aba com o topo correto para uma aba que afirma dois — na MESMA janela. E a setima reincidencia do defeito que o comentario de hefesto_vivo.py:659-668 diz existir para curar, viva exatamente na aba que a cura nao alcanca.",
    "NA 07, OS UNICOS BOTOES QUE FUNCIONAM SAO OS QUE GRAVAM. Medido: `gesto_da_pagina('07-lancadores.html', n)` devolve funcao para 'aplicar', 'salvar', 'importar', 'exportar' (pelo registro `('*', …)`) e None para 'abrir', 'consertar', 'procurar', 'detectar', 'ver-impede', 'criar-perfil', 'estilo-retro'. Ou seja: numa aba onde nada e vivo, \"Salvar Perfil\" escreve no disco dela.",
    "UM PORTAO TRANCA O BURACO EM VEZ DE ACUSA-LO: tests/unit/test_o_despachante_serve_as_dez.py:61 `test_a_sem_pacote_e_so_a_lancadores` AFIRMA que a 07 nao tem pacote (:65, `assert tem == set(ABAS) - SEM_PACOTE`). Nao e defeito — e decisao dela virada regua —, mas significa que ligar a aba 07 vai REPROVAR um teste verde, e quem executar a onda precisa saber disso antes. Nota de lado: `SEM_PACOTE` esta declarado em hefesto_vivo.py:71 e NUNCA E LIDO no piloto (grep no arquivo: so a linha da definicao). O unico consumidor e uma copia privada no teste, :43. A excecao vive no teste, nao no produto.",
    "A ONDA FOI ESCRITA PARA UMA ARQUITETURA QUE NAO FOI A CONSTRUIDA. As sprints 01, 02, 05-10 assumem enxerto em `gui/main.glade` + `app/app.py` e um modulo `app/telas/lancadores.py`. O que 01/09 construiu foi uma JANELA SEPARADA: layout/_ferramentas/hefesto_vivo.py, aberta por ./interface → scripts/abrir_interface.py:78. `app/app.py` tem zero WebKit, e o entry point instalado continua `hefesto-dualsense4unix-gui = hefesto_dualsense4unix.app.main:main` (pyproject.toml:80) — ou seja, o .desktop dela abre a interface VELHA, com a aba Emulacao intacta, enquanto o ./interface abre a nova. Duas interfaces convivem, e as dez sprints desta onda descrevem a que nao foi feita.",
    "A INTERFACE NOVA INTEIRA NAO CHEGA A UMA MAQUINA INSTALADA. `grep -n layout install.sh` devolve ZERO linhas e pyproject.toml:83-91 leva so glade/png/mo. Como layout/ mora fora de src/, nenhuma das dez paginas nem o piloto entram no wheel. E a trava P4 que a MIGRA-LANCADORES-02 nomeia, e ela vale para as dez ondas de migracao, nao so para esta.",
    "docs/process/SPRINT_ORDER.md — que o CLAUDE.md aponta como \"a fila\" — tem ZERO mencoes a `MIGRA-` (`grep -cn 'MIGRA-' docs/process/SPRINT_ORDER.md` = 0). Nenhuma das ONZE ondas de migracao (MIGRA-A, MIGRA-CONEXOES, MIGRA-CONTROLES, MIGRA-GATILHOS, MIGRA-ILUMINACAO, MIGRA-JOGAR, MIGRA-LANCADORES, MIGRA-NAVEGACAO, MIGRA-PERFIS, MIGRA-SISTEMA, MIGRA-VIBRACAO) esta no documento de ordem. Quem for executar esta onda nao acha a posicao dela na fila.",
    "TEXTO CADUCO NO LANCADOR DELA: o ./interface ainda imprime, para ela ler, \"A aba Controles esta VIVA (dado do daemon). As outras nove sao o desenho.\" — e o cabecalho do arquivo repete a mesma frase. Desde 01/09 sao NOVE vivas e uma (a 07) e o desenho. A linha esta invertida em relacao ao produto.",
    "O DESENHO NAO E O QUE TRAVA ESTA ONDA. mockup/TODO-DELA.md:541-544 fecha a aba 07 sem pendencia (\"Sem ponto dela nesta aba\", com a palavra dela sobre o placeholder), `diff -q mockup/07-lancadores.html layout/07-lancadores.html` diz IDENTICOS, e mockup/DIVERGENCIAS.md nao lista a 07. O que trava sao as duas travas do indice, e as duas continuam sem resposta em codigo: (a) a regua do selo — nao existe `NAO SEI` e nao ha chave de lancador no mapa de canais; (b) a aba ja aparece na tira das dez paginas antes de ela dizer sim."
  ]
}


---

## Agente 2

{
  "onda": "MIGRA-NAVEGACAO",
  "total": 16,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-NAVEGACAO-02",
      "feito": "O endereçamento existe, mas em OUTRO vocabulário — `data-gesto` (23 marcas, 19 nomes distintos) e `data-campo` (7 marcas) em layout/06-navegacao.html, que é o que o piloto lê (layout/_ferramentas/hefesto_vivo.py:130-134) e o que a régua de casamento conta (layout/_ferramentas/casamento.py:42). E a cura do `<select>` DEIXOU de morar no visualizador: `select{appearance:none;-webkit-appearance:none}` está em src/hefesto_dualsense4unix/gui/ponte_da_tela.py:118 (FOLHA_DA_CASA), injetada pelo `JanelaDaAba` que hefesto_vivo.py:55 importa — logo viaja com o produto, versionada. Comando: grep -c 'appearance' src/hefesto_dualsense4unix/gui/ponte_da_tela.py → 2.",
      "falta": "`data-valor` = ZERO ocorrências (grep -o 'data-valor=' layout/06-navegacao.html | wc -l → 0). 52 dos 60 `<select>` não têm endereço nenhum; 8 dos 16 botões `.passo` também não; dos 8 `bignum`, só 2 têm `<b data-campo>` (vel-cursor, vel-rolagem) — 9 `<b>N</b>` ficam mudos. Zero `<template>` (grep -o '<template' → 0): os três moldes (tpl-cartao, tpl-linha-botao, tpl-linha-gesto) não existem. O gerador layout/_ferramentas/aba06.py não para quando um valor fica sem endereço. E a contagem dos filtros mortos NÃO foi feita por régua nenhuma — medido hoje são 16, não os 12 que a sprint afirma (1× filter-0, 5× filter-1, 9× filter-2, 1× filter-3)."
    },
    {
      "id": "MIGRA-NAVEGACAO-03",
      "feito": "O cartão sai da mesa REAL do daemon: layout/_ferramentas/pacotes/a06_navegacao.py:62-67 itera `ctx.conectados` e marca quem navega por `is_primary` — não por min(jogador), que era o defeito nomeado. `navega` casa de verdade (comando: HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 06-navegacao.html → casam: ['conta','conta-b','navega','perfil','vel-cursor','vel-rolagem']). E os lugares vazios recebem travessão e a moldura de desconectado (hefesto_vivo.py:669-681).",
      "falta": "src/hefesto_dualsense4unix/app/telas/navegacao/mesa.py NÃO EXISTE (o diretório inteiro não existe). Nenhum clone: os cartões são os quatro estáticos do mockup e SÓ DOIS carregam `data-controle` — layout/06-navegacao.html:1304 (p1) e :1684 (p2); os cartões P3 e P4 são `class=\"nav-ctl vazia\"` sem `data-controle`, e tanto a pintura por controle (hefesto_vivo.py:172) quanto o apagamento (:672) endereçam por `data-controle`. Um terceiro DualSense na mesa dela é INALCANÇÁVEL — que é exatamente a frase dela que a sprint cita. `via` é ÓRFÃO (o transporte não chega ao cartão). E a pintura DESTRÓI o desenho: `escrever` faz `el.textContent = t` (hefesto_vivo.py:116) e layout/06-navegacao.html:1682 é `<div class=\"nav-est\" data-campo=\"navega\"><span class=\"bolinha\"></span>USB • Navega o PC</div>` — o primeiro tique apaga a BOLINHA (a marca de quem navega) e o \"USB •\" junto. Não há frase para zero controles."
    },
    {
      "id": "MIGRA-NAVEGACAO-04",
      "feito": "As duas velocidades vêm do bloco `mouse_emulation` do daemon, não do mockup: a06_navegacao.py:72-73 (`rato.get('speed')`, `rato.get('scroll_speed')`), e casam (casamento.py: vel-cursor, vel-rolagem). A escrita usa o MESMO funil speed-only do produto — `mouse.emulation.set` com `origin='manual'` e SEM `enabled` (a06_navegacao.py:181 `_mandar`, :341, :359), que é o payload de app/actions/mouse_actions.py:559. O portão HARM-05 foi copiado para dentro do gesto: a06_navegacao.py:225-231 recusa fora de MODE_DESKTOP com a frase. Régua verde: HEFESTO_VARIANTE=dev .venv/bin/python -m pytest tests/unit/test_os_botoes_tem_dono.py -k '06 or navegacao' → 9 passed. Validado no aparelho dela (commit cc3227a1: \"a Navegação com os quatro de velocidade indo e voltando\").",
      "falta": "`mouse.enabled` NUNCA chega ao interruptor: layout/06-navegacao.html:2902 tem `<input id=\"st-modo\" class=\"tog-in\" checked>` fixo, sem `data-campo`, e `rato-ligado` sai como ÓRFÃO no casamento — a tela diz \"Ligado\" com o daemon desligado. `rato-bloqueio` e `rato-despachando` também órfãos: os cinco estados de `bloqueio` não têm rótulo nenhum. O portão HARM-05 só age NO CLIQUE — o interruptor nunca nasce inerte e a frase só sai no terminal (a06_navegacao.py:89 registra isso). Sem guarda de `draft.mouse.dirty`. Recusa-com-motivo NÃO é distinguida de falha de transporte — o próprio `_mandar` documenta (a06_navegacao.py:171-179): um `{\"status\":\"failed\"}` volta como sucesso. Nada marca o que o rádio não entrega. E o gesto `modo` está em hefesto_vivo.PERIGOSOS:332, logo nunca foi clicado."
    },
    {
      "id": "MIGRA-NAVEGACAO-05",
      "feito": "A lista \"Função do teclado\" tem dono: gesto `teclado` em a06_navegacao.py:252-313 chamando `keyboard.emulation.set`, com o select marcado em layout/06-navegacao.html:2903 (`data-gesto=\"teclado\" data-campo=\"teclado-estado\" data-hef-alvo=\"valor\"`). O degrau do meio NÃO mente: \"Só fora do jogo\" levanta com a razão medida (a06_navegacao.py:306-311, citando lifecycle.py:1879 e `suppress_desktop_emulation`). E a pintura só reescreve a lista quando o daemon falou (a06_navegacao.py:92-94) — chave ausente = não toca no `<select>`.",
      "falta": "`resolver_teclado_emulado` CONTINUA SEM CHAMADOR EM PRODUÇÃO. Comando: grep -rn 'resolver_teclado_emulado' src/ → 3 linhas, todas em profiles/schema.py (:1002 comentário, :1330 definição, :1375 __all__). A lápide segue registrada em tests/unit/portao_a_casa_sabe_e_o_produto_nao_faz.py:1853. Nada saiu de emulation_actions.py: `_refresh_keyboard_switch` (:1661) e `on_keyboard_toggle_set` (:1706) estão lá e ligados em app/app.py:461 e :1150 — o gesto tem DOIS donos agora, não um. `osk_disponivel` é órfão (`teclado-osk`), então a frase \"Neste computador: …\" (input_actions.py:265) não chega à tela. O `bloqueio` do teclado nem é emitido. E \"Só fora do jogo\" continua no desenho sem campo — a sprint pedia mapear em `suppress_desktop_emulation` OU tirar do desenho; não se fez nem um nem outro."
    }
  ],
  "abertas": [
    "MIGRA-NAVEGACAO-01 — a página entra no lugar da aba. NENHUM dos seis itens existe. main.glade:3683-4171 ainda é o bloco antigo (21 ids em 463 linhas de XML); `mouse_mapping_grid` ainda escrito à mão em main.glade:3856; app/app.py:1242 ainda `_MOTIVO_ALVO_AINDA_NAO_LIGADO` e app/app.py:1372 ainda tem a aba no `_PAGINAS_COM_TETO_ELASTICO`; os três ids seguem em footer_actions.py:127,130,131 (FROZEN_WIDGET_IDS); src/hefesto_dualsense4unix/app/telas/navegacao/ não existe; scripts/gui-captura/retratar_abas.py:1989 e :2006 ainda devolvem \"aba Navegação não montada\" dentro de dois `except`. grep -rn WebKit2 src/ não acha uma linha em app.py nem no glade.",
    "MIGRA-NAVEGACAO-06 — as quatro frases de diagnóstico. app/telas/navegacao/diagnostico.py não existe; grep -c 'diag\\.' layout/06-navegacao.html → 0; nenhum pacote importa `frase_dos_botoes_sem_tecla`, `frase_do_teclado_na_tela`, `frase_dos_atalhos_fora_da_lista` nem `MODE_GATE_HINT` (grep sobre layout/_ferramentas/pacotes/*.py acha só uma MENÇÃO em comentário, a06_navegacao.py:215). O defeito que a sprint nomeia continua vivo: a dica ainda diz \"Se a linha de estado abaixo estiver vermelha\" (aba06.py:1261, renderizado em layout/06-navegacao.html:1290) e não há linha de estado abaixo.",
    "MIGRA-NAVEGACAO-07 — os cinco gestos chegam à tela. Não há bloco `gestos` no state_full (grep -n '\\\"gestos\\\"' src/hefesto_dualsense4unix/daemon/ipc_handlers.py → nada); core/gestos_do_controle.py não existe; \"Religar o controle\" — a ação que não existe — continua nos cinco dropdowns (aba06.py:643 → layout/06-navegacao.html:2839,2848,2857,2868,2874); \"0,15 s\" continua literal (layout/06-navegacao.html:1298); o realce ainda usa o `QUEM_NAVEGA['pref']` fixo (aba06.py:768). O pacote registra a ausência: SEM_GESTO['acao-do-gesto'] em a06_navegacao.py:407-410.",
    "MIGRA-NAVEGACAO-08 — os cinco gestos ficam trocáveis. Os cinco `data-gesto=\"acao-do-gesto\"` da página não têm dono (a06_navegacao.py:407); nenhum select nasce `disabled` (as 3 ocorrências de `disabled` no HTML não estão nas linhas de gesto); nada escreve. A pergunta 4 do contrato continua sem resposta dela.",
    "MIGRA-NAVEGACAO-09 — a roda de pontes tem uma escada só. As DUAS escadas continuam divergentes: `CICLO_DE_PONTES` é tupla literal de 3 em daemon/subsystems/hotkey.py:104, e `ESCADA` tem 4 `Degrau(` em integrations/ponte_escada.py:294-338. `KIND_DESKTOP` (ponte_escada.py:169) segue fora da tupla. Nada foi derivado de nada.",
    "MIGRA-NAVEGACAO-10 — o PS solo ganha quem escreva a ação. Comando: grep -rn 'ps_button_command' src/ → EXATAMENTE 2 linhas (daemon/lifecycle.py:205, a declaração; daemon/subsystems/hotkey.py:71, a leitura). A régua 1 da própria sprint pede três ou mais, com uma atribuição. Ninguém escreve.",
    "MIGRA-NAVEGACAO-11 — as 21 linhas do que cada botão faz. app/telas/navegacao/botoes.py e core/disputa_de_botao.py não existem; `mouse_mapping_grid` continua XML escrito à mão (main.glade:3856); nenhuma das 21 linhas tem endereço (os 21 selects da pop-up estão entre os 52 sem `data-gesto` e nenhum tem `data-campo`); `guardar-definicoes` e `padrao-definicoes` estão em SEM_GESTO (a06_navegacao.py:421-433) — e ali está medido que `profile.apply_draft` alcança 9 das 21 linhas e nunca grava em disco.",
    "MIGRA-NAVEGACAO-12 — as três regiões do touchpad voltam. As quatro coisas seguem como estavam: core/keyboard_mappings.py:60 ainda `touchpad_left_press: (\"KEY_BACKSPACE\",)` — o padrão que apaga texto; `_combine_with_touchpad` segue calado (daemon/subsystems/keyboard.py:393-403, com a razão de 09/08 intacta); as três continuam fora de CANONICAL_BUTTONS (app/actions/input_actions.py:102-119, com o \"PARA VOLTAR\" ainda escrito como futuro); assets/profiles_default/point_and_click.json:26 ainda `KEY_E`. A decisão dela de 29/08 não virou uma linha de código.",
    "MIGRA-NAVEGACAO-13 — o remapeamento botão a botão nasce. core/remapeamento_de_botao.py não existe; grep -rn 'remap' src/hefesto_dualsense4unix/profiles/schema.py → 0; `guardar-remapeamento` e `padrao-remapeamento` em SEM_GESTO (a06_navegacao.py:434-436) com a razão \"não tem sequer campo no perfil\". A pop-up de 21 linhas continua desenhada sobre nada.",
    "MIGRA-NAVEGACAO-14 — o Point-and-click ganha quatro velocidades. core/estilo_point_and_click.py e app/telas/navegacao/estilo.py não existem; `guardar-ponto` em SEM_GESTO (a06_navegacao.py:437-441, medindo que \"Estilo de Jogo\" não existe em campo, widget ou preset nenhum); point_and_click.json intocado. A metade honesta existe no PYTHON — `vel-touch-menos/mais` e `rolagem-dedos-menos/mais` estão em SEM_GESTO com a razão medida (a06_navegacao.py:398-403) — mas na TELA os dois números mortos continuam mostrando 4 e 4 com botões `−`/`+` idênticos aos vivos, sem motivo nenhum à vista. E a faixa continua sendo duas: a dica diz \"De 1 a 10\" (layout/06-navegacao.html:2906-2907) contra 1-12 e 1-5 do daemon (daemon/lifecycle.py:1429,1431).",
    "MIGRA-NAVEGACAO-15 — o Modo Steam nasce sem medição. Comando: grep -rln 'steam_deck\\|big_picture\\|modo_steam\\|gamepadui' src/ → VAZIO, idêntico ao que a sprint mediu em 29/08. `modo-steam` em SEM_GESTO (a06_navegacao.py:397, \"não há método de Modo Steam no daemon — nenhum dos 39\"). Não existe o arquivo de medição das três perguntas que a régua 1 exige antes de qualquer afirmação.",
    "MIGRA-NAVEGACAO-16 — a Navegação Interna e o toque sem remetente. daemon/lifecycle.py:4767 ainda publica `EventTopic.BUTTON_DOWN` com `{\"button\": name, \"pressed\": True}` — sem `uniq`. A trava segue escrita nos dois lugares que a sprint cita (daemon/subsystems/hotkey.py:990 \"o `BUTTON_DOWN` não carrega `uniq`\", profiles/schema.py:892). `navegacao-interna` em SEM_GESTO (a06_navegacao.py:393-396), e a razão ali registra até que `core/disputa_de_botao.py` \"não existe no disco\"."
  ],
  "achados": [
    "O ÍNDICE TEM DEZESSEIS SPRINTS, NÃO DEZESSETE. `ls docs/process/sprints/*MIGRA-NAVEGACAO*` devolve 17 arquivos, e um deles é o INDICE. O próprio índice diz, na primeira linha do corpo: \"Dezesseis sprints, e o censo desta aba havia dito catorze\". O enunciado desta auditoria dizia 17 — conferi contra a fonte antes de propagar.",
    "O TRABALHO DE 01/09 NÃO EXECUTOU ESTA ONDA POR OUTRO NOME — ELE CORREU POR OUTRA ROTA. As dezesseis sprints assumem o enxerto SUBSTITUTIVO: a página trocando `scroll_tab_navegacao_dsx` dentro do `Gtk.Notebook`, com o dono em `src/hefesto_dualsense4unix/app/telas/navegacao/*.py`. O que existe é uma JANELA PARALELA (layout/_ferramentas/hefesto_vivo.py, aberta pelo `./interface`) com o dono em layout/_ferramentas/pacotes/a06_navegacao.py. NENHUM endereço de `posse:` ou `cria:` desta onda foi escrito. Consequência prática, e é a que importa para ela: a aba Navegação que a janela do produto abre HOJE continua sendo a do Glade, intacta, com os 21 widgets antigos — o que se ligou vive só no piloto. Ninguém deve ler \"4 parciais\" como \"a aba migrou\".",
    "A PINTURA DESTRÓI O DESENHO DO CARTÃO, e é medível agora. `escrever` faz `el.textContent = t` (layout/_ferramentas/hefesto_vivo.py:116); o alvo é layout/06-navegacao.html:1682, que é `<div class=\"nav-est\" data-campo=\"navega\"><span class=\"bolinha\"></span>USB • Navega o PC</div>`. O primeiro tique com daemon vivo apaga a BOLINHA — a marca de quem navega o PC — e apaga o \"USB •\" junto. O transporte não volta: `via` é órfão no casamento. É o inverso exato do item 3 da sprint 03, e é a mesma família de defeito que o commit cc3227a1 já pagou duas vezes hoje (o `select.textContent` que destruía as `<option>`, e o `<input>` que mostrava o mockup para sempre). Ninguém olhou esta aba com o daemon vivo.",
    "ESTA ABA SÓ CONSEGUE MOSTRAR DOIS CONTROLES, PARA SEMPRE. Só p1 e p2 carregam `data-controle` (layout/06-navegacao.html:1304 e :1684); os cartões P3 e P4 são `class=\"nav-ctl vazia\"` sem atributo nenhum. Tanto a pintura por controle (hefesto_vivo.py:172-173) quanto o apagamento de lugares vazios (:672-674) endereçam por `data-controle`, e `TODOS_OS_LUGARES` (:68) tem os quatro. Um terceiro DualSense na mesa dela é invisível nesta aba — que é literalmente a frase dela que a sprint 03 cita: \"se eu comprar outros dualsense eles aparecem também\". Por sorte o texto estático de P3/P4 já diz \"Desconectado\", então hoje o erro não aparece; ele aparece no dia em que ela ligar o terceiro.",
    "O NÚMERO DA SPRINT 02 JÁ CADUCOU: ela afirma 12 referências mortas `url(&quot;#outline-filter…&quot;)` (4× filter-1, 8× filter-2). Medido hoje em layout/06-navegacao.html: SÃO 16 — 1× filter-0, 5× filter-1, 9× filter-2, 1× filter-3. A régua 5 daquela sprint é de contagem EXATA (\"mudar de 12 para 11 reprova igual\"), então quem a escrever como está reprova no dia em que nascer, e vai procurar o defeito no lugar errado.",
    "O VOCABULÁRIO DE ENDEREÇO DA SPRINT 02 FOI SUPERADO SEM NOTA. Ela manda criar `data-valor`; o produto adotou `data-campo` + `data-gesto`, e layout/_ferramentas/casamento.py:42 documenta TRÊS vocabulários vivos nas dez abas (`data-campo`, `data-papel`, `data-hef`), com a razão de nenhum se aposentar. Quem executar a 02 como está escrita cria o QUARTO. A sprint precisa de nota datada antes de ser despachada.",
    "O INTERRUPTOR PRINCIPAL DESTA ABA NUNCA FOI CLICADO. `(\"06-navegacao.html\", \"modo\")` está em hefesto_vivo.PERIGOSOS:332, e com razão — ligar a emulação move o cursor DELA. Mas isso deixa o único gesto desta aba que toca DOIS métodos do daemon (`mouse.emulation.set` + `keyboard.emulation.set`) com régua de mesa (PROVAS, a06_navegacao.py:478-480) e ZERO prova de bancada; o commit cc3227a1 registra só \"os quatro de velocidade indo e voltando\". O gesto `teclado`, que nasceu na segunda leva, também não aparece nomeado na prova do aparelho.",
    "OITO DAS CATORZE CHAVES QUE O PACOTE CALCULA NÃO TÊM ONDE APARECER, e cada uma é entrega de sprint. Comando: HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 06-navegacao.html → casam 6, órfãos 8: `rato-ligado`, `rato-bloqueio`, `rato-despachando` (sprint 04), `teclado-ligado`, `teclado-osk` (sprint 05), `gestos`, `gestos-lista` (sprint 07), `via` (sprint 03). O lado Python está meio feito e o gerador (layout/_ferramentas/aba06.py) não foi tocado — é a metade que ninguém dividiu, e é onde a onda vai emperrar quando alguém tentar fechar a 04.",
    "O `SEM_GESTO` É INVENTÁRIO HONESTO, MAS COBRE UMA MINORIA DA TELA. 13 dos 19 gestos endereçados recusam pelo nome com razão medida (a06_navegacao.py:392-442), e várias dessas razões são medições NOVAS que valem mais que a sprint que as pedia — o `guardar-definicoes`, por exemplo, mede que `profile.apply_draft` alcança 9 das 21 linhas, nunca grava em disco, e ainda responde `applied: [keyboard]`. Mas 52 dos 60 `<select>` e 8 dos 16 botões `.passo` desta página não têm endereço nenhum: não recusam, não dizem nada, e um clique neles é silêncio puro. É o buraco que a sprint 02 existe para fechar, e ele continua aberto.",
    "DUAS DICAS DA TELA CONTRADIZEM O DAEMON E NENHUMA RÉGUA PEGA. layout/06-navegacao.html:2906-2907 diz \"De 1 a 10\" nas duas velocidades; o daemon apara em 1-12 (cursor) e 1-5 (rolagem) — daemon/lifecycle.py:1429 e :1431. O pacote registra o erro em comentário (a06_navegacao.py:354-355, \"nas duas está errada — está no relato\") e a tela nunca foi corrigida. É exatamente a régua 4 da sprint 14, e é o tipo de coisa que vira erro de validação mudo no Salvar."
  ]
}


---

## Agente 3

{
  "onda": "MIGRA-ILUMINACAO",
  "total": 12,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-ILUMINACAO-03",
      "feito": "Os endereços NASCERAM hoje, e é o maior avanço da onda. `layout/04-iluminacao.html` tem 21 `data-campo` e 28 `data-gesto`; o gerador os emite (`layout/_ferramentas/aba04.py:494` player-N+data-gesto=player+data-player, `:583` data-gesto=cor+data-hex, `:591` identidade, `:598` hex, `:601` brilho-pct, `:602` brilho, `:607` aceso, `:613` auto, `:614` apagar). Provado que vieram de 01/09: `git show 274b2720:layout/_ferramentas/aba04.py | grep -c data-campo` = 0 e no HEAD = 6; `data-campo` entrou em d81b776e (01:11) e `data-gesto` em 46dc6a96 (03:34). `mockup/04-iluminacao.html` e `layout/04-iluminacao.html` têm o MESMO inventário (diff dos data-campo = idênticos) e `scripts/check_o_desenho_aprovado.py` está verde.",
      "falta": "1) `data-uniq` e `data-pos`: ZERO no HTML (`grep -o data-uniq= layout/04-iluminacao.html | wc -l` = 0). O endereçamento é por `data-controle`, que já existia. 2) Dos OITO `data-campo` por coluna que a sprint lista, faltam TRÊS: `moldura` (aba04.py:590, a div `.moldura` não tem endereço), `desenho` (o `svg()` de aba04.py:591 não tem) e `cor-guia` (os oito `.tom` de aba04.py:583 não têm). 3) Dos SEIS tipos de gesto, faltam DOIS: `livre` (o `<input type=color class=livre>`, aba04.py:595, sem `data-gesto`) e `brilho` (o `.trilho`, aba04.py:601, sem `data-gesto`). 4) O gerador NÃO recusa desenho incompleto — não há `SystemExit` por campo faltando em `aba04.py`. 5) `tests/unit/test_migra_iluminacao_03_todo_valor_tem_endereco.py` não existe (`ls tests/unit | grep migra_ilumina` → vazio)."
    },
    {
      "id": "MIGRA-ILUMINACAO-05",
      "feito": "O `hex` e o `brilho`/`brilho-pct` de cada coluna chegam à tela pelo dado dela. `layout/_ferramentas/pacotes/a04_iluminacao.py:88-108` monta `colunas[uniq]` num LAÇO sobre `ctx.conectados` (a sprint pedia laço, e é laço). Medido: `HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 04-iluminacao.html` → casam: aceso, brilho, brilho-pct, conta, conta-b, hex, identidade, perfil. Provado em Chrome headless injetando o BOOTSTRAP de `hefesto_vivo.py` na página publicada: o `.hex` foi de `#0000FF` para `#FF0000`.",
      "falta": "A CURA PINTA UM LUGAR DOS QUATRO E QUEBRA DOIS. Medido no mesmo ensaio: `.tom.on` continua 1 e no tom velho (a guia não repinta), o `--luz` do SVG não muda, e as duas `.tira-luz` são DESTRUÍDAS — o `data-campo=\"aceso\"` está na div que as contém (aba04.py:607) e `escrever()` (hefesto_vivo.py:116) faz `el.textContent = t`: 3 filhos → 0 filhos, innerHTML vira o literal `Aceso`. Do brilho: `data-campo=\"brilho-pct\"` está no `.cheio` (aba04.py:601) e recebe TEXTO — `getComputedStyle(.cheio).width` = 141.641px ANTES e DEPOIS, e o texto do `.cheio` foi de '' para '82'. A causa é uma só: `layout/04-iluminacao.html` tem ZERO `data-hef-alvo` (02, 03, 05, 06, 08 e 10 têm). E `app/actions/lightbar_actions.py` NÃO foi tocado hoje (`git log --since=midnight -- src/.../lightbar_actions.py` → vazio): `_refresh_lightbar_from_draft:577` continua com alvo único e a fonte do pacote é o `lightbar_rgb` do daemon + o perfil, não `draft.effective_leds_for`. Sem test file."
    },
    {
      "id": "MIGRA-ILUMINACAO-06",
      "feito": "O `P{n}` do rótulo chega: `a04_iluminacao.py:104` emite `identidade` = `P{player}` e o `casamento.py` confirma que casa.",
      "falta": "O PRÓPRIO ITEM ENTREGUE PERDE DADO: provado em Chrome headless que o `.ctrl-rot` vai de `P1 • Cosmic Red • USB` (2 filhos) para `P1` (0 filhos) — o nome do controle e o transporte somem no primeiro tique, porque `aba04.py:591` desenha três pedaços e o pacote emite só o primeiro. Os outros três itens estão abertos: (1) as cinco lâmpadas não são pintadas — não há endereço nelas e o `aceso` as apaga; (3) qual botão fica `on` e (4) o anelzinho: `casamento.py` lista `player-1`,`player-2`,`player-3`,`player-4` como VAZIOS (lugar na página, ninguém pinta); (4-coop) `coop_manda_nas_luzes` não aparece no pacote. Sem test file."
    },
    {
      "id": "MIGRA-ILUMINACAO-07",
      "feito": "A METADE (a) está feita no piloto novo, e é a entrega mais sólida da onda. `layout/_ferramentas/pacotes/a04_iluminacao.py` tem quatro `@gesto` que carregam o `uniq` da coluna: `cor` (:136, `p.led_set(rgb, uniq=uniq)`), `apagar` (:162), `auto` (:172, `p.chamar('lightbar.reset', uniq=)` + `led_set(player_slot_color(slot))`) e `player` (:214, `p.identity_number_set(uniq, n)`). Nada foi reescrito: `pacotes/ponte.py` expõe o `app/ipc_bridge.py`. Item 4 (uma ponte só): `hefesto_vivo.py:241` tem um `manda()` e um roteador por `data-gesto`. PROVADO POR CLIQUE, em Chrome headless com o BOOTSTRAP real e um `webkit.messageHandlers.hefesto` dublê: clicar um tom da 2ª coluna → `{g:'cor', c:'p2', hex:'#0000FF'}`; Desligar da 2ª → `{g:'apagar', c:'p2'}`; o botão 3 da 1ª → `{g:'player', c:'p1', p:'3'}`. E `.venv/bin/python -m pytest tests/unit/test_os_botoes_tem_dono.py tests/unit/test_o_casamento_das_dez.py -q` → 65 passed, com as quatro PROVAS do pacote cobrando QUAL função da ponte foi chamada e com quê.",
      "falta": "Dos SEIS tipos, DOIS não existem: no mesmo ensaio de clique, o `change` no `<input class=livre>` e o clique no `.trilho` produziram ZERO mensagens (total = 3 para 5 acionamentos). A METADE (b) está inteira aberta: `src/hefesto_dualsense4unix/app/app.py:1234` continua `\"tab_lightbar_box\": None` (a fita segue viva), `_persist_leds_update` não recebe `uniq`, os cinco handlers GTK não foram tocados, e nada disto grava no rascunho — a decisão dela de 01/09 (o clique aplica na hora) trocou o modelo, mas ninguém escreveu isso na sprint. Sem test file."
    },
    {
      "id": "MIGRA-ILUMINACAO-11",
      "feito": "Os itens 1 e 2. `src/hefesto_dualsense4unix/daemon/ipc_handlers.py:1880-1884` faz a permutação de dois (`nova_ordem[indice_atual], nova_ordem[indice_alvo] = nova_ordem[indice_alvo], nova_ordem[indice_atual]`), com o comentário `TROCA, não rodízio` e a medição (55 casos, coincidem em 35, divergem em 20). O portão da mesa continua em `:1839-1840` (`numero > len(presentes)` → `_NumeroForaDaMesaError`). MAS NÃO FOI O TRABALHO DE 01/09: `git log -1 -S'nova_ordem[indice_atual], nova_ordem[indice_alvo]'` devolve **091ab2e5, 29/08 21:28**, a sprint do CRC do rádio. Hoje o `ipc_handlers.py` só ganhou a rota do alto-falante no `state_full`.",
      "falta": "Item 3 — o contrato NÃO foi corrigido: `docs/process/2026-08-26-O-REDESENHO-as-dez-abas.md:341` continua dizendo *\"Os outros deslizam para abrir lugar\"*, que é o rodízio. As duas promessas vivas que a sprint existe para matar continuam vivas, só que agora é a DOC que diverge do daemon. Item 4 — não há portão da promessa (nada compara os 16 tooltips e a legenda de `aba04.py` com o comportamento do daemon). E `tests/unit/test_migra_iluminacao_11_troca_ou_rodizio.py` não existe; nenhum teste da suíte exercita o caso de salto ≥ 2 que SEPARA troca de rodízio."
    }
  ],
  "abertas": [
    "MIGRA-ILUMINACAO-01 — o desenho das luzes sai das cinco caixas. NADA feito. `src/hefesto_dualsense4unix/app/actions/lightbar_actions.py:1466-1472` continua lendo a TELA: `get_current_player_leds` faz `self._get(f\"player_led_{i}\")` na linha :1469. O handler morto `on_player_led_toggled` continua em :1383. Não existe `_player_leds_desenho` (grep → zero). `git log --since=midnight -- src/.../lightbar_actions.py` → VAZIO. Sem test file. A sprint era a única que podia rodar antes do ok dela, e é a que protege o perfil dela — continua sendo o risco que ela descreve.",
    "MIGRA-ILUMINACAO-02 — a página toma o lugar da aba. As 538 linhas de Glade estão intactas: `gui/main.glade:1146` scroll_tab_lightbar_box, `:1155` tab_lightbar_box, `:1233` lightbar_color_button, `:1247` lightbar_preview, `:1377` lightbar_brightness_scale, `:1579` player_leds_apply. `app/app.py:1095/1234/1370` continuam com a chave. O piloto de 01/09 é uma JANELA SEPARADA (`layout/_ferramentas/hefesto_vivo.py`, que o `./interface:49` abre), não o enxerto substitutivo no `Gtk.Notebook` do produto. Nenhum número de PSS antes/depois foi escrito. Sem test file.",
    "MIGRA-ILUMINACAO-04 — a página nasce da mesa real. `layout/_ferramentas/aba04.py:133` continua com `repeat(4,1fr)` cravado; não há `--n`. `layout/_ferramentas/monta.py:219-228` continua com a `MESA` literal de quatro. E a medição que fecha o caso: `grep -o 'data-controle=\"[^\"]*\"' layout/04-iluminacao.html | sort | uniq -c` devolve **p1 (1), p2 (1)** e mais nada — só DUAS colunas endereçáveis. `coluna_vazia()` (aba04.py, a função dos dois lugares cinzas) emite ZERO `data-campo` e ZERO `data-controle`: um TERCEIRO DualSense ligado não ganha coluna, não ganha botão e não produz erro nenhum — some calado, que é a armadilha nomeada no CLAUDE.md. Zero controles também não tem desenho próprio. Sem test file.",
    "MIGRA-ILUMINACAO-08 — o trilho de brilho vira controle. `grep 'type=\"range\"' layout/04-iluminacao.html` → zero; os únicos `<input>` do miolo são dois `type=\"color\"` (:1642 e :2074), exatamente o que a sprint mediu em 29/08. O `.trilho` não tem `data-gesto` e no ensaio de clique não produziu mensagem alguma. Não há adiamento na página (`pointerup`/`keyup`/`change` → grep zero em aba04.py). `_fiar_aplicar_ao_soltar` (lightbar_actions.py:770-813) não foi tocado. Sem test file.",
    "MIGRA-ILUMINACAO-09 — os quatro que o contrato preserva. `grep -o 'Reenviar[^<]*\\|Cores automáticas[^<]*\\|de onde veio[^<]*\\|auto-por-controle' layout/04-iluminacao.html` → NENHUMA ocorrência. Não há `data-campo=\"estado-barra\"`. Opções continua com dois botões (aba04.py:613-614). E o pior: o pacote REESCREVE a frase da disputa em vez de consumir `rotulo_lightbar` — `a04_iluminacao.py:110-112` compõe *\"A Steam tem este controle aberto…\"* à mão, que é exatamente a segunda verdade que a sprint proíbe (*\"A frase NÃO se reescreve\"*), e ela nem chega à tela: `casamento.py` lista `recado` e `fonte` como ÓRFÃOS, e no ensaio em Chrome `document.body.innerText.includes('Steam tem este controle')` = **False**. Sem test file.",
    "MIGRA-ILUMINACAO-10 — a cor do plástico nas colunas. A cor não chega ao `state_full`: ela continua num dicionário de instância, agora o `LeitorDeCor`/`self.perguntados` do piloto (`hefesto_vivo.py:414-417`, `mesa_viva.py:327`). Ela alimenta a FITA (`hefesto_vivo.py:265`) e não as colunas: nenhum pacote menciona `plastico`/`colorway` (grep em `layout/_ferramentas/pacotes/*.py` → zero) e a `.moldura` e o `data-colorway` não têm endereço. Nenhuma coluna sem cor tem desenho de fallback. A trava declarada (ONDA-CONEXOES-11/12) continua de pé. Sem test file.",
    "MIGRA-ILUMINACAO-12 — duas peças nunca têm a mesma cor. `grep -rn 'DUAS-PECAS\\|duas_pecas\\|tom_vizinho\\|cor_ja_usada' src/hefesto_dualsense4unix/` → ZERO linhas. A regra continua sem uma linha em `src/`, exatamente como a sprint diz. E a onda de 01/09 tornou a colisão mais fácil, não menos: os 16 botões `data-gesto=\"cor\"` das duas colunas agora aplicam no aparelho na hora (`a04_iluminacao.py:136`), então dar o mesmo tom a dois controles é UM CLIQUE e ninguém avisa. Sem test file."
  ],
  "achados": [
    "O ENUNCIADO DESTA AUDITORIA DIZ 15 SPRINTS; SÃO DOZE. O índice escreve *\"Doze sprints\"* e tem a seção *\"Por que doze, e não dez\"*; `ls docs/process/sprints/*MIGRA-ILUMINACAO*` devolve 12 arquivos + o índice, numerados 01 a 12. Não existe 13, 14 nem 15. Auditei as doze.",
    "A PINTURA DESTRÓI O DESENHO, E ESTÁ PUBLICADA. `escrever()` em `hefesto_vivo.py:116` faz `el.textContent = t` quando não há `data-hef-alvo`, e `layout/04-iluminacao.html` tem ZERO `data-hef-alvo` (as abas 02, 03, 05, 06, 08 e 10 têm 4, 16, 6, 1, 6 e 5). Provado em Chrome headless com o BOOTSTRAP real: `data-campo=\"aceso\"` (aba04.py:607) vai de 3 filhos (as duas `.tira-luz` e as cinco lâmpadas) para 0 e vira o literal `Aceso`; `data-campo=\"identidade\"` (aba04.py:591) vai de `P1 • Cosmic Red • USB` para `P1`. No primeiro tique do piloto a coluna perde a fileira de luzes, o nome do controle e o transporte. Isto não é 'falta ligar' — é regressão visível na tela que ela abre pelo `./interface`.",
    "O TRILHO DE BRILHO RECEBE O NÚMERO DENTRO DELE. `data-campo=\"brilho-pct\"` está no `.cheio` (aba04.py:601), que é a barra preenchida, e sem `data-hef-alvo=\"largura\"` ele recebe TEXTO. Medido: `getComputedStyle(.cheio).width` = 141.641px antes e 141.641px depois, e o `textContent` foi de '' para '82'. A barra nunca se move e ganha um número solto no meio.",
    "O VALOR QUE O PRÓPRIO PACOTE CHAMA DE MAIS IMPORTANTE NÃO TEM ONDE CAIR. O docstring de `a04_iluminacao.py` diz *\"A `lightbar_disputada` É O VALOR MAIS IMPORTANTE DESTA ABA\"* — e `casamento.py 04-iluminacao.html` devolve `órfãos: ['fonte', 'player', 'recado']`. Os três são compostos e jogados fora a cada tique. Ao mesmo tempo, `vazios: ['player-1','player-2','player-3','player-4']`: a página tem quatro lugares para o número do jogador e ninguém os pinta, enquanto o pacote emite um `player` que não tem endereço. É um desencontro de nome (`player` × `player-N`), não falta de dado.",
    "A SPRINT 11 FECHOU FORA DA ONDA E NINGUÉM ATUALIZOU O PAPEL. A troca substituiu o `pop`+`insert` em **091ab2e5, 29/08 21:28** (a sprint da semente 0x53), antes de a onda ser autorizada. O índice ainda a descreve como *\"espera a palavra dela\"* e como TRAVA da 07 — e a 07, hoje, liga o gesto de número sobre um daemon que já faz o que a tela promete. O que sobrou aberto é o inverso do escrito: o CONTRATO (`2026-08-26-O-REDESENHO-as-dez-abas.md:341`) é que continua prometendo rodízio.",
    "`data-controle` CARREGA DOIS VOCABULÁRIOS NA MESMA PÁGINA. `grep -o 'data-controle=\"[^\"]*\"'` devolve `dualsense` × 4 (o modelo, do desenho do controle) e `p1`/`p2` × 1 (a coluna, do piloto). A pintura (`hefesto_vivo.py:163,173`) e a resolução do dono do clique (`:219`, `alvo.closest('[data-controle],[data-uniq]')`) usam o mesmo seletor. Hoje não colide porque nenhum clicável mora dentro do SVG — mas basta um `data-gesto` entrar na moldura para o gesto sair endereçado a um controle chamado `dualsense`, que não existe na mesa, e recusar calado.",
    "AS DOZE SPRINTS DECLARAM UM ARQUIVO DE TESTE EM `cria:`, E NENHUM EXISTE. `ls tests/unit | grep -i migra_ilumina` → vazio. As réguas novas de hoje (`test_os_botoes_tem_dono.py`, `test_o_casamento_das_dez.py`, `test_o_rodape_das_dez.py`, `test_o_perfil_chega_na_tela.py`) são transversais às dez abas e medem BEM o que medem — o gesto chamar a ponte, o pacote emitir onde há lugar. Mas nenhuma delas mede o que estas doze sprints pedem: a foto que não muda, a mordida da carga que falha, o arraste que vira 1 IPC, o caso de salto ≥ 2, as N cores distintas. Os 65 testes verdes que rodei não contradizem nada do que está acima — eles simplesmente não olham para lá.",
    "A ROTA WEBKIT CONTINUA FORA DO PRODUTO INSTALADO, E O RISCO MUDOU DE FORMA. `src/hefesto_dualsense4unix/gui/ponte_da_tela.py` EXISTE (a MIGRA-CONTROLES-01/03 entregou), mas `src/hefesto_dualsense4unix/gui/telas/` NÃO existe e `grep -n 'layout/' install.sh` → zero. Todo o trabalho de 01/09 mora em `layout/_ferramentas/`, fora do pacote. Em compensação, uma das premissas do índice CADUCOU: ele repete que o mockup é `.gitignore:108` e não viaja em worktree — hoje `git check-ignore -v layout/...` sai com rc=1 e `git ls-files layout/` lista a pasta inteira. Metade do risco 1 do índice está morta; a outra metade (o instalador não copiar o HTML) está viva."
  ]
}


---

## Agente 4

{
  "onda": "MIGRA-GATILHOS",
  "total": 11,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-GATILHOS-02",
      "feito": "A pagina ganhou endereco, e ele casa: `HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 03-gatilhos.html` devolve `html 25 · pac 28 · casam 19 · orfaos 9 · vazios 6`, e `tests/unit/test_o_casamento_das_dez.py:47` fixa o piso de 19 para esta aba (65 testes passam). Medido no HTML publicado: 16 `<select>` com `data-gesto`+`data-campo`+`data-lado` (layout/03-gatilhos.html:1139, :1164, :1210, :1235, ...), 4 `.ctrl` enderecaveis por `data-controle=\"p1..p4\"` (:1135), 4 `button[data-gesto=\"guardar\"]` (:1261, :1384, :1484, :1584) e 11 `.barra` com `data-ajuste` + tres `data-campo` cada.",
      "falta": "(a) O CHIP NAO TEM ENDERECO NENHUM: 4 chips no miolo, 0 com `data-campo` — o esquema da sprint pedia `.chip[data-papel=\"identidade\"]`, e sem ele a identidade da coluna nao se pinta (layout/03-gatilhos.html:1137). (b) A `.barra` e enderecada por POSICAO (`data-ajuste=\"e-0\"`), nao por `data-param` = `TriggerParamSpec.name` — a regra 1 explicita da sprint; 0 de 11 barras tem `data-param`. (c) `.ajustes` sem `data-lado`: 0 de 8. (d) `.ctrl` sem `data-uniq`: 0 — a traducao `pref→uniq` vive no piloto (layout/_ferramentas/hefesto_vivo.py:510-514), o que funciona mas nao e o contrato escrito. (e) O teste `tests/unit/test_migra_gatilhos_a_pagina_tem_endereco.py` NAO EXISTE (`ls tests/unit/ | grep migra` nao o traz), logo nenhuma das cinco mordidas foi escrita — nem a foto pixel a pixel, nem `data-param` contra `get_spec`, nem a unicidade, nem o `check_endereco_de_radio.py` sobre o HTML. (f) Sobram 9 orfaos do pacote (`l2-raw`, `l2-pct`, `r2-raw`, `r2-pct`, `modo-e`, `modo-d`, `aj-nome-d-2`, `aj-pct-d-2`, `aj-val-d-2`) e 6 vazios da pagina (`aj-*-e-2`, `aj-*-e-3`)."
    },
    {
      "id": "MIGRA-GATILHOS-04",
      "feito": "O pacote monta as colunas a partir da MESA REAL, uma entrada por controle presente e chaveada pelo `uniq`: layout/_ferramentas/pacotes/a03_gatilhos.py:204-236 (`for c in ctx.conectados`). E o override por controle VENCE o perfil, lendo `p[\"controllers\"][uniq][\"triggers\"]` — a03_gatilhos.py:211-214. Provado rodando o pacote com um controle sintetico: sai `colunas: {\"aa:bb:cc:00:00:01\": {...}}` com 25 valores lidos do perfil dela no disco (`modo-chave-e: \"Rigid\"`, `aj-val-e-1: 180`). O piloto apaga os lugares que a mesa nao tem (hefesto_vivo.py:668-676).",
      "falta": "(a) A GRADE CONTINUA FIXA EM QUATRO: `hefesto_vivo.py:68` — `TODOS_OS_LUGARES = {\"p1\", \"p2\", \"p3\", \"p4\"}` — e o HTML tem 4 `.ctrl` estaticos. Com CINCO controles a quinta coluna nao existe; com ZERO, sobram quatro colunas travessadas, e nao a tela de mesa vazia que a sprint pede como estado desenhado. (b) O CHIP NAO SE PINTA (0 de 4 com endereco): com um controle na mesa a coluna do P2 continua dizendo `P2 • Starlight Blue • BT` — e o proprio piloto declara essa cura como a setima reincidencia do defeito (hefesto_vivo.py:658-667), que aqui nao alcanca. (c) A MARCA DE VAZIO NAO PINTA NADA NESTA ABA: `grep '\\.off\\b|data-conectado' layout/03-gatilhos.html` acha os 4 atributos e ZERO regra CSS; so 01-jogar, 02-controles e 07-lancadores tem `.off{`. O `carga[\"vazios\"]` escreve um atributo que a folha desta pagina ignora. (d) `player`, `transport` e nome do plastico nao chegam a coluna; a ponte MAC→colorway continua sem dono em `src/` (o unico hit de `cores-do-dualsense` em src/ e um comentario, utils/maquina.py:536; `NOMES_DE_FABRICA` tem 21 modelos). (e) `tests/unit/test_migra_gatilhos_a_mesa_real_desenha_as_colunas.py` nao existe; nenhum dos seis casos (0, 1, 2, 4, 5 controles; hotplug; `player: None`) e medido."
    },
    {
      "id": "MIGRA-GATILHOS-05",
      "feito": "A CHAVE passou a ter um dono so, e ha guarda alta. `layout/_ferramentas/aba03.py:434-440`: se `len(MODOS) != len(PRESETS)` o gerador morre com `SystemExit`, e `CHAVE_DO_MODO` sai de `PRESETS` pela ordem — o `value` de cada `<option>` e agora `Off`, `Rigid`, `Bow`, `Weapon`... lido do produto, nao digitado. O mesmo para o efeito pronto: `aba03.py:445-460` deriva `CHAVE_DO_PRONTO` de `profiles.trigger_presets.FEEDBACK_POSITION_LABELS` e reprova com `SystemExit` se um rotulo da pagina nao existir la. Foi isso que deu ao clique o que mandar ao daemon.",
      "falta": "(a) `aba03.MODOS` CONTINUA VIVO com os 19 rotulos e as 19 descricoes DIGITADOS (aba03.py:327-347) — a sprint pede que ele morra e que `label`/`description` venham de `PRESETS`. (b) `PRONTOS` (aba03.py:349) e `MEUS` (:351) continuam digitados: `— Nenhum —` sobrevive, `Personalizar` continua ausente, e os seis `VIBRATION_POSITION_LABELS` nao aparecem em lugar nenhum da pagina. (c) As 19 descricoes longas NAO substituiram as curtas no produto: `grep -rn 'Sem resistência\\.' src/` ainda acha `app/actions/trigger_specs.py:100`. (d) Os dois nomes em ingles continuam divergindo — produto `Arco de flecha (Bow)` (trigger_specs.py:135) e `Disparo (Weapon)` (:200) contra pagina `Arco de flecha` / `Disparo`; o comentario novo em aba03.py:429-432 ADMITE a divergencia e a contorna casando por ordem, em vez de cura-la. Ver o achado sobre `D-ARCO-DE-FLECHA-SEM-O-INGLES`: a direcao da cura inverteu. (e) `tests/unit/test_migra_gatilhos_o_texto_dos_modos_tem_um_dono.py` nao existe; nenhum dos cinco controles (inclusive o negativo do `— Nenhum —`) foi escrito."
    },
    {
      "id": "MIGRA-GATILHOS-07",
      "feito": "No motor novo (o piloto), tres dos seis itens existem e sao provados. (1) A pintura e um laco sobre as colunas presentes, com o override por MAC: a03_gatilhos.py:200-236. (3) O gesto sabe de quem e — `(uniq, lado)` viajam juntos, e a traducao `e/d → left/right` tem UM dono, o `LADOS` de a03_gatilhos.py:50, usado pela pintura (:200) e pelo gesto (:271-282). (5-bis) NENHUMA COLUNA CAI NO RAMO \"Todos\": `_uniq` vazio levanta (a03_gatilhos.py:390-391 e :430-432) e o `uniq=` vai em toda chamada (:399, :402, :440). Provado por execucao: `.venv/bin/python -m pytest tests/unit/test_os_botoes_tem_dono.py -q` → 65 passed, cobrindo as quatro PROVAS de a03_gatilhos.py:459-484, que exigem `trigger_set_detalhado([\"left\",\"Rigid\",...], uniq=...)`.",
      "falta": "(a) OS ARQUIVOS DA POSSE NAO FORAM TOCADOS: `git log --since='2026-09-01' -- src/hefesto_dualsense4unix/app/actions/triggers_actions.py` devolve VAZIO, e `app.py` tambem nao mudou nessa regiao. O produto que ela usa continua lendo e escrevendo para UM alvo. (b) AS BARRAS NAO VIRARAM CONTROLE — item 2 da sprint: zero `<input>` no miolo de `layout/03-gatilhos.html` (medido), nenhum `data-gesto=\"ajuste\"`, e `pacotes.gesto_da_pagina('03-gatilhos.html','ajuste')` devolve `None`. Nao ha como mudar um valor de ajuste na tela nova. (c) O que a pintura mostra e o PERFIL NO DISCO (`perfil.ativo(...)`, a03_gatilhos.py:196), nao o rascunho — `draft.effective_triggers_for(uniq)` nao e chamado em lugar nenhum do caminho novo. (d) Item 5: `_ALVO_POR_ABA[\"tab_triggers_box\"]` continua como estava. (e) `tests/unit/test_migra_gatilhos_o_rascunho_e_a_escrita_por_coluna.py` nao existe; nenhuma das sete mordidas foi escrita."
    },
    {
      "id": "MIGRA-GATILHOS-08",
      "feito": "O item 3 — o coracao do diagnostico da ONDA-GATILHOS-01, e o mais dificil de adivinhar — esta feito no motor novo: escolher \"Desligado\" percorre `trigger.reset`, e nao `trigger.set` com `Off`. `layout/_ferramentas/pacotes/a03_gatilhos.py:398-399`: `if chave == \"Off\": ok, motivo = _desfecho(p.trigger_reset_detalhado(lado, uniq=uniq))`, com a R-19 escrita no docstring (:374-381). Ha prova executavel, e ela morde pelo nome da funcao: `a03_gatilhos.py:470-471` exige `(\"trigger_reset_detalhado\", [\"right\"], {\"uniq\": ...})`, e `pytest tests/unit/test_os_botoes_tem_dono.py -q` passa.",
      "falta": "(a) Item 1 NAO FEITO: `src/hefesto_dualsense4unix/app/actions/triggers_actions.py:106` continua `self._trigger_live_preview_timer = {\"left\": 0, \"right\": 0}` — UM relogio por lado, nao por `(uniq, lado)`. Com quatro colunas, mexer no L2 do P3 continua cancelando em silencio o preview do L2 do P1. (b) Item 2 NAO FEITO: `_adiantar_live_preview` (triggers_actions.py:283) continua adiantando UM. (c) O produto continua com o defeito do item 3 tambem: escolher \"Desligado\" na lista de modos passa por `_apply_trigger` (triggers_actions.py:580) e ARMA a trava; so o botao \"Desligar\" chama `_reset_trigger` (:654, :229-232) — visivel na foto `docs/usage/assets/readme_gatilhos.png`, que ainda mostra os botoes \"Aplicar em L2\" e \"Desligar\". (d) `tests/unit/test_migra_gatilhos_a_trava_manual_vezes_oito.py` nao existe; as oito combinacoes de coluna que mexe × coluna que desliga nunca foram varridas."
    }
  ],
  "abertas": [
    "MIGRA-GATILHOS-01 — a medicao do popup do `<select>` sob a COSMIC NAO EXISTE. A entrega nomeada e `docs/process/medicoes/2026-08-29-o-popup-do-select-no-webkitgtk-sob-cosmic.md`; `ls docs/process/medicoes/` devolve o diretorio VAZIO. Nenhum dos seis itens (abre? fica aberta quantos ms? escolhe? teclado? as 19 opcoes sao legiveis? o mesmo sem a HeaderBar do ver.py) foi registrado, e nao ha PNG. O unico parente e `select{appearance:none;-webkit-appearance:none}` em src/hefesto_dualsense4unix/gui/ponte_da_tela.py:118 — que e a cura da CAIXA FECHADA (armadilha 3 do indice, item 5 da sprint 03), nao do popup, e e de 30/08 (`git log -- gui/ponte_da_tela.py` → 48b4e1a2, 2026-08-30). O `regua_popup.py` mede `.tela-nova` em Chrome headless, nao `<select>` em WebKitGTK. A onda seguiu com 16 `<select>` vivos na aba sem a medicao que podia derruba-los.",
    "MIGRA-GATILHOS-03 — o enxerto SUBSTITUTIVO nao aconteceu. `src/hefesto_dualsense4unix/gui/main.glade` ainda tem o `GtkScrolledWindow` de fora (:767) e os CATORZE ids `trigger_*` (:837, :848, :863, :874, :907, :936, :950, :998, :1009, :1023, :1034, :1062, :1091, :1105); `app/app.py:1502` e `:1830` ainda chamam `install_triggers_tab`; e `grep -rn 'ponte_da_tela' src/.../app/app.py` devolve ZERO — o `Gtk.Notebook` do produto nao tem `WebView` nenhum. A foto `docs/usage/assets/readme_gatilhos.png` (lida) mostra a aba GTK antiga: os 19 modos como botoes segmentados, \"Aplicar em L2\" e \"Desligar\". O que 01/09 entregou foi um SEGUNDO programa, `layout/_ferramentas/hefesto_vivo.py`, aberto pelo `./interface` — nao a troca de uma pagina do notebook. E os TRES numeros que sao a razao de a sprint existir (objetos/tempo do builder, PSS com a janela nesta aba, linhas mortas × nascidas) nao estao em commit nenhum.",
    "MIGRA-GATILHOS-06 — a caixa de ajustes nao cresceu, e o fato errado sobreviveu. Medido agora: `len(get_spec('MultiPositionVibration').params) == 11`. E `layout/_ferramentas/aba03.py:821` continua escrevendo `<span class=\"marca\">Vibração por posição</span> 10`, com a conta em `10 * ALT_BARRA` e `20 * ALT_BARRA` (:821-822) — publicado em layout/03-gatilhos.html:1677 E em mockup/03-gatilhos.html:1677 (os dois arquivos sao identicos). Nenhuma das tres saidas foi desenhada; `N_ESQ`/`N_DIR` continuam fixos (aba03.py:616) e o `TETO_DA_GRADE` de 477px (:670) segue como `SystemExit` que so protege o mockup. Consequencia viva, medida pelo casamento: com o perfil \"Ação\" o pacote emite `aj-nome-d-2`, `aj-val-d-2` e `aj-pct-d-2` para um modo de tres ajustes e a coluna do P1 so tem DUAS barras a direita — o terceiro ajuste some da tela, orfao. `tests/unit/test_migra_gatilhos_a_caixa_de_ajustes_cabe_onze.py` nao existe.",
    "MIGRA-GATILHOS-09 — o \"Efeito pronto\" continua sem nome no disco. Parte A: `src/hefesto_dualsense4unix/profiles/schema.py:197-203` — `TriggerConfig` tem `mode` e `params` e `extra=\"forbid\"`, e NENHUM campo `preset`; `TriggerDraft` idem. `triggers_actions.py:471` continua terminando em `combo.set_active_id(\"custom\")`, e o `return` mudo de `_on_preset_changed` continua (triggers_actions.py, ramo `else: return` logo apos os dois modos). `_MODES_COM_PRESET` continua com dois (:101). Parte B: a pergunta (a)-ou-(b) nao foi levada a ela — nao ha linha em `docs/data/decisoes-dela.csv`. `tests/unit/test_migra_gatilhos_o_efeito_pronto_tem_nome.py` nao existe.",
    "MIGRA-GATILHOS-10 — \"Meus efeitos\" continua sem tela E sem dono. `src/hefesto_dualsense4unix/profiles/catalogo_de_curvas.py` NAO EXISTE (`ls profiles/ | grep curva` so traz `curva_propria.py`); `grep -n curvas_dir src/.../utils/xdg_paths.py` nao acha nada; `grep -rl curva_propria src/` continua vazio — a lapide do portao segue de pe. E o botao esta MORTO na tela: ha 4 `button[data-gesto=\"guardar\"]` no HTML (layout/03-gatilhos.html:1261, :1384, :1484, :1584) e `pacotes.gesto_da_pagina('03-gatilhos.html','guardar')` devolve `None` — clicar imprime `[gesto sem dono]` no terminal (hefesto_vivo.py:504) e nao muda um pixel. O separador `──── Meus efeitos ────` continua no HTML com as duas entradas de mentira e `value=\"\"` (aba03.py:351, :462-467), que e exatamente o que a prova 5 da sprint reprova.",
    "MIGRA-GATILHOS-11 — a prova de tela nao correu, e nao poderia: a onda nao fechou. `docs/usage/assets/readme_gatilhos.png` tem como ultimo commit `454c4ef4`, de 2026-08-26 (`git log -1 -- docs/usage/assets/readme_gatilhos.png`), e a imagem lida e a aba GTK velha — nao ha foto do \"depois\". Nenhum dos seis estados da tabela (lista dos 19 aberta, modo de 11 barras, mesa com 1 e com 0, chip cinza, \"Meus efeitos\" com uma curva, recibo de recusa) foi fotografado. E a divergencia que a sprint leva para a bancada continua exata: `src/hefesto_dualsense4unix/core/trigger_effects.py:461-466` (`weapon` → `PULSE_B`) e `:469-481` (`vibration` → `PULSE_A`), com `docs/data/mapa-controles.csv:108` ainda `parcial`/`parcial` em `gatilho.modos_firmware@dualsense`."
  ],
  "achados": [
    "TRES PERGUNTAS \"o que e dela decidir\" JA TINHAM RESPOSTA DELA — no MESMO DIA em que as sprints foram escritas, e as sprints nao as citam. `docs/data/decisoes-dela.csv:114` (`D-OS-CINCO-MODOS-DE-GATILHO-CRESCEM-NA-PROPRIA-ABA`, decidida 2026-08-29): *\"A CAIXA CRESCE E A ABA ROLA. Decisao dela contra as duas alternativas (pop-up propria, ou esconder os seis)\"* — a sprint 06 lista tres saidas e diz *\"Esta sprint nao escolhe\"*. E `:115` (`D-A-METRALHADORA-TEM-QUATRO-AJUSTES`, decidida): *\"QUATRO — Forca, Frequencia, Inicio do curso e Fim do curso\"* — a sprint 06 fecha dizendo *\"Uma das duas esta errada — e a escolha e sua\"*. Quem executar a 06 lendo so a sprint vai reabrir duas decisoes fechadas.",
    "A SPRINT 05 PEDE O CONTRARIO DA ULTIMA DECISAO DELA. O item 4 manda *\"os dois parenteses voltam — `Arco de flecha (Bow)` e `Disparo (Weapon)`\"*, citando a decisao dela de 07/08. Mas `docs/data/decisoes-dela.csv:116` (`D-ARCO-DE-FLECHA-SEM-O-INGLES`, decidida 2026-08-29) diz: *\"SO 'ARCO DE FLECHA'. O ingles sai. (...) a decisao dela nao muda o mockup, que ja obedece. Ela vale para o PRODUTO, no transplante, e cobre os dois pela FORMA\"*. A direcao da cura inverteu: quem tem de mudar e `src/.../app/actions/trigger_specs.py:135` e `:200`, nao a pagina. Executar a 05 como escrita reintroduziria o ingles que ela mandou tirar.",
    "NENHUMA REGUA COMPARA O `data-gesto` DO HTML COM OS GESTOS REGISTRADOS. `test_os_botoes_tem_dono.py` prova que os gestos que EXISTEM chamam o daemon (ele le `PISO_DA_ABA` e `PROVAS` de dentro do pacote), e `PISO_DA_ABA = 2` em a03_gatilhos.py:455. A pagina carrega TRES nomes de gesto (`modo`, `pronto`, `guardar`) e quatro botoes `guardar` sem dono. `grep -rn data-gesto tests/ scripts/` mostra que so `test_a_aba_sistema_nomeia_o_servico.py` e `check_o_desenho_aprovado.py` tocam o atributo, e nenhum dos dois cruza o inventario da pagina com o registro. Quatro botoes mortos atravessam os 30 portoes verdes.",
    "A RECUSA NAO CHEGA NA TELA — e o docstring do piloto afirma que chega. `hefesto_vivo.py:490-497` diz que ele *\"RECUSA o resto **com o motivo na tela**\"*, e a implementacao imprime no terminal: `:504` (`print(f\"[gesto sem dono] ...\")`) e `:530` (`print(f\"[gesto falhou] ...\", file=sys.stderr)`). Nao ha toast, nem barra, nem `__hef.aviso` — `grep -n 'toast|aviso' hefesto_vivo.py` nao acha nada. Entao os quatro \"Guardar esse efeito\" e as duas entradas de \"Meus efeitos\" (que o gesto `pronto` recusa em a03_gatilhos.py:434-438) respondem CALADOS para quem clica. E a propria `A-CASA-SABE-E-O-PRODUTO-NAO-FAZ` em miniatura, na cura que diz preveni-la.",
    "A MARCA DE VAZIO DO PILOTO NAO PINTA NADA NESTA ABA. `hefesto_vivo.py:668-676` marca `data-conectado=\"nao\"` e a classe `off` nos lugares sem controle, com um comentario de tres paragrafos chamando isso de \"a setima aparicao do mesmo defeito\". Medido: `layout/03-gatilhos.html` tem os 4 atributos `data-conectado` e ZERO regra de CSS para `.off` ou `[data-conectado]` — so `01-jogar.html`, `02-controles.html` e `07-lancadores.html` tem `.off{`. Somado ao chip sem endereco, com um controle na mesa a coluna do P2 desta aba continua afirmando \"P2 • Starlight Blue • BT\": o defeito que a cura declara ter matado esta vivo aqui.",
    "O DOCSTRING DO PACOTE ESTA CADUCO E DESMENTE O PROPRIO PILOTO. `a03_gatilhos.py:290-295` (`_escolhido`) diz *\"O piloto unico nao trouxe esse ramo, e e por isso que os campos de escolha desta aba ainda nao entregam a escolha (esta no relato)\"*. Mas o commit `f53361db` (\"feat(ouvinte): o valor de `<input>` e `<select>` chega ao Python\") e POSTERIOR ao ultimo commit que tocou o pacote (`a97aeeea`), e `hefesto_vivo.py:196` ja registra `document.addEventListener('change', ...)` mandando `valor: alvo.value`. O gesto funciona; a frase que diz que ele nao funciona ficou no arquivo.",
    "O PACOTE TOMA UMA DECISAO DE PRODUTO QUE E DELA, E ELA NAO A TOMOU. O gesto `pronto` implementa a leitura (a) da sprint 09 — escolher um efeito pronto TROCA o modo da coluna para `MultiPositionFeedback` (a03_gatilhos.py:440) — e o proprio docstring admite: *\"**Isto e escolha de produto e e dela**; esta no relato para ela decidir\"* (:415-423). Nao ha linha correspondente em `docs/data/decisoes-dela.csv`. E divergencia dupla com o produto, que esconde a linha de preset nos outros 17 modos (`_update_preset_row_visibility`, triggers_actions.py:446).",
    "AS SPRINTS FORAM ESCRITAS CONTRA UM VOCABULARIO DE ENDERECO QUE O TRABALHO NAO ADOTOU. Todas usam `data-papel` + `data-uniq` + `data-param`; o construido usa `data-campo` + `data-controle` + `data-ajuste`, e a razao esta escrita em hefesto_vivo.py:117-128 — tres vocabularios convivem de proposito (`data-campo` em oito abas, `data-papel` so na Vibracao, `data-hef` so na Perfis). Quem auditar as outras nove ondas MIGRA pelo texto literal da sprint vai marcar como aberto o que existe com outro nome — e vai marcar como fechado o `data-param`, que e a UNICA das tres divergencias com consequencia real (enderecar ajuste por posicao e o defeito que a propria sprint 02 nomeia).",
    "O INDICE MANDA ESPERAR E A ONDA NAO ESPEROU — mas nao por desobediencia: o trabalho de 01/09 nao foi esta onda. O indice diz *\"As onze sprints desta onda SAO ESCRITAS. Nenhuma executa antes do ok dela sobre o piloto\"*, com a 01 como unica excecao. O que 01/09 entregou foi o piloto unico e os pacotes das dez abas — nenhum arquivo de `src/` desta onda foi tocado (`git log --since='2026-09-01' -- src/.../triggers_actions.py`, `schema.py` triggers, `main.glade` faixa 767-1133: vazio). O efeito colateral e que a aba Gatilhos JA ESTA VIVA no `./interface` com 16 `<select>` e quatro botoes mortos, sem a medicao da 01 e sem a prova de tela da 11 — a onda esta sendo consumida por ela antes de comecar."
  ]
}


---

## Agente 5

{
  "onda": "MIGRA-CONEXOES",
  "total": 12,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-CONEXOES-01",
      "feito": "A infraestrutura do WebView existe e o piloto a usa — mas em `src/hefesto_dualsense4unix/gui/ponte_da_tela.py`, NÃO no `config/pagina.py` que a sprint pede. Lá estão 4 dos 8 entregáveis: os QUATRO pinos na ordem, com o Gdk depois do Gtk (`ponte_da_tela.py:84-87`); o par load-changed/load-failed com a armadilha escrita (`:49-50`, `:95-97`); e a folha da casa `.nota{display:none !important}select{appearance:none;-webkit-appearance:none}` como UserStyleSheet (`:114-118`, `:189-198`). O piloto consome o módulo em `layout/_ferramentas/hefesto_vivo.py:55`. ATENÇÃO: `git log --diff-filter=A -- src/.../gui/ponte_da_tela.py` devolve `52651638 2026-08-29` — isso é de 29/08, NÃO é entrega de 01/09.",
      "falta": "O enxerto em si. `ls src/hefesto_dualsense4unix/app/actions/config/pagina.py` → inexistente. `install_config_tab` continua montando os CINCO `moldura_de_secao` num laço (`app/actions/config/mixin.py:51-61`) e `secao_janela` continua na tupla (`app/actions/config/secoes.py:30`) — os dois são mordidas explícitas da sprint. `ls tests/unit/ | grep migra_conexoes` → NENHUM arquivo. A medição de PSS do enxerto SUBSTITUTIVO (item 8) não existe em lugar nenhum. A aba Configurações do produto GTK está byte-a-byte como estava."
    },
    {
      "id": "MIGRA-CONEXOES-02",
      "feito": "O HTML e o gerador estão VERSIONADOS, que era o defeito de uma frase da sprint. `git check-ignore -v layout/08-conexoes.html` → rc=1 (não ignorado); `git ls-files layout | wc -l` → 87 arquivos. A cópia à mão morreu junto com a `novo-layout/`. E `diff -q mockup/08-conexoes.html layout/08-conexoes.html` → idêntico, com o dono dos dois caminhos em `layout/_ferramentas/onde.py`.",
      "falta": "Os caminhos não são os da sprint: `ls src/hefesto_dualsense4unix/gui/telas/` e `ls scripts/telas/` → as duas pastas NÃO EXISTEM. Consequência medida: a página vive em `layout/`, fora de `src/`, logo NÃO entra no wheel nem no `install.sh` — o defeito 'a aba nasce em branco em toda instalação' continua de pé para quem instalar. A `.nota` continua na página (só escondida por CSS em `ponte_da_tela.py:118`); a decisão dela sobre instalar os 22.887 bytes segue aberta. E os NOVE `<a class=\"aba\" href=\"NN-*.html\">` continuam vivos (medido: 9 ocorrências em `layout/08-conexoes.html`), que é exatamente a mordida `zero <a class=\"aba\" href=\"0` reprovando."
    },
    {
      "id": "MIGRA-CONEXOES-03",
      "feito": "A página saiu do zero absoluto. Medição da sprint em 29/08: `ids: 5 · data-*: 0`. Medição de hoje no HTML publicado, com os comentários removidos antes de contar: 28 `data-campo=` em 9 nomes (achado, conta, conta-b, exame, mic-existe, perfil, selo, vizinho-nome, vizinho-tipo), 56 `data-gesto=` em 18 nomes, 6 `data-modo=`. O gerador emite os endereços na mesma f-string do valor (`layout/_ferramentas/aba08.py`), nunca à mão no HTML.",
      "falta": "A gramática é `data-campo`/`data-gesto`, não a `data-v`/`data-g` proposta — isso a própria sprint autoriza (\"adota a da moldura sem discussão\"). O que falta de verdade: dos ~50 valores, só 9 nomes têm endereço. A tabela dos adaptadores e a pista do Desempenho têm ZERO `data-campo` (medido por recorte do corpo do HTML nos dois blocos). E `tests/unit/test_migra_conexoes_todo_valor_tem_endereco.py` não existe — a régua viva é `tests/unit/test_o_casamento_das_dez.py`, que mede o casamento pacote↔página, não o inventário fechado que a sprint pede."
    },
    {
      "id": "MIGRA-CONEXOES-04",
      "feito": "Na rota nova o defeito não existe: o piloto pede UM `daemon.state_full` por tique e desce UM payload — `self.ponte.perguntar(BOOTSTRAP, self._instalado)` em `layout/_ferramentas/hefesto_vivo.py:566` e `:569`, com coalescência por `self.agendado`. E o IPC cru morreu: tudo passa pelo `app/ipc_bridge.py` via `layout/_ferramentas/pacotes/ponte.py:53` (`from hefesto_dualsense4unix.app import ipc_bridge as _b`), sem uma linha reescrita.",
      "falta": "As TRÊS leituras que a sprint existe para matar continuam vivas na aba GTK. O `grep` da mordida — `grep -rn state_full src/hefesto_dualsense4unix/app/actions/config/` — devolve `secao_controles.py:734`, `secao_mesa.py:975` e `secao_orcamento.py:671`, e a dívida declarada em `secao_orcamento.py:612` (\"este é o SEGUNDO state_full por entrada na aba\") continua escrita. `dados(host, estado) -> dict` não existe em `secao_*.py` nenhum; `pagina.py` não existe; `tests/unit/test_migra_conexoes_um_state_full_so.py` não existe."
    },
    {
      "id": "MIGRA-CONEXOES-05",
      "feito": "O contador do topo passou a ser conta sobre a mesa DELA: `conta` e `conta-b` casam — `HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 08-conexoes.html` devolve `casam: ['achado','conta','conta-b','mic-existe','perfil','selo','vizinho-nome','vizinho-tipo']`. E o lugar que a mesa não preenche ganha a marca do desenho em vez de mentir: `carga[\"vazios\"]` em `hefesto_vivo.py:679`, aplicado em `:162-172` (`data-conectado=\"nao\"` + classe `off`).",
      "falta": "A `MESA` fixa NÃO morreu — `layout/_ferramentas/monta.py:219` ainda a define e `layout/_ferramentas/aba08.py:2321` gera `linha_do_controle(c) for c in MESA`. A página nasce com QUATRO `gc-item` (medido) e só p1 e p2 carregam `data-controle` (medido: `Counter({'dualsense': 2, 'p1': 1, 'p2': 1})`) — com um terceiro controle na mesa dela não há onde pintar. Pior: quatro dos cinco campos por controle são ÓRFÃOS medidos — o `casamento.py` devolve `órfãos: ['achados','bateria','fragil','graves','ponte','via']`, ou seja, o pacote emite transporte, bateria, ponte e fragilidade e a página não tem onde pôr. Só `mic-existe` casa por controle. Zero/um/cinco controles continuam sem desenho, e o microfone nascer ligado continua sem a palavra dela (§0.2). `tests/unit/test_migra_conexoes_a_mesa_e_a_de_verdade.py` não existe."
    },
    {
      "id": "MIGRA-CONEXOES-07",
      "feito": "As linhas do Check-up passaram a sair do PRODUTO, e este é o avanço mais real da onda. `layout/_ferramentas/pacotes/a08_conexoes.py:257-275` (`_conferencias`) importa `integrations.exame_da_mesa` e chama `energia_do_radio`, `energia_das_portas` e `suporte_ao_controle`; `_exame()` (`:331-352`) devolve as linhas e o pacote emite `selo`+`achado`, que CASAM (comando do casamento acima). O 'Examinar de novo' ganhou dono de verdade — `@gesto(\"08-conexoes.html\", \"examinar-portas\")` em `a08_conexoes.py:869-924` roda o exame COMPLETO com `leitura_das_ordens=exame_da_mesa.leitura_do_sistema`, trazendo `pareamentos` e as ordens de serviço que não cabem no tique. E o ⊘ ganhou sujeito: `@gesto ... \"ignorar\"` (`:927-970`) grava `ordens_dispensadas` com a chave certa (o ARRANJO, não a recomendação) e cala a linha na hora.",
      "falta": "No TIQUE só rodam TRÊS das cinco conferências (`a08_conexoes.py:262` lista as três); `pareamentos` e `vizinhanca_das_portas` só chegam se ela clicar em Examinar Portas. A tela é endereçada por POSIÇÃO, não por chave: a sprint pede `data-v=\"exame.<chave>.selo\"` e exige igualdade de CONJUNTOS de chaves; o que existe são 5 `data-campo=\"selo\"`/`\"achado\"` preenchidos na ordem, e o piloto apaga o que sobra (`hefesto_vivo.py:152`) — logo a 4ª e a 5ª linha do Check-up ficam VAZIAS até o botão ser clicado. O `data-campo=\"exame\"` é vazio medido (`casamento.py` → `vazios: ['exame']`), porque `normalizar` descarta lista de dicionários (`pacotes/__init__.py:244-247`). Faltam ainda: o selo do quadro por `o_mais_grave`, o carimbo por `frase_de_quando`, os quatro botões na ordem dela, e `tests/unit/test_migra_conexoes_o_exame_mostra_o_que_confere.py`. As TRÊS linhas inventadas continuam no HTML (medido, linhas 3-5 do bloco: 'O controle no cabo tem uma entrada só para ele', '4 rádios vizinhos ativos na faixa de 2,4 GHz', 'Nenhuma outra ordem de serviço pendente') — a contradição do §0.3 segue aberta."
    },
    {
      "id": "MIGRA-CONEXOES-09",
      "feito": "A METADE DOS VIZINHOS está viva, e ela é a que a sprint mais temia. Os rádios são os DELA, lidos do `/sys`: `a08_conexoes.py:114-130` (`_mesa_do_radio` → `mesa_de_radio.ler_a_mesa`) e `:401-413`, que substitui os quatro rádios do mockup pelos dela — `vizinho-nome` e `vizinho-tipo` CASAM, 4 cada. O `<select>` '— O que é? —' grava com o id do produto, traduzido de `secao_mesa._TIPOS_DE_RADIO` (`a08_conexoes.py:143-159`; gesto em `:827-866`), com a chave `vid:pid` e não o nó do sysfs. E o ponto frágil da rota foi respondido: o `<select>` sobrevive com `appearance:none` (`ponte_da_tela.py:118`) e o `change` chega ao Python (`hefesto_vivo.py:191`).",
      "falta": "A TABELA DOS ADAPTADORES continua sendo o mockup: zero `data-campo` no bloco 'Adaptadores Bluetooth' (medido; o texto 'Sala / TP-Link UB500 / Entrada 3 · traseira' é literal do gerador). O pacote até emite `adaptadores` (`a08_conexoes.py:442`), mas `normalizar` descarta dicionário (`pacotes/__init__.py:248-249`), então ele nem chega à tela. O recibo da `D-A-CONEXOES-GRAVA-NA-HORA-E-LEMBRA` não existe. `tests/unit/test_migra_conexoes_a_mesa_de_radio_chega_a_pagina.py` não existe. Dois números do enunciado caducaram: são 13 `<select>` no miolo (não 16), 10 deles com gesto; e o `renomear` NÃO tem gesto porque ela mandou tirar o botão em 31/08 (`layout/_ferramentas/aba08.py:1157`) — o entregável 'data-g=adaptador.renomear' virou pergunta, não dívida."
    },
    {
      "id": "MIGRA-CONEXOES-10",
      "feito": "Nenhum literal: as constantes vêm do dono por AST — `layout/_ferramentas/aba08.py:112-120` lê `SLOTS_POR_SEGUNDO`, `SLOTS_POR_RELATORIO`, `HZ_INPUT_SEM_MIC`, `HZ_INPUT_COM_MIC`, `HZ_AUDIO_COM_MIC` e os cortes de `src/hefesto_dualsense4unix/integrations/radio_da_mesa.py`, e deriva `TETO`/`CUSTO_COM_MIC`/`CUSTO_DO_MIC`. A confissão continua na tela — as três frases estão no `.pista` (medido: 'espec do Bluetooth Classic (625 µs cada) e nunca foram medidas aqui', 'o A/B desta bancada de 25/07/2026, com um controle', 'a soma de 4 é derivada'). E o item 4 fechou: o teto global SAIU desta aba e a Sistema tem o campo — `grep -c 'Perfil de Bateria' layout/09-sistema.html` → 10, com `data-gesto=\"perfil-da-mesa\"`.",
      "falta": "A pista NÃO é pintada: zero `data-campo` no bloco Desempenho (medido) — os blocos `bloco usa`, `bloco mic` e `bloco vaga` são HTML estático gerado da `MESA` de exemplo, e o pacote não os toca. A conta das VAGAS continua vivendo SÓ no gerador (`layout/_ferramentas/aba08.py:1421`), sem dono em `src/`: `grep 'def ' src/.../integrations/radio_da_mesa.py` não tem função de vagas — que é exatamente o risco que o item 3 nomeia ('enquanto ela viver só no gerador, a tela do produto e a tela aprovada podem divergir sem que régua nenhuma veja'). A 'conta só para o rádio' não foi unificada: `secao_orcamento.py:671` e `secao_mesa.py:975` continuam medindo em separado. `tests/unit/test_migra_conexoes_a_regua_do_radio.py` não existe."
    },
    {
      "id": "MIGRA-CONEXOES-12",
      "feito": "A ESPINHA da sprint andou: as duas perguntas da sala MUDARAM DE CASA no desenho e estão LIGADAS. Elas estão dentro da `.tela-nova id=\"mapear-entradas\"` (`layout/08-conexoes.html:2607`; medido: os `data-gesto=\"sala-altura\"` em pos. 234298 e `\"sala-visada\"` em 234797 caem depois do início da tela-nova em 225475). Os gestos são dois, separados de propósito para que responder uma não apague a outra — `a08_conexoes.py:659-681` e `:683-702` — e recusam valor fora do `Literal` do pydantic em vez de derrubar o documento. E o laço fecha: o que ela declara entra em `exame_da_mesa.exame(altura_da_antena=..., linha_de_visada=...)` (`a08_conexoes.py:908-921`), mudando a linha do Check-up desta mesma aba.",
      "falta": "A CHAVE não mudou — `_declarar` manda `machine_declare({\"mesa\": ...})` (`a08_conexoes.py:603`), a chave antiga; não há disciplina nova nem caminho de migração. As perguntas agora estão em DUAS casas: continuam também em `secao_mesa.py:583` (`_declaracoes`, com `_PERGUNTA_DA_ALTURA:335` e `_PERGUNTA_DA_VISADA:344`). Os defeitos das janelas GTK estão intocados: `grep -c ScrolledWindow src/.../app/widgets/mapa_da_mesa.py` → 0 (a janela do desenho continua sem rolar); `mapa_da_mesa.TITULO_DA_JANELA = \"A minha mesa\"` (`:83`) contra `_BOTAO_DESENHAR = \"Desenhar a minha mesa\"` (`secao_mesa.py:1810`), e `calibrar_entradas.TITULO_DA_JANELA = \"Calibrar as entradas\"` (`:155`) contra `_BOTAO_CALIBRAR = \"Ensinar as minhas entradas\"` (`secao_mesa.py:1842`). A lápide da peneira do jogo continua aberta (`tests/unit/portao_a_casa_sabe_e_o_produto_nao_faz.py:1816`). `tests/unit/test_migra_conexoes_as_duas_janelas_e_as_duas_perguntas.py` não existe. E a decisão HTML-ou-GTK (§0.6) continua sem a palavra dela."
    }
  ],
  "abertas": [
    "MIGRA-CONEXOES-06 — o botão do microfone ganha superfície. O único vestígio é o MARCADOR: `data-gesto=\"mic-escopo\"` existe na página (2 ocorrências, os dois `<select>` 'Só este controle / O computador inteiro'), mas ele está em `SEM_GESTO` com o motivo medido escrito (`layout/_ferramentas/pacotes/a08_conexoes.py:496-503`): a contradição de ESCOPO — a tela oferece por controle, `daemon/lifecycle.py:272` guarda um por máquina. Nenhum chamador de tela escreve o campo: `grep -rn button_toggles_system src/ layout/` só acha `profiles/schema.py:451`, `app/draft_config.py` e `daemon/ipc_draft_applier.py:585-593` — o caminho do daemon inteiro, e nenhuma ponta de cá. `tests/unit/test_migra_conexoes_o_botao_do_mic_tem_superficie.py` não existe. A escolha entre as três saídas (§0.7) continua sendo dela.",
    "MIGRA-CONEXOES-08 — a ordem diz o que mover para onde. As DUAS lápides continuam abertas, e é a prova mais direta: `grep -n arranjo_da_mesa tests/unit/portao_a_casa_sabe_e_o_produto_nao_faz.py` devolve `:1598` (`consequencias`) e `:1655` (`receita`), entre outras sete do mesmo módulo. Nenhum chamador em `src/` nem em `layout/_ferramentas/`. Nenhum endereço de `ordem.imperativo`, `ordem.receita.origem/destino` ou `ordem.ganho` na página (os 9 nomes de `data-campo` medidos não incluem nenhum). `tests/unit/test_migra_conexoes_a_receita_chega_a_tela.py` não existe. A trava é a `D-QUAL-REGUA-MANDA-NO-ARRANJO` — a medição que ela pediu existe (`tests/unit/test_as_duas_reguas_do_arranjo_divergem_onde.py`), a frase dela não.",
    "MIGRA-CONEXOES-11 — o teto por controle vence, ou é `min`? Nem a Forma A nem a Forma B. `src/hefesto_dualsense4unix/core/rumble.py:87-103` continua com `_sob_o_teto` devolvendo `min(mult, teto)`, com o docstring intacto explicando os dois argumentos contra o produto. O campo tem marcador na tela (`data-gesto=\"teto-da-vibracao\"`, 2 ocorrências) e está em `SEM_GESTO` com a contradição escrita (`a08_conexoes.py:504-510`), dizendo que sobrepor mudaria o DAEMON e não a tela. A Forma A (teto folgado RECUSADO COM MOTIVO) também não existe — hoje ele seria aceito e ignorado em silêncio, que é o defeito `LIGHTBAR-BT-RESET-01` com outra roupa. `tests/unit/test_migra_conexoes_o_teto_por_controle.py` não existe."
  ],
  "achados": [
    "O ENUNCIADO DA TAREFA CARREGA UM NÚMERO ERRADO. O prompt diz '18 sprints distintas' no índice. O índice diz DOZE, com todas as letras — `docs/process/sprints/2026-08-29-MIGRA-CONEXOES-INDICE.md` tem a seção '## Por que DOZE, e não as onze do censo' e a tabela '## As doze'. E `ls docs/process/sprints/*MIGRA-CONEXOES*` devolve 13 arquivos: as doze mais o índice. Auditei as doze. (O índice prevê que a onda VIRE catorze se ela escolher a Forma B da sprint 12 — talvez seja daí a confusão, mas 14 também não é 18.)",
    "ZERO SPRINTS FECHARAM, E A CAUSA É ESTRUTURAL, NÃO DESLEIXO: o trabalho de 01/09 tomou uma ROTA DIFERENTE da que as doze especificam. As sprints endereçam `src/hefesto_dualsense4unix/app/actions/config/pagina.py`, `src/.../gui/telas/08-conexoes.html` e `scripts/telas/aba08.py`; o que existe é `layout/_ferramentas/hefesto_vivo.py` + `layout/_ferramentas/pacotes/a08_conexoes.py` + `layout/08-conexoes.html`. As três pastas que as sprints pressupõem (`gui/telas/`, `scripts/telas/`, e o `pagina.py`) NÃO EXISTEM. Nenhum dos doze arquivos de `cria:` foi criado: `ls tests/unit/ | grep migra_conexoes` → nenhum. Nove sprints avançaram em SUBSTÂNCIA; nenhuma fecha pelo próprio critério. Quem for subir versão sobre isto precisa saber que a aba Configurações do produto GTK está INTOCADA — `install_config_tab` monta as mesmas cinco molduras de sempre.",
    "A MORDIDA DA SPRINT 02 REPROVARIA O PRODUTO DE HOJE, E MESMO ASSIM O DEFEITO NÃO EXISTE. A sprint exige 'zero `<a class=\"aba\" href=\"0`' porque duas tiras apareceriam (a do `Gtk.Notebook` em cima, a do HTML dentro). Medido: os NOVE `href` continuam lá. Mas o piloto único NÃO TEM `Gtk.Notebook` — é UM WebView só, e a navegação entre as dez abas é justamente esses `href`, com o despachante descobrindo a aba pelo nome do arquivo à vista (`hefesto_vivo.py:14-17` e `_pagina_da_uri:372`). Quem executar a 02 ao pé da letra hoje QUEBRA a navegação da interface nova. A sprint precisa ser reescrita antes de correr.",
    "O ENDEREÇO POR POSIÇÃO VOLTOU, E A SPRINT 03 O PROIBIA COM TODAS AS LETRAS. Ela diz: 'a chave do controle é o `uniq`, NUNCA o `p1`/`p2` do mockup — um endereço por posição volta a ser o jogador 3 fantasma', e a mordida é 'nenhum `data-v` nem `data-g` casa com `^p[0-9]`'. Medido no HTML publicado: `data-controle=\"p1\"` e `data-controle=\"p2\"`, mais `data-v=\"0\"..\"4\"` endereçando as linhas do exame e os blocos de vizinho. O piloto traduz `uniq→pref` (`hefesto_vivo.py:20-28`) e o pacote confere a faixa (`a08_conexoes._slot:755-773`), então não há clique no alvo errado — mas o DOM é posicional, e a sprint 03 e a 05 reprovam isso por escrito. É uma decisão que alguém tomou sem desfazer a proibição.",
    "AS SPRINTS 09 E 12 SE CONTRADIZEM SOBRE AS DUAS PERGUNTAS DA SALA, E O PRODUTO SEGUIU UMA TERCEIRA VIA. A 09 diz: 'As duas perguntas da sala NÃO estão aqui... quem as move é a MIGRA-CONEXOES-12. Entre a saída daqui e a chegada lá elas não estão em tela nenhuma: buraco declarado.' A 12 diz que elas mudam de casa E DE CHAVE. O trabalho de 01/09 fez metade: pôs as duas na tela-nova `#mapear-entradas` e as ligou, mas gravando na chave ANTIGA (`mesa`), e sem tirá-las de `secao_mesa.py:583`. Resultado: as perguntas existem agora em DOIS lugares, na mesma chave. Não é buraco — é duplicata.",
    "UM DOS TRÊS DEFEITOS DA SPRINT 12 JÁ ESTAVA CURADO ANTES DE ELA SER ESCRITA. Ela afirma: 'A ordem da confissão é SORTEADA. As lacunas vivem num `set`... É defeito, e é de uma linha.' Medido: `Bancada.lacunas` é `tuple[str, ...]` (`src/.../integrations/mapa_das_portas.py:466`) e é construída como `lacunas=tuple(sorted(lacunas))` (`:583`); `confissao_do_desenho` itera essa tupla (`app/widgets/mapa_da_mesa.py:404-407`). `git blame` da linha 583 devolve `0b815161e [REDACTED] 2026-08-26` — três dias ANTES de a sprint ser escrita. Quem executar a 12 vai procurar um defeito que não está lá. (Os outros dois — a janela sem `ScrolledWindow` e os dois `TITULO_DA_JANELA` divergentes — conferi e continuam reais.)",
    "DOIS NÚMEROS DO ENUNCIADO DA 09 CADUCARAM, E UM DELES POR DECISÃO DELA. A sprint diz '16 `<select>` no miolo — 12 nas linhas dos controles e 4 nos rádios vizinhos', e pede `data-g=\"adaptador.renomear\"`. Medido hoje, contando TAGS fora de comentário: 13 `<select>` no arquivo, 10 no corpo com gesto (4 vizinho-o-que-e, 2 mic-existe, 2 mic-escopo, 2 teto-da-vibracao). E o Renomear deixou de ser botão por palavra dela em 31/08 — `layout/_ferramentas/aba08.py:1157`: 'O RENOMEAR DEIXOU DE SER BOTÃO — 31/08/2026: tirar o botão Renomear'. O entregável da sprint virou pergunta, não dívida.",
    "O ÓRFÃO MAIS CARO DESTA ABA É A LINHA DO CONTROLE. `HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 08-conexoes.html` devolve `html 9 · pac 14 · casam 8 · órfãos 6 · vazios 1`, e os órfãos são `['achados','bateria','fragil','graves','ponte','via']`. Ou seja: o pacote JÁ CALCULA transporte, bateria, ponte confirmada e fragilidade de cada controle, e a página não tem um `data-campo` onde pôr. O acordeão desta aba — que é o que ela olha para saber quem está no cabo e quem está no rádio — continua mostrando o dado do mockup. Isso é dinheiro no chão: são quatro valores prontos esperando um atributo no gerador.",
    "O `data-campo=\"exame\"` É VAZIO POR CONSTRUÇÃO, E NINGUÉM ACUSOU. O pacote emite `exame` como lista de DICIONÁRIOS (`a08_conexoes.py:439`), e `normalizar` descarta lista de dicionários de propósito (`pacotes/__init__.py:241-247`: 'escrever `[object Object]` numa caixa é pior que nada'). Os 5 `<div class=\"exame\" data-campo=\"exame\">` do HTML nunca recebem nada — o casamento os lista em `vazios`. Não é defeito de dado (o selo e o achado casam), mas é um endereço que existe e não serve para nada, e o `test_o_casamento_das_dez` passa mesmo assim.",
    "O EXAME MOSTRA TRÊS LINHAS E APAGA DUAS ATÉ ELA CLICAR. No tique só rodam três das cinco conferências (`a08_conexoes._conferencias:262`), porque `pareamentos` forka `busctl` com teto de 5 s. O piloto distribui a lista pelos 5 elementos e escreve `''` nos que sobram (`hefesto_vivo.py:152`). Logo, ao abrir a aba, a 4ª e a 5ª linha do Check-up ficam EM BRANCO — sem selo e sem frase — até ela apertar 'Examinar Portas'. É honesto (melhor que mostrar o texto do mockup), mas é um estado que o desenho aprovado não prevê e que ela não viu. Vale a foto antes de fechar."
  ]
}


---

## Agente 6

{
  "onda": "MIGRA-VIBRACAO",
  "total": 8,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-VIBRACAO-01",
      "feito": "O adaptador `app/telas/vibracao.py` EXISTE (298 linhas, /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/src/hefesto_dualsense4unix/app/telas/vibracao.py) e em 01/09 passou a ter chamador: `layout/_ferramentas/pacotes/a05_vibracao.py:88` chama `_tela.pacote_da_mesa(...)`. Mas o arquivo NASCEU em 30/08 (git log --follow: commit 89646354/8fd32ba5), antes desta leva. A aba roda num WebKit2.WebView — no piloto `hefesto_vivo.py:846` (`load_uri(onde.pagina(pagina, publicado=True))`), que é o que `./interface` abre (scripts/abrir_interface.py:78).",
      "falta": "TUDO o que a sprint tem em posse. (1) A página 6 do Gtk.Notebook continua o Glade: `src/hefesto_dualsense4unix/gui/main.glade:1704` `GtkScrolledWindow id=scroll_tab_rumble_box` e `:1713` `tab_rumble_box`; os três adjustments seguem em `:37`, `:45`. (2) O `_read_scales` continua inventando zero: `src/hefesto_dualsense4unix/app/actions/rumble_actions.py:1189-1190` `weak = int(w.get_value()) if w is not None else 0`. (3) Nenhuma das 4 tabelas chaveadas por id mudou, nenhuma das 3 réguas cegas ganhou olho. (4) `git log --since='2026-09-01' -- main.glade app.py rumble_actions.py footer_actions.py` devolve VAZIO — a leva não tocou um byte dos quatro arquivos da sprint. (5) Os dois testes do `cria` não existem."
    },
    {
      "id": "MIGRA-VIBRACAO-02",
      "feito": "O esquema de endereços existe no gerador e na página. Medido em layout/05-vibracao.html: 8 `button[data-papel=forca][data-forca]`, 4 `button[data-papel=lado]`, 4 `.motor[data-papel=motor][data-lado]`, 2 `.motor[data-papel=forca]`, 2 testar, 2 parar, 2 identidade, 4 desenho, 2 `.ctrl[data-uniq]`. `data-forca` carrega a CHAVE do produto e o gerador reprova a si mesmo se divergir de RUMBLE_POLICY_MULT (layout/_ferramentas/aba05.py:105 `SystemExit`). Zero MAC: os `data-uniq` estão vazios, como a sprint manda. A leva de 01/09 acrescentou o segundo vocabulário `data-campo` (aba05.py:469-486, commit 01b9a6b0) — 15 na página.",
      "falta": "(1) A régua do `cria` — `tests/unit/test_migra_vibracao_02_a_pagina_tem_endereco.py` NÃO EXISTE; nenhuma das 6 mordidas (foto pixel a pixel, contagem por papel, `set(RUMBLE_POLICY_MULT)`, MAC, unicidade, filtros) foi escrita. (2) O endereço que o motor NOVO usa não existe: o piloto pinta por coluna com `[data-controle=\"pN\"]` (hefesto_vivo.py:173) e a 05 tem ZERO — é a única das dez com colunas e sem raiz (01:4, 02:4, 03:4, 04:2, 06:2, 08:2, 05:0). (3) O critério numérico da sprint (48 gestos, 4 colunas × 12) é inatingível: a decisão dela de 31/08 (monta.py:206-227) deixa DOIS conectados, e a coluna vazia é proibida de ter ajuste vivo (aba05.py:743). Hoje são 22 elementos endereçados em 2 colunas."
    },
    {
      "id": "MIGRA-VIBRACAO-03",
      "feito": "A camada do produto monta coluna POR CONTROLE do estado: `app/telas/vibracao.py:236 pacote_da_mesa` chaveia por `uniq` de `conectados`, e `aba05.py:534 _coluna(c, e)` aceita o estado vivo. O lugar vazio tem tela (`aba05.py:503 _coluna_vazia`). Isso é de 30/08; o que 01/09 fez foi LIGAR: a05_vibracao.py:88.",
      "falta": "(1) A grade continua fixa em quatro: `layout/_ferramentas/aba05.py:189` `grid-template-columns:var(--larg-rot) repeat(4,1fr)` — a sprint pede `repeat(N,1fr)`. (2) O `ESTADO` chumbado com p1..p4 continua em `aba05.py:110`. (3) Zero controles não tem frase — não há tela de mesa vazia. (4) O item 4 da sprint (\"o adaptador pinta data-uniq, a identidade e a cor por coluna a cada state_full\") NÃO acontece: medido, o pacote emite `colunas={'p1': {...}}` e a página tem `data-controle=\"p1\"` ZERO vezes → a pintura por coluna escreve nada. Consequência viva: com UM controle na mesa, a coluna do P2 continua mostrando o que o mockup escreveu (o defeito que hefesto_vivo.py:657-668 diz ter curado nas outras abas — a cura é por `data-controle`, que a 05 não tem). (5) A decisão dela sobre a cor no rádio segue em aberto. (6) O teste do `cria` não existe."
    },
    {
      "id": "MIGRA-VIBRACAO-07",
      "feito": "O valor certo é CALCULADO no caminho da tela: `app/telas/vibracao.py:232` emite `\"treme\": {lado: bool(motores[lado])}` a partir de `motores_do_controle` (`:163`), que delega a `controller_card.motores_no_fisico` — o leitor que já sabe da idade (ATIVIDADE_FRESCA_S) e do `(0,0)` fresco. O par lado→motor está no dono único (`LADO_PARA_MOTOR = {\"e\": \"strong\", \"d\": \"weak\"}`, vibracao.py:46), sem inversão. Isso é de 30/08.",
      "falta": "NADA CHEGA À TELA. O pacote da aba descarta o `treme` no achatamento (a05_vibracao.py:90-101 leva só identidade/forca/forca-pct/motor-*), e o `casamento.py` confirma: `HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 05-vibracao.html` → `vazios: ['desenho', 'lado', 'motor', 'parar', 'testar']` — o `[data-papel=\"desenho\"]` (4 na página) não recebe valor nenhum. Nenhum `feat-rumble-esquerdo`/`-direito` é tocado por linha de código nenhuma do piloto (grep 'feat-rumble' em hefesto_vivo.py: 0). Os dois testes do `cria` não existem."
    }
  ],
  "abertas": [
    "MIGRA-VIBRACAO-04 — a força por endereço. `grep -rn \"rumble_policy_por_uniq|politica_do_uniq\" src/` devolve ZERO. `_handle_rumble_policy_set` (daemon/ipc_handlers.py:4466) não tem `uniq`. `_game_rumble_mult` não recebe endereço. O `to_ipc_dict` continua descartando a seção por unidade. O pacote de 01/09 ESCREVE que a política é da mesa e que clicar numa coluna muda as quatro (a05_vibracao.py:255-262) — a sprint continua inteira, e o defeito que ela existe para curar está agora documentado dentro do produto novo. O teste do `cria` não existe.",
    "MIGRA-VIBRACAO-05 — as quatro travas. `daemon_cfg.rumble_active` continua um PAR único (ipc_handlers.py:3245-3247 publica `[weak, strong]`; `:4307` grava `(weak, strong)` e `:4308` grava `rumble_active_uniq`). `rumble.set`/`rumble.stop` continuam sem `uniq` — está medido no próprio pacote (a05_vibracao.py:126-131, censo do daemon). Nenhum dos dois testes do `cria` existe. E a leva foi pela rota que a sprint declara PROIBIDA — ver achados.",
    "MIGRA-VIBRACAO-06 — os oito interruptores de lado. `grep -rn \"motor_esquerdo|motor_direito|lado_desligado|rumble_lados|rumble.lados\" src/` devolve ZERO — nem campo em RumbleConfig/ControllerRumbleOverride/RumbleDraft, nem porteira em apply_game_rumble/escrever_rumble_no_dono, nem IPC, nem função de ponte. Na tela os 4 `button[data-papel=\"lado\"]` existem e NÃO têm gesto: `pacotes.GESTOS` traz só ('05-vibracao.html','forca'|'testar'|'parar'), então o clique cai em `gesto_da_pagina(...) is None` → `[gesto sem dono]` (hefesto_vivo.py:501-504). Os dois testes do `cria` não existem.",
    "MIGRA-VIBRACAO-08 — o que a tela deixou de dizer. `grep -o 'data-papel=\"avisos\"' layout/05-vibracao.html` → 0. `grep -c \"Deixar o jogo\" layout/*.html` → 0 nos DEZ arquivos, e a constante segue viva em rumble_actions.py:108 `BTN_GIVE_BACK_TO_GAME`, citada pelo banner de qualquer aba em status_actions.py:2267-2269 (\"aba Rumble → …\"). Não há linha de estado, não há sufixo do teto do orçamento, não há área de recusa do Modo Nativo. `estado_da_coluna` (vibracao.py:265), que é justamente a peça desta sprint, continua SEM CHAMADOR. Os dois testes do `cria` não existem."
  ],
  "achados": [
    "A PINTURA DA ABA 05 DESTRÓI O DESENHO QUE ELA APROVOU, a cada 500 ms — provado em DOM real (Chrome headless, sem janela). O pacote emite `mesa = {\"forca\": \"balanceado\", ...}`; o piloto pinta campo de mesa com `achar(document, 'forca')`, que casa `[data-campo=\"forca\"]` (2) E `[data-papel=\"forca\"]` (10) = 12 elementos, e `escrever()` escreve `textContent` (hefesto_vivo.py:117, 133, 143-146). Rodado sobre layout/05-vibracao.html com o BOOTSTRAP real e a carga real: `pintou n = 12`; os oito degraus ['Economia','Balanceado','Máximo','Auto', ×2] viraram ['balanceado' ×8]; a barra `.motor[data-papel=\"forca\"]` teve o innerHTML de 213 caracteres (interruptor + trilho + número + teto) substituído pela string 'balanceado'. Script da prova: /tmp/claude-1000/-mnt-Apate-Desenvolvimento-hefesto-dualsense4unix-dev/4d211abb-52de-46ad-83fc-f5e1943569e6/scratchpad/dom05.py",
    "\"TESTAR\" E \"PARAR\" NÃO DISPARAM NA PÁGINA PUBLICADA — nunca. O ouvinte manda `controle: dono.dataset.controle || dono.dataset.uniq` (hefesto_vivo.py:219-226); a coluna da 05 é `.ctrl[data-uniq=\"\"]` (vazio por contrato do mockup) e não tem `data-controle`, então `controle` chega \"\" e a resolução `pref → uniq` (hefesto_vivo.py:510-513) não casa nada. Rodado com esse payload exato: `testar -> levantou: ValueError o clique não disse em qual controle — e sem alvo a mesa inteira treme`; `parar` idem. Script: .../scratchpad/gesto05.py. Dois dos três gestos da aba são inalcançáveis; só o `forca` (que lê `data-forca` do próprio botão) funciona — e é o único que a validação no aparelho dela registrou (commit fe9da58a: \"05 forca rumble_policy: balanceado → economia\").",
    "AS DUAS RÉGUAS NOVAS DÃO VERDE SOBRE ISSO, e pela mesma forma. `tests/unit/test_os_botoes_tem_dono.py:110-111` monta o clique com `base = {\"controle\": \"p1\", \"uniq\": UNIQ, ...}` — injeta o endereço que a página real não entrega, então prova o gesto e não o botão. `layout/_ferramentas/casamento.py` mede INTERSEÇÃO DE NOMES, não escopo: devolve `casam: 10` para a 05 enquanto zero desses dez valores alcança um elemento, porque a raiz `[data-controle=\"pN\"]` não existe. É o padrão que esta casa já nomeou — a régua que confunde a palavra com o ato.",
    "A 05 É A ÚNICA ABA COM COLUNAS E SEM RAIZ DE COLUNA. Contagem de `data-controle=\"pN\"` nas dez páginas publicadas: 01→4, 02→4, 03→4, 04→2, 06→2, 08→2, 05→0 (07, 09 e 10 não têm colunas). O guia `layout/_ferramentas/pacotes/COMO-LIGAR-UMA-ABA.md` não menciona `data-controle` uma única vez — o padrão que os agentes leram não carrega a regra que faz a pintura por coluna funcionar.",
    "A LEVA FOI PELA ROTA QUE A MIGRA-VIBRACAO-05 DECLARA PROIBIDA. A sprint pede a régua `test_migra_vibracao_05_a_aba_nao_troca_o_alvo_global.py` com o critério \"nenhum caminho desta aba chama `controller.target.set`\", citando a medição da PERFIL-01 (backend_pydualsense.py:4770-4774). O pacote de 01/09 chama exatamente isso, duas vezes por gesto: `a05_vibracao.py:232` `p.chamar(\"controller.target.set\", index=_indice(ctx, uniq))` dentro de `_mirar()`, usado por `testar` e por `parar`. O pacote argumenta a escolha por escrito (rumble.set/stop não têm endereço), mas a decisão contradiz a sprint e ninguém a declarou caduca.",
    "O `parar` TOMOU UMA DECISÃO QUE A SPRINT 08 RESERVA PARA ELA. A 08 põe duas saídas na mesa — (a) o botão \"Deixar o jogo controlar a vibração\" volta, (b) o \"Parar\" passa a devolver ao jogo — e escreve \"nenhuma é do executor\". O pacote implementou (b): `a05_vibracao.py:335-341` faz `rumble_stop_checked()` e em seguida `rumble_passthrough(True)`. O `testar` faz o mesmo par no fim. A escolha pode ser a certa; ela não foi feita por ela, e nenhuma nota datada registra a mudança.",
    "OS \"12 FILTROS MORTOS\" DA SPRINT 02 NÃO REPRODUZEM. Medido hoje em layout/05-vibracao.html: 17 referências `filter: url(&quot;#…&quot;)` e 17 `<filter id>`; ZERO referências sem alvo. As 12 de dentro dos SVGs `vb-pN` apontam para os ids NÃO prefixados (`outline-filter-1`, `outline-filter-2`), que existem no documento porque o SVG da LOGO do Hefesto os define. Ou seja: o WebKit vai desenhar — mas com o filtro de outro SVG. O número e o diagnóstico da sprint (\"nenhum casa\", \"o WebKit não desenha\") precisam ser refeitos antes de virarem cura, e a cura pode já não custar os 1,09% de desenho que ela teria de aprovar.",
    "O ÍNDICE TRAZ OITO SPRINTS, NÃO DOZE. `ls docs/process/sprints/*MIGRA-VIBRACAO*` devolve 8 sprints + o índice, e a tabela \"As oito\" do índice lista 01..08. As seis `ONDA-VIBRACAO-*` de 27/08 continuam no disco (três que o índice manda dissolver, três que ele diz virarem 04/06/07); nenhuma foi apagada, e o manifesto de apagamento que o índice exige não foi escrito.",
    "NENHUM DOS 13 ARQUIVOS DE TESTE QUE A ONDA PROMETE EXISTE. Conferido um a um em tests/unit/ (925 arquivos): os 13 nomes do `cria` das oito sprints estão todos ausentes. As réguas que a leva escreveu (test_os_botoes_tem_dono, test_o_casamento_das_dez, test_o_rodape_das_dez, test_o_perfil_chega_na_tela) são de outro alvo — nenhuma delas mede uma linha do contrato desta onda.",
    "DUAS CAMADAS DO PRODUTO CONTINUAM SEM CHAMADOR, e o pacote reescreveu uma delas. `grep -rn` fora do próprio módulo: `estado_da_coluna` (app/telas/vibracao.py:265) e `gesto_do_clique` (`:289`) só aparecem em prosa — a docstring do a05_vibracao.py:62-63 e um comentário do portão `casa-sabe`. O `gesto_do_clique` é a tradução do clique que o produto já tinha; o pacote escreveu a sua (`_uniq`, `_indice`, `_resposta`, `_mirar`) em vez de chamá-la. O `estado_da_coluna` é a peça que remontaria a coluna pelo gerador — que é justamente o que faltou para a coluna viva parar de mostrar a cena do mockup.",
    "A ONDA INTEIRA AINDA ESTÁ PRESA À PALAVRA DELA, e isso é do desenho do índice, não atraso: todas as oito trazem `MIGRA-CONTROLES-PILOTO` no `depois_de`, e o índice diz \"as oito SE ESCREVEM agora; nenhuma se executa antes desse ok\". Sete decisões dela continuam abertas (a cor do plástico no rádio, o Auto na coluna, o Parar da coluna ou da mesa, o lado desligado por peça e no jogo, o botão do passthrough, onde mora a área de aviso, os filtros). O que a leva de 01/09 fez na aba 05 foi ligar TRÊS gestos e um pacote de pintura por fora dessa fila — sem tocar em nenhum dos oito contratos, e com os dois defeitos de tela acima."
  ]
}


---

## Agente 7

{
  "onda": "MIGRA-PERFIS",
  "total": 6,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-PERFIS-01",
      "feito": "As DUAS PONTES existem, e são das dez abas: /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/src/hefesto_dualsense4unix/gui/ponte_da_tela.py — os quatro pinos em :84-87 (Gtk 3.0, Gdk 3.0, GdkPixbuf 2.0, WebKit2 4.1), register_script_message_handler em :185, WebKit2.WebView em :198, e o load-failed tratado em :95 e :360 (a armadilha do FINISHED depois do load-failed está paga e escrita). A aba Perfis RENDERIZA de verdade no motor novo pelo piloto layout/_ferramentas/hefesto_vivo.py, que é o que ./interface abre. E o módulo que a sprint manda criar existe: src/hefesto_dualsense4unix/app/actions/perfis_web.py (23.310 bytes, criado 01/09 11:46).",
      "falta": "TUDO o que a sprint pede DENTRO DA JANELA DO PRODUTO. (1) A faixa não morreu: `awk 'NR>=2119 && NR<=2694' src/hefesto_dualsense4unix/gui/main.glade | grep -oc 'id=\"'` devolve 38 — os 38 ids seguem lá, com profile_editor_stack:2357, profile_window_class_entry:2552, profile_title_regex_entry:2567, profile_process_name_entry:2582, profile_mode_frame:2640, profile_priority_scale:2330. (2) install_profiles_tab (profiles_actions.py:1331) continua montando Gtk.TreeView + Gtk.ListStore; `grep -c WebKit` em app/app.py e em profiles_actions.py devolve 0 e 0. (3) O teto elástico não saiu: 'profiles_paned' segue em _PAGINAS_COM_TETO_ELASTICO (app/app.py:1371). (4) A página NÃO nasce vazia: layout/10-perfis.html traz os catorze nomes de exemplo do mockup (data-hef-perfil=\"Mortal Kombat\", \"Elden Ring\", \"Orpheus\", …) e quatro controles de exemplo na tabela de baixo — a régua que a sprint chama de 'a que impede o defeito mais fácil desta rota inteira' não existe e o defeito está publicado. (5) A foto: scripts/gui-captura/retratar_abas.py não foi tocado (último commit e39a9290, de 31/08) e continua fotografando a aba GTK. (6) A segunda dependência de fora segue aberta: pyproject.toml:84-91 ainda embarca só gui/*.glade e gui/assets/*.png — o HTML não viaja no wheel. (7) tests/unit/test_migra_perfis_01_a_pagina_troca_de_motor.py não existe."
    },
    {
      "id": "MIGRA-PERFIS-02",
      "feito": "Os dezenove endereços da tabela EXISTEM no HTML publicado. Medido em layout/10-perfis.html: 18 `data-hef` distintos (perfis.conta, perfis.com-ajuste, perfis.lista, perfis.linha.nome/prioridade/quando, perfis.remover, editor.nome/prioridade/prioridade.n/prioridade.dica/ambiente/jogo/estilo, guarda.linhas/nome/id/secao), mais as duas famílias por linha — 14 `data-hef-perfil` e 4 `data-hef-uniq` —, 16 `data-hef-secao` (4 leds + 4 triggers + 4 rumble + 4 speaker), 1 `class=\"ativo\"`, 10 `--plastico` e `title` nas 14 <tr> da lista e nas 4 da guarda. Os dois <tbody> têm endereço próprio. O gerador emite: `grep -c data-hef layout/_ferramentas/aba10.py` → 34.",
      "falta": "(a) A régua declarada não existe: tests/unit/test_migra_perfis_02_todo_valor_tem_endereco.py não está em tests/unit/ — não há teste de existência, nem de unicidade, nem a mordida 'ZERO PIXEL MUDOU' (nem no Chrome nem no WebKitGTK). (b) TRÊS endereços estão pregados no lugar errado e o pintor real os torna DESTRUTIVOS — medido em Chrome headless injetando o BOOTSTRAP de layout/_ferramentas/hefesto_vivo.py:97 sobre a página publicada: `editor.prioridade.dica` está no <span class=\"val\"> ANCESTRAL do trilho e do número (10-perfis.html:1147) — antes da pintura tem_trilho=true, tem_n=true; depois, os dois FALSE, a linha 'Prioridade' vira uma frase; `guarda.linhas` está no <tbody> (10-perfis.html:1198) — antes 4 <tr>, depois 0 <tr> e textContent='1'; e `editor.prioridade` está no <span class=\"cheio\"> sem data-hef-alvo=\"largura\", então recebe o número como TEXTO dentro da barra. (c) `data-hef-perfil` está morto no produto: a pintura nunca reescreve o atributo, e a própria docstring de a10_perfis.selecionar (layout/_ferramentas/pacotes/a10_perfis.py:311) diz 'Ler o data-hef-perfil da linha traria o nome do MOCKUP'. O item 3 da sprint ('a chave da linha é a chave do dado') é HTML e não é dado."
    },
    {
      "id": "MIGRA-PERFIS-03",
      "feito": "As três funções puras ATRAVESSAM sem uma linha nova: ordem_de_exibicao, rotulo_quando_usar e explicacao_da_disputa são importadas de profiles_actions em perfis_web.py:66-70 e usadas em _linhas_da_lista (perfis_web.py:342-362). O nome vai como DADO, nunca como markup — o pintor escreve textContent/value (hefesto_vivo.py:116-118); medido com os 33 nomes reais do disco dela, tem_img_injetada=false. A lista repinta viva: a10_perfis.pacote:196 delega para _tela.pacote_da_aba, e o casamento fecha 20 endereços (`HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 10-perfis.html` → html 21, pac 27, casam 20).",
      "falta": "(a) SÓ CATORZE LINHAS APARECEM. Ela tem 33 perfis no disco (medido: load_all_profiles() → 33) e a página tem 14 <tr>; o pintor distribui a lista pelos elementos existentes, na ordem (hefesto_vivo.py:151), e não cria nem apaga linha. Dezenove perfis dela nunca chegam à tela, sem uma palavra. (b) A LINHA VERDE PODE MENTIR. O pacote nunca emite `ativo`: a classe .ativo é a estática do mockup, na primeira linha. Só parece certa porque pacote_da_aba põe o ativo em primeiro — e o faz comparando NOME com `==` (perfis_web.py:357), enquanto o resto da casa compara por SLUG (a10_perfis._gravar:452 usa mesmo_slug, citando R-10 e profiles/slug.py:52). Medido: ativo='fps' → nenhuma linha marcada e a lista fica alfabética; a foto mostra o verde em 'Ação' com o cabeçalho dizendo 'Perfil ativo fps'. (c) ZERO PERFIS NÃO É A FRASE HONESTA: pacote_da_aba calcula `lista_vazia` (perfis_web.py:459) e não há endereço na página que a receba nem chave em a10_perfis.pacote que a repasse. (d) realce_do_perfil_ativo (profiles_actions.py:466) e o import de Pango não foram apagados — o item 2 da sprint ('menos código, não mais') não foi feito. (e) tests/unit/test_migra_perfis_03_a_lista_chega_viva.py não existe: nem a régua da injeção, nem a de tabela vazia, nem a da thread."
    },
    {
      "id": "MIGRA-PERFIS-04",
      "feito": "É a parte mais forte da onda. Os cinco campos pintam do Profile pelas funções puras que já existiam: detect_simple_preset e simple_extra importados em perfis_web.py:72-75, _pacote_do_editor em :308. E A VÁLVULA EXISTE E MORDE: _ambiente_do_perfil (perfis_web.py:259) devolve rótulo None para browser/terminal/editor E para regra que a tela não sabe mostrar, com a frase do que fazer; os dois gestos que escrevem RECUSAM enquanto travado — a10_perfis.py:618-620 (editor.ambiente) e :676-678 (editor.jogo), ambos `if editor.get(\"ambiente_travado\"): raise ValueError(recado)`, lendo o perfil do DISCO. Medido no perfil 'FPS': editor.ambiente.travado=True com o recado 'Este perfil casa por uma regra que esta tela não sabe mostrar — o seletor fica travado para que salvar não a rebaixe… (casa por título de janela e 12 nome(s) de programa)'. 'Estilo de Jogo' não engole escolha: não há @gesto para editor.estilo (os 10 registrados em a10_perfis são selecionar, ativar, voltar-a-de-ontem, editor.nome, editor.ambiente, editor.jogo, detectar, novo, duplicar, remover) e PRESET_DO_ROTULO o exclui de propósito (a10_perfis.py:445), então from_simple_choice nunca grava MatchAny() calado.",
      "falta": "(a) O ESTADO HONESTO NÃO CHEGA À TELA. editor.ambiente.travado, editor.ambiente.recado, editor.estilo.travado e editor.estilo.recado são os quatro ÓRFÃOS que o casamento.py acusa — a página não tem onde pô-los. Medido em Chrome depois da pintura real: amb_disabled=false, estilo_disabled=false, amb_value=\"\". Ela vê DOIS seletores em branco, habilitados, sem uma palavra do porquê — e a sprint pede, com todas as letras, 'o seletor aparece desabilitado com a frase' e 'o <select> está desabilitado'. (b) A recusa é invisível: sai em stderr, no terminal (dito no próprio produto, a10_perfis.py:812). (c) O campo Prioridade não pinta: a pintura APAGA o trilho e o número (ver MIGRA-PERFIS-02). (d) A página avançada não morre: profile_editor_stack e os três entries seguem em main.glade (:2357, :2552, :2567, :2582). (e) tests/unit/test_migra_perfis_04_o_editor_pinta_os_cinco_campos.py não existe — a MORDIDA PRINCIPAL (pintar, tocar no seletor, salvar, e o match gravado ser o do disco) não tem régua nenhuma. (f) A trava dela continua aberta e o código a declara: cinco ou seis ambientes (perfis_web.py:272-278 cita decisoes-dela.csv:55 contra :84)."
    },
    {
      "id": "MIGRA-PERFIS-05",
      "feito": "Dos treze gestos da tabela da sprint, OITO ganharam @gesto que chama o produto: selecionar (a10_perfis.py:296), ativar (:323), novo (:735), duplicar (:772), remover (:805, com pergunta armada por 8s no RÓTULO do botão), editor.nome (:512), editor.ambiente (:576), editor.jogo (:628). Prioridade ficou SEM gesto, exatamente como a sprint manda enquanto ela não decide. recarregar e editor.estilo ficaram sem dono com o motivo medido escrito (a10_perfis.py:880-895). O Salvar do rodapé existe como @gesto(\"*\", \"salvar\") em layout/_ferramentas/pacotes/rodape.py:114. `pytest tests/unit/test_os_botoes_tem_dono.py tests/unit/test_o_casamento_das_dez.py tests/unit/test_o_rodape_das_dez.py tests/unit/test_o_perfil_chega_na_tela.py` → 75 passed; PISO_DA_ABA=10 em a10_perfis.py:898.",
      "falta": "(a) A GUARDA NÃO ATRAVESSOU A PONTE — que é o título da sprint. _assinatura_da_regra_no_editor segue em profiles_actions.py:4050, intacta (mtime ago 29 15:37), servindo só a janela GTK; não há _regra_tocada, _prioridade_tocada nem _modo_tocado no caminho novo. O que existe é _so_mudou (a10_perfis.py:487), que só separa `click` de `change`. A SALVAR-NAO-REBAIXA-01 não foi migrada — foi CONTORNADA, trocando o Salvar por gravação imediata por campo. (b) 'UM SALVAR SÓ: contar os gestos de gravação registrados na ponte → 1' — medido: NOVE gravam perfil em disco nesta aba (editor.nome, editor.ambiente, editor.jogo, detectar, novo, duplicar, remover, voltar-a-de-ontem, mais rodape.salvar). (c) A DIVERGÊNCIA DOS DOIS ALVOS NÃO FOI FECHADA, foi ATRAVESSADA: rodape.salvar:129 grava no perfil ATIVO (ctx.state['active_profile']) e os gestos do editor gravam no ESCOLHIDO (_perfil_do_editor, a10_perfis.py:421). A sprint EXIGE que a ONDA-PERFIS-08 tenha fechado qual alvo manda, e diz 'se ela não fechou, esta sprint para aqui'. (d) 'os três desligados estão desligados' está contrariado: detectar (:682) e voltar-a-de-ontem (:356) ganharam motor, que a sprint atribui à ONDA-PERFIS-03 e à ONDA-PERFIS-05. (e) 'a caixa bloqueia a página' virou sem objeto: não há modal no caminho novo — logo não há prova de bloqueio. (f) tests/unit/test_migra_perfis_05_a_guarda_atravessa_a_ponte.py não existe."
    },
    {
      "id": "MIGRA-PERFIS-06",
      "feito": "A CAMADA existe e está certa: _linhas_da_guarda (perfis_web.py:378) monta uma linha por controle PRESENTE na mesa; _secoes_do_controle (:365) decide aceso/apagado pelo `is not None` do ControllerOverrides — o merge que já existe, não uma segunda regra; _texto_do_ajuste (:231) faz a conta do topo; _id_visivel (:244) formata o uniq; a dica de cada linha diz 'N de 4 ajustes só deste controle' ou 'nada só dele — herda os quatro ajustes do perfil'. Medido com uma mesa de DOIS controles: duas linhas, e o topo 'de 2 controles com ajuste próprio neste perfil' — a conta segue a mesa, não um número fixo. GUARDA_SEM_MESA e GUARDA_SEM_DAEMON separam 'mesa vazia' de 'Hefesto desligado'.",
      "falta": "NADA DISSO CHEGA À TELA — a tabela está VAZIA na página pintada. (a) Medido em Chrome com o BOOTSTRAP real: antes da pintura o <tbody data-hef=\"guarda.linhas\"> tem 4 <tr>; depois tem 0, e o textContent do tbody é '1'. A foto confirma: sob o cabeçalho 'Controle · Ajuste próprio · ID da peça' só existe um '1'. (b) O achatamento de guarda.secao está errado: a10_perfis.py:283 faz `[s for g in guarda for s in (g.get('secoes') or [])]`, e iterar um DICT devolve as CHAVES — medido: ['leds','triggers','rumble','speaker','leds','triggers','rumble','speaker']. São as palavras, não o estado aceso/apagado; se pintassem, escreveriam texto por cima dos glifos SVG. (c) A contagem de linhas não segue a mesa: a página tem 4 <tr data-hef-uniq> fixos e nada as remove — com um controle, três ficariam com travessão. (d) guarda_vazia (a frase de zero controles) é calculada e não tem endereço na página. (e) A cor do plástico não segue a mesa: não há data-hef para --plastico, então as 10 cores da página são as do mockup; e nas linhas medidas `plastico` e `nome` voltaram vazios. (f) O desvio da FOTO com a máscara da casa não existe: retratar_abas.py não monta esta tabela e _id_visivel só formata (aabbcc000001 → AA:BB:CC:00:00:01), não mascara. (g) tests/unit/test_migra_perfis_06_o_ajuste_proprio_nasce_da_mesa.py não existe — a mordida do anonimato, que a sprint diz medir o que os dois portões da casa não medem, não foi escrita."
    }
  ],
  "abertas": [],
  "achados": [
    "A REGRA DA PRÓPRIA ONDA FOI QUEBRADA, e é a mais alta dela: 'AS SEIS FECHAM JUNTAS OU NENHUMA FECHA'. A 01 — declarada 'o gargalo de todo mundo' — é a MENOS avançada (glade intacto, install_profiles_tab em Gtk.TreeView, WebKit=0 em app.py e profiles_actions.py), enquanto 03/04/05/06 ganharam substância por uma rota que não passa por ela: o piloto layout/_ferramentas/hefesto_vivo.py, que ./interface abre, e não a janela do produto. Hoje existem DUAS abas Perfis vivas ao mesmo tempo — a GTK de sempre e a do piloto — e as duas leem o mesmo disco dela.",
    "O PINTOR COMPARTILHADO NÃO SABE A REGRA QUE A CASA JÁ ESCREVEU. `escrever` (hefesto_vivo.py:116) faz `el.textContent = t` em QUALQUER elemento, e layout/_ferramentas/conexoes_vivas.py:68 já traz a regra em prosa: 'Escreve TEXTO, e só em folha. Escrever textContent num container APAGA os filhos'. Os dois estragos medidos nesta aba (a linha da Prioridade e a tabela do Ajuste próprio) são a MESMA forma, e ela vai reaparecer em qualquer aba que enderece um container.",
    "DUAS VERDADES SOBRE OS MESMOS BOTÕES, DENTRO DO PRODUTO: perfis_web.GESTOS_SEM_MOTOR ainda declara `detectar` ('o IPC não publica o título nem a classe') e `voltar-a-de-ontem` ('falta a tela') como sem motor — e a10_perfis registra gesto funcionando para os dois (:682 e :356). O mesmo pacote que delega a perfis_web para escrever as frases honestas contradiz duas delas.",
    "O MESMO ERRO, TERCEIRA VERDADE: perfis_web.DONOS_DOS_GESTOS['salvar'] diz que o Salvar do rodapé 'NÃO está ligado aqui' porque os dois alvos divergem — enquanto rodape.py:114 o registra como @gesto('*','salvar') e ele grava no disco dela.",
    "O DEFEITO R-10 REINCIDE, e a casa já o pagou: perfis_web.py:357 marca o perfil ativo com `str(p.name) == ativo`, comparação crua por nome, enquanto a10_perfis._gravar:452 usa mesmo_slug citando profiles/slug.py:52 justamente por isto. Medido: com active_profile='fps' nenhuma linha é marcada, a lista não reordena, e a tela deixa o verde no primeiro perfil alfabético — a linha verde afirmando um perfil que não está valendo.",
    "A DECISÃO QUE MUDOU O MODELO DE GRAVAÇÃO NÃO ESTÁ REGISTRADA. rodape.salvar (rodape.py:116) atribui a gravação sem perguntar a 'a decisão dela de 01/09: a interface nova é de AÇÃO IMEDIATA' — e `grep '2026-09-01\\|01/09/2026' docs/data/decisoes-dela.csv` não devolve nada (o arquivo tem 125 linhas). É o que substituiu a maquinaria anti-perda inteira da MIGRA-PERFIS-05, e é palavra sem lápide.",
    "NENHUM DOS SEIS ARQUIVOS DE TESTE DECLARADOS NO `cria:` DAS SPRINTS EXISTE: `ls tests/unit/test_migra_perfis*` não casa nada. As quatro réguas novas de 01/09 (test_os_botoes_tem_dono, test_o_casamento_das_dez, test_o_rodape_das_dez, test_o_perfil_chega_na_tela — 75 passed) medem OUTRA coisa: que o gesto chama o daemon e que o pacote emite onde a página tem onde pintar. Nenhuma delas mede o que a tela MOSTRA — e é exatamente por isso que os 30 portões estão verdes com a tabela do Ajuste próprio vazia e a linha da Prioridade apagada.",
    "O CRITÉRIO 'ZERO PIXEL MUDOU' DA MIGRA-PERFIS-02 JÁ NÃO DÁ PARA MEDIR do jeito que ela pede. O cabeçalho de a10_perfis.py:3-6 diz que 'o outro agente pôs 77 endereços (data-hef) e 11 gestos nela em 31/08, com um esquema de nomes próprio' — os endereços entraram direto no layout/ publicado, e o desenho de referência (mockup/) não guarda o 'antes' desta aba com que comparar.",
    "TRÊS VOCABULÁRIOS DE ENDEREÇO CONVIVEM (hefesto_vivo.py:124-127): data-campo em oito abas, data-papel só na Vibração, data-hef só na Perfis. A MIGRA-PERFIS-02 previu isso e mandou 'se a sprint da moldura já tiver escolhido outro prefixo, o executor usa o dela; o que não se admite é esta aba inventar um segundo padrão'. Hoje são três, e o piloto aceita os três — a dívida está declarada no código, não fechada."
  ]
}


---

## Agente 8

{
  "onda": "MIGRA-CONTROLES",
  "total": 13,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-CONTROLES-02",
      "feito": "A página e o gerador VIAJAM: `git ls-files layout/02-controles.html layout/_ferramentas/aba02.py` devolve os dois, e `git check-ignore -v` neles sai rc=1 (não ignorados). Era o defeito inteiro da sprint, e fechou em 30/08 (commit 48b4e1a2), não em 01/09.",
      "falta": "A CASA declarada não nasceu: `src/hefesto_dualsense4unix/gui/telas/` e `scripts/telas/` NÃO existem (ls falha nos dois) — e são exatamente os caminhos que MIGRA-SISTEMA-01, MIGRA-CONEXOES-01 e as duas de Lançadores esperam como `MIGRA-MOLDURA-01`. O WHEEL NÃO LEVA A PÁGINA: `pyproject.toml`, `[tool.hatch.build.targets.wheel].include`, tem só `gui/*.glade`, `gui/assets/*.png` e os `.mo` — zero `layout/`. O `install.sh` não copia nada da página: `grep -n 'layout/\\|telas/' install.sh` devolve ZERO linhas. AS FONTES CONTINUAM VINDO DA REDE: `layout/02-controles.html:6-8` carrega `fonts.googleapis.com`/`fonts.gstatic.com`; não há `gui/telas/fontes/` nem um `@font-face` local. E `scripts/check_a_pagina_e_a_do_gerador.py` não existe — o `check_o_desenho_aprovado.py` mede outra coisa (mockup/ × layout/, por sha256)."
    },
    {
      "id": "MIGRA-CONTROLES-03",
      "feito": "`src/hefesto_dualsense4unix/gui/ponte_da_tela.py` existe, 436 linhas, com o contrato quase inteiro: os quatro pinos na ordem com o Gdk antes dos imports (`:84-87`); `register_script_message_handler` com UM argumento, a forma da 4.1 (`:185`); o valor serializado por `json.dumps` e nunca interpolado (`literal_js`, `:143`); UMA chamada por tique, com contador próprio `self.chamadas` como régua do contrato (`:201-216`); recusa com motivo, nunca engolida, guardada em `self.recusas` (`:279`, `_recusar`); a folha de usuário `select{appearance:none}` (`:118`). Nasceu em 30/08 (48b4e1a2), não em 01/09.",
      "falta": "O RECIBO À PÁGINA NÃO EXISTE. A sprint pede que todo gesto devolva à página um recibo — aceito ou recusado, com motivo. O único Python→página do produto é `hefesto_vivo.py:701`, `window.__hef.pintar(...)`; não há um segundo `dizer(...)`. O que existe é `print` no terminal: `hefesto_vivo.py:503-504` (`[gesto sem dono]`), `:520-528` (`[gesto falhou]`, stderr) e `:539` (`[gesto] … → aplicado`). A medição declarada como produto da sprint (`docs/process/medicoes/2026-08-29-quanto-custa-pintar-uma-aba-por-run-javascript.md`) não existe — `docs/process/medicoes/` não existe como diretório. E `tests/unit/test_migra_controles_03_as_duas_pontes.py` não existe."
    },
    {
      "id": "MIGRA-CONTROLES-04",
      "feito": "A página saiu de 8 `id` e ZERO `data-*` para endereços de verdade: `data-controle` em cada cartão (`layout/02-controles.html:1427,1758,2089,2098`), 12 `data-campo` distintos em 32 ocorrências, e endereço de gesto em `data-gesto` (12 botões reais), `data-mudo` (4), `data-rota` (4), `data-mic-modo` (4), `data-sensor` (4). Os cinco rádios do acordeão continuam com o `id` que o CSS `:has(> input:checked)` usa (`:1200-1218`).",
      "falta": "O nome do atributo do cartão é `data-controle`, não o `data-ctl` que a sprint declara — mesma função, contrato diferente do escrito, e não há nota declarando a troca. A LISTA DOS VALORES NÃO É DADO NO GERADOR: não há tabela única em `aba02.py` de onde a régua leia; `tests/unit/test_migra_controles_04_todo_valor_tem_endereco.py` não existe, e a régua que existe (`test_o_casamento_das_dez.py`) compara PACOTE × página, não TABELA × página, nas duas direções. E o casamento não fecha: `HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 02-controles.html` → `html 12 · pac 13 · casam 10 · órfãos 3 · vazios 2`, com órfãos `['l2','r2','via']` e vazios `['l3','r3']`. Os 64 glifos (`data-glifo`) e os analógicos seguem sem endereço nenhum, e 11 dos 23 `<button>` da página não têm `data-gesto`."
    },
    {
      "id": "MIGRA-CONTROLES-05",
      "feito": "A MESA REAL chega à tela em tempo de execução: `hefesto_vivo.py:603` monta `mesa_viva.mesa_do_estado(st, self.leitor.conhecidos())` a cada tique, `:604` traduz `uniq → pref`, `:648` repinta a FITA inteira com os chips da mesa de agora (`carga['fita'] = _fita(ctx.mesa)`), e `:665-682` apaga o que sobra — travessão em todo campo e a moldura `off` nos lugares vazios, com o comentário medindo a reincidência (a foto de 01/09 com UM controle mostrando dois).",
      "falta": "O GERADOR CONTINUA COM A MESA ESCRITA À MÃO: `layout/_ferramentas/monta.py:219-228`, quatro entradas literais (dois `conectado: True`, dois `False`). NÃO HÁ MOLDE: `grep '<template' layout/02-controles.html` devolve zero — a ponte não clona cartão nenhum, ela pinta nos quatro que o gerador desenhou. Logo o teto `N ≤ 4` continua sendo do gerador e o quinto controle continua sem cartão. O ESTADO DE MESA VAZIA não existe: com zero conectados os quatro cartões viram travessão + `off`, que não é a tela que a sprint pede, e não há a frase que diz o quê, por quê e o que fazer. `tests/unit/test_migra_controles_05_a_mesa_e_a_real.py` não existe."
    },
    {
      "id": "MIGRA-CONTROLES-06",
      "feito": "O tradutor existe e tem contrato — mas em `layout/_ferramentas/pacotes/a02_controles.py:19-81` (`@registrar('02-controles.html')`), não em `app/actions/controles_web.py`, que NÃO existe. Uma chamada por tique é garantida pelo piloto (`hefesto_vivo.py:701`). Chegam de fato à tela CINCO das 18 famílias, medido pelo `casamento.py`: bateria (`bateria`, `bateria-barra`), máscara, selo do microfone / mudo do firmware (`mic-selo`), estado do alto-falante (`alto-estado`) e o hexadecimal da barra de luz (`luz-hex`).",
      "falta": "Treze famílias não chegam. Analógicos, os 16 botões e os 64 glifos: sem endereço e sem emissão (`grep glifo` nos pacotes = zero). L2/R2: EMITIDOS e ÓRFÃOS — o casamento lista `l2`,`r2` sem destino na página. Giroscópio: nada. Medidor do microfone e trilho de volume do alto-falante: nada. A COR da barra de luz: só o hex viaja; `--luz`/`data-hef-alvo=\"fundo\"` não é usado. IDENTIDADE no cartão: `via` é ÓRFÃO — o transporte do cartão continua o do mockup. `touch-estado` é um LITERAL `\"Sem toque\"` (`a02_controles.py:77`): casa com a página e não vem de fonte nenhuma. A PROCEDÊNCIA não atravessa: `lightbar_source` e `lightbar_disputada` não são emitidos, e a sprint manda reprovar quem os apague. `LeituraMic.nivel=None` × `0.0`: o pacote nem lê o `mic_monitor`, a distinção não existe do lado novo. O pintor velho sobrevive inteiro (`app/widgets/controller_card.py`, 5.783 linhas, vivo). `tests/unit/test_migra_controles_06_*.py` não existe."
    },
    {
      "id": "MIGRA-CONTROLES-07",
      "feito": "NADA APARECE DUAS VEZES, e é o defeito que a sprint existe para impedir: o piloto não usa `Gtk.Notebook` — a janela é `JanelaDaAba` (`hefesto_vivo.py:418`, de `gui/ponte_da_tela.py`), a tira/fita/rodapé são do HTML e a tira navega. O topo é pintado de dado, não digitado: `pacotes.topo(ctx)` (`hefesto_vivo.py:645-647`) preenche `conta`, `conta-b` e `perfil`, e os três casam no `casamento.py`. O rodapé ganhou os quatro botões com dono (`layout/_ferramentas/pacotes/rodape.py`, `PISO_DA_ABA = 4`, gestos `@gesto(\"*\")`).",
      "falta": "A SPRINT PROPUNHA O CONTRÁRIO e o 01/09 entregou a alternativa: ela pede que a tira, o 'Perfil ativo', a fita e o rodapé SAIAM do HTML e fiquem com o GTK, e diz com todas as letras que a escolha oposta 'custa as dez abas de uma vez e não se decide dentro de uma onda'. Não há registro de decisão dela. OS TRÊS AVISOS DO `header_bar` SUMIRAM: crachá 'Editando: Controle N', vibração travada e co-op derrubado — `grep` por eles em `hefesto_vivo.py` e `layout/_ferramentas/topo.html` devolve ZERO. É o que a `RUM-01` e a `CONTAGEM-E-COOP-01` pagaram para pôr lá. A PÁGINA NASCE COM ESCOLHA: `layout/02-controles.html:1428` — `<input class=\"radio-mesa\" type=\"radio\" name=\"mesa\" id=\"c-p1\" checked>`. O ALVO NÃO É IDA E VOLTA: `definir_alvo`/`EstadoDoAlvo` não aparecem em `layout/_ferramentas/pacotes/` nem em `hefesto_vivo.py` — abrir um cartão não escolhe alvo nenhum. `tests/unit/test_migra_controles_07_*.py` não existe."
    },
    {
      "id": "MIGRA-CONTROLES-08",
      "feito": "Três gestos com dono, cinco botões atendidos: `a02_controles.py:208` (`mudo`, o 🎙 por `mic.set` e o [nota] por `speaker.set`), `:269` (`rota`) e `:376` (`mic-modo`), com `PONTE = {'mic_set','speaker_set','machine_declare'}` (`:445`) — nada de socket à mão. CADA GESTO CHEGA NO CONTROLE CERTO: o clique carrega `data-controle` (`hefesto_vivo.py:219`), o piloto traduz para `uniq` (`:510-512`) e as quatro PROVAS (`:481-509`) exigem `uniq='aa:bb:cc:00:00:01'` em cada chamada. `HEFESTO_VARIANTE=dev .venv/bin/python -m pytest tests/unit/test_os_botoes_tem_dono.py tests/unit/test_o_casamento_das_dez.py -q` → 65 passed. E o 'Todo o som do PC' RECUSA dizendo por quê, em vez de escrever meio byte.",
      "falta": "OS DOIS TRILHOS CONTINUAM DESENHO: `grep -o '<input[^>]*type=\"[a-z]*\"' layout/02-controles.html` devolve só três `type=\"radio\"` — zero `type=\"range\"`. Sem controle não há 'valer ao SOLTAR', que é metade da sprint. `mic.volume.set` não é chamado por gesto nenhum desta aba. A RÉGUA QUE CLICA declarada (`scripts/telas/regua_que_clica.py`) não existe; o que existe é `hefesto_vivo.py --prova-clique` (`:930`), que clica UM gesto por nome — ele não exercita os estados do acordeão, não afirma que os cartões 3 e 4 estão fora da vista, e não fotografa cada estado. Quem mede os estados é `regua_estados.py` (28/08), e ela NÃO clica e NÃO reprova. 11 dos 23 `<button>` da página seguem sem `data-gesto`. `tests/unit/test_migra_controles_08_*.py` não existe."
    },
    {
      "id": "MIGRA-CONTROLES-11",
      "feito": "A ROTA ENTROU NO `state_full` em 01/09 — `daemon/ipc_handlers.py:3488-3502`, com o comentário datado e `None` tratado como 'o controle não disse', nunca como zero (`if isinstance(rota, int) and not isinstance(rota, bool): bloco['rota'] = rota`). Blame: `git log -L3480,3510` aponta `fe9da58a 2026-09-01`.",
      "falta": "A PROCEDÊNCIA NÃO É PUBLICADA: o bloco `speaker` leva `volume`, `muted` e `rota` e nada diz se a rota foi LIDA do controle ou é o último pedido do Hefesto — a sprint pede o par, como `lightbar_rgb`/`lightbar_source`. A TELA NÃO PINTA A ROTA EM VIGOR: o pacote não emite campo de rota e o `casamento.py` não lista nenhum; os dois botões nascem como o mockup os desenhou, sem o em vigor marcado — que é o defeito nº 1 da sprint. `_speaker_canal_pintando` continua nascendo `False` (`controller_card.py:2428`) e sendo só LIDO (`:4227`): a guarda nunca vale. O 'Liberar' do alto-falante continua criado e mantido em atributo (`:3959`) com sensibilidade recalculada a cada tique (`:5275`, `:5313`) sem estar na tela do cartão de um controle. `tests/unit/test_migra_controles_11_*.py` não existe."
    },
    {
      "id": "MIGRA-CONTROLES-12",
      "feito": "A cor lida do plástico chega à tela pela FITA, e pelo caminho certo: `mesa_viva.LeitorDeCor` (`:207-262`) pergunta UMA vez por peça, em thread (`hefesto_vivo.py:596-601`), e decide se pode perguntar consultando o MAPA, não um `if` decorado — `aciona('identidade.cor_do_aparelho', transporte) != 'sim'` (`mesa_viva.py:239-244`). O piloto tem a mordida: `--sem-cor` desliga o leitor (`hefesto_vivo.py:413`).",
      "falta": "A BORDA DO CARTÃO NÃO SAI DO DADO: `layout/02-controles.html:1427` continua com `style=\"--plastico:#ae335a\"` escrito pelo gerador, e `grep -rn plastico layout/_ferramentas/pacotes/a02_controles.py layout/_ferramentas/hefesto_vivo.py` devolve ZERO. O cartão do rádio fica com a cor do mockup em vez de neutro com o motivo — o oposto do que a sprint entrega. A LEITURA NÃO SOBREVIVE À JANELA: `LeitorDeCor._cache` é dicionário de INSTÂNCIA (`mesa_viva.py:226`) — o mesmo defeito de `secao_controles.py:646` que a sprint nomeia, transplantado para o instrumento novo. Nada é gravado em `ControleDeclarado.cor`. `tests/unit/test_migra_controles_12_*.py` não existe."
    },
    {
      "id": "MIGRA-CONTROLES-13",
      "feito": "Nasceu o leitor do mapa como MÓDULO reusável: `layout/_ferramentas/pacotes/mapa.py` (01/09, 01h07), que lê `docs/data/mapa-controles.csv` e oferece `aciona()` e `sem_dono()`, com a regra escrita no cabeçalho ('Todo valor que a tela AFIRMA tem de ter linha aqui, ou dizer que não tem dono'). Ele já é consumido pelo `LeitorDeCor` e pelos pacotes da Sistema e da Iluminação.",
      "falta": "O PORTÃO NÃO EXISTE: `scripts/check_a_tela_nao_promete_o_que_o_mapa_nega.py` não está em `scripts/` e não aparece em `scripts/portoes.sh` (rodei `bash scripts/portoes.sh --rapido`: 23 portões, todos verdes, nenhum com esse nome). A ABA 02 NÃO USA O MÓDULO: `a02_controles.py:80-81` devolve `\"sem_dono\": {}` e `cobertura.sem_dono: 0` — ela AFIRMA que tudo tem dono. As três afirmações que o mapa desmente continuam desenhadas iguais nos cartões de cabo e de rádio: o bloco do alto-falante, o medidor de 14 barras do microfone e os três eixos do acelerômetro. Nenhum valor carrega o transporte da peça — `via` é emitido e é ÓRFÃO. `tests/unit/test_migra_controles_13_*.py` não existe."
    }
  ],
  "abertas": [
    "MIGRA-CONTROLES-01 — o enxerto substitutivo, e o id que não pode sumir. `src/hefesto_dualsense4unix/gui/webview_de_aba.py` NÃO existe. O glade está intacto: `main.glade:286` ainda tem `scroll_tab_status_box` e `:295` `tab_status_box`, e `grep -c tab_no_jogo_box` devolve 2 — a aba 'No jogo' não saiu. Não há `set_name(..., ABA_STATUS)` em lugar nenhum (o único `set_name` em `src/` é `button_glyph.py:231`, num `get_accessible()`, e `app/app.py:1382` é uma prosa dizendo que `Gtk.Buildable.get_name` devolve `None` mesmo depois dele). A medição declarada como PRODUTO da sprint (`docs/process/medicoes/2026-08-29-o-enxerto-substitutivo-quanto-custa-trocar-uma-pagina.md`) não existe — o diretório `docs/process/medicoes/` não existe. Nenhum teste. O que o 01/09 fez foi uma JANELA SEPARADA (`layout/_ferramentas/hefesto_vivo.py`, e `scripts/abrir_interface.py:78` a elege), não a troca de uma página do `Gtk.Notebook` do produto — o preço de trocar uma página continua sem número, e é ele que a trava 2 do índice diz que o ok dela de 29/08 não cobre.",
    "MIGRA-CONTROLES-09 — a máscara por controle chega à tela. `_enrich_controllers_per_controller` (`daemon/ipc_handlers.py:3255`) acrescenta `player_slot`, `lightbar_rgb/on/source/disputada`, `nascimento`, `inputs`, `vpad_backend` e `vpad_motivo` — não acrescenta `mascara`. `grep -n 'external_mask\\|mascara_efetiva' src/hefesto_dualsense4unix/daemon/ipc_handlers.py` devolve só um comentário (`:5116-5117`); `mascara` só aparece como `gamepad_emulation.mascara_divergente/divergencias` (`:2762-2773`), que é outra coisa. O que a tela mostra hoje é a máscara GLOBAL: `layout/_ferramentas/mesa_viva.py:310-311` faz `sabor = state['gamepad_emulation']['flavor']` e escreve a MESMA máscara em todos os itens da mesa (`:332`) — exatamente o que a mordida da sprint manda reprovar ('Publique a máscara do jogo para todos e veja reprovar'). O `a02_controles.py` cai no `vpad_backend` quando a mesa não traz nada, que é o BACKEND, não a máscara por peça. Nenhum teste.",
    "MIGRA-CONTROLES-10 — o volume do microfone volta da escrita cega. `LeituraMic` (`app/mic_monitor.py:129-134`) continua com `nivel`, `muted`, `fonte`, `saida_muda` e `sink` — sem `volume`. `grep -rn '_pintar_volume_do_mic' src/ tests/ scripts/ layout/` devolve UMA linha, a própria definição (`app/widgets/controller_card.py:4087`): zero chamadores, idêntico ao medido em 29/08. Nenhum gesto de volume do microfone na aba nova (`PONTE` do `a02_controles.py:445` é `{mic_set, speaker_set, machine_declare}`) e a página não tem `<input type=\"range\">`. O ponteiro do mapa também não foi corrigido: `docs/data/mapa-controles.csv:29` (`audio.microfone.volume,dualsense`) continua sem apontar para `core/backend_pydualsense.py:4118-4121` nem `core/ds_output_report.py:122`. Nenhum teste."
  ],
  "achados": [
    "O ENUNCIADO DESTA AUDITORIA DIZ 18 SPRINTS; O ÍNDICE DIZ TREZE. `docs/process/sprints/2026-08-29-MIGRA-CONTROLES-INDICE.md` tem a seção 'As treze', com tabela de 13 linhas, e `ls docs/process/sprints/*MIGRA-CONTROLES*` devolve 13 arquivos + o índice. Não existe `MIGRA-CONTROLES-14..18`. Auditei 13.",
    "A ONDA MUDOU DE ENDEREÇO E O ÍNDICE NÃO SABE. Nenhum dos caminhos que as 13 sprints declaram em `cria:` existe: `gui/webview_de_aba.py`, `gui/telas/`, `scripts/telas/` (aba02.py, monta.py, topo.html, fim.html, regua_que_clica.py), `app/actions/controles_web.py`, `scripts/check_a_pagina_e_a_do_gerador.py`, `scripts/check_a_tela_nao_promete_o_que_o_mapa_nega.py` e nenhum dos 11 `tests/unit/test_migra_controles_*.py`. O que existe é `layout/` + `layout/_ferramentas/pacotes/`. Isso é grave FORA desta onda: `MIGRA-SISTEMA-01`, `MIGRA-CONEXOES-01`, `MIGRA-LANCADORES-01` e `-02` declaram `depois_de: MIGRA-MOLDURA-01` esperando `gui/telas/` e `scripts/telas/` — e o índice já avisava que 'inventar um id no meio do caminho cria referência falsa'. A reconciliação continua por fazer, e agora com os caminhos também divergindo.",
    "O RECIBO NÃO CHEGA À TELA, E A DOCSTRING AFIRMA QUE CHEGA. `hefesto_vivo.py:490-497` diz 'ele despacha o que tem DONO no daemon e RECUSA o resto **com o motivo na tela**'. O código só imprime no terminal: `:503-504` (`[gesto sem dono]`, stdout), `:520-528` (`[gesto falhou]`, stderr), `:539` (`[gesto] … → aplicado`, stdout). O único Python→página é `:701`, `window.__hef.pintar(...)`. Clicar 'Todo o som do PC' levanta e a tela não diz nada. Três sprints desta onda (03, 08, 11) pedem o recibo pelo nome, citando a `LIGHTBAR-BT-RESET-01` — 330 mil escritas ignoradas com a barra apagada. A repintura de 10 Hz mascara isso nos campos pintados; nos NÃO pintados (rota, mic-modo) não mascara nada.",
    "`touch-estado` É UM LITERAL. `layout/_ferramentas/pacotes/a02_controles.py:77` emite `\"touch-estado\": \"Sem toque\"` — constante, sem fonte. Ele CASA no `casamento.py` e entra na conta de 'pintados', então a régua o conta como valor vivo. É a tela afirmando sem medir, dentro do instrumento que existe para medir.",
    "O TRANSPORTE DO CARTÃO CONTINUA O DO MOCKUP. `via` é emitido pelo pacote e é ÓRFÃO (`casamento.py` → órfãos `['l2','r2','via']`): a página não tem onde pintá-lo. Com a mesa dela metade rádio, o cartão pode dizer USB sobre um controle no BT. É a mesma família de defeito que o piloto curou DUAS vezes hoje — a fita (`hefesto_vivo.py:641-648`) e os lugares vazios (`:665-682`), ambos com comentário contando a reincidência — e que sobrou nesta linha.",
    "OS DOIS 'VAZIOS' DO CASAMENTO SÃO RÓTULOS, NÃO VALORES. `layout/02-controles.html:1496` e `:1503` são `<span class=\"rotl\" data-campo=\"l3\">L3</span>` e o par R3 — os NOMES dos botões dos analógicos. O `escrever` do piloto faz `el.textContent = t` (`hefesto_vivo.py:100-116`), então quem emitir `l3`/`r3` TROCA o rótulo pelo número. É a mesma armadilha que já apagou dois botões nesta aba (o `mic-modo` que envolvia Virtual e Nativo, documentada em `a02_controles.py:59-76`) — e ela está armada de novo, esperando o pintor.",
    "A ABA 02 DECLARA COBERTURA TOTAL SOBRE COBERTURA PARCIAL. `a02_controles.py:80-81` devolve `\"sem_dono\": {}` e `cobertura: {pintados: …, sem_dono: 0}`, e o docstring do módulo (`:11-12`) diz 'Zero `sem_dono`'. Medido: 5 das 18 famílias da MIGRA-06 chegam à tela, três valores emitidos não têm destino, e as três afirmações que a MIGRA-13 nomeia (alto-falante no rádio, medidor de microfone no rádio, acelerômetro) continuam pintadas iguais no cabo e no rádio. O módulo que resolveria — `pacotes/mapa.py` — existe e esta aba não o importa.",
    "A DECISÃO DA MOLDURA FOI TOMADA SEM ELA. A MIGRA-07 põe a pergunta ('quem fica com a moldura: o GTK ou o HTML?') como decisão dela, propõe o GTK e diz que a alternativa 'custa as dez abas de uma vez e não se decide dentro de uma onda'. O 01/09 entregou a alternativa: janela sem `Gtk.Notebook`, tira/fita/rodapé no HTML. O preço já apareceu — os três avisos do `header_bar` (crachá 'Editando', vibração travada, co-op derrubado) não existem na janela nova.",
    "NÃO HÁ ONDE GRAVAR MEDIÇÃO. `docs/process/medicoes/` não existe como diretório. As duas sprints cujo PRODUTO é a medição (01 — o custo de trocar uma página; 03 — o custo de pintar por `run_javascript`) declaram arquivos lá dentro, e as duas dizem 'a medição é dupla ou não vale'. Nenhuma das duas medições foi feita, e a trava 2 do índice (o ok dela de 29/08 cobre 62 → ~285 MiB PSS no enxerto ADITIVO) segue sem o número do substitutivo.",
    "`install.sh:642` AINDA CHAMA O WEBKIT DE 'IMPORTANTE', com a razão escrita 'a interface GTK 3.0 de hoje continua inteira, e é só por isso que esta linha ainda não é obrigatória'. A frase continua verdadeira — porque o enxerto (MIGRA-01) não aconteceu. Mas o lançador `./interface` já abre a janela WebKit (`scripts/abrir_interface.py:78`): numa máquina sem `gir1.2-webkit2-4.1`, o botão que ela clica não abre nada, e o instalador não a avisou.",
    "A ÁRVORE ESTÁ SÃ: `git status --short` = 0 linhas; `bash scripts/portoes.sh --rapido` = TODOS VERDES, 23 portões (a camada rápida, não os 30); `pytest tests/unit/test_os_botoes_tem_dono.py tests/unit/test_o_casamento_das_dez.py -q` = 65 passed. Nada do que relatei acima é regressão — é trabalho que a onda pede e que ainda não existe."
  ]
}


---

## Agente 9

{
  "onda": "MIGRA-JOGAR",
  "total": 11,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-JOGAR-02",
      "feito": "O gerador EXISTE: /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix-dev/layout/_ferramentas/aba01.py (82.281 B) — criado em 30/08, commit 89646354 (`git log --diff-filter=A --follow -- layout/_ferramentas/aba01.py`), NÃO por esta leva. A `regerar.jogar()` morreu: regerar.py:29-32 roda `aba{n}.py` para as dez, sem remendo. O rodapé tem UM dono: monta.py:40 `FIM = (_F / \"fim.html\").read_text()`. A página viaja em worktree: `git ls-files layout | wc -l` = 87, e layout/01-jogar.html está entre eles.",
      "falta": "(a) `src/hefesto_dualsense4unix/gui/telas/` NÃO EXISTE (`ls` devolve 'Arquivo ou diretório inexistente') — a página não chegou ao endereço que a sprint fixa; (b) ela não entra no pacote: pyproject.toml:83-91 `[tool.hatch.build.targets.wheel].include` lista só `gui/*.glade`, `gui/assets/*.png` e os `.mo`; install.sh não copia `layout/`; (c) O GERADOR NÃO REPRODUZ O APROVADO — medido: `HEFESTO_BANCADA=<tmp> .venv/bin/python layout/_ferramentas/aba01.py` (com as dez páginas presentes no tmp) sai com 3 linhas diferentes de mockup/01-jogar.html (linhas 756, 769, 916: `# (noqa-acento)` no gerador contra `# noqa-acento` no disco). São comentários, zero pixel — mas é divergência gerador↔disco, que é o defeito que a sprint existe para matar; (d) tests/unit/test_migra_jogar_02_a_pagina_que_o_produto_carrega.py não existe; (e) não há régua comparando as duas cópias."
    },
    {
      "id": "MIGRA-JOGAR-03",
      "feito": "A página ganhou endereço — em OUTRO vocabulário, não o da tabela da sprint: 11 `data-campo` (atencao-conta, aviso-selo, aviso-texto, bateria×2, conta, conta-b, identidade×2, jogador×2, pendente, pendente-alvo, perfil), `data-controle=\"p1..p4\"` e 7 `data-gesto`. O canal do gesto é UM e é um ouvinte só: layout/_ferramentas/hefesto_vivo.py:195-240 (`document.addEventListener('click'…, true)` → `window.webkit.messageHandlers.hefesto.postMessage`). Casamento medido: `HEFESTO_VARIANTE=dev .venv/bin/python layout/_ferramentas/casamento.py 01-jogar.html` → html=11, pac=9, casam=9, órfãos=0, vazios=['pendente','pendente-alvo'].",
      "falta": "(a) NENHUM dos 18 ids do contrato existe. Medido um a um em layout/01-jogar.html E mockup/01-jogar.html: jg-conectado, jg-fita, jg-perfil-ativo, jg-modos, jg-conexao, jg-escada, jg-pecas, jg-modelo-cartao, jg-modelo-aviso, jg-atencao-conta, jg-atencao-lista, jg-pendente, jg-reconectar, jg-recibo, jg-aplicar, jg-salvar, jg-importar, jg-exportar → 0 ocorrências cada. Os únicos `id=\"jg-*\"` da página são os do SVG que o monta.py já prefixava (jg-p1-corpo, jg-p1-glifo-…); (b) `grep -c \"<template\" layout/01-jogar.html` = 0 — não há molde de cartão nem de aviso; (c) o SVG continua inlinado QUATRO vezes (`grep -o '<svg[^>]*data-colorway' | wc -l` = 4) e a página está em 306.143 bytes, ACIMA dos 258 KB que a sprint mede como defeito; (d) só os cartões p1 e p2 têm `data-campo` — jogador/identidade/bateria aparecem 2× cada; p3 e p4 (layout/01-jogar.html:2373, :2392+) têm `—` literal e nenhum endereço; (e) tests/unit/test_migra_jogar_03_os_enderecos_da_aba.py não existe."
    },
    {
      "id": "MIGRA-JOGAR-04",
      "feito": "A mesa real alimenta os cartões: layout/_ferramentas/pacotes/a01_jogar.py:34-48 monta um verbete por `ctx.conectados`, com jogador/bateria/identidade vindos do state_full e do `mesa_viva.mesa_do_estado`. Os lugares que a mesa não preenche são APAGADOS: hefesto_vivo.py:670-680 põe `—` em todos os campos e hefesto_vivo.py:162-169 marca `data-conectado=\"nao\"` + classe `off` + tira `alvo`. A contagem do cabeçalho tem dono único: pacotes/__init__.py:200-207 chama `mesa_viva.texto_da_contagem`. A fita é repintada da mesa viva: hefesto_vivo.py:657 `carga[\"fita\"] = _fita(ctx.mesa)`.",
      "falta": "(a) `src/hefesto_dualsense4unix/app/actions/jogar/mesa.py` NÃO EXISTE — `ls src/hefesto_dualsense4unix/app/actions/jogar/` devolve só `__init__.py` e `painel.py`; (b) o cartão não NASCE, é assado: hefesto_vivo.py:68 `TODOS_OS_LUGARES = {\"p1\",\"p2\",\"p3\",\"p4\"}`, teto de quatro; e como p3/p4 não têm `data-campo` (ver MIGRA-JOGAR-03), um TERCEIRO controle recebe `pref=\"p3\"` (hefesto_vivo.py:604) e cai num cartão que nunca pinta. A aba mostra no máximo DOIS controles — a sprint pede 0, 1, 2 ou N; (c) ZERO controles não diz nada: os quatro cartões viram `—` e a decisão dela (`o que a tela diz`) segue sem resposta; (d) A COR DO PLÁSTICO NÃO É PINTADA e por isso MENTE: `a01_jogar.pacote()` não emite colorway nem `--plastico`, e o HTML fica com os quatro `data-colorway` do mockup (cosmic-red, starlight-blue, galactic-purple, white — 10 ocorrências cada). `mesa_viva.CORES` (linha 199) existe e ninguém o usa nesta aba; (e) clicar num cartão não move a fita: `grep -rn alvo_de_edicao layout/` = 0 ocorrências; (f) tests/unit/test_migra_jogar_04_a_mesa_real.py não existe."
    },
    {
      "id": "MIGRA-JOGAR-05",
      "feito": "A coluna Atenção conta e mostra o pior achado: a01_jogar.py:52-66 emite `atencao-conta` (\"N aviso(s)\"), `aviso-selo` e `aviso-texto`, e os três CASAM (medido pelo casamento.py). O `_do_exame()` (a01_jogar.py:70-79) reusa `a08_conexoes._exame()`, com o motivo escrito: uma contagem só, não duas.",
      "falta": "(a) A FONTE É OUTRA. A sprint nomeia DOZE produtores de `app/actions/home_actions.py` (texto_da_pausa:216, autoswitch_lock_text:259, texto_do_cadeado_cego:310, texto_coop_degradado:476, texto_native_bt_fragil:539, wrapper_banner_text:565, texto_do_radio_fragil:589, vpad_degradation_text:613, texto_do_desktop_sem_emulacao:762, texto_da_divergencia:1016, aviso_de_grab:1394, state_full.controles_sem_driver). A Jogar colhe do exame da aba CONEXÕES. Nenhum dos doze chega a esta tela; (b) `src/hefesto_dualsense4unix/app/actions/jogar/avisos.py` não existe; (c) `painel.avisos_do_estado` (app/actions/jogar/painel.py:652) EXISTE e tem zero chamadores no piloto que o lançador abre — só em layout/_ferramentas/jogar_vivo.py:753 e :827; (d) só UM aviso é pintado (o mais grave), não a lista: não há `#jg-atencao-lista` nem `#jg-modelo-aviso`; (e) tests/unit/test_migra_jogar_05_a_coluna_atencao.py não existe."
    },
    {
      "id": "MIGRA-JOGAR-06",
      "feito": "A premissa da sprint foi SUPERADA pela decisão dela de 31/08 — a fileira de quatro modos virou escada de dois níveis, e \"Desligado\" É o Modo Nativo. O gesto existe e CHEGA AO DAEMON. Medido chamando o gesto com uma ponte falsa: `hefesto {'modo':'gamepad'}` → [('native.mode.set',{enabled:False,origin:'manual'}), ('gamepad.emulation.set',{enabled:True,origin:'manual'})]; `hefesto {'modo':'native'}` → [('native.mode.set',{enabled:True,origin:'manual'})]. A sequência é DELEGADA, sem regra reescrita: a01_jogar.py:149 `painel.plano_do_modo(chave, mascara)` → `mode_transition.plan_mode_transition`. A pergunta \"de quem é o gesto\" está respondida: Hefesto=modo (Jogar), serviço=daemon (Sistema).",
      "falta": "(a) A FILEIRA NÃO LÊ O MODO VIVO — a tela mente. layout/01-jogar.html:1121 traz `<input type=\"radio\" name=\"hefesto\" id=\"hef-ligado\" class=\"hef-rd\" checked>` assado; `grep -n \"mode_of_state\\|hefesto_ligado\" layout/_ferramentas/hefesto_vivo.py` = 0 ocorrências, e o BOOTSTRAP dele (hefesto_vivo.py:99-130) só sabe escrever texto/largura/fundo/valor — não mexe em `checked` nem em classe. Com o daemon em Modo Nativo a Jogar continua mostrando \"Ligado\"; (b) `painel.hefesto_ligado` (painel.py:202) e `painel.modo_vivo` (:179) existem e o piloto do lançador não os chama; (c) `src/hefesto_dualsense4unix/app/actions/jogar/modo.py` não existe; (d) `mode_transition.MODES` continua com três valores; (e) tests/unit/test_migra_jogar_06_o_quarto_botao.py não existe."
    },
    {
      "id": "MIGRA-JOGAR-07",
      "feito": "A tradução chip→ponte tem dono único no produto: app/actions/jogar/painel.py:464-502 `CHIPS_DA_ESCADA`, derivada de `integrations/ponte_escada`. Três dos quatro chips da tela chegam ao daemon — medido: `modo-dualsense` → gamepad.emulation.set{flavor:'dualsense'}, `modo-xbox` → {flavor:'xbox'}, `modo-navegacao` → native.mode.set OFF + gamepad.emulation.set OFF + mouse.emulation.restore (os três na ordem do plano). O `modo-steam` fica SEM dono e o piloto o recusa dizendo o nome (a01_jogar.py:107-121 `BOTOES_SEM_DONO`). `painel.degraus_sem_chip()` (:563) e `chips_sem_dono()` (:546) existem e derivam do produto.",
      "falta": "(a) `#jg-escada` não é pintada da `ESCADA`: layout/01-jogar.html:1163 traz `class=\"degrau on\"` assado no \"Sony DualSense\", e o piloto do lançador não escreve classe nenhuma. Com a máscara em Xbox a tela continua acesa em DualSense; (b) O CARIMBO NÃO CHEGOU À JOGAR: `grep -rn pontes_confirmadas src/hefesto_dualsense4unix/app/` só devolve app/actions/profiles_actions.py (a aba Perfis) e comentários em draft_config.py; (c) `POR_ESCOLHA_DELA` continua com ZERO ESCRITORES — `grep -rn POR_ESCOLHA_DELA src/` devolve só a definição (integrations/ponte_escada.py:174), o frozenset (:175), o `__all__` (:529) e comentários; (d) `src/hefesto_dualsense4unix/app/actions/jogar/escada.py` não existe; (e) o desenho publicado tem QUATRO degraus (`data-degrau=` dualsense, xbox, steam, navegacao) e `CHIPS_DA_ESCADA` tem CINCO — o \"Point And Click\" que ela mandou MANTER em 31/08 não está na fileira, embora a legenda (01-jogar.html:2936-2943) e o CSS `.degrau.sem-dono` (:724) o prometam; (f) tests/unit/test_migra_jogar_07_a_escada_na_tela.py não existe."
    },
    {
      "id": "MIGRA-JOGAR-08",
      "feito": "Os CINCO gestos têm dono. `reconectar` em a01_jogar.py:334 — medido: [('coop.sync',{}), ('identity.renumber',{})], NESSA ordem, que é a que home_actions.py:3122 exige. Os quatro do rodapé em layout/_ferramentas/pacotes/rodape.py:94 (aplicar → profile.apply_draft), :114 (salvar), :141 (exportar), :179 (importar), registrados como `(\"*\", nome)` e alcançados pela classe `r-<nome>` (hefesto_vivo.py:207). `.venv/bin/python -m pytest tests/unit/test_o_rodape_das_dez.py -q` passa.",
      "falta": "(a) A LINHA LARANJA NÃO É PINTADA POR NINGUÉM. `casamento.py 01-jogar.html` devolve `vazios: ['pendente','pendente-alvo']` — os dois endereços existem na página (layout/01-jogar.html:2805) e nenhum pacote os emite. `relancar.texto_do_pendente` não chega a esta tela; (b) O RECIBO NÃO NOMEIA O PERFIL — o texto saiu do rodapé e virou `title=`, com o nome do MOCKUP assado: layout/01-jogar.html:2847 \"Grava no perfil Mortal Kombat\" e :2849 \"Escreve o perfil Mortal Kombat\". A pintura escreve textContent/style/value e NUNCA `title`, então a dica diz \"Mortal Kombat\" qualquer que seja o perfil dela; (c) `footer_actions._perfil_que_as_abas_editam` (footer_actions.py:884) tem ZERO chamadores na interface nova — `rodape.salvar` (rodape.py:128) lê `ctx.state[\"active_profile\"]`, que é exatamente a segunda variável que a sprint nomeia como origem da queixa \"salva com nome de outro perfil\"; (d) `src/hefesto_dualsense4unix/app/actions/jogar/faixa.py` não existe; (e) tests/unit/test_migra_jogar_08_a_faixa_e_o_rodape.py não existe."
    },
    {
      "id": "MIGRA-JOGAR-09",
      "feito": "O DEFEITO CENTRAL FECHOU, mas noutra aba: `daemon.resume` ganhou um segundo chamador e ele é uma TELA — layout/_ferramentas/pacotes/a09_sistema.py:121-132 (`@gesto(\"09-sistema.html\",\"retomar\")` → `p.chamar(\"daemon.resume\")`), ligado ao botão de layout/09-sistema.html:1051 `data-gesto=\"retomar\"`. Antes disso o único chamador era a CLI (cli/app.py:421). A pausa deixa de exigir terminal.",
      "falta": "(a) NÃO ESTÁ NA JOGAR: layout/01-jogar.html não tem botão de despausar nem `#jg-despausar` (0 ocorrências), e o pedido da sprint é que ele nasça NA LINHA do aviso, ao lado da explicação; (b) O BOTÃO NÃO É CONDICIONAL — o `title` de 09-sistema.html:1051 promete \"Só acende com a pausa ativa\" e nada pinta esse acender: o piloto do lançador não escreve classe nem `disabled`. Ele está sempre lá, oferecendo conserto para estado são; (c) `texto_da_pausa` (home_actions.py:216) não é um dos avisos que a Jogar mostra — os dela vêm do exame da Conexões; (d) `src/hefesto_dualsense4unix/app/actions/jogar/pausa.py` não existe; (e) tests/unit/test_migra_jogar_09_a_pausa_se_desfaz_na_janela.py não existe."
    },
    {
      "id": "MIGRA-JOGAR-10",
      "feito": "Os três primeiros degraus estavam pagos ANTES desta leva (a própria sprint os declara superados em 29/08): `make_virtual_pad` tem `identity` (integrations/virtual_pad.py:153) e resolve `key = mascara_efetiva(identity, flavor)` em :213, ANTES de escolher o backend — que é a armadilha do primeiro degrau. Os dois chamadores passam o MAC: daemon/subsystems/coop.py:984 e daemon/subsystems/gamepad.py:2103. Régua: tests/unit/test_mascara_por_controle_manda_no_vpad.py.",
      "falta": "(a) ITEM 4 ABERTO — a rota IPC de escrita não aprendeu a identidade. `_handle_gamepad_emulation_set` (daemon/ipc_handlers.py) aceita só `enabled` e `flavor`; `grep -n \"mask\\|mascara\" src/hefesto_dualsense4unix/daemon/ipc_server.py` não devolve NADA. A escolha dela continua sendo uma para a mesa; (b) ITEM 5 ABERTO — o chip não mostra máscara viva. Os 12 chips são estáticos (`class=\"chip on\"` assado em 01-jogar.html:1595 e :1990), e o gesto `mascara` está deliberadamente em `a01_jogar.BOTOES_SEM_DONO` (:114-121) citando ipc_handlers.py:5060; (c) os chips de P3 e P4 (01-jogar.html:2383-2385, :2777-2779) têm `data-mascara` SEM `data-gesto`, e o seletor do ouvinte (hefesto_vivo.py:204-207) não inclui `data-mascara` — nesses dois cartões o clique some CALADO, em vez de recusar dizendo o nome; (d) app/app.py:1237 continua `\"tab_home_box\": _MOTIVO_ALVO_AINDA_NAO_LIGADO` — a fita desta aba segue inerte, e é a 10 que a acenderia; (e) tests/unit/test_migra_jogar_10_a_mascara_e_de_cada_controle.py não existe."
    }
  ],
  "abertas": [
    "MIGRA-JOGAR-01 — a página entra no lugar da `tab_home_box`. Nada do enxerto substitutivo existe: `grep -n \"WebKit\\|webview\" src/hefesto_dualsense4unix/gui/main.glade` devolve zero (só o `GtkScrolledWindow id=scroll_tab_home_box` da linha 234 e o `GtkBox id=tab_home_box` da 243, intactos); `grep -n \"WebKit\\|webview_de_aba\" src/hefesto_dualsense4unix/app/app.py` devolve zero; `find src -name '*webview*'` não acha módulo de enxerto; app/app.py:1237 segue `_MOTIVO_ALVO_AINDA_NAO_LIGADO`; tests/unit/test_migra_jogar_01_a_pagina_no_lugar_da_home.py não existe. O que a leva de 01/09 entregou é OUTRA COISA: `hefesto_vivo.py` é uma janela GTK3 PRÓPRIA, ao lado do produto, e o `./interface` a abre em vez de abrir o app (scripts/abrir_interface.py:77-81, `CANDIDATOS_DO_PILOTO`). Nenhuma das cinco entregas da sprint foi medida — nem o id que sobrevive, nem o poller que morde três tiques, nem o `load-failed`, nem a fita em UM lugar, nem o PSS antes/depois.",
    "MIGRA-JOGAR-11 — a terceira máscara, Nintendo Pro. O catálogo continua com DUAS entradas: integrations/uinput_gamepad.py:115-126 `FLAVORS = {\"dualsense\": …, \"xbox\": …}`. `app/actions/home_actions.py:164-167` `_FLAVOR_ITEMS` também tem duas. Nenhum descritor uinput novo, nenhum PID (a sprint exige que NÃO seja o 0x2009 do Pro físico). E o chip continua na tela: `grep -c 'data-mascara=\"Nintendo Pro\"' layout/01-jogar.html` = 4 — é o chip que não clica em nada que o índice previu. tests/unit/test_migra_jogar_11_a_mascara_nintendo_pro.py não existe."
  ],
  "achados": [
    "O ÍNDICE TEM ONZE SPRINTS, NÃO TREZE. O enunciado desta auditoria dizia 13. Medido: `grep -h '^sprint:' docs/process/sprints/*MIGRA-JOGAR*.md | sort -u` devolve MIGRA-JOGAR-01..11 mais o INDICE; a tabela 'As onze' do índice tem 11 linhas; `ls docs/process/sprints/ | grep MIGRA-JOGAR` devolve 11 arquivos + o índice. O próprio índice abre com 'ONZE sprints'.",
    "REGRESSÃO: o piloto único PERDEU o que o piloto de uma aba já pintava na Jogar. `layout/_ferramentas/jogar_vivo.py` (vivo e mantido — último commit a97aeeea, 01/09 10:33) pinta o interruptor pelo daemon (`painel.hefesto_ligado`, :869 e a função `lado()` do BOOTSTRAP, :271-278), o degrau vivo (`painel.degrau_vivo`, :873), a LISTA de avisos (`painel.avisos_do_estado`, :753 e :827, com remontagem), a cor do plástico (`varia(c,'--plastico',…)`, :318), a máscara acesa por cartão, o realce do alvo, a linha `pendente` e o `.degrau.sem-dono` para o que não tem dono. `layout/_ferramentas/hefesto_vivo.py` — que é o que `./interface` abre — não pinta NENHUM desses: o BOOTSTRAP dele (:99-170) só escreve texto/largura/fundo/valor e apaga lugares vazios. Nenhuma régua acusa a perda: `test_o_casamento_das_dez.py` só cobra endereços com `data-campo`, e o piso da Jogar (9) foi escrito DEPOIS da troca.",
    "A TELA DA JOGAR MENTE SOBRE O MODO E SOBRE A MÁSCARA. `layout/01-jogar.html:1121` traz `id=\"hef-ligado\" ... checked` assado e `:1163` traz `class=\"degrau on\"` assado no 'Sony DualSense'. Com o daemon em Modo Nativo, ou com a máscara em Xbox, a aba continua mostrando 'Ligado' e 'Sony DualSense'. É o F7 desta casa, e o comentário de jogar_vivo.py:246-259 o descreve palavra por palavra — como defeito que aquele piloto já tinha curado.",
    "O DOCSTRING DO PACOTE AFIRMA O CONTRÁRIO DO CÓDIGO. `layout/_ferramentas/pacotes/a01_jogar.py:14-16` diz: *'O piloto já usa isso para acender o interruptor, e é por isso que o botão da Jogar funciona hoje.'* Medido: `grep -n 'mode_of_state\\|hefesto_ligado' layout/_ferramentas/hefesto_vivo.py layout/_ferramentas/pacotes/a01_jogar.py` = ZERO ocorrências em ambos. O botão funciona porque o GESTO chama o daemon; a tela não acende nada.",
    "SEIS LEITORES PRONTOS EM `app/actions/jogar/painel.py` (criado em 30/08, commit 89646354) NÃO TÊM CHAMADOR NA INTERFACE QUE ELA ABRE: `modo_vivo`(:179), `hefesto_ligado`(:202), `modo_lembrado`(:349), `degrau_vivo`(:581), `avisos_do_estado`(:652), `texto_da_conta`(:678), `nome_do_perfil`(:693). Os únicos chamadores são `jogar_vivo.py` e `controles_vivos.py` — os dois pilotos que o lançador aposentou em 01/09. É a forma canônica do defeito mais caro desta casa: a cura escrita e nunca ligada, com o agravante de que ela JÁ ESTEVE ligada.",
    "A ABA JOGAR SÓ CONSEGUE MOSTRAR DOIS CONTROLES. `grep -o 'data-campo=\"[^\"]*\"' layout/01-jogar.html` devolve jogador, identidade e bateria DUAS vezes cada — os cartões p3 e p4 (:2373 e :2762) trazem `—` literal e nenhum endereço. Um terceiro controle recebe `pref='p3'` (hefesto_vivo.py:604) e cai num cartão estruturalmente mudo. A MIGRA-JOGAR-04 pede 0, 1, 2 ou N, e a palavra dela é *'se eu comprar outros dualsense eles aparecem também'*.",
    "OS CHIPS DE MÁSCARA DE P3/P4 CLICAM NO VAZIO, EM SILÊNCIO. `layout/01-jogar.html:2383-2385` e `:2777-2779` têm `data-mascara` SEM `data-gesto`, e o seletor do ouvinte (hefesto_vivo.py:204-207) não inclui `data-mascara`. Nos cartões p1/p2 o mesmo chip ao menos RECUSA dizendo o nome (`BOTOES_SEM_DONO['mascara']`); nos outros dois o clique some. É exatamente o que o `pacotes/COMO-LIGAR-UMA-ABA.md` proíbe: *'Um botão que responde calado é pior que um que recusa'*.",
    "AS DICAS DO RODAPÉ NOMEIAM UM PERFIL QUE NÃO É O DELA. `layout/01-jogar.html:2847` — 'Grava no perfil Mortal Kombat' — e `:2849` — 'Escreve o perfil Mortal Kombat' — vivem em `title=`, e a pintura do piloto escreve textContent/style/value, NUNCA `title`. O nome do mockup fica na tela dela para qualquer perfil ativo. Isso vale para todas as dez abas: o rodapé mora no `topo.html`.",
    "O 'SALVAR' DA INTERFACE NOVA USA A SEGUNDA VARIÁVEL QUE A SPRINT NOMEIA COMO ORIGEM DA QUEIXA DELA. `pacotes/rodape.py:128` lê `ctx.state['active_profile']`; `footer_actions._perfil_que_as_abas_editam` (footer_actions.py:884) — que existe justamente porque NÃO é o ativo — tem zero chamadores fora da janela estável. A MIGRA-JOGAR-08 declara que a segunda variável é a origem literal de *'clico em salvar e ele salva com nome de outro perfil'*. E o gesto ESCREVE NO DISCO DELA (`save_profile`, rodape.py:136).",
    "O GERADOR DA JOGAR JÁ DIVERGIU DO DESENHO NO DISCO, E PORTÃO NENHUM VÊ. Medido: `HEFESTO_BANCADA=<tmp com as dez páginas> .venv/bin/python layout/_ferramentas/aba01.py` sai diferente de mockup/01-jogar.html em 3 linhas (756, 769, 916: `# (noqa-acento)` contra `# noqa-acento`) — o gerador foi editado e nunca rodado. É o mesmo buraco que `layout/_ferramentas/onde.py:60-73` descreve como medido na aba06, e o desvio `HEFESTO_BANCADA` que ele criou para fechá-lo NÃO TEM PORTÃO USANDO: `grep -rn HEFESTO_BANCADA scripts/ tests/` só devolve `HEFESTO_BANCADA_ARQ/AGORA/PID`, do `bancada.sh`, que é outra coisa. mockup/01-jogar.html e layout/01-jogar.html são idênticos (md5 597deb5263a8b92c16b0a8b59a5aa9c4).",
    "A LEGENDA PROMETE UM CHIP QUE A FILEIRA NÃO TEM. `painel.CHIPS_DA_ESCADA` (painel.py:464-502) tem CINCO chips, incluindo `Chip('pointclick','Point And Click', None)`; a página publicada tem QUATRO (`data-degrau=` dualsense, xbox, steam, navegacao). A legenda (:2936-2943) e o CSS `.degrau.sem-dono` (:724) descrevem em detalhe o Point And Click marcado na tela — e a ordem dela de 31/08 era MANTÊ-LO. `chips_sem_dono()` devolve um chip que a tela não desenha.",
    "DUAS SPRINTS DESTA ONDA TÊM A PREMISSA SUPERADA PELA DECISÃO DELA DE 31/08, e o índice não registra. A MIGRA-JOGAR-06 fala do 'quarto botão Desligado na fileira de modos' — a fileira de quatro modos não existe mais: virou interruptor Hefesto (Ligado/Desligado, onde Desligado É o Modo Nativo) mais uma fileira de chips. A MIGRA-JOGAR-07 conta CINCO degraus na tela incluindo 'Automático' e 'Teclado + Mouse' — `grep -c 'Teclado + Mouse' layout/01-jogar.html` = 0, e 'Automático' só aparece em comentários dizendo que ELA O TIROU (:2920: *'na aba jogar o Botão Automático não existe'*). Quem executar essas duas pelo texto vai construir uma tela que ela já derrubou.",
    "A REGRA DAS DEZ ('clicar já aplica', COMO-LIGAR-UMA-ABA.md) COLIDE COM A LINHA LARANJA QUE A MIGRA-JOGAR-08 PEDE. A faixa `pendente` existe na página (`data-campo=\"pendente\"`, `pendente-alvo`, :2805) para provar que *'o Aplicar ainda deve'* — e a interface nova aplica no clique. Os dois endereços saem como `vazios` no casamento, e ninguém decidiu se a linha morre ou se o modo/máscara continuam sendo o 'depois' que só o Aplicar leva (rodape.py:100-103 declara essa lacuna por escrito).",
    "MEDIÇÃO DE CONTEXTO, para não superestimar a leva: os 48 gestos com dono conferem — `pacotes.GESTOS` devolve 48, dos quais 5 na Jogar (hefesto, modo-dualsense, modo-xbox, modo-navegacao, reconectar) mais os 4 do rodapé compartilhado. Os cinco da Jogar chegam ao daemon, provado chamando cada um com uma ponte falsa. As quatro réguas novas passam: `pytest tests/unit/test_os_botoes_tem_dono.py tests/unit/test_o_casamento_das_dez.py tests/unit/test_o_rodape_das_dez.py tests/unit/test_o_perfil_chega_na_tela.py -q` → 75 passed. Nenhuma delas mede TELA: todas medem pacote e chamada. Nenhum arquivo `test_migra_jogar_*` existe — as onze réguas que as onze sprints pedem nominalmente ainda não nasceram."
  ]
}


---

## Agente 10

{
  "onda": "MIGRA-SISTEMA",
  "total": 11,
  "fechadas": [],
  "parciais": [
    {
      "id": "MIGRA-SISTEMA-INDICE",
      "feito": "O índice existe e lista as dez; foi corrigido hoje (88b08e0a) trocando `novo-layout/` por `layout/` em duas citações. `scripts/portoes.sh --rapido` → colisao-de-sprints ok, 561 ms.",
      "falta": "A base factual dele caducou e ninguém a atualizou. `grep -c novo-layout` = 3 no índice e ≥1 em CADA um dos dez arquivos — pasta que não existe mais (`ls novo-layout` → 'Arquivo ou diretório inexistente'). Os dois ids que as dez declaram em `depois_de` — MIGRA-CONTROLES-PILOTO e MIGRA-MOLDURA-01 — não existem como sprint (`grep -rl '^sprint: MIGRA-CONTROLES-PILOTO' docs/process/sprints/` → vazio). Nenhuma das dez foi marcada fechada ou anotada com o que a leva de 01/09 fez."
    },
    {
      "id": "MIGRA-SISTEMA-01",
      "feito": "O motor existe DENTRO do produto: `src/hefesto_dualsense4unix/gui/ponte_da_tela.py:84-89` traz os quatro pinos na ordem certa (Gtk 3.0, Gdk 3.0, GdkPixbuf 2.0, WebKit2 4.1), `:185` o `register_script_message_handler` de UM argumento, `:95-97` a armadilha do FINISHED-depois-do-load-failed escrita em código, `:336` a guarda de carga. E a página 09 roda num WebView — no piloto `layout/_ferramentas/hefesto_vivo.py`, que o `./interface` abre (`scripts/abrir_interface.py:77-81`).",
      "falta": "A SUBSTITUIÇÃO, que é a sprint inteira, não aconteceu. `main.glade:2709` (`scroll_daemon_box`) e `:2718` (`GtkBox id=\"daemon_box\"`) continuam lá — 27 `id=` no bloco 2709-3100. `daemon_actions.py` continua com os 9 `self._get` (linhas 1167, 1201, 1917, 1957, 1963, 2002, 2011, 2621, 2726) e 13 `def on_` (`grep -c 'def on_'` = 13); o arquivo tem 2807 linhas, contra as 2794 do enunciado — nenhuma linha de Python morreu nesta aba. O que existe é uma SEGUNDA janela, não a troca da página. E o item 5 (O NÚMERO) não existe em lugar nenhum: nenhuma medição de PSS antes/depois, de linhas mortas × nascidas, nem de tempo até a página em tela. `tests/unit/test_migra_sistema_01_a_pagina_e_um_webview.py` não existe."
    },
    {
      "id": "MIGRA-SISTEMA-02",
      "feito": "Os endereços saem do GERADOR e são cobrados contra o contrato do produto: `layout/_ferramentas/aba09.py:172-174` lê `ENDERECOS`/`GESTOS` de `src/.../gui/aba_sistema.py` por AST, e `_id()` (`:178-182`) e `_gesto()` (`:186-190`) levantam `SystemExit` sobre nome desconhecido. O HTML emite `data-` e não `id` (`aba09.py:575`, `:651`). Medido no publicado: 11 `data-id`, 12 `data-gesto`, nenhum duplicado, e os 12 nomes de gesto são EXATAMENTE os que a sprint lista. A lista de achados é UM endereço, não oito (`data-id=\"exame-lista\"`, `aba09.py:878`). O miolo continua cabendo: `aba09.py:43` grava `MIOLO_H, ALTURA = 544, 542`.",
      "falta": "Um dos doze endereços do contrato não tem lugar na página: `set(aba_sistema.ENDERECOS) - {data-id do HTML}` = `['bateria-frase']` (medido). E o gerador só reprova o sentido inverso — endereço na página que não está no contrato; a única régua que compara os dois é `tests/unit/test_a_aba_sistema_nomeia_o_servico.py:146`, e ela só exige INTERSEÇÃO NÃO VAZIA, então uma chave do contrato sem lugar passa calada. A régua que a sprint manda criar (`cria: novo-layout/_ferramentas/regua_enderecos09.py`) não existe, e nem a pasta existe."
    },
    {
      "id": "MIGRA-SISTEMA-03",
      "feito": "A camada de pintura existe no PRODUTO e tem um escritor só: `src/hefesto_dualsense4unix/gui/aba_sistema.py` (669 linhas), com `pacote()` em `:614` juntando tudo numa chamada; `layout/_ferramentas/pacotes/a09_sistema.py:90` delega para ela. A pausa CHEGA À TELA pela primeira vez e diz o que ela é: `aba_sistema.linha_da_pausa:286` → medido, `state_full{'paused':True}` devolve 'Sim, e volta pausado'. Zero controles é estado legítimo (`linha_do_vale_para:372`: 'Nenhum controle na mesa'), e nenhum número é digitado (`forca_do_perfil:377` lê `TETO_POR_PERFIL` + `core.rumble.teto_do_orcamento`). `casamento.py 09-sistema.html` → 9 casam, 8 órfãos, 0 vazios. Dos dez valores, CINCO pintam com dado real hoje (medido com estado realista): hefesto-estado='Ligado', hefesto-pausa='Não', hefesto-troca-de-perfil='Ligado', bateria-vale-para='Os 2 controles', e o rótulo de estado.",
      "falta": "A mordida central da sprint — 'com o daemon desligado, NENHUMA linha fica com o literal do mockup' — reprova hoje em CINCO endereços: `exame-contagem`, `exame-lista`, `registro-texto`, `hefesto-autostart` e `bateria-perfil` não têm `data-campo` e por isso ninguém os pinta (medido: `set(data-id) - set(data-campo)`). A página publicada continua exibindo '8 linhas · nenhum aviso', os 8 achados estáticos, o [OK] do autostart e o log falso. Dois dos que pintam mostram '—' PARA SEMPRE porque `a09_sistema._leitura:69` preenche só 3 dos 7 campos de `Leitura` (`achados`, `deteccao`, `ambiente` e `perfil` ficam `None`): medido com `rumble_policy='economia'` no state, `bateria-impoe` = '—' e `hefesto-ambiente` = '—'. Os três achados do exame não chegam. `tests/unit/test_migra_sistema_03_a_ponte_de_leitura.py` não existe."
    },
    {
      "id": "MIGRA-SISTEMA-04",
      "feito": "Há UM despachante e ele é tabela: `pacotes.GESTOS` + `hefesto_vivo.py:489 _gesto()`, com o gesto rodando em thread (`:520-535`, medido: `daemon.reload` = 9,5 s). TRÊS dos doze têm dono, provado por régua que exige a CHAMADA à ponte: `tests/unit/test_os_botoes_tem_dono.py` (70 passed em 1,26 s) sobre as `PROVAS` de `a09_sistema.py:266-285`. `retomar` → `p.chamar('daemon.resume')` (`a09_sistema.py:132`) — é o SEGUNDO chamador do `daemon.resume` na casa (o outro é `cli/app.py:421`) e é uma tela, que é o ponto da sprint. `atualizar` → `daemon.reload` (`:143`). `perfil-da-mesa` → `machine_declare` (`:246`).",
      "falta": "NOVE dos doze não têm dono (medido): autostart, desligar, procurar-camadas, refazer-consertos, refazer-proton, reiniciar, restaurar-de-fabrica, ver-detalhes, ver-plugins. O RECIBO NÃO CHEGA À TELA — item 3 da sprint: `hefesto_vivo.py:503-504` faz `print('[gesto sem dono] …')` no stdout, `:530` imprime a falha em stderr e `:537` o sucesso no stdout; nada é escrito na página. O que está cinza NÃO fica cinza e não diz por quê — item 4: `aba_sistema.travas()` (`:576`) calcula `{gesto: motivo}` e o achatamento de `a09_sistema.pacote` (`:105-107`) o DESCARTA, porque só copia `valores` e quatro chaves escalares. `on_camadas_engasgo` continua em `emulation_actions.py`. Não há régua que compare a tabela e a página nos dois sentidos — `PISO_DA_ABA = 3` (`a09_sistema.py:264`) só exige um piso. `tests/unit/test_migra_sistema_04_a_ponte_de_gestos.py` não existe."
    },
    {
      "id": "MIGRA-SISTEMA-05",
      "feito": "A contagem DERIVADA existe e está certa, no produto: `gui/aba_sistema.py:534-568 exame()` conta `len(linhas)` e os WARN, e emite 'nenhum aviso'/'1 aviso'/'N avisos' — nenhum número digitado; cada achado sai em campos separados (`selo`, `cls`, `g`, `txt`) via `_selo()` (`:526`), que normaliza os '[ OK ]'/'[WARN]' com colchetes do produto. E há um piloto que REALMENTE pinta isso na página: `layout/_ferramentas/sistema_viva.py:253-260` escreve `exame-contagem` e distribui `exame-lista` nas duas colunas por `meio`.",
      "falta": "Nada disso chega à tela que ela abre. `a09_sistema.pacote` (`:105-107`) descarta a chave `exame` (é `dict`), e `exame-contagem`/`exame-lista` não têm `data-campo`, que é o único vocabulário que `hefesto_vivo.py:131-134` pinta. Medido: o pacote de 09 emitido não contém `exame`. `sistema_viva.py` ficou ÓRFÃO — `scripts/abrir_interface.py:77-81` só lista `hefesto_vivo.py` e `controles_vivos.py`. As duas condicionais não foram convertidas, a `LinhaDeSaude` de quatro campos não existe em lugar nenhum (`grep -rn LinhaDeSaude src tests layout` → zero), a ordem dos achados não tem dono declarado, e não há foto com 6, 8 e 9 achados. `tests/unit/test_migra_sistema_05_o_exame_na_pagina.py` não existe."
    },
    {
      "id": "MIGRA-SISTEMA-08",
      "feito": "A faixa mudou de aba: o Perfil de Bateria está na 09 (`09-sistema.html:1036` e `:1079`), e o gesto aceita o valor da página — `a09_sistema.perfil_da_mesa:225` lê `o['v']`, traduz por `_teto_do_perfil` (`:152`), que importa `TETO_POR_PERFIL` de `secao_orcamento.py` em vez de digitar, e recusa por nome um `data-v` que não é perfil (`KeyError` → ValueError, `:238`). O `None` do 'Eu escolho' viaja como escolha PRESENTE, e a prova não digita a chave de disco (`PROVAS`, `a09_sistema.py:276-285`). A cura do `<select>` branco viaja no produto por padrão: `ponte_da_tela.py:118 FOLHA_DA_CASA` com `select{appearance:none}`, aplicada em `:189`.",
      "falta": "As três linhas da faixa não são pintadas com o disco: `bateria-impoe` devolve '—' sempre (medido com `rumble_policy` no state, porque `_leitura` nunca preenche `Leitura.perfil`), `bateria-perfil` não tem `data-campo` (o botão aceso continua sendo o que o gerador escolheu, `PERFIS[0]`), e `bateria-frase` não tem lugar na página. Depois do clique, portanto, a tela não confirma nada. E o item 2 da sprint ('o contrato de gravação NÃO muda: acumula em `host._maquina_pendente` e não grava') está INVERTIDO — o gesto grava na hora via `machine_declare` (`a09_sistema.py:246`), por decisão dela de 01/09 documentada no próprio docstring. Isso é decisão nova, não defeito, mas a sprint como escrita não fecha assim. `tests/unit/test_migra_sistema_08_o_perfil_da_mesa_muda_de_aba.py` não existe."
    },
    {
      "id": "MIGRA-SISTEMA-09",
      "feito": "A linha do ambiente tem dono declarado e endereço que PINTA: `gui/aba_sistema.py:330 linha_do_ambiente()` recorta o predicado da frase e tira o markup do Pango (`sem_markup:190`); `hefesto-ambiente` está entre os 9 que casam. E os plugins estão DECLARADOS como sem dono, com o motivo medido, em vez de fingirem funcionar: `a09_sistema.py:33-37 SEM_DONO['plugins']` e `aba_sistema.py:95 GESTOS['ver-plugins']`; o `title` do botão na página até avisa 'Hoje só o terminal alcança isso'.",
      "falta": "(a) O protocolo não é publicado: `grep -rn 'XDG_SESSION_TYPE|session_type' src/.../daemon/` só acha `subsystems/keyboard.py:118-120` — nada no `state_full`. `descrever_display_grafico` continua com ZERO chamador de produção e continua sendo lápide em `tests/unit/portao_a_casa_sabe_e_o_produto_nao_faz.py:2250`; `a09_sistema._leitura` não preenche `Leitura.ambiente`, então a linha mostra '—' sempre (medido). O único chamador vivo é `sistema_viva.py:194-201`, o piloto órfão. (b) Os plugins continuam só no terminal: `grep -rn 'plugin\\.list|plugin\\.reload' src layout` devolve `cli/cmd_plugin.py:43` e `:77` como únicos chamadores; `ver-plugins` não tem `@gesto`. O `title` promete 'Lista os plugins do daemon e relê' e nada acontece. `tests/unit/test_migra_sistema_09_o_ambiente_e_os_plugins.py` não existe."
    }
  ],
  "abertas": [
    "MIGRA-SISTEMA-06 — as cinco linhas de exame que não existem. `storm_report` continua devolvendo EXATAMENTE os mesmos seis checks: `src/hefesto_dualsense4unix/integrations/storm_doctor.py:774-779` (check_snd_quirk, check_snd_audio_healthy, check_quirk, check_steam_input, check_wireplumber, check_authorized_rule). `grep -n 'camadas_vulkan|radio_da_mesa|proton_pin|coop|is_enabled' storm_doctor.py` → ZERO ocorrências. Nenhuma das cinco leituras novas existe; `LinhaDeSaude` não existe; `tests/unit/test_migra_sistema_06_os_cinco_achados_novos.py` não existe.",
    "MIGRA-SISTEMA-07 — o pretérito espera o motor. Nada da sprint existe, e o defeito que ela existe para impedir ESTÁ NA TELA PUBLICADA: `layout/09-sistema.html`, dentro de `data-id=\"exame-lista\"`, traz estático e nunca repintado 'OK · Steam Input estava ligado em 2 jogos — desliguei · O que eu vi: Mortal Kombat 1 e Elden Ring estavam com o Steam Input ligado. O exame desligou nos dois…' e 'NOTA · Proton fixado em 9.0-4 para 3 jogos'. Nenhum motor rodou. A lápide `integrations/proton_pin.py::steam_root_ou_recusa` continua de pé em `tests/unit/portao_a_casa_sabe_e_o_produto_nao_faz.py:2351`. Não há marca de 'houve ato' em campo nenhum, o rótulo 'Refazer os consertos automáticos' é fixo, e `tests/unit/test_migra_sistema_07_o_preterito_nao_mente.py` não existe.",
    "MIGRA-SISTEMA-10 — o registro técnico e o Restaurar de fábrica. (a) `registro-texto` não é pintado por ninguém (não tem `data-campo`) e a página mostra um log FALSO com hora: `data-id=\"registro-texto\">[23:41:02] daemon pronto · 2 controles · 2 gamepads virtuais · uinput ok`. O `title` do botão continua prometendo 'Joga as últimas 80 linhas do registro técnico no painel ao lado' e `ver-detalhes` não tem `@gesto`; não nasceu método de IPC de log (`gui/aba_sistema.py:108-115 SEM_FONTE` declara isso por escrito), e o `registro` que `pacote()` devolve é descartado pelo achatamento (`a09_sistema.py:105-107`). (b) O Restaurar de fábrica NÃO mudou de casa: `btn_footer_restore_default` continua em `src/hefesto_dualsense4unix/gui/main.glade:4269` com `<signal handler=\"on_restore_default\"/>` em `:4272`, e `on_restore_default` continua em `app/actions/footer_actions.py:1477`. O botão da página nova (`data-gesto=\"restaurar-de-fabrica\"`) não tem dono — existe nos dois lugares, e o novo é morto. `tests/unit/test_migra_sistema_10_o_registro_e_o_restaurar.py` não existe."
  ],
  "achados": [
    "CINCO ENDEREÇOS DA ABA 09 NÃO SÃO PINTADOS POR NINGUÉM, e a página mostra o mockup no lugar deles. O piloto único pinta SÓ por `data-campo`/`data-papel`/`data-hef` (`hefesto_vivo.py:131-134`, função `achar`). Medido: `set(data-id) - set(data-campo)` em `layout/09-sistema.html` = `bateria-perfil, exame-contagem, exame-lista, hefesto-autostart, registro-texto`. O `casamento.py` NÃO enxerga isso porque só conta `data-campo` (`casamento.py:39-42`) — a régua e o piloto compartilham o mesmo ponto cego, então os cinco não aparecem nem como 'vazios'.",
    "REGRESSÃO MEDIDA: a aba 09 pinta MENOS hoje do que em 30/08. `layout/_ferramentas/sistema_viva.py` (577 linhas, criado em 48b4e1a2, 30/08) pinta por `data-id` (`:211 const end = id => q('[data-id=\"'+id+'\"]')`), desenha o exame com contagem derivada em duas colunas (`:253-260`), chama `storm_report` (`:168`) e `descrever_display_grafico` (`:194-201`), e tem a mordida `--arranca-enderecos` (`:418-427`). Ele ficou ÓRFÃO: `scripts/abrir_interface.py:77-81` só lista `hefesto_vivo.py` e `controles_vivos.py`, e `grep -rn sistema_viva` não acha nenhum lançador. O trabalho de 01/09 trocou o veículo e deixou a carga para trás.",
    "O ACHATAMENTO DE `a09_sistema.pacote` DESCARTA TRÊS ENTREGAS INTEIRAS, sem erro e sem log. `gui/aba_sistema.pacote()` (`:614-637`) devolve `valores`, `autostart`, `perfil`, `frase`, `exame`, `registro`, `travas`. O laço de `a09_sistema.py:105-107` só copia quatro chaves e só se NÃO forem `dict`/`list` — então `exame` (o exame inteiro), `travas` (a matriz de botão cinza + motivo) e `registro` caem no chão. Medido: o pacote emitido não contém nenhuma das três.",
    "`hefesto_vivo._gesto` AFIRMA NA DOCSTRING o que o código não faz: `:492-495` diz 'despacha o que tem DONO no daemon e RECUSA o resto **com o motivo na tela**' — e `:504` faz `print('[gesto sem dono] …')` no stdout, `:530` imprime a falha em stderr, `:537` o sucesso no stdout. Nada chega à página. Nove dos doze botões da aba Sistema respondem calados para quem clica, que é exatamente o `A-CASA-SABE-E-O-PRODUTO-NAO-FAZ` em miniatura que o próprio docstring cita.",
    "`a09_sistema._leitura:69` deriva o estado do serviço de 'o daemon respondeu' (`'online_systemd' if ctx.state else 'offline'`), não da matriz de três fontes de `daemon_actions._daemon_status`. Consequência: os estados `online_avulso` e `iniciando` NUNCA podem aparecer na tela nova — e `online_avulso` é justamente o caso em que a MIGRA-SISTEMA-01 e a 04 dizem que falta o botão 'Corrigir modo de execução'. A tela nova não tem como nem sequer relatar o estado.",
    "O REGISTRO DA CASA-SABE GANHOU 18 ENTRADAS `gui/aba_sistema.py::*` HOJE, e uma delas isenta um valor morto. As entradas (a partir de `tests/unit/portao_a_casa_sabe_e_o_produto_nao_faz.py:959`) dizem 'Auxiliar INTERNA… o caminho existe e passa pela porta `pacote`, que layout/_ferramentas/pacotes/a09_sistema.py chama'. Para `gui/aba_sistema.py::exame` (`:968`) isso é verdade no grafo de IMPORT e falso na tela: o valor que `exame` produz é descartado antes de sair do pacote. O portão mede alcance por grafo, então ele não pode ver isso — mas a isenção passa a esconder um valor que não chega a lugar nenhum.",
    "CONTRATO DESALINHADO E SEM RÉGUA QUE PEGUE: `aba_sistema.ENDERECOS` tem 12 chaves, a página tem 11 `data-id`, e `bateria-frase` não tem lugar. O gerador só reprova o sentido inverso (`aba09.py:180`, endereço na página fora do contrato); a única régua que compara os dois lados é `tests/unit/test_a_aba_sistema_nomeia_o_servico.py:146`, e ela exige apenas interseção NÃO VAZIA. É o mesmo ponto cego que a MIGRA-SISTEMA-02 mandava fechar com a régua que nunca foi escrita.",
    "A PREMISSA DA MIGRA-SISTEMA-08 CADUCOU e ninguém anotou: o `<select>` do Perfil de Bateria virou três botões (`09-sistema.html:1079`, `<div class=\"seg bat-perfis\">` com `data-v=\"tudo_ligado|bateria_longa|eu_escolho\"`). A página inteira tem ZERO `<select>` (`grep -n '<select' layout/09-sistema.html` só acha comentários de CSS). A armadilha da caixa branca do WebKitGTK, que a sprint usa como mordida obrigatória, não se aplica mais a esta aba — embora a cura (`FOLHA_DA_CASA`) esteja no produto de qualquer modo.",
    "NENHUM dos dez arquivos de teste que as sprints declaram em `cria:` existe: `ls tests/unit/ | grep -c migra_sistema` = 0. As réguas que a leva de 01/09 entregou (`test_os_botoes_tem_dono.py`, `test_o_casamento_das_dez.py`, `test_o_rodape_das_dez.py`, `test_o_perfil_chega_na_tela.py` — 70 passed) são de OUTRO recorte: por aba viva, não por sprint, e nenhuma delas cobre exame, registro, travas ou os nove gestos sem dono.",
    "Texto GERADO com erro de concordância na página que ela vê: `layout/09-sistema.html` traz 'Bluetooth: 1 adaptador, 1 controles no rádio'. A frase é montada por número no gerador e não trata o singular.",
    "As dez sprints continuam citando `novo-layout/` (pasta que não existe mais nesta árvore), e a `cria:` da MIGRA-SISTEMA-02 aponta para `novo-layout/_ferramentas/regua_enderecos09.py`. Os `depois_de` das dez nomeiam `MIGRA-CONTROLES-PILOTO` e `MIGRA-MOLDURA-01`, que não existem como sprint. O portão `colisao-de-sprints` fica verde porque não confere existência de caminho nem de id.",
    "PORTÕES E SUÍTE conferidos por mim: `bash scripts/portoes.sh --rapido` → 'TODOS VERDES — 23 portões' (o `--rapido`, não os 30 completos); `pytest tests/unit/test_os_botoes_tem_dono.py test_o_casamento_das_dez.py test_o_rodape_das_dez.py -q` → 70 passed. Nenhum desses portões mede o que esta onda pede: o verde de hoje convive com o exame, o registro e nove gestos mortos na aba 09."
  ]
}
