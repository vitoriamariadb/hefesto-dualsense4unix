# ONDA5-09-01 · o botão volta a se chamar "Atualizar"

Árvore: `hefesto-voo/ONDA5-09-01-A0901`, branch `voo/ONDA5-09-01-A0901`, nascida
de `onda/atual-0609`. Metade de TELA; a de mecanismo é a `ONDA5-09-02`, noutro
arquivo, e nada dela foi tocado.

## O que mudou

**É uma REVERSÃO, e a reversão é dela.** Em 04/09 o PO decidiu `"Reaplicar
ajustes"`; em 05/09 ela leu a mesma pergunta (09-Q1) e escolheu o contrário:
*"Segue fazendo os dois. Com mesmo nome"*. A palavra dela vence a recomendação.

| onde | antes | depois |
| --- | --- | --- |
| `interface/aba09.py:996` | `ROTULO_REAPLICAR = "Reaplicar ajustes"` | `ROTULO_ATUALIZAR = "Atualizar"` |
| `interface/aba09.py:997` | `EM_VOO_REAPLICAR = "Reaplicando…"` | `EM_VOO_ATUALIZAR = "Atualizando…"` |
| `interface/aba09.py` (a dica) | *"reaplicar a configuração"* | *"reler os atalhos do controle"* |
| `interface/aba09.py:973-996` | 24 linhas defendendo o nome que caiu | o comentário novo, abaixo |
| `interface/aba09.py` (régua 9) | cita *"o PO decidiu"* e `ipc_handlers.py:4823-4832` | cita a 09-Q1 dela e os endereços medidos |
| `mockup/09-sistema.html:1245` | `Reaplicar ajustes` · `data-hef-em-voo="Reaplicando…"` | `Atualizar` · `data-hef-em-voo="Atualizando…"` |

**Os NOMES trocaram junto com os valores** (Passo 1 da sprint): `ROTULO_REAPLICAR`
guardando `"Atualizar"` seria a próxima pessoa lendo o nome e concluindo o
contrário do valor. Alcance: a montagem, as três mensagens da régua do gerador,
e seis pontos da suíte da aba.

### O comentário que substituiu as 24 linhas

```python
# ---------------------------------------------------------------------------
# O BOTÃO SE CHAMA "ATUALIZAR", E O NOME É PALAVRA DELA — 05/09/2026, a 09-Q1:
# *"Segue fazendo os dois. Com mesmo nome"*.
#
# A RECOMENDAÇÃO DE 04/09 PROPUNHA O CONTRÁRIO — rebatizá-lo de "Reaplicar
# ajustes", nomeando-o pela metade cara — e PERDEU quando ela leu a mesma
# pergunta. O que faz o nome parar de mentir não é o rótulo: é a DICA, que diz
# os dois trabalhos na ordem em que acontecem.
#
# A METADE BARATA JÁ ACONTECE SOZINHA, e é ela que deixa o nome caber num botão
# que faz os dois: a releitura da aba custa 4 ms e o `LENTO_S = 2.0`
# (`interface/pacotes/a09_sistema.py:172`) a refaz a cada dois segundos, sem
# ninguém clicar. Este botão nunca foi a única forma de reler a aba.
#
# E ELE FICAVA NOVE SEGUNDOS E MEIO CALADO (medido no daemon dela em 01/09):
# o gesto corre em thread para a janela não congelar, e até 04/09 NENHUMA das
# dez abas tinha estado "em voo" — o clique sumia e o segundo clique parecia o
# primeiro. O `data-hef-em-voo` é o rótulo da espera, e o piloto o põe no lugar
# exato do clique e devolve o original nos três desfechos.
#
# A PALAVRA NÃO É INVENÇÃO DE FORMA: `Atualizando…` é o gerúndio do rótulo — e
# é literal dela, na 09-Q3 —, na mesma gramática que os recibos do motor já
# usam (*"Reiniciando o Hefesto…"*, `app/actions/daemon_actions.py:2306`).
```

Ele SAI e não vira nota, pela regra da casa (*fato errado se SUBSTITUI*): o
argumento das 17 letras defendia um rótulo que a decisão dela derrubou, e
nenhuma medição se perde — a das 4 ms e do `LENTO_S` migrou para a redação nova,
porque é ela que sustenta a dica.

**Uma citação morta caiu junto:** o comentário antigo apontava
`daemon_actions.py:2293` para *"Reiniciando o Hefesto…"*, e naquela linha mora
outra coisa. O endereço certo é `:2306`.

### A dica passou a dizer o que foi MEDIDO

Ela prometia *"reaplicar a configuração"*. Com `config_overrides` vazio isso
**não acontece**: `daemon/lifecycle.py:1353` e `:1361` comparam `old` com `new`
e nunca disparam. O que acontece são duas coisas —
`daemon/lifecycle.py:1351-1352` derruba e sobe o leitor dos atalhos do controle,
e `daemon/ipc_handlers.py:5472` reescreve os arquivos de ambiente da Steam. A
dica passou a nomear essas duas e continua fechando com a releitura da aba.
**Nenhum número de tempo entrou** — *"Leva alguns segundos"* é o que a medição
sustenta em qualquer máquina.

A terceira mensagem da régua também citava `ipc_handlers.py:4823-4832`, onde
hoje mora um docstring sobre outra coisa; passou a citar `:5472` dentro de
`_handle_daemon_reload` (`:5431-5473`), e a dizer os dois trabalhos medidos no
lugar do que se supunha.

## A prova de tela

Janela SEMPRE oculta (`--oculta` / `oculta=True`), Playwright em `headless`.
Nada foi mandado ao daemon: o gesto é um dublê que entra pelo registro
(`pacotes.GESTOS`) e dorme 1,4 s. O driver do clique rodou com `HOME` e os
quatro `XDG_*` desviados para um lar de mentira — conferido depois: só cache do
WebKit e do GTK nasceram lá.

**A FOTO**, `interface/olhar.py 09-sistema.html` (a bancada, Chrome headless,
1180x777, `passa_da_dobra: 0`): antes o terceiro botão da coluna do serviço diz
`Reaplicar ajustes`; depois diz `Atualizar`. Os quatro botões continuam na
ordem — Retomar · Reiniciar o serviço · Atualizar · Parar o serviço — e as duas
colunas irmãs continuam acabando no mesmo `y`.

**O CLIQUE**, no motor do produto (WebKit do piloto, página da bancada):

```json
"1-repouso": { "rotulo": "Atualizar",    "em_voo": false }
"clique":      "cliquei"
"2-no-voo":  { "rotulo": "Atualizando…", "em_voo": true  }
"3-pousou":  { "rotulo": "Atualizar",    "em_voo": false }
```

O `title` lido nos três instantes é a dica nova, inteira.

## Qual mordida prova

**As três guardas do gerador** (`python3 aba09.py`), cada uma com a cura
arrancada e devolvida:

```
===== o rótulo antigo cravado na montagem =====
ERRO: o botão do `daemon.reload` diz 'Reaplicar ajustes' e ela mandou manter
'Atualizar' (09-Q1, 05/09/2026: *"Segue fazendo os dois. Com mesmo nome"*). …
rc=1

===== a dica volta a dizer 'Não muda nada' =====
ERRO: a dica do botão do `daemon.reload` voltou a dizer 'Não muda nada'. É
FALSO e está medido: … o serviço derruba e sobe o leitor dos atalhos do
controle (`daemon/lifecycle.py:1351-1352`) e reescreve os arquivos de ambiente
da Steam (`daemon/ipc_handlers.py:5472`, dentro de `_handle_daemon_reload`,
`:5431-5473`). …
rc=1

===== o botão perde o rótulo da espera =====
ERRO: o botão 'Atualizar' perdeu o `data-hef-em-voo`. Sem ele o clique some por
nove segundos e meio sem uma letra na tela, e o segundo clique parece o
primeiro — é a 09-Q3 …
rc=1

===== a cura devolvida =====
09-sistema: OK, 59 divs · a faixa do serviço: 'O serviço' · 'O serviço está' ·
'Retomar' · 'Reiniciar o serviço' · 'Atualizar' · 'Parar o serviço'
rc=0
```

**A régua da dica** — tirei a palavra que ela cobra:

```
>       assert "atalhos" in dica.lower() and "Steam" in dica, dica
E       AssertionError: Manda o serviço mexer no que ele já tinha lido e
        reescrever os arquivos de ambiente que a Steam usa …
FAILED …::test_a_dica_do_reload_parou_de_negar_o_trabalho_caro
1 failed, 31 deselected
```

**O portão do desenho**, antes e depois de declarar a divergência:

```
FALHA: o produto está ATRÁS do desenho em 1 página(s), sem declaração:
  - 09-sistema.html
rc=1
                       ↓  (declarada em mockup/DIVERGENCIAS.md)
OK: o produto não está atrás do desenho dela sem dizer por quê.
rc=0
```

### DUAS MORDIDAS DA PRÓPRIA SPRINT NÃO MORDIAM — e é o achado do dia

**1. Nenhuma régua segurava a palavra dela.** A §3 Passo 1 mandava pôr
`"Reaplicar ajustes"` de volta na constante e prometia que `aba09.py:1665`
reprovaria. **Medido: `rc=0`.**

```
09-sistema: OK, … 'Retomar' · 'Reiniciar o serviço' · 'Reaplicar ajustes' · 'Parar o serviço'
gerador rc=0
1 passed, 31 deselected      ← a régua da suíte também passou
```

A guarda do gerador compara o HTML montado com a **mesma constante que o
montou**; a régua da suíte também lê a constante, e `import aba09` reescreve a
página antes de ela olhar. As duas mediam o acordo do arquivo consigo mesmo. O
rótulo podia voltar ao nome que ela recusou **com tudo verde**.

**Curado onde cabia** (a camada 2 é da minha posse): ela passou a cobrar
`"Atualizar"` e `"Atualizando…"` LITERAIS, ao lado das comparações por
constante. As duas palavras são dela, e não existe outro dono a quem perguntar —
`docs/data/decisoes-dela.csv` não tem a linha da 09-Q1. Agora morde:

```
>       assert rotulo == "Atualizar", (
E       AssertionError: o botão diz 'Reaplicar ajustes'. Ela mandou manter
        'Atualizar' — *"Segue fazendo os dois. Com mesmo nome"*, 09-Q1 …
FAILED …::test_o_botao_do_reload_manteve_o_nome_e_diz_a_espera

>       assert 'data-hef-em-voo="Atualizando…"' in atributos, (
E       AssertionError: o rótulo da espera não é o dela: … data-hef-em-voo="Reaplicando…"'.
        Na 09-Q3 ela escreveu *"o botão diz Atualizando…"*.
```

**2. A camada 3 nunca chega a medir o `em_voo` arrancado.** A §5.3 mandava tirar
o `em_voo=EM_VOO_ATUALIZAR` e *"ver a camada 3 medir o botão dizendo Atualizar a
espera inteira"*. O que acontece:

```
E       SystemExit: ERRO: o botão 'Atualizar' perdeu o `data-hef-em-voo`. …
src/hefesto_dualsense4unix/interface/aba09.py:1679: SystemExit
2 failed, 30 deselected
```

A régua 9 do gerador levanta no `import aba09` e a página nunca é escrita. A
guarda do gerador é mais dura que a régua da tela — e quem responde por este
atributo é ela. As duas correções estão na sprint, no lugar onde a afirmação
falsa estava, e na nota datada do topo.

**Arrancar o atributo direto do HTML não morde**, e vale registrar: `import
aba09` regera `mockup/09-sistema.html` no próprio *setup* do teste, então uma
edição à mão na página é desfeita antes da leitura.

## O que NÃO verifiquei

* **O produto instalado.** Não publiquei — `interface/paginas/09-sistema.html`
  está no `nao_toca` e publicar é ato dela. O que ela vê hoje continua dizendo
  `Reaplicar ajustes`.
* **O botão contra o daemon vivo.** Não cliquei nada que falasse com o serviço
  dela: o gesto foi dublado nas duas medições. Se `daemon.reload` diz "Pronto."
  sem ter feito é a `ONDA5-09-02`, e não medi.
* **A piscada verde da 03-Q4** neste botão — é do piloto, está no `nao_toca`, e
  a §4 da sprint declara a dívida: até ela chegar, o desfecho é a tarja verde de
  rodapé que já existe.
* **A suíte inteira.** Rodei só o meu escopo, 32 verdes.
* **Fotos da documentação** (`docs/usage/assets/`): não rodei nada que as
  reescreva.

## O que sobrou para o próximo

* **`docs/data/decisoes-dela.csv` não tem a 09-Q1 nem a 09-Q3.** A régua da
  camada 2 teve de digitar as duas palavras dela porque não há dono a quem
  perguntar. Enquanto a linha não existir, toda régua que precise da palavra
  dela vai redigitá-la — que é a forma de defeito que esta casa persegue. O
  arquivo não é da minha posse.
* **Duas citações de `"Reaplicando…"` sobrevivem em arquivo alheio**, e as duas
  são prosa de comentário, não código:
  `src/hefesto_dualsense4unix/interface/hefesto_vivo.py:821` (*"«Reaplicando…»
  na `09`"*, `nao_toca`) e
  `tests/unit/test_o_recado_de_sucesso_pousa_no_cartao.py:176`. Ficaram
  descrevendo um rótulo que não existe mais. Um `sed` de duas linhas, na posse
  de quem for dono desses arquivos.
* **A GTK-1 vai querer saber:** a aba 09 lê o dono em `gui/aba_sistema.py`
  (`interface/pacotes/a09_sistema.py:63`, e a régua desta aba importa
  `hefesto_dualsense4unix.gui.aba_sistema` como `_tela`). **É reuso de dono, e
  ele fica** — não achei nenhum outro ponto em que esta aba dependa da janela
  GTK além desse.
* **A `A-TELA-SAMBA-01` não me atrapalhou:** o clique aplicou de primeira nas
  duas execuções do driver, e nenhuma repetição foi necessária.
* **A bancada** (`scripts/bancada.sh`) não foi reservada: nenhum caminho meu
  parou o daemon, escreveu no aparelho ou chamou `systemctl`.
