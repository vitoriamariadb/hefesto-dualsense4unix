# ONDA5-06-02 — o interruptor apagado diz o lado, e o PS ganha a 22ª linha

**Agente B0602 · árvore `hefesto-voo/ONDA5-06-02-B0602` · branch
`voo/ONDA5-06-02-B0602` · base `onda/atual-0609` (`eb7b844c`).**
Bancada: **não reservada** — nenhum caminho parou o daemon nem escreveu no
aparelho. O daemon ficou VIVO e só foi LIDO; o `HOME` de toda execução foi de
mentira (ver *A prova de tela*).

---

## O que mudou

### 1 · `interface/aba06.py` — a folha do portão, a 22ª linha e o `?`

| passo | o quê | onde |
| --- | --- | --- |
| **P1** | a regra que pintava o `.pino` sob o portão **SAIU** | a folha do `.tog` |
| **P2** | `pointer-events:none` no rótulo, e o `cursor:not-allowed` **mudou para a `.at-linha`** que o contém | mesma regra `:has()` |
| **P3** | `(gl("ps"), "ps")` entre `create` e as três regiões do touchpad | `BOTOES` |
| **P4** | o parágrafo do `?` virou o contrário: **o PS faz as duas coisas** | `D_DEFINICOES` |
| **3-bis** | conferência NOVA: a lista do desenho tem de ser a do produto | `_conferir` |

**Os cinco números da tela acompanharam sozinhos** (`len(BOTOES)`): as duas
dicas, as duas confirmações de "Voltar ao padrão" e o `title` do banco de
provas. Nenhum foi digitado — é o que a §3 da sprint mandava conferir em vez de
editar, e foi o que se conferiu.

### 2 · `interface/pacotes/a06_navegacao.py` — a quinta frase da tira

* **`_o_que_o_ps_faz(p)`** — a linha que faz a última oração da decisão dela
  (*"e a tabela não avisa isso"*) deixar de ser verdade. Ela nasce **só quando
  há o que dizer**: com `— Nada —` as duas metades calam, sem escolha o PS é o
  que sempre foi, e com uma tecla ela nomeia as duas. Segundo ramo: a escolha
  que **ninguém atende** (um `BTN_*`, um papel de eixo) — o `resolver()` tira o
  PS das três sacolas, então ela não aparecia em lugar nenhum da tela e o daemon
  a registrava só no journal (`ps_solo_escolha_sem_atendente`).
* **`_o_ps_digita(token)`** — o predicado **perguntado ao dono**
  (`hotkey._o_ps_digita`, import tardio, 158 ms no primeiro tique, sem arrastar
  `gi`). Devolve `None` quando não dá para perguntar, e aí a tira **não escreve
  nada** em vez de inventar a resposta.
* **A prosa do cabeçalho, corrigida** — dizia que o `ps_button_action` é *"o
  único pedaço ajustável"* e que *"método de IPC nenhum escreve"*. As duas
  metades caíram: escreve (`ipc_handlers.py:5450` · `:5462-5463`, os números
  que a 06-01 mediu; os antigos `:4556`/`:4567` são hoje o cache de órfãos HID)
  e deixou de ser o único (o perfil vence, desde a 06-01). O mesmo par de
  números foi remedido na entrada `acao-do-gesto` do `SEM_GESTO`.
* **Quatro contagens de "21" que eram afirmação de HOJE** passaram a ler
  `len(acoes.BOTOES)` — inclusive **as duas frases que chegam à tela dela**
  (as recusas do "Guardar" e do "Voltar ao padrão"). As citações históricas
  ficaram: são falas dela e medições datadas.

### 3 · O desenho, e a decisão de PUBLICAR

`mockup/06-navegacao.html` foi regerado e **`interface/paginas/06-navegacao.html`
publicado** (`--publicar 06`). As duas ficaram byte a byte iguais, então **não
há seção a declarar em `mockup/DIVERGENCIAS.md`** — o texto que a §5 do despacho
pedia não existe, e é por isso.

**A §9 da sprint diz o contrário ("o desenho não se publica sozinho"), e a
decisão de publicar é minha, com quatro razões medidas:**

1. `interface/paginas/06-navegacao.html` está em **`posse:`**. As sprints irmãs
   da mesma leva (`ONDA5-05-01`, `ONDA5-02-02`) põem o publicado da aba delas em
   **`nao_toca:`** — a diferença é deliberada;
2. o plano das 24 horas roteia esta frente como **`aba06 · a06 · paginas/06`**;
3. **o piloto renderiza o PUBLICADO** (`hefesto_vivo`, `onde.pagina(...,
   publicado=True)`). Sem publicar não existe prova de tela, e ela é obrigatória;
4. **duas das sete réguas leem a página publicada**
   (`test_a_tela_entrega_as_vinte_e_uma_linhas`), e o despacho exige as sete
   verdes.

**Se quem costurar discordar, o desfazer é um comando** —
`git checkout HEAD -- src/hefesto_dualsense4unix/interface/paginas/06-navegacao.html`
— e o custo é conhecido: aquelas duas réguas voltam ao vermelho e a aba fica com
21 linhas na tela dela sobre um produto de 22 (o `Guardar` descartaria a escolha
do PS em silêncio, porque a varredura não a traria).

### 4 · As SETE réguas, uma a uma — o que cada uma passou a perguntar

**Nenhuma substituição em massa. As sete foram lidas e decididas uma a uma**, e
**cinco não precisaram de uma linha de edição**: a linha do PS na tela as curou.

| régua | o que ela passou a perguntar | editada? |
| --- | --- | --- |
| `test_o_que_cada_botao_faz_tem_campo::test_as_vinte_e_uma_linhas_tem_padrao_e_rotulo` | **"a tela e o produto listam os mesmos botões?"** — conta os `data-linha` da página PUBLICADA contra `BOTOES`, no lugar do `assert len(BOTOES) == 21` | **sim** |
| `test_a_tela_entrega…::test_a_forma_traz_as_vinte_e_uma_linhas_com_o_endereco_do_produto` | a mesma de sempre — *"o Guardar recolheu toda linha de `BOTOES`?"* — e agora a resposta é sim, porque o `ps` está no DOM | não |
| `test_a_tela_entrega…::test_o_que_a_tela_mostra_e_o_que_o_produto_faz` | idem — *"a tela abre mostrando o padrão?"*; a linha do PS abre em `Abrir a Steam`, que é `padrao()["ps"]` | não |
| `test_a_06_a_escolha_dela_sobrevive_ao_tique::test_as_vinte_e_uma_linhas_dizem_ao_python_que_ela_esta_mexendo` | idem — *"toda linha tem `data-gesto`?"*; o `ps` passou a ter | não |
| `test_a_06_nao_manda_para_o_vazio::test_toda_chave_emitida_tem_endereco_ou_esta_declarada` | idem — *"o `acao-ps` tem onde pousar?"*; passou a ter | não |
| `test_a_06_nao_manda_para_o_vazio::test_todo_valor_emitido_existe_como_opcao_daquela_lista` | idem — *"há `<select data-campo="acao-ps">`?"*; há, com 26 opções | não |
| `test_a_aba_06_navegacao_fecha_as_linhas::test_a_dica_da_tela_de_botoes_diz_por_que_o_ps_fica_fora` | **virou o contrário**: (1) `"ps" in BOTOES`; (2) a dica **não** volta a dizer "não entra"; (3) ela nomeia as DUAS metades (`PS`, `saída de emergência`, `modo jogo`, `junto`) | **sim** |

**O NOME DA SÉTIMA FICOU**, e é decisão declarada, não descuido: ele é citado em
`core/acoes_de_botao.py:76` — **arquivo de outra posse** —, em duas sprints e em
dois relatórios. Renomear aqui deixaria um ponteiro para nada num arquivo que
esta frente não pode editar. **Para quem costurar:** se quiser o nome certo, os
dois lados mudam juntos —
`test_a_dica_da_tela_de_botoes_diz_o_que_o_ps_faz` aqui e a citação lá.

### 5 · Uma régua nova (`cria:`) e uma acrescentada

* **`tests/unit/test_a_06_o_interruptor_apagado_diz_o_lado.py`** — 6 casos, no
  Chrome do sistema. **Ela LÊ a cascata (`getComputedStyle`) e o hit-test, nunca
  o texto da folha**: procurar `--green` no CSS daria verde com a regra do
  portão vencendo por especificidade, que é o defeito que a sprint existe para
  matar. E o clique é do **rato** (`page.mouse.click`) — `el.click()` dispara o
  evento mesmo com `pointer-events:none` e daria verde sobre a metade que não
  foi feita.
* **`test_a_tira_nomeia_as_duas_coisas_que_o_ps_faz`**, na régua da aba.

---

## Qual mordida prova

**Oito mordidas. Cada uma arrancou a cura, viu reprovar e devolveu.**

**1 · P1 — devolver a regra do `.pino` ao portão.** É o defeito original, medido:

```
E  AssertionError: o portão mexeu no que diz o LADO: sem portão o pino e o fundo
E  são ('rgb(80, 250, 123)', 'rgba(80, 250, 123, 0.1)'), com portão são
E  ('rgb(52, 55, 70)', 'rgba(80, 250, 123, 0.1)').
2 failed, 4 passed
```

O pino ficava **cinza com o mouse LIGADO**, e o que sobrava dizendo o lado era o
fundo esverdeado — que sobrevive por acidente, porque a regra do portão não
declara `background`.

**2 · P2 — arrancar o `pointer-events:none`.** O gesto volta a ser postado:

```
E  AssertionError: o clique no interruptor apagado chegou ao Python:
E  ['{"gesto":"modo","campo":"rato-ligado",…,"evento":"click","texto":"Ligado",…}']
1 failed, 5 passed
```

**A ORDEM DAS DUAS ASSERÇÕES É MEDIDA:** o clique vem ANTES do hit-test. Na
primeira redação o hit-test vinha primeiro, a régua morria nele e **nunca
exercitava o caminho que a §4-P2 manda ver**.

**3 · P2b — arrancar o `not-allowed` da `.at-linha`.** Prova a §2.2 letra por
letra — quem decide o cursor passa a ser o pai:

```
E  AssertionError: a linha do interruptor apagado não mostra a recusa…
E  assert 'auto' == 'not-allowed'
```

**4 · P3 — tirar a entrada do PS de `BOTOES`.** Com a conferência 3-bis:

```
ERRO em 06-navegacao — decisão dela desfeita:
  - a tela e o produto não listam os mesmos botões — a mais no desenho: [];
    a menos: ['ps'].
```

**5 · P4 — tirar o parágrafo do PS do `D_DEFINICOES`.**

```
E  AssertionError: a dica não conta as duas coisas que o PS faz — falta
E  ['PS', 'saída de emergência', 'modo jogo', 'junto'] em: 'As <b>22 linhas</b>…'
```

**6 · P5 — cravar o texto (`rotulo = "Enter"` no lugar de `acoes.rotulo`).**

```
E  AssertionError: a tira não nomeia o botão ('Botão PS') e a tecla ('F11') que
E  ele passou a digitar: … Ele digita “Enter” …
```

**7 · P5b — arrancar a guarda do "há o que dizer".** A tira passa a falar sempre:

```
E  AssertionError: a tira fala do PS num perfil que não opinou sobre ele:
E  '…<b>Botão PS: duas coisas ao mesmo tempo.</b> Ele digita “Enter”…'
```

**8 · A régua 1 — o publicado envelhece e o produto anda.** Apagada a linha do
PS do HTML publicado:

```
E  AssertionError: a tela e o produto contam listas diferentes — a mais na tela:
E  []; a menos: ['ps']
```

**9 · A OITAVA RÉGUA, que não estava na lista das sete.**
`test_a_06_o_duble_decide_o_indecidivel.py` cobrava `conferidos == 24`, com a
conta digitada ao lado (*"as 21 linhas de botão, a 'Função do teclado' e as DUAS
barras"*). Com a 22ª linha ela **reprovou a MELHORA** — a forma exata do defeito
que esta casa pagou onze vezes em 26/08. Passou a perguntar ao dono
(`len(BOTOES) + 3`), e a mordida é voltar a digitar:

```
E  AssertionError: conferi 25 linhas na página da bancada e a aba tem 24 …
2 failed, 2 passed
```

**Ela é a prova de que a varredura larga vale mais que a lista herdada:** as
sete vieram do relatório da 06-01, e esta só apareceu rodando tudo que cita
`BOTOES`.

### A mordida que a MINHA régua não aguentou, e é a mais útil daqui

**A mordida 6 PASSOU na primeira tentativa.** A régua da tira usava uma tecla
só (`KEY_ENTER`, rótulo `"Enter"`), então cravar exatamente `"Enter"` no pacote
era invisível para ela. **Uma régua que sobrevive à própria mordida não mede
nada.** Ela passou a percorrer **duas** teclas (`KEY_ENTER` e `KEY_F11`), e a
mordida 6 acima é a segunda tentativa, já com a régua consertada.

---

## A prova de tela

**Janela OCULTA (`Gtk.OffscreenWindow`) em todas as execuções. `HOME` e os
quatro `XDG_*` desviados para um lar de mentira, com uma CÓPIA dos perfis dela**
— `hefesto_vivo` dispara migrações one-shot no `~/.config` real, e o "Guardar"
desta aba grava no perfil ativo. Nada foi escrito na casa dela.

O instrumento é **`scripts/ensaios/a_linha_do_ps_no_webkit.py`** (novo), e ele
roda no **WebKitGTK**, com o daemon vivo — não no Chrome de uma régua:

```
perfil ativo (segundo o daemon): 'Personalizado'

  a linha nasce      {'linhas': 22, 'campo': 'acao-ps', 'valor': 'Abrir a Steam',
                      'gesto': 'linha-de-botao', 'opcoes': 26}
  ela escolhe        {'ok': True, 'agora': 'F11'}
  depois do change   {'linhas': 22, 'valor': 'F11'}
  clica no Guardar   {'ok': True}
  o perfil no disco  {'ps': 'KEY_F11'}          ← o TOKEN, não o rótulo da tela

  portão + LIGADO     pino rgb(80,250,123) fundo rgba(80,250,123,.1) borda rgb(52,55,70)
                      ponteiro none  cursor da linha not-allowed
  portão + DESLIGADO  pino rgb(68,71,90)   fundo rgb(33,34,44)       borda rgb(52,55,70)
                      ponteiro none  cursor da linha not-allowed
  sem portão + LIGADO pino rgb(80,250,123) fundo rgba(80,250,123,.1) borda rgb(80,250,123)
                      ponteiro auto  cursor da linha auto
```

**As duas cenas do portão diferem no pino e no fundo, e são IGUAIS na borda** —
é o esclarecimento dela, medido: *apagado* diz "não dá para mexer" e o lado
continua sendo dito. E o pino sob o portão com o mouse ligado é **idêntico** ao
sem portão: o portão não encosta no lado.

**As mutações da aba 06 continuam em ZERO:**

```
MUTAÇÕES DE DOM em 100 tiques (10.1 s) na 06-navegacao.html, com a mesa parada
TOTAL: 0 mutações · 0.0 por tique
```

**A geometria, remedida** (Chrome, viewport 1212×757, página publicada):

| | linhas | caixa `.tn-cx` | rola? |
| --- | --- | --- | --- |
| HEAD (antes) | 21 | 634,0px | não |
| agora | 22 | 657,0px | não |

Os números que estavam escritos no gerador (*660,3px · sobram 96,7px · y=707,7*)
**já não batiam antes desta frente**, e foram substituídos pelos medidos. No
WebKit, com a tira em DUAS frases, a caixa passa a rolar por dentro — o teto de
altura fazendo o que existe para fazer.

**As fotos** (todas `--oculta`, no scratchpad da sessão):
`06-antes.png` · `06-depois.png` · `ps5-popup.png` (a pop-up com a 22ª linha e a
tira nomeando as duas coisas) · `ps5-portao-ligado.png` · `ps5-portao-desligado.png`
· `interruptor-lado-a-lado.png` (as duas cenas coladas).

**UMA ARMADILHA DE FOTO, medida aqui:** fotografar 700 ms depois de pintar a
cena do portão devolvia o quadro do DAEMON — o tique é de 100 ms e já tinha
repintado o `rato-ligado`. **As duas fotos saíam trocadas uma da outra**, e a
leitura de `getComputedStyle` (feita no mesmo turno do JS) estava certa o tempo
todo. A cura no ensaio tem duas partes, as duas declaradas no arquivo: arrancar
o `data-campo` do interruptor antes de pintar (o `escrever()` deixa de achá-lo;
a cascata não depende dele) **e** esperar 500 ms antes do obturador, porque o
`fotografar` da janela oculta entrega o quadro anterior quando chamado no
instante da escrita.

---

## O que NÃO verifiquei

* **Nada com o aparelho na mão.** O DualSense estava ligado e o daemon vivo, mas
  **não apertei o botão PS**. O que está provado é o caminho até o disco
  (tela → gesto → `Guardar` → `button_actions["ps"]`); que o toque físico digita
  o F11 **e** abre a Steam é a prova da ONDA5-06-01, que também não teve
  aparelho (`bancada: false` lá).
* **A suíte inteira** — é de quem coordena. Rodei o escopo (abaixo).
* **O `mockup/DIVERGENCIAS.md`** não foi tocado, e não precisava: bancada e
  publicado ficaram idênticos.
* **A cena do portão com o daemon dizendo `mouse_emulation.enabled = true` de
  verdade.** Ligar a emulação de mouse **move o cursor na tela dela** — é a razão
  pela qual o gesto `modo` está na lista de perigosos do piloto. As duas cenas
  foram pintadas pelas MESMAS duas escritas que o pacote produz (a razão sai de
  `RAZAO_DO_PORTAO`, a palavra de `LIGADO`/`DESLIGADO`), e o que se mediu foi a
  cascata resultante.
* **O `?` da tela de Remapeamento** não fala do PS, e não precisa: a segunda
  tabela ganhou a linha por consequência (mesma lista), e lá o PS é ORIGEM de
  troca, não de ação.

---

## O que sobrou para o próximo

### Para a `NAVEGACAO-TECLAS-01` (onda C), que vem depois de mim

1. **A aba tem 22 linhas, e o `ps` é a 19ª** — depois do `create`, antes das três
   regiões do touchpad, nas DUAS telas. `aba06.BOTOES` e
   `core.acoes_de_botao.BOTOES` estão na mesma ordem, e agora **há portão** que
   exige o mesmo conjunto (a conferência 3-bis do `_conferir`). Acrescentar
   linha em um lado sem o outro faz o gerador recusar.
2. **`acao-ps` é endereço vivo** — `data-linha="ps"`, `data-campo="acao-ps"`,
   `data-gesto="linha-de-botao"`, 26 opções. O `Guardar` grava o TOKEN.
3. **O PS NÃO está em `key_bindings`** e nunca esteve: `resolver()` o tira das
   três sacolas de propósito (o atendente dele é o `ps_solo`, não um device).
   Quem mexer em `key_bindings` **não** encontra o PS lá, e isso é o desenho.
4. **A tira sob a tabela tem CINCO frases**, e a quinta é do PS
   (`_o_que_o_ps_faz`). Quem acrescentar uma sexta: elas só nascem quando o fato
   existe, e o texto sai do produto — nunca digitado.
5. **A folha do portão mudou de forma.** O `.tog[data-gesto="modo"]` é
   `pointer-events:none` sob o portão, e o `cursor:not-allowed` mora na
   `.at-linha`. Quem mexer no `.at-linha` da aba tem de saber disso: mudar o
   elemento pai quebra a recusa do ponteiro sem quebrar nenhuma outra coisa.
6. **A página publicada foi atualizada nesta frente** (ver §3). Se a onda C
   regerar a aba, tem de republicar — senão a tela dela volta a 21 linhas.

### Prosa e ponteiros que envelheceram em arquivo ALHEIO

* **`core/acoes_de_botao.py:76`** cita
  `test_a_dica_da_tela_de_botoes_diz_por_que_o_ps_fica_fora` e diz que *"quem a
  aposenta é a frente da TELA (ONDA5-06-02)"*. **Ela foi aposentada** — o corpo
  mede o mundo de hoje —, mas o NOME ficou, pelo motivo da §4. A frase de lá
  continua verdadeira; se alguém renomear a régua, este ponteiro morre.
* **`docs/data/paridade-gtk-html.csv`** — o CSV tem UM dono por leva, e **estas
  duas linhas foram fechadas por esta frente, não editadas**:
  * **`:195`** o portão de modo — o interruptor agora apaga o CONTROLE e não o
    LADO, e recusa o ponteiro;
  * **`:207`** a tabela, que diz *"21 linhas"* — **são 22**.
* **`app/actions/mouse_actions.py:299`** (a janela GTK) apaga o switch desde
  sempre e não é desta posse. Não muda: a frase dela manda para a aba "Início",
  que não existe no desenho das dez.
* **`tests/unit/test_a_06_nao_manda_para_o_vazio.py:194`** e
  **`test_o_ps_digita_e_continua_sendo_a_saida.py:259`** têm "vinte e uma" no
  NOME. Verdes, e o nome é histórico — mesma decisão da §4.

### Dívida de produto, declarada e NÃO fechada

**O estado que o daemon publica à tela não traz `ps_button_action`.** Ele viaja
só na resposta de `daemon.reload` (`_config_que_viaja`), nunca no tique. Por
isso a tira **não nomeia o ato da máquina**: dizer *"continua abrindo a Steam"*
seria afirmar o degrau de fábrica sobre uma máquina que ninguém perguntou, e ele
é ajustável (`steam` · `none` · `custom`). A frase que ficou é a que vale nos
três casos (*"continua sendo a saída de emergência"*). **Publicar
`ps_button_action` no estado é uma linha de IPC, e fecha a frase inteira.**

**E a que a 06-01 deixou continua aberta:** `Escolher um programa…` no PS é meio
atendente — o caminho existe (`DaemonConfig.ps_button_command`) mas mora na
MÁQUINA, não no perfil. Hoje a tira **diz isso** (o segundo ramo de
`_o_que_o_ps_faz`), que é o contrário de um botão que responde calado; a cura de
verdade é dar campo de CAMINHO ao perfil, e é sprint própria.

### Uma dúvida de DESENHO, que é dela

A linha do PS nasceu com **glifo + a palavra "PS"**, como as outras dezessete
linhas que não são face nem direcional. **`linha_combo` faz o contrário** (só o
glifo), e a razão é dela, de 27/08: *"temos o icone do PS e do lado direito PS
escrito novamente, tira a parte escrita"*. Ali o PS aparece dentro de um combo;
aqui ele é uma LINHA, numa coluna chamada "Botão do controle", e sem a palavra
seria a única das vinte e duas que não se lê em texto. **Segui a sprint, que
escreve `(gl("ps"), "ps")` com todas as letras** — mas a escolha é de tela, logo
é dela. Está na foto `ps5-popup.png` para o olho dela decidir.

---

## As três armadilhas deste turno

**1 · A SPRINT ESTAVA ERRADA SOBRE A PRÓPRIA MORDIDA, e a correção virou
portão.** A §4-P3 mandava tirar a entrada do PS e ver a autoconferência do
gerador reprovar *"nomeando quantas linhas achou contra quantas o produto
declara"*. **Não reprovava.** Aquela conferência conta `data-gesto` contra
`len(BOTOES)` — **as duas pontas da mesma lista deste arquivo** —, então tirar
uma entrada diminuía os dois lados: o gerador saía `OK, … 21 botões do mapa`,
rc=0, com a tela mostrando uma linha a menos que o produto atende. Quem pegava
era a SUÍTE, que roda depois, e nunca o gerador, que roda na mão de quem mexe no
desenho. Nasceu a conferência **3-bis**: a lista do desenho tem de ser a de
`core.acoes_de_botao.BOTOES`.

*A forma é a de sempre nesta casa: o instrumento respondia sobre si mesmo.*

**2 · A FOTO MOSTRAVA O TIQUE, NÃO A CENA.** Ver *A prova de tela*. As duas
fotos do interruptor saíam **trocadas uma da outra**, e a leitura de
`getComputedStyle` — feita no mesmo turno do JS — estava certa o tempo todo.
Duas fotos assim, lado a lado, provariam o contrário do que provam. São DOIS
efeitos somados, e cada um sozinho já bastava para o erro: o tique de 100 ms
repintando o `rato-ligado`, e o `fotografar` da janela oculta entregando o
quadro ANTERIOR quando chamado no instante da escrita.

**3 · O PORTÃO DAS CITAÇÕES DE LINHA, que a 06-01 avisou que pegaria.** Ele não
está no `portoes.sh` (só na camada `suite`, de quem coordena) e reprovou com
**16 endereços**. **UM era meu** — `a06_navegacao.py:117` e `:82` citando
`hefesto_vivo.py:2236` para a âncora `_recusou_dizendo`, que hoje está na
`:2523`. Medido com `grep -n` e remedido nas DUAS ocorrências (a régua só
acusava uma; corrigir só a acusada deixaria as duas versões vivas, que é o
defeito que a regra existe para matar).

**Os outros 15 NÃO são meus e vêm da base** — `a10_perfis.py` (×11),
`aba10.py`, `a02_controles.py`, `a03_gatilhos.py`. Não toquei nenhum desses
arquivos (`git diff --name-only HEAD` prova), e a árvore veio assim. **É relato
para quem costurar**, e a lista está em
`test_portao_o_par_com_metade_ligada.py::TestTodaCitacaoDeLinhaConfere`.

---

## O estado ao fechar

* **`bash scripts/portoes.sh`** — ver a linha final desta seção.
* **O escopo, verde:** as SETE réguas da 06-01, a régua nova (6 casos), a régua
  da aba (19 casos), a oitava (`o_duble_decide_o_indecidivel`) e
  `test_a_tela_nao_samba.py` (12 casos). Mais a varredura LARGA — todo
  `tests/unit/test_*.py` que cita `aba06`, `a06_navegacao`, `06-navegacao`,
  `acoes_de_botao`, `button_actions`, `BOTOES`, `ps_solo` ou `ps_button_action`.
* **`git status --short`** limpo depois do commit.
