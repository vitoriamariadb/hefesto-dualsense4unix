# ONDA5-06-01 — o PS ganha dono no motor antes de ganhar linha

**Agente A0601 · árvore `hefesto-voo/ONDA5-06-01-A0601` · branch
`voo/ONDA5-06-01-A0601` · base `onda/atual-0609` (`ec6811aa`).**
Bancada: **não usada** (`bancada: false`; nenhum caminho parou o daemon nem
escreveu no aparelho).

---

## O que se mediu, antes de mexer

* **`ps` não estava em NENHUM dos quatro mapas do produto** — nem
  `BUTTON_TO_UINPUT`, nem `DPAD_TO_KEY`, nem `EDGE_KEY_MAP`, nem
  `DEFAULT_BUTTON_BINDINGS`. Logo o laço de `padrao()` o deixaria em
  `__NADA__`: a tela diria que o botão que abre a Steam há meses não faz nada.
  É o que justifica o degrau da máquina como fonte do de fábrica dele.
* **O validador do perfil é DERIVADO, e a prova é que ele mudou sozinho.**
  `profiles/schema.py` está em `nao_toca:` e não foi tocado; com o `ps` na lista
  do dono, `Profile(button_actions={"ps": "KEY_F11"})` passou a ser aceito, e as
  duas recusas (botão desconhecido, ação desconhecida) continuam de pé.
* **Linha de base da suíte do escopo: 402 verdes** nos 30 arquivos que citam
  `acoes_de_botao` / `button_actions` / `ps_solo` / `ps_button_action`.

---

## O que mudou

**Três arquivos de posse, um teste novo — e quatro citações de linha que o
deslocamento tornou falsas.**

### 1 · `core/acoes_de_botao.py` — o PS vira botão do produto

| o quê | onde |
| --- | --- |
| `BOTAO_PS = "ps"` e o `ps` na tupla, **depois do `create`, antes do touchpad** | `:62`, `:84` |
| `ACAO_DA_MAQUINA_PARA_TOKEN` — `steam→__STEAM__`, `none→__NADA__`, `custom→__PROGRAMA__` | `:105` |
| `PS_DA_MAQUINA_DE_FABRICA = "steam"` — cópia declarada, com régua que pergunta ao dono | `:121` |
| `token_do_ps_da_maquina()` | `:248` |
| **`acao_do_ps(escolhas)` — A QUARTA SAÍDA** | **`:261`** |
| `padrao(ps_button_action=None)` responde pelo PS | `:287`, `:334` |
| `resolver()` tira o PS das três sacolas | `:338`, `:397` |

**A quarta saída é PORTA, não quarta posição na tupla — e é desvio declarado da
§4-P2 da sprint.** Medido: três chamadores desempacotam três valores
(`profiles/manager.py:659` e `interface/pacotes/a06_navegacao.py:1153,1188`), e
devolver quatro viraria `ValueError: too many values to unpack` na aba que ela
abre hoje — o produto quebrado agora para servir uma frente que roda depois. O
destino é o mesmo (o PS não tem device; quem o atende é o `ps_solo`), e nenhum
chamador precisou mudar.

### 2 · `profiles/manager.py` — o canal do PS

* `ProfileManager.ps_action_sink` (`:156`) — canal `lambda`, da família dos dois
  `*_device_provider`. **Não se chama `*_applier` de propósito:**
  `test_a_fabrica_do_gerente_e_a_unica_lista_de_appliers` confere os campos com
  esse sufixo contra `APPLIERS_DO_DAEMON`, e um nome errado ali reprovaria por
  forma, não por fato.
* `ProfileManager._empurrar_o_ps()` (`:587`) — chamado por
  `apply_button_actions` **antes de toda saída antecipada** (perfil sem
  `button_actions`, e "sem device de mouse"). As duas razões são medidas: o PS
  não passa por device, e um perfil que não opina precisa **apagar** o que o
  perfil anterior opinou.
* `_canal_do_ps(daemon)` (`:2213`) + a linha na fábrica (`:2314`) — import lazy
  do subsistema, para não arrastar o daemon para dentro de `core`/`profiles`.

### 3 · `daemon/subsystems/hotkey.py` — o toque faz as duas coisas

* `definir_acao_do_ps` / `acao_do_ps_do_perfil` (`:70`, `:80`) — a escolha do
  perfil **em memória**, no atributo `_acao_do_ps_do_perfil` do daemon. Nada de
  disco no poll loop (o `pgrep` que travava input/IPC/co-op por 2 s continua
  proibido, e `store.active_profile` só devolve o nome).
* `_o_ps_digita` (`:86`) — só `KEY_*` e os três tokens do teclado na tela.
* `_digitar_o_ps` (`:101`) — emite pelo `UinputKeyboardDevice` que já existe.
* `_a_metade_da_maquina` (`:153`) — **a precedência, em tabela**, e ela vem
  ANTES do antigo `if cfg.ps_button_action == "none": return`.
* `build_ps_solo_callback` (`:191`) — **a tecla primeiro, a Steam depois.**

**A tabela da precedência** (`_a_metade_da_maquina`):

| o perfil deu ao PS | digita | a metade da máquina |
| --- | --- | --- |
| nada (`None`) | — | `cfg.ps_button_action`, intocado |
| uma tecla / o teclado na tela | **sim, antes** | `cfg.ps_button_action` — *"SEM parar de abrir a Steam"* |
| `— Nada —` | — | **nada** — o espelho exato do `"none"` |
| `Abrir a Steam` | — | abre, mesmo com a máquina em `"none"` |
| `Sair do modo jogo`, `Escolher um programa…`, `BTN_*`, os dois papéis de eixo | — | `cfg.ps_button_action` + `ps_solo_escolha_sem_atendente` no journal |

### 4 · As quatro citações de linha que eu desloquei, e consertei

`core/rumble.py:113` (`manager.py:1950-1997`→`2079-2129`),
`a02_controles.py:2432` (`hotkey.py:1004`→`1234`),
`a08_conexoes.py:3670` (`manager.py:2046`→`2106`, `:2049`→`:2109`) e
`a08_conexoes.py:3682` (`manager.py:2063-2066`→`2123-2126`). Medidas com
`grep -n` contra o HEAD desta árvore, nunca copiadas de relatório.

---

## Qual mordida prova

**Oito mordidas. Cada uma arrancou a cura, viu reprovar e devolveu.** A suíte do
arquivo volta a **28 verdes** depois de cada uma.

**1 · P1 — tirar o `ps` da tupla `BOTOES`.** Reprovam 6 casos, e o que importa é
que a reprovação vem do arquivo que eu **não** toquei:

```
E  pydantic_core._pydantic_core.ValidationError: 1 validation error for Profile
E  button_actions
E    Value error, button_actions: 'ps' não é um dos botões da tela. Os que
E    existem: cross, circle, square, triangle, l1, r1, l2, r2, l3, l3_direcao,
E    r3, r3_direcao, dpad_up, dpad_down, dpad_left, dpad_right, options, create,
E    touchpad_left_press, touchpad_middle_press, touchpad_right_press
6 failed, 22 passed
```

**2 · P2 — apagar o `tabela.pop(BOTAO_PS, None)` do `resolver()`.**

```
E  AssertionError: com None o PS foi listado como sem dono — ele TEM dono, e o
E  dono é o callback do `ps_solo`.
E  assert 'ps' not in ['ps']
1 failed, 27 passed
```

**3 · P3 — arrancar o `self._empurrar_o_ps(profile)`.**

```
E  AssertionError: o mouse virtual estava de pé? Não — e mesmo assim o PS tinha
E  de receber a escolha, porque quem o atende é o `ps_solo`.
E  assert [] == ['KEY_F11']
3 failed, 25 passed
```

**4 · P3b — mover o empurrão para DEPOIS da saída antecipada.** É a cura que
apaga a escolha do perfil de ontem:

```
E  AssertionError: o canal recebeu ['KEY_F11']: o perfil sem opinião não
E  devolveu o PS ao degrau da máquina.
E  assert ['KEY_F11'] == ['KEY_F11', None]
1 failed, 27 passed
```

**5 · P4 — pôr o `if cfg.ps_button_action == "none": return` de volta como
primeira porta.** É a armadilha que a sprint nomeia:

```
FAILED ...::test_o_perfil_vence_a_maquina_calada
FAILED ...::test_o_steam_do_perfil_vence_a_maquina_calada
2 failed, 26 passed
```

**6 · P5 — inverter a ordem das duas metades.**

```
E  AssertionError: a ordem medida foi ['steam', 'tecla']: a Steam roubou o foco
E  antes de a tecla sair, e a tecla chegou à Steam em vez de chegar ao que
E  estava na frente dela.
1 failed, 27 passed
```

**7 · O emissor — trocar `_emit_sequence_press/_release` por dois `dispatch()`.**
Esta é a que mede uma decisão de engenharia que a sprint não pedia, e o defeito
aparece inteiro na saída — o `circle` que estava segurado **é solto no meio do
toque**:

```
E  AssertionError: o device recebeu [('KEY_ENTER', 1), ('KEY_F11', 1),
E  ('KEY_ENTER', 0), ('KEY_F11', 0)]: o toque no PS mexeu no que estava segurado.
1 failed, 27 passed
```

**8 · As duas guardas do modo jogo, arrancadas juntas.**

```
E  AssertionError: assert [('KEY_F11', 1), ('KEY_F11', 0)] == []
FAILED ...::test_o_modo_jogo_pula_as_duas_metades
FAILED ...::test_o_modo_nativo_pula_as_duas_metades
2 failed, 26 passed
```

**O dublê do teclado é o device de verdade:** `UinputKeyboardDevice` com um
módulo `uinput` de mentira que **só conhece nomes `KEY_*`** — um objeto que
respondesse a qualquer atributo imitaria um teclado que sabe tudo, e a casa já
pagou três vezes por dublê mais frouxo que a função real.

---

## O que NÃO verifiquei

* **Nada com o aparelho na mão.** A sprint declara `bancada: false` e o toque no
  PS não foi medido num DualSense vivo. O que está provado é o CAMINHO
  (perfil → canal → callback → device virtual), com o device de verdade e o
  uinput dublado.
* **Nada na tela.** Esta sprint não muda um pixel, e os quatro arquivos de tela
  estão em `nao_toca:`. Não abri o produto, não fotografei, não cliquei.
* **`padrao(ps_button_action=…)` não tem chamador que passe a config.** O
  parâmetro existe e está provado por régua, mas hoje todo chamador o omite —
  quem tem a config do daemon na mão é a frente da tela, e é ela que vai passá-lo.
* **A suíte inteira.** Rodei 2.795 + 430 casos do escopo largo; não rodei o
  resto (é de quem coordena).
* **`portoes.sh` completo** — resultado abaixo, com um vermelho que não é meu.

---

## O que sobrou para o próximo

### As SETE réguas de tela que ficaram vermelhas — todas da `ONDA5-06-02`

Todas dizem a mesma frase por sete bocas: **o produto tem 22 linhas e o desenho
tem 21.** Elas estão CERTAS; o motor andou primeiro, que é o que a §7 da sprint
pediu. A cura de todas é uma só: a linha do PS na aba.

| régua | o que ela diz hoje |
| --- | --- |
| `test_a_aba_06_navegacao_fecha_as_linhas.py::test_a_dica_da_tela_de_botoes_diz_por_que_o_ps_fica_fora` | mede a decisão de 04/09, que a palavra dela reverteu. **A sprint já a declarava.** Quem a aposenta é a frente da tela, junto com o parágrafo do `?` em `interface/aba06.py:1640` |
| `test_o_que_cada_botao_faz_tem_campo.py::test_as_vinte_e_uma_linhas_tem_padrao_e_rotulo` | *"a tela mostra 21 linhas e `BOTOES` tem 22"* |
| `test_a_tela_entrega_as_vinte_e_uma_linhas.py::test_a_forma_traz_as_vinte_e_uma_linhas_com_o_endereco_do_produto` | *"a varredura do Guardar não trouxe `['ps']`"* |
| `test_a_tela_entrega_as_vinte_e_uma_linhas.py::test_o_que_a_tela_mostra_e_o_que_o_produto_faz` | `ps: None -> __STEAM__` |
| `test_a_06_nao_manda_para_o_vazio.py::test_toda_chave_emitida_tem_endereco_ou_esta_declarada` | *"o pacote da 06 manda `['acao-ps']` e o desenho de hoje não tem onde pôr"* |
| `test_a_06_nao_manda_para_o_vazio.py::test_todo_valor_emitido_existe_como_opcao_daquela_lista` | *"acao-ps: o desenho não tem `<select>` com esse endereço"* |
| `test_a_06_a_escolha_dela_sobrevive_ao_tique.py::test_as_vinte_e_uma_linhas_dizem_ao_python_que_ela_esta_mexendo` | *"as linhas sem `data-gesto` são `['ps']`"* |

**Não apaguei nenhuma, e não afrouxei nenhuma.** Apagar régua alheia é como se
perde a prova de que a decisão mudou.

### As quatro citações de linha que ficaram em `_CITACOES_PENDENTES`

Estão em arquivo do `nao_toca:` (`interface/pacotes/a06_navegacao.py` ×3,
`profiles/schema.py` ×1) e o número certo já está **medido** no comentário que
as acompanha em `tests/unit/test_portao_o_par_com_metade_ligada.py`. Quem for
dono do arquivo troca o número e apaga a linha de lá — o
`test_a_lista_de_pendentes_nao_vira_paisagem` cobra sozinho.

### Prosa que envelheceu em arquivo alheio

* `interface/pacotes/a06_navegacao.py:11` — o cabeçalho diz que *"método de IPC
  nenhum escreve"* o `ps_button_action`. **Escreve**, e a nota que corrige isso
  mais abaixo aponta para `ipc_handlers.py:4556`/`:4567`, que hoje são o cache
  de órfãos HID. Os números certos são **`:5450`** (a leitura dos overrides) e
  **`:5462-5463`** (`replace(config, **overrides)` + `reload_config`).
* `interface/pacotes/a06_navegacao.py:1024` cita `profiles/manager.py:570` para o
  `apply_button_actions` — hoje `:617` (já está no pendente acima).
* `docs/data/paridade-gtk-html.csv:207` diz *"21 linhas"* na tabela do que cada
  botão faz. O CSV tem UM dono por leva — **listado, não editado**.
* `tests/unit/test_toda_secao_de_perfil_tem_quem_a_aplique.py`, na classificação
  de `button_actions`: a razão escrita diz *"resolve a escolha em DUAS metades e
  as empurra aos dois devices virtuais"*. São **três** destinos desde hoje, e o
  terceiro não é device. A régua continua verde (é prosa), mas envelheceu.
* `grep -rn "21 linhas\|21 botões\|vinte e uma" src/ tests/` devolve **109**
  ocorrências em 32 arquivos. As duas da minha posse foram corrigidas; as de
  `interface/` e das réguas caem junto com a linha do PS na tela.

### Dívida de produto, declarada e NÃO fechada

**`Escolher um programa…` no PS é meio atendente.** O caminho do programa existe
(`DaemonConfig.ps_button_command`), mas mora na MÁQUINA e não no perfil. Fazê-lo
disparar daria à escolha do perfil o comando de outro dono — um fato com dois
donos, que é a família que esta casa persegue. Hoje ele cai em
`ps_solo_escolha_sem_atendente` e o degrau da máquina segue valendo. **Fechar
isso é dar campo de CAMINHO ao perfil, e é sprint própria.**

**E um método público de TOQUE falta no `UinputKeyboardDevice`.**
`_digitar_o_ps` usa `_emit_sequence_press`/`_release` — os únicos emissores que
não mexem no rastreador de bordas do `dispatch()`. O `dispatch()` é snapshot e
soltaria toda tecla segurada no instante do toque (mordida 7, com a saída
colada). `integrations/uinput_keyboard.py` é de outra posse: a cura de verdade é
um `tocar(seq)` público lá.

---

## O estado ao fechar

* **`bash scripts/portoes.sh`: 41 verdes de 43.** Os dois vermelhos são
  **byte a byte os da base** (`ec6811aa`), medidos com a minha árvore guardada:
  `referencias-docs` (7 citações de `scripts/migrar-mapa-v2.py`, que não existe
  nesta árvore, em documentos de agosto) e `acentuacao` (7 em
  `docs/data/decisoes-dela.csv:200`, texto dela). **Nenhum dos dois toca arquivo
  meu.** As duas violações de acentuação que ERAM minhas foram corrigidas.
* **Escopo largo, com a árvore parada: 3.016 verdes, 8 pulados, 4 xfail e os
  SETE vermelhos declarados acima** — 163 arquivos de teste (tudo que cita
  `acoes_de_botao`, `button_actions`, `ps_solo`, `ps_button_action`, `aba06`,
  `a06_navegacao`, `BOTOES`, `profiles.manager`, `gerente_do_daemon` ou
  `subsystems.hotkey`). **Não rodei a suíte inteira** — é de quem coordena.
* `ruff check src/ tests/` limpo · `mypy src/hefesto_dualsense4unix`: 302
  arquivos sem queixa.

---

## As duas armadilhas deste turno

**1 · MEDI A ÁRVORE EM MOVIMENTO, e ela me devolveu seis vermelhos falsos.**
Editei dois docstrings em `profiles/manager.py` **enquanto** um `pytest` do
escopo largo rodava. `inspect.getsource` recarrega o arquivo pelo `linecache`
mas usa o `co_firstlineno` do código já importado: os seis casos de
`test_perfil_por_controle_o_campo_espera_o_caminho.py` passaram a comparar a
fonte da função ERRADA, e a queixa saiu perfeitamente convincente —
*"`_controllers_to_specs` não lê o campo `triggers`"*, mostrando o corpo de
`_to_key_bindings`. Com a árvore parada, **66 verdes**. É a armadilha 9 do
`COMO-EXECUTAR-UMA-SPRINT`, e ela pega igual quando quem mexe na árvore é você
mesmo.

**2 · O PORTÃO DAS CITAÇÕES DE LINHA PEGA TODA MUDANÇA QUE ACRESCENTA LINHA.**
`test_portao_o_par_com_metade_ligada.py::TestTodaCitacaoDeLinhaConfere` estava
**verde na base** e ficou vermelho com oito endereços — nenhum deles um defeito
de lógica, todos deslocamento. Ele não está no `portoes.sh`: só aparece na
camada `suite`, que é de quem coordena e roda no fim. **Quem acrescentar código
a `profiles/manager.py`, `daemon/subsystems/hotkey.py` ou
`core/acoes_de_botao.py` nesta leva vai encontrá-lo**, e o caminho é o que ele
mesmo manda: medir com `grep -n` e reescrever, ou declarar em
`_CITACOES_PENDENTES` quando o arquivo citante é de outra posse.
