# ONDA5-05-03 · a confirmação sai do cartão e vai para a faixa

**Agente B0503 · 06/09/2026 · árvore `hefesto-voo/ONDA5-05-03-B0503`, branch
`voo/ONDA5-05-03-B0503`, nascida de `onda/atual-0609` em `eb7b844c`.**

A decisão que manda é a **05-Q4 dela, 05/09/2026**, e ela é literal:

> **"Linha embaixo da grade"** — *"a frase entra na faixa que já existe sob a
> grade, nomeando a coluna (`P2 · voltou ao ajuste geral`) e some logo depois;
> **nada se mexe dentro das colunas**."*

**A PROVA DE PARTIDA**, com o `PYTHONPATH` do `.envrc-voo` e o python da raiz:

```
import hefesto_dualsense4unix →
  /mnt/Apate/Desenvolvimento/hefesto-voo/ONDA5-05-03-B0503/src/hefesto_dualsense4unix/__init__.py
```

---

## O que mudou

**Dois arquivos de produto, três de régua, e a bancada.** `hefesto_vivo.py`,
`monta.py`, `onde.py`, `paginas/`, `app/telas/vibracao.py`,
`app/actions/rumble_actions.py` e `docs/data/paridade-gtk-html.csv` **não foram
tocados** — são `nao_toca` no frontmatter, e o que precisava deles está no
relato abaixo.

### `interface/pacotes/a05_vibracao.py`

**Passo 1 — a frase ganha o nome da coluna.** Nasceu `_na_faixa(ctx, uniq,
frase)`, e ela é de **um lugar só**: os três ramos de `_aplicar_a_forca` passam
por ela, de modo que um quarto ramo não nasce sem prefixo. O número **não se
digita** — é o `jogador` do item de `ctx.mesa`, o MESMO que a coluna já mostra
no rótulo (`aba05`, `P{c["jogador"]}`); contar a posição na lista daria um
segundo número para a mesma coluna, e os dois divergem no dia em que um controle
cair. A grafia do `uniq` não se assume: a comparação é pelas duas, como
`_forca_da_coluna`. **Sem coluna, sem prefixo** — `P?` seria a tela afirmando um
endereço que ela não sabe.

**Passo 2 — as três frases encolheram para caber numa linha.** Elas foram
escritas para o CARTÃO, que é uma caixa que CRESCE; a faixa reserva UMA linha e
corta o resto. Cada uma perdeu o MECANISMO e ficou com o FATO e o CONSERTO:

| frase | antes | depois (com `P4 ·` e o degrau mais longo) |
| --- | --- | --- |
| `FRASE_DA_MESA_EM_AUTO` | 331 | **134** |
| `FRASE_DO_QUE_A_COLUNA_MOSTRA` | 267 | **162** |
| `FRASE_DO_AJUSTE_GERAL` (nova) | 201 | **66** |
| `_ressalva_da_mesa` (a linha que FICA) | 148 | 148 |

O mecanismo do `Auto` **mudou de casa e não morreu**: está no `?` do rótulo
"Força da vibração", que é onde há espaço para ele — e a dica cresceu de 190
para **262 px de altura com sangria ZERO nos quatro lados** (medido abaixo).

**A palavra "mesa" saiu das linhas da faixa** — decisão dela, 06/09/2026
(`docs/A-LINGUA-DESTA-CASA`). Na tela é **força geral**; na casa continua sendo
a mesa (`ctx.mesa`). Saiu das três frases novas, da `_ressalva_da_mesa` (que
mora na MESMA faixa: duas linhas coladas, uma dizendo "força da mesa" e a outra
"força geral", seriam dois nomes para o mesmo botão) e do recurso de
`_nome_do_degrau`.

**Passo 3 — o quarto tom.** `TOM_DO_RECIBO = "recibo"`, ao lado dos três de
`app/telas/vibracao` (`DIZ`/`ALERTA`/`INFO`). Ele mora no pacote e não lá porque
é o único tom que a janela estável não tem — lá o recibo é um toast, não uma
linha de card.

**`TETO_DA_LINHA_DA_FAIXA = 180`, e ele é MEDIDO três vezes.** Quantas letras
cabem depende de QUAIS letras são, e a régua não podia guardar um número de um
vocabulário só. No Chrome a 1920x1080, na bancada, com a receita do
`interface/olhar.py` (o `.est` mede 1104 px numa janela de 1180 e uma linha tem
17 px):

```
palavras longas da aba ......... 181  ← o último que cabe
as palavras da própria faixa ... 189
palavras muito curtas .......... 187
```

O teto guardado fica **abaixo do menor**, e o caractere de folga é o que absorve
um `P10` no lugar de um `P2`.

### `interface/aba05.py`

* **Passo 3** — `.vib-estado .est.recibo{color:var(--green)}` e o par do
  `.sinal`, ao lado dos três que já existiam. Verde é a cor que esta casa usa
  para o que deu certo nas dez abas, e `hefesto_vivo.COR_DO_SUCESSO` lê o
  **mesmo** `--green`.
* **Passo 4, a metade que É desta posse** — o `#vib-estado` passou a declarar
  **dois atributos**: `data-hef-recados="sucesso"` (QUAL tom pousa aqui) e
  `data-hef-recado-classe="est recibo"` (COM QUE classes). O par é o mesmo do
  alvo `classe` do piloto (`data-hef-classe`/`data-hef-quando`), e ele é o que
  faz o piloto não precisar conhecer a palavra `recibo` nem o desenho da linha
  de estado. **O tom viaja no atributo porque só o SUCESSO muda de lugar** — a
  recusa continua no cartão, laranja, por 30 s.
* **a régua 17 do gerador** guarda as duas metades juntas: sem o endereço, o CSS
  veste um elemento que a página não recebe (dado morto); sem o CSS, o recado
  pousa sem tom no meio das outras linhas.
* o mecanismo do `Auto` entrou no `?` do "Força da vibração".

### As réguas

`tests/unit/test_a_aba_05_vibracao_fecha_as_linhas.py` — **27 casos, 7 novos.**

**A que existia foi REESCRITA, não apagada.**
`test_o_aviso_do_que_a_coluna_mostra_e_sucesso_e_nao_recusa` fazia
`inspect.getsource(_aplicar_a_forca)` e procurava as strings
`'return {"recado":'` e `"raise RuntimeError"`: **a régua mediu o FONTE**, e não o
que o gesto devolve — a forma de defeito que esta casa pagou onze vezes em
26/08, e que reprovaria esta sprint sem que nada na tela tivesse piorado. Agora
ela CHAMA o gesto, com um disco dublê que devolve um `Profile` de verdade, e
olha o que volta.

As sete novas: `…nomeia_a_coluna` · `…cabe_numa_linha` ·
`…diz_o_que_o_produto_diz` · `…e_recibo_e_nao_alerta` ·
`test_o_deu_certo_seco_nao_vira_frase` · `test_a_faixa_declara_que_recebe_o_recado`
· `test_nenhuma_linha_da_faixa_diz_mesa`.

**A `…diz_o_que_o_produto_diz` PERGUNTA AO DONO em vez de digitar a resposta.**
A metade do fato (`FATO_DO_AJUSTE_GERAL`, as palavras dela) tem de continuar
DENTRO de `rumble_actions.TEXTO_A_PECA_VOLTOU_AO_AJUSTE_GERAL` — o dia em que a
janela estável renomear o "ajuste geral", a régua reprova em vez de as duas
telas passarem a dizer coisas diferentes sobre o mesmo fato.

**`test_o_deu_certo_seco_nao_vira_frase` é a que guarda a 03-Q4 dela.** Sem ela,
alguém põe um `"Pronto."` na faixa e *"nenhuma palavra nova entra na tela"*
morre calada.

### DOIS arquivos que não são da minha posse e eu tive de curar

**E a segunda só apareceu quando rodei o BAIRRO INTEIRO, não o meu escopo.**
Três réguas guardavam o mesmo contrato, em três arquivos, e as três exigiam a
oração INTEIRA do produto dentro do recado — 143 caracteres que não cabem na
faixa:

| arquivo | dono | achei em |
| --- | --- | --- |
| `tests/unit/test_a_aba_05_vibracao_fecha_as_linhas.py` | **meu** | o escopo |
| `tests/unit/test_a05_a_vibracao_aplica_e_fala.py` | `ONDA5-05-02`, fechada | o escopo |
| `tests/unit/test_a_forca_da_vibracao_e_por_controle.py` | ninguém desta leva | **o bairro** |

**Sem tocá-las, esta sprint entregaria dois vermelhos.** A mudança é a mesma nas
três e **inverte o contrato em vez de apagá-lo**: onde se exigia a oração
inteira DENTRO da frase, passa-se a exigir que a metade do FATO
(`FATO_DO_AJUSTE_GERAL`) continue DENTRO da oração do produto. As três continuam
importando `rumble_actions`; o que muda é a direção da pergunta.

**A LIÇÃO É DE MÉTODO, e é a terceira vez que esta casa a paga:** *rodar só o
próprio escopo esconde a régua da vizinha que mede a mesma coisa*. O bairro da
aba 05 são **34 arquivos e 482 casos**, e ele fecha em 22 s — barato demais para
não rodar antes de fechar.

---

## Qual mordida prova

**Nove mordidas, uma por vez, com a cura devolvida no fim.** As sete do pacote
(`pytest -k`, saída cortada no essencial):

```
### MORDIDA 1 · o `_na_faixa` arrancado de `_aplicar_a_forca` (Passo 1)
E  AssertionError: a linha da faixa não nomeia a coluna: 'esta coluna vai
   continuar mostrando Máximo: a sua escolha é igual à força geral, …'
FAILED …::test_a_frase_da_faixa_nomeia_a_coluna

### MORDIDA 2 · `FRASE_DA_MESA_EM_AUTO` de volta aos 331 caracteres (Passo 2)
E  AssertionError: FRASE_DA_MESA_EM_AUTO tem 379 caracteres na linha e o teto
   medido é 180 — a faixa corta a segunda linha, e uma explicação que ela não
   consegue ler ocupa o lugar sem informar
E  assert 379 <= 180
FAILED …::test_a_frase_da_faixa_cabe_numa_linha

### MORDIDA 3 · o fato do ajuste geral renomeado (a régua PERGUNTA ao dono)
E  AssertionError: a linha da faixa diz 'voltou ao ajuste padrão' e o produto
   diz ' — e este controle voltou ao ajuste geral: …' — as duas telas deixaram
   de contar o mesmo fato com as mesmas palavras
FAILED …::test_a_frase_da_faixa_diz_o_que_o_produto_diz

### MORDIDA 4 · o `Pronto.` na faixa — a 03-Q4 dela morrendo calada
E  AssertionError: o clique que deu certo e não tem notícia falou na tela:
   {'recado': 'P1 · Pronto.'}. A 03-Q4 dela é a piscada verde, sem palavra nova
FAILED …::test_o_deu_certo_seco_nao_vira_frase

### MORDIDA 5 · a palavra que ela baniu de volta à ressalva
E  'mesa' is contained here:
E    a força da mesa está em auto, e por isso a força própria de 1 controle(s)…
FAILED …::test_nenhuma_linha_da_faixa_diz_mesa

### MORDIDA 6 · o recibo vira RECUSA (o `RuntimeError` de volta)
E  RuntimeError: P1 · esta coluna vai continuar mostrando Máximo: …
FAILED …::test_o_aviso_do_que_a_coluna_mostra_e_sucesso_e_nao_recusa

### MORDIDA 7 · `TOM_DO_RECIBO = ALERTA` — laranja sobre um clique que gravou
E  AssertionError: o recibo passou a usar um tom que já tem dono ('alerta') —
   o `alerta` é laranja, e alerta sobre um clique que gravou ensina que o botão
   falha
FAILED …::test_a_frase_da_faixa_e_recibo_e_nao_alerta
```

As duas do gerador, **e as duas reprovam nos DOIS instrumentos** — o
`aba05.py` na hora de gerar e a régua da suíte depois:

```
### MORDIDA 8 · o `data-hef-recados` arrancado do `#vib-estado` (Passo 4)
ERRO em 05-vibracao — decisão dela desfeita:
  - a faixa deixou de declarar que recebe o recado de sucesso — sem isso ele
    volta a pousar DENTRO da coluna, cobrindo o desenho do controle por 6 s a
    cada clique
rc=1
E  assert 'data-hef-recados="sucesso"' in ' id="vib-estado"\n  data-hef-recado-classe="est recibo"'
FAILED …::test_a_faixa_declara_que_recebe_o_recado

### MORDIDA 9 · o recibo pintado de LARANJA (Passo 3)
ERRO em 05-vibracao — decisão dela desfeita:
  - o tom `recibo` da faixa não é verde — e verde é a cor que esta casa usa
    para o que deu certo em todas as dez abas
  - o recibo da faixa ficou laranja — laranja é o `alerta`, e alerta sobre um
    clique que gravou ensina que o botão falha
rc=1
FAILED …::test_a_frase_da_faixa_e_recibo_e_nao_alerta
```

**A cura devolvida**, e a bancada volta byte a byte (`diff -q` silencioso):

```
27 passed in 0.65s   (a régua desta sprint)
51 passed in 1.42s   (mais a da irmã, `test_a05_a_vibracao_aplica_e_fala.py`)
482 passed in 22.28s (o BAIRRO: os 34 arquivos que citam a aba 05)
05-vibracao: OK, 59 divs · 2 conectado(s) + 2 lugar(es) vazio(s)
mockup/05-vibracao.html IDÊNTICO
```

---

## A PROVA DE TELA — e ela é o coração desta entrega

Janela **OCULTA** sempre (Xvfb `:87`); a tela dela não recebeu nada. Daemon dela
vivo, **um DualSense no cabo**, página da **BANCADA** (publicar é ato dela).

**O que é REAL:** a janela (`WebKit2.WebView`), a página, o daemon, o ouvinte da
página, o gesto `forca` inteiro e o canal de recado do piloto
(`_depositar` → `pintar_recados`).
**O que é DUBLÊ, e está declarado:** o DISCO. `loader.load_profile` devolve um
perfil sintético e `perfil.gravar_e_reaplicar` só registra — o `Personalizado`
**dela** não foi lido nem escrito, e o daemon não recebeu `profile.switch`. O
disco tem régua própria; o que se mede aqui é a TELA.

### 1. O CLIQUE, e a frase colada

Um clique em **Economia** dentro da coluna do P1 (`el.click()`, pelo ouvinte da
página), com o global do perfil em `economia` e o daemon em `balanceado` — o
ramo *"a escolha não diverge"*:

```
mesa de agora: ['p1'] · clique: cliquei economia em p1
gestos que chegaram ao Python: 1
  [desfecho] 05-vibracao.html:forca: aplicou ·
gravações interceptadas (o disco DELA não foi tocado): 0
```

**A frase que nasceu na faixa, com a coluna nomeada:**

```
P1 · esta coluna vai continuar mostrando Balanceado: a sua escolha é igual à
força geral, e só o que difere dela fica guardado no controle.
```

```json
{"lugar": "faixa", "classe": "hef-recado est recibo", "pai": "vib-estado",
 "cor": "rgb(80, 250, 123)", "y": 695, "alt": 16}
```

`rgb(80, 250, 123)` é o `--green` da paleta, e `alt: 16` é **uma linha**.

### 2. NADA SE MEXEU DENTRO DAS COLUNAS — as quatro, nos três momentos

```
                ANTES              DURANTE            DEPOIS
coluna p1  topo=224 alt=452   topo=224 alt=452   topo=224 alt=452
           desenho_topo=224   desenho_topo=224   desenho_topo=224
           rotulo_topo=358    rotulo_topo=358    rotulo_topo=358
           recados_dentro=0   recados_dentro=0   recados_dentro=0
p2, p3, p4 ......................... idem, os três ..................
faixa      alt=17 · 1 linha   alt=35 · 2 linhas  alt=17 · 1 linha
```

**A única coisa que se moveu na tela inteira foi a faixa**, que ganhou a segunda
linha e a devolveu. E as fotos dizem o mesmo, sem eu ter de acreditar na
geometria:

```
9514f7b65e05bd70728175a1de9829af  faixa-antes.png
cda722ebe2ef09176985c6480ab516a7  faixa-durante.png
9514f7b65e05bd70728175a1de9829af  faixa-sumiu.png   ← BYTE A BYTE igual à ANTES
```

### 3. E SOME LOGO DEPOIS

Medido no TEMPO, não no instante: a leitura DEPOIS é
`SEGUNDOS_DO_RECADO_DE_SUCESSO + 1,5 s` após o clique, e ela devolve
`recados: []` com a faixa de volta a uma linha. O relógio **não foi escrito
aqui** — é o `Piloto._depositar`/`_recados_para_a_tela`, um só para as dez abas.

### 4. A MORDIDA DO PASSO 4, nos dois estados

Com o terceiro lugar no ar, o recado nasce dentro do `#vib-estado`. **Sem ele**,
com o mesmo clique e a mesma frase:

```
[recado] {"lugar": "grade", "classe": "hef-recado", "pai": "ctrl",
          "y": 228, "alt": 94}
REPROVA:
  - o recado pousou DENTRO da coluna p1
  - o recado pousou em 'grade', não na faixa
```

**94 px de tarja começando em y=228, sobre um desenho que começa em y=224** — a
foto `mordida-no-cartao.png` mostra o DualSense do P1 com só os dois punhos
aparecendo por baixo do aviso. É exatamente o custo que a 05-Q4 remove, e ele já
estava medido no piloto (o comentário do `ESTILO_NA_GRADE`: *"o aviso que veio
explicar quebrava a tela que estava explicando"*).

### 5. A TELA NÃO SAMBA — 0 mutações

Com a mesa parada e **sem gesto nenhum**, na bancada desta sprint:

```
MUTAÇÕES DE DOM em 40 tiques (4.0 s) na 05-vibracao.html, com a mesa parada
TOTAL: 0 mutações · 0.0 por tique
custo do tique: mediana 1.92 ms · max 12.69 ms · teto 100 ms
```

A faixa só muta quando **há frase**, que é a mutação legítima desta sprint.

### 6. A DICA QUE CRESCEU, medida porque régua nenhuma desta casa mede `.dica`

A `ONDA5-05-01` deixou escrito que **nenhuma régua mede `.dica`** — a
`regua_popup.py` só mede `.tela-nova` —, e foi por isso que 224 px de texto
cortado atravessaram semanas. O mecanismo do `Auto` foi para o `?` do "Força da
vibração", que abre **para cima** (`.vib .rotulos .dica{bottom:-4px}`), então
medi:

```json
{"dica": {"x": 528, "fim": 858, "y": 175, "base": 436, "larg": 330, "alt": 262},
 "janela": {"x": 363, "fim": 1543, "y": 16, "base": 793},
 "sangra_esq": 0, "sangra_dir": 0, "sangra_topo": 0, "sangra_base": 0}
```

**Zero nos quatro lados**, com 159 px de folga acima. Chrome a 1920x1080.

---

## O TERCEIRO LUGAR — o relato do Passo 4, com a forma exata

**`hefesto_vivo.py` não é desta posse**, e o Passo 4 manda relatar. O patch
abaixo foi **aplicado, medido e revertido** (`git status` confirma: o arquivo
não está no diff desta sprint). Ele é de **quatro trechos** em
`pintar_recados`, e o que ele NÃO muda é tanto quanto o que muda: a recusa
continua no cartão, a tarja continua para quem não tem cartão, o depósito, a
poda e a tradução `uniq → pref` são os mesmos.

```diff
       const cartao = onde
         ? document.querySelector('[data-controle="' + onde + '"],[data-uniq="' + onde + '"]')
         : null;
+      // O TERCEIRO LUGAR — A FAIXA QUE A PÁGINA DECLARA. 05-Q4 dela,
+      // 06/09/2026. O endereço é da PÁGINA (`data-hef-recados`), e o tom viaja
+      // nele porque só o SUCESSO muda de lugar: a recusa continua no cartão.
+      const faixa = document.querySelector('[data-hef-recados~="' + tom + '"]');
-      const pai = cartao || document.body;
+      const pai = faixa || cartao || document.body;
@@
         var foraDoFluxo = false;
-        if(cartao){
+        if(!faixa && cartao){
           var disp = getComputedStyle(pai).display;
@@
-        el.dataset.hefLugar = cartao ? (foraDoFluxo ? 'grade' : 'fluxo') : 'tarja';
-        if(cartao){ pai.insertBefore(el, pai.firstChild); } else { pai.appendChild(el); }
+        el.dataset.hefLugar = faixa ? 'faixa'
+          : (cartao ? (foraDoFluxo ? 'grade' : 'fluxo') : 'tarja');
+        if(!faixa && cartao){ pai.insertBefore(el, pai.firstChild); }
+        else { pai.appendChild(el); }
@@
       if(el.dataset.hefTom !== tom){
         const lugar = el.dataset.hefLugar || 'tarja';
-        el.style.cssText = ESTILO_DO_RECADO
-          + (lugar === 'grade' ? ESTILO_NA_GRADE : (lugar === 'tarja' ? ESTILO_DA_TARJA : ''))
-          + (tom === 'sucesso' ? COR_DO_SUCESSO : '');
+        if(lugar === 'faixa'){
+          // NA FAIXA QUEM PINTA É A PÁGINA: ela já tem o desenho da linha de
+          // estado, e o `data-hef-recado-classe` diz com que classes o recado
+          // se veste. Uma caixa com borda do piloto ao lado das linhas da
+          // faixa seriam dois desenhos para a mesma linha.
+          el.className = 'hef-recado ' + (faixa.dataset.hefRecadoClasse || '');
+          el.style.cssText = 'pointer-events:none;';
+        } else {
+          el.style.cssText = ESTILO_DO_RECADO
+            + (lugar === 'grade' ? ESTILO_NA_GRADE : (lugar === 'tarja' ? ESTILO_DA_TARJA : ''))
+            + (tom === 'sucesso' ? COR_DO_SUCESSO : '');
+        }
         el.dataset.hefTom = tom;
         n += 1;
       }
```

**QUATRO COISAS QUE MEDI E QUEM FOR APLICAR PRECISA SABER:**

1. **O `~=` não é enfeite.** `[data-hef-recados~="sucesso"]` casa por PALAVRA,
   de modo que uma página que um dia queira as duas escreve
   `data-hef-recados="sucesso recusa"` sem inventar um segundo atributo.
2. **A ordem no `pintar` já está certa e não precisa mudar.** Os blocos entram
   na linha 1094 e `pintar_recados` roda na 1179 — quando o bloco do
   `#vib-estado` troca o miolo e mata o recado, o mesmo tique o recria, porque o
   `querySelector` volta `null`. Medido: o recado sobreviveu aos 6 s inteiros.
3. **A memória `alvo.__hefBloco` da `A-TELA-SAMBA-01` continua valendo.** Ela
   compara com o que o piloto ESCREVEU, não com o `innerHTML` lido, então um
   filho a mais dentro do bloco não reabre o ciclo de repintura. Medido: 0
   mutações em 40 tiques.
4. **O `.vib-estado:empty{display:none}` faz o que se espera**: com o recado
   dentro, a faixa deixa de estar vazia e aparece mesmo quando não há linha de
   estado — que é a resposta certa, porque HÁ o que dizer.

---

## Para o `mockup/DIVERGENCIAS.md` — o texto pronto (o arquivo não é meu)

Na seção `## 05-vibracao.html`, depois da entrada da `ONDA5-05-01`:

> - **06/09/2026** — **A CONFIRMAÇÃO SAIU DO CARTÃO E FOI PARA A FAIXA**
>   (`ONDA5-05-03`, a 05-Q4 dela: *"Linha embaixo da grade (…) nomeando a
>   coluna (`P2 · voltou ao ajuste geral`) e some logo depois; nada se mexe
>   dentro das colunas"*). O `#vib-estado` passou a declarar
>   `data-hef-recados="sucesso"` e `data-hef-recado-classe="est recibo"`, e a
>   folha ganhou o quarto tom, o `recibo`, em `--green`. As três frases do
>   clique encolheram para caber numa linha (331 → 134, 267 → 162) e ganharam o
>   `P{n} ·` na frente; o mecanismo do `Auto` foi para o `?` do "Força da
>   vibração", que passou de 190 para 262 px de altura com sangria zero nos
>   quatro lados.
>
>   **O que ela vê HOJE, até publicar:** a aba Vibração de ontem — o aviso do
>   clique nascendo DENTRO da coluna, 94 px de tarja verde cobrindo o topo do
>   desenho do controle por 6 s. Medido nesta sprint, com foto.
>
>   **E a metade do piloto ainda não está no ar:** `pintar_recados` conhece dois
>   lugares (cartão e tarja) e o terceiro está relatado, com o diff exato, em
>   `docs/process/agentes/2026-09-06/ONDA5-05-03.md`. Enquanto ele não entrar, a
>   regra `.est.recibo` da folha veste um elemento que a página não recebe.
>
>   **O que fecha:** o `--publicar 05` depois do OK dela na aba inteira, **e** o
>   terceiro lugar no piloto.

---

## O que NÃO verifiquei

* **O ramo do `Auto` NA TELA — e ele não é alcançável pelo clique dela.**
  Medido: o botão `Auto` saiu em 05/09 (`aba05.FORCA` tem três, e
  `RUMBLE_POLICY_MULT` também tem três), então `policy == "auto"` não chega a
  `_aplicar_a_forca` pelo gesto `forca`. A sprint pedia os três ramos clicados;
  **cliquei o que existe** (o ramo *"não diverge"*), e o do `Auto` tem régua de
  unidade (a da irmã, `test_o_auto_diz_que_a_coluna_volta_ao_ajuste_geral`) mais
  a `…diz_o_que_o_produto_diz` desta. O ramo FICA, pela mesma razão do `raise`
  de `_indice`: um perfil antigo ou a janela estável ainda produzem o caso, e
  devolver a coluna ao ajuste geral calado é o silêncio que a função cura.
  Está escrito no docstring, com a data.
* **O terceiro ramo (`FRASE_DA_MESA_EM_AUTO`) na TELA.** Ele precisa do perfil
  ativo com `rumble.policy = auto`, e eu não ia pôr isso no perfil dela. Tem
  régua de unidade nas duas irmãs.
* **O rato de verdade.** O WebKitGTK não sintetiza ponteiro por
  `run_javascript`; o clique deste ensaio é `el.click()`, e não passa pelo par
  `mousedown`/`click` que a `A-TELA-SAMBA-01` mede.
* **A aba publicada.** Esta sprint **não publica**. Tudo o que está medido aqui
  vale para `mockup/05-vibracao.html`.
* **A suíte inteira.** Rodei o meu escopo e o da irmã (51 casos). A suíte é de
  quem coordena.
* **A bancada física.** `scripts/bancada.sh status` disse **LIVRE** e eu **não
  reservei**: não parei o daemon, não escrevi no aparelho, não toquei em
  `systemctl` e o gesto `forca` não fala com o motor. O piloto só LEU o estado.
* **Não rodei `retratar_abas.py`** — as onze fotos são de quem integra.

---

## O que sobrou para o próximo

1. **O TERCEIRO LUGAR NO PILOTO** — o diff acima, aplicado e medido. É a metade
   que falta para a 05-Q4 estar na tela dela. **Enquanto ele não entrar, a regra
   `.vib-estado .est.recibo` da folha é CSS para um elemento que a página não
   recebe.** As duas metades nascem juntas ou a primeira a chegar fica sem par —
   está escrito no comentário do CSS, com o ponteiro para este relatório.
2. **`FRASE_DO_CONTROLE_QUE_SAIU` ainda diz "mesa" DUAS vezes**
   (`a05_vibracao.py`, o texto da recusa da irmã): *"este controle saiu da
   **mesa** entre o clique e agora… faria a **mesa** inteira tremer"*. E
   `_mirar` diz *"iria para a mesa inteira"*. Não toquei: é a RECUSA (cartão),
   não a faixa, e é texto que a `ONDA5-05-02` acabou de pousar com régua
   própria no arquivo dela. **A varredura da palavra banida na tela precisa de
   um dono só** — hoje ela não tem portão, só o glossário.
3. **Nenhuma régua desta casa mede `.dica`, e continua assim.** Medi a minha à
   mão (§6 da prova de tela), como a irmã mediu a dela. A `regua_popup.py` só
   olha `.tela-nova`; a segunda dica desta aba (`aba02.py:1920`, o `?` do
   Giroscópio com `left:auto;right:22px`) continua **não medida**, como a
   `ONDA5-05-01` deixou escrito.
4. **`docs/data/paridade-gtk-html.csv` envelheceu mais um degrau, e é posse de
   outro.** A linha do aviso do clique aponta o cartão como canal desta aba.
   Depois da 05-Q4 o canal desta aba é a faixa. O portão não fica vermelho — o
   sinal continua presente —, mas a prosa mente. É relato, não edição.
