# ONDA5-08-02 · O rodapé do mapa tem dois donos, e um deles era a aba

**Sprint:** `docs/process/sprints/2026-09-05-ONDA5-08-02-o-rodape-do-mapa-tem-dois-donos.md`
**Árvore:** `hefesto-voo/ONDA5-08-02-B0802` · branch `voo/ONDA5-08-02-B0802` (de
`onda/atual-0609`, `eb7b844c` — a costura da ONDA A)
**Posse:** `app/widgets/mapa_da_mesa.py` · `interface/aba08.py` ·
`mockup/08-conexoes.html` · `interface/paginas/08-conexoes.html` ·
`tests/unit/test_a_aba_08_conexoes_fecha_as_linhas.py`
**Decisão:** **08-Q8** — *"Trocar pela verdade"*. **Ela já estava na tela**; o
que esta sprint fechou é a DÍVIDA que a entrega de 04/09 declarou no próprio
arquivo.
**Bancada:** LIVRE o tempo todo, e **não precisei dela**. Nenhuma escrita no
aparelho, nenhum `systemctl`, nenhum pareamento tocado, o daemon nunca parado.
`scripts/bancada.sh` não foi reservado. Os dois DualSense dela continuam no cabo.

**Portões:** `bash scripts/portoes.sh` → **43 VERDES**. No primeiro giro havia
UM vermelho, e **ele era meu**: `referencias-docs` acusou uma referência morta —
a nota de fecho da sprint citava **este relatório antes de ele existir**, o
mesmo tropeço que a `ONDA5-08-01` registrou ontem. Escrito o arquivo, o portão
fecha:

```
$ python scripts/validar-referencias-docs.py --all
OK: 851 documento(s) sem referência morta.
```

Os dois que a `ONDA5-08-01` deixou herdados da base (`referencias-docs` com 7
mortas e `acentuacao` com 7 violações) **já estavam curados** em `eb7b844c`.

**Testes do escopo:** os **38 arquivos** que citam `mapa_da_mesa`, `aba08` ou
`08-conexoes`, mais `test_a_tela_nao_samba.py` — **576 passed, 7 skipped, 0
failed**. O meu arquivo sozinho fecha **32** (eram 31; **nasceu 1**). **Não
rodei a suíte inteira** — é de quem coordena.

---

## O QUE MUDOU — os três passos da sprint, e nada além

### Passo 1 — o dono ganhou a frase irmã

`app/widgets/mapa_da_mesa.py`, ao lado de `ESPERA_O_APLICAR`:

```python
GRAVA_NO_CLIQUE = (
    "Cada mudança aqui já foi gravada, no clique. Não há nada a aplicar depois."
)
```

**`ESPERA_O_APLICAR` não saiu e não ganhou nota de caducidade** — ela continua
verdadeira para o `Gtk.Label` daquela janela, que espera mesmo o "Aplicar". O
comentário acima das duas passou a explicar por que são DUAS: são dois
comportamentos, e a decisão dela de 01/09 (*"clicar já aplica"*) é o que os
reparte. **Unificá-las poria a mentira numa das duas.**

### Passo 2 — a aba lê, em vez de digitar

* `"GRAVA_NO_CLIQUE"` entrou no conjunto do `MAPA = _constantes(...)`
  (`aba08.py`), ao lado dos outros treze;
* `MAPA_JA_GRAVOU` **deixou de existir**. O bloco virou o comentário que guarda
  a razão da repartição, com a data — decisão medida fica;
* a linha do rodapé passou a escrever `{MAPA["GRAVA_NO_CLIQUE"]}`, como as
  quatro linhas de botão acima dela.

### Passo 3 — as afirmações que caducaram saíram

* o docstring de `pergunta_da_sala` dizia que a frase na tela *"três linhas
  abaixo"* era `ESPERA_O_APLICAR`. **Era `MAPA_JA_GRAVOU`**, e agora é
  `GRAVA_NO_CLIQUE`. Substituído, com a razão de não ser a outra;
* o ponteiro para `mapa_da_mesa.py:559` saiu junto com o bloco que o continha.

---

## O EFEITO COLATERAL QUE A PRÓPRIA SPRINT PREVIU — cinco citações deslocadas

A §1.3 da sprint mede o custo do ponteiro de linha entre dois arquivos: `:559`
virou `:566` **em um dia**. A frase irmã nasce na linha 135 de `mapa_da_mesa.py`
e **empurrou 27 linhas para baixo** as OUTRAS CINCO citações de linha que
`aba08.py` fazia àquele arquivo:

| citação antiga | o que ela aponta | virou |
| --- | --- | --- |
| `mapa_da_mesa.py:434-442` | o rótulo `espécie · caminho` | `mapa_da_mesa.rotulo_do_aparelho` |
| `mapa_da_mesa.py:515-523` | a lista à esquerda, as faces à direita | `mapa_da_mesa._Janela.__init__` |
| `mapa_da_mesa.py:645` | o nome da face é `Gtk.Label`, não campo | `mapa_da_mesa._Janela._desenhar_faces` |
| `mapa_da_mesa.py:667-675` | as oito entradas da traseira | `mapa_da_mesa._Janela._desenhar_uma_face` |
| `mapa_da_mesa.py:691-697` | a entrada por extensão é DECLARAÇÃO dela | `mapa_da_mesa._Janela._quadrado` |

**As cinco ficariam erradas no mesmo commit em que a sprint mede esse custo.**
Elas passaram a citar o **símbolo**, que não desloca — e o número saiu, porque
guardá-lo é assinar a próxima podridão. **O portão `citacoes-de-linha` não
alcança comentário de `.py`**, então ninguém teria avisado: continua sendo
buraco de régua, e este relatório é o aviso.

Duas dessas cinco moram em comentário de CSS e por isso viajam para o HTML
gerado: **são as duas únicas linhas do diff de `mockup/08-conexoes.html`.**

---

## AS TRÊS MORDIDAS, com a saída colada

### MORDIDA 1 · Passo 1 — arranca `GRAVA_NO_CLIQUE` do dono

A régua é a GERAÇÃO, e ela já existia (`aba08._constantes`, `SystemExit`):

```
$ python src/hefesto_dualsense4unix/interface/aba08.py
ERRO: …/app/widgets/mapa_da_mesa.py não tem mais ['GRAVA_NO_CLIQUE'] —
      a tela dependia deles.
rc=1
```

### MORDIDA 2 · Passo 2 — a aba volta a DIGITAR, e o dono corrige a frase

É a mordida que a sprint escreveu: *"troque a frase no dono e regere sem tocar
em mais nada. O HTML gerado tem de mudar."* Com a cura arrancada, ele não muda:

```
E   AssertionError: o rodapé da página da bancada não é o do dono:
E       página: 'Cada mudança aqui já foi gravada, no clique. Não há nada a aplicar depois.'
E       dono:   'Cada mudança aqui já foi gravada, no clique.'
FAILED tests/unit/…::test_o_rodape_do_mapa_vem_do_dono
```

### MORDIDA 3 · a anti-cópia — só a aba volta a digitar, o dono INTACTO

A página sai **byte a byte igual** à da cura. Nenhuma régua de tela veria isto:

```
E   AssertionError: `aba08.py` voltou a DIGITAR a frase do rodapé — ela tem
    dono, e o gerador a lê por `MAPA["GRAVA_NO_CLIQUE"]`
```

```
A CURA DEVOLVIDA: 32 passed
```

### A MORDIDA 2 DERRUBOU UMA RÉGUA QUE JÁ EXISTIA, e a lição é sobre o `in`

`test_o_rodape_do_mapa_diz_que_o_clique_ja_gravou` cobra `frase in html`. Na
mordida 2 a frase do dono virou **prefixo** da que a página trazia, e o `in`
casou: **a régua velha PASSOU sobre o defeito**. É por isso que a régua nova
compara por **igualdade**, contra o `textContent` do nó `.mm-aplicar`, e não com
um `in` sobre o arquivo inteiro.

*Um `in` frouxo passa em toda mudança que só ACRESCENTA* — e "acrescentar ao
fim" é exatamente a forma de um texto que se corrige.

---

## A PROVA DE TELA — e o que ela prova aqui é que NADA mudou

### A foto, `--oculta`, antes e depois

O retrato é da **janelinha do mapa aberta**, recortada em `#mapear-entradas
.tn-cx`, num Chrome headless (não há janela na tela dela):

```
ANTES   janelinha 660x717 · rodapé 622x18 px
        'Cada mudança aqui já foi gravada, no clique. Não há nada a aplicar depois.'
DEPOIS  janelinha 660x717 · rodapé 622x18 px
        'Cada mudança aqui já foi gravada, no clique. Não há nada a aplicar depois.'

md5  092fc0ddba818d0c33b865f6cf13db99   mapa-ANTES.png
md5  092fc0ddba818d0c33b865f6cf13db99   mapa-DEPOIS.png
```

**Zero byte de diferença.** Uma sprint de dono que mexe no que ela vê errou de
passo — e esta não errou.

### O CLIQUE, no motor que ela usa: WebKitGTK, `Gtk.OffscreenWindow`

Um driver próprio (`clique_no_mapa.py`, no scratchpad) monta a `PonteDaTela`
numa janela **oculta**, carrega a aba 08, **clica** em «Mapear Entradas» e lê o
DOM vivo antes e depois — nas DUAS páginas:

```
=== BANCADA ===
  ANTES  janelinha aberta=False (display=none)   rodapé 0x0 px
>>> CLIQUE em «Mapear Entradas» (a[href="#mapear-entradas"])
  DEPOIS janelinha aberta=True  (display=flex)   rodapé 622x17 px · 11.5px · rgb(154, 158, 184)
         texto: 'Cada mudança aqui já foi gravada, no clique. Não há nada a aplicar depois.'

=== PUBLICADA ===
  DEPOIS janelinha aberta=True  (display=flex)   rodapé 622x17 px
         texto: 'Cada mudança aqui já foi gravada, no clique. Não há nada a aplicar depois.'

=== O VEREDITO ===
  BANCADA    aberta=True  texto == mapa_da_mesa.GRAVA_NO_CLIQUE: True
  PUBLICADA  aberta=True  texto == mapa_da_mesa.GRAVA_NO_CLIQUE: True
```

O driver também recusa se `ESPERA_O_APLICAR` vazar para esta tela — **não
vazou**. E `HOME` mais os quatro `XDG_*` foram desviados para um lar de mentira:
**nenhum byte foi ao `~/.config` dela.**

### A TELA NÃO SAMBA — a aba 08 continua em ZERO

```
$ hefesto_vivo.py --oculta --abre 08 --conta-mutacoes 40

MUTAÇÕES DE DOM em 40 tiques (4.0 s) na 08-conexoes.html, com a mesa parada
endereço                           tipo           o quê                       n    nós
TOTAL: 0 mutações · 0.0 por tique
custo do tique: mediana 4,55 ms · max 39,42 ms · teto 100 ms
```

A ONDA A tirou a aba 08 de 40 mutações em 40 tiques para zero. **Ela continua
em zero** — esta sprint não acrescentou alvo nenhum, e o rodapé não é pintado.

---

## O QUE EU **NÃO** FIZ

* **Não publiquei.** `interface/paginas/08-conexoes.html` está **intocado**
  (`git diff` vazio nele): publicar é ato dela, e a costura da leva faz numa
  volta só. **E aqui não faria diferença de pixel:** a linha publicada já é
  byte a byte a do dono, o que o driver acima mediu.
* **Não rodei a suíte inteira** — só o meu escopo. É de quem coordena.
* **Não toquei `mockup/DIVERGENCIAS.md`** (posse do coordenador). O texto está
  na seção seguinte.
* **Não curei `integrations/ordens_da_mesa.py`** — não é minha posse.
* **Não medi o tempo longo.** O driver do clique roda ~10 s; uma regressão que
  só aparecesse aos 181 segundos passaria. O `--conta-mutacoes` cobre 40 tiques.

---

## O QUE SOBROU PARA O PRÓXIMO

### 1. `integrations/ordens_da_mesa.py` — RELATADO DE NOVO, e é da T-10

A `ONDA5-08-01` mediu e não pôde curar; **eu também não posso** — o arquivo não
está na minha `posse` e não está no meu `nao_toca` só porque ninguém o listou.
O defeito, sem uma linha de mudança desde ontem:

```python
# ordens_da_mesa.py:846-850   ordens_novas
        if dispensadas.get(ordem.chave) != ordem.arranjo
# ordens_da_mesa.py:861-865   ordens_caladas
        if dispensadas.get(ordem.chave) == ordem.arranjo
```

Com o desfazer da 08-01 gravando `arranjo=""`, uma `Ordem` **viva** que chegue
com `arranjo` vazio (o padrão do campo, `ordens_da_mesa.py:258`) casa com o
vazio guardado pelo desfazer e **nasce calada** para os dois consumidores
daquele módulo — **inclusive a janela GTK**. A correção é a mesma linha que o
pacote da aba já usa: `and ordem.arranjo`.

**Candidata seguinte: `CONEXOES-LIGAR-TUDO-01` (T-10, onda C).** E a régua tem
de MORDER dos dois lados, como a da 08-01: uma ordem viva sem assinatura e uma
ordem realmente dispensada.

### 2. `mockup/DIVERGENCIAS.md` — DUAS LINHAS a somar na seção `08-conexoes.html`

A seção já existe (escrita pela costura da ONDA A). O que esta sprint acrescenta
é **prosa de folha de estilo, zero pixel** — mas o diff existe e não se declara
por omissão:

```markdown
- **06/09/2026** — a 08-Q8 (`ONDA5-08-02`). **Nenhum pixel muda**: o rodapé da
  janelinha do Mapa passou a ser LIDO do dono (`mapa_da_mesa.GRAVA_NO_CLIQUE`)
  em vez de digitado no gerador, e o texto é byte a byte o mesmo — as duas
  fotos da janelinha têm o mesmo md5. O diff do HTML são **duas linhas de
  comentário de CSS**, que trocaram um ponteiro de linha por um de símbolo.

  **O que ela vê HOJE, enquanto não publicar:** exatamente a mesma linha, com
  o mesmo tamanho e a mesma cor — medido no WebKit, nas duas páginas.

  **O que fecha:** o mesmo `--publicar 08` da linha acima.
```

### 3. Para quem mexer em `aba08.py` depois de mim

* `MAPA_JA_GRAVOU` **não existe mais**. Se você precisar da frase, ela é
  `MAPA["GRAVA_NO_CLIQUE"]`, e o `_constantes` derruba a geração se o nome
  sumir do produto;
* **não digite texto de tela no gerador.** A régua nova
  (`test_o_rodape_do_mapa_vem_do_dono`) lê o fonte de `aba08.py` e reprova a
  segunda cópia desta frase — e a mordida 3 mostra que a página fica idêntica,
  isto é, **nenhuma régua de tela veria**;
* **os cinco ponteiros para `mapa_da_mesa.py` agora citam símbolo.** Se você
  acrescentar linha àquele arquivo, eles continuam certos. Não os converta de
  volta para número.

### 4. `gui/aba_conexoes.py` — nenhum ponto novo

Não mexi nele e não achei ponto de leitura novo além dos que o inventário da
`GTK-1` já persegue. **`mapa_da_mesa.py` é widget e FICA** — a D-19 tirou o
lançador, não o módulo, e a frase nova mora lá justamente porque aquele arquivo
é o dono do texto do mapa nas DUAS telas.

---

## Anonimato

**Nenhum endereço de rádio, serial ou identificador de aparelho foi escrito em
arquivo versionado por esta frente.** O que entrou é uma frase de tela em
português e cinco nomes de função. As palavras da tela seguem o glossário: a
aba fala **rádio** e **cabo**; `MAC` e `uniq` não aparecem em texto nenhum, e
**"mesa" não entrou na tela** — as duas ocorrências no código-fonte são o nome
do módulo `mapa_da_mesa`, que é endereço de arquivo e não palavra de tela.
