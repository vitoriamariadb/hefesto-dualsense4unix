# agente a38001a6166621467 (sessao 6eac9113)

**tipo:** Explore
**tarefa:** Read Steam Input/override sprints
**profundidade:** 2  **pai:** a48ea78b0b42a9356

## O QUE FOI PEDIDO

Repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix. Leia INTEGRALMENTE estes documentos (todos sob /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/):

1. 2026-07-25-AUTO-01-um-clique-em-vez-de-dez.md
2. 2026-07-26-STEAM-INPUT-01-ela-nunca-mais-precisa-decidir.md
3. 2026-07-26-DUPLO-REGISTRO-01-o-steam-input-tem-dois-cadastros.md
4. 2026-07-26-BOTAO-QUE-NAO-MENTE-01-clico-e-nao-acontece-nada.md
5. 2026-08-03-AUDIO-QUE-TRANCA-01-um-toque-no-volume-congela-a-troca-de-perfil.md
6. 2026-08-03-POSSE-POR-CONTROLE-01-a-trava-de-um-controle-congela-os-quatro.md
7. 2026-08-05-TRAVA-QUE-SOLTA-TARDE-01-o-gesto-explicito-e-vitima-da-propria-trava.md

Para CADA um, extraia com precisão:
- estado declarado (PROPOSTA / ENTREGUE / CURADA / CADUCADA / ABERTA / parcial) e a data;
- o defeito que ela cura, em uma frase;
- **a lista de ENTREGAS (E1, E2, ...) com o SÍMBOLO/mecanismo concreto prometido** (função, constante, campo, arquivo:linha citados). O mais importante: preciso da lista de símbolos prometidos para conferir por grep se têm chamador em produção;
- qualquer nota datada de caducidade/refutação;
- o que a sprint diz explicitamente para NÃO fazer;
- **CRÍTICO — `manual_override_categories`**: as sprints AUDIO-QUE-TRANCA-01, POSSE-POR-CONTROLE-01 e TRAVA-QUE-SOLTA-TARDE-01 todas mexem nesse campo. Extraia EXATAMENTE o que cada uma propõe fazer com ele (estrutura de dados nova? escopo por controle? TTL? quem limpa?), e se elas se contradizem entre si. Diga qual cura é mais recente e qual supõe a outra já aplicada;
- toda menção a: Steam Input, decisão por jogo, steam_input, troca de perfil durante a partida, congelamento de perfil, autoswitch.

NÃO altere nenhum arquivo. Devolva relato estruturado por sprint, denso. Cite caminho absoluto e linha.

## RESULTADO FINAL

Li os sete documentos integralmente e verifiquei os símbolos contra a árvore atual. Relato abaixo.

---

# 1. AUTO-01 — "um clique em vez de dez"
`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-25-AUTO-01-um-clique-em-vez-de-dez.md`

**Estado declarado (:3-5):** `ABERTA`, prioridade ALTA, aberta em 25/07/2026.
**CONTRADIÇÃO DE ESTADO:** o índice da mesma leva marca `AUTO-01 | alta | **ENTREGUE** 8fe735d` (`/mnt/.../sprints/2026-07-25-INDICE-leva-quatro-controles.md:99`), mas `PEDIDOS-DELA-01` de 03/08 ainda a trata como *"(25/07, ABERTA)"* e acrescenta um item novo **AUTO-01.2-b (03/08)** (`/mnt/.../sprints/2026-08-03-PEDIDOS-DELA-01-o-roteiro-dos-seis-pedidos-da-interface.md:19,30-31`). Também aparece como pendente em `/mnt/.../sprints/2026-08-03-ONDAS-a-ordem-de-execucao-da-leva-do-bluetooth.md:150` (item 7.3, "co-op sempre ativo", dependente da 6.4/E1). Ou seja: entregue em parte, reaberta por pedido novo.

**Defeito que cura (uma frase):** a usuária tem de fazer à mão o que o produto deveria encadear — o instalador fecha/reabre a Steam duas vezes e falha no terceiro passo por causa de si mesmo, e o co-op de quatro jogadores só existe pelo terminal.

**Entregas — esta sprint NÃO usa rótulos E1/E2; usa AUTO-01.1..7 + tabela de defaults + ordem de execução:**

| item | símbolo / mecanismo concreto prometido | citação |
|---|---|---|
| AUTO-01.1 | ligar emulação quando houver ≥2 controles físicos; hoje `daemon/lifecycle.py:132` a define desligada e `daemon/subsystems/coop.py:186` só ativa co-op com ela de pé; `coop_enabled=True` é decorativo | `:39-48` |
| AUTO-01.2 | botão **"Preparar co-op (N jogadores)"** na aba Início; `grep -ci coop gui/main.glade` → 0; cita `utils/session.py:446` | `:50-61` |
| AUTO-01.3 | prioridade do preset `coop_local` de 45 → **≥75** ("uma linha") | `:63-75` |
| AUTO-01.4 | dar **modo de linha de comando** a `integrations/steam_launch_options.py:466` (a função que aplica o wrapper a todos os jogos) e chamá-la no instalador | `:77-83` |
| AUTO-01.5 | um dono só para a máscara padrão: `lifecycle.py:140` (`dualsense`) vs `integrations/uinput_gamepad.py:136` (`xbox`) | `:85-90` |
| AUTO-01.6 | fazer a máscara da aba Início persistir; hoje `app/draft_config.py:476` reemite o modo fotografado e só o editor de Perfis grava a seção | `:92-96` |
| AUTO-01.7 | copiar as 3 linhas de parâmetros a quente de `scripts/install-host-udev.sh:306` para o `install.sh`; separar `--no-dkms` em opções por módulo | `:98-107` |
| defaults | vibração `balanceado`(×0,7)→`máximo`/`auto`; presets de gênero ganham seção de modo `gamepad`; **um** catch-all em vez de dois; `--help` inteiro | `:109-116` |

**Símbolos para grep:** `coop_enabled`, `coop_local`, `steam_launch_options.py:466` (função de aplicar wrapper a todos os jogos), `draft_config.py:476`, `uinput_gamepad.py:136`, `lifecycle.py:132`/`:140`, `session.py:446`, `install-host-udev.sh:306`, `--no-dkms`, `proton_pin --lock`.

**Nota de caducidade/refutação:** nenhuma dentro do documento. A caducidade é externa e é a divergência de estado acima.
**O que diz para NÃO fazer:** não há bloco explícito de vetos. Os "não" implícitos: não deixar dois catch-all semeados (`:115`), e o critério de aceite (`:126-131`) proíbe o resultado — *"sem abrir terminal, sem editar arquivo, sem reiniciar nada"* e o instalador termina **"sem pedir nada — nem um comando para copiar"**.

**Steam Input / decisão por jogo / autoswitch:** só menção indireta — passo 11 do instalador *"edita Steam Input"* (`:24`) e o desdobramento AUTO-02 (Steam) (`:15`). Não fala de `steam_input`, congelamento de perfil nem troca durante a partida.

---

# 2. STEAM-INPUT-01 — "ela nunca mais precisa decidir"
`/mnt/.../sprints/2026-07-26-STEAM-INPUT-01-ela-nunca-mais-precisa-decidir.md`

**Estado (:3-6):** `ABERTA`, ALTA, aberta em 26/07/2026 junto com o rollback da leva da madrugada. Confirmada ABERTA em três índices posteriores (`2026-07-26-INDICE-a-fila-do-jogador.md:97`; `2026-07-29-INDICE-o-que-falta-depois-da-v030.md:45`; `2026-07-30-INDICE-as-tres-faixas-depois-da-v040.md:191`).

**Defeito que cura:** a regra do Steam Input existe e funciona no código, mas a tela ensina o contrário em quatro pontos medidos — a usuária é obrigada a tomar uma decisão técnica ("ligo a entrada Steam?") que o produto deveria tomar por ela.

**A regra implementada, com endereços (:29-50):** padrão desligado por `install.sh:2300` → `scripts/disable_steam_input.sh --apply` (opt-out `--keep-steam-input`); vigia `hefesto-steam-input-guard.path` + `.timer` (30 min) por `install.sh:2318`/`:2204`; global sempre zerado em `disable_steam_input.sh:267-268` (`SteamController_PSSupport`, `SteamController_SwitchSupport`), per-app poupado se estiver na allowlist (`:269-272`, allowlist lida em `:226`); escritor único `add_appid_to_steam_input_allowlist` (`integrations/steam_launch_options.py:765`) chamado só de `app/actions/daemon_actions.py:994` dentro de `on_steam_game_broken` (`:970`); saída de cena por `daemon/launch_env.py` + `daemon/subsystems/gamepad.py:231` (`sync_steam_input_exception`) e `:405` (`suspend_vpads_for_steam_input`).

**Entregas (numeradas 1..9, :170-210) — símbolo/mecanismo prometido:**

| # | símbolo/mecanismo | linha |
|---|---|---|
| 1 | **remover** a frase de `app/actions/daemon_actions.py:313-317` (o toast que ensina Steam → Propriedades → Controle → Ativar) | `:174-177` |
| 2 | lista por nome de jogo substituindo `app/actions/emulation_actions.py:762-764` e o WARN de `integrations/storm_doctor.py:148` | `:178-181` |
| 3 | **dar gatilho a `remove_appid_from_steam_input_allowlist`** (`integrations/steam_launch_options.py:821`) ao lado de "Este jogo não funciona"; enquanto não houver, tirar a promessa do tooltip `gui/main.glade:1880` | `:182-186` |
| 4 | divergência `vdf` × allowlist vira **pergunta** com dois botões (manter = entrar na allowlist) | `:187-191` |
| 5 | aviso de co-op no clique de "Este jogo não funciona" com >1 controle | `:192-193` |
| 6 | **dar consumidor GUI a `daemon/subsystems/gamepad.py:310` (`steam_input_vpad_suspenso`)** — hoje "grep em `src/` só acha a definição e os usos internos do próprio módulo" | `:194-199` |
| 7 | consertar o timer (`OnBootSec` vencido sem âncora) e mostrar no cartão que o guarda está vivo | `:200-203` |
| 8 | documentar o guarda em `docs/usage/troubleshooting.md`; criar ou remover a referência a `FEAT-STEAM-INPUT-SELF-HEAL-01.md` (**arquivo não existe**, `:157`) | `:204-207` |
| 9 | corrigir rótulo em `storm_doctor.py:148-149` e `:215` ("Reaplicar fixes seguros" → **"Aplicar correções"**, `gui/main.glade:1904`, id `btn_storm_fix_safe`) e a aba citada em `docs/usage/jogos-e-mascaras.md:43` (aba Emulação → aba **Sistema**) | `:208-210` |

**Símbolos para grep:** `add_appid_to_steam_input_allowlist`, `remove_appid_from_steam_input_allowlist`, `on_steam_game_broken`, `sync_steam_input_exception`, `suspend_vpads_for_steam_input`, `steam_input_vpad_suspenso`, `btn_storm_fix_safe`, `UseSteamControllerConfig`, `SteamController_PSSupport`, `SteamController_SwitchSupport`, `steam_input_apps.txt`, `hefesto-steam-input-guard.timer/.path`, `disable_steam_input.sh --apply/--restore/--keep-steam-input`.

**Notas de caducidade/refutação:**
- **Entrega 7 CADUCOU em 27/07:** `/mnt/.../sprints/2026-07-27-INDICE-a-blindagem.md:146-150` — *"`STEAM-INPUT-01` descreve, na entrega 7, um estado de timer parado. Medido: `hefesto-steam-input-guard.timer` está **active**, disparou há menos de um minuto e tem o próximo agendado — ciclo de 30 minutos."* Reconfirmado em 29/07 (`2026-07-29-INDICE-o-que-falta-depois-da-v030.md:45`, "disparo em 12 minutos").
- **Entrega 3 parcialmente paga, mas no lugar errado:** o mesmo índice de 29/07 e o de 30/07 (`:191`) registram que `remove_appid_from_steam_input_allowlist` **já tem chamador, mas em `cli/cmd_steam.py:215` — terminal, não janela.** Verifiquei: `src/hefesto_dualsense4unix/cli/cmd_steam.py:206,215`. **Zero chamadores em `app/` ou `gui/`.**
- **O item 0 (o portão) continua aberto:** ver "O que NÃO foi medido".

**O que diz para NÃO fazer:** *"Se a resposta desta sprint for uma frase melhor num tooltip, ela terá falhado"* (`:14-16`); *"prometer desfazer sem desfazer é pior do que não prometer"* (`:185-186`) — se não entregar o botão, **tirar** o tooltip; *"Divergência vira pergunta, não repreensão"* (`:187`); *"Em nenhum ponto da interface aparece a instrução de ir na Steam mexer em Propriedades → Controle"* (`:228-229`).

**O que NÃO foi medido (:235-250) — inclui o portão bloqueante:** não abriu jogo nenhum; não viu a tela; não sabe quem ligou o Steam Input do Pragmata; e — **crítico** — *"Não testei se `UseSteamControllerConfig="2"` per-app funciona com `SteamController_PSSupport="0"` global (…) Se a Steam moderna não honrar o per-app (…) **a exceção é decorativa e falha em silêncio** — e o botão 'Este jogo não funciona' nunca funcionou de verdade. (…) é o primeiro passo desta sprint."*

**Menções pedidas:** Steam Input em todo o documento; decisão por jogo = a allowlist per-app (`steam_input_apps.txt`, `:78`); **troca de perfil durante a partida:** não trata; **congelamento de perfil:** não trata; **autoswitch:** não menciona. **Co-op durante a exceção — ponto 5 (`:118-124`):** entrar na exceção **derruba o gamepad virtual e desliga o co-op**; num jogo da allowlist só o jogador 1 joga e *"os outros três somem"*, e isso não está escrito em lugar nenhum da tela.

---

# 3. DUPLO-REGISTRO-01 — "o Steam Input tem dois cadastros"
`/mnt/.../sprints/2026-07-26-DUPLO-REGISTRO-01-o-steam-input-tem-dois-cadastros.md`

**Estado (:3-9):** `ABERTA`, ALTA, aberta em 26/07/2026 **durante uma partida de Pragmata**. Declara-se *"o mecanismo que faltava em STEAM-INPUT-01"* — aquela trata do que a tela **diz**, esta de dois registros do mesmo fato divergindo em silêncio. Confirmada ABERTA em 29/07 (`2026-07-29-INDICE-o-que-falta-depois-da-v030.md:37`) e 30/07 (`2026-07-30-INDICE-as-tres-faixas-depois-da-v040.md:54`), com a nota de que **nenhum arquivo desta sprint foi tocado** desde então e *"o remendo de 26/07 continua sendo o que segura"*.

**Defeito que cura:** o fato "o Steam Input está ligado para este jogo" está escrito em dois lugares (`localconfig.vdf` e `steam_input_apps.txt`), o Hefesto consulta só o segundo, e a divergência produz **input duplicado dentro do jogo** — quatro joysticks para um controle.

**Medição (:29-36):** `js0` vpad do Hefesto, `js2` DualSense físico visível, `js4`/`js5` Steam Virtual Gamepad. Grab não fechou: `23:26:22 gamepad_controller_grab grab=True ok=True state=pending` (`:58`).

**Entregas (1..5, :105-150):**

| # | símbolo/mecanismo | linha |
|---|---|---|
| 1 | **Um cadastro só — a Steam é a fonte da verdade.** O Hefesto passa a **ler o `localconfig.vdf`** para decidir se sai da frente; a allowlist muda de papel: deixa de ser a verdade e vira *"a intenção declarada pelo Hefesto"*, usada só pelo guarda para saber o que **não** desfazer | `:107-116` |
| 2 | **Piso, se a 1 for grande:** comparador das duas listas na subida e a cada varredura do guarda; cartão na aba Sistema com **nome do jogo** e dois botões `[ Registrar no Hefesto ]  [ Desligar na Steam ]`; registro no journal | `:117-126` |
| 3 | `daemon_actions.py:313-317` para de ensinar o caminho que quebra (some sozinho com a entrega 1) | `:128-132` |
| 4 | **grab pendente deixa de ser silencioso**: `state=pending` passa a ser **retentado enquanto a sessão de jogo estiver de pé** e a virar aviso legível na aba Status | `:134-141` |
| 5 | testes que mordem: (a) `vdf` com `UseSteamControllerConfig=2` + allowlist sem o appid → detectar divergência; (b) appid registrado → vpad retirado, **contando dispositivos, não chamando método privado**; (c) `state=pending` que nunca fecha vira aviso | `:143-149` |

**Símbolos para grep:** `localconfig.vdf`, `UseSteamControllerConfig`, `steam_input_apps.txt`, `gamepad_controller_grab`, `state=pending`, `EVIOCGRAB`, `daemon_actions.py:313-317`, `app/actions/daemon_actions.py:994`.

**Notas de caducidade/REFUTAÇÃO (:75-93) — bloco explícito:** *"Hipótese que foi levantada e REFUTADA por medição"* — suspeitava-se que o guarda tivesse desligado o Steam Input do Pragmata no boot (timer morto ressuscitado com `OnBootSec=3min`). O journal confirma que o guarda **rodou e editou** o arquivo às 22:48:05, **mas o campo não mudou** (`app=3357650 valor=2` antes e depois). *"**O guarda preservou a escolha dela.** A hipótese cai."* Fica registrada *"porque era a explicação mais plausível e teria fechado o caso no lugar errado"*.

**Conserto manual já aplicado (:95-103):** `+ 3357650` em `~/.config/hefesto-dualsense4unix/steam_input_apps.txt`. Explicitamente **remendo** — *"o registro continua duplicado, e a próxima divergência acontece do mesmo jeito no próximo jogo"*.

**O que diz para NÃO fazer:** não há veto rotulado; o "não" central é **não manter duas listas que podem divergir** (`:116`) e, no teste, **não chamar o método privado** (`:148`). Regra da casa citada: *"nunca escrever [no vdf] com a Steam viva"* (`:173`).

**O que NÃO foi medido (:161-176):** não confirmou ao vivo que o duplicado sumiu (*"reiniciar o daemon no meio da partida contraria as regras da casa"*); não sabe por que o grab ficou `pending`; os dois `X-Box 360 pad` não foram investigados; **e a entrega 1 depende de responder antes se é seguro ler o `localconfig.vdf` em runtime com a Steam aberta.**

**Menções pedidas:** Steam Input o tempo todo; decisão por jogo = a allowlist; **troca de perfil durante a partida:** relato dela — *"o dualsense mudou de perfil e virou o player 2"* (`:14-15`); autoswitch/congelamento: não menciona.

---

# 4. BOTÃO-QUE-NÃO-MENTE-01 — "clico e não acontece nada"
`/mnt/.../sprints/2026-07-26-BOTAO-QUE-NAO-MENTE-01-clico-e-nao-acontece-nada.md`

**Estado (:3-7):** `ABERTA`, ALTA, aberta em 26/07/2026, escopo = a janela inteira.
**PARCIAL, com nota datada:** `/mnt/.../sprints/2026-07-27-INDICE-a-blindagem.md:72` — *"**ENTREGUE 1 a 4**: cor acende ao soltar; 'Ver daemon.toml' removido; 'Modo jogo' virou 'Suspender mouse e teclado'; tooltips pararam de mentir; **desfazer do Steam Input existe**"*. Ficam abertas as **entregas 5 e 6** (`2026-07-29-INDICE-...v030.md:46`; `2026-07-30-INDICE-...v040.md:192,363`), com a dívida residual: `on_emulation_open_toml` **continua registrado em `app/app.py:320`** e implementado em `app/actions/emulation_actions.py:449`, embora o glade (`:2483-2489`) registre que o botão saiu.

**Defeito que cura:** dez controles agem só no segundo clique sem avisar e dois botões mentem no rótulo/tooltip — a usuária clica, nada visível acontece, e conclui que a janela é maquete.

**Veredito medido (:19-43):** "mockup puro" **REFUTADO** — 66 handlers no glade, 66 no dict de sinais `app/app.py:247-349`, 66 com `def` em `app/`, **0** com corpo vazio, **0** órfãos, **0** botões que nascem cinza e nunca ligam (registrado como `BUG-GUI-EMULATION-HANDLERS-UNWIRED-01`). "Confuso demais" **CONFIRMADO** — 145 controles acionáveis (183 no cenário cheio), 6 "Aplicar", 6 "Desligar/Apagar/Parar", 2 "Salvar" com semânticas diferentes visíveis ao mesmo tempo, 48 de 85 widgets sem tooltip.

**Os 10 diferidos (:49-55):** `lightbar_color_button` (`main.glade:765`, aplica em `:796`), `lightbar_brightness_scale` (`:871`), `rumble_weak_scale`/`rumble_strong_scale` (`:1234`,`:1252` — **nenhum sinal**), `profile_name_entry` + 5 campos (`:1495,:1511,:1578,:1613,:1627,:1641`). Handler que grava no rascunho: `lightbar_actions.py:414`.

**Os dois que mentem:** (1) `gui/main.glade:1878` + tooltip *"e dá para desfazer"* contra handler `app/actions/daemon_actions.py:970` que só chama `add_appid_to_steam_input_allowlist`, enquanto `remove_appid_from_steam_input_allowlist` (`integrations/steam_launch_options.py:821`) tem **zero chamadores em `src/`, nove em `tests/`** (`:71-88`); (2) `gui/main.glade:2148` → `app/actions/emulation_actions.py:274-303` **cria** `~/.config/hefesto-dualsense4unix/daemon.toml` com `[hotkey] buffer_ms=… ps_long_press_ms=…`, abre no editor, e **o daemon não lê o arquivo** (`BUG-DAEMON-TOML-DEAD-01`, admitido na linha 275) (`:90-102`).

**Outros ruídos (:106-123):** `main.glade:2234` "Modo jogo" chama `daemon.emulation.suppress`; `player_led_1..5` (`main.glade:926-930`) invisíveis mas declarando sinal, com tooltip em `:1022` explicando refactor interno (`LEDS-SO-PLAYER-01`); dois "Salvar" — `profile_save_button` (`:1705`) e `btn_footer_save_profile` (`:2746`); "Aplicar" redundante sobre `lightbar_actions.py:822`.

**Entregas (1..6, :125-174):**

| # | símbolo/mecanismo | linha |
|---|---|---|
| 1 | cor e brilho aplicam em **`button-release`**; o que continuar diferido ganha marca de pendente + rodapé "há mudanças não aplicadas" | `:127-137` |
| 2 | **ligar `remove_appid_from_steam_input_allowlist` a um botão real**, listar jogos da exceção **por nome** com "Desfazer" cada; senão o tooltip para de prometer | `:139-143` |
| 3 | **remover** "Ver daemon.toml" da janela (remoção, não reparo) | `:145-151` |
| 4 | "Modo jogo" → **"Suspender mouse e teclado"**; tirar as 5 caixas fantasma do glade; reescrever o tooltip do refactor; o "Salvar" do editor vira **"Salvar este perfil"** | `:153-160` |
| 5 | 145 controles vira **medição de partida**: toda sprint de UI informa quantos controles adiciona/remove | `:162-167` |
| 6 | testes: widget `visible=False` **e** `<signal>` reprova; tooltip com "desfazer" cujo handler não chama função de remoção reprova (*"Este teste, rodado hoje, reprova — é a prova de que morde"*); handler que só escreve no rascunho sem marca de pendente reprova | `:169-174` |

**Símbolos para grep:** `remove_appid_from_steam_input_allowlist`, `add_appid_to_steam_input_allowlist`, `on_emulation_open_toml`, `daemon.emulation.suppress`, `btn_footer_save_profile`, `profile_save_button`, `player_led_1..5`, `lightbar_color_button`, `lightbar_brightness_scale`, `rumble_weak_scale`, `rumble_strong_scale`, `BUG-DAEMON-TOML-DEAD-01`, `BUG-GUI-EMULATION-HANDLERS-UNWIRED-01`.

**O que diz para NÃO fazer:** *"mentira na tela é pior que falta"* (`:143`) — se não há desfazer, o tooltip **não** promete; entrega 3 é **remoção, não reparo** — *"criá-lo é pior que não ter o botão"* (`:147`); a entrega 5 **não** entra como redesenho (`:166`); *"Diferir a escrita é uma decisão **correta** (…) O defeito não é o adiamento: é o adiamento **silencioso**"* (`:64-66`) — ou seja, **não** desfazer o diferimento.

**Menções pedidas:** Steam Input só via o botão "Este jogo não funciona" e o desfazer órfão; **decisão por jogo:** *"um jogo marcado por engano deixa de ter cor, gatilhos e co-op do Hefesto até ser desmarcado"* (`:87`). Sem autoswitch, sem troca de perfil em partida.

---

# 5. ÁUDIO-QUE-TRANCA-01
`/mnt/.../sprints/2026-08-03-AUDIO-QUE-TRANCA-01-um-toque-no-volume-congela-a-troca-de-perfil.md`

**Estado (:3-13):** `PROPOSTA, escrita em 03/08/2026. **Nenhuma linha de código tocada**`. ALTA. Faixa 1. Causa-raiz **PROVADA no código** nos três casos. Paga o furo já nomeado pela `AUTOMATISMO-MORTO-01` (linha 647, citando `autoswitch.py:505-518`). Índice a lista como pendência com *"só a E5 (decisão)"* em aberto (`/mnt/.../sprints/2026-08-03-INDICE-o-bluetooth-de-primeira-classe.md:88`).

**Defeito que cura:** um toque no volume arma a categoria `audio` na trava manual, que **não tem clear nenhum**, e por isso congela a troca automática de perfil inteira — para gatilhos, LEDs, rumble e modo.

**Os três defeitos:** (1) `audio` congela o autoswitch inteiro, sem porta de saída — nasceu no commit `c10adaf`; (2) `controller_card.py:3010` chama `ipc_bridge.speaker_set(rota=rota)` **sem `volume` e sem `uniq`** → zera o alto-falante, escreve no controle errado, e arma o defeito 1 — nasceu no commit `19acbeb`; (3) o botão do microfone (`integrations/audio_control.py:95-114`, `fonte_padrao_e_o_controle()`) e a política do instalador (`assets/wireplumber/51-hefesto-dualsense-no-default-source.conf`, `scripts/doctor.sh:532`) se anulam — nasceu no commit `5801de9`.

**Entregas E1..E5 (:180-264):**

| E | símbolo/mecanismo prometido | linha |
|---|---|---|
| **E1** | criar **`clear_manual_trigger_active("audio")`** ao lado de `daemon/ipc_handlers.py:676`; e `_marcar_audio_manual` (`daemon/ipc_handlers.py:2960-2975`) na **devolução da posse (`release`) passa a LIMPAR em vez de armar** | `:184-196` |
| **E2** | **`manual_trigger_active` deixa de ser booleano de tudo-ou-nada.** O autoswitch (`profiles/autoswitch.py:505-518`) passa a perguntar pelas **categorias que ele vai aplicar**. Aceite: só `audio` armado → perfil sem seção de áudio **entra** | `:198-214` |
| **E3** | `controller_card.py:3010` → `speaker_set(rota=rota, volume=<o vigente>, uniq=uniq)`. Mordida: teste de card com dois `uniq`, espionando `ipc_bridge.speaker_set` e conferindo kwargs | `:216-230` |
| **E4** | **`set_speaker_volume` recusa (ou herda explicitamente)** uma chamada com rota sem volume, em vez de assumir zero. A regra de `profiles/schema.py:359-392` passa a valer no caminho vivo. *"o `pref = 0` transforma 'não me disseram' em 'me disseram zero'"* | `:232-244` |
| **E5** | (a) botão do mic age sobre o microfone **do controle onde foi apertado** — resolve também `core/backend_pydualsense.py:1920-1926` lendo `micBtn` só do primário (**os botões dos controles 2, 3 e 4 nunca viram evento**); **ou** (b) retirar o drop-in 51 dos padrões. **Recomendação: (a)** | `:246-264` |

**Símbolos para grep:** `clear_manual_trigger_active("audio")`, `_marcar_audio_manual`, `manual_trigger_active`, `MANUAL_OVERRIDE_CATEGORIES`, `_manual_override_categories`, `speaker_set`, `set_speaker_volume`, `fonte_padrao_e_o_controle`, `micBtn`, `speaker_state_for`, `release_speaker_volume`, `51-hefesto-dualsense-no-default-source.conf`.

**Notas de caducidade/REFUTAÇÃO (:161-177):** *"O que foi REFUTADO nesta investigação, e o registro fica"* — a hipótese de que `"dualsense" in saida.lower()` não casaria o nome real do nó **foi medida como falsa em 03/08**: `alsa_input.usb-Sony_Interactive_Entertainment_DualSense_Wireless_Controller-00.iec958-stereo` contém "DualSense". *"A hipótese está refutada e não deve ser reaberta."* Fica dívida menor: o casador canônico da casa usa **três** marcadores (`app/mic_monitor.py:55-60`) — *"é **robustez**, não defeito ativo"*.

**O que diz para NÃO fazer (:278-288) — bloco explícito:**
- **Não tirar `"audio"` da lista de categorias** como cura do defeito 1 — a justificativa de `c10adaf` está certa; falta o clear e a granularidade;
- **Não fazer o `release` limpar TUDO** — deve limpar `audio`, não a trava de gatilho de outra aba;
- **Não reabrir a hipótese do casador do microfone**;
- **Não mexer no drop-in 51 sem decidir a E5.**

**Fica ABERTO (:290-294):** a escolha (a)/(b) da E5 — *"é dela"*; e o botão do mic dos controles 2/3/4.

**Menções pedidas:** **autoswitch** é o núcleo (`profiles/autoswitch.py:505-518`, `_activate` retorna para toda troca de janela salvo *"o candidato é regra própria de jogo E difere do ativo"*); **congelamento de perfil** é o título; **troca de perfil durante a partida** não é tratada. Steam Input / `steam_input` / decisão por jogo: **nenhuma menção**.

---

# 6. POSSE-POR-CONTROLE-01
`/mnt/.../sprints/2026-08-03-POSSE-POR-CONTROLE-01-a-trava-de-um-controle-congela-os-quatro.md`

**Estado (:3-13):** `PROPOSTA, escrita em 03/08/2026. **Nenhuma linha de código tocada**`. ALTA — *"um dos quatro defeitos aqui é consequência do trabalho que está na árvore agora, não commitado"*. Faixa 2. Causa-raiz **PROVADA** nos quatro casos. Índice: *"só a E2 (decisão)"* em aberto (`/mnt/.../sprints/2026-08-03-INDICE-o-bluetooth-de-primeira-classe.md:90`). Declara-se o mesmo defeito de *"a config que eu deixo nunca é respeitada"* **no eixo CONTROLE A CONTROLE**.

**Defeito que cura:** quatro caminhos escapam do modelo de precedência por campo e por MAC (`core/backend_pydualsense.py:273-341`) e agem globalmente — mexer num controle congela ou pinta os quatro.

**Os quatro (+1) defeitos:** (1) trava manual global — `daemon/state_store.py:102` é `set[str]` único, **sem TTL e sem chave por MAC**, armado por `ipc_handlers.py:735` (`led.set`) e `:783` (`led.player_set`), obedecido por `profiles/manager.py:342-346` (`led=None if "led" in travadas else effective.lightbar`); (2) `_registrar_em_todos` (na árvore **não commitada**) carimba `_LAYER_USER` nos quatro via `ipc_handlers.py:717`/`:605-607` → `backend_pydualsense.py:2627`, e o perfil só ocupa slot vago (`:2729-2731`); (3) rumble do jogo cai no fallback `gamepad.py:765-766` → `backend_pydualsense.py:2178`/`:1996-1999`, que **não é broadcast** — vai só para o `_output_target_key`, o seletor da aba, e a docstring de `gamepad.py:744-745` mente; (3-bis) **MEDIDO AO VIVO em 03/08** — `led.player_set` respondeu `{"status":"ok", "aplicado_em":["143a9a0000ab"]}` e o sysfs **não mudou** (`player-3 = 1` antes e depois), porque a camada do co-op vence (`R-13`, `backend_pydualsense.py:335-341`); (4) `_for_each` (`:1995-2001`) e `_for_each_led` (`:2033-2039`) com `broadcast=False` sequestram também `set_trigger` (`:2154`) e `set_led` (`:2164`).

**Entregas E1..E4 (:177-260):**

| E | símbolo/mecanismo prometido | linha |
|---|---|---|
| **E1** | **`manual_override_categories` vira INDEXADO POR MAC, com um balde `None` para o que é genuinamente global.** Onde: `daemon/state_store.py:102` + os dois pontos de armação (`ipc_handlers.py:735,783`); consumidor `profiles/manager.py:342-346`. **Compatibilidade não opcional:** nenhum campo do `StateStore` é indexado por uniq hoje (`state_store.py:82-166`) — **manter a assinatura antiga devolvendo a UNIÃO dos baldes** e acrescentar consulta por MAC | `:179-196` |
| **E2** | broadcast volta a gravar em **`_desired_default`** e passa a vencer a paleta automática, sem descer para `_LAYER_USER` de cada MAC. Ponto exato: `_merged_desired_for_key` (`backend_pydualsense.py:1222`) põe a automática acima do `_desired_default` e abaixo do `_desired_by_uniq`. Duas saídas: **(a)** quinto nível "broadcast da usuária" — **recomendado**; **(b)** marcar `_desired_default` com dono `usuaria` | `:198-226` |
| **E3** | `gamepad.py:765-766` passa a chamar com **`broadcast=True`**, e a docstring de `:744-745` passa a dizer a verdade. Regra a escrever no código: ***"escrita originada da usuária respeita o seletor; escrita originada do jogo, nunca."*** | `:228-243` |
| **E4** | quatro bancadas, **todas com dois controles**: (1) trava de quem; (2) broadcast + perfil convivem; (3) rumble do jogo ignora o seletor; (4) o seletor continua valendo para o gesto dela | `:245-260` |

**Símbolos para grep:** `manual_override_categories`, `_manual_override_categories`, `mark_manual_trigger_active`, `_registrar_em_todos`, `apply_output_for`, `_stamp_owner_locked`, `_LAYER_USER`, `_desired_default`, `_desired_by_uniq`, `_merged_desired_for_key`, `_output_target_key`, `_for_each`, `_for_each_led`, `set_rumble_for`, `set_rumble`, `set_trigger`, `set_led`, `apply_output_defaults`, `aplicado_em`, `forward_all`.

**Nota de refutação/correção datada:** o **defeito 3-bis** é a nota medida ao vivo em 03/08 e corrige uma promessa anterior — *"o campo `aplicado_em` — acrescentado justamente para o daemon parar de mentir — ainda mente"*; ele *"informa em que MACs a intenção foi **registrada**, não **onde ela pegou**"* (`:121-158`). Também refuta a docstring de `gamepad.py:744-745`: *"**Não é broadcast.**"* (`:106`).

**O que diz para NÃO fazer (:274-287) — bloco explícito:**
- **Não reverter a cura `BROADCAST-QUE-NÃO-MENTE-01`** — corrige-se o **alcance**, não a cura;
- **Não tirar a camada automática do merge** (é a cor por jogador, `COR-03`);
- **Não mover a camada do co-op para `_desired_by_uniq`** (`R-13`, `backend_pydualsense.py:335-341`);
- **NÃO DAR TTL À TRAVA MANUAL** como atalho para o eixo por controle — *"o tempo não é o eixo errado por acaso — a trava existe para sobreviver ao autoswitch, que dispara a cada troca de janela"*;
- **Não mexer no `apply_output_defaults`**, que escapa do seletor de propósito.

**Fica ABERTO (:289-297):** a escolha (a)/(b) da E2 (*"vale escrever a nota datada do que foi escolhido e por quê"*); e `mode`, `mouse`, `rumble`, `key_bindings`, `speaker` **continuam globais no perfil** (`profiles/schema.py:468-500`) — só `leds` e `triggers` têm eixo por controle (`:447-448`).

**Menções pedidas:** **autoswitch** aparece como uma das três saídas da trava (`profiles/autoswitch.py:508`) e no veto do TTL; **troca de perfil durante a partida:** implícita no defeito 3 (`forward_all` a 60 Hz em `coop.py:1329-1331` vs teardown no `sync` de 2 s); **congelamento de perfil** é o título. Steam Input / decisão por jogo: **nenhuma menção**.

---

# 7. TRAVA-QUE-SOLTA-TARDE-01
`/mnt/.../sprints/2026-08-05-TRAVA-QUE-SOLTA-TARDE-01-o-gesto-explicito-e-vitima-da-propria-trava.md`

**Estado (:3-14):** **`CURA APLICADA`, com teste que morde**, achada em **05/08/2026** ao restaurar o perfil dela depois de uma medição de gatilho — *"O defeito não estava sendo procurado"*. ALTA. Causa-raiz **PROVADA no código e MEDIDA ao vivo** com o daemon de produção. Declara-se *"Parente, e distinta"* da `ÁUDIO-QUE-TRANCA-01` — mesmo campo, defeito diferente. O índice de 03/08 já a incorpora: *"**Cura aplicada, com teste que morde**"* (`/mnt/.../sprints/2026-08-03-INDICE-o-bluetooth-de-primeira-classe.md:150-155`).

**Defeito que cura:** os dois gestos explícitos de troca de perfil (janela/CLI e PS+D-pad) limpavam a trava manual **depois** de aplicar o perfil, e por isso aplicavam o perfil pela metade — a mesma ação repetida dava resultados diferentes.

**Prova (:36-40):** `00:02:06 profile_apply_respeita_override_manual categorias=['audio','trigger']` / `00:02:06 profile_activated name=vitoria origin=manual` (pulou) / `00:03:22 profile_activated name=vitoria origin=manual` (aplicou tudo).

**Alcance (:74-79):** `ipc_handlers.py` (janela e CLI) — **defeito**; `subsystems/hotkey.py` (**PS + D-pad**) — **defeito**; `profiles/autoswitch.py:505-518` (troca automática) — **correto** (limpa antes). *"a paridade copiou a ordem errada"*.

**Entregas — esta sprint não numera E1/E2; a "cura aplicada" (:128-154) é o mecanismo concreto:**

| item | símbolo/mecanismo | linha |
|---|---|---|
| cura | em **ambos** os caminhos, `clear_manual_trigger_active()` + `mark_manual_profile_lock(now + MANUAL_PROFILE_LOCK_SEC)` passam para **antes** do `manager.activate(...)` | `:129-141` |
| decisão 1 | **o lock sobe junto com o clear** — não pode existir janela em que nem a trava nem o lock suprimam o autoswitch (*"seria trocar um defeito por outro (o Bug C)"*) | `:145-148` |
| decisão 2 | **restore no `except`**: `travadas_antes = getattr(store, "manual_override_categories", ()) or ()` e re-`mark_manual_trigger_active(categoria)` no erro — *"Ativação que falhou não é gesto cumprido"* | `:149-152` |
| decisão 3 | **`getattr` na leitura das categorias**, pelo motivo de `manager.py:384-387`: dublês e stores parciais continuam funcionando | `:153-154` |
| teste | **`tests/unit/test_trava_que_solta_tarde_01.py`** — seis casos, bancada hermética (`ProfileManager` real + `StateStore` real + `FakeController` espião). Mordida verificada em 05/08: com a cura arrancada, **4 dos 6 reprovam**, inclusive `test_duas_ativacoes_seguidas_sao_indistinguiveis` | `:156-170` |

**Símbolos para grep:** `clear_manual_trigger_active`, `mark_manual_trigger_active`, `mark_manual_profile_lock`, `MANUAL_PROFILE_LOCK_SEC`, `manual_override_categories`, `_handle_profile_switch`, `save_active_marker`, `profile_apply_respeita_override_manual`, `test_trava_que_solta_tarde_01.py`, `test_duas_ativacoes_seguidas_sao_indistinguiveis`, `test_falha_na_ativacao_devolve_a_trava`, `test_onda_u_trava_por_categoria.py`, `test_perfil_respeita_trava_manual.py`.

**Notas de refutação datadas (:86-98 e :99-112):**
- **Derruba uma entrega declarada:** `hotkey.py:132-135` justificava o `speaker_applier` desde a `SOM-02/E4` com *"o ciclo PS+D-pad (…) limpa as categorias travadas (inclusive `audio`) e portanto aplica o volume do perfil que entra"* — **"Não aplicava."** Catalogado pela `ENTREGA-QUE-NÃO-LIGOU-01`;
- **Refuta a cobertura dos testes existentes:** `test_onda_u_trava_por_categoria.py:144-155` chama `clear_manual_trigger_active()` à mão com o comentário `# o que profile.switch chama` e **nunca chama `_handle_profile_switch`**; `test_perfil_respeita_trava_manual.py:122-132` *"documenta a ordem certa que o produto não executava"*. Ambos verdes com o produto quebrado — *"mordida na metade errada da cadeia"*;
- **Honestidade sobre o 5º caso (:167-170):** `test_falha_na_ativacao_devolve_a_trava` passa nos dois estados — protege a borda que a **cura** introduziu, não o defeito original.

**O que diz para NÃO fazer:** *"esta sprint **não o toca**"* (o defeito 1 da ÁUDIO-QUE-TRANCA-01, `:174-176`) — ou seja, não confundir as duas e não tentar resolver `audio` aqui. E o alerta de arquitetura: dar granularidade por categoria ao autoswitch **não cura o caminho manual** (`:123-126`).

**Fica ABERTO (:172-181):** `audio` continua sem clear próprio; `manual_trigger_active` continua booleano de tudo-ou-nada; e o aceite em uso real.

**Menções pedidas:** **autoswitch** — o lock existe para suprimi-lo entre o clear e o fim do `activate`; **troca de perfil durante a partida** — *"atinge os dois gestos explícitos dela, inclusive o **PS + D-pad que ela usa dentro do jogo**"* (`:6-7`); **congelamento de perfil** — o inverso (perfil que entra pela metade). Steam Input: **nenhuma menção**.

---

# CRÍTICO — `manual_override_categories`: o que cada uma propõe, e se colidem

| | ÁUDIO-QUE-TRANCA-01 (03/08) | POSSE-POR-CONTROLE-01 (03/08) | TRAVA-QUE-SOLTA-TARDE-01 (05/08) |
|---|---|---|---|
| **eixo** | **categorias** (qual) | **controles/MAC** (de quem) | **ordem temporal** (quando limpa) |
| **estrutura de dados nova?** | **Não muda a estrutura.** Muda o **consumidor**: o autoswitch para de ler o booleano `manual_trigger_active` e passa a perguntar pelas categorias que vai aplicar (E2) | **SIM.** `set[str]` → **indexado por MAC**, com balde `None` para o global. É *"o primeiro"* campo do `StateStore` indexado por uniq (E1) | **Não muda nada.** Só lê (`getattr`) para poder restaurar no `except` |
| **escopo por controle?** | não | **sim, é o objeto da sprint** | não |
| **TTL?** | não propõe | **VETA explicitamente** — *"Não dar TTL à trava manual como atalho para o eixo por controle"* (`:284-285`) | não. Usa o **outro** relógio: `MANUAL_PROFILE_LOCK_SEC` (30 s), que é o lock do autoswitch, não a trava |
| **quem limpa?** | **cria** `clear_manual_trigger_active("audio")` ao lado de `ipc_handlers.py:676`, e faz o **`release` limpar em vez de armar** (`_marcar_audio_manual`) | não mexe nos clears; muda **quem obedece** (`profiles/manager.py:342-346`) | **os mesmos clears que já existiam**, movidos para **antes** do `activate` em `ipc_handlers.py` e `hotkey.py`; + **restore no `except`** |

**Contradição direta entre elas: NÃO HÁ.** As três são ortogonais e cada uma diz isso por escrito:
- ÁUDIO E2, **Nota de escopo** (`:211-214`): *"a POSSE-POR-CONTROLE-01 mexe no **mesmo** `manual_override_categories`, no eixo por controle. **As duas entregas tocam a mesma estrutura** — executá-las na mesma leva, e **nesta ordem (categoria primeiro, controle depois)**, evita duas migrações do mesmo campo."* O índice repete a ordem (`/mnt/.../2026-08-03-INDICE-o-bluetooth-de-primeira-classe.md:168-171`);
- TRAVA (`:113-126`), bloco **"Por que não é a ÁUDIO-QUE-TRANCA-01"**: *"o defeito 1 daquela é a **ausência** de `clear_manual_trigger_active("audio")`, e o efeito é sobre o **autoswitch**; **este** é sobre a **ordem** num clear que **existe** — tanto que aquela sprint o lista como cobertura (`ipc_handlers.py:412`, na tabela de clears). A medição mostra que ele não serve à ativação que o dispara."*

**Tensões reais (não contradições), e são três:**
1. **Um erro de fato na ÁUDIO:** a tabela de clears (`:44-50`) lista `ipc_handlers.py:412 clear_manual_trigger_active() (global)` como cobertura existente. A TRAVA prova que **esse clear rodava tarde demais** — a ÁUDIO contava com uma porta que não funcionava para a própria ativação. Ponto de correção datado 05/08.
2. **A TRAVA agrava a E2 da ÁUDIO** (`:123-126`): *"dar granularidade por categoria ao autoswitch, como a E2 propõe, **não cura o caminho manual** — lá o problema não é granularidade, é ordem."* Ou seja, a E2 sozinha teria sido declarada entregue com o caminho manual ainda quebrado.
3. **A E1 da POSSE muda a assinatura de leitura que a TRAVA acabou de passar a usar.** A cura de 05/08 lê `getattr(store, "manual_override_categories", ())` em dois lugares (`ipc_handlers.py:426`, `hotkey.py:166`) e restaura com `mark_manual_trigger_active(categoria)` — **sem MAC**. Quando a E1 indexar por MAC, esse restore volta a jogar tudo no balde global e perde a atribuição por controle. A própria E1 antecipa metade disso (*"mantenha a assinatura antiga devolvendo a **união** dos baldes"*) — mas a união resolve a **leitura**, não a **reescrita**. **Este é o ponto de colisão concreto a vigiar.**

**Qual é mais recente / qual supõe a outra aplicada:**
- **Mais recente: TRAVA-QUE-SOLTA-TARDE-01 (05/08)** — e é a **única já aplicada**. As outras duas são de 03/08 e continuam `PROPOSTA`.
- **Nenhuma supõe outra aplicada como pré-requisito.** A ordem prescrita é: **ÁUDIO E1/E2 → POSSE E1**, na mesma leva (para uma migração só do campo). A TRAVA é **independente e já foi executada antes das duas** — o que significa que quem for fazer a ÁUDIO e a POSSE encontra o campo já com leitura+restore transacional em volta, e tem de preservá-los.

---

# Verificação por grep na árvore atual (05/08, `HEAD` = `5f1b588`, com árvore suja)

**Números de linha das sprints derivaram** — usei os símbolos.

| símbolo prometido | sprint | estado em produção |
|---|---|---|
| `clear_manual_trigger_active("audio")` | ÁUDIO E1 | **NÃO EXISTE** em `src/` nem `tests/`. Clears reais: `ipc_handlers.py:430` (global), `:710` (`"trigger"`), `:2705` (`"rumble"`), `hotkey.py:168` (global), `autoswitch.py:508` (global) |
| `_marcar_audio_manual` no `release` | ÁUDIO E1 | **AINDA ARMA.** `daemon/ipc_handlers.py:2974` (dentro de `_speaker_release`) chama `_marcar_audio_manual()`, cuja docstring (`:2994-3007`) ainda diz *"A devolução da posse também arma"* |
| `manual_trigger_active` granular | ÁUDIO E2 | **NÃO APLICADO.** `state_store.py:425-428` ainda é `bool(self._manual_override_categories)`; `autoswitch.py:466` e `:505` ainda leem o booleano |
| `speaker_set(rota=…, volume=…, uniq=…)` | ÁUDIO E3 | **APLICADO** — `app/widgets/controller_card.py:3034`, com comentário histórico em `:3011` (*"A chamada era `speaker_set(rota=rota)`, sem volume e sem uniq"*). Provavelmente pelo commit `108b711` |
| `set_speaker_volume` recusa rota sem volume | ÁUDIO E4 | parcial — há guarda em `ipc_handlers.py:2932` (*"a recusa vira MENSAGEM"*) e nota em `lifecycle.py:2483-2498`; a raiz em `backend_pydualsense.py:2371` merece leitura direta |
| `fonte_padrao_e_o_controle` | ÁUDIO E5 | def em `integrations/audio_control.py:72`; **um** chamador: `daemon/subsystems/hotkey.py:303`. Escolha (a)/(b) ainda **não tomada** |
| `manual_override_categories` por MAC | POSSE E1 | **NÃO APLICADO.** `daemon/state_store.py:102` continua `self._manual_override_categories: set[str] = set()` |
| `_registrar_em_todos` | POSSE E2 | existe e é chamado: `ipc_handlers.py:585` (def), `:751` (`led=(r,g,b)`), `:808` (`player_leds=bits`) — o defeito 2 **está na árvore**, agora commitado |
| cura da TRAVA | TRAVA | **APLICADA e NÃO COMMITADA** — `git status` mostra `MM src/.../daemon/ipc_handlers.py` e `MM src/.../daemon/subsystems/hotkey.py`, `AM tests/unit/test_trava_que_solta_tarde_01.py`. Confirmado no código: `ipc_handlers.py:426` (`travadas_antes`), `:430` (clear), `:434` (`mark_manual_profile_lock`), `:451` (restore); `hotkey.py:166,168,169,186,187` |
| `remove_appid_from_steam_input_allowlist` | STEAM-INPUT E3 / BOTÃO E2 | def `integrations/steam_launch_options.py:828`; **único chamador de produção: `cli/cmd_steam.py:206,215` — terminal.** **Zero chamadores em `app/` ou `gui/`.** `gui/main.glade:2434` só a cita em texto |
| `add_appid_to_steam_input_allowlist` | STEAM-INPUT | def `:772`; chamador vivo `app/actions/daemon_actions.py:1182` (via `getattr`) |
| `steam_input_vpad_suspenso` | STEAM-INPUT E6 | **DEIXOU DE SER ÓRFÃO.** Consumidores fora do módulo: `daemon/lifecycle.py:1762-1765` e `daemon/ipc_handlers.py:1445`; superfície de tela em `app/actions/emulation_actions.py:329` (`"vpad_suspenso_pelo_steam_input"`) e `app/actions/status_actions.py:175,242,248` (*"O jogo assumiu o controle: o Hefesto saiu da frente dele"*) |
| `sync_steam_input_exception` / `suspend_vpads_for_steam_input` | STEAM-INPUT | vivos — `gamepad.py:231`/`:425`, chamados em `gamepad.py:1077` e `:284` |
| `MANUAL_OVERRIDE_CATEGORIES` | ÁUDIO/POSSE | `state_store.py:58-60` = `frozenset({"trigger","led","rumble","audio"})` — `audio` continua lá (correto, é o que a ÁUDIO manda **não** tirar) |

**Resumo do risco de execução:** a ÁUDIO E1/E2 e a POSSE E1 continuam integralmente por fazer sobre um campo cuja vizinhança **acabou de mudar** (a cura da TRAVA, ainda não commitada). Fazer a POSSE E1 sem reconciliar o restore por categoria de `ipc_handlers.py:451` e `hotkey.py:186` reintroduz a globalidade que a E1 existe para matar.
