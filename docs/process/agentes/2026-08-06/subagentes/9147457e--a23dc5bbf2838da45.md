# agente a23dc5bbf2838da45 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Escrever SALVAR-NAO-REBAIXA e ATIVAR-NAO-MENTE
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, ESCREVA DUAS sprints que faltam.

ANTES leia, como exemplar de estilo:
- docs/process/sprints/2026-08-05-TRAVA-QUE-SOLTA-TARDE-01-o-gesto-explicito-e-vitima-da-propria-trava.md
E a síntese consolidada:
- docs/process/estudos/2026-08-05-o-sistema-de-perfis-o-que-dezessete-agentes-mediram.md

ARQUIVO 1: docs/process/sprints/2026-08-05-SALVAR-NAO-REBAIXA-02-o-novo-perfil-desligava-as-proprias-guardas.md
Evidências: `git grep -n "SALVAR-NAO-REBAIXA"` (a 02 tem 15 citações; a 01 tem 11 e NUNCA virou página — o arquivo TEM de abrir declarando que está sendo escrito sobre um vazio, e resumir a 01 a partir das docstrings). Leia o `git diff --cached` de app/actions/profiles_actions.py e app/gui_dialogs.py, e tests/unit/test_salvar_nao_rebaixa_02_o_novo_perfil_desligava_as_guardas.py.
Núcleo: `_esquecer_a_fotografia_do_editor` saiu do topo de `on_profile_new` para o fim (o set_value(0)/set_active_id("any") de nascimento levantava os gestos e a guarda acreditava numa escolha que ela não fez); releitura da fotografia do disco no `_build_profile_from_editor` via `_perfil_que_o_salvar_sobrescreve` (busca por SLUG, lição do R-10); `queda_de_prioridade_pede_aviso` + `confirm_downgrade_priority`; a guarda de match estendida de MatchCriteria para "não é MatchAny" (cobre MatchManual e criteria vazio, que eram o furo).

ARQUIVO 2: docs/process/sprints/2026-08-05-ATIVAR-NAO-MENTE-01-o-botao-que-parecia-falhar-e-ativava-duas-vezes.md
Evidências: `git grep -n "ATIVAR-NAO-MENTE-01"` (14 citações). Leia o diff de app/ipc_bridge.py, app/actions/profiles_actions.py, app/gui_dialogs.py e packaging/cosmic-applet/src/ipc.rs, e tests/unit/test_ativar_nao_mente_01_o_botao_que_parecia_falhar.py.
Núcleo: `PROFILE_SWITCH_TIMEOUT_S = 3.0` como fonte única na ponte — o timeout de LEITURA era curto demais e a janela lia False de uma troca que TINHA acontecido (isso corrigiu também a CLI e o Salvar da aba Perfis); o espelho `SWITCH_IPC_TIMEOUT` no applet COSMIC em Rust (e por que o IPC_TIMEOUT de leitura NÃO foi esticado); a janela passa a LER o `secoes` do relatório do daemon em vez de inventar o toast; `_refazer_as_abas_apos_ativar` com o diálogo `confirm_discard_pending_edits` (default = MANTER). É a única sprint da leva que atravessa a fronteira Python↔Rust — destaque isso.

REGRAS DA CASA para os dois: português do Brasil acentuado (nomes de arquivo sem acento); nada de MAC real; declare o grau de cada afirmação (MEDIDO / SU

## RESULTADO FINAL

As duas sprints estão escritas, staged e com os portões de documentação verdes.

**Arquivos criados** (ambos em `/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/`): `2026-08-05-SALVAR-NAO-REBAIXA-02-o-novo-perfil-desligava-as-proprias-guardas.md` (321 linhas) e `2026-08-05-ATIVAR-NAO-MENTE-01-o-botao-que-parecia-falhar-e-ativava-duas-vezes.md` (298 linhas).

**A 02 abre declarando o vazio:** a SALVAR-NÃO-REBAIXA-01 nunca virou página (11 citações, zero em `docs/`), então há uma seção que a reconstrói das docstrings e do commit `8d7fd45` (28/07 19:44) — e o núcleo da 02 é que a cura de 27/07 **não falhou, foi desligada por um botão vizinho**; documenta também que a faixa de perfis está órfã de índice desde 30/07, o que explica estruturalmente por que isso passou oito dias.

**Correção de precisão que fiz contra o briefing:** o furo da guarda de match era o `MatchManual` (classe irmã, `schema.py:108`); o `MatchCriteria` vazio *já era pego* pelo `isinstance` antigo — nele o furo era a **frase** do diálogo, que é o que o `regra_atual` cura. Está escrito assim na sprint.

**Verifiquei as mordidas eu mesmo** (não herdei da síntese): 13 mutações cirúrgicas, uma cura por vez, arrancada / rodada / devolvida, mais `cargo test ipc::` = 16 passed. Na 02: 8 dos 19 casos mordem, 11 são guardas declaradas. Na ATIVAR: 13 dos 22 mordem, 9 são guardas. Rodei os pares **número/fiação** e **regra/fiação** separadamente, porque constante certa com chamada errada não cura nada. A árvore de código ficou intacta (`git diff --stat -- src/ packaging/ tests/` vazio; 41 verdes ao final).

**A fronteira Python↔Rust está destacada** na ATIVAR, incluindo a decisão que *não* foi tomada: o `IPC_TIMEOUT` de leitura do applet segue em 250 ms de propósito, e `test_a_leitura_do_applet_continua_curta` existe para impedir a cura preguiçosa de esticá-lo. Graus declarados em toda afirmação: o defeito é MEDIDO, a atribuição do 191 e da rajada de cliques ficaram como SUSPEITA COM MECANISMO (DIV-1 e DIV-3), e o aceite de tela dos três diálogos novos fica ABERTO — ninguém fotografou nada.
