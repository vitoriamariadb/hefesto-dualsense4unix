# agente a6ad9ed8d1165faed (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Curar o salvar no arquivo errado
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix (branch restauro/inicio-da-sessao, HEAD 0bb92a5), CURE o defeito mais grave da aba Perfis: **a janela troca o nome no editor sozinha, e o Salvar seguinte grava no arquivo errado, evaporando o trabalho dela.**

Já foi REPRODUZIDO em bancada. As bancadas estão em `/tmp/claude-1000/-mnt-Apate-Desenvolvimento-hefesto-dualsense4unix/9147457e-032a-4d1e-aa95-63cdf0d6434e/scratchpad/bancada_q3.py`, `bancada_q2.py` e `bancada_q3b.py` (rodam com `xvfb-run -a .venv/bin/python <arquivo>`). LEIA-AS e reproduza você mesmo antes de curar.

O RELATO DELA: *"clico em salvar e ele salva com um nome aleatório ou de outro perfil"*.

CAUSA-RAIZ MEDIDA: `_populate_editor` (`app/actions/profiles_actions.py:1688`) reescreve o campo Nome sem nenhuma guarda, e é disparado pelo sinal `changed` da seleção (`:543`), que a janela emite PROGRAMATICAMENTE em três caminhos. Não existe flag de supressão — embora o padrão exista no mesmo arquivo para o `advanced_switch` (`_suppress_advanced_toggle`).

OS TRÊS CAMINHOS, todos medidos:
1. **Autoswitch + volta para a aba Perfis.** Ela ativa `vitoria`, edita as abas, abre o jogo, o autoswitch troca para `sackboy_nativo`; ao voltar para a aba Perfis, `switch-page` → `_sync_selection_with_active_profile` (`app/app.py:900`) → `_select_profile_by_name` (`profiles_actions.py:803`) → `changed` → `_populate_editor` → **o campo Nome vira `sackboy_nativo` sozinho**. Salvar grava lá, e o trabalho dela evapora (`_edita_o_perfil_do_rascunho`, `:1879`, compara com `_active_profile_name`, que ficou em `vitoria`, então a base vem do DISCO).
2. **O "nome aleatório".** `_populate_profiles_store` (`:1635`) cai no PRIMEIRO da lista quando não há alvo (`target = select_iter if select_iter is not None else first_iter`), e `_reload_profiles_store()` é chamado SEM `select_name` em três lugares: `on_profile_remove` (`:1216`), `on_profile_reload` — o botão "Recarregar lista" (`:1329`) — e `install_profiles_tab` (`:591`). No disco dela o primeiro arquivo é `acao.json` → o editor pula para **"Ação"**.
3. **O rodapé.** `footer_actions.py:301-302` pré-preenche o diálogo com `_active_profile_name`, que envelhece pelo ramo SEM edição pendente de `_reconciliar_draft_com_perfil_ativo` (`app/app.py:793`). O `prompt_overwrite_existing` dispara, mas pergunta *"substituir sackboy_nativo?"* — o nome que a própria janela acabou de pôr. A guarda não salva ninguém: o defeito é o pré-preenchimento.

AGRAVANTE COMPOSTO: de

## RESULTADO FINAL

Reproduzi as três bancadas antes de curar (`bancada_q3.py` confirmou: campo Nome virava `sackboy_nativo`, o verde dela **evaporava**; `on_profile_reload` levava o editor para `Pragmata`; o rodapé pré-preenchia com o perfil do daemon). Curei os quatro caminhos.

**A forma escolhida (argumentada na docstring de `_ha_trabalho_no_editor`)**: as três formas sugeridas eram necessárias **juntas**, não alternativas. Suprimir só o sinal deixaria a barra azul numa linha e o editor em outra — e `on_profile_save` lê as duas, então divergentes elas viram um RENAME aos olhos do R-10 (a janela se ofereceria para apagar um perfil que ninguém tocou). Então: (1) a lista **não se move sozinha** quando há trabalho não salvo (`_select_profile_by_name` recusa; o negrito de `_mark_active_profile_row` continua dizendo qual é o ativo); (2) a repintura da lista corre marcada como programática e **não repinta o editor** — a marca cobre o `store.clear()` inteiro, porque medi que é ele quem emite `changed` no meio, não só o `select_iter`; (3) o Salvar mira `_alvo_do_salvar`, memorizado por `_populate_editor`, não o widget.

**Mordida por mordida** (arranquei cada cura e rodei; script em `scratchpad/morder.py`):
- **A** portão de `on_profile_selection_changed` → reprova `test_uma_edicao_pendente_sobrevive_a_recarga_da_lista`
- **B** recusa de `_select_profile_by_name` → reprova `test_o_campo_nome_nao_troca_sozinho`
- **C** seleção preservada em `_populate_profiles_store` → reprova `test_o_botao_recarregar_lista_nao_troca_o_perfil_do_editor`
- **D** alvo memorizado no Salvar → reprova `test_o_salvar_nao_segue_a_linha_quando_o_arquivo_dela_some`
- **E** prefill do rodapé vindo de `draft.source_name` → reprova `test_o_prefill_vem_do_rascunho_e_nao_do_perfil_ativo`
- **F** anúncio da troca silenciosa (`app.py:872`) → reprova `test_a_janela_anuncia_quando_ela_mesma_troca_o_alvo`
- **G/G2** `find_by_slug` no importar (I-1), inclusive no nome novo do renomear → reprovam os dois testes do acento

**Duas decisões medidas ao ver o teste NÃO morder**: zerar `_alvo_do_salvar` em `on_profile_remove` era ativamente nocivo (o fallback cai na linha selecionada e o Salvar seguinte viraria rename de um perfil inocente) — descartado, com a razão escrita no lugar. E o `assert app.toasts == []` de `test_gui_draft_reconcilia_perfil_ativo.py` ganhou **nota datada**: a decisão medida era não recarregar sob edição pendente; o silêncio nunca foi medido e era metade do defeito do rodapé.

**Portões**: `7083 passed, 1 skipped` (linha de base 7055 + meus 15 + 13 de outro agente no mesmo tree); `mypy` limpo; `validar-glifos --all` limpo; sem emoji. `ruff` e `validar-acentuacao` acusam **6 achados que não são meus** (`test_profile_mode.py`, `test_coop_*`, `test_home_render_state.py`, `test_auto01_*`, `test_r03_*`) — outro agente está mexendo em `home_actions.py`, `mode_transition.py`, `schema.py` e `main.glade` agora, e a suíte chegou a falhar 10 testes deles no meio da minha sessão. Meus arquivos passam nos dois. **NÃO commitei**; `git add -A` foi rodado (e inevitavelmente encenou o trabalho do outro agente também). Não toquei em `main.glade` nem nos perfis dela.
