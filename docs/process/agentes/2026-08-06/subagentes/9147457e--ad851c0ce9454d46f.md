# agente ad851c0ce9454d46f (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Revisão adversarial da onda
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, faça uma REVISÃO ADVERSARIAL do trabalho no índice do git (`git diff --cached`, ~45 arquivos, +3406/-701, sobre o HEAD `0bb92a5`).

CONTEXTO DE RISCO — e é o motivo desta revisão: **três agentes trabalharam em PARALELO no MESMO working tree**, com divisão de arquivos combinada mas sem isolamento real. Eles se viram: um relatou reprovações "que não são minhas", outro relatou que a suíte falhou 10 testes dele no meio da sessão, e o `git add -A` de cada um encenou o trabalho dos outros.

O que cada um deveria ter feito:
1. **Agente A** — curar "a janela troca o nome do editor sozinha e o Salvar grava no arquivo errado" (4 caminhos) + o importar por nome cru (I-1). Arquivos: `app/actions/profiles_actions.py`, `app/actions/footer_actions.py`, `app/app.py`, testes. **Proibido de tocar no glade.**
2. **Agente B** — co-op sempre ligado: remover o frame/botão da aba Início, matar a OPÇÃO preservando a FORMA, e entregar o gesto de recuperação (`coop.sync`) ANTES de o botão sair. Arquivos: `gui/main.glade`, `app/actions/home_actions.py`, `app/actions/mode_transition.py`, `daemon/lifecycle.py`, `daemon/ipc_handlers.py`, `utils/session.py`, `cli/cmd_coop.py`, `profiles/schema.py`, testes, `scripts/gui-captura/retratar_abas.py`. **Proibido de tocar em `profiles_actions.py`, `footer_actions.py`, `app.py`.**
3. **Agente C** — duas sprints, só `docs/`.

PROCURE, em ordem de gravidade:

1. **CLOBBER.** Alguma edição de um agente sumiu por o outro ter reescrito o arquivo? Confira especialmente arquivos que os dois poderiam ter tocado, e testes que citam mecanismos dos dois. Sinal: docstring que descreve uma cura cujo código não está lá; teste que testa algo inexistente; constante referenciada e não definida.
2. **VIOLAÇÃO DE FRONTEIRA.** Algum agente editou arquivo proibido? Confira `git diff --cached --name-only` contra a divisão acima.
3. **CURA QUE NÃO CURA.** Para o defeito do Agente A, o cenário completo da usuária é: ativa `vitoria` → edita todas as abas → abre o jogo (autoswitch troca) → volta para a aba Perfis → clica Salvar. **Monte a bancada e rode o cenário INTEIRO de ponta a ponta** (as bancadas originais estão em `/tmp/claude-1000/-mnt-Apate-Desenvolvimento-hefesto-dualsense4unix/9147457e-032a-4d1e-aa95-63cdf0d6434e/scratchpad/bancada_q3.py`, `bancada_q2.py`, `bancada_q3b.py`, rodam com `xvfb-run -a .venv/bin/python`). O trabalho dela sobrevive? Vai para o arquivo certo? Se não, é o defeito ainda

## RESULTADO FINAL

I have everything I need. Report follows.

---

# Revisão adversarial — onda de 3 agentes paralelos sobre `0bb92a5`

45 arquivos, +3406/−701. **Índice preservado do início ao fim**: usei `git worktree` (nunca `git stash`), toda arrancadura de cura foi desfeita com `git checkout -- <arquivo>` (que restaura *do índice*), e verifiquei antes e depois: 45 entradas em stage, `git diff` (não-staged) vazio. Removi o único resíduo que eu mesmo criei (`./--help/`, de uma invocação minha do `retratar_abas.py`).

**Portões, rodados por último, com a árvore restaurada:** `7083 passed, 1 skipped` (duas execuções independentes), `ruff check src/ tests/` limpo, `mypy` limpo em 163 arquivos, e os sete portões de docs/empacotamento/anonimato/versão/dados verdes.

---

## 1. CLOBBER — **nenhum** · GRAU: MEDIDO

Procurei os três sinais que você nomeou e não achei nenhum.

- **Referência órfã:** varri `coop_prep`, `COOP_PREP`, `home_coop`, `plan_coop_prep`, `apply_coop_prep`, `_on_home_renumber_clicked`, `RENUMBER_GAME_OPEN_TEXT`. Toda ocorrência sobrevivente é ou lápide comentada ou teste que afirma a **ausência** (`assert not hasattr(...)`). Zero chamada viva a símbolo removido.
- **Constante citada e não definida:** `mypy` cobre exatamente isso e passou. O ponto de contato entre os dois agentes é real — `app/actions/profiles_actions.py:23` importa de `app/actions/home_actions.py`, que o Agente B reescreveu — e sobreviveu.
- **Docstring que descreve cura ausente:** conferi as quatro mais arriscadas (`draft.source_name`, `find_by_slug`, `mesmo_slug`, `_status_toast`) uma a uma no código. Todas de pé.
- Os testes que os dois agentes editaram **ganharam** asserção em vez de perder (ex.: `test_profile_mode.py` troca `assert calls.coop == [(True,"profile")]` por `assert calls.coop == []` **mais** `assert daemon.config.coop_enabled is True`).

## 2. VIOLAÇÃO DE FRONTEIRA — **uma extrapolação, benigna** · GRAU: MEDIDO

`src/hefesto_dualsense4unix/daemon/ipc_server.py` **não está na lista de ninguém**. Foi o Agente B: 7 linhas, registro de `coop.sync` no dicionário de métodos e a entrada no cabeçalho de protocolo. Sem isso a entrega 5 dele não existe. Nada mais no arquivo mudou.

As fronteiras que importavam ficaram de pé:
- **Agente A não encostou no glade.** O diff inteiro de `main.glade` (+17/−39) é a remoção do frame de co-op — assinatura do B.
- **Agente B não encostou em `profiles_actions.py`, `footer_actions.py` nem `app.py`.**
- **Agente C ficou em `docs/`.** `packaging/` está intocado.

## 3. CURA QUE NÃO CURA — **a cura cura; a BANCADA é que mente** · GRAU: MEDIDO

Este é o achado que quase virou um falso alarme grave, e vale a íntegra.

**Rodei `bancada_q3.py` como está, contra a árvore curada. Ela imprime `>>> REPRODUZIDO`.** O campo Nome pula para `sackboy_nativo`, o Salvar grava lá, e o verde dela evapora — o defeito inteiro, aparentemente de pé.

**É artefato da bancada.** A `Janela` dela compõe `ProfilesActionsMixin` + `FooterActionsMixin`, mas `_tem_edicao_pendente` mora em `HefestoApp` (`app/app.py:822`), não nos mixins. E é exatamente esse o ramo que o portão da cura lê (`profiles_actions.py:947`). Sem o método, `_ha_trabalho_no_editor()` devolve `False` e a cura nunca chega a ser exercida. A `bancada_q2.py` já fazia certo (`_tem_edicao_pendente = HefestoApp._tem_edicao_pendente`); a `q3` não.

**Com o método real ligado, montei o cenário INTEIRO** (`/tmp/.../scratchpad/rev_bancada_e2e.py`) — ativa `vitoria` → edita lightbar **e** rumble → o jogo abre e o tique de 2 Hz roda de verdade (`HefestoApp._reconciliar_draft_com_perfil_ativo`) → volta para a aba Perfis (`_on_daemon_status_for_sync`) → Salvar:

```
[1] ATIVOU 'vitoria'.  editor Nome = 'vitoria' | _alvo_do_salvar = 'vitoria'
[2] EDITOU lightbar=(0,255,0) e rumble=0.42.  _tem_edicao_pendente() = True
[3] JOGO ABRIU.  tique NÃO recarregou; _active_profile_name = 'vitoria'
    aviso: "O perfil ativo virou 'sackboy_nativo', mas suas alterações são de 'vitoria'…"
[4] VOLTOU p/ Perfis.  editor Nome = 'vitoria' | _alvo_do_salvar = 'vitoria'
[5] SALVAR -> 'Perfil salvo: vitoria'
    vitoria.leds       [0,0,255] -> [0,255,0]      <- o verde dela
    vitoria.rumble     None      -> 0.42           <- o rumble dela
    sackboy.leds       [255,0,0] -> [255,0,0]      <- intacto
```

**O trabalho dela sobrevive e vai para o arquivo certo.** O rodapé, no mesmo estado, nasce pré-preenchido com `'vitoria'` (era `'sackboy_nativo'`) e grava em `vitoria.json` sem tocar no do jogo.

**Ação recomendada:** apagar ou corrigir a `bancada_q3.py`. Deixá-la no scratchpad como "prova de reprodução" é uma armadilha carregada para o próximo — ela grita `REPRODUZIDO` sobre um defeito curado.

## 4. MECANISMO DE CO-OP — **intacto** · GRAU: MEDIDO

`daemon/subsystems/coop.py` e `core/led_control.py` **não aparecem no diff em stage**. Nem a numeração de jogador, nem o grab por jogador.

Medi as três perguntas que você fez:
- **2+ controles ainda montam?** `should_be_active()` (coop.py:183) inalterado: `config.coop_enabled` **e** `_gamepad_device is not None`. O piso agora nasce `True`, então o gate abre mais, nunca menos. Os dois ramos continuam medidos (`test_should_be_active_gates` sobreviveu com o ramo `coop=False` explicitamente preservado como mecanismo).
- **`disable()` ainda suspende e o ciclo seguinte devolve?** Sim, e agora tem teste próprio: `disable()` → `player_count()==1`, `config.coop_enabled` **continua True**, `sync(force=True)` → `player_count()==2`. Reproduzi em bancada independente: 3 → 1 → 3.
- **`coop.sync` ressuscita o que o Steam Input suspendeu?** **Não** — e essa era a regressão mais plausível do IPC novo. `suspend_vpads_for_steam_input` chama `stop_gamepad_emulation`, que zera `daemon._gamepad_device` (`gamepad.py:1549`); com ele nulo `should_be_active()` é `False` e o ciclo só desmonta. Medido: no meio do jogo, `coop.sync` devolve `{'players': 1, 'active': False}` e não remonta nada; com o jogo fechado o ciclo seguinte traz os 3 de volta.

`set_coop_enabled(False)` ficou **inalcançável** por qualquer superfície (único chamador é `_handle_coop_set`, que recusa antes). O ramo continua escrito, que é o certo: é o mecanismo.

## 5. MORDIDA FALSA — **nenhuma. 8/8 mordem** · GRAU: MEDIDO

Arranquei cada cura, rodei o teste citado, vi reprovar, devolvi.

| # | cura arrancada | reprovou |
|---|---|---|
| 1 | `on_profile_save` volta a `_selected_profile_name()` | `test_o_salvar_nao_segue_a_linha_quando_o_arquivo_dela_some` |
| 2 | `_populate_profiles_store` volta a cair no primeiro | `test_o_botao_recarregar_lista_nao_troca_o_perfil_do_editor` + `test_recarregar_e_salvar_nao_grava_no_primeiro_da_lista` |
| 3 | portão de `on_profile_selection_changed` removido | `test_uma_edicao_pendente_sobrevive_a_recarga_da_lista` + 1 |
| 4 | importar volta a comparar nome cru (I-1) | `test_o_acento_dela_nao_passa_batido` × 3 (Navegação, AÇÃO, FPS) |
| 5 | prefill do rodapé volta a `_active_profile_name` | `test_o_prefill_vem_do_rascunho_e_nao_do_perfil_ativo` |
| 6 | `DaemonConfig.coop_enabled` volta a `False` | `test_o_piso_do_coop_nasce_ligado_no_dataclass` |
| 7 | `coop.set` volta a aceitar `enabled:false` | `test_desligar_e_recusado_em_voz_alta` |
| 8 | `coop.sync` desregistrado do IPC | `test_o_gesto_de_recuperacao_ganhou_dono_antes_de_o_botao_sair` |

**Duas ressalvas honestas, nenhuma delas uma mordida falsa:**
- A mordida 6 morde **um** teste, e é asserção de **forma** (`DaemonConfig().coop_enabled is True`), não de comportamento de boot. É deliberado — a sprint explica que o sósia (`run()` forçando `True`) foi removido justamente para o dataclass virar o dono único. Mas o aceite é magro: nenhum teste mede *"a máquina dela sobe com co-op ligado"* de ponta a ponta.
- A mordida 1 morde 1 de 15. A cura do Agente A é em três camadas que se sustentam, e cada camada tem a sua própria mordida (2 e 3). Isso é bom desenho, não mordida fraca.

## 6. REGRESSÃO SILENCIOSA — **PRÉ-EXISTENTE** · GRAU: MEDIDO

**Não reproduzi falha nenhuma na suíte.** `pytest -q` completo, duas vezes: `7083 passed, 1 skipped`. Zero falhas.

E o diagnóstico do teste citado é o **oposto** do relatado:

- `test_keyboard_wire_up.py::test_poll_loop_chama_dispatch_keyboard_com_buttons_pressed` **falha 10/10 vezes quando rodado ISOLADO** (`assert 0 >= 8` — zero ticks em 60 ms) e **passa** com o arquivo inteiro ou com a suíte. É custo de **boot/import a frio**, não ordem — a favor disso: `pytest-randomly` **não está instalado** neste venv, então `-p no:randomly` é no-op e a ordem já é determinística.
- **Montei um worktree separado em `0bb92a5`** (índice intocado, verificado antes e depois) e rodei lá com `PYTHONPATH` apontando para a fonte do worktree — confirmei a fonte com `__file__`. **Mesmo comportamento, mesma mensagem, 10/10 falhas isoladas, arquivo inteiro passando.**

**Veredito: pré-existente, e é flakiness de tempo, não de ordem.** A onda não toca `_poll_loop` nem o teclado; a única mudança no caminho de boot do `run()` **removeu** trabalho (o `load_coop_enabled()` saiu). Hipótese que explica o que vocês viram: três agentes martelando a mesma máquina fazem um teste de 60 ms passar a perder a corrida.

## 7. CONTRATO PÚBLICO — **preservado, com uma quebra deliberada e documentada** · GRAU: MEDIDO

Exercitei os handlers de verdade:

- `coop.set` existe. `players` está nas **duas** respostas: `{'status':'ok','enabled':True,'players':3}` e `{'status':'recusado','enabled':True,'players':3,'motivo':'…'}`. A flag fica ligada após um `false`.
- `coop.sync` → `{'status':'ok','players':3,'active':True}`, e usa `force=True`.
- **A GUI não chama mais `coop.set`** — o botão saiu, e o que a tela dispara agora é `coop.sync`. Quem ainda chama com `{"enabled": True}` é a CLI (`cmd_coop.py:57`). Isso é a entrega, não um esquecimento.
- **Campo `coop` do perfil: aceito.** `ProfileModeConfig` continua com `extra="forbid"` **e** com `coop: bool = True`. Validei `coop:false` no modelo e num `Profile` inteiro, e os dois presets de fábrica (`coop_local.json`, `sackboy_nativo.json`) carregam.
- **`coop off` quebra script — de propósito e por escrito.** Sai com **código 2** (era 0). Está documentado em `docs/usage/cli.md:297-299` e no CHANGELOG, e a mensagem ensina a saída real. É a decisão certa (um `0` silencioso seria a CLI mentindo), mas é quebra de contrato e merece a linha de release que já tem.
- O applet nunca chamou `coop.set(false)` (já tinha saído na LEIGO-01); `packaging/` está fora do diff.

---

## Achados menores — relatados, não consertados

**M1 · O Agente A não documentou nada.** `NUNCA-TROCA-O-ALVO-01` tem **22 citações no código e 0 em `docs/`**. Não há sprint doc, não há entrada no CHANGELOG (o único bloco novo é o do co-op), e o roteiro `PEDIDOS-DELA-01` não a cobre (os seis pedidos são outros). O `COOP-SEM-INTERRUPTOR-01` do Agente B, em contraste, tem três âncoras — sprint marcada CUMPRIDO com tabela de sete entregas, `docs/protocol/ipc-unix-socket.md` e CHANGELOG. É o buraco típico de trabalho em paralelo: cada um documentou o seu, e a cura de uma queixa literal dela (*"clico em salvar e ele salva com um nome aleatório ou de outro perfil"*) fica sem rastro em `docs/`. · GRAU: MEDIDO

**M2 · O portão da cura falha ABERTO.** `src/hefesto_dualsense4unix/app/actions/profiles_actions.py:947-953`:

```python
checar = getattr(self, "_tem_edicao_pendente", None)
if callable(checar):
    try:
        if bool(checar()):
            return True
    except Exception:
        return False        # <- "não sei" vira "não há trabalho a proteger"
```

Medi a consequência: forcei `_tem_edicao_pendente` a estourar e **o defeito inteiro voltou** — editor pula para `sackboy_nativo`, o Salvar grava lá, o verde e o rumble dela somem, sem diálogo. Em produção não achei gatilho: `self.draft` nasce em `app.py:275` e `_draft_baseline` em `:303`, ambos no `__init__`, e `baseline is not None and …` curto-circuita enquanto o baseline é `None`. Então o alcance é latente. Mas para uma guarda cujo único trabalho é proteger trabalho não salvo, o default deveria ser **fechar** (`return True`), não abrir. · GRAU: MEDIDO (consequência) + SEM GATILHO CONHECIDO (alcance)

**M3 · Duas respostas para a mesma pergunta, na mesma onda.** `footer_actions.py:331` (`_perfil_que_as_abas_editam`) argumenta, com razão medida, que `_active_profile_name` é a fonte **errada** para "qual perfil o rascunho representa", e passa a `draft.source_name`. Mas `profiles_actions.py:2132` (`_edita_o_perfil_do_rascunho`) continua respondendo a mesmíssima pergunta com `_active_profile_name`, e a sua docstring ainda afirma isso como doutrina. Tentei construir um cenário de perda e não consegui — nos caminhos que percorri as duas fontes andam juntas. Fica como inconsistência de doutrina a resolver antes que divirjam. · GRAU: SUSPEITA COM MECANISMO

**M4 · Nome de atributo defasado.** `home_actions.py:839,845,994,1038` seguem `self._home_renumber_btn` / `_home_renumber_hint` depois de o botão virar "Reconciliar jogadores" e ganhar um segundo passo. Cosmético, mas é a classe de dívida que esta casa persegue. · GRAU: MEDIDO

**M5 · `bancada_q3.py` virou armadilha.** Ver item 3. · GRAU: MEDIDO

---

## Veredito

**Pode ser commitada como está** — os três trabalhos convivem sem clobber, as fronteiras seguraram, a cura do Salvar foi verificada de ponta a ponta no cenário real dela, o mecanismo de co-op está intocado, as oito mordidas mordem e a única falha de suíte relatada é pré-existente a `0bb92a5` — mas **antes de fechar a leva, dê ao `NUNCA-TROCA-O-ALVO-01` a sprint e a linha de CHANGELOG que ele não tem** (M1), porque é a única coisa nesta onda que a casa exige e que não foi entregue.
