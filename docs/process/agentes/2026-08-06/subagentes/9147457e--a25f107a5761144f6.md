# agente a25f107a5761144f6 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Escrever PERFIL-REESCRITO-NA-PARTIDA-01
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, ESCREVA uma sprint que falta.

ANTES DE ESCREVER, leia como exemplar (a auditoria chamou de exemplar do estilo da casa):
- docs/process/sprints/2026-08-05-TRAVA-QUE-SOLTA-TARDE-01-o-gesto-explicito-e-vitima-da-propria-trava.md
E leia a síntese consolidada da leva:
- docs/process/estudos/2026-08-05-o-sistema-de-perfis-o-que-dezessete-agentes-mediram.md

CRIE o arquivo:
docs/process/sprints/2026-08-05-PERFIL-REESCRITO-NA-PARTIDA-01-o-perfil-dela-era-reescrito-sozinho-no-meio-da-partida.md

O mecanismo tem 23 citações na árvore. Levante-as você mesmo com:
  git grep -n "PERFIL-REESCRITO-NA-PARTIDA-01"
e leia CADA docstring citada, além do `git diff --cached` dos arquivos envolvidos
(profiles/autoswitch.py, profiles/manager.py, daemon/lifecycle.py, tests/unit/test_perfil_reescrito_na_partida_01.py).

São 6 itens: (1) `_store_de_estado`/`_perfil_corrente` sincronizam a crença do autoswitch com store.active_profile; (2) gate de catch-all no ramo que LIGA a supressão; (3) rumble_policy_applier ganha `profile=` e as duas guardas; (4) relatório da ativação carrega categorias travadas e modo_jogo_padrao; (5) log `secoes=` inteiro; (6) `_reapply_last_profile` ganha mode/rumble/speaker applier.

A sprint precisa de: sintoma na língua dela, evidência (journal/medição — a síntese traz o journal com sackboy_nativo em priority 200 → 22 → 191), causa-raiz por item, alcance, por que a suíte não pegou, a cura por item, a tabela item→cura→teste com a MORDIDA verificada, e o que fica aberto.

REGRAS DA CASA, obrigatórias:
- Português do Brasil com acentuação correta (há portão que reprova). O NOME DO ARQUIVO vai sem acento.
- Nada de MAC real; máscara da casa é octetos 4 e 5 zerados.
- Não afirme o que não puder provar: marque grau (MEDIDO / SUSPEITA COM MECANISMO / SEM PROVA).
- "Não se apaga decisão medida" — se algo caducou, nota datada.
Escreva o arquivo com a ferramenta Write. Retorne só um resumo de 5 linhas do que escreveu.

## RESULTADO FINAL

Escrevi `docs/process/sprints/2026-08-05-PERFIL-REESCRITO-NA-PARTIDA-01-o-perfil-dela-era-reescrito-sozinho-no-meio-da-partida.md` (staged), no estilo do exemplar: sintoma na língua dela, prova por journal do daemon de produção (PID 1670), causa-raiz por item, alcance, por que a suíte não pegou, cura por item, tabela item→cura→teste e o que fica aberto — com grau declarado (MEDIDO / SUSPEITA COM MECANISMO / SEM PROVA) em cada afirmação.

**Evidência nova que levantei eu mesmo** (o estudo não a tinha nesta forma): `autoswitch_cadeado_cedeu_a_regra_de_jogo current=` vazio às 02:24:41 e `profile_autoswitch from_=None` às 02:24:42 com `vitoria` ativo desde 04/08 23:40:18 — crença **2 h 44 min** atrasada, em dois campos independentes; `emulation_suppressed_changed suppressed=True` quatro vezes e **zero** `suppressed=False` na vida inteira do daemon; `profile_rumble_policy_reverted` 130 µs antes do tique que leu `wm_class=steam_app_1599660`; e a leitura direta do disco dela hoje confirmando `sackboy_nativo.json` = catch-all / 191 / `suppress: true` (nenhum MAC citado).

**Mordida verificada por mim hoje**, não herdada: arranquei cada uma das sete curas na árvore de trabalho, rodei, e devolvi com `git checkout --` a partir do índice (nunca `git stash` — a leva está staged e não commitada). Reprovas por item: 1→1, 2→2, 3→2, 4a→2, 4b→1, 5→2, 6→2 = **dez testes distintos** dos 18; ao fim, árvore conferida byte a byte contra cópia de segurança e **18 verdes**, `git diff -- src/ tests/` vazio.

Dois achados de bancada que reforçam o caso: arrancado o item 1, o teste emite `profile_autoswitch adiado=[] from_=None to=sackboy_nativo wm_class=steam_app_1599660` — a assinatura do journal dela; arrancado o item 3, emite `profile_rumble_policy_reverted mult=0.7 policy=balanceado`, com os mesmos valores das 02:24:42/02:48:56. E o "por que a suíte não pegou" é medido por grep: `test_native_mode.py` **monkeypatcha `_reapply_last_profile` em três lugares** e só afirma "foi chamado" (`:78`); `catch_all=True` aparece **uma vez** em `test_profile_suppression_lock.py` (`:276`) e só no ramo que LIBERA; `test_profile_rumble_policy.py` tem **zero** ocorrências de catch-all/janela-de-jogo.

Portões limpos sobre o arquivo novo (rodados após `git add`): acentuação 0, glifos 0, referências-docs OK (189 documentos, nenhuma morta), `check_anonymity.sh` OK. Marquei o item 6 como **SEM PROVA** de episódio ao vivo (não há `native_mode_changed` na sessão dela) e o item 5 como SUSPEITA COM MECANISMO no efeito, porque os `ignorado_*` daquela noite caíram em trocas manuais, que não emitem `profile_autoswitch` — e registrei nota datada de 05/08 dizendo que a decisão da FEAT-PROFILE-MODE-01 **não** caducou, só a implementação por remoção.
