# agente a86c76bdaf91ce661 (sessao 9147457e)

**tipo:** Explore
**tarefa:** Mapear regra de match e prioridade
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, faça um mapeamento MUITO minucioso do conceito de "regra de match" (MatchAny, MatchAll, match rules) e de "prioridade" (priority) nos perfis.

Preciso saber, com caminho:linha:

1. Onde a regra de match é DEFINIDA (classes/dataclasses: MatchAny, MatchAll, o que existe), em qual módulo.
2. Onde a regra de match é SERIALIZADA e DESSERIALIZADA (leitura/escrita em disco — TOML/JSON/YAML?).
3. Todos os pontos que CONSTROEM um perfil novo e decidem a regra de match — em especial qualquer lugar que use `MatchAny()` vazio como padrão.
4. Onde `priority` é definido, lido, escrito, e onde é usado para ordenar/decidir qual perfil ganha (autoswitch).
5. O caminho do "salvar por cima" pela aba Perfis vs o caminho do "salvar" pelo rodapé (footer) — quais funções, quais arquivos, e onde eles divergem.

Não altere nada. Retorne uma lista estruturada de caminho:linha com uma frase por item. Seja muito thorough — vasculhe src/, tests/ e docs/process/sprints/.

## RESULTADO FINAL

Mapeamento completo. Tudo abaixo é caminho absoluto:linha, uma frase por item.

---

## 1. Onde a regra de match é DEFINIDA

Módulo dono: `src/hefesto_dualsense4unix/profiles/schema.py` (pydantic, união discriminada por `type`).

- `/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/profiles/schema.py:51` — `class MatchCriteria(BaseModel)`, `type: Literal["criteria"]`, campos `window_class: list[str]`, `window_title_regex: str|None`, `process_name: list[str]`.
- `.../profiles/schema.py:70` — `MatchCriteria.matches()`: AND entre campos preenchidos, OR dentro das listas, sem condição nenhuma devolve `False`.
- `.../profiles/schema.py:24` — `_casa_sem_caixa()`: comparação case-insensitive (R-12) usada por `window_class`/`process_name`; alvo vazio nunca casa.
- `.../profiles/schema.py:87` — `re.search(pattern, title, re.IGNORECASE)` para `window_title_regex`.
- `.../profiles/schema.py:97` — `class MatchAny(BaseModel)`, `type: Literal["any"]` — o sentinel catch-all (V2-8).
- `.../profiles/schema.py:104` — `MatchAny.matches()` devolve sempre `True`.
- `.../profiles/schema.py:108` — `class MatchManual(BaseModel)`, `type: Literal["manual"]` — sentinel "só manual", R-12 item 3.
- `.../profiles/schema.py:136` — `MatchManual.matches()` devolve sempre `False`.
- `.../profiles/schema.py:140` — `Match = MatchCriteria | MatchAny | MatchManual` (a união; **não existe MatchAll** neste repositório).
- `.../profiles/schema.py:466` — `match: Match = Field(discriminator="type")` no `Profile`.
- `.../profiles/schema.py:622` — `Profile.matches()` delega para `self.match.matches()`.
- `.../profiles/schema.py:625` — `@property e_catch_all`: `MatchAny` OU `MatchCriteria` com os três campos vazios contam igual (R-01); `MatchManual` fica de fora de propósito.
- `.../profiles/schema.py:658` — `perfil_e_regra_de_jogo()`: exige `criteria` com critério real E a `wm_class steam_app_<id>` em foco listada em `window_class`.
- `.../profiles/schema.py:722` — `perfil_declara_modo_de_jogo()`, predicado irmão que consulta `e_catch_all`.
- `.../profiles/schema.py:765` — `__all__` exporta `MatchAny`, `MatchCriteria`, `MatchManual`, `perfil_e_regra_de_jogo`.
- `.../profiles/simple_match.py:52` — `SIMPLE_MATCH_PRESETS: dict[str, MatchCriteria | MatchAny]` — o vocabulário do radio "Aplica a".
- `.../profiles/simple_match.py:53` — `"any": MatchAny()` é o primeiro preset da tabela.
- `.../profiles/simple_match.py:77` — `from_simple_choice()` traduz escolha do radio → `MatchCriteria`/`MatchAny`.
- `.../profiles/simple_match.py:114` — `detect_simple_preset()` faz o caminho inverso (leitura tolerante; `MatchManual` → `None`).
- `.../profiles/simple_match.py:183` — `_detect_steam_appid()` reconhece `steam_app_<id>` como "jogo da Steam".

---

## 2. Onde a regra de match é SERIALIZADA / DESSERIALIZADA

Formato em disco: **JSON**, um arquivo por perfil (`<slug(name)>.json`) no `profiles_dir` XDG. Não há TOML nem YAML para perfis.

- `/mnt/Apate/.../src/hefesto_dualsense4unix/profiles/loader.py:19` — `import json`, único formato do módulo.
- `.../profiles/loader.py:488` — `_profile_path()`: o nome do arquivo é `slugify(profile.name) + ".json"`.
- `.../profiles/loader.py:500` — `_read_profile()`: `json.loads(path.read_text())` → `Profile.model_validate` (aqui a união discriminada reconstrói `MatchAny`/`MatchCriteria`/`MatchManual`).
- `.../profiles/loader.py:506` — `load_profile()` (slug direto → slug normalizado → varredura por `name`).
- `.../profiles/loader.py:559` — `load_all_profiles()`: `sorted(directory.glob("*.json"))` — esta ordem é o terceiro termo do desempate do autoswitch.
- `.../profiles/loader.py:585` — `audit_profiles()` lê os JSON crus e reporta os que não validam.
- `.../profiles/loader.py:787` — `save_profile(profile, *, origem=None)` — **o único escritor de perfil do projeto**.
- `.../profiles/loader.py:830` — `payload = profile.model_dump(mode="json")` (aqui `match` vira `{"type": ...}`).
- `.../profiles/loader.py:834` — omite seções opcionais `None` (compat de downgrade); `match` e `priority` NUNCA são omitidos.
- `.../profiles/loader.py:844` — `FileLock` + `_atomic_write_json(path, payload)` na linha 850.
- `.../profiles/loader.py:749` — `_estado_gravado()`: lê do disco o **discriminador do `match`** e a **`priority`** ANTES da gravação, para o journal.
- `.../profiles/loader.py:855` — `_registrar_gravacao()` emite `profile_salvo` com `match_antes`/`match_depois`/`priority_antes`/`priority_depois`/`origem` (linhas 873-887).
- `.../profiles/loader.py:703` — `_arquivar_versao()` guarda os bytes anteriores em `historico/<carimbo>.json`.
- `.../profiles/loader.py:890` — `restaurar_do_historico()` reescreve os BYTES originais (não reserializa).
- `.../profiles/loader.py:253` — `migrate_coop_local_match()`: migração que reescreve `match` e `priority` a partir do asset.
- `.../profiles/loader.py:283` — `data["priority"] = asset_data.get("priority", ...)` dentro dessa migração.
- `.../profiles/loader.py:385` / `:434` — `migrate_modo_jogo_nos_presets()` corrige `priority` 45 → asset (75) do `coop_local`.
- `.../profiles/loader.py:130` — `seed_default_presets()` copia os JSON de `assets/profiles_default/`.
- `/mnt/Apate/.../assets/profiles_default/*.json` — presets de fábrica com `priority` gravada: `fallback.json:5` (0), `meu_perfil.json:5` (1), `bow.json:9` (10), `navegacao.json:15` (50), `corrida.json:16` / `esportes.json:15` (55), `fps.json:15` / `point_and_click.json:10` (60), `acao.json:14` (65), `aventura.json:15` (70), `coop_local.json:8` (75), `sackboy_nativo.json:12` (80).
- `/mnt/Apate/.../src/hefesto_dualsense4unix/cli/cmd_profile.py:313` — a CLI de histórico relê `match.type` e `priority` do JSON cru para a tabela de versões.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/footer_actions.py:480` — importação: `json.loads(...)` + `Profile.model_validate(raw)` (desserialização fora do loader).
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/footer_actions.py:576` — "Restaurar Padrão" desserializa o asset `meu_perfil.json`.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/daemon/ipc_handlers.py:505` — `profile.list` expõe `match_type` (só o discriminador) e `priority` (linha 500) pelo IPC.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/ipc_bridge.py:236` — fallback local do `profile.list` emite `priority` + `match_type`.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/tui/app.py:70` e `:215` — a TUI transporta e mostra `priority`.

---

## 3. Todos os pontos que CONSTROEM perfil novo e decidem a regra de match

### 3a. `MatchAny()` vazio como padrão — os lugares críticos

- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/draft_config.py:578-582` — **o coração do problema**: `match = self.source_match if (mesmo_perfil and self.source_match is not None) else MatchAny()` — se o gate `mesmo_perfil` responder `False`, a regra de janela é substituída por `MatchAny()` em silêncio.
- `.../app/draft_config.py:562` — o gate: `mesmo_perfil = self.source_name is not None and (name == self.source_name or mesmo_slug(name, self.source_name))`; fotografia envelhecida = `False` para sempre.
- `.../app/draft_config.py:503` — o import de `MatchAny` dentro de `to_profile`.
- `.../app/draft_config.py:551` — comentário declarando que "`MatchAny()` para nome novo é deliberado" (contrato do diálogo do rodapé, que não tem campo de regra).
- `/mnt/Apate/.../src/hefesto_dualsense4unix/profiles/simple_match.py:111` — `return SIMPLE_MATCH_PRESETS.get(choice, MatchAny())` — chave de radio desconhecida degrada para catch-all.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/cli/cmd_profile.py:180` — `profile create --fallback` → `match = MatchAny()` e `priority = 0` (linha 181).

### 3b. Construção de `Profile` novo (todas as ocorrências em `src/`)

- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/draft_config.py:575` — `profile = Profile(...)` dentro de `to_profile()`, o construtor usado pelos três botões do rodapé.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/cli/cmd_profile.py:193` — `Profile(...)` do `profile create`; ramos em `:179` (fallback→MatchAny), `:185` (`--manual`→MatchManual), `:187` (MatchCriteria a partir de `--match-class/--match-regex/--match-exe`).
- `/mnt/Apate/.../src/hefesto_dualsense4unix/cli/cmd_profile.py:205` — aviso quando `create` produz um `criteria` que nunca casa.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/emulation_actions.py:404` — `Profile(name="sonda", match=match)` construído só como SONDA para consultar `e_catch_all` do `source_match` do rascunho.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/profiles_actions.py:2150` — `Profile.model_validate(base)` no fim de `_build_profile_from_editor`.

### 3c. Aba Perfis — como a regra é escolhida

- `.../app/actions/profiles_actions.py:1947` — `_build_profile_from_editor()`, a montagem completa.
- `.../app/actions/profiles_actions.py:1953-1975` — modo avançado: monta `MatchCriteria` a partir dos três campos CSV/regex.
- `.../app/actions/profiles_actions.py:1961-1969` — **avançado com os três campos vazios → `MatchManual()`** (não `MatchCriteria` vazio).
- `.../app/actions/profiles_actions.py:1976-1979` — modo simples: `from_simple_choice(choice, custom_name)`.
- `.../app/actions/profiles_actions.py:2139-2141` — `regra_final = regra_do_disco if (regra_do_disco is not None and not regra_mexida)` — a guarda SALVAR-NAO-REBAIXA que impede o editor de rebaixar a regra sem gesto.
- `.../app/actions/profiles_actions.py:2127-2135` — releitura da fotografia do disco quando o alvo existe mas o editor a esqueceu (SALVAR-NAO-REBAIXA-02).
- `.../app/actions/profiles_actions.py:1781` — `_regra_foi_mexida()` (sem fotografia → `True`, ou seja, os widgets vencem).
- `.../app/actions/profiles_actions.py:1746` — `_assinatura_da_regra_no_editor()`, a fotografia dos 5 widgets de regra.
- `.../app/actions/profiles_actions.py:1738-1739` — `_populate_editor` grava `_regra_do_disco` e a assinatura de abertura.
- `.../app/actions/profiles_actions.py:1829` — `_esquecer_a_fotografia_do_editor()` (chamado por "Novo perfil").
- `.../app/actions/profiles_actions.py:1049` — `on_profile_new` faz `self._select_radio("any")` — perfil novo nasce apontando para "Qualquer"/MatchAny.
- `.../app/actions/profiles_actions.py:1108` — `_aplicar_nascimento_com_jogo()`: com jogo Steam em foco, troca o radio para `steam_game` e preenche o appid (linhas 1148-1150).
- `.../app/actions/profiles_actions.py:1386-1403` — aviso `confirm_downgrade_match_to_any` quando `profile.match` virou `MatchAny` e o original não era.

### 3d. Rodapé — como a regra é escolhida

- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/footer_actions.py:415-421` — `_construir()`: `draft.to_profile(nome, priority=...)` e depois **`perfil.model_copy(update={"match": existente.match})` quando o perfil já existe em disco** (REGRA-NAO-SE-PERDE-01, 05/08).
- `.../app/actions/footer_actions.py:388-410` — a docstring que declara a divisão: "nasce sempre" vale só para quem NASCE; quem já existe herda o próprio `match` do DISCO (não da fotografia).
- `.../app/actions/footer_actions.py:531` — importar grava o `Profile` lido do arquivo tal qual (o `match` vem do JSON importado).
- `.../app/actions/footer_actions.py:575-577` — "Restaurar Padrão" constrói o `Profile` do asset (match do asset).

---

## 4. `priority`: definição, leitura, escrita e uso na decisão

### Definição / faixa
- `/mnt/Apate/.../src/hefesto_dualsense4unix/profiles/schema.py:467` — `priority: int = 0` (o default do esquema; sem validação de faixa).
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/profiles_actions.py:78` — `PRIORIDADE_MAXIMA = 200` (teto da escala da janela; o glade tem de acompanhar).
- `.../app/actions/profiles_actions.py:83` — `_FOLGA_ACIMA_DO_CATCH_ALL = 10`.
- `.../app/actions/profiles_actions.py:311` — `QUEDA_DE_PRIORIDADE_QUE_PEDE_AVISO = 10`.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/footer_actions.py:131` — `_PISO_ACIMA_DOS_CATCH_ALL = 15` (fallback do rodapé quando o mixin da aba Perfis não está montado; deliberadamente não-zero).
- `/mnt/Apate/.../src/hefesto_dualsense4unix/profiles/sanidade.py:43-44` — `PRIORIDADE_MINIMA = 0`, `PRIORIDADE_MAXIMA = 200` (cópia declarada da faixa da janela).
- `.../profiles/sanidade.py:53` — `PRIORIDADE_DE_FUNDO = 0`.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/cli/cmd_profile.py:157` — `--priority` default **5** no `profile create` (o único literal 5 que sobrou).

### Quem DECIDE o vencedor (autoswitch)
- `/mnt/Apate/.../src/hefesto_dualsense4unix/profiles/manager.py:817-825` — `_chave_de_selecao()` devolve `(not profile.e_catch_all, profile.priority)`; **especificidade antes de prioridade** (R-01).
- `.../profiles/manager.py:798` — `candidates = [p for p in load_all_profiles() if p.matches(window_info)]`.
- `.../profiles/manager.py:805-813` — veto R-21: janela `steam_app_*` com TODOS os candidatos catch-all → `None` + `MOTIVO_JOGO_SEM_PERFIL_PROPRIO` (a prioridade não salva um catch-all aqui).
- `.../profiles/manager.py:843-892` — `_melhor_candidato()`: `max(chave)`, e no empate vence o INCUMBENTE (EMPATE-01); sem incumbente, o primeiro da ordem alfabética do `glob`.
- `.../profiles/manager.py:890` — log `profile_select_empate_resolvido` com `prioridade=vencedor.priority`.
- `.../profiles/manager.py:250` — `activate()` loga `priority=profile.priority`.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/profiles/autoswitch.py:410` — `_selecionar_com_motivo()` (o autoswitch não lê `priority` diretamente; delega ao manager).
- `.../profiles/autoswitch.py:332` / `:509` / `:674` — usa `e_catch_all` (não `priority`) para debounce e cadeado.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/daemon/lifecycle.py:1762` — `_perfil_tem_opiniao()`: catch-all não reverte modo/supressão.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/daemon/connection.py:179` — restore de boot só aceita perfis "sempre" (`MatchAny`), RESTORE-ESCOPO-01.

### Leitura/escrita de priority na GUI
- `.../app/actions/profiles_actions.py:1670-1671` — `_populate_editor`: `prio = max(0, min(PRIORIDADE_MAXIMA, profile.priority))` e `set_value(prio)` (perfil acima de 200 abre CLAMPADO).
- `.../app/actions/profiles_actions.py:1740-1741` — grava `_prioridade_do_disco` (valor real) e `_prioridade_ao_abrir` (valor clampado).
- `.../app/actions/profiles_actions.py:1777` — `_on_prioridade_tocada()` marca o gesto.
- `.../app/actions/profiles_actions.py:1796-1812` — `_prioridade_foi_mexida()` (gesto OR diferença de valor; sem fotografia → `True`).
- `.../app/actions/profiles_actions.py:1950` — `priority = int(self._get("profile_priority_scale").get_value())`.
- `.../app/actions/profiles_actions.py:2136-2138` — `prioridade_final = prioridade_do_disco` quando não houve gesto.
- `.../app/actions/profiles_actions.py:2143-2149` — `base.update({"name", "priority", "match"})`, a linha final da montagem.
- `.../app/actions/profiles_actions.py:1844-1858` — `_prioridade_acima_dos_catch_all()` = `max(priority dos e_catch_all do cache) + 10`, clampado a `[0, 200]`.
- `.../app/actions/profiles_actions.py:1048` — "Novo perfil" zera a escala (`set_value(0)`), antes de `_esquecer_a_fotografia_do_editor()` (a ordem importa — SALVAR-NAO-REBAIXA-02).
- `.../app/actions/profiles_actions.py:1151-1157` — nascimento com jogo em foco escreve a prioridade calculada na escala e explica no toast.
- `.../app/actions/profiles_actions.py:1407-1420` — `queda_de_prioridade_pede_aviso(original.priority, profile.priority)` → `gui_dialogs.confirm_downgrade_priority`.
- `.../app/actions/profiles_actions.py:314-318` — a função pura do limiar.
- `.../app/actions/profiles_actions.py:1601` — a coluna da lista mostra `profile.priority`.
- `.../app/actions/profiles_actions.py:216-238` — `perfis_em_disputa()` / `vencedor_da_disputa()`: espelho da regra do manager para a coluna "Quando usar".
- `.../app/actions/profiles_actions.py:284-294` — `explicacao_da_disputa()` cita a prioridade do vencedor no tooltip.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/gui_dialogs.py:154` — `confirm_downgrade_priority(parent, name, de, para)`.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/gui_dialogs.py:105` — `confirm_downgrade_match_to_any(parent, name, regra_atual)`.

### Priority no rodapé e no draft
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/draft_config.py:331` — campo `source_priority: int | None`.
- `.../app/draft_config.py:458` — `from_profile` tira a fotografia: `source_priority=profile.priority`.
- `.../app/draft_config.py:568-574` — resolução: **origem (se `mesmo_perfil`) > argumento do chamador > default do ESQUEMA** — note que o `priority=` passado pelo rodapé PERDE para `source_priority` quando o gate `mesmo_perfil` responde `True`.
- `.../app/draft_config.py:660` — `with_profile_identity` reaponta `source_priority` para o perfil recém-gravado.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/footer_actions.py:334-369` — `_prioridade_do_save(existente)`: existe em disco → `int(existente.priority)`; não existe → `_prioridade_acima_dos_catch_all()` ou o piso 15.
- `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/profile_writer.py:213-216` — assert da invariante `draft.source_priority == profile.priority`.

### Diagnóstico (doctor) sobre priority
- `.../profiles/sanidade.py:137` — `_tem_dispensa`: catch-all com `priority <= 0` é perdoado.
- `.../profiles/sanidade.py:142-170` — `catch-all com priority >= a de um específico` é ERRO com receita ("baixe para 0").
- `.../profiles/sanidade.py:196-228` — catch-all de nome próprio acima do piso vira aviso.
- `.../profiles/sanidade.py:245-266` — `_prioridades_empatadas()`: dois disputáveis na mesma prioridade = sorteio.
- `.../profiles/sanidade.py:279-299` — `_prioridade_fora_da_faixa()`: fora de 0-200 "este número NÃO veio do controle de prioridade".
- `.../profiles/sanidade.py:311-325` — vários coringas ao mesmo tempo.

---

## 5. "Salvar por cima" pela aba Perfis × "Salvar" pelo rodapé

### Caminho do RODAPÉ (footer)
1. `/mnt/Apate/.../src/hefesto_dualsense4unix/app/app.py:412` — registro do handler `"on_save_profile"`.
2. `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/footer_actions.py:285` — `on_save_profile()`: só pede o NOME (`gui_dialogs.prompt_profile_name`, linha 302) — **não há campo de regra nem de prioridade**.
3. `.../app/actions/footer_actions.py:307-319` — worker lê o disco (`load_all_profiles`) e `find_by_slug(nome, existentes)` decide se já existe.
4. `.../app/actions/footer_actions.py:320` — se existe, `gui_dialogs.prompt_overwrite_existing` (é o ÚNICO diálogo de proteção deste caminho).
5. `.../app/actions/footer_actions.py:329` — `self._persist_profile_async(nome, existente=alvo)`.
6. `.../app/actions/footer_actions.py:413` — `prioridade = self._prioridade_do_save(existente)`.
7. `.../app/actions/footer_actions.py:418` — `draft.to_profile(nome, priority=prioridade)` → `/mnt/Apate/.../src/hefesto_dualsense4unix/app/draft_config.py:463`.
8. `.../app/actions/footer_actions.py:419-420` — `perfil.model_copy(update={"match": existente.match})` quando o alvo já existe.
9. `.../app/actions/footer_actions.py:423` — entrega ao funil `_gravar_perfil_async`.
10. `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/profile_writer.py:69` — o funil; grava em `:116` com `save_profile(profile, origem=f"janela:{evento}")`, evento `"footer_save_profile"`.
11. `.../app/actions/profile_writer.py:165-193` — `_reapontar_rascunho()` → `with_profile_identity` + `_draft_baseline`; invariante conferida em `:195`.

### Caminho da ABA PERFIS
1. `/mnt/Apate/.../src/hefesto_dualsense4unix/app/actions/profiles_actions.py:1313` — `on_profile_save()`.
2. `.../app/actions/profiles_actions.py:1315` — `_build_profile_from_editor()` (nome, prioridade da escala, regra dos widgets, base do rascunho quando é o mesmo perfil — linhas 2019-2046).
3. `.../app/actions/profiles_actions.py:1339-1350` — diálogo de RENAME vs CÓPIA (`_prompt_rename_or_copy`) — não existe no rodapé.
4. `.../app/actions/profiles_actions.py:1357-1372` — `prompt_overwrite_existing` só quando NÃO é edição in-place.
5. `.../app/actions/profiles_actions.py:1386-1403` — `confirm_downgrade_match_to_any` — **não existe no rodapé**.
6. `.../app/actions/profiles_actions.py:1407-1420` — `confirm_downgrade_priority` — **não existe no rodapé**.
7. `.../app/actions/profiles_actions.py:1429` — **`save_profile(profile)` DIRETO, fora do funil e SEM `origem=`**.
8. `.../app/actions/profiles_actions.py:1436-1446` — `delete_profile(renomeando_de)` no rename (o rodapé nunca apaga nada).
9. `.../app/actions/profiles_actions.py:1454` — `_reconciliar_rascunho_com_perfil_salvo()` (definido em `:1899`) — o equivalente próprio do `_reapontar_rascunho`.
10. `.../app/actions/profiles_actions.py:1467-1472` — `profile_switch(profile.name)` reaplica no hardware quando o salvo é o ativo — o rodapé NÃO faz isso (só `_notify_launch_env_refresh`, `profile_writer.py:145-147`).

### Onde os dois DIVERGEM (resumo por linha)
- **Regra de match**: rodapé nunca lê widget de regra — `draft_config.py:578-582` decide (herda se `mesmo_perfil`, senão `MatchAny()`), com o remendo de herança do disco em `footer_actions.py:419-420`; a aba lê os widgets em `profiles_actions.py:1953-1979` e aplica a guarda de gesto em `:2139-2141`.
- **Prioridade**: rodapé em `footer_actions.py:366-369` (disco se existe, calculada se novo) mas passando por `draft_config.py:569`, onde `source_priority` ainda pode vencer o argumento; a aba em `profiles_actions.py:2136-2138` (escala se houve gesto, disco senão).
- **Avisos**: a aba tem dois (`profiles_actions.py:1397` e `:1413`); o rodapé tem zero — só o `prompt_overwrite_existing` de `footer_actions.py:320`.
- **Funil de gravação**: `profile_writer.py:116` (com `origem=`) vs `profiles_actions.py:1429` (sem `origem=`) — o journal `profile_salvo` do caminho da aba cai no basename do processo (`loader.py:773` `_origem_do_processo`), então uma gravação pela aba Perfis não é distinguível de qualquer outra da GUI no journal.
- **Portão que trava a divergência**: `/mnt/Apate/.../tests/unit/test_gravacao_de_perfil_passa_pelo_funil.py:456` — `_AUTORIZADOS_A_GRAVAR = {"actions/profile_writer.py", "actions/profiles_actions.py"}`, com `profiles_actions.py` declarado EXCEÇÃO DATADA (04/08/2026) que "só pode encolher"; testes em `:485`, `:508` e `:520`.
- **Reaponte do rascunho**: `profile_writer.py:186-193` (gate `adotar_como_ativo or mesmo_slug`) vs `profiles_actions.py:1938-1944` (gate `mesmo_slug(profile.name, ativo) or rename do ativo`).

---

## 6. Testes e docs (pontos de referência)

Testes:
- `/mnt/Apate/.../tests/unit/test_gravacao_de_perfil_passa_pelo_funil.py:212` — a catraca 10→20→30 do rodapé; `:288` herda prioridade do disco; `:312` herda a regra do disco; `:358` perfil novo pelo rodapé continua nascendo "sempre".
- `/mnt/Apate/.../tests/unit/test_salvar_nao_rebaixa_02_o_novo_perfil_desligava_as_guardas.py:311` — salvar por cima preserva regra e prioridade; `:487` limiar do aviso; `:510` os dois diálogos.
- `/mnt/Apate/.../tests/unit/test_footer_salvar_nasce_acima_dos_catch_all.py:86` / `:157` / `:185` — a prioridade calculada do rodapé e o piso não-zero.
- `/mnt/Apate/.../tests/unit/test_profile_manager.py:92`, `:113`, `:139`, `:203`, `:259` — prioridade × especificidade × veto R-21.
- `/mnt/Apate/.../tests/unit/test_profiles_sanidade.py:153`, `:192`, `:337`, `:370` — catch-all acima de específico, criteria vazio, empates, faixa.
- `/mnt/Apate/.../tests/unit/test_simple_match.py:25`, `:55`, `:114` — presets e o fallback `MatchAny` da chave desconhecida.
- `/mnt/Apate/.../tests/unit/test_match_sem_caixa_e_sentinel_manual.py:185`, `:237` — `e_catch_all` e round-trip do sentinel manual.
- `/mnt/Apate/.../tests/unit/test_draft_config.py:104`, `/mnt/Apate/.../tests/unit/test_perfil_salva_tudo_rascunho.py` (portão que impede o default 5 voltar à assinatura de `to_profile`).
- `/mnt/Apate/.../tests/unit/test_profiles_preset.py:209` / `:239` — `meu_perfil` e `fallback` têm de ser `MatchAny`.

Docs:
- `/mnt/Apate/.../docs/process/sprints/2026-07-26-PERFIL-NASCE-CERTO-01-o-perfil-do-jogo-que-nunca-vence.md:32-45` — o disco medido (`any`/prio 5 vs `any`/prio 100) e a origem da prioridade calculada; `:124` declara "priority nasce acima de todo catch-all".
- `/mnt/Apate/.../docs/process/sprints/2026-07-27-EMPATE-01-tres-perfis-empatados-e-quem-ganha-e-o-alfabeto.md:30` — a chave `(not e_catch_all, priority)` e o desempate alfabético.
- `/mnt/Apate/.../docs/process/sprints/2026-07-25-ABAS-01-as-abas-brigam-pelo-mesmo-estado.md` — origem do `with_profile_identity`.
- `/mnt/Apate/.../docs/process/sprints/2026-07-29-PERFIL-SALVA-TUDO-01-salvei-todas-as-abas-e-so-parte-ficou.md` — origem do "sem número inventado" no `to_profile`.
- `/mnt/Apate/.../docs/process/sprints/2026-07-30-AUTOMATISMO-MORTO-01-o-perfil-do-jogo-nunca-entra.md` e `.../2026-07-31-RADAR-01-...md` — o veto R-21 / autoswitch.
- **Não há sprint doc** para `GRAVA-POR-UM-FUNIL-01`, `SALVAR-NAO-REBAIXA-01/02`, `REGRA-NAO-SE-PERDE-01` nem `PERFIL-SEM-RASTRO-01` — as únicas menções fora do código estão em `/mnt/Apate/.../docs/process/sprints/2026-08-03-DOC-QUE-NAO-MENTE-03-a-foto-vazia-a-env-negada-e-a-tag-velha.md` e `/mnt/Apate/.../docs/process/estudos/2026-08-03-o-backlog-real-conferido-contra-o-codigo.md`; a documentação viva desses mecanismos são as docstrings de `profile_writer.py:1-37`, `footer_actions.py:334-410` e `profiles_actions.py:2090-2122`.
