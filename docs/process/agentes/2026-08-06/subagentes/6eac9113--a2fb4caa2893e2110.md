# agente a2fb4caa2893e2110 (sessao 6eac9113)

**tipo:** Explore
**tarefa:** Read autoswitch/apply sprints
**profundidade:** 2  **pai:** a48ea78b0b42a9356

## O QUE FOI PEDIDO

Repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix. Leia INTEGRALMENTE estes documentos (todos sob /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/):

1. 2026-07-25-MODO-01-o-modo-jogo-liga-sozinho.md
2. 2026-07-28-JANELA-CEGA-01-o-detector-que-nunca-adoece.md
3. 2026-07-30-AUTOMATISMO-MORTO-01-o-perfil-do-jogo-nunca-entra.md
4. 2026-07-31-SINAL-DE-JOGO-01-o-daemon-desiste-do-jogo-antes-do-jogo-acabar.md
5. 2026-07-31-JANELA-FIEL-01-a-janela-que-para-de-reconciliar-e-o-botao-morto-no-pacote.md
6. 2026-08-01-APLICAR-VERDADE-01-o-rodape-nao-mente-mais-a-ponte-ainda-mente.md

Para CADA um, extraia com precisão:
- estado declarado (PROPOSTA / ENTREGUE / CURADA / CADUCADA / ABERTA / parcial) e a data dessa declaração;
- o defeito que ela cura, em uma frase;
- **a lista de ENTREGAS (E1, E2, ...) com o SÍMBOLO/mecanismo concreto que cada uma promete** (nome de função, constante, campo, arquivo:linha citados no documento). Isto é o mais importante: preciso da lista de símbolos prometidos para conferir por grep se têm chamador em produção;
- qualquer nota datada de caducidade/refutação;
- o que a sprint diz explicitamente para NÃO fazer;
- toda menção a: autoswitch, debounce (0,5 s entrar / 12 s sair), janela ativa / window_detect, prioridade, catch-all, aplicação imediata do perfil nas abas da janela, reconciliação do rascunho com o perfil ativo, `apply_output_defaults`, `manual_override_categories`.

NÃO altere nenhum arquivo. Devolva relato estruturado por sprint, denso. Cite sempre caminho absoluto e número de linha quando o documento der.

## RESULTADO FINAL

Li os seis documentos integralmente. Nenhum arquivo foi alterado.

---

# Achado transversal, antes das seis fichas

Três dos termos que você pediu **não existem em nenhum dos seis documentos** (grep case-insensitive sobre os seis arquivos, contagem 0/0/0):

- `apply_output_defaults` — **zero ocorrências**
- `manual_override_categories` — **zero ocorrências** (a única palavra "override" nos seis é `AUTOMATISMO-MORTO-01:337`, e é "overrides por-MAC" do `navegacao.json`, coisa diferente)
- **debounce / 0,5 s entrar / 12 s sair** — **zero ocorrências**. O vocabulário de temporização desta família é outro: `HYSTERESIS_SEC = 30.0` (`game_signal.py:62`), a histerese UX-01 do autoswitch (`autoswitch.py:198-225`), `WINDOW_DETECT_BLIND_AFTER_SEC = 300.0` (`state_store.py:43`), tique de 2,0 s (`lifecycle.py:3291-3293`), TTL de 900 s do marker (`launch_env.py:106`). Se existe um debounce 0,5/12 s em produção, **ele não é prometido nem discutido por nenhuma destas seis sprints** — veio de outro documento.

E um aviso metodológico que a própria casa escreve, e que muda como você deve ler os campos `Status:`:

> `/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-08-01-APLICAR-VERDADE-01-o-rodape-nao-mente-mais-a-ponte-ainda-mente.md:62-64` — *"a auditoria de 31/07 mediu **41 de 50 cabeçalhos dizendo ABERTA, incluindo entregas provadas**"*; e `:198-199`: **"Não confiar no campo `Status:` de nenhum documento citado aqui."**

---

# 1. MODO-01 — o modo jogo liga sozinho

`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-25-MODO-01-o-modo-jogo-liga-sozinho.md` (155 linhas)

**Estado declarado:** `:3-5` — **ABERTA**, Prioridade ALTA, aberta em **25/07/2026**. Nenhuma nota de fechamento dentro do documento.
**Contradição externa datada:** `/mnt/.../sprints/2026-07-25-INDICE-leva-quatro-controles.md:101` declara **ENTREGUE `54f1f3b` — confirmado ao vivo**; e a AUTOMATISMO-MORTO-01 (`:359-365`, 30/07) reconfirma por medição: *"A MODO-01/B3 entregou o que prometeu"* e *"o cadeado está antes do modo de propósito (`autoswitch.py:234-237`)"*. Ou seja: **B3 e B2 entraram; o cabeçalho envelheceu.**

**Defeito que cura (uma frase):** o modo jogo nunca liga sozinho porque o veto R-21 devolve `None` para janela de jogo quando todos os candidatos são catch-all — e o caminho que a interface oferece como solução (criar perfil de jogo) nasce com modo `"none"`, então também não liga nada.

**ENTREGAS (aqui são B1..B5) e símbolos prometidos:**

| # | Linha | Símbolo/mecanismo concreto prometido |
|---|---|---|
| B1 | `:83-85` | `app/actions/profiles_actions.py:630` — perfil novo nasce com modo `"none"`; deve **pré-selecionar `gamepad` com a máscara corrente** quando a escolha é "jogo"/"jogo da Steam" |
| B2 | `:87-97` | `perfil_e_regra_de_jogo` (`profiles/schema.py:602`) exige `window_class` **e** `steam_app_`; conserto: **ceder também quando o perfil não é catch-all e declara `mode.kind` em `{gamepad, native}`** — este é literalmente o símbolo que virou `perfil_declara_modo_de_jogo` (`schema.py:666-702`) |
| B3 | `:99-111` | **`apply_profile_mode(ProfileModeConfig(kind="gamepad", gamepad_flavor=config.gamepad_flavor), origin="game_signal")`**; exige que **`select_for_window` devolva o MOTIVO do veto** (`None` é ambíguo entre "nada casou" e "vetei catch-all num jogo"). Log de aceite: `profile_mode_aplicado origin=game_signal` |
| B4 | `:113-118` | `integrations/window_backends/xlib.py:248` — gate de foco devolve foco nulo; **47 episódios**; faz a autoridade cair de `game` para `daemon` 30 s depois com o jogo aberto |
| B5 | `:120-124` | dedup por campo de instância em `profiles/manager.py:102`, anulada porque `daemon/lifecycle.py:2512` **cria um `ProfileManager` novo a cada tique**; evento `profile_select_catch_all_sem_autoridade_em_jogo` a 0,5 Hz (12 linhas idênticas, 2,00 s de intervalo) |

**Nota datada de caducidade/refutação:** nenhuma **dentro** deste arquivo. Mas a SINAL-DE-JOGO-01 caduca uma citação dele por escrito: `2026-07-31-SINAL-DE-JOGO-01...md:124-126` — *"A MODO-01 §B4 cita 47 episódios e a linha `xlib.py:248`; **a linha mudou de número** — hoje o `get_input_focus()` está em `:256` e as saídas do gate em `:267-278`. O fato é o mesmo, a citação é que envelheceu."*

**O que a sprint diz para NÃO fazer:** `:150-155` — **não** unificar os seis conceitos de "modo jogo" nesta sprint (*"mexer nos seis conceitos ao mesmo tempo é como se cria um sétimo"*); o mínimo é **não criar nomes novos**. Também `:126-135`: três coisas do estado dela **não são defeito** e não devem ser "consertadas" (não existe perfil para `steam_app_2111190`; 11 de 13 perfis com `mode: null`; o cadeado ligado a pedido dela).

**Menções aos termos pedidos:**
- **autoswitch:** `:39-40` (`autoswitch_window_info_unavailable`, `autoswitch_congelado_pelo_cadeado`), `:54` (último `profile_autoswitch` em **24/07 20:37**), `:56` (`autoswitch_locked.flag` ligado desde 24/07 20:42).
- **catch-all (8 ocorrências):** `:37`, `:47-50` (R-21 em `profiles/manager.py:557`, regra em `:561`), `:67-68` — *"a R-21 trocou 'o catch-all entra num jogo' por '**ninguém** entra num jogo', e não pôs nada no lugar"*.
- **janela ativa / window_detect:** o campo `window_detect_*` **não aparece**; o assunto entra por B4 (gate de foco) e pelo `x11_focus_gate_no_x_focus focus=0` de `:38`.
- **prioridade:** só no cabeçalho. Nenhuma análise de `priority`.
- **aplicação imediata do perfil nas abas / reconciliação do rascunho:** **ausentes**.
- **`apply_output_defaults`, `manual_override_categories`, debounce 0,5/12 s:** **ausentes**.

---

# 2. JANELA-CEGA-01 — o detector que nunca adoece

`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-28-JANELA-CEGA-01-o-detector-que-nunca-adoece.md` (239 linhas)

**Estado declarado:** `:3-4` — **PARCIALMENTE ENTREGUE** (*"observabilidade entregue; uma linha de fiação e a decisão do `game_signal` ficam declaradas abaixo"*), Prioridade ALTA, Faixa 2, **aberta em 28/07/2026**, medida ao vivo no daemon dela nesse dia.

**Defeito que cura:** um detector de janela cego para sempre era bit a bit indistinguível de um saudável — `window_detect_healthy` é trinco de mão única, `window_detect_last_class` é sticky que nunca decai, e seis causas distintas colapsavam num `None` só.

**ENTREGAS (numeradas 1-3, `:95-121`) — símbolos ENTREGUES:**

| # | Linha | Símbolos concretos |
|---|---|---|
| 1 | `:95-107` | `daemon/state_store.py`: **`window_detect_seeing(now=None)`**, **`window_detect_useful_age(now=None)`**, **`WINDOW_DETECT_BLIND_AFTER_SEC = 300.0`** (300 s = 600 tiques a 2 Hz; tempo, não contagem) |
| 2 | `:108-115` | seis constantes **`MOTIVO_*`** em `integrations/window_backends/xlib.py`, publicadas em **`last_failure_reason`**; **`sem_backend`** no `NullBackend`; **`cascata_wayland_sem_leitura`** na cascata; **`janela_sem_classe`** / **`backend_sem_motivo`** no leitor; **`WindowReaderDiag.last_reason`**; **`StateStore.record_window_detect_read(..., reason=...)`**. O `Protocol` **`WindowBackend` ficou intocado** (`getattr` defensivo) |
| 3 | `:116-121` | `daemon/ipc_handlers.py`: **`_window_detect_payload()`**, usado por `daemon.state_full` **e** `daemon.status`. Campos novos: **`window_detect_current_class`** (leitura CRUA), **`window_detect_useful_age_sec`**, **`window_detect_seeing`**, **`window_detect_reason`** |

**A entrega que NÃO foi feita, e que é a mais importante para você conferir** (`:123-148`): `window_detect_healthy` **não** decai, deliberadamente. Motivo medido: `Daemon._gather_game_signal_inputs` alimenta `game_signal.classify`; com `window_healthy=False` a autoridade vira `unknown` em vez de `daemon`, e em `lifecycle._sync_game_signal` a transição `daemon -> unknown` chama **`replay_retained_game_outputs()`**, que repinta a lightbar com resíduo de jogo. Divisão declarada: **`healthy` é o fail-safe do `game_signal` (trinco); `seeing` é a resposta honesta sobre o agora e não decide nada.** `:146-148`: *"A leva seguinte é de uma linha: trocar `window_healthy` de `healthy` para `seeing` em `daemon/lifecycle.py:_gather_game_signal_inputs`"*.

**A linha de fiação que ficou faltando** (`:152-166`) — este é o símbolo mais provável de estar sem chamador: o motivo chega ao `WindowReaderDiag.last_reason` e o store sabe guardá-lo, mas **quem chama o store é `daemon/subsystems/autoswitch.py`, que ficou fora do escopo**. Enquanto não entrar, **`window_detect_reason` sai `null` no daemon vivo**. O ponto exato, em `_build_diag_window_reader`:

```python
store.record_window_detect_read(
    _backend_name(),
    wm_class if isinstance(wm_class, str) else None,
    reason=getattr(reader, "last_reason", None),   # <- isto
)
```

**Notas datadas de caducidade/refutação:**
- `:185-189` — **"RESOLVIDO em 29/07/2026 pela CODIGO-MORTO-01"**: `integrations/xlib_window.py` (111 linhas, mina armada) virou lápide com `raise ImportError` apontando `integrations/window_detect.build_window_reader()`; coberto por `tests/unit/test_xlib_window_nao_importavel.py`.
- `:168-178` — a cascata Wayland (`window_backends/wayland_portal.py` 201 linhas + `wlr_toplevel.py` 184 = **385 linhas que nunca executam aqui**), porque `detect_window_backend()` testa `DISPLAY` primeiro e o `systemd --user` desta máquina exporta `DISPLAY=:1`. Suporte real exigiria `zcosmic_toplevel_info_v1`.

**O que diz para NÃO fazer:** não fazer `healthy` decair nesta leva (`:123-148`, com o custo nomeado: *"um vaivém na cor do controle dela, entregue de lambuja numa leva de observabilidade"*); não mexer no `Protocol WindowBackend` (`:113-115`, para backend de terceiro seguir válido); a linha da aba Sistema fica **fora do escopo** (`:197-202`, a aba não estava entre os arquivos liberados).

**Menções aos termos pedidos:**
- **window_detect (26 ocorrências):** todo o documento; escritores do `healthy` tabelados em `:48-53` (`__init__` nasce `False`; `set_window_detect_backend`; `record_window_detect_read`); *"nenhum caminho jamais atribui `False` depois da semeadura"* `:55`.
- **autoswitch:** `:66-70` — o achatamento em `profiles/autoswitch.py` (**`_tick_sem_informacao`**), onde "info vazio" e `wm_class="unknown"` entram no MESMO ramo; e `:154-156` (a fiação faltante mora em `daemon/subsystems/autoswitch.py`).
- **prioridade / catch-all / debounce / `apply_output_defaults` / `manual_override_categories`:** **ausentes**.
- **aplicação imediata do perfil nas abas / reconciliação do rascunho:** o **Anexo APLICAR-VERDADE-02** (`:217-239`) toca o vizinho disso: `app/actions/footer_actions.py` usava `ok = result.get("status") == "ok"`, e **`_clear_mouse_dirty()`** baixava a pendência da aba Mouse com as sete seções FORA. Agora são duas perguntas — `aceita` (decide a FRASE) e `aplicou` (decide o `dirty` e o journal) —, e *"a pendência do mouse só acaba se a seção **mouse** entrou"*. Compatibilidade preservada: resposta **sem `applied`** continua contando como aplicada (mesma regra do `_mensagem_de_aplicacao`). Commit citado: `e8f9060`. `tests/unit/test_harmonia_mouse_um_dono.py` não precisou ser tocado.
- **Provas:** `tests/unit/test_janela_cega_01_o_detector_que_adoece.py` (23) e `tests/unit/test_aplicar_verdade_02_a_contabilidade_do_aplicar.py` (7); 30 verdes; suíte 5767 (`:204-215`).

---

# 3. AUTOMATISMO-MORTO-01 — o perfil do jogo nunca entra

`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-30-AUTOMATISMO-MORTO-01-o-perfil-do-jogo-nunca-entra.md` (752 linhas)

**Estado declarado:** **NÃO HÁ campo `Status:`.** O cabeçalho (`:3`) diz apenas *"Aberta em: 2026-07-30, sobre a árvore `restauro/inicio-da-sessao` em `e74077c` (tag `v0.4.0`, publicada hoje)"*. É documento de medição + plano: **as cinco entregas E0-E4 são todas PROPOSTA**, e E4 está congelada esperando a palavra dela. A **nota datada de 03/08/2026** (`:695-752`) não declara entrega — **confirma que o defeito persiste** seis dias depois.

**Defeito que cura:** o autoswitch de perfil está morto na máquina dela — dentro do jogo o veto R-21 esvazia o candidato porque todos os perfis que casam são catch-all; no desktop o cadeado é o único freio; resultado medido: `profile_autoswitch` = **0** em três dias, com Pragmata aberto 30 vezes.

**ENTREGAS e símbolos prometidos (o que você quer para grep):**

### E0 — a janela diz POR QUE o perfil não trocou (`:376-418`)
- Carimbar no `StateStore` **o último motivo, a última `wm_class` julgada e o último candidato**; nome usado no teste: **`store.autoswitch_ultimo_motivo`** (`:408`).
- Motivos que **já existem** e nunca chegam ao IPC: **`MOTIVO_JOGO_SEM_PERFIL_PROPRIO`**, **`MOTIVO_SEM_CANDIDATO`**, **`MOTIVO_SELECIONADO`** (`profiles/manager.py:60-62`), recebidos em `profiles/autoswitch.py:230`. Medição citada: *"não há nenhuma ocorrência de `motivo` em `daemon/ipc_handlers.py`"* (`:382-383`).
- Arquivos: `daemon/state_store.py` (três campos + property, ao lado de `_window_detect_*`, `:111-137`); `profiles/autoswitch.py` (carimbo logo depois de `:230`); `daemon/ipc_handlers.py` (bloco do `state_full`, junto de `:1276-1298`); `app/actions/home_actions.py` (frase, junto de **`autoswitch_lock_text`**, `:131`).
- Reaproveita a linha da aba Sistema `app/actions/daemon_actions.py:126-160`, que já sabe dizer **`window_detect_seeing`** e **`window_detect_reason`**.

### E1 — o campo próprio da janela de jogo (`:420-463`)
- Cria **`_game_window_last_class`** no `StateStore`, ao lado de `_game_window_seen_at` (`state_store.py:130`), gravado **no mesmo `if`** de `state_store.py:331-334` (só quando casa `steam_app_\d+`); property nova perto de `state_store.py:509`.
- Publica **`game_window_last_class`** no `state_full` (`daemon/ipc_handlers.py:1276-1298`).
- Consumidores a migrar: `app/actions/profiles_actions.py:726` (prefill do appid), `app/actions/profiles_actions.py:862` (**`_aplicar_nascimento_com_jogo`**), `app/actions/launch_wrapper_dialog.py:135`, `daemon/ipc_handlers.py:2280`.
- Decisão: o campo **não decai**; o `game_signal` continua consumindo o CRU `_window_detect_current_class`, por veto documentado em `state_store.py:120-127`; idade vem de `game_window_seen_at` (`state_store.py:509-518`).

### E2 — UM CLIQUE: "fazer este perfil valer neste jogo" (`:465-553`) — *"a entrega que resolve a faixa"*
- **Função PURA nova: `regra_de_jogo_para(profile, wm_class, flavor) -> Profile`**, em `profiles/schema.py` **ou num módulo `profiles/promocao.py`** — e o próprio documento marca, em comentário HTML na `:520`: `<!-- ref-externa: arquivo a CRIAR por esta entrega, ainda não existe -->`.
- Mecânica: `match` vira **`MatchCriteria(window_class=["steam_app_<id>"])`**; `priority` vira **`_prioridade_acima_dos_catch_all()`** (`app/actions/profiles_actions.py:1425-1439`, hoje **15**); `mode` preenchido com o modo VIVO (`kind: gamepad` + flavor de `gamepad_emulation.flag`) via **`normalizar_gamepad_flavor`** (`profiles/schema.py:645-663`).
- Efeito prometido: o veto R-21 deixa de disparar (`manager.py:620` testa `all(p.e_catch_all)`) **e** o cadeado cede pela Porta 1 (`perfil_e_regra_de_jogo`, `schema.py:633-642`) **com a flag ainda ligada**.
- Texto: *"Usar este perfil sempre neste jogo"*; toast *"«Pragmata2» agora vale sempre que você abrir este jogo."*; fallback numérico como em `profiles_actions.py:877-880`.
- Guardas obrigatórias (`:499-513`): exige `game_window_last_class` + `game_window_seen_at` recente; botão desabilitado com *"Já vale neste jogo"*; preset de fábrica (em **`profiles/.seeded_presets`**) → oferecer duplicar; escrita atômica por `save_profile` respeitando o `.lock`; **backup do `.json` antes de reescrever**.
- Arquivos: `app/actions/footer_actions.py` (perto de `:283`), `app/actions/profiles_actions.py`, `src/hefesto_dualsense4unix/gui/main.glade`.

### E3 — o rodapé para de criar perfil catch-all quando há jogo em foco (`:555-586`)
- Hoje `app/draft_config.py:495-498` monta **`MatchAny()`** com nome novo. Passa a fazer o que `_aplicar_nascimento_com_jogo` (`profiles_actions.py:830-884`) já faz.
- Arquivos: `app/draft_config.py:421-500` (aceitar um `match` do chamador, sem mudar o default), `app/actions/footer_actions.py:283-315`.
- Teste existente a estender: `tests/unit/test_footer_salvar_nasce_acima_dos_catch_all.py`. Armadilha R-11 em `draft_config.py:484-498`.

### E4 — migrar os perfis dela ▲ **ESPERA A PALAVRA DELA** (`:588-610`)
- Congelada. Reescreveria `pragmata.json` e `pragmata2.json`. *"Nenhuma linha desta entrega é escrita antes de ela dizer, com todas as letras, quais arquivos podem ser reescritos."*

**Símbolos de diagnóstico medidos (contexto, não promessa):** `utils/session.py:195` `load_autoswitch_locked`, `:189`, `:198`; `profiles/autoswitch.py:252` `if self.travado():`, predicado `:165-189`, portas em `:259-262`; `perfil_e_regra_de_jogo` `profiles/schema.py:602-642` (`:633`, `:636-642`, `:620-624`, `:632-634`); `perfil_declara_modo_de_jogo` `profiles/schema.py:666-702` (`:697-698`, `:699-700`, `:701-702`); `Profile.e_catch_all` `schema.py:569-599`; veto R-21 `profiles/manager.py:620-628` dentro de `select_for_window_ex`, `_STEAM_APP_WM_CLASS_RE` `manager.py:43`, doutrina `manager.py:588-611`, `_chave_de_selecao` `manager.py:632-640`; `_current_profile` `autoswitch.py:111` preenchido só em `_activate` `autoswitch.py:551`; gate `autoswitch.py:304` `if stable and candidate and candidate != self._current_profile:`; `_sincronizar_modo_jogo_padrao` `autoswitch.py:237`; `_selecionar_com_motivo` `autoswitch.py:230`; `_janela_propria` `profiles/autoswitch.py:438-449` com `OWN_GUI_WM_CLASSES` `:71-78`; `state_store.py:317`/`:326` (sticky), `daemon/subsystems/autoswitch.py:144`; histerese UX-01 `autoswitch.py:198-225`; `integrations/window_backends/xlib.py:276`.

**Notas datadas de caducidade/refutação:**
- `:302-329` — **▲ NÃO é o detector de janela.** *"Esta sessão REFUTOU a hipótese com que a faixa começou"*, medida 30/07: `window_detect_diag_seeded backend=xlib healthy=True` e 40 linhas com `wm_class=steam_app_*`. *"**Não gaste a próxima leva no detector.**"*
- `:348-357` — **▲ NÃO é a prioridade.** Refutado em 27/07 e reconfirmado no código: `_chave_de_selecao` devolve `(not profile.e_catch_all, profile.priority)` — **especificidade antes de prioridade** — e o veto nem lê `priority`. *"Subir `Pragmata2` de 5 para 200 não muda absolutamente nada dentro do jogo."* Derruba meia cura da v0.4.0 (`footer_actions.py:283-315`).
- `:331-346` — **▲ NÃO é desligar o cadeado** (soltaria Navegação por cima da config dela 18 vezes em 3 dias).
- `:359-366` — **▲ NÃO é o modo jogo padrão** (funciona; MODO-01/B3 entregue).
- `:695-752` — **Nota de medição de 03/08/2026**, com `daemon.state_full` vivo: `autoswitch_locked: True` (seis dias), **`active_profile: None`** com 15 perfis no disco. E o achado novo: **a paleta automática por slot (COR-03) mascara o automatismo morto** — P1 nasce azul, P2 vermelho, e não há nada na interface dizendo *"nenhum perfil ativo; a cor que você vê é automática"*. (Inconsistência interna: a nota chama essa entrega de "E4"; pelo corpo do documento, é a **E0**.)
- `:93` e `:94` versus enunciado da faixa: *"Correção honesta ao enunciado da faixa: **não são todos catch-all**. São catch-all exatamente cinco"*.

**O que diz para NÃO fazer** (`:614-655`, sete itens, todos numerados e com símbolo):
1. **Não apertar o predicado do cadeado** — tirar a Porta 2 `perfil_declara_modo_de_jogo` (`schema.py:666-702`) reabre o congelamento que a MODO-01/B2 curou.
2. **Não apagar a cascata Wayland** `integrations/window_backends/wayland_portal.py` e `wlr_toplevel.py` (vão parecer código morto).
3. **Não desligar o cadeado** — nem por script, nem "só para testar", nem apagando o `.flag`.
4. **Não revogar nem afrouxar o veto R-21** (`manager.py:620-628`) — sem ele volta o ping-pong de 18-28 s.
5. **Não afrouxar `perfil_e_regra_de_jogo` para aceitar regex de título** (`schema.py:620-624`; segundo consumidor é o furo da trava manual em `_activate`, `autoswitch.py:505-518`).
6. **Não mexer em prioridade achando que resolve** (`manager.py:632-640`, `:620`).
7. **Não escrever nos `.json` dela sem autorização explícita** — inclusive "só para normalizar/migrar" ou dentro de script de instalação.

**Menções aos termos pedidos:** autoswitch **53×**, catch-all **38×**, prioridade **25×**, window_detect **16×** (todos acima). **Debounce, `apply_output_defaults`, `manual_override_categories`: zero.** **Aplicação imediata do perfil nas abas / reconciliação do rascunho: ausentes** (isso é a JANELA-FIEL-01/E1).

---

# 4. SINAL-DE-JOGO-01 — o daemon desiste do jogo antes do jogo acabar

`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-31-SINAL-DE-JOGO-01-o-daemon-desiste-do-jogo-antes-do-jogo-acabar.md` (558 linhas)

**Estado declarado:** `:3-12` — **ABERTA — documento de medição e plano. Nada de código nesta rodada.** Prioridade **MÉDIA**, e a prioridade é **uma correção do próprio documento sobre o achado que o originou** (o auditor abriu como ALTA; o verificador independente reenquadrou). Faixa 2. Aberta em **31/07/2026**, branch `restauro/inicio-da-sessao`, HEAD `7bd0cb7`, daemon pid 3615 vivo e **PRAGMATA.exe rodando durante a medição**.
**Paga:** dívidas 2 e 3 do índice de 30/07 (`2026-07-30-INDICE-as-tres-faixas-depois-da-v040.md:310-324`).

**Defeito que cura:** o sinal de três estados (`game`/`daemon`/`unknown`) que decide quem pinta o controle está pendurado, na máquina dela, numa perna só (ver a janela do jogo) — e a flag que ele usa para dizer "não tem jogo" (`window_detect_healthy`) sobe e nunca desce.

**ENTREGAS e símbolos prometidos:**

### E1 — o experimento que ninguém fez (`:299-334`)
- **Zero de código.** Roteiro de 90 s com jogo vivo e foco fora dele. Aceite: bloco de journal com carimbo de tempo **mais `ps` com `etimes`** no instante da transição. Eventos a observar: **`x11_focus_gate_no_x_focus`** e **`game_signal_transition de=game para=daemon evidencia=daemon_histerese_expirada`**.

### E2 — a telemetria que falta: qual evidência caiu (`:336-359`)
- Hoje o log da transição é **`evidencia=daemon_histerese_expirada`** (`daemon/subsystems/game_signal.py:179`) — string que diz **que** caiu e não **por quê**.
- Acrescentar ao `logger.info` existente o retrato das três evidências no tique da queda: janela (classe corrente + idade do carimbo), perfil (bool), marker (presente/expirado/pid morto), mais o **`window_detect_seeing`** do momento.

### E3 — o probe de perfil recebe a janela inteira (`:361-390`)
- **`daemon/lifecycle.py:3131-3133`** hoje é `select_for_window({"wm_class": wm_class})`. Passa a receber **`wm_name`** e **`exe_basename`**.
- O `StateStore` passa a carregá-los: `daemon/state_store.py:279-334`, alimentado por **`daemon/subsystems/autoswitch.py:144-148`** (que já tem os dois no `info` — `profiles/autoswitch.py:434-435`). *"Não é uma linha de conserto: o store precisa passar a carregar os outros dois campos."* (`:254`)
- Função nomeada na mordida: **`_profile_rule_matches_game`**.
- Matcher: `profiles/schema.py:70-94` (`window_class`; `window_title_regex` via `wm_name` `:76-87`; `process_name` via `exe_basename` `:88-90`; **E** entre campos preenchidos `:92-94`); alvo vazio nunca casa (`schema.py:42-47`).
- Testes de hoje que passariam intactos com o defeito: **`tests/unit/test_game_signal_wiring.py:282-343`**.
- Risco nomeado: regex solto `|Control)`, `|Metro)` do `fps.json`, armadilha conhecida em `profiles/schema.py:685-691`.

### E4 — o processo do jogo vivo como evidência que não depende de foco (`:392-421`)
- Evidência nova: pid do marker quando existe, ou **varredura barata de `/proc`** quando não existe.
- **Critério tem de ser o executável do jogo, não a árvore da Steam**: `steam`, `steamwebhelper`, `reaper` e `wineserver` **não** são jogo (`:410-411`), pelo incidente registrado em `daemon/subsystems/game_signal.py:5-10`.
- Custo por tique a medir; tique 2 s (`lifecycle.py:3291-3293`), roda no executor junto com `_gather_game_signal_inputs`.

### E5 — `healthy` vira `seeing` — **SOZINHA**, com ela olhando a lightbar (`:423-464`)
- **`daemon/lifecycle.py:3163`** passa a ler **`self.store.window_detect_seeing()`** no lugar de **`self.store.window_detect_healthy`**.
- **Ordem obrigatória: E5 depois de E3 e E4.** Entra num commit só. Motivo: a transição `daemon → unknown` dispara **`replay_retained_game_outputs()`** (`lifecycle.py:3224-3228`), que repinta a lightbar.
- Terceiro teste da mordida fixa que `replay_retained_game_outputs()` é chamado **exatamente uma vez**.

**Mecanismo medido (símbolos de contexto):** `game_signal.classify` `daemon/subsystems/game_signal.py:109-126`; evidência 1 `:109-112`; evidência 2 `profile_rule_match` `:113` calculada em `lifecycle.py:3119-3142`; evidência 3 **`wrapper_game_running`** `:114-121` (`daemon/launch_env.py:264-314`, TTL 900 s em `launch_env.py:106`, `pid_alive` `:308-309`); **`HYSTERESIS_SEC = 30.0`** `game_signal.py:62`; queda `game_signal.py:157-180` (`session_open=True`, `:171-174`); docstring `game_signal.py:102-104`; gate `integrations/window_backends/xlib.py:244-279` (comentário `:245-255`, `get_input_focus()` `:256`, saídas `:267-272` e `:273-278`); `state_store.py:320-322` e `:331-334`; `window_detect_healthy` `state_store.py:420-441`, semeado em `daemon/subsystems/autoswitch.py:99-103`; **contrato escrito em `state_store.py:427-438`** com a frase que governa a sprint: *"Trocar o consumidor do `game_signal` de `healthy` para `seeing` é uma leva própria, com ela vendo."*; `window_detect_seeing` `state_store.py:459-472`; **`WINDOW_DETECT_BLIND_AFTER_SEC = 300.0`** `state_store.py:43`; `window_detect_current_class` property `state_store.py:496-506`.

**Custo da autoridade errada (`:207-225`):** `lifecycle.py:3219-3223` → **`controller.defend_display()`** (`backend_pydualsense.py:3109-3132`); merge exclui a camada do jogo (`backend_pydualsense.py:1170-1172`); gates que negam: modo jogo padrão `lifecycle.py:2039-2042` (`modo_jogo_padrao_adiado ... sem_autoridade_de_jogo`), gate do teclado (recusado por escrito no commit **`2bbfa22`**), aviso "Renumerar agora" `app/actions/home_actions.py:287-304`.

**Notas datadas de caducidade/refutação:**
- `:163-205` — **"O que o verificador derrubou"**: as **seis** transições de journal apresentadas como prova **não provam** a queda com jogo vivo. 29/07 16:56:49 e 17:53:48 → marker fresco, logo `pid_alive=False`, processo morto. 29/07 17:13:29, 20:15:35 e 30/07 02:30:00 → fechamento real, comportamento projetado (`core/backend_pydualsense.py:1164-1169`, *"fechar o jogo devolve a paleta em ≤ ~32 s"*). 28/07 23:17:49 → sem contexto. Episódio inverso 28/07 23:16:22-23:16:39: 16 s sem foco com jogo vivo e **o sinal não caiu** — a histerese funcionou.
- `:124-126` — caduca a citação `xlib.py:248` da MODO-01 §B4 (hoje `:256` / `:267-278`).
- `:279-293` — **a evidência nº 2 é letra morta na máquina dela**: em 15 perfis, o único que o probe casa é `sackboy_nativo`, e por uma `wm_class` que a evidência 1 já pegaria sozinha. E o paradoxo: *"o mesmo `coop_local` que **fura o cadeado** por título é o perfil que **nunca vira evidência de jogo** por título"*.
- `:51-53` — **a evidência nº 3 está estruturalmente ausente**: ela joga com `VKD3D_CONFIG=no_upload_hvv %command%`, sem o wrapper; `last_run` e `last_exit` **não existem** em `~/.local/state/hefesto-dualsense4unix/launch_env/`.

**O que diz para NÃO fazer (`:500-533`):**
- **O furo do cadeado por título fica de fora** (`profiles/autoswitch.py:252`, `:260-263`; `schema.py:666-702`) — é da AUTOMATISMO-MORTO-01; medido **zero** ocorrências no journal desde 25/07. Nota de ordenação: uma das curas propostas lá é **exigir `display_authority == "game"` no predicado do cadeado**, o que depende deste sinal ser confiável — *"A ordem certa é esta sprint primeiro."*
- **Não fazer `window_detect_healthy` decair** — não é o que a E5 propõe; o trinco continua trinco, muda **quem consulta**.
- **Não pôr o wrapper na opção de lançamento dela** (*"launch option é veneno"*, 16/07).
- **Não baixar a histerese de 30 s** (travada por teste; alt-tab curto não pode derrubar a posse).
- **Não reescrever o god-object** (`Daemon` ~3280 linhas, ~95 métodos).
- **Não reabrir o gate do teclado** (commit `2bbfa22`).

**Menções aos termos pedidos:** autoswitch 7×, window_detect 12×, catch-all 3× (os cinco nomeados no `profile_select_catch_all_sem_autoridade_em_jogo` das 01:51:04), prioridade só na coluna `prio` da tabela dos 15 perfis. **Debounce 0,5/12 s: ausente** (o que existe é histerese 30+30 s e tique de 2 s). **`apply_output_defaults`, `manual_override_categories`: ausentes.** **Aplicação imediata nas abas / reconciliação do rascunho: ausentes**, mas há a nota de validação (`:477-480`) de que a **aba Início só se reconcilia quando é a aba visível** (`home_actions.py:794-798`, `HOME_POLL_INTERVAL_MS = 2000` em `:49`) — *"Numa outra aba, o aviso mente."*

---

# 5. JANELA-FIEL-01 — a janela que para de reconciliar, e o botão morto no pacote

`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-31-JANELA-FIEL-01-a-janela-que-para-de-reconciliar-e-o-botao-morto-no-pacote.md` (649 linhas)

**Estado declarado:** `:3-6` — **ABERTA — documento de medição e plano. "Nenhuma linha de código, de teste ou de configuração foi tocada nesta rodada"**. Prioridade **ALTA nas E1 e E4** (as duas mexem no perfil dela sem perguntar e sem avisar), **MÉDIA na E2, E3 e E5**, **BAIXA na E6**. Aberta em **31/07/2026**, HEAD `7bd0cb7`, daemon PID 3615 e janela PID 7271 vivos e intocados.

**Defeito que cura:** cinco defeitos com a mesma assinatura — a janela continua desenhando com confiança um estado que ela deixou de ter.

**ENTREGAS e símbolos prometidos:**

### E1 — o reload que não volta trava a janela no perfil anterior (`:125-203`)
Este é **o item que mais casa com "reconciliação do rascunho com o perfil ativo"**.
- Caminho feliz: `app/app.py:737-770`. O buraco em três linhas: **`app.py:764` `self._draft_reload_for = ativo`** (marca o alvo ANTES de disparar o worker); **`app.py:753` `if self._draft_reload_inflight or self._draft_reload_for == ativo: return`**; **`app.py:707-716` e `:718-723`** — `_apply` só escreve `self.draft`/`_active_profile_name`/`_draft_baseline` `if draft is not None`, e `_falhou` só solta o `inflight`: **nenhum dos dois limpa `_draft_reload_for`**.
- Worker **`_compute_draft_from_active_profile`** devolve `(None, "")` em dois casos que **não distingue** (`app.py:688-692`): perfil ativo inexistente em disco (permanente) e `daemon_state_full()` sem resposta (transitório). Timeout de **0,25 s** cobrindo conexão e leitura (`app/ipc_bridge.py:45-66`, `:71`, `:173-178`).
- Decisão que a cura **não pode** desfazer: `app.py:233-237` (zerar o latch reabre IPC + I/O a 2 Hz para sempre), congelada por `tests/unit/test_gui_draft_reconcilia_perfil_ativo.py:126-142`.
- Dois desenhos: **(a) prazo no latch**, receita pronta em `app/actions/home_actions.py:800-839` com a lição em `:803-810` (**`_home_inflight`**, *"uma reconciliação de 2 s levar 78 s na tela"*); **(b) o worker dizer qual das duas falhas foi** (terceiro valor ou exceção própria).
- Estrago descrito: as abas seguem mostrando e editando o perfil ANTERIOR, "Salvar Perfil" vem pré-preenchido com o nome anterior (`footer_actions.py:263-264`) e **"Aplicar" empurra as seções do perfil anterior por cima do perfil do jogo** — o estrago que `app.py:226-231` descreve como já curado (R-08). O latch só se solta quando o perfil ativo virar um **terceiro** nome.
- Armadilha do teste existente: `:126-142` diz `# 5 segundos de ticks` e roda dez chamadas em microssegundos de relógio real — **uma cura por prazo passa nele sem ser exercitada**; a entrega tem de injetar relógio.

### E2 — os dois relógios mais rápidos apontam para a aba pelo número (`:205-263`)
- Regra violada: `app/app.py:777-782` (comentário do mapa **`_REFRESH_POR_ABA`**) e `app.py:909-913` (a mesma lição com o nome **EST-10**).
- Ofensores: **`status_actions.py:1269-1271`** `if notebook is not None and notebook.get_current_page() != 1: return True` a **10 Hz** (`LIVE_POLL_INTERVAL_MS = 100`, `constants.py:41`); **`home_actions.py:795-797`** `get_current_page() == 0` a cada 2 s (`HOME_POLL_INTERVAL_MS = 2000`, `home_actions.py:49`).
- Cura: extrair **`_id_da_pagina_corrente()`** de `_on_notebook_switch_page` (`app.py:821-842`), que já desembrulha `GtkScrolledWindow` + `GtkViewport` e lê `Gtk.Buildable.get_name(alvo)`. O desembrulho não é opcional: **`_wrap_notebook_pages_in_scroll`** (`app.py:890-935`) envolve oito das nove páginas.
- Glade de hoje: `main_notebook:190`, `tab_home_box:197`, `tab_status_box:251`. Consumidor: `_sync_status_cards` (`status_actions.py:1580-1593`); decisão STATUS-02 em `status_actions.py:1661-1664`. Formato de teste: `tests/unit/test_notebook_switch_page.py`.

### E3 — "Restaurar Padrão" só acha o arquivo na máquina de quem programou (`:265-323`)
- **`footer_actions.py:36`: `_MEU_PERFIL_ASSET = ROOT_DIR / "assets" / "profiles_default" / "meu_perfil.json"`**; `ROOT_DIR` é `Path(__file__).resolve().parents[3]` (`constants.py:37`, aviso em `:34-36`); verificação `footer_actions.py:464`, toast `:465-474`; `install.sh:1162` instala com `-e`.
- Cura: promover **`_seed_source_file(fname)`** (`profiles/loader.py:113-126`, já usado em produção em `:273` e `:414`) a pública, **ou publicar `asset_de_preset(nome)`**, e chamar dela no rodapé. Tupla de candidatos: **`_DEFAULT_SEED_SOURCE_DIRS`, `loader.py:101-106`** (o achado dizia `:102-105` — **corrigido**). Semeadura em `profiles/loader.py:129-152`.
- Teste de hoje que **não morde**: `tests/unit/test_footer_restore_default.py:69-72` faz `pytest.skip`; ponto de injeção a preservar: `:243-246` monkeypatcha `_MEU_PERFIL_ASSET` como atributo de módulo.

### E4 — "Navegacao" sem acento come a "Navegação" dela sem perguntar (`:325-407`)
- **`footer_actions.py:269-270` `_existing_names()`** e **`footer_actions.py:273` `if nome in existentes and not gui_dialogs.prompt_overwrite_existing(...)`**; texto do diálogo `gui_dialogs.py:92`.
- Identidade é o **slug**: `save_profile` grava `<slugify(name)>.json` (`profiles/loader.py:606-607`); `slugify` `profiles/slug.py:24-36`; R-10 escrita em `draft_config.py:499-505`; **`mesmo_slug`** e **`find_by_slug`** existem em `profiles/slug.py:52-71` e `:74-84`.
- Quem já usa certo: `profiles_actions.py:977`, `:1004` (comentário `:999-1003`), `cli/cmd_profile.py:58`, `to_profile` `draft_config.py:506-508`. **O rodapé é o último que não usa.**
- Cura: trocar por **`find_by_slug(nome, perfis)`** e citar **o nome do perfil do disco**, como `profiles_actions.py:1017`.
- **Amarra explícita com a AUTOMATISMO-MORTO-01** (`:372-385`): se o perfil ativo for outro, `mesmo_perfil` é falso e o arquivo é regravado com **`MatchAny()`** (`draft_config.py:522-526`) e prioridade de **`_prioridade_acima_dos_catch_all`** (`profiles_actions.py:1425-1439`) = **15** — a "Navegação" dela (prioridade 50, com regra de janela e processo) **vira um sexto catch-all**.
- Teste de hoje que não testa nada do R-10: `tests/unit/test_footer_actions.py:227-243` (só nome idêntico).

### E5 — a TUI desenha gatilho zerado e analógico centrado (`:409-463`)
- **`tui/app.py:163-171`**: `_apply_preview(l2=0, r2=0, lx=128, ly=128, rx=128, ry=128, battery=status.get("battery_pct"))`, com o comentário `# daemon.status não inclui analog ainda` em `:164`. Relógio 10 Hz `tui/app.py:149`, `_tick_preview` `:151-174`, `IpcClient.connect()` por tique `:161`, `except` `:172-174`, `_apply_preview` `:176-195`, `BatteryMeter` real em `:170`.
- Dado que existe: **`daemon/ipc_handlers.py:1400-1411`** (`:1405-1410`) devolve **`l2_raw`, `r2_raw`, `lx`, `ly`, `rx`, `ry`** no **topo** do `daemon.state_full` — mesmos nomes dos parâmetros. Widgets: **`TriggerBar`**, **`StickPreview`**.
- Hoje **nenhum** teste toca `_tick_preview` (`tests/unit/test_tui_app.py` cobre montagem, `q` e a barra de status).

### E6 — os relógios continuam correndo com a janela na bandeja (`:465-522`)
- (a) **`tray.py:191`** e **`compact_window.py:122`** descartam o id de `GLib.timeout_add_seconds`; `AppTray.stop()` `:288-296`; `CompactWindow.stop()` `:129-135` — **sem nenhum chamador em produção** (`:102-103`, grep de hoje: só a definição). Padrão certo: `rumble_actions.py:317-327` (`GLib.source_remove`).
- (b) Quatro pollers: **`_tick_live_state`** 10 Hz `status_actions.py:1263-1271`; **`_tick_profile_state`** 2 Hz `status_actions.py:1306-1319` (sem gate); **`_tick_reconnect_state`** 0,5 Hz `status_actions.py:1334-1347` (sem gate); **`_tick_home_state`** `home_actions.py:794-798`. Esconder a janela: `on_window_delete_event` `app.py:397-419`, `_has_persistent_access` `:421-438`, precedente do microfone `app.py:408-410`. Medido: **12,5 chamadas IPC por segundo** com a janela escondida na aba Status. O relógio da bandeja **deve continuar**.
- Cuidado declarado: `show_window` é `show_all()` + `present()` (`app.py:559-561`); a linha de bateria é escondida por código (`status_actions.py:1666-1671`) e **não** tem `no-show-all` no glade (`main.glade:363-372`).

**Notas datadas de caducidade/refutação (seção "Antes de tudo: eu reconferi cada linha citada", `:39-68`, e "Onde eu me afasto do achado original", `:70-103`):**
- *"o .deb não embala `profiles_default`"* → **REFUTADO** (`scripts/build_deb.sh:133`).
- *"morto no AppImage e no Flatpak"* → **REFUTADO como causa** (`scripts/build_appimage_gui.sh:112-117`; `flatpak/br.andrefarias.Hefesto.yml:170-183`, FIX-FLATPAK-PRESET-SEED-01 de 30/07). *"O botão morre mesmo assim — mas por causa do resolvedor, não do pacote."* Só o **wheel puro** não embala (`pyproject.toml:84-91`).
- `profiles/loader.py:102-105` → **corrigido** para `:101-106` + existe `_seed_source_file` em `:113-126`.
- `rumble_actions.py:322-326` → **ajustado** para `:322-327` (comentário `:317-320`); `constants.py:34-38` → **ajustado** para `:34-36` + `ROOT_DIR` em `:37`.
- E1: *"a cura sugerida reabriria um defeito já curado"*. E5: *"o remédio é menor do que o achado sugere"*. E6: *"são quatro relógios, não três, e o quinto é legítimo"*.
- **Caducidade de âncoras de glade** (`:63-68`): *"as âncoras de linha do `main.glade` andaram desde 29/07. A LARGURA-01 cita `profiles_paned` em `:1481`; hoje é `:1527`"*; tudo depois de Rumble deslocou ~46 linhas. **Citação de glade envelhece rápido.**

**O que diz para NÃO fazer (`:589-618` + `:105-123`):**
- **Não mexer em empacotamento** — a E3 não toca `pyproject.toml`, `build_deb.sh`, Flatpak nem AppImage. Arch/Fedora/Nix ficam fora.
- **Não tratar como defeito** três decisões registradas: o latch `_draft_reload_for` existir separado de `_active_profile_name` (`app.py:233-237`); `on_player_led_toggled` sem sinal no glade (`main.glade:980-1010`); `on_emulation_open_toml` órfão (`main.glade:2478-2489`) — as duas últimas são da **entrega 5 da BOTÃO-QUE-NÃO-MENTE-01**, não desta.
- A janela compacta (SEGUNDA-JANELA-01, `compact_window.py:9-12` contra `:54-58`, `:61-69`), os dois docstrings do `ipc_bridge` (`:543-551`, `:589-593` → SOM-02), `_refresh_daemon_view` síncrono e redesenhar o painel da TUI **ficam de fora**.
- **Regra explícita:** E1 e E4 **não vão para commit** sem os passos 1, 2 e 5 da validação feitos **por ela, na tela**, com foto antes e depois (PROVA-DE-TELA-01, `:583-587`).

**Menções aos termos pedidos:** **autoswitch** só 2× (`:129` — o tick de 2 Hz vê o perfil ativo mudar por fora, inclusive *"autoswitch ao abrir o jogo"*; e o passo 1 da validação, `:552-557`). **catch-all** 5× e **prioridade** 9×, todos na E4 (15 vs 50) e na nota final *"Se o número 15 é o certo"* (`:647-649`, decisão da PERFIL-NASCE-CERTO-01). **window_detect: zero.** **Debounce, `apply_output_defaults`, `manual_override_categories`: zero.** **Reconciliação do rascunho com o perfil ativo: é a E1 inteira.**
**Achado de observabilidade que atrapalha qualquer verificação por log** (`:626-634`): *"o journal do usuário tem **zero** linhas da janela nos últimos sete dias (`apptray_started`, `gui_draft_*`, `draft_carregado_*`, `footer_*`: nenhuma)"* — o `stderr` da janela não chega ao journal.

---

# 6. APLICAR-VERDADE-01 — o rodapé não mente mais, a ponte ainda mente

`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-08-01-APLICAR-VERDADE-01-o-rodape-nao-mente-mais-a-ponte-ainda-mente.md` (220 linhas)

**Estado declarado:** `:3-8` — **ENTREGUE EM CÓDIGO, com um resto medido**, medido em **01/08/2026**: a cadeia daemon → IPC → rodapé está completa e coberta por teste; o que ficou de fora é **`ipc_bridge.apply_draft()`, que estreita a verdade de volta para um `bool`**. Prioridade MÉDIA. Aberta em **01/08/2026** pela auditoria de 31/07 (`docs/process/estudos/2026-07-31-auditoria-geral-o-que-treze-agentes-mediram.md:316`).
**Distinção declarada (`:20-23`):** a **01** fez a falha SUBIR do daemon até a tela (campo `failed`); a **02** consertou a *contabilidade* que decidia baixar o `dirty` (`footer_actions.py:196`, `:604`, `:622`). A 02 é anexo da JANELA-CEGA-01 (`:217`).

**Defeito que cura:** `profile.apply_draft` responde `{"status": "ok"}` sempre, então o rodapé anunciava *"Perfil aplicado ao controle."* com as sete seções fora.

**Cadeia já existente (símbolos com chamador provado, `:45-60`):** `daemon/ipc_draft_applier.py:40-44` (**`self.failed: dict[str, str]`**), `:90-111` (`self.failed[section] = motivo[:120]`), `:71-73` (zera a cada `apply`), `:74-87` (as **sete seções**: `leds`, `triggers`, `controllers`, `rumble`, `mouse`, `keyboard`, `mic`); `daemon/ipc_handlers.py:456` (**`{"status": "ok", "applied": applied, "failed": dict(applier.failed)}`**), `:443-448` (contrato); `app/actions/footer_actions.py:65-80` (**`_NOMES_DE_SECAO`**: `leds`→`luzes`, `triggers`→`gatilhos`, `rumble`→`vibração`, `mic`→`microfone`, `controllers`→`ajustes por controle`), `:80-82` e `:595-599`, `:635-657` (**`_mensagem_de_aplicacao`**, três frases), `:578-584` (**`_lista_de_secoes`**), `:235-241` (**`on_apply_draft`** usa `ipc_bridge.call_async`, que entrega o **dicionário** — por isso o rodapé enxerga o `failed`); `po/en.po:1553`; `tests/unit/test_aplicar_verdade_rodape.py` (334 linhas).

**ENTREGAS:**

### E1 — este documento existe, e o código deixa de apontar para o vazio (`:105-112`)
- Sete citações órfãs em três arquivos de `src/`: **`footer_actions.py:65`, `:581`, `:638`; `ipc_handlers.py:443`; `ipc_draft_applier.py:6`, `:40`, `:104`**.
- Aceite: `grep -rn "APLICAR-VERDADE-01" docs/process/sprints/`. **Nenhuma linha de `src/` muda.**

### E2 — a ponte para de estreitar a verdade (`:114-142`) — **este é o resto pendente**
- **`app/ipc_bridge.py:495-523`: `def apply_draft(draft_dict) -> bool`**; recusa `status != "ok"` e trata `applied` vazio como no-op (`:518-521`, cura R-18 de 23/07), **mas o `failed` nunca sai da função**.
- Dois chamadores que pagam: **`app/actions/lightbar_actions.py:568-577`** (mensagem `:586-590`: *"não consegui aplicar a cor — o Hefesto pode estar desligado…"*) e **`app/actions/lightbar_actions.py:653`** (mensagem `:669`: *"Falha (daemon offline?)"*).
- Duas formas: **aditiva — `apply_draft_detalhado()`** ao lado, com `apply_draft()` virando invólucro e o **`__all__` do bridge (`ipc_bridge.py:653`)** ganhando um nome; ou **direta** — mudar o tipo de retorno.
- Obrigatório reusar **`_mensagem_de_aplicacao`** (`footer_actions.py:635`) *"para não nascer um terceiro dono da frase"*.
- Armadilha nomeada: **`_algo_foi_aplicado`** (`footer_actions.py:603-618`) e `_mensagem_de_aplicacao` (`:635-657`) tratam resposta SEM os campos novos como sucesso, de propósito — **quem mexer na ponte não pode fazer o `bool` cru virar `{"status": "ok"}` sintético**.

### E3 — o contrato do `status` fica travado por escrito (`:144-152`)
- `status` é `"ok"` para sempre; motivo em `daemon/ipc_handlers.py:445-448` e `app/ipc_bridge.py:509-512`. **Esta entrega é a frase, não código.**

**Teste que a E2 precisa criar (`:169-181`):** dublê de **`_safe_call`** devolvendo `{"status": "ok", "applied": [], "failed": {"leds": "erro qualquer"}}` → a frase da Lightbar tem de conter **`luzes`** e **não** conter `desligado` nem `offline`; com falha de transporte, a frase de hoje permanece. Nota de portão (`:162-167`): `tests/unit/test_aplicar_verdade_rodape.py` chama **`exigir_gi_real()`** nas linhas 20 e 26, então **pula no CI** (`.github/workflows/ci.yml`, `CI-GUI-PULAVA-CALADO-01`).

**O que diz para NÃO fazer (`:183-199`):**
- **Não trocar o `status` por `"partial"`/`"failed"`** — applet, CLI e TUI decidem pelo `status`.
- **Não traduzir `_NOMES_DE_SECAO` no ponto de declaração** (nasce no import, antes do `init_locale()`); a tradução acontece em `_lista_de_secoes` (`footer_actions.py:592`).
- **Não fazer resposta sem `applied`/`failed` virar erro** — `_algo_foi_aplicado`, **`_secao_aplicada`** e `_mensagem_de_aplicacao` não podem divergir.
- **Não criar uma quarta frase de estado** — a Lightbar tem de **chamar** `_mensagem_de_aplicacao`, não copiar (RADAR-01/E4).
- **Não confiar no campo `Status:`** de nenhum documento citado.

**O que NÃO foi medido (`:201-220`):** não rodou a janela; não provocou falha de seção real (o motivo é cortado em 120 caracteres, `ipc_draft_applier.py:111`); **não mediu o caminho `led_set` clássico** (com controle selecionado nunca toca em `apply_draft`); não conferiu o catálogo `.mo` (o mapa de 29/07 registra os `.mo` três commits atrás dos `.po` e sem gate).

**Menções aos termos pedidos:** **autoswitch, catch-all, window_detect, debounce, `apply_output_defaults`, `manual_override_categories`: zero.** Prioridade só no cabeçalho. O único vizinho de "aplicação imediata nas abas" é a contabilidade do `dirty` por seção (herdada da 02, `footer_actions.py:196`, `:604`, `:622`).

---

# Lista consolidada de símbolos prometidos (para o seu grep de chamador em produção)

**Nunca existiram no fonte até onde as sprints afirmam (a criar):**
`regra_de_jogo_para` · módulo `profiles/promocao.py` · `_game_window_last_class` / `game_window_last_class` · `store.autoswitch_ultimo_motivo` (+ campos de última `wm_class` julgada e último candidato) · `_id_da_pagina_corrente()` · `asset_de_preset(nome)` (ou `_seed_source_file` promovida a pública) · `apply_draft_detalhado()` · evidência de processo vivo do jogo (E4/SINAL-DE-JOGO).

**Existem mas a sprint declara sem chamador / sem fiação:**
`WindowReaderDiag.last_reason` → `record_window_detect_read(..., reason=...)` **não fiado** em `daemon/subsystems/autoswitch.py::_build_diag_window_reader` (JANELA-CEGA-01:152-166) · `window_detect_seeing()` **não consumido** por `daemon/lifecycle.py:3163` (SINAL-DE-JOGO-01/E5) · `MOTIVO_JOGO_SEM_PERFIL_PROPRIO` / `MOTIVO_SEM_CANDIDATO` / `MOTIVO_SELECIONADO` **não chegam ao IPC** (AUTOMATISMO-MORTO-01/E0) · `find_by_slug` / `mesmo_slug` **não usados pelo rodapé** (JANELA-FIEL-01/E4) · `_seed_source_file` **não usado pelo rodapé** (E3) · `CompactWindow.stop()` **sem nenhum chamador em produção** (E6) · `on_emulation_open_toml` e `on_player_led_toggled` **registrados no dicionário e sem sinal no glade**.

**Existem e a sprint prova chamador:** `apply_profile_mode(... origin="game_signal")`, `perfil_declara_modo_de_jogo`, `perfil_e_regra_de_jogo`, `_prioridade_acima_dos_catch_all`, `_window_detect_payload()`, `WINDOW_DETECT_BLIND_AFTER_SEC`, `HYSTERESIS_SEC`, `_NOMES_DE_SECAO`, `_mensagem_de_aplicacao`, `_algo_foi_aplicado`, `_secao_aplicada`, `applier.failed`.
