# agente ab4c66591d2146796 (sessao 6eac9113)

**tipo:** general-purpose
**tarefa:** Conflito de perfis em jogo
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

Repositório: /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix (Hefesto DualSense4Unix, Python). LEVA 1 — ENTENDER. NÃO altere nenhum arquivo; só leia, meça e proponha.

TEMA: **por que os perfis entram em conflito e se alteram sozinhos DURANTE a partida, e por que ativar um perfil não aplica imediatamente o que está nas abas.**

A mantenedora relata: *"ao jogar um jogo os perfis ficam em conflito e se alteram sempre"* e *"o perfil que eu ativei ele não aplica imediatamente as features das abas nele"*.

Investigue com arquivo:linha:
1. TODOS os atores que podem trocar/reaplicar perfil em runtime: `profiles/autoswitch.py` (tique, debounce, cadeado, veto), `daemon/subsystems/game_signal.py`, `daemon/subsystems/autoswitch.py`, `daemon/lifecycle.py` (restore de boot, reconexão, hotplug), `daemon/subsystems/hotkey.py`, `daemon/connection.py`. Monte a lista completa de "quem pode mudar o perfil ativo, e com que gatilho".
2. A frequência: com que taxa o autoswitch avalia? Onde está o debounce e a assimetria (entrar rápido / sair devagar)? Cite números reais do código.
3. O CICLO DE CONFLITO: reconstrua, passo a passo, como dois ou mais atores podem se alternar reescrevendo o perfil durante uma partida. Use o journal da usuária como evidência: rode `journalctl --user -u hefesto-dualsense4unix --since "2026-08-05 02:00" --no-pager` e procure `profile_activated`, `profile_autoswitch`, `autoswitch_`, `profile_select_`. Mostre a sequência real.
4. "NÃO APLICA IMEDIATAMENTE": investigue o que acontece entre clicar Ativar e o hardware mudar. O que é aplicado sincronamente, o que é adiado, o que é ignorado. Procure especificamente: `manual_override_categories` (categorias travadas atravessam a ativação como None), `_emu_manual_ts` e os locks de emulação (`lifecycle.py`), `profile_mode_revert_skipped`, `adiado=` no log, `relatorio` do `profile.switch`. Quais SEÇÕES de um perfil (gatilhos, cor, rumble, máscara, mouse, áudio) podem não entrar, e por quê.
5. Meça: qual a lista de razões pelas quais uma seção do perfil NÃO é aplicada numa ativação manual? Enumere-as com o log que cada uma emite.
6. PROPOSTA DE SOLUÇÃO: desenhe (a) como tornar a ativação manual AUTORITATIVA e imediata — a escolha dela vence tudo; (b) como impedir a alternância entre atores durante a partida; (c) qual feedback a janela deve dar quando uma seção for propositalmente adiada (hoje o daemon devolve um `relatorio` — a GUI o usa?). Inclua os testes que morderiam.

Separe MEDIDO de HIPÓTESE. Cite evidê

## RESULTADO FINAL

I have everything measured. Here is the investigation.

---

# LEVA 1 — ENTENDER: por que os perfis brigam e por que "Ativar" não aplica

Nada foi alterado. Todas as citações são `arquivo:linha` absolutas.

## [!] Descoberta que muda a leitura de tudo (MEDIDO)

**O daemon que produziu o journal NÃO é o código da árvore.**

- Daemon vivo: PID 1670, subiu **04/08 23:39:46** (`systemctl --user show`, `ActiveEnterTimestamp`).
- `/mnt/Apate/.../daemon/ipc_handlers.py` tem **mtime 05/08 03:02** e está `MM` no `git status`; `git log -S "TRAVA-QUE-SOLTA-TARDE-01"` **não acha nada** → a cura **nunca rodou**.
- `~/Desenvolvimento/...` e `/mnt/Apate/Desenvolvimento/...` são **a mesma árvore** (mesmo inode, 7227150).

Ou seja: a sprint `docs/process/sprints/2026-08-05-TRAVA-QUE-SOLTA-TARDE-01-*.md` já curou **uma** das causas do "não aplica imediatamente" — e a queixa dela é de um daemon **sem** essa cura. **Reiniciar o daemon é o primeiro ato desta leva.** Mas ele não fecha o caso: as outras causas abaixo sobrevivem à cura.

---

## 1. Todos os atores que trocam/reaplicam perfil (MEDIDO)

| # | Ator | Arquivo:linha | Gatilho | `origin=` | Frequência |
|---|---|---|---|---|---|
| 1 | **Autoswitch** | `profiles/autoswitch.py:547` | janela em foco mudou e ficou estável | `autoswitch` | avalia **2 Hz**, ativa quando `candidate != _current_profile` |
| 2 | **IPC `profile.switch`** | `daemon/ipc_handlers.py:437` | GUI / CLI / bandeja / applet COSMIC | `manual` | por clique |
| 3 | **Hotkey PS+D-pad** | `daemon/subsystems/hotkey.py:181` | botão físico | `manual` | por gesto |
| 4 | **Restore de boot** | `daemon/connection.py:247` (fallback `:274`) | primeiro connect, via `lifecycle.py:748` | `system` | 1x por boot |
| 5 | **Reconexão/hotplug** | `daemon/connection.py:450` | 1ª transição offline→online (`if not restored:`) | `system` | 1x por vida do daemon |
| 6 | **Saída do Modo Nativo** | `daemon/lifecycle.py:970` (`_reapply_last_profile`, `:893`) | `set_native_mode(False, reapply=True)` | `system` | por gesto |
| 7 | **Reapply do alto-falante** | `daemon/connection.py:128` → `manager.reapply_speaker_on_connect` | **toda** transição offline→online | `system` | **todo replug/BT** |

Atores que **não** trocam o perfil mas reescrevem **seções** dele:

| Ator | Arquivo:linha | O que mexe |
|---|---|---|
| `aplicar_modo_jogo_padrao` / `reverter_modo_jogo_padrao` | `lifecycle.py:2001` / `:2102` | liga/desliga vpad+co-op, `origin=game_signal` — chamado **a cada tique de 2 Hz** por `autoswitch.py:237` |
| Dreno da pendência de `mode` | `lifecycle.py:2233`, agendado em `lifecycle.py:3414-3420` | reentra em `apply_profile_mode` a **1 Hz** |
| `aplicar_gamepad_para_multiplos_controles` | `lifecycle.py:1433`, poll `lifecycle.py:3394-3405` | pode chamar `set_gamepad_emulation(True, origin="profile")` a cada **2 s** |
| `_reassert_rumble` | `lifecycle.py:3475` | rumble, a cada **200 ms** |
| `reassert_resolved_outputs` | `manager.py:373` | LEDs, ao fim de **todo** `apply` |

Pause/resume **não** tocam no perfil (`lifecycle.py:771-796`).

---

## 2. Frequência, debounce e a assimetria (MEDIDO — números reais)

| Constante | Valor | Onde |
|---|---|---|
| Poll do autoswitch | **0,5 s (2 Hz)** | `profiles/autoswitch.py:41` |
| Debounce para **ENTRAR** | **0,5 s** | `profiles/autoswitch.py:42` |
| Debounce para **SAIR** rumo a catch-all | **12,0 s** | `profiles/autoswitch.py:58` |
| Lock manual de perfil | **30,0 s** | `daemon/state_store.py:31` (`MANUAL_PROFILE_LOCK_SEC`) |
| Lock de gesto de emulação (`_emu_manual_ts`) | **30,0 s** (mesma constante) | `lifecycle.py:418`, lido em `:1892`, `:2052`, `:2339` |
| Histerese do sinal de jogo | **30,0 s**, avaliada a cada **2 s** | `game_signal.py:62`; poll em `lifecycle.py:3412` |
| Dreno da pendência de `mode` | **1 Hz**, mais cedo **30 s** após o gesto | `lifecycle.py:3414-3420`, `:2195` |
| Timeout do IPC da GUI | **0,25 s** | `app/ipc_bridge.py:104` |

### A assimetria de 12 s **não protege ela** (MEDIDO)

```python
# profiles/autoswitch.py:392-396
if profile is None or not self._current_especifico:
    return False                       # ← sai aqui
...
return bool(getattr(profile, "e_catch_all", True))
```

`_current_especifico` é gravado em `autoswitch.py:554` a partir do perfil que **entrou pelo autoswitch**. Os perfis de jogo dela (`sackboy_nativo`, `vitoria`) são **`{"type":"any"}`** → `_current_especifico = False` → **o debounce lento nunca arma**. E o alvo da fuga (`Navegação`) é `criteria`, não catch-all → a segunda condição também falharia. **Toda transição dela custa 0,5 s (1 tique).**

---

## 3. O CICLO DE CONFLITO — reconstruído com o journal dela

### 3.a A causa de fundo: o jogo perdeu o dono, e a Steam ganhou (MEDIDO)

Cinco perfis no disco dela são **catch-all** `{"type":"any"}`:

| arquivo | prioridade | `mode` |
|---|---|---|
| `fallback.json` | 0 | — |
| `vitoria.json` | 0 | gamepad/dualsense/coop |
| `meu_perfil.json` | 1 | — |
| `pragmata.json` | 5 | — |
| `sackboy_nativo.json` | **191** | gamepad/dualsense/coop |

E **`navegacao.json` casa `"steam"` e `"Steam"` por `window_class`** (prioridade 50, `criteria`).

A chave de seleção é `(not e_catch_all, priority)` — `manager.py:786-787`. Logo:

- Janela **`steam`** (cliente/overlay) → `Navegação` casa por **critério** → `(True, 50)` **vence** `sackboy_nativo` `(False, 191)`. **Especificidade vem antes de prioridade, e 191 não serve para nada.**
- Janela **`steam_app_1599660`** (o jogo) → só catch-all casa → veto `manager.py:768-777` → `profile_select_catch_all_sem_autoridade_em_jogo` → **retém** o perfil corrente.

**Prova de que o perfil do jogo foi destruído no meio da sessão:**

```
02:24:41  autoswitch_cadeado_cedeu_a_regra_de_jogo candidate=sackboy_nativo wm_class=steam_app_1599660
02:24:42  profile_activated name=sackboy_nativo origin=autoswitch priority=200
...
02:27:05  profile_select_catch_all_sem_autoridade_em_jogo candidatos=['Pragmata','fallback','meu_perfil','sackboy_nativo','vitoria']
```

Às 02:24:41 o cadeado só cede a `perfil_e_regra_de_jogo` **ou** `perfil_declara_modo_de_jogo` (`autoswitch.py:259-262`) — **os dois exigem `not e_catch_all`** (`schema.py:658`, `:722`). Portanto às 02:24 `sackboy_nativo` **tinha regra específica**. Às 02:27 ele já está na lista de catch-all. Hoje no disco é `{"type":"any"}`, mtime **02:51**.

**O mecanismo que apaga a regra** (MEDIDO, código):

```python
# src/hefesto_dualsense4unix/app/draft_config.py:578-581
match=(
    self.source_match
    if (mesmo_perfil and self.source_match is not None)
    else MatchAny()          # ← "Salvar Perfil" do rodapé zera a regra
),
```
e a prioridade vira `min(200, maior_catch_all + 10)` (`footer_actions.py:344-347` → `profiles_actions.py:1620-1634`). Isso explica a dança medida de prioridades do **mesmo** perfil: `sackboy_nativo` 200 → 22 → 191; `vitoria` 0 → 200 → 0.

### 3.b A sequência real do ping-pong (MEDIDO, journal)

```
02:29:33.1  profile_activated  name=sackboy_nativo origin=manual   ← ela escolhe, 4ª vez
            [lock manual de 30 s armado → expira 02:30:03]
02:30:45.9  profile_activated  name=Navegação origin=autoswitch priority=50
02:30:45.9  profile_autoswitch adiado=['suppression'] from_=sackboy_nativo to=Navegação wm_class=steam
```

O foco passou pela janela `steam` (overlay/biblioteca), `Navegação` casou por critério, 1 tique (0,5 s) de debounce, e **o perfil dela foi reescrito 72 s depois de ela escolher**. Daí em diante, ao voltar para o jogo, o veto retém `Navegação` — **o jogo roda com o perfil do navegador**.

Confirmação física: `Navegação` pinta `(97, 53, 131)`; `sackboy_nativo` pinta `(80, 60, 220)`.

```
02:46:43 / 02:46:46 / 02:50:52  lightbar_reassert_skip_cache rgb=(97, 53, 131)
```
Às 02:50:52 ela já tinha clicado "Ativar sackboy_nativo" **quatro vezes** (02:48:56, 02:49:43, 02:49:44, 02:50:19) e o controle **ainda estava roxo do Navegação**.

### 3.c O segundo eixo de alternância: o MODO, a 2 Hz (MEDIDO)

Independente do perfil, `autoswitch.py:237` chama `_sincronizar_modo_jogo_padrao` **a cada tique**:

```
02:44:31.7  modo_jogo_padrao_adiado  estado=ignorado_sem_jogo  motivo=sem_autoridade_de_jogo
02:44:33.0  game_signal_transition   de=daemon → game
02:44:33.2  profile_mode_aplicado    kind=gamepad origin=game_signal
02:45:43.5  modo_jogo_padrao_solto   de=steam_app_1599660 motivo=janela_fora_do_jogo
02:46:25.3  game_signal_transition   de=game → daemon (histerese expirada)
```

Ela alterna **liga vpad / solta vpad** por causa de foco de janela, **dentro da partida**. E `lifecycle.py:1911-1913` faz **toda** ativação de perfil com `origin != "game_signal"` zerar `_modo_jogo_padrao` — então cada `profile.switch` dela **re-arma esse ciclo do zero**.

### 3.d O terceiro eixo: `_current_profile` do autoswitch é cego a ela (MEDIDO)

`_current_profile` é escrito **num único lugar**: `profiles/autoswitch.py:551`, dentro de `_activate`. **Nada** o sincroniza com `store.active_profile` (grep no `src/` inteiro confirma). Prova no journal:

```
02:24:42  profile_autoswitch  from_=None  to=sackboy_nativo
```
`from_=None` embora `vitoria` estivesse ativo desde o boot (23:40:18). O autoswitch decide sobre uma crença **desatualizada** do que está valendo.

---

## 4. "NÃO APLICA IMEDIATAMENTE" — o que acontece entre o clique e o hardware

### 4.a A GUI **sempre** desiste antes da resposta (MEDIDO)

```python
# app/actions/profiles_actions.py:1107-1112 — sem timeout_s
call_async(method="profile.switch", params={"name": name}, ...)
# app/ipc_bridge.py:104 — timeout_s: float = 0.25
```

O handler só responde **depois** de `activate` + `save_active_marker` + `materialize_launch_env` (`ipc_handlers.py:437 → :456 → :466 → :471`). No journal:

```
02:40:22.106  profile_activated          name=Pragmata origin=manual
02:40:23.323  launch_env_materializado                       ← +1,217 s
02:40:23.378  profile_activated          name=Pragmata       ← ela clicou de novo
02:40:24.195  launch_env_materializado                       ← +0,817 s
02:40:24.201  profile_activated          name=Pragmata       ← e de novo
```

A resposta chega **1,2 s** depois; o cliente desistiu aos **0,25 s**. Consequências, todas em código:
- `_on_profile_switch_failure` (`profiles_actions.py:1124-1128`) mostra **"Falha (daemon offline?)"**;
- `_on_profile_switch_success` (`:1114-1122`) **nunca roda** → a linha **não fica em negrito** e a seleção **não sincroniza**;
- o daemon, porém, **executou a ativação inteira**.

Ela vê "falhou", clica de novo — e cada clique é uma ativação real. Os três `profile_activated` em 2 s **são isso**. O applet COSMIC tem o mesmo defeito (`packaging/cosmic-applet/src/ipc.rs:30`, `IPC_TIMEOUT = 250 ms`).

### 4.b As categorias travadas atravessam a ativação como `None` (MEDIDO)

```python
# profiles/manager.py:341-348
trigger_left = None if "trigger" in travadas else left,
trigger_right= None if "trigger" in travadas else right,
led          = None if "led"     in travadas else effective.lightbar,
player_leds  = None if "led"     in travadas else settings.player_leds,
```
`None` = "não mexe". Journal:
```
02:40:22  profile_apply_respeita_override_manual categorias=['audio','led','rumble','trigger'] profile=Pragmata
02:48:56  profile_apply_respeita_override_manual categorias=['led','rumble','trigger'] profile=sackboy_nativo
02:51:10  profile_apply_respeita_override_manual categorias=['led','rumble','trigger'] profile=sackboy_nativo
```
**Na ativação do Pragmata, as QUATRO seções foram puladas — a ativação não escreveu nada visível.**

As categorias **não têm TTL nenhum** (`state_store.py:102`, `:220-253`) — só somem por gesto explícito. E qualquer mexida nas abas as arma: `trigger.set`→`ipc_handlers.py:673`, `led.set`→`:769`, `led.player_set`→`:817`, `rumble.set`→`:2649`, `rumble.stop`→`:2666`, `speaker.set`→`:3009`, `profile.apply_draft`→`ipc_draft_applier.py:57-69` (payload sem seção mapeável arma **as três**). A pré-visualização ao vivo dos Gatilhos (`triggers_actions.py:241-260`, 300 ms) re-arma `trigger` **a cada mexida de slider**.

É exatamente o que a `TRAVA-QUE-SOLTA-TARDE-01` cura (`ipc_handlers.py:430`, `hotkey.py:168` agora **antes** do `activate`) — **e que ainda não rodou**.

### 4.c O lock de 30 s adia mode/mouse/supressão/rumble (MEDIDO)

`_emu_manual_ts` (`lifecycle.py:418`) é armado por gesto manual em `set_native_mode` (`:854`), `set_mouse_emulation` (`:1049`), `set_gamepad_emulation` (`:1229`), `set_coop_enabled` (`:1302`), `mark_rumble_policy_manual` (`:2540`). Uma ativação `origin="manual"` **fura** o lock (`_furar_lock_de_emulacao`, `:1592-1619`), mas **`autoswitch` e `system` não furam** — e só `mode` tem retentativa.

A pendência de `mode` (`lifecycle.py:2233-2294`) tem **quatro formas de morrer**:
1. espera `carimbo_manual + 30 s` (`:2195`, `:2252`);
2. **descartada** se `_emu_manual_ts` mudou (`:2254-2261`) — **qualquer `profile.switch` manual no meio mata a pendência**;
3. **descartada** se `store.active_profile` mudou (`:2262-2271`) — trivial com autoswitch a 2 Hz;
4. **segurada indefinidamente** enquanto `display_authority == "game"` e a mudança for destrutiva (`:2272-2282`).

---

## 5. Lista completa: por que uma seção NÃO entra numa ativação (MEDIDO)

| # | Seção | Motivo | Log emitido | Estado no `relatorio` |
|---|---|---|---|---|
| 1 | trigger / led | categoria travada → `None` no `OutputSpec` | `profile_apply_respeita_override_manual` (`manager.py:328`) | **não aparece** |
| 2 | speaker | categoria `audio` travada | `profile_speaker_ignorado_trava_manual` (`manager.py:621-626`) | `ignorado_trava_manual` |
| 3 | mouse | gesto manual < 30 s | `profile_mouse_skipped_manual_lock` (`lifecycle.py:1658-1666`) | `adiado_lock_manual` |
| 4 | suppression | gesto manual < 30 s | `profile_suppression_skipped_manual_lock` (`:1555-1563`) | `adiado_lock_manual` |
| 5 | suppression | reverter, mas perfil é catch-all | `profile_suppression_revert_skipped motivo=catch_all_sem_opiniao` (`:1574-1580`) | `ignorado_catch_all` |
| 6 | suppression | reverter, mas janela de jogo em foco | `..._revert_skipped motivo=janela_de_jogo_em_foco` (`:1581-1587`) | `ignorado_janela_de_jogo` |
| 7 | mode | gesto manual < 30 s (**silencioso se `mode=None`**) | `profile_mode_skipped_manual_lock` + `profile_mode_deferred` (`:1892-1902`, `:2199`) | `adiado_lock_manual` |
| 8 | mode | `kind=None` + catch-all | **`profile_mode_revert_skipped motivo=catch_all_sem_opiniao`** (`:1931-1938`) | `ignorado_catch_all` |
| 9 | mode | `kind=None` + janela de jogo | `profile_mode_revert_skipped motivo=janela_de_jogo_em_foco` (`:1939-1946`) | `ignorado_janela_de_jogo` |
| 10 | rumble_policy | gesto manual < 30 s (**silencioso se `policy=None`**) | `profile_rumble_policy_skipped_manual_lock` (`:2339-2348`) | `adiado_lock_manual` |
| 11 | rumble_policy | política inválida | `profile_rumble_policy_invalida` (`:2368-2372`) | `falhou` |
| 12 | speaker | `volume=None` | `profile_speaker_sem_volume_recusado` (`:2493-2497`) | `falhou` |
| 13 | speaker | backend sem suporte / sem handle | `profile_speaker_backend_sem_suporte` / `_sem_controle` (`:2498`, `:2514`) | `ignorado_sem_controle` |
| 14 | qualquer | applier levantou exceção | `profile_<x>_apply_failed` (`manager.py:485`, `:506`, `:529`, `:551`) | `falhou` |
| 15 | modo jogo padrão | sem autoridade de jogo | **`modo_jogo_padrao_adiado estado=ignorado_sem_jogo motivo=sem_autoridade_de_jogo`** (`:2046-2049`) | — |
| 16 | modo jogo padrão | gesto manual < 30 s | `modo_jogo_padrao_adiado estado=adiado_lock_manual motivo=gesto_manual_recente` (`:2052-2055`) | — |
| 17 | modo jogo padrão | modo nativo manual ativo | `modo_jogo_padrao_adiado estado=ignorado_gesto_dela motivo=modo_nativo_manual` (`:2063-2069`) | — |

**Buracos de rastro (MEDIDO):**
- `profile_autoswitch adiado=` (`autoswitch.py:565-569`) filtra `estado.startswith("adiado")` → **`ignorado_*` e `falhou` nunca aparecem**. O `adiado=['suppression']` de 02:30:45 estava **escondendo** o resto.
- Os casos 1 e 15-17 **não entram** no `relatorio` de forma alguma.
- `apply_profile_rumble_policy` **não tem** guarda de catch-all nem de janela de jogo — ao contrário de `mode` e `suppression`. Daí o `profile_rumble_policy_reverted` de 02:48:56 **dentro da sessão de jogo**.
- `_reapply_last_profile` (`lifecycle.py:964-967`) monta o `ProfileManager` **sem** `mode_applier`, `rumble_policy_applier` nem `speaker_applier` → sair do Modo Nativo **não** restaura essas seções.

**A GUI não lê o `relatorio` (MEDIDO).** `profiles_actions.py:1110` descarta o resultado (`lambda _result:`); `ipc_bridge.py:254-256` faz `ok, _ = _safe_call(...)`. Os únicos leitores de `secoes`/`mode_aplicado`/`motivo`/`expira_em_sec` no repositório inteiro são **testes**. E `manual_override_categories` **nem é exportado** por `daemon.state_full` (`ipc_handlers.py:1549-1625`). Não há **nenhuma** afordância na interface dizendo "esta seção está travada".

---

## 6. PROPOSTA DE SOLUÇÃO

### (a) Tornar a ativação manual AUTORITATIVA e imediata

1. **Reiniciar o daemon** — a `TRAVA-QUE-SOLTA-TARDE-01` já está na árvore e nunca rodou. Custo zero, elimina os casos 1 e 2 da tabela para gestos explícitos.
2. **Consertar o timeout do cliente.** `profiles_actions.py:1107-1112` → passar `timeout_s=3.0` (o irmão `footer_actions.py:236-242` já usa 1.5). Idem `packaging/cosmic-applet/src/ipc.rs:30`. Sem isso, **todo** clique em "Ativar" mostra "Falha (daemon offline?)" e ela clica de novo — a origem medida das rajadas.
3. **Ativação manual fura o lock de 30 s inteiro, não só parte dele.** Hoje `origin=="manual"` fura `_emu_manual_ts` (`lifecycle.py:1657`, `:1891`, `:2338`) mas **não** desarma `_manual_profile_lock_until` nem sincroniza o autoswitch. Fazer `_handle_profile_switch` e `hotkey._cycle` também **empurrarem `AutoSwitcher._current_profile`** (ver (b)).
4. **Fechar a assimetria do `apply`:** `apply_profile_rumble_policy` (`lifecycle.py:2350-2366`) precisa da mesma guarda `catch_all_sem_opiniao` que `mode` (`:1931`) e `suppression` (`:1574`) já têm.
5. **`_reapply_last_profile` (`lifecycle.py:964-967`) deve injetar os appliers que faltam** (`mode`, `rumble_policy`, `speaker`), como as outras três rotas já fazem.

### (b) Impedir a alternância entre atores durante a partida

6. **Sincronizar a crença do autoswitch.** Trocar `AutoSwitcher._current_profile` por uma leitura de `store.active_profile` (ou publicar nele em toda ativação, qualquer origem). É a correção de uma linha com maior alcance: hoje `autoswitch.py:304` compara contra um valor que gestos manuais nunca atualizam.
7. **Cadeado que vale durante a partida.** `autoswitch_locked` só protege contra perfis **sem** opinião de jogo (`autoswitch.py:259-262`); `Navegação` **é** `criteria` e por isso o cadeado cede... mas não para o jogo dela, e sim para a **Steam**. Propor: enquanto `display_authority == "game"`, o autoswitch **não troca de perfil**, ponto — só o modo sincroniza (que já é o desenho de `_sincronizar_modo_jogo_padrao`, chamado antes do cadeado, `autoswitch.py:237`).
8. **Tirar `steam`/`Steam` de `navegacao.json`** — ou, melhor, ensinar `select_for_window_ex` que a `wm_class` `steam` **durante** `display_authority == "game"` é o *chrome* do jogo, não uma janela de navegação. Hoje é a única razão de o ping-pong existir.
9. **Parar de destruir a regra do perfil.** `app/draft_config.py:578-581` escreve `MatchAny()` sempre que o nome não bate com `source_name`. Um perfil que já existe no disco **nunca** deveria perder o `match` por um "Salvar Perfil" do rodapé: ler o `match` do disco antes de gravar por cima. Sem isso, todas as outras curas são temporárias — o perfil do jogo volta a virar catch-all na próxima gravação.
10. **Prioridade não pode ser recalculada em silêncio.** `footer_actions.py:344-347` inventa `min(200, maior_catch_all + 10)`; foi isso que fez `sackboy_nativo` andar 200 → 22 → 191 num intervalo de 20 minutos.

### (c) O feedback que a janela deve dar

11. **Consumir o `relatorio`.** `profiles_actions.py:1110` deve receber o `result` e converter `secoes` numa frase — o vocabulário já existe pronto no irmão (`footer_actions.py:636-655`, `_mensagem_de_aplicacao`: *"Aplicado, menos: {seções}."*). Reaproveitar, não inventar.
12. **Completar o `relatorio` na origem.** As categorias travadas (caso 1) e o modo-jogo-padrão (casos 15-17) **não entram** no dicionário. `manager.py:326-332` já sabe quais são; basta gravá-las como `ignorado_trava_manual` por seção.
13. **Corrigir o filtro do log.** `autoswitch.py:565-569` deve reportar `ignorado*` e `falhou` também, não só `adiado*`.
14. **Exportar `manual_override_categories` em `daemon.state_full`** (`ipc_handlers.py:1549-1625`) e desenhar o cadeadinho por aba — hoje não há **nenhum** jeito de ela saber que a aba Gatilhos está travando a cor.

---

## 7. Testes que morderiam

Arranque a cura, veja reprovar, devolva.

| Teste | Morde | Arquivo-modelo existente |
|---|---|---|
| `steam` em foco durante `display_authority=="game"` **não** troca o perfil ativo | (b7) | `tests/unit/test_autoswitch_lock.py` |
| Ativação manual de X → tique do autoswitch com candidato X **não** re-ativa (crença sincronizada) | (b6) | `tests/unit/test_autoswitch_manual_lock.py` |
| `profile.switch` com `trigger`+`led`+`audio` travados devolve `secoes` marcando **as três** e a GUI mostra a frase | (c11, c12) | `tests/unit/test_trava_que_solta_tarde_01.py` |
| `on_profile_activate` com daemon que leva 1,2 s a responder chama `_on_profile_switch_success`, não `_failure` | (a2) | `tests/unit/test_profiles_gui_sync.py` |
| `to_profile` com nome que casa slug de um perfil no disco preserva `match` e `priority` | (b9, b10) | `tests/unit/test_profile_editor_roundtrip.py` |
| `apply_profile_rumble_policy(None)` com perfil catch-all **não** reverte | (a4) | `tests/unit/test_profile_rumble_policy.py` |
| `_reapply_last_profile` restaura `mode`/`rumble`/`speaker` | (a5) | `tests/unit/test_profile_mode.py` |
| `profile_autoswitch adiado=` reporta `ignorado_catch_all` | (c13) | `tests/unit/test_autoswitch.py` |

---

## 8. MEDIDO vs HIPÓTESE

**MEDIDO** (código lido e/ou journal/disco dela): tudo nas seções 1, 2, 4, 5; os números da tabela de frequências; as 5 profiles catch-all e o `"steam"` em `navegacao.json`; a reescrita `sackboy_nativo` específico→catch-all entre 02:24:41 e 02:27:05; o ping-pong de 02:30:45; a lightbar roxa de `Navegação` às 02:50:52 após quatro "Ativar" de `sackboy_nativo`; o timeout de 250 ms contra uma resposta de 1,2 s; a GUI descartar o `relatorio`; `_current_profile` sem sincronização; o daemon vivo não ter a `TRAVA-QUE-SOLTA-TARDE-01`.

**HIPÓTESE (bem apoiada)**: que a rajada de cliques seja consequência do toast "Falha (daemon offline?)" — o toast é certo (código), o espaçamento de ~1 s bate com re-clique humano, mas não há log do lado da GUI que prove o gesto.

**HIPÓTESE (a confirmar com ela)**: que o "Salvar Perfil" do rodapé tenha sido o gesto específico que apagou o `match` do `sackboy_nativo` entre 02:24 e 02:27. O caminho de código existe e é o único que escreve `MatchAny()` por cima de perfil existente (`draft_config.py:578-581`), mas o journal não registra gravações de perfil — **sugiro adicionar um `profile_salvo` com `match_antes`/`match_depois` antes de qualquer outra coisa**, porque essa é a causa que faz todas as outras voltarem.
