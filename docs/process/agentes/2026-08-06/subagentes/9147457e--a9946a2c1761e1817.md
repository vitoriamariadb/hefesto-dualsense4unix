# agente a9946a2c1761e1817 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Sprints do Sony e dos jogos instalados
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, ESCREVA DUAS sprints. Só documentação — **não toque em nenhum arquivo de `src/`, `tests/` ou `gui/`**, porque dois agentes irmãos estão editando código agora.

Leia antes, como exemplares de estilo e para não repetir o que já está escrito:
- `docs/process/sprints/2026-08-05-FIACAO-QUE-FALTA-01-o-verificador-que-ela-nao-tem-como-ver.md`
- `docs/process/sprints/2026-08-05-STEAM-QUE-DECIDE-01-ela-nao-tem-como-saber-quando-ligar.md`
- `docs/process/estudos/2026-08-05-o-sistema-de-perfis-o-que-dezessete-agentes-mediram.md` (seção 1.5)
Confira você mesmo, por grep/leitura, cada caminho:linha que citar.

---

**SPRINT 1** — `docs/process/sprints/2026-08-06-CONTROLE-SONY-MEDIDO-01-o-experimento-que-decide-metade-da-doutrina.md`

É o **M-04**, e ela pediu explicitamente que virasse sprint. A fala dela, em 06/08/2026:
> *"sobre testar o controle da Sony, coloca como sprint. Na época tínhamos testado e tinha dado certo, mas não sei hoje."*

Isso é informação NOVA e muda o grau: deixa de ser "nunca testado" e passa a ser **"testado e funcionou em data não registrada; sem verificação desde então"**. Registre assim, com honestidade — e registre que a data e as condições do teste antigo NÃO foram anotadas, que é justamente por isso que o experimento precisa ser refeito e DOCUMENTADO.

A pergunta: `UseSteamControllerConfig="2"` por jogo funciona com `SteamController_PSSupport="0"` global?

**A semântica correta, corrigida por ela em 06/08 — use ESTA, e não a das sprints anteriores:** a allowlist NÃO serve para "o Hefesto sair da frente". Quando um jogo tem conexão nativa com DualSense, os controles **aparecem dobrados** (Xbox e Sony, por causa do gamepad virtual do Hefesto). Permitir a allowlist faz o Hefesto **continuar funcionando**, com a saída sendo Xbox ou DualSense e as features que ela marcou nas abas. As sprints anteriores descrevem isso ao contrário — escreva uma **nota datada de correção** apontando quais afirmações caducaram, sem apagá-las. Confira o `DUPLO-REGISTRO-01` (`docs/process/sprints/2026-07-26-DUPLO-REGISTRO-01-o-steam-input-tem-dois-cadastros.md`), que registra "4 joysticks para 1 controle" — é a mesma observação do controle dobrado, e ela CONFIRMA a leitura dela.

A sprint precisa de: o protocolo do experimento em passos que ela possa seguir (ela tem um DualSense e a Steam), o que observar em cada passo, a tabela de leitura do resultado (incluindo o caso intermediário), e o que cada r

## RESULTADO FINAL

Duas sprints escritas, só documentação — nenhum arquivo de `src/`, `tests/` ou `gui/` foi tocado.

**SPRINT 1** — `docs/process/sprints/2026-08-06-CONTROLE-SONY-MEDIDO-01-o-experimento-que-decide-metade-da-doutrina.md` (396 linhas). Registra o grau novo do M-04 como *"testado e funcionou em data não registrada; sem verificação desde então"*, com o argumento de por que isso não decide nada (condições não anotadas + a Steam se atualiza sozinha). Traz a **nota datada de 06/08** corrigindo a semântica da allowlist, numa tabela que aponta as três afirmações que caducam na STEAM-QUE-DECIDE-01 sem apagá-las, e mostra que o `DUPLO-REGISTRO-01` (quatro joysticks para um DualSense) **confirma** a leitura dela. Registra a tensão que descobri: o código de hoje (`gamepad.py:166`, `:207-212`, `sync_steam_input_exception` em `:231`, `suspend_vpads_for_steam_input` em `:425`) **retira o vpad de cena** — literalmente "sair da frente" — o que contraria a fala dela; o que reconcilia os dois é que nenhum applier de lightbar/gatilho consulta os flags da exceção (SUSPEITA COM MECANISMO), e isso virou o passo 2.4 do protocolo. Protocolo em 5 passos, tabela de leitura com 7 linhas (inclusive os dois casos intermediários), mordida = o registro escrito, veto = nenhuma linha de código de Steam Input antes.

**SPRINT 2** — `docs/process/sprints/2026-08-06-JOGOS-QUE-ELA-TEM-01-escolher-da-biblioteca-em-vez-de-adivinhar-o-numero.md` (506 linhas). Sete achados medidos e quatro entregas (E1 unificar a varredura, E2 a lista no campo, E3 os textos, E4 a semeadura em lote com o risco declarado e o `--dry-run` primeiro).

**Achado próprio, MEDIDO nesta sessão e que não estava no mapa:** `~/.steam/steam` é symlink para `~/.steam/debian-installation`, e a dedução de `pastas_steamapps` (`steam_launch_options.py:809`) é `candidata not in pastas` — comparação de `Path`, que não resolve symlink. Resultado ao vivo: **43 manifests varridos, 22 appids únicos, 21 ocorrências repetidas**. Os dois consumidores atuais são imunes por acidente (`list_installed_appids` usa `set`; `nome_do_appid` retorna no primeiro acerto), mas qualquer lista nova iterando aquele módulo mostraria **cada jogo dela duas vezes**. Por isso a E1 vem primeiro, e em dois passos separados (dedup sozinha, reversível; depois a fusão).

**Números reais, contra a estimativa de 16-22:** ela tem **13 jogos** instalados (22 appids únicos menos 9 ferramentas filtradas pelo `_TOOL_MANIFEST_RE`) e **15 perfis** na pasta — a semeadura em lote dobraria a lista dela, o que reescreveu o tom da E4.

**Portões:** `git add -A` feito; `validar-glifos.py --all`, `validar-referencias-docs.py --all` (194 documentos) e `check_anonymity.sh` passam. O `validar-acentuacao.py --all` reprova com 2 violações em `tests/unit/test_home_render_state.py:447` (`metodo`) e `:652` (`media`) — arquivo de **agente irmão**, com status `MM` no git agora; meus dois arquivos passam isolados. Não corrigi por ser fora do meu escopo. Nada commitado.
