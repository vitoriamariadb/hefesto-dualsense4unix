# agente aadbb976f300ee644 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Escrever GRAVA-POR-UM-FUNIL-01
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, ESCREVA uma sprint que falta.

ANTES DE ESCREVER leia, como exemplar de estilo:
- docs/process/sprints/2026-08-05-TRAVA-QUE-SOLTA-TARDE-01-o-gesto-explicito-e-vitima-da-propria-trava.md
E leia a síntese consolidada:
- docs/process/estudos/2026-08-05-o-sistema-de-perfis-o-que-dezessete-agentes-mediram.md

CRIE o arquivo:
docs/process/sprints/2026-08-04-GRAVA-POR-UM-FUNIL-01-o-rodape-gravava-e-o-rascunho-nao-ficava-sabendo.md

Levante as evidências você mesmo:
  git grep -n "GRAVA-POR-UM-FUNIL-01"   (13 citações)
  git grep -n "REGRA-NAO-SE-PERDE-01"   (2 citações)
Leia integralmente src/hefesto_dualsense4unix/app/actions/profile_writer.py (módulo novo, 232 linhas, a docstring dele é quase a sprint pronta), o `git diff --cached` de app/actions/footer_actions.py, e tests/unit/test_gravacao_de_perfil_passa_pelo_funil.py.

O núcleo: o DraftConfig guarda uma fotografia do perfil de origem (`source_*`); o gate `mesmo_perfil` do `to_profile` responde False para sempre quando a fotografia envelhece, e cada save seguinte grava MatchAny() com prioridade max(catch-all)+folga. MEDIDO em 04/08: a catraca 10 → 20 → 30. `with_profile_identity` existia desde 25/07 e tinha UM chamador em produção.

Inclua REGRA-NAO-SE-PERDE-01 como SEÇÃO NOMEADA desta sprint (a auditoria recomendou isso: 2 citações só não sustentam arquivo próprio) — a herança do match do disco no salvar por cima.

Precisa ter: sintoma, evidência medida, causa-raiz, por que disciplina não bastava (o argumento do funil), a cura, os TRÊS portões estáticos do teste (nenhum save_profile novo em app/ fora do funil; a lista de autorizados só encolhe; o funil carimba origem=), a tabela cura→teste com mordida, e o que fica aberto — inclusive que `profiles_actions.py` é EXCEÇÃO DATADA na lista de autorizados e só pode encolher.

REGRAS DA CASA: português do Brasil acentuado (nome do arquivo sem acento); nada de MAC real; declare o grau de cada afirmação (MEDIDO / SUSPEITA / SEM PROVA); decisão medida não se apaga, ganha nota datada.
Escreva com Write. Retorne só um resumo de 5 linhas.

## RESULTADO FINAL

Sprint criada em `/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-08-04-GRAVA-POR-UM-FUNIL-01-o-rodape-gravava-e-o-rascunho-nao-ficava-sabendo.md` (staged; os quatro portões de documentação passam — acentuação, glifos, referências e anonimato).

Estrutura: sintoma (dois "Salvar Perfil" seguidos reescrevem o perfil) → evidência MEDIDA (catraca 10→20→30 em bancada e 11/21/31/41/51 sob Xvfb, `match=any` nos cinco) → causa-raiz (a fotografia `source_*` envelhece, o gate `mesmo_perfil` de `draft_config.py:562` responde `False` para sempre) → por que disciplina não bastava (`with_profile_identity` existe desde 25/07, docstring descreve o rodapé como o defeito, UM chamador em produção, três botões gravando) → a cura em três partes → REGRA-NÃO-SE-PERDE-01 como seção nomeada → três portões → tabela cura→teste → aberto.

Achados que valem relatar ao usuário: (1) a síntese de 05/08 lista *"o `match` não é herdado do disco"* como resíduo/defeito aberto nas seções 5.1 e 5.7 — isso **caducou**, a REGRA-NÃO-SE-PERDE-01 fechou depois de a síntese ser escrita; registrei como NOTA DATADA em vez de contradizer o documento. (2) Marquei honestamente que as mordidas do `match` e do `origem=` são **declaradas nas docstrings**, sem registro de execução independente — só as três primeiras linhas da tabela têm mordida MEDIDA na síntese. (3) A atribuição do 191 ao rodapé continua **SEM PROVA** (DIV-1, três teses incompatíveis), então a sprint não a afirma. (4) `profiles_actions.py` está registrado como exceção datada com a trava do Portão 2 (a lista só encolhe).
