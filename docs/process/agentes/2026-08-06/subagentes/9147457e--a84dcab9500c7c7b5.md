# agente a84dcab9500c7c7b5 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Co-op sempre ligado
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, EXECUTE uma decisão da dona, tomada mais de uma vez e ainda não cumprida.

A DECISÃO, palavra dela em 06/08/2026:
> *"Independente do que escolhermos, todos e tudo no Hefesto tem que tá com o permitir co-op ligado. Eu já havia pedido pra removermos até o botão da aba Início e tirar essa seção de lá também, já que isso não faz sentido — afinal, se eu conecto 4 controles no PC eu espero, com 4 pessoas jogando, que cada um controle o próprio personagem. Ninguém esperaria controlar o mesmo personagem com cada controle."*

A sprint `docs/process/sprints/2026-08-03-PEDIDOS-DELA-01-o-roteiro-dos-seis-pedidos-da-interface.md` JÁ prescreve isto (leia as linhas 34-46 e 51-57 e 72-75) — inclusive a nota datada e o caminho barato. **Ela pediu, virou sprint, e o botão sobreviveu.** Cumpra agora.

MAPA JÁ LEVANTADO (confira cada caminho:linha antes de editar — a árvore mudou hoje):

**A seção na aba Início** (é o único conteúdo Glade dessa aba): `src/hefesto_dualsense4unix/gui/main.glade:206-217` (comentário AUTO-01.2), `:218` `GtkFrame id="home_coop_frame"`, `:219` rótulo "Jogar acompanhada", `:221` `GtkBox id="home_coop_box"`, `:226` `GtkButton id="home_coop_prep_btn"` ("Preparar co-op"), `:235` `GtkLabel id="home_coop_prep_hint"`.
Fiação: `app/actions/home_actions.py:687-697`, `:1281-1332` (`_on_home_coop_prep_clicked`), `:1129-1145` (`_render_coop_prep`), `:431-490` (rótulos e dicas), `app/actions/mode_transition.py:171-231` (`plan_coop_prep`/`apply_coop_prep`).

**CONFIGURAÇÃO — o que deixa de ser opção:** `utils/session.py:527-596` (flag de opt-out e migração), `daemon/lifecycle.py:141-145` (`coop_enabled: bool = False` — o default tem de virar ligado), `:648-656`, `:1339-1375` (`set_coop_enabled`), `:2084-2087`, `daemon/ipc_handlers.py:3218-3238` (`coop.set`), `:1817`, `cli/cmd_coop.py` (`coop on|off|status`), `profiles/schema.py:426-429` (`coop: bool = True`), `profiles/loader.py:320-365` (a migração que JÁ existe e serve de molde).

**MECANISMO — NÃO TOCAR:** `daemon/subsystems/coop.py` inteiro (`should_be_active`, `player_count`, `player_indexes`, `numeros_de_jogador`, os player-LEDs), `core/led_control.py:92-97`, a numeração de jogador, o grab por jogador em `core/evdev_reader.py`, e principalmente `CoopManager.disable()` — a suspensão por Steam Input, que NÃO depende da flag e é caso legítimo (`app/actions/status_actions.py:185-210` documenta).

O CAMINHO BARATO que a sprint prescreve, e que v

## RESULTADO FINAL

**O botão SAIU** — e saiu porque o gesto de recuperação ganhou dono primeiro. Nada foi bloqueado.

**A ordem, que era a entrega.** Entreguei a 5 antes da 6: nasceu o IPC `coop.sync` (`ipc_handlers.py::_handle_coop_sync`, registrado em `ipc_server.py`), que roda `CoopManager.sync(force=True)` sem ligar, desligar, persistir ou tomar a posse do eixo `mode`. "Renumerar agora" virou **"Reconciliar jogadores"**: encadeia `coop.sync` → `identity.renumber`, com toast único (`reconciliar_toast`). E — além do roteiro — **deixou de ser desabilitado com jogo aberto**: o P2 cai *durante* a partida, e o gate antigo escondia o gesto na hora exata do defeito; agora a frase explica que só a numeração espera.

**Só então o botão saiu.** `main.glade:206-247` virou lápide — era o único conteúdo Glade da aba Início, que agora é 100% código. Junto foram `_render_coop_prep`, `_on_home_coop_prep_clicked`, `coop_prep_label/hint`, `plan_coop_prep`/`apply_coop_prep` e o parâmetro `coop=` do escritor de rascunho (ninguém mais o passava). No lugar ficou `_format_players_hint`, contando do `state_full`.

**A opção morreu, a forma sobreviveu.** `DaemonConfig.coop_enabled` → `True` com nota datada (e o boot **deixou de reler o disco**, matando o sósia da cura que a sprint nomeou); `coop.set {enabled:false}` devolve `status:"recusado"` com `motivo`, preservando `players`; `session.save_coop_enabled` só apaga e `load_coop_enabled` devolve `True` (lápides); o perfil parou de governar (campo `coop` **aceito e ignorado**, não removido — `extra="forbid"`); `coop off` explica e sai com código 2.

**Mecanismo intocado:** `subsystems/coop.py`, `led_control.py`, numeração, grab — zero linhas. `CoopManager.disable()` segue sendo a suspensão por Steam Input.

**12 mordidas, 12 reprovações** (piso do dataclass, recusa do `coop.set`, registro do `coop.sync`, `force=True`, botão de volta no Glade, `coop off` chamando IPC, opt-out ressuscitado, perfil governando, gate desabilitando, clique sem `coop.sync`, boot lendo o disco, frase apagada). Testes caducos reescritos com nota datada em 8 arquivos; os de teardown do `test_subsystem_coop.py` migraram para `disable()` (e agora medem o que a versão antiga não podia: o co-op **volta** no ciclo seguinte).

**Curei o retratador**, que a sprint apontava como pré-requisito: ele nunca montava a aba Início (só fotografava aquele frame). Sem a cura, o README ficaria com um retângulo vazio. Agora `_montar_aba_inicio` roda `install_home_tab` + `_render_home`, e o PNG mostra "2 controles = 2 jogadores" e "Reconciliar jogadores" — conferi a imagem.

**Gates:** `7083 passed, 1 skipped` (base 7055), ruff, mypy, acentuação, glifos, referências, anonimato, versão, packaging, test-data — todos verdes. Não commitei; tudo em `git add -A`. Não toquei em `profiles_actions.py`, `footer_actions.py` nem `app.py`. O primeiro run de baseline teve 1 falha de ordem aleatória em `test_poll_loop_evdev_cache.py` (passa isolada, some com `-p no:randomly`) — pré-existente, não minha.
