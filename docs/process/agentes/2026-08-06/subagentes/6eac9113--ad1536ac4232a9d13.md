# agente ad1536ac4232a9d13 (sessao 6eac9113)

**tipo:** general-purpose
**tarefa:** Integridade e corrupção
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

Repositório: /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix (Hefesto DualSense4Unix, Python). LEVA 1 — ENTENDER. NÃO altere NENHUM arquivo, e em especial NÃO escreva em ~/.config/hefesto-dualsense4unix (é a config real da usuária, já corrompida — só leitura).

TEMA: **como saber se um teste, um script ou a suíte corrompeu dados reais da usuária — e como impedir que possam.**

A mantenedora pergunta: *"como sabemos se algum teste ou algo a mais corrompeu algo?"*

Investigue com arquivo:linha:
1. HERMETICIDADE DA SUÍTE: a suíte de testes (398 arquivos em tests/) escreve fora de tmp_path? Procure por: uso de `Path.home()`, `os.environ["HOME"]` sem monkeypatch, `xdg_paths` sem override, `config_dir()` real, escrita em `~/.config`, criação de uinput real, chamadas a `systemctl`, `subprocess` sem sandbox. Existe conftest que isole HOME? Ver `tests/conftest.py`. Liste os testes que PODEM tocar dados reais, com evidência.
2. Existe a sprint `docs/process/sprints/2026-08-04-SUITE-QUE-SUJA-O-JORNAL-01-*.md` — leia-a e diga o que já está catalogado e o que ela NÃO cobre.
3. FORENSE do caso real: os perfis da usuária em ~/.config/hefesto-dualsense4unix/profiles/ foram corrompidos (viraram match=any com prioridades escaladas). Determine, por mtime e pelo journal, SE algum teste ou script tocou nesses arquivos, ou se foi só a GUI. A suíte completa rodou em 2026-08-05 00:44:43-00:48:07 e 01:14:03-01:16:50. Compare com os mtimes. Dê um veredito com evidência.
4. DETECÇÃO: existe hoje alguma validação de integridade ao carregar um perfil (`profiles/loader.py`, `profiles/schema.py`)? Um perfil semanticamente absurdo (catch-all com prioridade 191, perfil de jogo sem window_class) passa sem aviso? Existe backup automático? Existe o diretório `backup-*` — quem o cria?
5. PROPOSTA DE SOLUÇÃO, e é o coração do pedido dela: desenhe
   (a) um portão que PROVE que a suíte é hermética (falha se qualquer teste escrever fora de tmp_path/HOME isolado) — veja se dá para fazer com um autouse fixture + monkeypatch de HOME/XDG, e como detectar violação;
   (b) um verificador de integridade de perfis, rodável pela usuária (`doctor`?), que aponte perfis semanticamente suspeitos: catch-all com prioridade alta, perfil cujo nome sugere jogo mas sem window_class, prioridades empatadas no teto, mais de N catch-all;
   (c) backup automático versionado antes de toda escrita de perfil, com restauração fácil.
   Para cada um: onde mora, o teste que morde, e o custo.

Separe MEDIDO de HIPÓTESE. Cit

## RESULTADO FINAL

LEVA 1 completa. Nenhum arquivo alterado (todos os meus comandos foram de leitura; o *staging* que aparece no `git status` é de **outra sessão concorrente** — os dois caminhos são o mesmo inode `66305:6554096`).

## Veredito curto

**MEDIDO: nenhum teste e nenhum script tocou os perfis dela. Foi a GUI — e por um caminho que está documentado no código como deliberado.** A suíte é hermética *para perfis*. Mas ela **não** é hermética em geral, e o furo tem nome e linha.

---

## 3. FORENSE (respondo primeiro, é o que ela perguntou)

### A prova negativa: as janelas da suíte estão vazias

```
find ~/.config/hefesto-dualsense4unix -newermt "00:44:43" ! -newermt "00:48:07"  -> VAZIO
find ~/.config/hefesto-dualsense4unix -newermt "01:14:03" ! -newermt "01:16:50"  -> VAZIO
```

Nenhum arquivo — perfil ou não — foi escrito nas duas execuções. Os mtimes dos perfis alterados são **02:33:31, 02:42:39, 02:43:06, 02:43:19, 02:49:55, 02:51:09**, todos ≥ 77 min depois da segunda janela.

### Por que a suíte *não podia* tocar: o isolamento funciona

`tests/conftest.py:347-391` tem autouse `_hefesto_fake_env` que isola `XDG_CONFIG_HOME/DATA/CACHE/STATE` em `tmp_path/.xdg/`. Isso só funciona se `platformdirs` reler o env a cada acesso — `src/hefesto_dualsense4unix/utils/xdg_paths.py:12` constrói `_DIRS = PlatformDirs(...)` **no import**, o que seria fatal se fosse cacheado. **Medi: é lazy** (platformdirs 4.11.0, `user_config_dir` volta `/tmp/PROVA/...` após alterar o env num objeto já construído). Portanto `config_dir()` → `profiles_dir()` nunca resolve para o dir real sob teste.

### A prova positiva: a série temporal do journal

```
2026-08-02T21:26:15 sackboy_nativo 80
2026-08-02T21:31:36 sackboy_nativo 200     <- 80 -> 200
2026-08-03T21:36:48 vitoria 0
2026-08-05T02:24:42 sackboy_nativo 200
2026-08-05T02:29:32 sackboy_nativo 22      <- 200 -> 22
2026-08-05T02:33:54 vitoria 200            <- 0 -> 200
2026-08-05T02:49:43 sackboy_nativo 191     <- 22 -> 191
```

Nenhuma dessas transições cai numa janela de suíte. E os números são o que **um slider arrastado** produz: `src/hefesto_dualsense4unix/gui/main.glade:49-51` define `profile_priority_adj` com **`lower=0, upper=200`**. 200 é o fim do curso; 191 e 22 são posições de arrasto. A tooltip (linha 2017) diz "(0-200)". **191 não é corrupção — é o polegar dela parando ali.**

### O `match=any` é ANTERIOR e é deliberado

O `backup-20260726-233630/` guarda as cópias pré-corrupção. Diff contra o atual:

| perfil | match 26/07 | match hoje | priority 26/07 | priority hoje |
|---|---|---|---|---|
| `pragmata.json` | **`{"type":"any"}`** | `{"type":"any"}` | 5 | 5 |
| `vitoria.json` | **`{"type":"any"}`** | `{"type":"any"}` | 100 | **0** |

`pragmata` já era catch-all em 26/07 — **inalterado**. E `vitoria` teve a prioridade **reduzida** (100→0), não escalada. A premissa "viraram match=any com prioridades escaladas" está **parcialmente refutada**: o catch-all é antigo, e o movimento líquido de `vitoria` foi para baixo.

**O mecanismo está no código, com comentário:** `src/hefesto_dualsense4unix/app/draft_config.py:570-576`

```python
match=(
    self.source_match
    if (mesmo_perfil and self.source_match is not None)
    else MatchAny()
),
```

Salvar pelo rodapé com nome que **não bate com o slug de origem** faz o perfil nascer `MatchAny()`. O comentário nas linhas 555-568 diz textualmente: *"`MatchAny()` para nome novo é deliberado e casa com o contrato do diálogo do rodapé, que não tem campo de regra: o perfil nasce 'sempre'"*. Foi a cura de um bug anterior (herdar o regex do FPS). Os 5 catch-all (`fallback`, `meu_perfil`, `pragmata`, `sackboy_nativo`, `vitoria`) são todos `0600`; os presets semeados são `0664` — a permissão separa "escrito pelo app" de "semeado".

**Veredito: GUI, via o diálogo "Salvar Perfil" do rodapé + o slider 0-200. Zero participação de teste ou script.** (`Sackboy` subiu pela Steam às 02:24:02 e `sackboy_nativo.json` foi salvo às 02:51:09 — a sessão de jogo dela.)

Detalhe que confunde: os `.lock` mudam de mtime a cada ~70 s (03:29:27 → 03:30:37 → 03:33:51). Não é escrita — `loader.py:499-502` pega `FileLock` **para ler**; é o daemon (PID 1670) recarregando.

---

## 1. HERMETICIDADE — o que vaza de verdade

Isolado: XDG config/data/cache/state, `FAKE=1`, seed de presets, socket do broker. **NÃO isolado: `HOME`** — zero `setenv("HOME")` no autouse. Consequências medidas:

- **`src/hefesto_dualsense4unix/integrations/storm_doctor.py:34-36`** — `_ALLOWLIST_PATH = Path.home() / ".config/hefesto-dualsense4unix/steam_input_apps.txt"`, **constante de módulo, avaliada no import**, apontando para o arquivo REAL dela (existe, 721 bytes). Leitura, não escrita — mas o resultado de teste passa a depender do conteúdo dela. 3 arquivos tocam `storm_doctor`.
- **`app/actions/emulation_actions.py:718`** — `_WP_DROPIN_DIR = Path.home() / ".config/wireplumber/wireplumber.conf.d"`, mesma forma, e aqui o dir é de **escrita** em produção.
- Outros bypasses de `Path.home()`: `core/system_check.py:37`, `gui/widgets/button_glyph.py:74`, `utils/i18n.py:61`, `integrations/steam_launch_options.py:295,752`, `integrations/proton_pin.py:159,176,189`, `app/actions/daemon_actions.py:752`, `app/actions/emulation_actions.py:1193` (glob em `~/.steam/.../localconfig.vdf`).
- **uinput real**: 9 instanciações diretas de `UinputKeyboardDevice()` em 6 arquivos — `tests/unit/test_keyboard_emulator.py:34,71,231,240`, `test_keyboard_tokens.py:56,93`, `test_daemon_connect_grace.py:427,452`, `test_profiles_preset.py:406`. **Isto responde parcialmente o item 1 da lista "ainda NÃO medido" da SUITE-QUE-SUJA-O-JORNAL-01**: são 9 diretas para 17 dispositivos observados, logo ~8 nascem por fiação indireta do daemon.
- 18 arquivos citam `systemctl`; 58 usam `subprocess` real.

Só 4 arquivos protegem `HOME` por conta própria (`test_system_check.py:42,56`, `test_steam_input_desfazer.py:65,160`, `test_storm_launch_options.py:64,81`).

## 2. A sprint SUITE-QUE-SUJA-O-JORNAL-01

Cataloga: 17 teclados uinput/execução, 6 rajadas, contaminação do journal por `hefesto-bt-rebind`/`hefesto-bt-bonds`, amplificação pelo `input-remapper`, e a constatação de que limpador no fim é placebo. Cura E1-E4.

**Confirmei que continua viva** — o journal do sistema tem `pytest-of-vitoriamaria/pytest-6/...` às 00:45:07 e `pytest-17/...` às 01:14:26.

**O que ela NÃO cobre** (o buraco que a pergunta dela abre):
1. Só trata **journal e uinput**. Nada sobre **escrita em arquivo** fora de `tmp_path`.
2. Não menciona `HOME` nem os `Path.home()` de módulo — o vetor de vazamento que sobrou depois do BUG-TEST-CONFIG-LEAK-01.
3. O E4 propõe portão só para "não deixou rastro no journal"; **não há portão de sistema de arquivos**.
4. Não trata **integridade de dados** — nem detecção, nem backup, nem restauração.

## 4. DETECÇÃO — hoje não existe nenhuma

- `profiles/loader.py:499-502` — `_read_profile` faz só `Profile.model_validate(raw)`: sintaxe e tipo. Nada semântico.
- `profiles/schema.py:467` — **`priority: int = 0`, sem `ge`/`le`**. 191, 200, -5 e 10⁹ passam iguais.
- Catch-all com prioridade 191 e perfil de jogo sem `window_class`: **passam calados**. Hoje ela tem **5 catch-all**, dois deles (`pragmata`, `sackboy_nativo`) com nome de jogo.
- **Backup automático: não existe.** `backup-*` só sai de `scripts/purge.sh:86` e `uninstall.sh:1375`, e ambos criam **irmão** (`~/.config/hefesto-dualsense4unix.backup-<stamp>`), não o `backup-20260726-233630/` **interno** que ela tem — esse tem 3 arquivos e nenhum criador no repo. É órfão, manual, e por sorte é a única testemunha do estado pré-corrupção.
- `doctor` existe (`cli/cmd_doctor.py`, `cli/app.py:82`) e é infra pura — **não olha perfil nenhum**.
- `save_profile` (`loader.py:621`, escrita em `:724-738`) é atômica (`mkstemp`+`os.replace`) — protege contra truncamento, **não** contra sobrescrita semanticamente errada.

## 5. PROPOSTA

**(a) Portão de hermeticidade** — `tests/conftest.py`, autouse session-scoped. Monkeypatch de `HOME` para `tmp_path` no autouse existente **não basta** (os `Path.home()` de módulo já congelaram no import, e subprocessos/shell escapam). O detector que pega tudo é um **canário de sistema de arquivos**: no `pytest_sessionstart`, gravar `{path: (mtime_ns, size)}` de `~/.config/hefesto-dualsense4unix`, `~/.config/wireplumber`, `~/.local/share/hefesto-dualsense4unix`; no `pytest_sessionfinish`, refazer e **falhar listando os deltas**. Custo ~1 min de I/O. O teste que morde: escrever de propósito em `config_dir()` real dentro de um teste marcado e ver a sessão reprovar. Some a isto mover `_ALLOWLIST_PATH` e `_WP_DROPIN_DIR` de constante de módulo para função (`def _allowlist_path()`), que é o que torna o monkeypatch de `HOME` eficaz.

**(b) Verificador de perfis** — `src/hefesto_dualsense4unix/profiles/sanidade.py`, exposto como `doctor --perfis` em `cli/cmd_doctor.py`. Regras, todas com evidência no caso real dela: catch-all com `priority > N`; nome que casa lista de jogos mas `match.type == "any"` (pegaria `pragmata` e `sackboy_nativo`); prioridades empatadas (hoje `fallback`=0 e `vitoria`=0, `corrida`/`esportes`=55); mais de N catch-all (hoje 5); `priority` fora de 0-200 — a faixa que a própria GUI oferece. Custo baixo, é leitura pura. O teste que morde: um diretório-fixture com os 15 perfis reais anonimizados exigindo exatamente esses achados; arrancar uma regra e ver reprovar. **Ponto fino:** `fallback` é catch-all legítimo — a regra precisa de allowlist, senão vira alarme que ela aprende a ignorar.

**(c) Backup versionado** — dentro de `save_profile` (`loader.py:621`), sob o `FileLock` que já existe: antes do `os.replace`, copiar o alvo para `profiles/.historico/<slug>/<ts>.json`, retendo os últimos N. Restauração como `hefesto-dualsense4unix profile restore <slug> [--em <ts>]`. Custo: um `copyfile` por save e disco limitado por N (perfis têm ~1 KB). O teste que morde: salvar 3 vezes, conferir 3 versões e que a restauração devolve byte a byte; arrancar o backup e ver reprovar. **Isto é o que teria evitado a pergunta dela** — o `backup-20260726-233630` só existe por acaso, e sem ele eu não teria como provar que `pragmata` já era catch-all.

Prioridade que eu sugiro: **(c) primeiro** (é o que para o sangramento e custa menos), depois **(b)**, depois **(a)**.

**HIPÓTESE, não medido:** que o `MatchAny()` do rodapé seja *o* defeito a curar. É deliberado e resolveu um bug pior. O que os dados sustentam é mais modesto e mais útil — o rodapé cria catch-all **sem avisar**, e nada depois disso reclama. A cura pode ser só um aviso no diálogo, ou o (b).
