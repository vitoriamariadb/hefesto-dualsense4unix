# agente a82bcd7a5da463eb2 (sessao 9147457e)

**tipo:** Explore
**tarefa:** Mapear constantes e caminhos de import
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, investigue "constantes que apontam para arquivos reais do import".

Procure por:
1. Qualquer constante literal duplicada (string ou nome) que represente um nome de campo, chave de perfil, nome de arquivo ou caminho — que exista definida em mais de um módulo em vez de ser importada de uma fonte única.
2. Em especial em: src/hefesto_dualsense4unix/profiles/ (manager.py, loader.py, autoswitch.py, sanidade.py), src/hefesto_dualsense4unix/app/actions/ (footer_actions.py, profiles_actions.py, profile_writer.py), cli/cmd_profile.py.
3. Onde constantes de match/prioridade/campos de perfil estão hardcoded como string literal em vez de importadas do módulo que as define.
4. Liste os módulos que definem constantes "canônicas" de perfil e quem as importa vs quem redefine localmente.

Não altere nada. Retorne caminho:linha com o literal encontrado e onde deveria vir de. Seja thorough.

## RESULTADO FINAL

I have a complete picture. Here are the findings.

## 1. Duplicated priority constant — the biggest one

| Where | Line | Literal |
|---|---|---|
| `src/hefesto_dualsense4unix/profiles/sanidade.py` | **44** | `PRIORIDADE_MAXIMA = 200` |
| `src/hefesto_dualsense4unix/app/actions/profiles_actions.py` | **78** | `PRIORIDADE_MAXIMA = 200` |
| `src/hefesto_dualsense4unix/gui/main.glade` | **51** | `<property name="upper">200</property>` (`profile_priority_adj`) |

`sanidade.py:37-42` justifies the duplication ("`profiles/` não pode depender de `app/`") — but the argument only justifies **sanidade not importing app**. The reverse direction is legal and already used: `profiles_actions.py:35-55` already imports `profiles.loader`, `profiles.schema`, `profiles.simple_match`, `profiles.slug`. `sanidade` pulls only `schema` + `slug` (no GTK, no typer), so `profiles_actions.py:78` should be `from hefesto_dualsense4unix.profiles.sanidade import PRIORIDADE_MAXIMA`.

Aggravating: **no test asserts the two are equal**. `tests/unit/test_empate01_a_cor_volta_a_ser_dela.py:416` pins `pa.PRIORIDADE_MAXIMA == 200` and `:432` pins glade `upper == pa.PRIORIDADE_MAXIMA`, but `tests/unit/test_profiles_sanidade.py` never cross-checks. Changing the slider ceiling silently makes the doctor's "fora da faixa da janela" rule (`sanidade.py:287,295`) lie.

Consumers of the `app/` copy that would inherit the fix: `profiles_actions.py:1670`, `profiles_actions.py:1858`.

## 2. Section-state vocabulary — canonical in `daemon/lifecycle.py`, re-typed as literals in 3 modules

Canonical block: `src/hefesto_dualsense4unix/daemon/lifecycle.py:217-228`
```
APLICADO = "aplicado"; ADIADO_LOCK_MANUAL = "adiado_lock_manual"
IGNORADO_CATCH_ALL / IGNORADO_JANELA_DE_JOGO / FALHOU = "falhou" / IGNORADO_SEM_CONTROLE
```
Re-typed as raw string instead of imported:

- `profiles/manager.py:904` — `else "aplicado"` → `lifecycle.APLICADO`
- `profiles/manager.py:524, 543, 568, 597, 685` — `= "falhou"` → `lifecycle.FALHOU`
- `profiles/autoswitch.py:478, 489` — `self._estado_modo_jogo_padrao = "falhou"` → `lifecycle.FALHOU`
- `app/actions/profiles_actions.py:362, 366` — `== "aplicado"` / `!= "aplicado"` → `lifecycle.APLICADO`
- `daemon/ipc_handlers.py:470, 473, 476` — `"aplicado"` ×3, in the same package as the definition (no cycle argument at all)

Note the asymmetry: `profiles/manager.py:70` *did* create `IGNORADO_TRAVA_MANUAL = "ignorado_trava_manual"` as a named constant (with a comment at :64-69 explaining the cycle), but the same file then hand-types `"falhou"`/`"aplicado"` five more times. `IGNORADO_TRAVA_MANUAL` is also a member of lifecycle's `IGNORADO_*` family that lifecycle itself doesn't declare — the vocabulary has two owners.

Tests re-typing it too: `tests/unit/test_profile_speaker_section.py:438,528`, `tests/unit/test_daemon_speaker_wiring.py:331` (`"ignorado_trava_manual"` instead of `manager.IGNORADO_TRAVA_MANUAL`).

## 3. `steam_app_` — four regexes + one `startswith`, one with divergent case-sensitivity

- `profiles/manager.py:43` — `re.compile(r"^steam_app_\d+$", re.IGNORECASE)`
- `profiles/simple_match.py:21` — `re.compile(r"^steam_app_(\d+)$")` — **no IGNORECASE**
- `profiles/simple_match.py:25` — `re.compile(r"^\s*(?:steam_app_)?(\d+)\s*$", re.IGNORECASE)`
- `daemon/launch_env.py:93` — `re.compile(r"^steam_app_(\d+)$")` — **no IGNORECASE**
- `app/actions/launch_wrapper_dialog.py:68` — `re.compile(r"^steam_app_(\d+)$")` — **no IGNORECASE**
- `profiles/schema.py:692` — `wm_class.startswith("steam_app_")` — **case-sensitive**, followed by an explicit comment at :694-697 saying the divergence between predicates "é justamente o buraco que este módulo existe para fechar"

`manager.py:37-42` names the problem out loud ("a doutrina do `catch_all_sem_opiniao` precisa da MESMA noção de 'isto é um jogo' nos três lugares") and then implements a fifth copy. The single source that already exists is `daemon/launch_env.steam_appid_from_wm_class` (line 129, exported at :1055) — `state_store.py:378`, `game_signal.py:111`, `ipc_handlers.py:2505` and `lifecycle.py:1820` all use it; `profiles/`, `simple_match` and `launch_wrapper_dialog` do not.

## 4. Import-dialog choice tokens — literals with no definition anywhere

Producer: `app/gui_dialogs.py:273` `return "sobrescrever"` / `:275` `return "renomear"` (contract documented only in prose at `:249-250`).
Consumer: `app/actions/footer_actions.py:493` — `if escolha == "renomear":`

A *different* dialog reuses the same literal for a different meaning: `app/actions/profiles_actions.py:435` returns `"renomear"` / `:437` `"copia"`, consumed at `:1349`. Two independent protocols share the untyped token `"renomear"`. Neither has a module-level constant; a `Literal["sobrescrever","renomear"]` alias in `gui_dialogs` is the natural home. Test doubles hardcode it too: `tests/unit/test_perfil_salva_tudo_abas.py:241`.

## 5. History timestamp format — write side and read side don't share it

- `profiles/loader.py:659` — `datetime.now().strftime("%Y%m%dT%H%M%S_%f")` (write, `_carimbo_de_versao`)
- `cli/cmd_profile.py:324` — `_datetime.strptime(base, "%Y%m%dT%H%M%S")` (read, `_quando_legivel`)

`cmd_profile.py:322` also re-derives the separator convention (`split("_",1)[0].split("-",1)[0]`) that only `loader.py:713,718` produces. Changing the stamp in the loader turns the whole `profile historico` "Quando" column into raw stamps with no test failing. `loader.__all__` (`:1034-1049`) exports `HISTORICO_DIR_NAME`/`HISTORICO_MAX_VERSOES` but not the format.

## 6. `meu_perfil` — canonical asset name, three independent spellings

- `app/actions/footer_actions.py:47-48` — `_MEU_PERFIL_NOME = "meu_perfil"`, `_MEU_PERFIL_ARQUIVO = f"{_MEU_PERFIL_NOME}.json"` (the real file: `assets/profiles_default/meu_perfil.json`)
- `profiles/sanidade.py:81` — `"meu_perfil"` inside `VOCABULARIO_GENERICO`
- `app/gui_dialogs.py:297` — `'meu_perfil'` inside a translatable user-facing string (so renaming the asset silently produces a dialog naming a file that no longer exists)

Same class: `profiles/sanidade.py:48,66` `"fallback"` vs. the real `assets/profiles_default/fallback.json`, and `"padrao"`/`"perfil_padrao"` at `:67,82` which match no asset at all.

## 7. Preset filenames inside the loader

`profiles/loader.py` repeats `"coop_local"` in two forms across five sites with no constant: `:192` (`_FLAVOR_MIGRATION_PRESETS`, with `.json`), `:273`, `:274`, `:289`, `:411` (bare), `:434` (bare). `_PRESETS_DE_JOGO` (`:382`) is bare-slug while `_FLAVOR_MIGRATION_PRESETS` (`:192`) is `.json`-suffixed — two conventions for the same asset set, reconciled ad hoc at `:412` (`arquivo = f"{nome}.json"`).

## 8. Catch-all predicate — `e_catch_all` bypassed by a raw discriminator comparison

- Canonical: `profiles/schema.py:626` `Profile.e_catch_all` (handles `MatchAny` **and** empty `MatchCriteria`)
- `app/actions/profiles_actions.py:218` — `getattr(getattr(p, "match", None), "type", None) == "any"`
- `app/actions/profiles_actions.py:274` — same expression, negated

The narrowing is deliberate and documented at `:205-209`, but it re-implements the discriminator string `"any"` (defined at `schema.py:102` as `Literal["any"]`) rather than `isinstance(p.match, MatchAny)` — which `MatchAny` is already imported for (`profiles_actions.py:42`). Same family: `profiles_actions.py:169` `tipo == "criteria"` re-implements the empty-criteria half of `e_catch_all`; `loader.py:304` `match.get("type") != "criteria"`.

## 9. Manual-override categories

- Canonical: `daemon/state_store.py:59` `MANUAL_OVERRIDE_CATEGORIES = frozenset({"trigger","led","rumble","audio"})`, enforced at `:233`
- `profiles/manager.py:78` — `_CATEGORIAS_SILENCIADAS_NO_APPLY = frozenset({"trigger","led"})` (deliberate subset, but built from bare literals rather than `MANUAL_OVERRIDE_CATEGORIES & {...}`)
- `daemon/ipc_draft_applier.py:57-61, 68` — `{"led"}`, `{"trigger"}`, `{"rumble"}`, `{"led","trigger","rumble"}` all hand-typed; a typo here raises `ValueError` at runtime only (`state_store.py:232`)

## 10. Own-GUI wm_class list vs. where the names are actually set

`profiles/autoswitch.py:72-79` `OWN_GUI_WM_CLASSES` hardcodes `"hefesto-dualsense4unix"` and `"hefesto-dualsense4unix-gui"`. The values are actually *produced* by `app/main.py:183` (`GLib.set_prgname("hefesto-dualsense4unix")`) and `app/main.py:100` (`r"hefesto-dualsense4unix-gui"`), plus `packaging/cosmic-applet/src/app.rs` for the APP_ID. The comment at `:64-68` documents the provenance; nothing enforces it. Renaming the prgname makes the alt-tab retention silently stop working.

## 11. Lower-severity: cross-language and untyped-key duplication

- `.seeded_presets`: `profiles/loader.py:89` (`SEED_MARKER_NAME`) vs. `scripts/install_profiles.sh:46` (`readonly MARKER="${DEST_DIR}/.seeded_presets"`). Comment at `loader.py:86-88` calls it a "contrato COMPARTILHADO"; unavoidable across languages, but undertested.
- Seed source dirs `assets/profiles_default`: `loader.py:103-106` vs. `scripts/build_appimage_gui.sh:114-117`; the `/usr/share/hefesto-dualsense4unix/assets` prefix is separately re-typed at `daemon/service_install.py:59`, `gui/widgets/button_glyph.py:80`, `cli/cmd_doctor.py:26`, `app/actions/daemon_actions.py:574`.
- IPC method name `"profile.switch"` has no constant anywhere: `app/ipc_bridge.py:281`, `cli/cmd_profile.py:455`, `cli/cmd_tray.py:84`, `tui/app.py:235` (contrast with `PROFILE_SWITCH_TIMEOUT_S` at `ipc_bridge.py:49`, which *is* a shared constant).
- Raw JSON field names in the CLI's schema-bypassing reader: `cli/cmd_profile.py:311` `dados.get("match")`, `:312` `match.get("type")`, `:313` `dados.get("priority")` — deliberate (reads corrupt files that don't validate), but the names duplicate `Profile.match`/`.priority` with nothing linking them.

## Summary: canonical owners vs. redefiners

| Canonical module | Constant | Imported by | Redefined locally by |
|---|---|---|---|
| `profiles/sanidade.py` | `PRIORIDADE_MAXIMA/MINIMA`, `CATCH_ALL_LEGITIMOS`, `VOCABULARIO_GENERICO`, `MAX_CATCH_ALL_TOLERADOS` | `cli/cmd_doctor.py:91` only | `app/actions/profiles_actions.py:78`; glade `:51` |
| `daemon/lifecycle.py:217-228` | `APLICADO`/`FALHOU`/`ADIADO_LOCK_MANUAL`/`IGNORADO_*` | only within `daemon/lifecycle.py` | `profiles/manager.py:70,524,543,568,597,685,904`; `profiles/autoswitch.py:478,489`; `daemon/ipc_handlers.py:470-476`; `app/actions/profiles_actions.py:362,366` |
| `profiles/manager.py:60-70` | `MOTIVO_*`, `IGNORADO_TRAVA_MANUAL` | `profiles/autoswitch.py:28-29`; tests | `tests/unit/test_empate01_a_coluna_diz_quem_ganha.py:248`; `tests/unit/test_profile_speaker_section.py:438,528`; `tests/unit/test_daemon_speaker_wiring.py:331` |
| `profiles/loader.py:1034-1049` | `HISTORICO_DIR_NAME`, `SEED_MARKER_NAME`, `SEED_SKIP_ENV_VAR`, `HISTORICO_MAX_VERSOES` | `tests/unit/test_profile_historico.py:21`, `test_preset_seed_runtime.py:36` | timestamp format not exported → `cli/cmd_profile.py:324` |
| `daemon/launch_env.py:129` | `steam_appid_from_wm_class` | `state_store.py`, `game_signal.py`, `ipc_handlers.py`, `lifecycle.py` | `profiles/manager.py:43`; `profiles/simple_match.py:21`; `app/actions/launch_wrapper_dialog.py:68`; `profiles/schema.py:692` |
| `daemon/state_store.py:59` | `MANUAL_OVERRIDE_CATEGORIES` | `state_store.py:232` only | `profiles/manager.py:78`; `daemon/ipc_draft_applier.py:57-68` |
| `profiles/schema.py:65,102,134` | `Literal["criteria"/"any"/"manual"]` | via `isinstance` in `schema.py`, `simple_match.py` | `profiles_actions.py:144-149,169,218,274`; `loader.py:304` |
| `app/gui_dialogs.py:273-275` | (no constant) `"sobrescrever"`/`"renomear"` | — | `footer_actions.py:493`; `profiles_actions.py:435,1349` |

`app/actions/profile_writer.py` is clean — it imports everything it uses (`loader.save_profile`, `schema.Profile`, `slug.mesmo_slug`) and defines no literals of its own.
