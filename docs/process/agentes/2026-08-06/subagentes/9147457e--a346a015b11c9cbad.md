# agente a346a015b11c9cbad (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Unificar PRIORIDADE_MAXIMA
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, IMPLEMENTE a unificação da constante `PRIORIDADE_MAXIMA`, decidida pela dona em 05/08/2026 ("preciso que as constantes apontem pros arquivos reais do import").

ESTADO MEDIDO — três lugares, um portão faltando:
- `src/hefesto_dualsense4unix/profiles/sanidade.py:44` — `PRIORIDADE_MAXIMA = 200`, com comentário em `:37-43` declarando a duplicação deliberada ("profiles/ não pode depender de app/")
- `src/hefesto_dualsense4unix/app/actions/profiles_actions.py:78` — `PRIORIDADE_MAXIMA = 200` (a "fonte" que os outros citam)
- `src/hefesto_dualsense4unix/gui/main.glade:51` — `<property name="upper">200</property>`, XML que não importa nada
Portão existente: `tests/unit/test_empate01_a_cor_volta_a_ser_dela.py:414-432` já compara o glade com `pa.PRIORIDADE_MAXIMA`.
Portão FALTANDO: nada compara `sanidade.PRIORIDADE_MAXIMA` com `pa.PRIORIDADE_MAXIMA`. Esse é o buraco real.

ORDEM SEGURA — siga exatamente, 1 → 2 → 3 → 4 → 5:

1. **Crie o portão que falta ANTES de mover número nenhum.** Ele afirma `sanidade.PRIORIDADE_MAXIMA == pa.PRIORIDADE_MAXIMA == int(upper do glade)`. ATENÇÃO: leia o número de `profiles_actions.py` por AST em vez de importar o módulo — o import puxa `gi` e o portão morreria no CI headless. O padrão alternativo da casa é `exigir_gi_real` (`tests/unit/test_gravacao_de_perfil_passa_pelo_funil.py:52-58`); escolha o que couber e justifique na docstring. Rode e veja PASSAR (ele documenta o estado atual).

2. **Eleja a fonte canônica na camada mais baixa: `src/hefesto_dualsense4unix/profiles/schema.py`.** É de onde `draft_config.py:574` já lê o default de `priority`, então "quem manda na faixa" e "quem manda no default" passam a morar juntos. `schema.py` importa só stdlib + pydantic. Mova para lá `PRIORIDADE_MAXIMA` (e `PRIORIDADE_MINIMA`, se `sanidade.py:43` a tiver), exporte em `__all__`.

3. **`sanidade.py` vira import.** O comentário `:37-43` está CERTO e NÃO é violado (a constante desceu para `profiles/`, não subiu para `app/`) — ele ganha NOTA DATADA de 05/08, não é apagado.

4. **`profiles_actions.py:78` vira reexport** (`PRIORIDADE_MAXIMA = schema.PRIORIDADE_MAXIMA` ou import direto), MANTENDO o nome no módulo — seis asserções existentes importam `pa.PRIORIDADE_MAXIMA` (`test_empate01_a_cor_volta_a_ser_dela.py:128,349,416,432`; `test_salvar_nao_rebaixa_02_...py:128,467`) e quebrar isso transforma unificação em churn.

5. **O glade fica com o literal `200` + o portão.** Atualize o c

## RESULTADO FINAL

Unificação implementada e provada. Relatório:

1. **Fonte canônica** — `PRIORIDADE_MINIMA = 0` / `PRIORIDADE_MAXIMA = 200` agora moram em `/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/profiles/schema.py` (logo acima de `class Profile`, coladas no `priority: int = 0` de onde `draft_config` lê o default), exportadas no `__all__`.
2. **Portão que faltava** — novo `/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/tests/unit/test_teto_da_prioridade_tem_uma_fonte_so.py` (8 testes): fecha o triângulo `sanidade == profiles_actions == upper do glade` (+ `lower == PRIORIDADE_MINIMA`) e exige que não haja cópia (`is` contra o schema, e proibição do literal via AST). Criado e rodado ANTES de mover número: 4 verdes contra a árvore duplicada.
3. **AST em vez de `exigir_gi_real`**, justificado na docstring: `profiles_actions` faz `import gi` no topo, e um portão que confere números não pode SUMIR justamente no CI headless, que é onde ele mais vale.
4. **`sanidade.py`** virou `from ...schema import PRIORIDADE_MAXIMA, PRIORIDADE_MINIMA`; o comentário original ganhou NOTA DATADA de 05/08 dizendo que a justificativa continua CERTA (a constante desceu para `profiles/`, não subiu para `app/`) — nada apagado. `__all__` mantido.
5. **`profiles_actions.py`** virou reexport (`PRIORIDADE_MAXIMA = _schema.PRIORIDADE_MAXIMA`), nome preservado no módulo — as 6 asserções que importam `pa.PRIORIDADE_MAXIMA` seguem intactas.
6. **Glade** ficou com o literal `200` e o comentário atualizado: cita `profiles/schema.py` como fonte e nomeia o portão.
7. **Mordida 1** (glade `upper` 200→150): reprovaram `test_o_glade_acompanha_o_verificador` e `test_os_tres_lugares_dizem_o_mesmo_numero` (`assert 200 == 150`). **Mordida 2** (`schema.PRIORIDADE_MAXIMA` 200→175): reprovaram 5 testes, incluindo os 2 do portão antigo `test_empate01` — prova de que o fio novo puxa a cadeia inteira. Ambas devolvidas.
8. **Portões**: `7004 passed, 1 skipped`; ruff, mypy (162 arquivos) e os três validadores verdes. Ressalva: em 2 das 4 rodadas completas apareceu 1 falha ISOLADA e DIFERENTE a cada vez (`test_keyboard_wire_up`, `test_regra_nao_se_perde_02`, `test_autoswitch::test_aplica_apos_debounce`) — todas passam sozinhas, nenhuma toca meus arquivos, e o total de testes cresceu de 6995→7004 entre rodadas: é o outro agente mexendo em `draft_config.py`/`footer_actions.py` ao vivo. Não commitei; não toquei nos arquivos vetados.
