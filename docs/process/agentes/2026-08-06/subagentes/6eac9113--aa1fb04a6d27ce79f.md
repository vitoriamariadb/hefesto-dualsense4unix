# agente aa1fb04a6d27ce79f (sessao 6eac9113)

**tipo:** general-purpose
**tarefa:** Aba Perfis: guardas e resposta
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

Repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix. LEVA 2 — IMPLEMENTAR. Você PODE e DEVE alterar arquivos.

SEUS ARQUIVOS (não toque em outros de src/, para não colidir com agentes irmãos):
- `src/hefesto_dualsense4unix/app/actions/profiles_actions.py`
- `src/hefesto_dualsense4unix/app/gui_dialogs.py`
- `src/hefesto_dualsense4unix/app/ipc_bridge.py` (só a mudança de timeout descrita)
- `packaging/cosmic-applet/src/ipc.rs` (só o timeout)
- testes novos em `tests/unit/`

Um agente irmão está mexendo em `footer_actions.py`/`base.py`/`draft_config.py` — NÃO toque nesses.

TAREFAS (todas com defeito já provado; não reinvestigue):

**1. DEFEITO B — as guardas desligam no "Novo perfil".** `on_profile_new` (~:944-958) chama `_esquecer_a_fotografia_do_editor()` (~:1607-1618), que zera `_regra_do_disco`/`_prioridade_do_disco`. Em `_build_profile_from_editor` (~:1878-1887) as duas guardas SALVAR-NAO-REBAIXA-01 são `if <do_disco> is not None and not <foi_mexida>()` — com None, ambas são puladas e os widgets vencem. Medido: salvar por cima de um perfil existente leva `prio=200, criteria` para `prio=0, any`.
   A cura é de ESCOPO, não de remoção — `_esquecer_a_fotografia_do_editor` existe porque perfil novo não tem valor de disco a preservar. Faça o save por cima de um arquivo EXISTENTE reabilitar as guardas (reler a fotografia do disco no momento do salvamento, quando o alvo já existe).

**2. Diálogo de rebaixamento de PRIORIDADE — não existe hoje.** Só existem `prompt_overwrite_existing` e `confirm_downgrade_match_to_any` em `gui_dialogs.py`. E atenção a um furo verificado: `confirm_downgrade_match_to_any` só dispara se o match ORIGINAL for `MatchCriteria` — com os perfis já em `MatchAny` (o caso atual dela) o diálogo nunca aparece. Cure os dois: avisar quando a prioridade cai muito, e cobrir o caso do match já rebaixado.

**3. O timeout que faz TODO "Ativar" parecer falha.** `app/ipc_bridge.py:104` usa `timeout_s: float = 0.25`; o handler `profile.switch` leva ~1,2 s medidos no journal dela (activate + save_active_marker + materialize_launch_env). Resultado: `_on_profile_switch_failure` mostra "Falha (daemon offline?)", `_on_profile_switch_success` nunca roda, e ela clica de novo — cada clique é uma ativação real. Passe `timeout_s=3.0` na chamada de `on_profile_activate` (~:1107-1112); o irmão do rodapé já usa 1.5. Faça o mesmo no applet (`packaging/cosmic-applet/src/ipc.rs:30`, `IPC_TIMEOUT`).

**4. "Ativar" passa a refazer as abas na hora.** `on_profil

## RESULTADO FINAL

LEVA 2 implementada. Tudo verde, todas as mordidas verificadas (cura arrancada → reprovou → devolvida).

## O que mudou

**1. DEFEITO B — as guardas voltam a valer no Salvar por cima**
- `app/actions/profiles_actions.py:1044,1072` — `_esquecer_a_fotografia_do_editor()` saiu do topo de `on_profile_new` e foi para **depois** de posicionar os widgets (a mesma ordem que `_populate_editor` sempre teve). Antes, o `set_value(0)`/`set_active_id("any")` de nascimento levantava `_prioridade_tocada`/`_regra_tocada`, e a guarda acreditava numa escolha que ela não fez.
- `profiles_actions.py:1814` — novo `_perfil_que_o_salvar_sobrescreve(name)` (busca por **slug**, cache em memória, lição do R-10).
- `profiles_actions.py:2118-2140` — `_build_profile_from_editor` relê a fotografia do disco quando o alvo já existe; a evidência de intenção passa a ser só o **gesto** (não há assinatura de abertura, porque o editor nunca mostrou aquele perfil). Nota datada no comentário da SALVAR-NAO-REBAIXA-01 dizendo o que caducou.

**2. Os dois avisos de rebaixamento**
- `app/gui_dialogs.py:152` — `confirm_downgrade_priority(parent, name, de, para)`, novo.
- `gui_dialogs.py:105` — `confirm_downgrade_match_to_any` ganhou `regra_atual` (rótulo do que o perfil é HOJE, o mesmo da coluna "Quando usar").
- `profiles_actions.py:1379-1417` — a guarda do match virou `not isinstance(original.match, MatchAny)` (cobre `MatchManual` e o `criteria` vazio, que eram o furo), e a queda de prioridade passou a perguntar. Regra pura testável: `queda_de_prioridade_pede_aviso` + `QUEDA_DE_PRIORIDADE_QUE_PEDE_AVISO = 10` (a mesma folga do `_FOLGA_ACIMA_DO_CATCH_ALL`; queda de 5 pontos não vira ruído).

**3. O timeout**
- `app/ipc_bridge.py:34` — `PROFILE_SWITCH_TIMEOUT_S = 3.0`, fonte única; `profile_switch()` (`:283`) passou a usá-lo — **isso corrigiu também a CLI e o Salvar da aba Perfis**, que liam `False` de uma troca que tinha acontecido.
- `profiles_actions.py:1220` — `on_profile_activate` usa `timeout_s=PROFILE_SWITCH_TIMEOUT_S`.
- `packaging/cosmic-applet/src/ipc.rs:40` — **`SWITCH_IPC_TIMEOUT` (3 s) como constante própria**, e `switch_profile` (`:392`) usa `call_raw_with_timeout`. Não estiquei o `IPC_TIMEOUT` de leitura de propósito: ele cobre todo refresh do painel e penduraria 3 s num daemon morto — mesmo desenho que a casa já usa no `MODE_IPC_TIMEOUT`.

**4. Ativar refaz as abas**
- `profiles_actions.py:1247,1252,1281` — `_on_profile_switch_success` chama `_refazer_as_abas_apos_ativar`, que recarrega via `_bootstrap_draft_async` (fallback `_refresh_all_tabs`). Com edição pendente **pergunta** (`gui_dialogs.confirm_discard_pending_edits`, default = MANTER); se ela mandar manter, o toast diz que as abas seguem mostrando as alterações não salvas.

**5. A janela lê o relatório**
- `profiles_actions.py:297-388` — `relato_da_ativacao` traduz `secoes` (R-03) para `applied`/`failed` e `mensagem_de_ativacao` delega o texto a `footer_actions._mensagem_de_aplicacao` — vocabulário **reusado, não reescrito**. `mode_aplicado`/`motivo` não são lidos porque derivam de `secoes["mode"]` (`ipc_handlers.py:470-477`). Mapa local só para as seções que o rodapé não conhece (`mode`→modo, `suppression`→modo jogo, `rumble_policy`, `speaker`).

## As mordidas (arranquei, reprovou, devolvi)

| cura arrancada | reprovou |
|---|---|
| releitura da fotografia no salvar | `test_salvar_por_cima_preserva_regra_e_prioridade_do_disco` (`0 == 200`), `test_o_alvo_e_achado_pelo_slug...` |
| `_esquecer...` de volta ao topo de `on_profile_new` | `test_o_nascer_do_novo_perfil_nao_conta_como_gesto`, `test_olhar_o_perfil_antes_de_clicar_novo_nao_o_rebaixa` |
| diálogo de prioridade | `test_baixar_a_prioridade_pergunta_e_cancelar_nao_toca_o_disco` |
| guarda do match de volta a `MatchCriteria` | `test_so_manual_virando_vale_para_tudo_avisa...` |
| `regra_atual=` removido | 2 testes de rótulo |
| `timeout_s` do ativar | `test_ativar_pede_a_folga_e_nao_o_timeout_de_leitura` |
| timeout do `profile_switch` síncrono | `test_o_helper_sincrono_tambem_usa_a_folga` |
| `_refazer_as_abas_apos_ativar` | 3 testes de refresh/diálogo |
| toast sem relatório / `lambda _result:` | 3 testes de leitura do relatório |
| applet: `switch_profile` de volta ao `call_raw` | Python `test_o_switch_do_applet_usa_a_folga` **e** Rust `switch_profile_usa_a_folga_e_nao_o_timeout_de_leitura` |

## Portões

`pytest -k "profile or perfil or gui or dialog"` → **1004 passed**. `ruff check src/ tests/` → OK. Extras: `mypy src/hefesto_dualsense4unix` OK, acentuação/glifos/referências/anonimato/test_data/paridade/versão OK. `cargo test ipc::` no applet → **16 passed** (4 novos).

Dois testes falham na suíte completa e **não são meus**: `test_profiles_sanidade.py::test_nome_proprio_com_match_any_levanta_aviso` (o `sanidade.py` está em voo por outro agente) e `test_keyboard_wire_up.py::test_poll_loop_...` (flaky de tempo — passa quando o arquivo roda inteiro, falha isolado).

Ajustes mínimos em 3 testes existentes que não são de irmão (`test_r10_slug_e_rename.py:241`, `test_abas01_conflito_entre_abas.py:401`, `test_perfil_salva_tudo_abas.py:304`): os dublês de `confirm_downgrade_match_to_any` ganharam `**_kw` (assinatura nova) e um dublê para `confirm_downgrade_priority`. Nada commitado; os arquivos novos estão só `git add`-ados para os portões enxergarem.
