# agente a3ef69eb475645c33 (sessao 6eac9113)

**tipo:** general-purpose
**tarefa:** Daemon: crença e supressão
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

Repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix. LEVA 2 — IMPLEMENTAR. Você PODE e DEVE alterar arquivos.

SEUS ARQUIVOS (não toque em outros de src/, para não colidir com agentes irmãos):
- `src/hefesto_dualsense4unix/profiles/autoswitch.py`
- `src/hefesto_dualsense4unix/daemon/lifecycle.py`
- `src/hefesto_dualsense4unix/profiles/manager.py` (só o que a tarefa 4 pedir)
- testes novos em `tests/unit/`

NÃO toque em `daemon/ipc_handlers.py`, `daemon/subsystems/hotkey.py`, `app/*`, `profiles/loader.py`.

CONTEXTO (medido no journal dela, não reinvestigue): durante a partida, o perfil dela era reescrito sozinho. A janela `steam` (overlay/biblioteca) casa `window_class: "steam"` do perfil `Navegação`, que entra por autoswitch e retém o controle mesmo depois de voltar ao jogo.

TAREFAS:

**1. Sincronizar a crença do autoswitch (maior alcance por linha).** `AutoSwitcher._current_profile` é escrito num único lugar — `autoswitch.py:551`, dentro do próprio `_activate`. NADA o sincroniza com `store.active_profile`; um `profile.switch` manual dela nunca o atualiza. Prova no journal: `profile_autoswitch from_=None to=sackboy_nativo` com outro perfil ativo havia horas. Faça o autoswitch decidir contra o estado REAL (ler `store.active_profile`, ou publicar nele em toda ativação de qualquer origem).
   TESTE QUE MORDE: ativação manual de X seguida de tique do autoswitch com candidato X não deve re-ativar.

**2. A armadilha de mão única da supressão.** `lifecycle.py:1564-1567` LIGA `suppress_desktop_emulation` sem o gate `_perfil_tem_opiniao`; só o ramo que LIBERA tem o gate (`:1574`). Consequência medida: um perfil catch-all liga a supressão de mouse/teclado e nenhum outro catch-all consegue desligar — que é exatamente o estado do disco dela hoje (`sackboy_nativo` catch-all com `suppress_desktop_emulation=true`). Torne a assimetria simétrica: se catch-all não tem opinião para LIBERAR, também não tem para LIGAR.
   TESTE QUE MORDE: catch-all com `suppress=true` não deve ligar a supressão; e um estado já ligado tem de conseguir sair.

**3. `apply_profile_rumble_policy` não tem as guardas que os irmãos têm.** `mode` (`:1931`) e `suppression` (`:1574`) têm guarda de `catch_all_sem_opiniao` e de `janela_de_jogo_em_foco`; a política de rumble (`:2350-2366`) não tem nenhuma — daí um `profile_rumble_policy_reverted` medido DENTRO da sessão de jogo dela. Dê a ela a mesma guarda.

**4. Completar o `relatorio` na origem.** As categorias travadas (`manager.py:326-332`,

## RESULTADO FINAL

LEVA 2 implementada. Tudo verde, sem commit.

## O que mudou

**Item 1 — a crença do autoswitch decide contra o estado REAL** (`profiles/autoswitch.py`)
- `_store_de_estado()` (:200) e `_perfil_corrente()` (:215) — novos; `_perfil_corrente()` adota `store.active_profile` (que `ProfileManager.activate` publica em toda ativação, de qualquer origem) e re-arma `_current_especifico` conservadoramente. **Zero I/O**: a primeira versão relia o disco via `manager.get()` e o CANÁRIO-FS-01 flagrou escrita de `.lock` no diretório de perfis REAL dela — trocado por palpite `True` + correção de graça em `_tick:315` quando o candidato é o próprio corrente.
- Chamado no topo de `_tick` (:274) e de `_activate` (:574).
- Colateral declarado na docstring: o autoswitch deixa de "entrar" no perfil que o boot restaurou. Isso **respeita** o `BUG-BOOT-RESTORE-FLIPS-EMULATION-01` — o restore monta o manager com `mouse_applier=None`/`mode_applier=None` de propósito, e a re-ativação pelo autoswitch reintroduzia por acidente o que aquela cura removeu.

**Item 2 — simetria da supressão** (`daemon/lifecycle.py:1621`): gate `catch_all_sem_opiniao` no ramo que LIGA, via o predicado novo `_perfil_e_catch_all` (:1783), de evidência POSITIVA (irmão de `_perfil_tem_opiniao`; a diferença é a resposta na dúvida, documentada).

**Item 3 — guardas da política de rumble** (`lifecycle.py:2464-2493`): `catch_all_sem_opiniao` + `janela_de_jogo_em_foco`, dentro do `if self._rumble_policy_from_profile`. Exigiu `profile=` na assinatura (:2403) e no callsite do manager (`manager.py:582`) — **um dublê existente** (`tests/unit/test_profile_rumble_policy.py:274`) ganhou o parâmetro e uma asserção nova.

**Item 4 — relatório na origem**: `manager.apply()` recebe `relatorio=` e grava `trigger`/`led` → `ignorado_trava_manual` (`manager.py:360`, constante `IGNORADO_TRAVA_MANUAL` + `_CATEGORIAS_SILENCIADAS_NO_APPLY`, só o que `apply` de fato silencia). O **modo jogo padrão** entra como seção própria pelo autoswitch (`autoswitch.py:472/671`), capturando o retorno que `_sincronizar_modo_jogo_padrao` descartava.

**Item 5 — log completo** (`autoswitch.py:683`): campo `secoes=["secao=estado", …]` com o relatório INTEIRO; `adiado=` fica onde estava (é o campo que a leitura do journal já procura).

**Item 6 — appliers ao sair do Nativo** (`lifecycle.py:964-984`): `rumble_policy_applier`, `speaker_applier` e `mode_applier` **embrulhado** por `_mode_applier_ao_sair_do_nativo` (:990), que barra só `kind="native"` (devolve `IGNORADO_GESTO_DELA`) — a decisão da FEAT-PROFILE-MODE-01 continua de pé como nota datada, sem o preço colateral de perder `gamepad`/`desktop`.

## Prova das mordidas (arrancar → reprovar → devolver, 05/08)

`tests/unit/test_perfil_reescrito_na_partida_01.py` — 18 casos, bancada hermética. Cada cura arrancada isoladamente e devolvida (árvore conferida byte a byte depois):

| cura arrancada | reprova |
|---|---|
| item 1 (sync no tique) | `…manual_seguida_de_tique_com_o_mesmo_candidato_nao_reativa` |
| item 2 (gate ao LIGAR) | `…catch_all_com_suppress_true_nao_liga…` + `…disco_dela_dois_catch_all_nao_prendem…` |
| item 3 (2 guardas) | `…catch_all_nao_reverte_a_politica…` + `…janela_de_jogo_em_foco_nao_reverte…` |
| item 4 (travadas) | `…relatorio_registra_as_categorias_travadas…` + `…log_do_autoswitch_reporta_estados_ignorados` |
| item 4 (modo jogo padrão) | `…relatorio_do_autoswitch_carrega_o_modo_jogo_padrao` |
| item 5 (`secoes=`) | os dois de log |
| item 6 (appliers) | `…restaura_a_politica_de_rumble…` + `…aplica_as_demais_secoes_de_modo` |

Os demais casos passam nos dois estados e estão declarados como **guardas** na docstring do módulo (a troca automática continua acontecendo, quem tem opinião continua mandando, o lock de 30 s continua vencendo, sair do Nativo continua sem religar o nativo).

## Portões

`pytest -k "autoswitch or lifecycle or profile or supress or suppress"` → **717 passed**; suíte inteira → **6979 passed, 1 skipped, sem canário de FS**; `ruff check src/ tests/` → All checks passed; `mypy` → Success (162 arquivos); acentuação, glifos, referências-docs, anonimato e test-data → rc=0. Nada tocado em `~/.config`, daemon dela não reiniciado, nada commitado (os arquivos já apareceram staged — alguém rodou `git add -A` durante a sessão).

**Para o agente irmão da GUI:** o relatório de `profile.switch` (`resposta["secoes"]`) pode agora trazer `trigger`/`led` = `ignorado_trava_manual`. Na rota manual eles raramente aparecem — a TRAVA-QUE-SOLTA-TARDE-01 limpa as categorias antes do `activate` —, então as chaves materializam sobretudo em ativações do autoswitch e no restore de boot; `modo_jogo_padrao` só existe no relatório do autoswitch (o `profile.switch` não mexe nesse eixo).
