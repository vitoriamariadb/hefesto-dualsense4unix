# workflow curar-os-achados-do-bluez

- runId: wf_d99fbd5f-ecb | status: completed | agentes: 4 | tokens: 597,187 | duracao: 50 min
- summary: Curar os achados alta/media das duas verificacoes adversariais sobre a cura do BlueZ, e verificar de novo
- fases: Curar, Reverificar

## RESULTADO

### cura

Todos os seis achados e os sete baixos curados; 7144 passed, 1 skipped (linha de base 7119 + 28 testes novos − 3 substituídos). Nada foi escrito em `/etc` — os 37 backups dela seguem lá.

```
ACHADO 1 (ALTO) — poda automática RETIRADA de scripts/bluez_config.sh:275,329. `aplicar`/`remover` só CONTAM e reportam (arquivos + bytes); a poda virou subcomando `podar`, --dry-run por padrão, que nunca apaga o mais antigo nem backup de conteúdo único (cksum), e recusa argumento desconhecido (rc=2). Nota datada na sprint RADIO-ABERTO-01 com a tabela dos dois arquivos do colapso. MORDIDAS: poda automática de volta -> test_aplicar_nao_apaga_backup_nenhum VERMELHO; `if false` na proteção do mais antigo -> test_podar_nunca_apaga_o_mais_antigo VERMELHO; idem na do conteúdo único -> test_podar_nunca_apaga_backup_de_conteudo_unico VERMELHO.
ACHADO 2 (MÉDIA) — install.sh:1351 ganhou ramo `SKIP_UDEV -eq 1` que anuncia "PULADO (--no-udev)", diz os dois comandos que faltam e LÊ o disco pelo dono único, gritando se o valor for `always` agora; e um `else` final para "sem sudo nesta máquina". DISCORDEI de separar o gate: install.sh:55 documenta --no-udev como "pula os que tocam /etc" e o CI o usa — separar faria o CI reescrever /etc/bluetooth/main.conf da máquina de build. Anunciar + nomear o perigo entrega o objetivo sem esse custo. MORDIDA: gate antigo sem else -> test_install_anuncia_o_pulo_do_bluez_com_no_udev VERMELHO.
ACHADO 3 (MÉDIA) — `_remover` agora grita, simétrico ao `aplicar`: nomeia o valor que volta a ficar ATIVO, cita INJEÇÃO DE TECLAS para `always`, e diz "default da distro" quando a chave deixa de existir. MORDIDA: bloco de aviso arrancado -> test_remover_grita_o_always_que_devolve VERMELHO.
ACHADO 4 (MÉDIA) — `_gravar_se_mudou` faz troca ATÔMICA: mktemp `.main.conf.hefesto-novo.XXXXXX` no MESMO diretório, chmod/chown/chcon --reference (com fallback 644) e `mv -f`; falha limpa o temporário e diz "o arquivo ficou INTACTO". O segundo temporário de `:317` saiu do sufixo fixo para mktemp. MORDIDAS: `install -m644` de volta -> test_escrita_interrompida_nao_trunca_o_main_conf e test_falha_na_troca_atomica_devolve_o_arquivo_intacto VERMELHOS (shim de PATH corta pela metade quem escreve direto no arquivo vivo, e outro faz o `mv` falhar).
ACHADO 5 (MÉDIA) — CONFIRMADO morto junto com o 1: não há mais chamada de poda em caminho nenhum, nem antes nem depois do teste de rc. Portão em check_packaging_parity.sh reprova se `_podar_backups` reaparecer.
ACHADO 6 (MÉDIA) — `_alheio_no_bloco`/`_avisar_alheio_no_bloco` listam, NOMEANDO, toda linha dentro das sentinelas que não seja comentário, grupo ou as duas chaves nossas, e dizem como preservá-la. Roda no `aplicar` E no `remover`. MORDIDA: chamada trocada por `true` -> test_aplicar_nomeia_linha_de_terceiro_dentro_do_bloco VERMELHO; e test_bloco_so_com_o_nosso_conteudo_nao_gera_alarme impede aviso que sai sempre.
BAIXOS (a) portão vácuo trocado pela alternância das três sentinelas + dois portões novos (poda automática de volta, subcomando sumiu) e o do doctor com `grep -E` de fronteira, porque `-qF ... verificar` casava com `verificar-nao`; (b) doctor.sh:1712 consome `bluez_config.sh verificar` e perdeu o sed inline — o comentário de :334 que MENTIA virou verdade; (c) "garantidos" não sai mais com main.conf ausente ("NADA garantido"); (d) `_cat_conf` escala para `_r` só quando o arquivo é ilegível (sudo fixo transformaria o doctor em pedido de senha), e `aplicar`/`remover` RECUSAM em vez de concluir que não há nada nosso; (e) `never` é rebaixado mas com aviso em três linhas + ramo próprio no doctor + nota datada; (f) exceção das linhas em branco do fim DECLARADA no script e fixada por teste; (g) cada `rm` é conferido depois e a contagem é a real.
DISCORDÂNCIAS — só a do achado 2 (gate mantido, pulo anunciado). Em (e) mantive o rebaixamento: com `never`, o re-pareamento legítimo dela para de funcionar quando o bond se perde, e o sintoma chega como "o controle não conecta"; a linha é neutralizada, não apagada, e o `remover` a devolve inteira. Duas mordidas minhas NÃO morderam na primeira tentativa (d e f: os testes provavam a recusa e uma frase que existia em dois lugares) — troquei por um `sudo` FALSO que registra a escalada e por asserção de comportamento sobre as linhas do fim, e aí ficaram vermelhas.
ARQUIVOS: /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/{scripts/bluez_config.sh,scripts/doctor.sh,scripts/check_packaging_parity.sh,install.sh,CHANGELOG.md,tests/unit/test_bluez_config_sh.py,docs/process/sprints/2026-08-04-RADIO-ABERTO-01-o-que-instalamos-por-padrao-anula-a-autenticacao.md}. Portões: pytest 7144/1 skip, ruff, mypy, os quatro de documentação, paridade, anonimato, test_data, versão e `bash -n` nos cinco shells — todos verdes. Não commitei.
```

### verificacoes

[
  {
    "veredito": "REFUTADO na lente: SOBRA um caminho em que um `main.conf.bak.hefesto-*` e destruido SEM ela pedir — dentro do proprio `aplicar`/`remover`, enquanto a mesma execucao imprime \"nenhum e apagado automaticamente\".\n\nO QUE SE SUSTENTOU (MEDIDO, bancada de raiz falsa em /tmp; /etc so foi LIDO):\n- Bancada com 37 backups sinteticos (um de 404 linhas, um de 3 linhas) + um `main.conf.bak.claude-*` alheio: `aplicar`, `remover` e `verificar` nao apagaram nem alteraram NENHUM deles. Inventario sha256 antes/depois so acusa `main.conf`. Contagem 37 -> 38 (aplicar/remover, backup novo) e 37 -> 37 (verificar).\n- Bancada com os 37 backups REAIS dela (`cp -a /etc/bluetooth` para raiz falsa, 24 estados distintos): `podar --aplicar` com a retencao PADRAO 10 tira 6 arquivos e os 24 estados distintos sobrevivem; com retencao 1 tira 15 e os dois pontos do colapso (14797 B / 404 linhas e 59 B / 3 linhas) sobrevivem aos dois. O `main.conf.bak.claude-1784689791` e o `main.conf.dpkg-dist` nunca sao tocados.\n- `podar` simula por padrao, recusa argumento desconhecido (rc=2, zero remocoes), retencao 0 desliga, e `HEFESTO_BT_BACKUPS_MANTER='0; rm -f <pre-colapso>'` e recusado pela regex `^[0-9]+$` (rc=2, arquivo intacto).\n- NENHUM chamador invoca `podar`: `grep` em install.sh, uninstall.sh, doctor.sh e nos assets so acha a palavra dentro de strings de conselho. A poda so roda se ela digitar.\n- MORDIDAS confirmadas, uma a uma (arrancar, ver vermelho, devolver): (A) `_podar --aplicar` de volta no `_aplicar` -> test_aplicar_nao_apaga_backup_nenhum VERMELHO; (A-bis) no `_remover` -> test_remover_nao_apaga_backup_nenhum VERMELHO; (B) protecao do mais antigo desligada -> test_podar_nunca_apaga_o_mais_antigo VERMELHO; (C) protecao do conteudo unico desligada -> test_podar_nunca_apaga_backup_de_conteudo_unico VERMELHO; (D) troca atomica trocada por `install -m644` -> test_escrita_interrompida_nao_trunca_o_main_conf e test_falha_na_troca_atomica_devolve_o_arquivo_intacto VERMELHOS; (E) `_avisar_alheio_no_bloco` trocado por `true` no aplicar -> test_aplicar_nomeia_linha_de_terceiro_dentro_do_bloco VERMELHO; (F) gate antigo `SKIP_UDEV -eq 0` sem else no install.sh -> os dois test_install_anuncia_o_pulo_* VERMELHOS; (G) bloco de aviso do `_remover` arrancado -> test_remover_grita_o_always_que_devolve e test_remover_diz_quando_a_chave_deixa_de_existir VERMELHOS.\n- Portoes conferidos por mim: pytest 7144 passed / 1 skipped (189 s), ruff, mypy (163 arquivos), acentuacao, glifos, referencias (196 docs), anonimato, versao, paridade, test_data, `bash -n` nos cinco shells — todos verdes. A contagem do curador confere.\n- /etc/bluetooth conferido so por leitura: os 37 backups seguem la, `main.conf:25` ainda tem `JustWorksRepairing=always`. Nada foi escrito.\n\nConcordo com a discordancia do achado 2 (manter o gate `--no-udev` e anunciar o pulo): separar o gate faria o CI reescrever o `/etc/bluetooth/main.conf` da maquina de build, e isso e pior que o silencio que a cura fecha.\n\nO QUE NAO SE SUSTENTOU esta nos achados abaixo. O 1 e o que quebra a lente.",
    "aprovado": false,
    "achados": [
      {
        "gravidade": "alta",
        "onde": "scripts/bluez_config.sh:281-282 (nome do backup + `cp` sem checagem de existencia), com a frase falsa em :336",
        "o_que": "O nome do backup e `${MAIN_CONF}.bak.hefesto-${rotulo}$(date +%s)` — resolucao de UM SEGUNDO — e o `_r cp` nao tem `-n`, nao testa `-e` e nao usa `mktemp`. Duas gravacoes do MESMO rotulo dentro do mesmo segundo de relogio fazem a segunda SOBRESCREVER o backup da primeira. Isso acontece DENTRO do `aplicar`/`remover`, sem `podar`, sem `--aplicar`, sem gesto nenhum dela — e a mesma execucao imprime `nenhum e apagado automaticamente`. O `_resumo_backups` nao consegue detectar: como um arquivo e destruido e outro criado, a CONTAGEM nao muda. E o alvo destruido e sempre o de maior valor: o backup do estado que estava no disco imediatamente antes.",
        "prova": "MEDIDO na bancada (nada em /etc). (1) Reproducao minima, tres passos no mesmo segundo:\n\n  T=$(mktemp -d)/bluetooth; mkdir -p \"$T\"\n  printf '[General]\\n\\n# >>> hefesto bluetooth >>>\\n[General]\\nFastConnectable=true\\nJustWorksRepairing=always\\n# <<< hefesto bluetooth <<<\\n' > \"$T/main.conf\"\n  export HEFESTO_BT_ETC=\"$T\" HEFESTO_BT_ASSETS=\"$PWD/assets/bluetooth\" HEFESTO_BT_SUDO=\"\"\n  bash scripts/bluez_config.sh aplicar >/dev/null\n  B=$(ls \"$T\" | grep '^main.conf.bak.hefesto-')   # guarda o always DELA\n  bash scripts/bluez_config.sh remover >/dev/null\n  bash scripts/bluez_config.sh aplicar | grep 'nenhum é apagado'\n  cat \"$T/$B\"\n\nSaida: o backup que continha `JustWorksRepairing=always` (o estado MEDIDO da maquina dela) passou a conter apenas `[General]`; sobraram 2 arquivos .bak.hefesto onde deviam existir 3; e a linha impressa foi `... 2 arquivo(s), 2182 byte(s) — nenhum é apagado automaticamente`.\n\n(2) Escala: 12 ciclos `aplicar; remover` seguidos sobre a raiz falsa de 37 backups deixaram 39 arquivos em vez de 61 — 22 dos 24 backups gravados foram sobrescritos. Uma execucao de `aplicar` leva 0,01 s (medido com /usr/bin/time), entao cabem ~50 pares por segundo: qualquer laco, ritual de reinstalacao ou script de bancada dela cai nisso.\n\n(3) O MAIS ANTIGO, especificamente: com um shim de PATH que faz `date +%s` devolver `1784672963` (relogio andado para tras — NTP makestep, RTC errado no boot), UM UNICO `aplicar` sobre a raiz falsa com os 37 backups sobrescreveu `main.conf.bak.hefesto-1784672963`, o pre-colapso de 404 linhas: 14797 bytes -> 184 bytes, conteudo virou o main.conf de agora. O script imprimiu na mesma tela `37 arquivo(s), 591 byte(s) — nenhum é apagado automaticamente`.\n\nGRAU: mecanismo e efeito MEDIDOS; o gatilho em producao e SUSPEITA COM MECANISMO (precisa de duas gravacoes do mesmo rotulo dentro de um segundo, ou de um relogio que repita um segundo ja usado). NENHUM teste cobre: `test_rodar_duas_vezes_nao_gera_backup_novo` so exercita o caso no-op, em que nao ha segunda gravacao. A cura e barata e cabe numa linha: `mktemp \"${MAIN_CONF}.bak.hefesto-${rotulo}$(date +%s).XXXXXX\"` ou `cp -n` com recusa explicita."
      },
      {
        "gravidade": "media",
        "onde": "scripts/doctor.sh:1721 (a funcao) e :3302 (a unica chamada); portao em scripts/check_packaging_parity.sh:563-570",
        "o_que": "O detector novo `check_bluez_justworks_repairing` — a peca central da leva, a que existe para que o `always` nunca mais viva quatro dias sem ninguem ver — nao tem NENHUM teste de comportamento e nada prende a sua chamada no `main()`. Os cinco ramos (`confirm`, `always`, `ausente`, `ilegível`, `never`) e o aviso do `hefesto-bt-agent.service` inativo nunca foram executados por teste algum; os dois testes que existem (`test_doctor_le_pelo_dono_unico`, `test_doctor_avisa_em_vez_de_mentir_quando_o_dono_some`) sao grep de TEXTO no arquivo — exatamente a classe de portao que esta sprint declara ter vindo fechar.",
        "prova": "MEDIDO. Arranquei a linha `check_bluez_justworks_repairing` do `main()` (doctor.sh:3302), deixando a funcao viva e nao chamada — o defeito literal do sprint irmao ENTREGA-QUE-NAO-LIGOU-01 (`o codigo que existe e ninguem chama`). Resultado: `pytest -q tests/unit/test_bluez_config_sh.py tests/unit/test_bt_resilience_assets.py tests/unit/test_plataforma_wiring.py` -> 131 passed; `bash scripts/check_packaging_parity.sh` -> `[ OK ] config do BlueZ: dono único, chamado nos dois lados, com detector no doctor`. Tudo VERDE com o detector desligado. O portao de paridade passa porque suas duas asercoes (`grep -qF JustWorksRepairing` e `grep -qE '(bluez_config\\.sh|\\$\\{dono\\})\" verificar'`) casam com o corpo da funcao, chamada ou nao.\n\nA casa JA TEM o padrao certo e ele nao foi aplicado aqui: `tests/unit/test_doctor_bond_sem_servicos.py:71` faz `bash -c 'source \"$DOCTOR_SH\"; check_bt_radio'` e testa a saida do check com binarios falsos. Agrava: a funcao le `/etc/bluetooth/main.conf` por caminho FIXO no primeiro `if` (doctor.sh:1723), entao ela so e exercitavel contra o /etc real — reproduzindo, dentro do doctor.sh, a mesma propriedade (mecanismo intestavel porque preso a /etc) que motivou extrair o bluez_config.sh do install.sh."
      },
      {
        "gravidade": "media",
        "onde": "scripts/bluez_config.sh:341-347 (a promessa), :395-398 (a regra) e :428 (a frase impressa)",
        "o_que": "A protecao de `conteúdo ÚNICO` guarda ARQUIVO, nao ESTADO. O comentario promete `se nenhum outro backup tem os mesmos bytes, aquele arquivo é a ÚNICA cópia daquele estado do main.conf dela`, e a frase final afirma `o mais antigo e os de conteúdo único ficam sempre` — mas quando TODAS as copias de um estado caem fora da janela de retencao, todas sao apagadas e o estado desaparece do disco. `vezes[soma] > 1` marca cada copia como descartavel sem verificar se alguma sobrevivente ficara.",
        "prova": "MEDIDO nos dois sentidos. (1) Sobre uma copia dos 37 backups REAIS dela (24 estados distintos): `HEFESTO_BT_BACKUPS_MANTER=1 bash scripts/bluez_config.sh podar --aplicar` removeu 15 arquivos e DOIS estados distintos sumiram de vez — md5 72ae0c40… (1395 bytes, tinha exatamente 2 copias: `hefesto-1785047146` e `hefesto-uninstall-1784900060`) e md5 acbba30f… (1394 bytes, tinha 8 copias, todas fora da janela). Comparacao de conjuntos de md5 antes/depois: 24 -> 22.\n\n(2) Reproducao sintetica dirigida: raiz falsa com o mais antigo (`PRE-HEFESTO`), DUAS copias identicas de um estado `COLAPSO-404` e doze recentes. `podar --aplicar` com retencao 10 removeu as duas copias do COLAPSO-404; `grep -l COLAPSO-404` depois -> 0 arquivos. Estados distintos 3 -> 2, e a frase impressa continuou sendo `o mais antigo e os de conteúdo único ficam sempre`.\n\nATENUANTE MEDIDO, e por isso media e nao alta: (a) o gatilho e `podar --aplicar`, que E o gesto explicito dela; (b) na retencao PADRAO 10, sobre os 37 reais, saem 6 arquivos e os 24 estados sobrevivem — dano zero hoje; (c) os dois pontos do colapso (14797 B e 59 B) sobrevivem a retencao 10 E a retencao 1. O defeito e a frase prometer mais do que a regra entrega; a regra correta seria `so descarte se alguma copia deste conteudo FICAR`."
      },
      {
        "gravidade": "baixa",
        "onde": "scripts/check_packaging_parity.sh:554-556",
        "o_que": "O portao que impede a volta da poda automatica testa um NOME LITERAL: `grep -qF '_podar_backups'`. Renomear a funcao (`_faxina`, `_poda_auto`, ou chamar `_podar --aplicar` direto de dentro do `_aplicar`, que foi exatamente a mordida A) passa pelo portao sem ruido. O comentario logo acima o descreve como se travasse o mecanismo, e ele trava so uma grafia.",
        "prova": "MEDIDO. A mordida A inseriu `_podar --aplicar` dentro do `_aplicar` sem usar a string `_podar_backups`: `bash scripts/check_packaging_parity.sh` continuou imprimindo `[ OK ] config do BlueZ: ...`. Quem pegou foi o teste de COMPORTAMENTO `test_aplicar_nao_apaga_backup_nenhum` (e o simetrico `test_remover_nao_apaga_backup_nenhum`, confirmado na mordida A-bis) — ou seja, a protecao real e a bancada, e a linha do portao e decorativa, do mesmo feitio do `grep -qF FastConnectable` que a propria leva acabou de remover por ser vacuo (check_packaging_parity.sh:543-549). Impacto limitado porque os testes de comportamento existem e mordem."
      },
      {
        "gravidade": "baixa",
        "onde": "scripts/bluez_config.sh:289 (mktemp em ${ETC}) e :306 (a limpeza, que so cobre o caminho de erro tratado)",
        "o_que": "O temporario `.main.conf.hefesto-novo.XXXXXX` nasce DENTRO de /etc/bluetooth (correto, para o `mv` ser atomico), mas se o processo morrer entre o `mktemp` e o `mv` — o mesmo kill/queda de energia que motivou a troca atomica — o arquivo fica orfao no diretorio do conffile dela, e nada o reporta nem o limpa: `_resumo_backups` e `verificar` so contam `main.conf.bak.hefesto-*`, e o `remover` nao varre. O ponto inicial o esconde ate de um `ls` simples.",
        "prova": "SUSPEITA COM MECANISMO. Leitura do caminho: `_r rm -f \"${novo}\"` (:306) so roda quando a cadeia de `cp`/`chmod`/`mv` retorna erro DENTRO do script; um SIGKILL entre :289 e :302 nao passa por ali, e nao ha `trap`. O teste `test_a_troca_atomica_nao_deixa_temporario_para_tras` (:851) cobre so o caminho feliz. Nao reproduzi a queda por nao valer o custo — e sujeira, nao perda de evidencia: nenhum backup e destruido por esta via, e o `main.conf` fica intacto (que e o que a troca atomica promete e cumpre, confirmado pela mordida D)."
      }
    ]
  },
  {
    "aprovado": true,
    "veredito": "REFUTACAO FALHOU — a atomicidade se sustenta. Sob a lente \"o arquivo dela nao pode quebrar\", tentei quebrar `/etc/bluetooth/main.conf` por seis caminhos e nao consegui por nenhum. Bancada sempre com raiz falsa (HEFESTO_BT_ETC); /etc de verdade so foi LIDO, e segue com os 37 backups dela e o main.conf 644 root:root intacto.\n\nPROVA DE SISTEMA (MEDIDO, strace -f -y em `aplicar` e em `remover`): o `main.conf` e aberto EXCLUSIVAMENTE com O_RDONLY — nenhum openat com O_WRONLY/O_RDWR/O_TRUNC/O_CREAT tem ele como destino, nem em `aplicar`, nem em `remover`. A unica syscall que o muta e `renameat(.main.conf.hefesto-novo.XXXXXX -> main.conf) = 0`. Nao ha caminho que ainda escreva no lugar. Confirmado tambem por grep: o `sed -i` de faixa do uninstall.sh e o `install -m644` do install.sh sairam os dois; nao sobrou nenhum outro escritor de main.conf no repositorio.\n\nCINCO FALHAS INJETADAS, TODAS COM main.conf INTACTO byte a byte (mesmo cksum, MESMO inode, modo 640 e dono preservados):\n  A. disco cheio no conteudo novo (shim de `cp` escreve metade e sai 1) -> rc=1, \"o arquivo ficou INTACTO\";\n  B. `kill -9` no script inteiro entre o `cp` e o `mv` -> rc=137, arquivo intacto;\n  C. permissao negada no ULTIMO instante (o diretorio vira r-x durante o `chown`, um passo antes do `mv`) -> rc=1, arquivo intacto;\n  D. `mv` falhando (EIO) -> rc=1, arquivo intacto;\n  E. disco cheio no BACKUP -> rc=1, \"NAO mexi em main.conf\" — verdade, o arquivo dela nao foi tocado.\nEm nenhum caso o script mentiu: falha sempre saiu com rc!=0 e sem a frase \"garantidos\".\n\nDONO/MODO/INODE: modo e dono vem do original por `chmod/chown --reference` (medido 640 vitoriamaria:vitoriamaria preservado; na maquina dela seria 644 root:root). O inode MUDA — 3279709 -> 3279711 — e isso e inerente ao rename e correto: o `main.conf` e conffile do dpkg, que casa por md5 do caminho, nao por inode, e nlink=1 na maquina dela (medido), entao nenhum hardlink e rompido.\n\nO SEGUNDO TEMPORARIO E SEGURO: `devolvido=\"$(mktemp)\"` (O_EXCL, modo 600, nome imprevisivel) e o `mv -f devolvido tmp` acontece entre dois arquivos do MESMO /tmp — atomico e, sobretudo, longe do arquivo dela; o `${tmp}.devolvido` de sufixo fixo sumiu de fato.\n\nMORDIDA (arranquei a cura e vi reprovar): troquei o bloco `mktemp + chmod/chown/chcon --reference + mv -f` (:286-308) pelo `install -m644 -o root -g root` antigo. VERMELHOS os dois: test_escrita_interrompida_nao_trunca_o_main_conf (\"o main.conf dela ficou TRUNCADO no meio do bloco\") e test_falha_na_troca_atomica_devolve_o_arquivo_intacto (\"o main.conf mudou apesar de a escrita ter falhado\", diff no indice 74). Cura devolvida e conferida por cksum (1343174918 32202, `git diff` vazio).\n\nPORTAO CONFERIDO: `.venv/bin/python -m pytest -q` = 7144 passed, 1 skipped em 169s — bate exatamente com o que o curador relatou.\n\nBONUS MEDIDO CONTRA OS DADOS REAIS DELA: copiei os 37 backups de /etc/bluetooth para a bancada (leitura pura) e rodei `podar --aplicar` com a retencao padrao 10. Remove 6 de 37, e os DOIS pontos do colapso \"404 linhas -> 3 linhas\" (main.conf.bak.hefesto-1784672963, 404 linhas, e main.conf.bak.hefesto-1784694261, 3 linhas) SOBREVIVEM, protegidos por \"conteudo UNICO\". O achado 1 do curador se confirma contra a carteira verdadeira, nao so contra fixture.\n\nRESSALVA QUE VALE UMA VOLTA: a cura fechou a porta do main.conf e deixou a do BACKUP aberta (achado 1 abaixo, media). Nao quebra o arquivo dela, mas quebra o caminho de recuperacao dele — que e o mesmo defeito de costas. Nao e bloqueador da leva; e a proxima linha a escrever.",
    "achados": [
      {
        "gravidade": "media",
        "onde": "/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/scripts/bluez_config.sh:282-285",
        "o_que": "O `cp` do BACKUP que falha no meio deixa o backup PARCIAL no disco, sem limpeza e sem uma palavra — e todo consumidor passa a trata-lo como backup legitimo. A assimetria e exata e esta no proprio corpo da funcao: o caminho do temporario tem `_r rm -f \"${novo}\"` (:306); o caminho do backup nao tem `rm -f \"${backup}\"`. Consequencia medida: a cura mudou o beco sem saida de lugar em vez de fecha-lo — o main.conf sobrevive, mas o artefato de que ela recupera pode nascer cortado no meio do bloco, e e justamente ele que a mensagem de erro de :307 aponta (\"backup em ...\"). Nenhum teste cobre esse ramo: o helper `_shim` de tests/unit/test_bluez_config_sh.py:783 so sabota escrita cujo destino seja o main.conf VIVO, nunca o backup.",
        "prova": "MEDIDO, cadeia inteira reproduzida em /tmp/.../scratchpad/bancada/e5_backup_beco.sh (raiz falsa, /etc nunca tocado). Shim de PATH: `cp` cujo destino casa *main.conf.bak.hefesto-* escreve so os primeiros 118 bytes e sai 1 (ENOSPC). (1) `bluez_config.sh aplicar` -> \"nao consegui gravar o backup ... — NAO mexi em main.conf\"; main.conf INTACTO (correto). (2) fica no disco `main.conf.bak.hefesto-1786046550`, 118 bytes, cortado logo apos a linha `# >>> hefesto bluetooth >>>`, e `bluez_config.sh verificar` o conta como legitimo: `backups-hefesto: 1` / `backups-hefesto-bytes: 118` — indistinguivel de um backup bom pelo nome, pelo modo e pela contagem. (3) `podar --aplicar` NUNCA o remove: cai em \"preservado (conteudo UNICO — e a unica copia deste estado do main.conf)\" (reproduzido no caso H de e4_simlink_backup.sh). (4) restaurando-o como main.conf — e ele e o MAIS RECENTE, a escolha natural — `aplicar` e `remover` recusam OS DOIS, para sempre: \"sentinela de abertura sem fechamento em .../main.conf — nao mexi no arquivo\", rc=1 nos dois. E exatamente o beco que o comentario de :265-274 diz que a troca atomica existe para impedir."
      },
      {
        "gravidade": "baixa",
        "onde": "/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/scripts/bluez_config.sh:289-308",
        "o_que": "Nao ha `trap` de limpeza (grep: NENHUM trap no arquivo). Quando a queda acontece depois do `cp` e antes do `mv`, ou quando o proprio `rm -f` de :306 tambem falha, sobra um `.main.conf.hefesto-novo.XXXXXX` em /etc/bluetooth com a config INTEIRA dela dentro — e nada no projeto o conta, o reporta ou o varre: o glob de `_lista_backups` (:320) e `main.conf.bak.hefesto-*`, o `verificar` nao o menciona e o `podar` nao o enxerga. E a mesma classe dos 37 backups acumulados que esta leva veio combater, em miniatura e invisivel. O temporario de /tmp (`tmp=\"$(mktemp)\"`, :484 e :556, com o conteudo dela) tambem sobrevive ao kill, pelo mesmo motivo.",
        "prova": "MEDIDO em /tmp/.../scratchpad/bancada/e3_falhas.sh. CASO B (`kill -9` do script entre o `cp` e o `mv`): sobras no diretorio = `main.conf`, `main.conf.bak.hefesto-...` e `.main.conf.hefesto-novo.EFoIaN`. CASO C (diretorio vira r-x durante o `chown`, um passo antes do `mv`): o `mv` falha E o `rm -f` de limpeza tambem nao consegue desligar o nome de um diretorio sem escrita — sobra `.main.conf.hefesto-novo.NyHsPy`. Nos dois o main.conf ficou intacto (mesmo cksum 1595117548, mesmo inode): e sujeira, nao estrago. Confirmado por leitura pura que hoje nao ha nenhum desses em /etc/bluetooth da maquina dela."
      },
      {
        "gravidade": "baixa",
        "onde": "/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/tests/unit/test_bluez_config_sh.py:851",
        "o_que": "`test_a_troca_atomica_nao_deixa_temporario_para_tras` nao MORDE: ele so exercita o caminho feliz, e o caminho feliz nunca deixa temporario nem antes nem depois da cura (com `install -m644` nao existe temporario nenhum para deixar). Passou VERDE com a cura arrancada. Nao e defeito de produto e nao invalida os dois testes vizinhos, que morderam de verdade — e um teste que da uma sensacao de cobertura que ele nao tem, e o ramo que importaria (o orfao das quedas, achado anterior) e o que ele nao cobre.",
        "prova": "MEDIDO: com `_gravar_se_mudou` revertido para `_r install -m644 -o root -g root \"${candidato}\" \"${MAIN_CONF}\"`, `.venv/bin/python -m pytest tests/unit/test_bluez_config_sh.py -q -k \"atomic or interrompida or temporario\"` deu `2 failed, 2 passed`: vermelhos test_escrita_interrompida_nao_trunca_o_main_conf e test_falha_na_troca_atomica_devolve_o_arquivo_intacto; VERDES test_a_troca_atomica_nao_deixa_temporario_para_tras e test_o_temporario_do_remover_sai_de_mktemp. Cura devolvida em seguida e conferida por cksum (1343174918 32202) e por `git diff` vazio."
      },
      {
        "gravidade": "baixa",
        "onde": "/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/scripts/bluez_config.sh:267",
        "o_que": "O comentario afirma que a troca atomica cobre \"disco cheio, queda de energia ou um kill no meio\". Disco cheio e kill ela cobre — provei os dois. QUEDA DE ENERGIA e outra coisa: `rename(2)` da ATOMICIDADE, nao DURABILIDADE, e o script nao tem nenhum `fsync`/`sync` (grep: SEM fsync/sync no arquivo). Sem fsync no temporario antes do rename, a garantia de que ela nao acorda com um main.conf vazio nao vem do script — vem de uma heuristica do sistema de arquivos. Precisao de frase, nao defeito de mecanismo; a frase e que promete mais do que o codigo entrega.",
        "prova": "MEDIDO na maquina dela, leitura pura: `findmnt -no SOURCE,FSTYPE,OPTIONS --target /etc` = `/dev/sdb3 ext4 rw,noatime,errors=remount-ro`, e `grep -c noauto_da_alloc /proc/mounts` = 0 — ou seja, o `auto_da_alloc` do ext4 esta LIGADO (e o padrao), e ele forca a resolucao da alocacao adiada quando um arquivo e renomeado por cima de um existente. Entao AQUI a promessa se cumpre, por conta do ext4 e nao do script. SUSPEITA COM MECANISMO para o resto: em sistema de arquivos sem essa heuristica, ou com data=writeback, rename sem fsync admite justamente o main.conf de tamanho zero depois de um corte de energia."
      },
      {
        "gravidade": "baixa",
        "onde": "/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/scripts/bluez_config.sh:302",
        "o_que": "Se `/etc/bluetooth/main.conf` for LINK SIMBOLICO (dotfiles, stow, NixOS), o `mv -f` substitui o LINK por arquivo comum: o arquivo real dela fica orfao com o conteudo velho, e o `remover` nunca devolve o link — instalar+desinstalar vira operacao destrutiva liquida sobre a indirecao, que e a mesma classe que a invariante 3 do cabecalho diz ter fechado para chave de terceiro. REGISTRO O GRAU COM HONESTIDADE: NAO e regressao desta leva, e nao se aplica a maquina dela. So vale como linha a acrescentar no comentario da troca atomica, para ninguem descobrir por acidente.",
        "prova": "MEDIDO nos dois lados. (a) Caso F de /tmp/.../scratchpad/bancada/e4_simlink_backup.sh: com main.conf apontando para dela/main.conf.real, depois do `aplicar` -> \"main.conf e link? NAO — virou arquivo comum\" (inode 3280247, 2241 bytes, confirm) enquanto o alvo real segue em 3280244, 173 bytes, `JustWorksRepairing = never`; depois do `remover` -> \"main.conf e link de novo? NAO — o link nao volta\". (b) Contraprova de que NAO e regressao: `/usr/bin/install -m644 novo main.conf` sobre um link simbolico tambem devolve \"link? NAO\" com o alvo real inalterado — o codigo ANTIGO destruia o link do mesmo jeito. (c) Nao se aplica hoje: `stat -c '%N modo=%a dono=%U:%G links=%h' /etc/bluetooth/main.conf` = arquivo comum, modo 644, root:root, links=1."
      }
    ]
  },
  {
    "achados": [
      {
        "gravidade": "alta",
        "onde": "scripts/doctor.sh:1742 e :3302 (o veredito e a fiação do detector novo)",
        "o_que": "MEDIDO. O detector que é a razão de ser desta leva não tem UM teste que morda. Duas mordidas independentes deixam a suíte inteira verde: (A) trocar `fail \"JustWorksRepairing=always ATIVO...` por `pass \"...` — o doctor passa a APROVAR o valor que a própria sprint classifica como injeção de teclas; (C) apagar a linha `check_bluez_justworks_repairing` de `main()` — a função continua no arquivo, e ninguém a chama. É a classe de defeito que esta casa já batizou (ENTREGA-QUE-NAO-LIGOU-01, `o codigo que existe e ninguem chama`). Os dois testes que existem (test_doctor_le_pelo_dono_unico, test_doctor_avisa_em_vez_de_mentir_quando_o_dono_some) são grep de TEXTO e sobrevivem às duas mutilações; o portão de paridade também, porque procura `JustWorksRepairing` e `verificar` — palavras que continuam vivas dentro da função morta.",
        "prova": "MORDIDA A: `sed -i 's/            fail \"JustWorksRepairing=always ATIVO/            pass \"JustWorksRepairing=always ATIVO/' scripts/doctor.sh` -> `.venv/bin/python -m pytest -q tests/unit/test_bluez_config_sh.py tests/unit/test_bt_resilience_assets.py tests/unit/test_radio_aberto_01.py tests/unit/test_plataforma_wiring.py` => `138 passed`; `bash scripts/check_packaging_parity.sh` => `[ OK ] config do BlueZ: dono unico, chamado nos dois lados, com detector no doctor`. MORDIDA C: `sed -i '/^    check_bluez_justworks_repairing$/d' scripts/doctor.sh` (chamadas restantes em main(): 0) => `138 passed` + paridade `[ OK ]`. Cura devolvida com `git checkout -- scripts/doctor.sh` nos dois casos; suíte cheia depois: 7144 passed, 1 skipped."
      },
      {
        "gravidade": "alta",
        "onde": "scripts/build_deb.sh:191 (e todo packaging/) contra scripts/doctor.sh:1721 e scripts/check_packaging_parity.sh:569",
        "o_que": "MEDIDO. O `doctor` EMPACOTADO é cego ao `always`, e o portão novo anuncia o contrário. O `build_deb.sh:191` copia `doctor.sh` para `/usr/share/hefesto-dualsense4unix/scripts/` e NÃO copia `bluez_config.sh`; nenhum empacotamento (deb, fedora, arch, flatpak, nix) o leva. Como `check_bluez_justworks_repairing` lê exclusivamente pelo dono único em `${ROOT_DIR}/scripts/bluez_config.sh`, no layout do .deb ela cai no ramo `warn ... o dono unico da config do BlueZ nao esta aqui` e não vê nada. Reproduzido contra o /etc REAL desta máquina, que tem `always` em main.conf:25. O curador registrou a dívida de empacotamento como `exige postinst proprio, e e entrega a parte` — verdade para APLICAR a config, falso para o DETECTOR: ele é leitura pura e entra numa linha do laço `for _s in ...` que já existe. E o portão imprime `[ OK ] ... com detector no doctor` sem nunca conferir que o dono viaja junto com o doctor: é o mesmo vácuo (linha que LÊ como proteção e não protege) que o próprio curador acabou de fechar duas seções acima.",
        "prova": "`mkdir -p $S/deb/usr/share/hefesto-dualsense4unix/scripts; for _s in doctor.sh disable_steam_input.sh fix_wireplumber_default_source.sh dsx_recover.sh; do install -Dm755 scripts/$_s $S/deb/usr/share/hefesto-dualsense4unix/scripts/$_s; done` (o laço exato do build_deb.sh) -> `ls` mostra 4 arquivos, sem bluez_config.sh. Harness com a função extraída por awk e stubs de pass/warn/fail/info: `bash $S/doctor_harness.sh $S/deb/usr/share/hefesto-dualsense4unix` => `WARN scripts/bluez_config.sh ausente — o dono único da config do BlueZ não está aqui`; `bash $S/doctor_harness.sh $PWD` (mesma máquina, mesmo /etc) => `FAIL JustWorksRepairing=always ATIVO no /etc/bluetooth/main.conf`. E `grep -rn bluez_config scripts/build_deb.sh packaging/` => zero."
      },
      {
        "gravidade": "media",
        "onde": "scripts/bluez_config.sh:167 (`_valor_ativo`, o `tail -n 1`)",
        "o_que": "MEDIDO. A regra de leitura que o script declara como invariante medida ao vivo em 2026-07-18 (`o GKeyFile faz o ultimo vencer`) não tem teste que morda. Trocar `tail -n 1` por `head -n 1` deixa a suíte verde, e a consequência é FALSO NEGATIVO no detector: num main.conf em que alguém apensou o próprio `[General] JustWorksRepairing=always` DEPOIS do nosso bloco — que é o estado real de quem edita o arquivo à mão depois do install — o `verificar` mutilado responde `confirm` / `veredito: OK`, o doctor daria `pass`, e o GKeyFile (o parser do bluetoothd) lê `always`. A cura de hoje acerta; nada impede que ela seja desfeita.",
        "prova": "Bancada f1 com bloco hefesto seguido de `[General]\\nJustWorksRepairing=always`. COM a cura: `HEFESTO_BT_ETC=... HEFESTO_BT_SUDO=\"\" bash scripts/bluez_config.sh verificar` => `JustWorksRepairing: always` / `veredito: INSEGURO`. Parser real: `JustWorksRepairing='always'`. SEM a cura (`sed -i 's/        | tail -n 1$/        | head -n 1/' scripts/bluez_config.sh`) => `JustWorksRepairing: confirm` / `veredito: OK`, e `pytest -q tests/unit/test_bluez_config_sh.py tests/unit/test_bt_resilience_assets.py tests/unit/test_radio_aberto_01.py` => `81 passed`."
      },
      {
        "gravidade": "media",
        "onde": "scripts/bluez_config.sh:162-169 (`_valor_ativo`) e tests/unit/test_bluez_config_sh.py:153 (`_valor`)",
        "o_que": "MEDIDO. O `verificar` — dono único, consumido pelo doctor E pelo ramo `--no-udev` do install — ignora o GRUPO; o parser real não. Ele varre o arquivo inteiro com sed e pega a última linha que casa, onde o GKeyFile resolve `JustWorksRepairing` DENTRO de `[General]`. Num main.conf com `[General] JustWorksRepairing=always` seguido de `[Policy] JustWorksRepairing=confirm`, o `verificar` responde `veredito: OK` e o doctor aprovaria uma máquina em que o bluetoothd lê `always`. A bancada não pega essa classe porque reimplementa um TERCEIRO parser em Python (`_valor`, tests/unit/test_bluez_config_sh.py:153) em vez de usar o GKeyFile — que está disponível no `.venv` (`from gi.repository import GLib`), e é com ele que eu medi. Duas fontes para a mesma regra é exatamente o defeito que esta leva veio fechar no doctor; ele sobreviveu no dono e na bancada. Anexo, SUSPEITA COM MECANISMO: o `verificar` nunca lê VALOR de `main.conf.d`, embora o `aplicar` grave drop-ins `POR CIMA` justamente para o BlueZ que lê o diretório — um drop-in de terceiro que ordene depois de `hefesto-justworks.conf` vence e o veredito continua `OK` (nesta máquina o bluetoothd mede-se ignorando main.conf.d, por isso não é MEDIDO).",
        "prova": "Bancada d1: arquivo com `[General]\\nJustWorksRepairing=always\\n\\n[Policy]\\nJustWorksRepairing=confirm`. `bash scripts/bluez_config.sh verificar` => `JustWorksRepairing: confirm` / `veredito: OK`. Parser real (`GLib.KeyFile().load_from_file(...).get_string('General','JustWorksRepairing')`) => `'always'`. Verdade-base do GKeyFile medida antes: grupos `[General]` duplicados são fundidos e a última chave vence (é o que torna correto apensar o bloco no fim)."
      },
      {
        "gravidade": "media",
        "onde": "scripts/bluez_config.sh:473 e :183-196 (`_alheio_no_bloco`), repetido em scripts/doctor.sh:1751",
        "o_que": "MEDIDO. O rebaixamento `never` -> `confirm` é decisão declarada e datada, e o único controle que a compensa é a reversibilidade prometida em voz alta: `a sua linha e neutralizada, nao apagada, e volta inteira`. Essa promessa é FALSA no lugar mais provável de a pessoa ter escrito o `never`: DENTRO do bloco hefesto — o bloco cujo corpo é literalmente `JustWorksRepairing=confirm`, e é onde quem lê o aviso do doctor vai endurecer o valor. Ali o `_despir_main_conf` descarta a faixa inteira das sentinelas, nenhuma MARCA é gravada, e o `remover` entrega um arquivo SEM a chave (`cai no default da distro`). Pior: o aviso do alheio, que existe para NOMEAR o que sai junto com o bloco, tem `FastConnectable|JustWorksRepairing` na lista de exceções (:192) — então a linha dela é destruída sem uma palavra. Só o backup a guarda, e o `podar` (mesmo protegido) é a ferramenta que mira nesses arquivos.",
        "prova": "Bancada a1 com `JustWorksRepairing=never` dentro de `# >>> hefesto bluetooth >>>`. `aplicar` imprime `se 'never' foi escolha sua, rode 'bash scripts/bluez_config.sh remover': a sua linha é neutralizada, não apagada, e volta inteira` e o GKeyFile passa a ler `confirm`. Em seguida `remover` imprime `sem o nosso bloco, JustWorksRepairing deixa de estar declarado` e o arquivo final é `[General]\\nName = %h` — GKeyFile: chave AUSENTE. Bancada c1 (mesmo caso): a saída do `aplicar` filtrada por `grep 'sai do arquivo\\|ATEN'` traz SÓ a linha do rebaixamento, nenhuma linha `sai do arquivo:`."
      },
      {
        "gravidade": "media",
        "onde": "docs/process/sprints/2026-08-04-RADIO-ABERTO-01-...md e install.sh:1358 (a justificativa da discordância do ACHADO 2)",
        "o_que": "MEDIDO, e é uma nota que entra no repositório como decisão medida. O curador manteve o gate do `--no-udev` justificando que `o CI o usa — separar faria o CI reescrever /etc/bluetooth/main.conf da maquina de build`. O CI não roda `install.sh`: nenhum workflow o invoca, e a única linha que o cita em `.github/` é `shellcheck -S error scripts/*.sh install.sh uninstall.sh` (ci.yml:136). A decisão até pode se sustentar pelo contrato documentado (`--no-udev pula os que tocam /etc`, install.sh:55), mas a prova citada é falsa — e `nao se apaga decisao medida` corta para os dois lados: uma decisão gravada sobre medição que não existe é a semente do próximo `hipotese que nao explica o que ja funcionava`.",
        "prova": "`grep -rn -- \"--no-udev\" . --exclude-dir=.git --exclude-dir=docs --exclude-dir=.venv` => só cabeçalhos/ramos do próprio install.sh, um comentário do uninstall.sh, o CHANGELOG e os testes; nenhuma INVOCAÇÃO. `grep -n \"install\" .github/workflows/ci.yml` => nenhuma execução de `./install.sh`, apenas `apt-get install`, `pip install` e o `shellcheck` da linha 136. `ls .github/workflows` => anonymity-check.yml, ci.yml, flatpak.yml, release.yml."
      },
      {
        "gravidade": "baixa",
        "onde": "scripts/bluez_config.sh:486 (`cat \"${BLOCO}\"` sem conferência) e :521 (a frase da garantia)",
        "o_que": "MEDIDO. O `aplicar` anuncia `JustWorksRepairing=confirm + FastConnectable=true garantidos` sem NUNCA reler o disco — e a tese inteira desta sprint é que o que está escrito não era o que estava no disco. Com o asset `hefesto-bt.block` de zero byte (checagem de :438-443 é só `-f`), o `aplicar` sai com rc=0, imprime a garantia, e o main.conf resultante não tem chave `JustWorksRepairing` nenhuma. A direção é segura (chave ausente = default da distro, mais restritivo que `always`) e o conteúdo do asset é portão em tests/unit/test_bt_resilience_assets.py:79, por isso baixa — mas o fecho natural é uma releitura pelo `_verificar`, que já existe e está a uma chamada de distância.",
        "prova": "Bancada e1: assets copiados para diretório próprio com `: > hefesto-bt.block`; `HEFESTO_BT_ETC=... HEFESTO_BT_SUDO=\"\" HEFESTO_BT_ASSETS=$BASE/e1/assets bash scripts/bluez_config.sh aplicar` => rc=0 e `JustWorksRepairing=confirm + FastConnectable=true garantidos`. Arquivo final: `[General]` / `#hefesto-desativou# JustWorksRepairing=always`. Parser real: `JustWorksRepairing='AUSENTE: Key file does not have key ...'`."
      }
    ],
    "aprovado": false,
    "veredito": "NÃO REFUTEI A CURA. Refutei as guardas dela — e por isso não aprovo.\n\nO QUE ATACOU E NÃO CAIU (tudo MEDIDO com o parser real, `GLib.KeyFile` do `.venv`, contra raiz falsa `HEFESTO_BT_ETC`; nunca `/etc`, nunca `install.sh`/`uninstall.sh`, nunca o `bluetoothd`). Verdade-base primeiro: o GKeyFile funde grupos `[General]` repetidos e a ÚLTIMA chave vence — é isso que torna correto apensar o bloco no fim. Sobre essa base, `aplicar` termina em `confirm` em todos os cenários pedidos: `always` DENTRO do bloco hefesto (o estado real de /etc/bluetooth/main.conf:25 desta máquina); `always` de terceiro fora do bloco; caixa e espaçamento variados (tab antes da chave, espaços em volta do `=`, valor `ALWAYS`, chave repetida em `[LE]`); os dois blocos LEGADOS; `main.conf.d` presente (o main.conf é normalizado E os drop-ins entram por cima — a assimetria está de fato fechada); `main.conf.d` presente com `main.conf` ausente (sai `NADA garantido`, honesto, sem frase de sucesso). O ciclo install+uninstall+install fecha em `confirm` nas duas variantes: com `always` de terceiro (install-1 confirm, uninstall devolve `always` ATIVO e grita a injeção de teclas, install-2 confirm) e com `always` nosso (install-1 confirm, uninstall deixa a chave ausente, install-2 e install-3 confirm, 2 backups, sem duplicar bloco). A troca atômica, a idempotência e a recusa por sentinela sem fechamento se comportaram como anunciado.\n\nPOR QUE MESMO ASSIM É REPROVA. A entrega vende um detector — `agora ela tem como ver o always entre um install e o proximo` — e o detector é a parte sem prova. Duas mordidas independentes (o veredito `fail` virando `pass`, e a função saindo do `main()`) deixam 138 testes verdes e o portão de paridade imprimindo `[ OK ] ... com detector no doctor`; os dois testes que existem são grep de texto e sobrevivem às duas. Some-se a isso que o `bluez_config.sh` não viaja em empacotamento nenhum, então o doctor do .deb responde `dono unico ... nao esta aqui` exatamente na máquina onde o `always` está — reproduzi o layout do `build_deb.sh:191` e comparei os dois doctors contra o mesmo /etc: o do repo dá FAIL, o empacotado é cego. A regra de leitura `ultimo vence` também não morde (`tail -n 1` -> `head -n 1` = suíte verde, e o falso negativo é construível), e o `verificar` ignora o grupo onde o parser real não ignora. Isso é `teste tem de MORDER` e `portao cego` na mesma leva que nasceu justamente porque uma cura escrita não chegava ao disco.\n\nDUAS NOTAS DE HONESTIDADE, não achados. (1) A discordância do ACHADO 2 continua defensável pelo contrato documentado do `--no-udev`; o que refutei foi a PROVA citada (o CI não roda o install), e isso importa porque a justificativa vira nota datada no repositório. (2) O rebaixamento do `never` é decisão declarada e eu não a contesto — contesto que a promessa que a compensa não vale dentro do bloco, que é onde a pessoa vai escrever.\n\nNada foi consertado, nada foi commitado. A árvore de trabalho voltou byte a byte ao índice (`git diff --stat` vazio), a suíte cheia refez 7144 passed / 1 skipped depois de todas as mordidas, e os 37 backups dela em /etc/bluetooth seguem lá — o único contato com /etc em toda a verificação foi leitura.\n\nUma observação SEM PROVA, para não sumir: a primeira execução da mordida A relatou `10 failed, 71 passed` nos mesmos três arquivos que, limpos, passaram 20 vezes seguidas e que, com a MESMA mordida reaplicada, passaram outras 4. Não reproduzi e não sei explicar; não conto como achado, mas registro que aconteceu uma vez."
  }
]

### reprovou

true

### achadosAltos

[
  {
    "gravidade": "alta",
    "onde": "scripts/bluez_config.sh:281-282 (nome do backup + `cp` sem checagem de existencia), com a frase falsa em :336",
    "o_que": "O nome do backup e `${MAIN_CONF}.bak.hefesto-${rotulo}$(date +%s)` — resolucao de UM SEGUNDO — e o `_r cp` nao tem `-n`, nao testa `-e` e nao usa `mktemp`. Duas gravacoes do MESMO rotulo dentro do mesmo segundo de relogio fazem a segunda SOBRESCREVER o backup da primeira. Isso acontece DENTRO do `aplicar`/`remover`, sem `podar`, sem `--aplicar`, sem gesto nenhum dela — e a mesma execucao imprime `nenhum e apagado automaticamente`. O `_resumo_backups` nao consegue detectar: como um arquivo e destruido e outro criado, a CONTAGEM nao muda. E o alvo destruido e sempre o de maior valor: o backup do estado que estava no disco imediatamente antes.",
    "prova": "MEDIDO na bancada (nada em /etc). (1) Reproducao minima, tres passos no mesmo segundo:\n\n  T=$(mktemp -d)/bluetooth; mkdir -p \"$T\"\n  printf '[General]\\n\\n# >>> hefesto bluetooth >>>\\n[General]\\nFastConnectable=true\\nJustWorksRepairing=always\\n# <<< hefesto bluetooth <<<\\n' > \"$T/main.conf\"\n  export HEFESTO_BT_ETC=\"$T\" HEFESTO_BT_ASSETS=\"$PWD/assets/bluetooth\" HEFESTO_BT_SUDO=\"\"\n  bash scripts/bluez_config.sh aplicar >/dev/null\n  B=$(ls \"$T\" | grep '^main.conf.bak.hefesto-')   # guarda o always DELA\n  bash scripts/bluez_config.sh remover >/dev/null\n  bash scripts/bluez_config.sh aplicar | grep 'nenhum é apagado'\n  cat \"$T/$B\"\n\nSaida: o backup que continha `JustWorksRepairing=always` (o estado MEDIDO da maquina dela) passou a conter apenas `[General]`; sobraram 2 arquivos .bak.hefesto onde deviam existir 3; e a linha impressa foi `... 2 arquivo(s), 2182 byte(s) — nenhum é apagado automaticamente`.\n\n(2) Escala: 12 ciclos `aplicar; remover` seguidos sobre a raiz falsa de 37 backups deixaram 39 arquivos em vez de 61 — 22 dos 24 backups gravados foram sobrescritos. Uma execucao de `aplicar` leva 0,01 s (medido com /usr/bin/time), entao cabem ~50 pares por segundo: qualquer laco, ritual de reinstalacao ou script de bancada dela cai nisso.\n\n(3) O MAIS ANTIGO, especificamente: com um shim de PATH que faz `date +%s` devolver `1784672963` (relogio andado para tras — NTP makestep, RTC errado no boot), UM UNICO `aplicar` sobre a raiz falsa com os 37 backups sobrescreveu `main.conf.bak.hefesto-1784672963`, o pre-colapso de 404 linhas: 14797 bytes -> 184 bytes, conteudo virou o main.conf de agora. O script imprimiu na mesma tela `37 arquivo(s), 591 byte(s) — nenhum é apagado automaticamente`.\n\nGRAU: mecanismo e efeito MEDIDOS; o gatilho em producao e SUSPEITA COM MECANISMO (precisa de duas gravacoes do mesmo rotulo dentro de um segundo, ou de um relogio que repita um segundo ja usado). NENHUM teste cobre: `test_rodar_duas_vezes_nao_gera_backup_novo` so exercita o caso no-op, em que nao ha segunda gravacao. A cura e barata e cabe numa linha: `mktemp \"${MAIN_CONF}.bak.hefesto-${rotulo}$(date +%s).XXXXXX\"` ou `cp -n` com recusa explicita."
  },
  {
    "gravidade": "alta",
    "onde": "scripts/doctor.sh:1742 e :3302 (o veredito e a fiação do detector novo)",
    "o_que": "MEDIDO. O detector que é a razão de ser desta leva não tem UM teste que morda. Duas mordidas independentes deixam a suíte inteira verde: (A) trocar `fail \"JustWorksRepairing=always ATIVO...` por `pass \"...` — o doctor passa a APROVAR o valor que a própria sprint classifica como injeção de teclas; (C) apagar a linha `check_bluez_justworks_repairing` de `main()` — a função continua no arquivo, e ninguém a chama. É a classe de defeito que esta casa já batizou (ENTREGA-QUE-NAO-LIGOU-01, `o codigo que existe e ninguem chama`). Os dois testes que existem (test_doctor_le_pelo_dono_unico, test_doctor_avisa_em_vez_de_mentir_quando_o_dono_some) são grep de TEXTO e sobrevivem às duas mutilações; o portão de paridade também, porque procura `JustWorksRepairing` e `verificar` — palavras que continuam vivas dentro da função morta.",
    "prova": "MORDIDA A: `sed -i 's/            fail \"JustWorksRepairing=always ATIVO/            pass \"JustWorksRepairing=always ATIVO/' scripts/doctor.sh` -> `.venv/bin/python -m pytest -q tests/unit/test_bluez_config_sh.py tests/unit/test_bt_resilience_assets.py tests/unit/test_radio_aberto_01.py tests/unit/test_plataforma_wiring.py` => `138 passed`; `bash scripts/check_packaging_parity.sh` => `[ OK ] config do BlueZ: dono unico, chamado nos dois lados, com detector no doctor`. MORDIDA C: `sed -i '/^    check_bluez_justworks_repairing$/d' scripts/doctor.sh` (chamadas restantes em main(): 0) => `138 passed` + paridade `[ OK ]`. Cura devolvida com `git checkout -- scripts/doctor.sh` nos dois casos; suíte cheia depois: 7144 passed, 1 skipped."
  },
  {
    "gravidade": "alta",
    "onde": "scripts/build_deb.sh:191 (e todo packaging/) contra scripts/doctor.sh:1721 e scripts/check_packaging_parity.sh:569",
    "o_que": "MEDIDO. O `doctor` EMPACOTADO é cego ao `always`, e o portão novo anuncia o contrário. O `build_deb.sh:191` copia `doctor.sh` para `/usr/share/hefesto-dualsense4unix/scripts/` e NÃO copia `bluez_config.sh`; nenhum empacotamento (deb, fedora, arch, flatpak, nix) o leva. Como `check_bluez_justworks_repairing` lê exclusivamente pelo dono único em `${ROOT_DIR}/scripts/bluez_config.sh`, no layout do .deb ela cai no ramo `warn ... o dono unico da config do BlueZ nao esta aqui` e não vê nada. Reproduzido contra o /etc REAL desta máquina, que tem `always` em main.conf:25. O curador registrou a dívida de empacotamento como `exige postinst proprio, e e entrega a parte` — verdade para APLICAR a config, falso para o DETECTOR: ele é leitura pura e entra numa linha do laço `for _s in ...` que já existe. E o portão imprime `[ OK ] ... com detector no doctor` sem nunca conferir que o dono viaja junto com o doctor: é o mesmo vácuo (linha que LÊ como proteção e não protege) que o próprio curador acabou de fechar duas seções acima.",
    "prova": "`mkdir -p $S/deb/usr/share/hefesto-dualsense4unix/scripts; for _s in doctor.sh disable_steam_input.sh fix_wireplumber_default_source.sh dsx_recover.sh; do install -Dm755 scripts/$_s $S/deb/usr/share/hefesto-dualsense4unix/scripts/$_s; done` (o laço exato do build_deb.sh) -> `ls` mostra 4 arquivos, sem bluez_config.sh. Harness com a função extraída por awk e stubs de pass/warn/fail/info: `bash $S/doctor_harness.sh $S/deb/usr/share/hefesto-dualsense4unix` => `WARN scripts/bluez_config.sh ausente — o dono único da config do BlueZ não está aqui`; `bash $S/doctor_harness.sh $PWD` (mesma máquina, mesmo /etc) => `FAIL JustWorksRepairing=always ATIVO no /etc/bluetooth/main.conf`. E `grep -rn bluez_config scripts/build_deb.sh packaging/` => zero."
  }
]


## LOGS

reverificacao: 3 lentes, 2 reprovaram, 3 achados altos
