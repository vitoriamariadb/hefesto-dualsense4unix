# agente adf47f558f750276b (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Curar M1 M2 M4 M5 da revisão
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, cure quatro achados de uma revisão adversarial que acabou de rodar sobre o índice do git (45 arquivos em stage sobre o HEAD `0bb92a5`). **NÃO commite. NÃO perca o índice** — há 45 arquivos não commitados e perdê-los é inaceitável; nunca use `git stash`.

**M2 (o mais importante) — o portão da cura falha ABERTO.**
`src/hefesto_dualsense4unix/app/actions/profiles_actions.py:947-953`:
```python
checar = getattr(self, "_tem_edicao_pendente", None)
if callable(checar):
    try:
        if bool(checar()):
            return True
    except Exception:
        return False        # <- "nao sei" vira "nao ha trabalho a proteger"
```
O revisor MEDIU a consequência: forçando `_tem_edicao_pendente` a estourar, **o defeito inteiro volta** — o editor pula para o perfil do jogo, o Salvar grava lá, e a cor e o rumble dela somem sem diálogo. Não há gatilho conhecido em produção (o alcance é latente), mas para uma guarda cujo único trabalho é proteger trabalho não salvo, o default tem de ser **FECHAR**, não abrir.
Cure: a exceção passa a significar "há trabalho a proteger" (`return True`), com o motivo escrito no comentário — e um teste que MORDA (force o `_tem_edicao_pendente` a estourar e prove que o campo Nome NÃO troca e que o Salvar NÃO vai para o arquivo errado). Confira os números de linha antes de editar; a árvore mudou.

**M1 — a cura não tem documento.** `NUNCA-TROCA-O-ALVO-01` tem **22 citações no código e ZERO em `docs/`**. É a cura de uma queixa literal dela, de 06/08/2026:
> *"Ainda tá confuso a aba Perfis, nada indica que eu tô alterando o perfil pra jogar na hora. Clico em ativar perfil vitória, altero todas as abas esperando que esse perfil fique salvo e seja usado no atual momento. Abro o jogo e muda. Ou pior: clico em salvar e ele salva com um nome aleatório ou de outro perfil."*
ESCREVA `docs/process/sprints/2026-08-06-NUNCA-TROCA-O-ALVO-01-a-janela-trocava-o-nome-e-o-salvar-ia-para-o-arquivo-errado.md`.
Use como exemplar de estilo `docs/process/sprints/2026-08-05-TRAVA-QUE-SOLTA-TARDE-01-*.md`. Levante as evidências você mesmo com `git grep -n "NUNCA-TROCA-O-ALVO-01"` e lendo o `git diff --cached` de `app/actions/profiles_actions.py`, `app/actions/footer_actions.py`, `app/app.py` e os testes novos.
Precisa conter: os TRÊS caminhos medidos pelos quais a janela trocava o nome sozinha (autoswitch + voltar para a aba Perfis; `_reload_profiles_store` sem `select_name` caindo no primeiro da lis

## RESULTADO FINAL

All four findings cured, nothing committed, index intact (46 staged files = the original 45 + the new sprint doc).

**M2 — o portão que falhava aberto (o mais importante).** `profiles_actions.py:952-963`: o `except` de `_ha_trabalho_no_editor` agora responde `True` e registra `perfis_edicao_pendente_indeterminada`, com o motivo escrito (falso "sim" custa um clique; falso "não" custa o trabalho dela). Três testes novos em `tests/unit/test_nunca_troca_o_alvo_01_o_salvar_que_mirava_outro_perfil.py` (classe `TestNaoSeiEHaTrabalhoAProteger`). **Mordida medida:** devolvido o `return False`, 3/3 reprovam e o journal mostra `sackboy_nativo.json` sendo regravado com backup — o defeito inteiro de volta.

**M1 — a cura ganhou documento.** `docs/process/sprints/2026-08-06-NUNCA-TROCA-O-ALVO-01-a-janela-trocava-o-nome-e-o-salvar-ia-para-o-arquivo-errado.md`, com os três caminhos medidos (autoswitch + `switch-page`; `_populate_profiles_store` caindo no primeiro da lista = `acao.json` → "Ação"; prefill do rodapé vindo de `_active_profile_name`), o agravante composto do `_reapontar_rascunho`, as três camadas e por que só juntas, o vizinho I-1, a armadilha da bancada, e o M3 registrado como inconsistência de doutrina (GRAU: SUSPEITA COM MECANISMO). Entrada correspondente no CHANGELOG.

**Tabela cura→teste medida, não afirmada:** arranquei cada camada e rodei — camada 1: 2 reprovam; camada 2: 2; camada 3 (repintura): 2; alvo memorizado: 1; rodapé: 1; M2: 3. Todas devolvidas e confirmadas verdes.

**Achado extra, registrado e NÃO tocado:** `_refazer_as_abas_apos_ativar` (`profiles_actions.py:1444`, da ATIVAR-NÃO-MENTE-01) faz a mesma pergunta com `contextlib.suppress` e default `pendente = False` — o mesmo fail-open do M2, em outra sprint. Está em "o que fica ABERTO" com grau declarado; não mexi porque é cura de outra leva.

**M4** — `_home_renumber_btn`/`_home_renumber_hint` → `_home_reconciliar_btn`/`_home_reconciliar_hint` (e as locais `renumber_row/btn/hint`), em `home_actions.py` e nos três arquivos de teste. Os `_renumber_ok`/`_renumber_fail` internos ficaram: referem-se honestamente ao IPC `identity.renumber`. **M5** — `bancada_q3.py` passou a compor `_tem_edicao_pendente = _HefestoApp._tem_edicao_pendente` como a `q2`, com a armadilha explicada no topo; contra a árvore curada ela agora para em `AssertionError: não reproduziu` em vez de mentir `>>> REPRODUZIDO`.

**Portões:** 7086 passed / 1 skipped (= a linha de base 7083 + meus 3 testes novos); `ruff` e `mypy` limpos; acentuação, glifos, referências (195 docs) e anonimato OK; e de brinde versão, paridade de empacotamento e dados de teste. Nenhum emoji nem U+2713/U+2717 (conferido por regex, já que o `validar-glifos.py` não pega).
