# agente a1986c743c60cc080 (sessao 6eac9113)

**tipo:** Explore
**tarefa:** Read profile-core sprints
**profundidade:** 2  **pai:** a48ea78b0b42a9356

## O QUE FOI PEDIDO

Repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix. Leia INTEGRALMENTE estes documentos (caminhos absolutos, todos sob /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/):

1. 2026-07-25-ABAS-01-as-abas-brigam-pelo-mesmo-estado.md
2. 2026-07-26-PERFIL-JOGO-01-as-configs-somem-ao-abrir-o-jogo.md
3. 2026-07-26-PERFIL-NASCE-CERTO-01-o-perfil-do-jogo-que-nunca-vence.md
4. 2026-07-27-EMPATE-01-tres-perfis-empatados-e-quem-ganha-e-o-alfabeto.md
5. 2026-07-29-PERFIL-SALVA-TUDO-01-salvei-todas-as-abas-e-so-parte-ficou.md
6. 2026-08-01-ESCOLHA-DELA-VENCE-01-a-mascara-do-perfil-e-o-tooltip-do-xbox.md

Para CADA um, extraia com precisão e cite linha quando útil:
- estado declarado (PROPOSTA / ENTREGUE / CURADA / CADUCADA / ABERTA / parcial) e a data dessa declaração;
- o defeito que ela cura, em uma frase;
- **a lista de ENTREGAS (E1, E2, ...) com o SÍMBOLO/mecanismo concreto que cada uma promete** (nome de função, constante, campo, arquivo:linha citados no documento). Isto é o mais importante: preciso da lista de símbolos prometidos para depois conferir por grep se têm chamador em produção;
- qualquer nota datada de caducidade/refutação;
- o que a sprint diz explicitamente para NÃO fazer;
- toda menção a: prioridade, escala de prioridade, catch-all, MatchAny, match/regra de jogo, source_name, with_profile_identity, rascunho/draft, autoswitch, manual_override_categories, aplicação imediata do perfil nas abas.

NÃO altere nenhum arquivo. Devolva um relato estruturado por sprint, denso, sem enrolação. Cite sempre caminho absoluto e número de linha quando o documento der.

## RESULTADO FINAL

Li os seis documentos integralmente. Relato por sprint.

---

# 1. ABAS-01 — as abas brigam pelo mesmo estado
`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-25-ABAS-01-as-abas-brigam-pelo-mesmo-estado.md` (177 linhas)

**Estado:** `ABERTA` (linha 3), prioridade **ALTA — "há perda silenciosa de dados"** (l.4). Aberta em **25/07/2026** (l.5). Nenhuma nota de entrega, curadoria ou caducidade em nenhum ponto do arquivo — o documento nunca foi atualizado após a abertura.

**Defeito que cura:** a aba Perfis edita e persiste perfil **sem nunca ler o rascunho** (l.23-24), e cada aba se reconstrói de uma fonte diferente (daemon / rascunho / disco / systemd, tabela l.16-21), de modo que qualquer sequência de cliques entre abas apaga configuração sem aviso.

**Não tem seção "Entregas" formal** — tem **achados numerados** + uma **Ordem de execução** (l.162-171). Símbolos/mecanismos concretos citados, item a item:

| item | linha | símbolo / arquivo:linha prometido ou acusado |
|---|---|---|
| ABAS-01 | l.28-40 | `app/draft_config.py:476` · `app/actions/profiles_actions.py:1204`; ausência de qualquer atribuição a `self.draft` na aba Perfis; seção `mode` gravada direto no disco |
| ABAS-02 | l.42-48 | `app/actions/lightbar_actions.py:138-174` — brilho em "Todos" limpa campo de cor por controle |
| ABAS-03 | l.50-56 | `app/actions/profiles_actions.py:1163` — mesclagem com rascunho só quando nome bate com perfil ativo; rename ⇒ base vem do disco |
| ABAS-04 | l.58-64 | `app/actions/rumble_actions.py:361` — "Parar"/"Devolver ao jogo" não escrevem no rascunho |
| ABAS-05 | l.68-75 | `app/actions/triggers_actions.py:562` → `daemon/state_store.py:190`; **`trigger.reset` limpa a trava manual sem categoria** — apaga `led`, `trigger` e `rumble` juntos (é a menção a granularidade por categoria / manual_override_categories) |
| ABAS-06 | l.77-82 | `trigger_reset` sem parâmetro de controle (`triggers_actions.py:562`) |
| ABAS-07 | l.84-89 | `app/ipc_bridge.py:361` — nenhum comando de rumble aceita alvo; tooltip do seletor promete rumble |
| ABAS-08 | l.91-97 | `daemon/ipc_draft_applier.py:51` — **o rascunho emite as três seções sempre**, logo todo "Aplicar" arma a trava manual para LED/gatilho/vibração sem avisar |
| PLACEHOLDER-01 | l.101-107 | `app/draft_config.py:140` — campo de microfone no rascunho sem widget no glade; nunca fica sujo ⇒ "Aplicar" nunca envia a seção `mic` |
| PLACEHOLDER-04 | l.109-114 | `app/actions/emulation_actions.py:274` — botão cria `daemon.toml` que o daemon não lê |
| PLACEHOLDER-05 | l.116-120 | `daemon/subsystems/coop.py:1084` — exceção vira aviso e a função **devolve sucesso** |
| PLACEHOLDER-12 | l.122-127 | `app/widgets/controller_card.py:1042` — gatilho ausente vira `0`, analógico ausente vira centro |
| Funções sem chamador | l.129-136 | `_query_gamepad_state`, `_stop_metrics`, `_stop_hotkey_manager`, `_stop_mouse_emulation`, `_start_mic_hotkey`, `_stop_plugins`, `_read_player_calibration`, `reset_counters`, `mark_disconnected`, `short_button_label`, `_identity_for`, **`speaker.set`** (IPC sem chamador) — e **`mic.set` não existe** |
| ABAS-11/16/17, PLACEHOLDER-10/17 | l.140-152 | ABAS-16: **oito donos do valor padrão de máscara**; **ABAS-17: os dois caminhos de "criar perfil" usam prioridades padrão diferentes (5 e 0)** para o mesmo conceito |

**Menções aos temas pedidos:** rascunho/draft é o eixo do documento inteiro (l.19-24, 32, 53, 62, 94, 165). Prioridade: só ABAS-17 (l.147-148, "5 e 0"). `manual_override` aparece como conceito — "trava manual **sem categoria**" (l.71-74) e "arma a trava manual" (l.95). Autoswitch: só indireto — "reabre a troca automática" (l.73-74). Não há menção a catch-all, MatchAny, `source_name`, `with_profile_identity`, autoswitch nominal nem aplicação imediata do perfil nas abas.

**O que NÃO fazer:** não há seção dedicada. O único imperativo negativo é l.170-171: sobre código morto, *"remover, ou dar uso e testar. **Ficar no meio é o pior dos três**"*. E l.156-160 lista o que **não deve ser reauditado** (66 handlers do glade existem; nenhum import morto; cartão de atalhos vivo; travessões iniciais são convenção).

**Critério de aceite (l.175):** nenhuma sequência de cliques entre abas pode perder configuração sem aviso.

---

# 2. PERFIL-JOGO-01 — as configs somem ao abrir o jogo
`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-26-PERFIL-JOGO-01-as-configs-somem-ao-abrir-o-jogo.md` (274 linhas)

**Estado:** `ABERTA` (l.3), **CRÍTICA** (l.4-5), aberta em **26/07/2026, depois de ela pedir o rollback** (l.6). Sem nota posterior de entrega/caducidade neste arquivo (mas a entrega 6 é referenciada como ainda aberta pela PERFIL-SALVA-TUDO, l.696-698 daquele doc).

**Defeito que cura:** sob autoridade `daemon` (sem jogo reconhecido) o portão de saída passou a barrar **só a cor**, então o `player_leds` que o cliente Steam escreve atravessa a camada dela e vai ao hardware — e, no rollback, sobram dois defeitos vizinhos (cadeado cedendo por título de janela e modo jogo soltando a cada alt-tab).

**Símbolos medidos (l.38-45):** `core/backend_pydualsense.py:122` `_GAME_LAYER_GATED_FIELDS = frozenset({"led"})`; `:1183` `_game_layer_admitida(...)`; `:1277` `admitida = self._game_layer_admitida(game)`; `:2877` `set_game_output_for(...)`; `:2949` log `game_output_retido_sem_jogo`; `:2971` log `game_output_aplicado`. Outros: `daemon/subsystems/coop.py:1431-1436` + `daemon/ipc_handlers.py:1350` (l.114-116, cosmético); `profiles/autoswitch.py:252-261` com `perfil_e_regra_de_jogo(profile, info)` e `perfil_declara_modo_de_jogo(profile)` (l.128-133); `profiles/loader.py:384` `migrate_modo_jogo_nos_presets` (l.135); `profiles/autoswitch.py:547` `manager.activate` (l.152-153); `profiles/autoswitch.py:237` `_sincronizar_modo_jogo_padrao` + `daemon/lifecycle.py:1812` (l.160-163); `set_game_trigger_for` (l.208-209).

**ENTREGAS (l.214-241):**

- **E1 (l.216-217)** — "Rodar o experimento acima e nomear o sintoma". **Entrega zero** (l.211-212: *"Nada deve ser corrigido antes dele"*). Mecanismo: os 4 eventos discriminantes de journal da tabela l.201-205 — `profile_autoswitch` / `profile_activated`; `autoswitch_cadeado_cedeu_a_regra_de_jogo`; `profile_mode_aplicado` / `modo_jogo_padrao_solto`; `game_output_aplicado` com `player_leds`.
- **E2 (l.218-224)** — o número dela nunca é sobrescrito sem autoridade de jogo. Símbolos: o portão vale para **todo campo** que a usuária define (ou seja, `_GAME_LAYER_GATED_FIELDS` deixa de ser só `{"led"}`); teste que morde chama `set_game_output_for` com `led` **e** `player_leds` sob **`_game_wins() == False`** e afirma que o número não chega ao **`_write_partial_output`**.
- **E3 (l.225-228)** — o cadeado deixa de ceder a perfil casado por título. Símbolo: **`profiles/autoswitch.py:261`** volta a exigir a regra do jogo (i.e. remover/condicionar `perfil_declara_modo_de_jogo`).
- **E4 (l.229-232)** — o modo jogo para de soltar no alt-tab. Símbolos: **`profiles/autoswitch.py:237`** + **`daemon/lifecycle.py:1812`**, com histerese ou tratando a janela da Steam como parte da sessão.
- **E5 (l.233-237)** — a tela diz quando alguém mexeu no que ela deixou (quem escreveu e por quê: *"o jogo pediu o número 3"*, *"o preset FPS entrou pelo título da janela"*). Sem símbolo de código nomeado.
- **E6 (l.238-241)** — desfazer ou assumir a migração de 18:28 nos presets dela (`~/.config/hefesto-dualsense4unix/profiles/` com `mode: gamepad, flavor: xbox`). *"Reverter código não reverte arquivo de configuração — isso precisa de decisão dela"*.

**Notas datadas de refutação/descarte (seção "O que foi descartado, com a medição que descarta", l.86-116):**
- **O autoswitch NÃO trocou de perfil na madrugada** (l.88-109): `git diff --stat 4dd4652 main` sobre `profiles/`, `daemon/subsystems/autoswitch.py`, `app/draft_config.py`, `daemon/subsystems/game_signal.py` = **saída vazia**; um único `profile_autoswitch` no journal (25/07 21:19:51, `to=sackboy_nativo`); todas as demais janelas de jogo com `autoswitch_congelado_pelo_cadeado` e candidato vazio; nenhum `profile_activated` na madrugada. Conclusão: *"o rollback não pode ser a cura de uma troca de perfil"*.
- **O efeito colateral da numeração é cosmético** (l.111-116).
- Datação do rollback (l.79-81): main no ar ~02:49–03:23, rollback entre 03:23 e 03:26; presets migrados em **25/07 18:28**, commit `54f1f3b` 25/07 18:29, ancestral de `4dd4652`.

**O que NÃO fazer / o que NÃO foi medido (l.17-29 e l.262-274):** não fingir saber a causa (*"Uma sprint que finge saber a causa é pior do que uma sprint honesta"*, l.27-28); **não corrigir nada antes da E1** (l.211-212); não presumir qual sintoma ela chama de "o perfil muda"; nada foi observado com jogo aberto; o teste que morde não foi escrito; nenhuma captura de tela; não se sabe se algum jogo dela roda fora da Steam (l.272-274).

**Temas pedidos:** autoswitch — l.88-109, 124-133, 152-153, 161-163, 202-203, 227, 230. match/regra de jogo — `perfil_e_regra_de_jogo` (l.129), `window_title_regex` frouxo `|Metro)`, `|Control)`, `|LEGO |`, `|Portal 2|` (l.149-150). Aplicação imediata por cima do que ela deixou — l.152-154 (`manager.activate` reaplica gatilhos, LEDs e emulação, "inclusive trocando a máscara para **xbox**"). Não menciona: prioridade/escala, catch-all, MatchAny, `source_name`, `with_profile_identity`, rascunho, `manual_override_categories`.

---

# 3. PERFIL-NASCE-CERTO-01 — o perfil do jogo que nunca vence
`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-26-PERFIL-NASCE-CERTO-01-o-perfil-do-jogo-que-nunca-vence.md` (242 linhas)

**Estado:** `ABERTA` (l.3), **CRÍTICA** (l.4-5), aberta em **26/07/2026 durante uma partida de Pragmata** (l.6). É declarada **causa-raiz de PERFIL-JOGO-01** e **absorve AUTO-03.1 e AUTO-03.2** (l.7-9). Contém uma **decisão datada de 27/07 que refuta a própria proposta anterior** (ver abaixo).

**Defeito que cura:** o perfil que ela cria para um jogo **nasce catch-all e com prioridade baixa**, e o teto da escala da janela (100) é exatamente a prioridade do catch-all dela — não existia número, pela interface, que fizesse o perfil do jogo vencer.

**Símbolos-raiz (l.70-73):** `app/actions/profiles_actions.py:703` → `self._get("profile_priority_scale").set_value(0)`; `profiles/simple_match.py:111` → `return SIMPLE_MATCH_PRESETS.get(choice, MatchAny())`; `app/actions/profiles_actions.py:1111` → `prio = max(0, min(100, profile.priority))`. Regra citada: **R-21** (l.47-49: em janela `steam_app_*` onde todos os candidatos são catch-all, nenhum tem autoridade). Evento: `autoswitch_congelado_pelo_cadeado candidate= current= wm_class=steam_app_3357650` (l.51). Estado em disco medido (l.31-34): `pragmata.json` `match={"type":"any"}` prio 5; `vitoria.json` `match={"type":"any"}` prio 100.

**ENTREGAS (l.115-202):**

- **E1 (l.117-129)** — o perfil nasce com a regra do jogo em foco, sem perguntar. Símbolos: `match` nasce `{"type":"criteria","window_class":["steam_app_<appid>"]}` lido da janela ativa no salvamento; `priority` nasce **acima de todo catch-all existente no disco**, calculado; nome sugerido é o do jogo. Sem janela de jogo em foco, **o catch-all atual continua certo** (l.127-129).
- **E2 (l.131-161)** — a lista de perfis **fica**; o que muda é deixar de ser adivinhação. Quatro itens: (1) **o teto sobe de 100 para 200** — "conserto mínimo e obrigatório" (l.145-149); (2) a tela mostra a consequência ao lado da escolha, com texto do tipo *"prioridade 110 — vence 'vitoria' (100) e todos os presets"* / *"prioridade 5 — perde para 'vitoria' (100), que vale em tudo"* (l.150-156); (3) a lista ganha coluna **quem vence onde** (l.157-160); (4) editor **Simples** mantém "Este jogo / categoria / tudo" e o **Avançado** mantém o campo cru — **os dois caminhos ficam** (l.159-160).
- **E2b (l.163-168)** — "Nascer certo continua valendo": a E1 não é afetada pela correção da E2.
- **E3 (l.170-182)** — a janela diz qual perfil vai valer antes de abrir o jogo: linha na aba Perfis **e** na aba Início (*"Neste jogo vai valer: Pragmata"*) e aviso de captura (*"o perfil vitoria vale em tudo e tem prioridade 100…"*) com botão **[ Restringir ao desktop ]**.
- **E4 (l.184-191)** — detector de armadilha rodando sozinho: perfil `any` com prioridade ≥ à de qualquer perfil específico é detectado **na subida e ao salvar**, avisa **uma vez**, com botão que resolve. **Não corrige sozinho** (l.190-191).
- **E5 (l.193-202)** — testes que mordem: criar perfil com janela de jogo em foco tem de produzir **`MatchCriteria`** com o `window_class` daquele jogo e prioridade maior que todo catch-all do disco; cenário com dois catch-all (um em 100) + perfil de jogo resolve para o perfil do jogo; o detector dispara no arranjo `vitoria` any/100 + `pragmata` any/5 e fica calado quando o perfil do jogo tem `match` específico.

**Nota datada de refutação (l.133-141):** decisão **dela, 27/07**, corrigindo a proposta anterior da própria sprint — *"o lance da lista de perfis, depois que vc explicou entendi como usar e vi que funcionam bem, talvez só deixar intuitiva, não precisamos desativar ela"*. A versão anterior propunha **tirar a prioridade numérica da tela e substituí-la por um seletor**: **"Está descartado."** (l.140). Princípio final (l.161): *"o número não é o problema; o número sem consequência visível é."*

**Conserto manual já aplicado no disco (l.92-100), não é entrega:** `match` any→criteria `steam_app_3357650`, `priority` 5→**110**, `player_leds` jogador 3→1; preservados lightbar (46,194,126), gatilhos Pulse/Pulse, rumble economia com passthrough, `mode: null`, override por controle. *"Isto é remendo, não cura"* (l.102).

**O que NÃO fazer:** descartado tirar a prioridade da tela (l.140-141); a E4 **não** corrige sozinha — *"corrigir o perfil dela sem pedir é a classe de defeito que esta casa passou a leva inteira consertando"* (l.190-191). **NÃO foi medido (l.224-242):** se o flip-flop do modo jogo para (causa própria em `profiles/autoswitch.py:237` + `daemon/lifecycle.py:1812`, é de PERFIL-JOGO-01); quantos perfis dela estão na armadilha (13 arquivos; `fallback` e `meu_perfil` também `any`, ambos prioridade **0**); o caminho do editor **Avançado** (o diagnóstico é do Simples); qual perfil ela queria fora do jogo; `window_class` de jogos **fora da Steam**.

---

# 4. EMPATE-01 — três perfis empatados, e quem ganha é o alfabeto
`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-27-EMPATE-01-tres-perfis-empatados-e-quem-ganha-e-o-alfabeto.md` (233 linhas)

**Estado (l.3-6):** **PARCIAL** — *"o desempate por incumbente entrou em `8d7fd45`; a E2 continua ABERTA"*, **conferido em 31/07**: `app/actions/profiles_actions.py:139` traduz `"any"` para `"Sempre"` e a coluna *Quando usar* termina aí, com **cinco** perfis catch-all dela empatando. Prioridade **CRÍTICA**, com trajetória datada: caiu para MÉDIA em 27/07 19h26 (item 0) e **subiu de volta em 27/07 21h45** (l.7-9). **Faixa 1** (l.10). Aberta em 27/07/2026 a partir de crítica adversarial; *"Mecanismo novo: nenhuma sprint existente o cita"* (l.11-12).

**Defeito que cura:** perfis catch-all empatados em prioridade são desempatados pela **ordem alfabética do nome do arquivo**, invisivelmente — e por isso `fallback` (cinza [40,40,40]) vence `vitoria` (roxo, gatilhos Pulse, co-op ligado).

**Mecanismo medido (l.27-33):** `profiles/loader.py:568` carrega com `sorted(directory.glob("*.json"))` (ordem alfabética); `profiles/manager.py:624` ordena com `key=lambda p: (not p.e_catch_all, p.priority), reverse=True`; os três produzem a mesma chave `(False, 0)`; o `sort` do Python é estável e `reverse=True` preserva a ordem de entrada. Semente errada: `assets/profiles_default/fallback.json:11` com `"lightbar": [40,40,40]`, `"player_leds": [false,false,true,false,false]`, `"lightbar_brightness": 1.0` (l.107-117). Cor automática correta: **`core/led_control.py:158`, `player_slot_color`** (l.136).

**ENTREGAS** — atenção à numeração inconsistente do documento: existe **E-1** (l.151) e depois **E1** (l.181), que são coisas diferentes.

- **E-1 (l.151-155)** — o `fallback` semeado deixa de apagar o controle: **remover o campo `lightbar` de `assets/profiles_default/fallback.json`**, com teste que reprova se ele voltar; `player_leds` fica como está. Justificativa (l.130-137): azul fixo deixaria o jogador 2 também azul; sem opinião, vale `player_slot_color`.
- **E1 (l.181-187)** — empate de catch-all deixa de ser silencioso: quando dois ou mais candidatos produzem a **mesma chave de ordenação**, o **`manager.py`** registra o empate e **quem ganhou**, com os nomes de todos. Hoje só loga o veto de catch-all em janela de jogo (**`profiles/manager.py:614-621`**); o empate no desktop não deixa rastro.
- **E2 (l.189-195)** — a janela mostra a disputa: na aba Perfis, a coluna *Quando usar* passa a dizer, para catch-all empatado, que existe disputa e quem vence. Regra **R-A** da fila (*todo controle que aceita um valor mostra, ali, o que aquele valor faz*). Texto-alvo: *"empatado com fallback e meu_perfil — quem vale é fallback"*. **Esta é a que o cabeçalho declara ainda ABERTA em 31/07.**
- **E3 (l.199-203)** — desempate deixa de ser o alfabeto: escolher e escrever um critério — **o mais recentemente ativado (incumbente), ou o marcado como padrão pela usuária**. *"Ordem de nome de arquivo não é critério — é acidente de `glob`."* **Cuidado:** não entra sem o item 0 respondido e sem ela saber qual perfil passa a valer. (Metade "incumbente" entrou em `8d7fd45`.)
- **E4 (l.205-210)** — teste que morde: três catch-alls empatados, assertando quem vence **pelo critério declarado**; **a mordida** é trocar o nome do arquivo perdedor para um que venha antes no alfabeto — se continuar verde, o teste trava o alfabeto, não o critério.

**Notas datadas de refutação (duas, e são o melhor material do documento):**
1. **Item 0 — RESPONDIDO em 27/07, 19h26 (l.158-179):** com o desktop em foco a aba Status diz `Perfil ativo: Nenhum` e o journal não tem nenhum evento de seleção de perfil no dia. Conclusão da hora: o mecanismo existe mas **não está mordendo agora**; prioridade caiu para MÉDIA e a sprint **não entrou** na leva de 27/07. Ressalva registrada: hoje está a jusante do cadeado **`autoswitch_locked.flag`**, ligado desde **24/07 20:42** — *"Assim que o autoswitch voltar a aplicar perfil sozinho, é o alfabeto que decide"* (l.174-176). Lacuna registrada: **nenhuma sprint de perfil diz o que muda com o cadeado ligado** (l.177-179, repetida em l.225-228).
2. **A confirmação de 27/07 21h45 (l.42-74), que refuta o item 0:** *"o controle no bt tá sem cor"*; medido `fallback.json` lightbar `[40,40,40]` vs `vitoria.json` `[129,61,156]`. Seção **"O erro de análise que isto corrige"** (l.61-73): *"'Perfil ativo: Nenhum' não prova que nenhum perfil forneceu os LEDs"*; armadilha de medição nomeada — **"quando a tela é suspeita, ela não pode ser a testemunha"** (l.73).

**O que a sprint diz para NÃO fazer:** *"O que esta sprint NÃO afirma"* (l.92-101) — **não afirma que este é o defeito que ela relatou**; entra como hipótese medida, não diagnóstico fechado. **Não mudar o perfil já instalado em silêncio** (l.142-147): mudar o arquivo semeado não conserta quem já tem `fallback.json` em `~/.config`; *"migração silenciosa de perfil é a classe de defeito que causou o rollback de 26/07: ou o programa avisa e ela decide, ou não mexe"*. **E3 não entra sem o item 0** (l.202-203). Apagar `meu_perfil`/`fallback` é **decisão dela** (l.229-230). **NÃO medido (l.224-233):** efeito do cadeado; se `meu_perfil`/`fallback` deveriam existir; se o mesmo empate ocorre entre perfis **específicos** de mesma prioridade (dois pares em 55 e dois em 60 no disco dela).

---

# 5. PERFIL-SALVA-TUDO-01 — salvei todas as abas e só parte ficou
`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-07-29-PERFIL-SALVA-TUDO-01-salvei-todas-as-abas-e-so-parte-ficou.md` (699 linhas)

**Estado:** `ABERTA — medida ponta a ponta, **nenhuma linha de código escrita**` (l.3). **CRÍTICA** (l.4-6), **Faixa 1** (l.7-8), aberta em **29/07/2026** (l.9). Medido no `HEAD` = **`e8e18b9`**; *"Nada foi executado do projeto"* (l.18-20).

**Defeito que cura (l.34-35):** *"O 'Salvar Perfil' grava as cinco seções que o rascunho POSSUI e reescreve como padrão de fábrica as cinco que ele apenas TRANSPORTA."* — `match`→`{"type":"any"}`, `priority`→`5`, `mode`→`null`, `suppress_desktop_emulation`→`false`, `mic` sempre `null` (tabela l.37-50); e as abas continuam mostrando o certo porque leem o rascunho em memória (l.52-54: **"a janela mente a favor dela"**).

**Símbolos da cadeia medida (elos 1-15, l.56-249)** — úteis como universo de grep:
`app/app.py:947` → `_bootstrap_draft_async` (`app/app.py:690`) → `_compute_draft_from_active_profile` (`app/app.py:629`) → `load_all_profiles()` → `DraftConfig.from_profile` (`app/app.py:651-654`); a fotografia em `app/draft_config.py:391-396`: **`source_match`, `source_mode`, `source_suppress`, `source_priority`, `source_controllers`, `source_name`**. Escritores de rascunho: `triggers_actions.py:289-290` (via `_persist_params_to_draft`, `:261`) e `:302`; `lightbar_actions.py:286`, `:291`, `:695`; `rumble_actions.py:242`, `:298`; `mouse_actions.py:273`, `:288`; `input_actions.py:354` (e `:305`). **Zero escritores**: `emulation_actions.py` (`grep -c 'self\.draft'` = 0, l.89-91) e `home_actions.py` (=0, l.107-109). Save: `footer_actions.py:257` → `gui_dialogs.prompt_profile_name` (`app/gui_dialogs.py:64-66`) → `footer_actions.py:281` `save_profile(draft.to_profile(nome))`. **O gate:** `app/draft_config.py:463` `mesmo_perfil = self.source_name is not None and name == self.source_name` — igualdade de string crua, existindo `profiles/slug.py:52` `mesmo_slug` (docstring `:53-59` = R-10), usado em `profiles_actions.py:1492` e `:1441` mas **não aqui**. O estrago: `app/draft_config.py:466-477`; default `app/draft_config.py:399` `def to_profile(self, name: str, priority: int = 5) -> Profile`. **`with_profile_identity`** (`app/draft_config.py:514`) tem **um único chamador**, `app/actions/profiles_actions.py:1497` (l.209-216 e l.258-261) — `_on_saved` (`footer_actions.py:283-299`) atualiza `_active_profile_name` (`:287`) e `_draft_baseline` (`:292`) mas **não** o chama. Caminho alternativo: `profiles_actions.py:1576` / `_edita_o_perfil_do_rascunho` (`:1414-1451`) / `:1596` (usa o disco) / `:1059-1064` (não chama `profile_switch`). Ativação: `profiles/manager.py:463-480` (suppress) e `:487-500` (mode); `profiles/manager.py:231`. Prioridade: `_prioridade_acima_dos_catch_all` (`profiles_actions.py:1398-1412`), `_FOLGA_ACIMA_DO_CATCH_ALL = 10` (`:78`), `PRIORIDADE_MAXIMA = 200` (`:74`). Mic: `MicDraft` **zero escritores**, `toggles_system` **zero widgets no glade**, `profile.mic` **zero leitores na ativação** (l.163-171); único consumidor `daemon/ipc_draft_applier.py:391-411` `_apply_mic` chamado em `:87`, gateado por `to_ipc_dict`/`self.mic.dirty` (`app/draft_config.py:901-905`).

**ENTREGAS (l.434-607), ordenadas por preço crescente:**

- **E1 — fechar o gate que rebaixa (l.438-468).** (a) `mesmo_perfil` passa a comparar por **slug**, usando **`mesmo_slug`** (`profiles/slug.py:52`); (b) o rodapé **reaponta a fotografia**: `self.draft = draft.with_profile_identity(profile)` no `_on_saved`. Arquivos: `app/draft_config.py:463`; `app/actions/footer_actions.py:283-299`. Testes A (dois saves seguidos com nome novo preservam match/priority/mode/suppress — **já vermelho hoje**), B (`source_name="Navegação"` salvo como `"Navegacao"`), **C, que morde do outro lado** (nome novo continua nascendo `MatchAny` no **primeiro** save — contrato **R-11**, `app/draft_config.py:448-462`).
- **E2 — tirar o 5 mágico (l.470-489).** Remove o default `priority: int = 5` de `to_profile` e obriga o chamador a passar a prioridade que ela vê na tela. Arquivos: `app/draft_config.py:399` e `:466-470`; `app/actions/footer_actions.py:281`. **Portão de busca:** `priority: int = 5` não pode existir como default em nenhuma assinatura de gravação. Varrer os dois callsites (`footer_actions.py:281`, `profiles_actions.py:1585`) e os dublês de teste.
- **E3 — dar escritor de rascunho às abas Emulação e Início (l.491-524).** `mode` e `suppress_desktop_emulation` deixam de ser só estado vivo e viram configuração do perfil. Arquivos: `emulation_actions.py:615`, `:649`, `:664`, `:667`; `home_actions.py:973-997`, `:1004`, `:1040-1076`; `app/draft_config.py:301-303` e `:476-477` (campos deixam de ser `source_*` só-transporte e ganham donos editáveis); `profiles_actions.py:1620-1621`. Teste que morde: reativar o perfil e exigir que o applier de `profiles/manager.py:463-480` receba `True` (hoje recebe sempre `False`). **Risco ALTO**, depende do gate **R-02** (`_perfil_tem_opiniao`, `daemon/lifecycle.py:1472`, alcançado pelo `profile=profile` que `profiles/manager.py:468` já passa). Conversa com **EMULACAO-NO-JOGO-01**, onde a supressão automática foi **descartada** porque `apply_profile_suppression` recusa liberar em catch-all (`daemon/lifecycle.py:1472-1478`, `IGNORADO_CATCH_ALL`) — *"as duas entregas… não podem ser feitas em desacordo"*.
- **E4 — decidir o destino de `Profile.mic` (l.526-549).** Ou ganha widget + escritor de `MicDraft` + applier no `ProfileManager`, ou **sai do esquema**. Arquivos: `profiles/schema.py:322-346` e `:435`; `app/draft_config.py:140-160` e `:442-446`; `gui/main.glade`; `profiles/manager.py:446-520` (applier novo); `daemon/ipc_draft_applier.py:391-411` (já pronto). Teste ponta a ponta exigindo `daemon.config.mic_button_toggles_system` mudado. Cuidado de **posse**: `mic unmute` toma a posse e mata o botão físico até `mic release`.
- **E5 — rastro e limpeza (l.551-579).** (a) remover campos mortos `EmulationDraft.xbox360_enabled` (`app/draft_config.py:132-137`) e `LedsDraft.mic_led` (`:73`), medidos com zero referências; (b) structlog da janela em destino que sobrevive e `profile.apply_draft` (`daemon/ipc_handlers.py:455-456`) logando `applied`/`failed`. Arquivos: `app/draft_config.py:73`, `:132-137`, `:271-293`; `utils/logging_config.py`; `daemon/ipc_handlers.py:455-456`. Portão de busca: reprova qualquer campo do `DraftConfig` sem escritor **nem** leitor. Armadilha: **não logar caminho de perfil com MAC dentro** (override dela é `a0fa9c0000f0`).
- **E6 — a janela passa a MOSTRAR o que está no arquivo (l.581-607).** Painel de **leitura** na aba Perfis + aviso de divergência rascunho×disco. Arquivos: `profiles_actions.py:1228-1316`; `gui/main.glade`; `footer_actions.py:593` (`_refresh_all_tabs`); `app/app.py:728-731` (`_tem_edicao_pendente`, que **já calcula** a divergência e hoje só serve ao aviso em `app/app.py:749-755`). Risco MÉDIO-ALTO; lição **R-08** em `app/app.py:739-743` (nunca recarregar do disco por baixo de uma edição).

**Refutações explícitas (seção "O que NÃO é a causa", l.281-363)** — todas com a palavra **REFUTADO**: o rascunho não desconhece as seções (`DraftConfig`, `app/draft_config.py:271`); não é "salva tudo mas o daemon não reaplica" (`profiles_actions.py:1059-1064`, `profiles/manager.py:231`); **não é a cura de 28/07 que falta instalar** — `8d7fd45` ("Salvar pela aba Perfis parou de rebaixar", 28/07 19:44) cobre **só** `match`/`priority` e **só** na aba Perfis (`profiles_actions.py:1637-1652`); `draft_config.py` não está entre os 12 arquivos e seu último commit é **`d92b544`, 25/07 19:12** (l.274-279, 297-302); **não é o autoswitch sobrescrevendo** — cadeado ligado (`autoswitch_locked.flag` = `1`), journal com `autoswitch_congelado_pelo_cadeado candidate=Navegação current= wm_class=steam` em 28/07 21:07:25 e hoje 17:13:38 / 18:11:49 / 20:14:33; quem desfaz é **a ativação** lendo o arquivo empobrecido (l.304-311); a aba **mente a favor dela** (l.313-320); **a prioridade 5 não foi escolha dela no slider** — `grep -rn 'priority: int = 5'` = **uma ocorrência**, `app/draft_config.py:399` (l.322-346), com refinamento honesto: só `pragmata.json` e `pragmata2.json` têm 5; `meu_perfil` tem 1, `vitoria` e `fallback` têm 0; Aplicar e Salvar **não** gravam as mesmas seções (tabela l.352-359: `mouse` no Aplicar é **só velocidade**, sem `enabled` — **HARM-05**); a aba Perfis **não lê de volta** as outras seções (`_populate_editor`, `profiles_actions.py:1228-1316`, grep vazio — sustenta a E6).

**O que NÃO fazer (l.609-670), seção explícita:**
1. **NÃO migrar os arquivos dela em silêncio** — `pragmata.json`, `pragmata2.json`, `meu_perfil.json`, `vitoria.json` ficam como estão até ela decidir; migração silenciosa causou o rollback de 26/07 e `8d7fd45` registra a decisão de não tocar no já instalado.
2. **NÃO afrouxar `mesmo_perfil`** a ponto de nome NOVO herdar a regra de origem no PRIMEIRO save — reabre **R-11** (`app/draft_config.py:448-462`; "Salvar como MadJack" com o FPS ativo produzia regex do FPS e prioridade 60).
3. **NÃO persistir `suppress_desktop_emulation: true` em catch-all sem o gate R-02** — os cinco perfis dela são catch-all; os únicos dois com `suppress: true` (`sackboy_nativo.json`, `coop_local.json`) casam por `criteria`.
4. **NÃO gravar `rumble.passthrough: false` a partir do "Aplicar"/"Parar"** (`rumble_actions.py:361-384`; ressuscita SPRINT-GAME-RUMBLE-01 e RUMBLE-PRESO-01).
5. **NÃO densificar entradas parciais de `controllers`** (`profiles_actions.py:1597-1605`, `app/draft_config.py:500-510`; `model_dump` apaga a lightbar do controle).
6. **NÃO tocar nos três perfis que dependem do que têm** — `point_and_click.json` (único com `key_bindings` próprio, `r1 -> KEY_DOT`, e `mouse.enabled: true`), `sackboy_nativo.json` e `coop_local.json`.
7. **NÃO tentar diagnosticar o próximo save pelo journal** — `grep -cE 'footer_|gui_'` = **0**; enquanto E5 não entrar, medir save é **mtime + conteúdo do JSON**.
Mais **quatro provas que exigem o olho dela** (l.658-670), incluindo "sair do jogo e confirmar que mouse e teclado voltam" — *"Se não voltarem, a E3 fez estrago pior que o defeito"*.

**Temas pedidos, extras:** empate/catch-all — `pragmata.json` e `pragmata2.json` **byte-idênticos fora o `name`** (l.382-389) e journal `profile_select_catch_all_sem_autoridade_em_jogo candidatos=['Pragmata','Pragmata2','fallback','meu_perfil','vitoria']` (l.396-399); **manual_override** — journal `profile_apply_respeita_override_manual categorias=['led','rumble','trigger'] profile=Pragmata2` (l.242) e `profile_mode_revert_skipped mode_from_profile=None motivo=catch_all_sem_opiniao` (l.243). **Não medido (l.672-699):** não viu a tela; não sabe qual save produziu qual arquivo; **não reproduziu o save — todos os "como PROVAR" são projeto de teste, nenhum executado**; não mediu o rename; não sabe se `Pragmata`/`Pragmata2` deveriam ser dois; **não olhou os presets de fábrica** (`acao`, `aventura`, `corrida`, `esportes`, `fps` com `mode: {"kind":"gamepad","gamepad_flavor":"xbox"}` da migração de 25/07 18:28 — *"já registrada na PERFIL-JOGO-01, entrega 6, e ainda **aberta**"*).

---

# 6. ESCOLHA-DELA-VENCE-01 — a máscara do perfil e o tooltip do Xbox
`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-08-01-ESCOLHA-DELA-VENCE-01-a-mascara-do-perfil-e-o-tooltip-do-xbox.md` (294 linhas)

**Estado:** **E1 e E4 ENTREGUES em 01/08/2026 (noite); E2, E3 e E5 seguem ABERTAS** (l.3-4). Status anterior: **PROPOSTA**, escrita em 01/08/2026 "para sobreviver à queda da sessão" (l.5-7). **ALTA** (l.8-10). Aberta em 01/08/2026 pelo pedido dela (l.11-13). Seção de fechamento datada: **"O que foi entregue — 01/08/2026, noite"** (l.248) e **"O que ficou ABERTO, e por quê"** (l.280).

**Defeito que cura:** três caminhos pelos quais a máscara escolhida por ela some — o editor converte "sem opinião" em Xbox, a máscara do perfil não sobrevive ao reboot, e a recusa de troca com jogo aberto **reporta sucesso**.

**Veredito da cadeia normal (l.20-24):** funciona — `activate → apply_emulation → apply_profile_mode → set_gamepad_emulation(origin="profile")`; o perfil **vence o flag** em tempo de execução.

**Defeitos e símbolos:**
- **D1 (l.28-45):** `{"kind":"gamepad","gamepad_flavor": null}` significa "mantém a atual" (`profiles/schema.py:701-718`, `daemon/lifecycle.py:1976-1979`), mas o editor converte em `"xbox"` **nas duas pontas**: abrir `flavor = (mode.gamepad_flavor if mode is not None else None) or "xbox"` (`app/actions/profiles_actions.py:605`) e salvar `flavor = flavor_sel.get_active_id() or "xbox"` (`:638-640`). *"nenhum teste morde"*.
- **D2 (l.47-58):** restore de boot roda com `mode_applier=None` (`daemon/connection.py:220-224`); quem manda é `gamepad_emulation.flag`. Mesmo na saída do Modo Nativo (`daemon/lifecycle.py:964-968`), o restore vem do stash capturado do flag (`:868`).
- **D3 (l.60-82):** `_recriacao_bloqueada_por_jogo` (`daemon/subsystems/gamepad.py:1008-1039`) recusa recriar o vpad com `display_authority == "game"` — **regra certa, não afrouxar**; mas `start_gamepad_emulation` devolve `True` mesmo com o gate recusando (`gamepad.py:1433-1434`), então `apply_profile_mode` devolve `APLICADO` e o relatório do `profile.switch` mente. Mitigação de armar antes (`daemon/launch_env.py:454-595`) **só cobre perfis que casam por `steam_app_<appid>` no `window_class`** (`_steam_profiles`, `:683-694`) — perfil que casa por **título de janela ou nome de processo nunca é armado** (l.75-82; presets de gênero `acao`, `aventura`, `fps` usam **`title_regex` e `process_name`**).
- **Tooltip (l.84-94):** tooltip único no widget inteiro (`profiles_actions.py:531-535`); o texto certo já existe: **`TEXTO_CUSTO_MASCARA_XBOX`** (`app/actions/home_actions.py:222-236`) e a função pura **`texto_do_custo_da_mascara`** (`:239-246`), da MASCARA-CUSTO-01, presentes só na aba Início.

**ENTREGAS (l.96-196):**
- **E1 — `null` continua `null` (l.98-117). ENTREGUE.** Onde: `profiles_actions.py:605` e `:638-640`. Duas saídas desenhadas: terceiro item `— manter a atual` em `_MODE_FLAVOR_ITEMS` (`:128-131`), ou (**recomendada, e foi a que entrou**) seletor **sem nenhum ativo** via `limpar_ativo` do `SegmentedSelector` (`tests/unit/test_segmented_selector.py:84-100`). Registro de entrega (l.250-264): `limpar_ativo` **não emite "changed"** de propósito — é POPULATE, não gesto dela, e `_modo_tocado` separa as duas coisas.
- **E2 — o perfil sobrevive ao reboot (l.119-138). ABERTA.** Onde: `daemon/connection.py:220-224`. Roteiro obrigatório: (1) `git log -S "mode_applier" -- src/hefesto_dualsense4unix/daemon/connection.py` e ler a mensagem; (2) se o motivo caducou, ligar o applier com **`origin="restore"`** — origin próprio, para não se confundir com `"profile"` nem `"manual"` nos quatro portões de `apply_profile_mode`; (3) se continuar válido, **o flag guarda também a procedência** (manual × perfil). Motivo de continuar aberta (l.282-287): não é uma linha, e não dava para reiniciar o daemon na máquina dela.
- **E3 — recusa com jogo aberto deixa de ser silenciosa (l.140-158). ABERTA.** R-04 não muda; fica **visível**. Onde: `daemon/subsystems/gamepad.py:1425-1435` (distinguir "apliquei" de "está ativo mas recusei"); `profiles/manager.py:515-533` (o relatório do `profile.switch` carregar a distinção); aba Status/rodapé. **Precedente obrigatório de leitura: APLICAR-VERDADE-01** (campo `failed`, frase *"Aplicado, menos: luzes, gatilhos."*), *"inclusive o 'O que NÃO fazer' dela"*.
- **E4 — o preço do Xbox onde ela escolhe (l.160-186). ENTREGUE.** (a) tooltip por botão: `SegmentedSelector` não suportava — `set_items` recebe só `(id, label)` (`app/widgets/segmented_selector.py:63-84`), `_create_buttons` (`:194-233`) não toca em tooltip; quatro passos, incluindo `_SegmentedLogic` (`:39-122`) com idempotência `if items == self._items: return` (`:76-77`) e `_index_of` (`:105-113`), `btn.set_tooltip_text(dica)` depois de `btn.set_mode(False)` (`:217`), **o stub sem GTK (`:265-297`) precisa da MESMA API**, e os dublês de teste (`tests/unit/test_r12_editor_simples_gui.py:98`). (b) reusar `texto_do_custo_da_mascara` (testado em `tests/unit/test_mascara_diz_o_que_custa.py`). **Como entrou (l.266-278):** método novo **`set_tooltips({id: texto})`**, deliberadamente **fora** da tupla de `set_items`, porque a forma `(id, label)` é load-bearing.
- **E5 — o empate deixa de ser decidido pelo alfabeto (l.188-196). ABERTA, marcada opcional.** `profiles/manager.py:836-848`: quando o incumbente não está entre os empatados, vence **`empatados[0]`** — ordem alfabética do nome do arquivo — e **é o `mode` do vencedor que entra**. *"A EMPATE-01 curou a metade do incumbente. Esta metade continua aberta… Se não for feita nesta sprint, some ao índice das pendências."*

**Testes que vão reprovar (tabela l.198-209):** `test_profiles_editor_mode.py` (`:239-320`, `:271-295`, `:327-360`); `test_segmented_selector.py` (`:76-82`, `:84-100`, `:204-230`) — **a forma da tupla `(id, label)` é load-bearing**; `test_profile_mode.py`, `test_r03_lock_manual_adia_modo.py`, `test_r04_gate_destrutivo_vpad.py`, `test_r04_arming_no_launch.py`, `test_modo01_*.py`; **`test_perfil_salva_tudo_registrar_nao_e_aplicar.py` — portão por AST que proíbe o escritor do rascunho disparar aplicação** (esta é a menção direta a "aplicação imediata do perfil nas abas": é **proibida por portão**); `test_preset_flavor_migration.py` (migração `dualsense→xbox` dos presets); `test_empate01_a_cor_volta_a_ser_dela.py` (desempate por incumbente).

**Armadilhas nomeadas (l.211-236), com regra e linha:** **R-07** (`gamepad.py:1505-1517`) — **o perfil NUNCA persiste a máscara no disco**; consertar a E2 fazendo o perfil gravar o flag quebra a cura de *"ela escolhia Xbox, abria o Sackboy e a flag virava dualsense"*. **R-04** (`gamepad.py:1008-1039`) — não afrouxe o gate, **torne visível**. `start_gamepad_emulation` retorna `True` quando o gate recusa. **R-02** (`lifecycle.py:1930-1947`) — **catch-all não é ordem de reverter**, e com janela de jogo em foco nenhum perfil reverte modo; mexer reabre *"abri o jogo e os controles morreram"*. **R-03** (`lifecycle.py:2172-2210`) — a pendência de modo é **ÚNICA** e sobrescrita de propósito; não vire fila. `ProfileModeConfig` é `extra="forbid"` (`schema.py:417`). **`coop` não é emitido pelo editor de propósito** (`profiles_actions.py:622-629`) — reemitir congela `coop: false`. **`_modo_tocado`** (`profiles_actions.py:397`, `:567`, `:616`, `:1822-1829`) — todo `set_active_id` programático DISPARA o handler; quem popula baixa a marca DEPOIS. Rodar os portões **depois** do `git add`. `ruff check .` ≠ `ruff check src/ tests/`.

**O que NÃO fazer (l.238-244), seção explícita:** não trocar os perfis dela (*"a entrega é a precedência e o aviso, nunca o arquivo dela"*); não escrever um segundo texto de custo da máscara (reusar a função pura); **não misturar com a PARIDADE-SONY-01** (mesmo subsistema de gamepad).

---

## Índice consolidado de símbolos prometidos (para o grep de chamador em produção)

**Existem e o doc afirma serem usados/entregues:** `with_profile_identity` (**um único chamador**, `app/actions/profiles_actions.py:1497`), `mesmo_slug` (`profiles/slug.py:52`, usado em `profiles_actions.py:1441`/`:1492`, **não** em `draft_config.py:463`), `texto_do_custo_da_mascara` / `TEXTO_CUSTO_MASCARA_XBOX` (`home_actions.py:222-246`), `set_tooltips` (novo, `SegmentedSelector`), `limpar_ativo`, `_prioridade_acima_dos_catch_all` + `_FOLGA_ACIMA_DO_CATCH_ALL` + `PRIORIDADE_MAXIMA=200` (`profiles_actions.py:78`/`:74`/`:1398-1412`), `player_slot_color` (`core/led_control.py:158`), `_perfil_tem_opiniao` (`daemon/lifecycle.py:1472`), `_tem_edicao_pendente` (`app/app.py:728-731`), `_apply_mic` (`daemon/ipc_draft_applier.py:391-411`).

**Prometidos como mudança/criação, ainda não confirmados:** `_GAME_LAYER_GATED_FIELDS` estendido além de `{"led"}`; `_game_wins()` + `_write_partial_output` no teste que morde; `origin="restore"` em `apply_profile_mode`; distinção "apliquei" × "recusei" em `gamepad.py:1425-1435` e `profiles/manager.py:515-533`; log de empate + vencedor em `profiles/manager.py` (E1 da EMPATE-01); critério de desempate substituindo `empatados[0]` (`manager.py:836-848`); escritores de rascunho em `emulation_actions.py`/`home_actions.py` (E3 da SALVA-TUDO); `MicDraft` com escritor e applier em `profiles/manager.py:446-520`; remoção de `EmulationDraft.xbox360_enabled` e `LedsDraft.mic_led`; remoção do `lightbar` de `assets/profiles_default/fallback.json`; remoção do default `priority: int = 5` em `app/draft_config.py:399`.

**Suspeitos declarados sem chamador (ABAS-01, l.129-136):** `_query_gamepad_state`, `_stop_metrics`, `_stop_hotkey_manager`, `_stop_mouse_emulation`, `_start_mic_hotkey`, `_stop_plugins`, `_read_player_calibration`, `reset_counters`, `mark_disconnected`, `short_button_label`, `_identity_for`, `speaker.set`.
