# agente afb5b950f19e255f4 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Escrever PERFIL-SEM-RASTRO e CANARIO-FS
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, ESCREVA DUAS sprints que faltam.

ANTES leia, como exemplar de estilo:
- docs/process/sprints/2026-08-05-TRAVA-QUE-SOLTA-TARDE-01-o-gesto-explicito-e-vitima-da-propria-trava.md
E a síntese consolidada:
- docs/process/estudos/2026-08-05-o-sistema-de-perfis-o-que-dezessete-agentes-mediram.md

ARQUIVO 1: docs/process/sprints/2026-08-05-PERFIL-SEM-RASTRO-01-o-perfil-mudava-e-nada-registrava-quem-mudou.md
Evidências: `git grep -n "PERFIL-SEM-RASTRO-01"` (10 citações). Leia o `git diff --cached` de src/hefesto_dualsense4unix/profiles/loader.py (+305 linhas) e src/hefesto_dualsense4unix/cli/cmd_profile.py (+100), e tests/unit/test_profile_historico.py e tests/unit/test_cli_profile_historico.py.
Núcleo: histórico versionado em `profiles/.historico/<slug>/`, poda em 10 versões, `restaurar_do_historico` byte a byte (não reserializa), `_atomic_write_bytes`, journal `profile_salvo`/`profile_apagado`/`profile_restaurado` com `match_antes`/`match_depois`/`priority_antes`/`priority_depois`/`origem`/`pid`/`backup`; `save_profile(..., origem=)`; CLI `profile historico` e `profile restore --em`.
A MOTIVAÇÃO medida: na madrugada de 05/08 foi impossível decidir se a prioridade 191 do disco dela veio da catraca do rodapé ou do controle deslizante, porque o journal carimbava só o basename do processo — que para a GUI é sempre o mesmo. Documente isso como o defeito que a sprint cura.
Registre também a DÍVIDA DE USO: `profile historico`, `profile restore` e `doctor --perfis` têm ZERO menção em docs/usage/cli.md e no README.md (classe DOC-VERDADE-01).
Registre a lacuna de teste: `_reject_traversal` (a defesa contra `../` no identificador do histórico) não tem um único caso, e é a única barreira entre um nome de perfil e uma escrita fora de profiles/.

ARQUIVO 2: docs/process/sprints/2026-08-05-CANARIO-FS-01-a-suite-escrevia-no-home-de-verdade.md
Evidências: `git grep -n "CANARIO-FS-01"` (9 citações). Leia o diff de tests/conftest.py, src/hefesto_dualsense4unix/integrations/storm_doctor.py, src/hefesto_dualsense4unix/app/actions/emulation_actions.py e tests/unit/test_conftest_canario_fs.py.
Núcleo: canário de sessão no conftest (foto sha256 + tamanho de três árvores reais do $HOME; reprova o run com a lista do que mudou; escotilha HEFESTO_SEM_CANARIO_FS); as duas constantes congeladas no import viram função — `storm_doctor._allowlist_path()` e `EmulationActionsMixin._wp_dropin_dir()`.
Registre com honestidade o LIMIT

## RESULTADO FINAL

All four documentation gates pass (exit 0), with the two new sprints staged.

**Resumo**

1. Escrevi `docs/process/sprints/2026-08-05-PERFIL-SEM-RASTRO-01-o-perfil-mudava-e-nada-registrava-quem-mudou.md` e `docs/process/sprints/2026-08-05-CANARIO-FS-01-a-suite-escrevia-no-home-de-verdade.md`, no estilo da TRAVA-QUE-SOLTA-TARDE-01, com grau declarado em cada afirmação e a MORDIDA de cada cura tabelada a partir das docstrings reais dos testes.
2. **Correção ao briefing (arquivo 1):** `_reject_traversal` **tem** seis casos (`tests/unit/test_profile_loader.py:245-275`) mais dois no IPC — a lacuna real, que documentei, é que **todos passam por caminhos de LEITURA**, e os dois chamadores novos (`historico_dir:650`, `_slug_para_historico:682`) e os três caminhos de ESCRITA que eles abrem não têm nenhum caso.
3. **Correção ao briefing (arquivo 2):** as quatro `Path.home()` citadas **não** estão em nível de módulo. Conferi uma a uma: `button_glyph.py:74` está em função mas é congelada no import por `GLYPHS_DIR = _resolver_dir_glyphs()` (`:96`); `i18n.py:61` e `system_check.py:37` resolvem na chamada e leem o `$HOME` real (porque o `HOME` não é isolado); `plugins.py:61` é efetivamente isolada, pois tenta `XDG_CONFIG_HOME` primeiro. Reformulei o limite como "três das quatro leem o `$HOME` real sob teste, e nenhuma seria vista pelo canário".
4. Achado extra registrado como ABERTO: `profiles_actions.py:1429` (`on_profile_save`, o caminho do controle deslizante) grava **sem** `origem=` — distinguível do rodapé só por ausência; `manager.py:170` idem.
5. Registrei a dívida DOC-VERDADE-01 com grep exato (`cli.md:23`, `:106-119`, `:212`; `README.md` sem menção) e as notas datadas de caducidade da seção 5.7 do estudo, que afirma que as duas constantes "continuam constantes de módulo" — o que deixou de valer no mesmo dia.
6. Verificações rodadas: 34 verdes em `test_profile_historico.py` + `test_cli_profile_historico.py` + `test_conftest_canario_fs.py`; após `git add`, os quatro portões de documentação (acentuação, glifos, referências, anonimato) saem 0.
