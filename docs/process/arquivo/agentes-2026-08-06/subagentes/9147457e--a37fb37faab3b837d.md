# agente a37fb37faab3b837d (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Renomear para Conexão Nativa (Sony)
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, execute um rename de interface que a dona **já pediu mais de uma vez** e que nunca foi cumprido. **NÃO commite** e **NÃO perca o índice** — há ~46 arquivos em stage sobre o HEAD `0bb92a5`; nunca use `git stash`.

A DECISÃO, palavra dela em 06/08/2026:
> *"Jogar direto é péssimo também. Já tinha pedido pra deixarmos: **Conexão Nativa (Sony)**."*

Confirmei na foto da aba Início gerada agora: o rótulo do meio ainda diz **"Jogar direto (Sony)"**.

O QUE FAZER: trocar o rótulo de interface `Jogar direto (Sony)` por **`Conexão Nativa (Sony)`** em TODA superfície que a usuária vê. Levante você mesmo, com `git grep -n "Jogar direto"`, e trate cada ocorrência. Os pontos conhecidos:
- `src/hefesto_dualsense4unix/app/actions/profiles_actions.py` — `_MODE_KIND_ITEMS`, o item `("native", "Jogar direto (Sony)")`
- `src/hefesto_dualsense4unix/app/actions/home_actions.py` — o seletor "O que o controle faz agora" da aba Início
- `src/hefesto_dualsense4unix/gui/main.glade` — tooltips que citam a frase (ex.: por volta de `:2844`)
- `docs/usage/modos.md` — a tabela dos modos (a linha `:16`) e qualquer menção no corpo
- `README.md`, `docs/usage/interface.md` e demais docs, se citarem
- testes que travam a string

REGRAS E CUIDADOS:
1. **É só o RÓTULO.** O id/kind interno continua `native` — não renomeie campo de esquema, IPC, CLI (`native on`) nem chave de perfil. Se encontrar um teste que trave a string do rótulo, atualize o teste; se encontrar um que trave o `kind`, NÃO toque.
2. **Coerência de vocabulário:** depois da troca, os quatro itens devem ler `Não mexer no modo` · `Controlar o PC` · `Jogar pelo Hefesto` · `Conexão Nativa (Sony)`. Se alguma frase vizinha (dica, tooltip, texto de ajuda) ficar incoerente por citar "jogar direto" em minúscula no meio de uma frase, ajuste-a para o novo termo — a lógica dos nomes tem de ficar óbvia, que é o pedido dela.
3. **`docs/usage/modos.md:16`** descreve o modo assim: *"O Hefesto solta o controle. O jogo fala com o DualSense físico, sem intermediário. Jogos que já falam DualSense nativamente e fazem tudo sozinhos."* Essa descrição está CERTA e explica por que o novo nome é melhor — mantenha-a, e ajuste só o rótulo da coluna.
4. **Nota datada**, como manda a casa: registre no lugar apropriado que o termo mudou em 06/08/2026 por decisão dela, e que o motivo é que "Jogar direto" não dizia o que a coisa é.
5. Se houver um mapa de `kind → rótulo` centralizado, use-o; se o

## RESULTADO FINAL

Rename cumprido, nada commitado, HEAD segue em `0bb92a5` e o índice está inteiro (60 arquivos em stage; nenhum `git stash`).

1. **Rótulo trocado em toda superfície viva** (`git grep -ni "jogar direto"` em `src/`, `tests/`, `packaging/`, `README.md`, `docs/usage/` volta **vazio**): `home_actions._MODE_ITEMS`, `profiles_actions._MODE_KIND_ITEMS`, applet COSMIC (`packaging/cosmic-applet/src/app.rs:659`), os dois avisos vivos da aba Emulação (`emulation_actions.py:936` e o markup `:956`), o tooltip do `main.glade:2822` e o comentário de largura `:2794`, mais `README.md:152`, `docs/usage/modos.md:16`, `interface.md:19`, `jogos-e-mascaras.md:17`, `quickstart.md:92`.
2. **Coerência de vocabulário:** o tooltip do editor de perfis dizia *"...ou jogar direto (Sony)"* → agora *"...ou a conexão nativa (Sony)"*; o tooltip do Glade dizia *"sai do"* → *"sai da"*. Os quatro itens leem `Não mexer no modo · Controlar o PC · Jogar pelo Hefesto · Conexão Nativa (Sony)`.
3. **Só o rótulo:** o id `native` não foi tocado em esquema, IPC, CLI nem chave de perfil — `test_profiles_vocabulario_leigo::test_ids_do_schema_intactos` continua verde intacto. Sprints antigas e a entrada histórica do `CHANGELOG:1491` ficaram como estão (não se apaga decisão medida).
4. **Não unifiquei, e o motivo é medido:** o rótulo mora em 2 listas Python + 1 Rust; o Rust não importa Python, então a fonte única é impossível de qualquer jeito, e o `tests/unit/test_vocabulario_das_quatro_superficies.py` já lê o fonte das três como TEXTO e reprova quem mudar um lado só — centralizar as duas em Python daria a mesma garantia ao custo de reescrever, durante o rename, justamente o portão que o protege.
5. **Nota datada (06/08/2026, decisão dela)** em três lugares: comentário `UX-MODE-TERMS-02` sobre `home_actions._MODE_ITEMS` (a frase-dona, com o motivo — o nome antigo dizia o gesto e não a coisa), ponteiro curto em `profiles_actions` e no `app.rs`, seção nova no topo do `CHANGELOG [Unreleased]` e uma citação em bloco em `docs/usage/modos.md` logo abaixo da tabela (a descrição da linha 16 ficou intacta, como pedido).
6. **Portão que MORDE:** novo `test_os_quatro_rotulos_leem_como_uma_lista_so`. Arranquei a cura (revertendo o rótulo em `profiles_actions`) e vi **2 reprovas** — a minha e a paridade `test_a_janela_nao_tem_duas_listas_de_modo_divergentes` (`'Jogar direto (Sony)' != 'Conexão Nativa (Sony)'`); devolvida, verde.
7. **A foto** (`retratar_abas.py` sem argumento, 9 abas regravadas): na `readme_inicio.png` o seletor "O que o controle faz agora" mostra os três botões com a mesma largura de antes — `Controlar o PC` | `Jogar pelo Hefesto` (realçado, ativo) | **`Conexão Nativa (Sony)`**, com o "ã" renderizado certo, sem corte, sem quebra de linha e sem empurrar nada; o resto da aba (dica do gamepad, "2 controles = 2 jogadores", máscara, cards P1/P2, Sessão) está idêntico. Olhei também a `readme_perfis.png`: o slot de Modo aparece vazio na captura offscreen, como já vinha antes (o seletor é montado em runtime) — sem regressão minha.
8. **Portões:** `7087 passed, 1 skipped` (7086 da linha de base + o teste novo; o `test_keyboard_wire_up` passou), `ruff check src/ tests/` limpo, `mypy` sem erro em 163 arquivos, e acentuação, glifos, referências (195 docs) e anonimato todos com saída 0.
