# agente ad33835b0b66702df (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Executar D-32 e D-33 do Steam Input
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix (branch restauro/inicio-da-sessao, árvore limpa, HEAD d20a7a9), EXECUTE duas curas de Steam Input já MEDIDAS. A base factual é `docs/process/estudos/2026-08-05-o-sistema-de-perfis-o-que-dezessete-agentes-mediram.md`, seção 1.5 — leia-a inteira antes (defeitos D-31 a D-34).

CONTEXTO HUMANO: a queixa dela é *"não faço ideia de quando é pra ativar os controles Steam e quando não, nem se os botões lá prestam"*. Estas duas curas são a parte MEDIDA dessa queixa.

CURA 1 — D-32: o pré-voo do `--apply` fecha a Steam à toa e mente "aplicado"
`scripts/disable_steam_input.sh:415-424` usa `needs_fix` (que casa a allowlist também) em vez de `needs_real_fix` (`:339`, que JÁ EXISTE e hoje serve só ao `--status`). Consequência medida: com SÓ appids da allowlist ligados, o `--apply` FECHA E REABRE A STEAM DELA para não mudar nada, e emite `resultado=aplicado`, que a janela traduz para "a Steam não sequestra mais o seu controle" (`src/hefesto_dualsense4unix/app/actions/daemon_actions.py:319-320`). Aconteceu às 02:13:13 de 05/08.
Confira os números de linha antes de editar — a árvore mudou.
A cura é de uma linha. O TESTE é o que importa: monte um `localconfig.vdf` de bancada com SÓ appids da allowlist em `"2"` e prove que o `--apply` NÃO pede para fechar a Steam e NÃO diz `aplicado`. Arranque a cura e veja reprovar.

CURA 2 — D-33: as três mensagens contam ARQUIVOS, não JOGOS, e chamam a escolha dela de "conflito"
- `src/hefesto_dualsense4unix/integrations/storm_doctor.py:151-155` — "Steam Input LIGADO em 1 perfil(is) fora da allowlist — clique 'Aplicar correções' na aba Sistema para desligar". O "1 perfil(is)" conta ARQUIVOS `vdf`, não jogos.
- `src/hefesto_dualsense4unix/app/actions/emulation_actions.py:1246-1248` — "Ligado — conflita com o Hefesto".
- `src/hefesto_dualsense4unix/app/actions/daemon_actions.py:301-321` — "Controle: a Steam não sequestra mais o seu controle."
Nenhuma diz DE QUAL JOGO fala; todas chamam a escolha dela de "conflito"; e mandam clicar num botão que APAGA exatamente a escolha que ela tomou.
A cura: as mensagens passam a nomear o(s) JOGO(S) por appid (e pelo nome quando o repositório souber traduzir — investigue se há tradução appid→nome disponível; se não houver, o appid cru é honesto e o nome inventado não é), e param de chamar a escolha dela de conflito. O vocabulário tem de dizer o que É: "o Steam Input está ligado para o jogo X, e o Hefesto vai desligá-lo no próximo ciclo porqu

## RESULTADO FINAL

Ambas as curas entregues, medidas e mordidas. Nada commitado.

**O que mudou**
- **CURA 1 (D-32)** — `scripts/disable_steam_input.sh`: o pré-voo passou de `needs_fix` para `needs_real_fix` em **dois** pontos (`:426` `--apply`, `:465` `--apply-quiet`), não só um. Motivo medido: a tag `resultado=aplicado` das 02:13:13 saiu do `--apply-quiet` (é o modo do `hefesto-steam-input-guard.timer`, e o único que emite `adiado-steam-aberta`, tag das 02:43/03:13), e é por ele que o `'Aplicar correções'` chega à janela. A sprint irmã escrita às 23:06 (`STEAM-QUE-DECIDE-01`, linha 421) diz o mesmo: `:418 e :452`.
- **CURA 2 (D-33)** — `nome_do_appid` saiu de `cli/cmd_steam.py` (estava atrás do `import typer`, inalcançável pelo doctor e pela janela) para `integrations/steam_launch_options.py`, junto de `rotulo_do_jogo` / `lista_de_jogos` / `juntar_rotulos`; a CLI virou reexport. `storm_doctor.steam_input_fora_da_allowlist` (novo) devolve `(appids, chave_global)`; o booleano antigo virou casca. `markup_status_steam_input` e `_frase_steam_input(rc, tag, jogos)` são puros e testáveis; os dois workers da aba Sistema **medem antes de rodar** (depois o `vdf` já foi zerado). Sem manifest, o appid cru — nome inventado, nunca.

**Mordida a mordida** (arrancar → reprovar → devolver)
1. `needs_real_fix` → `needs_fix` nos dois modos: **2 falhas** (`--apply` fechou a Steam e disse `aplicado`; `--apply-quiet` idem) — `TestPreVooNaoFechaSteamAToa`. A contraprova (appid fora da allowlist ainda fecha e ainda zera) continuou verde, provando que o teste não passa com o pré-voo anestesiado.
2. Mensagem do doctor de volta ao `"N perfil(is)"`: **5 falhas**.
3. Label da aba Emulação de volta a `"Ligado — conflita com o Hefesto"`: **3 falhas**.
4. `_frase_steam_input` de volta a `"a Steam não sequestra mais o seu controle"`: **6 falhas** (5 novas + a antiga, atualizada).
5. Fio desligado (`steam_input_jogos: None` nos workers): **2 falhas** — o formatter certo com o worter mudo não bastava.

**O texto novo das três**
- Doctor: `Steam Input ligado para Sackboy: A Big Adventure (appid 1599660) — o Hefesto vai desligá-lo no próximo ciclo do guarda, porque esse jogo não está na sua lista de exceções. Para manter a sua escolha, abra o jogo e clique 'Este jogo não funciona' na aba Sistema.` (ajuste **global**, que não é escolha dela por jogo, segue apontando `'Aplicar correções'`.)
- Aba Emulação: `Ligado para Sackboy: A Big Adventure (appid 1599660) — o Hefesto desliga no próximo ciclo, porque esse jogo não está na sua lista de exceções`
- Toast: `Controle: o controle de Sackboy: A Big Adventure (appid 1599660) voltou a ser entregue pelo Hefesto, porque esse jogo não está na sua lista de exceções.`

**Duas restrições que a cura teve de respeitar** — o toast do "Deixar tudo pronto" não pode pronunciar o jargão *"Steam Input"* (FEAT-STEAM-SIMPLES-01, teste vivo), daí o *"o controle de X voltou a ser entregue pelo Hefesto"*; e `test_steam_modo_simples::test_aplicado_relata_o_que_mudou` exigia a frase literal caducada — ganhou **nota datada** no lugar de sumir. Notas datadas também em D-32/D-33 do estudo. Bônus de higiene: `_roda` do teste de bancada agora fixa `XDG_CONFIG_HOME` (sem isso a suíte lia a allowlist real dela — CANARIO-FS-01).

**Portões**: `pytest 7047 passed, 1 skipped` (base 7017; +30); `ruff check src/ tests/` OK; `mypy` OK (163 arquivos); acentuação, glifos, referências-docs, anonimato, versão, paridade de empacotamento e dados de teste — todos OK. Zero emoji. Nem o `steam_input_apps.txt` nem o `localconfig.vdf` dela foram lidos ou tocados: tudo em fixture.

**Aviso**: `docs/process/sprints/2026-08-05-STEAM-QUE-DECIDE-01-ela-nao-tem-como-saber-quando-ligar.md` (665 linhas, mtime 23:06) apareceu na árvore durante minha execução e **não é meu** — é de um agente irmão. Ficou staged pelo `git add -A` que a instrução pedia.
