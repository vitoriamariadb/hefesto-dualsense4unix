# agente a9407bc6027631142 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Escrever sprint do Steam Input
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, ESCREVA a sprint que falta sobre Steam Input. Ela nasce de uma queixa direta da dona do projeto, dita em 05/08/2026:

> *"não faço ideia de quando é pra ativar os controles Steam e quando não. Ou quando é pra ir colocar os comandos da Steam em cada app, ou se os botões lá prestam ou não."*

ANTES DE ESCREVER, leia:
- `docs/process/estudos/2026-08-05-o-sistema-de-perfis-o-que-dezessete-agentes-mediram.md`, seção 1.5 (defeitos D-31 a D-34) e a linha do M-04 na tabela de suspeitas
- `docs/process/sprints/2026-07-26-STEAM-INPUT-01-ela-nunca-mais-precisa-decidir.md`
- `docs/process/sprints/2026-07-26-DUPLO-REGISTRO-01-o-steam-input-tem-dois-cadastros.md`
- `docs/usage/jogos-e-mascaras.md` (linhas 61-67, onde o conhecimento por jogo vive como PROSA)
- `docs/process/sprints/2026-08-05-FIACAO-QUE-FALTA-01-o-verificador-que-ela-nao-tem-como-ver.md` como exemplar de sprint executável por agentes
E confira você mesmo, com grep/leitura, cada caminho:linha que citar — a árvore mudou hoje.

CRIE `docs/process/sprints/2026-08-05-STEAM-QUE-DECIDE-01-ela-nao-tem-como-saber-quando-ligar.md`.

FATOS NOVOS, MEDIDOS HOJE (05/08, ~23h) — registre-os com grau MEDIDO:
1. **A previsão do D-31 se cumpriu.** O `localconfig.vdf` dela tem hoje o appid `1599660` (Sackboy) com `UseSteamControllerConfig "0"`. Na madrugada estava `"2"` — ela tinha ligado. A allowlist (`~/.config/hefesto-dualsense4unix/steam_input_apps.txt`) tem só `2111190` e `3357650`. O guarda zerou a escolha dela, sem aviso, sem toast, sem nada na tela. **Não cite o caminho do userdata dela nem números de conta; use o appid, que é público.**
2. O `hefesto-steam-input-guard.timer` segue ativo, ciclo de 30 min, e o journal de 24 h traz 24 `resultado=aplicado` e 24 `resultado=adiado-steam-aberta`.
3. A allowlist é escrita à mão, com comentários em português explicando POR QUE cada appid está lá (`2111190` — SetDualSenseTriggerEffect via Steamworks; `3357650` — suporte nativo entregue pela Steam). **Esse arquivo é o único lugar do projeto onde o critério por jogo existe de forma legível — e ela não tem como editá-lo pela janela.**

O CORAÇÃO DA SPRINT, e o que a torna diferente das duas anteriores: **STEAM-INPUT-01 se chama "ela nunca mais precisa decidir", e a realidade é que ela precisa decidir, o tempo todo, sem critério e sem informação.** Escreva isso com honestidade, com nota datada, sem apagar a decisão original.

ENTREGAS que a sprint precisa propor

## RESULTADO FINAL

Sprint escrita, staged, quatro portões verdes, sem commit.

**Arquivo:** `/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/2026-08-05-STEAM-QUE-DECIDE-01-ela-nao-tem-como-saber-quando-ligar.md` (665 linhas)

**A medição mais forte, que eu não esperava achar:** o guarda deixou testemunha datada. A cópia que ele mesmo fez às **14:51:59 de 05/08** ainda tem o appid `1599660` (Sackboy) com `UseSteamControllerConfig "2"`; o arquivo vivo, escrito **13 segundos depois** (`14:52:12`, journal `resultado=aplicado`), tem `"0"`. Os dois appids da allowlist (`3357650`, `2111190`) saíram intactos na mesma passagem. A previsão do D-31 não é mais previsão — é fato com hora, e o único registro dela é uma linha de journal indistinguível de "não mudei nada".

**Números conferidos por mim** (a árvore mudou hoje, e várias citações do estudo já estão defasadas): guarda `active`, ciclo 30 min, janela de 24 h com **23 `aplicado` + 24 `adiado-steam-aberta`**; `remove_appid_from_steam_input_allowlist` migrou de `:821` para `:828` e tem chamador só em `cli/cmd_steam.py:206,215`; `grep` do critério por jogo em `src/` e `assets/` segue em **zero**.

**O coração, como pedido:** nota datada dizendo que a STEAM-INPUT-01 se chama "ela nunca mais precisa decidir" e o estado medido é o oposto — sem apagar a decisão original, e creditando as cinco das nove entregas daquela sprint que **foram** pagas (conferi cada uma nesta árvore).

**Sete entregas,** cada uma com mordida e veto: E1 é o experimento M-04 com protocolo de quatro passos que ela pode seguir, tabela de leitura de resultado (incluindo o terceiro caso, "meia entrega", que ninguém tinha nomeado) e o veto de devolver as unidades do guarda ao fim; E2 o M-05 + comparador; E3 o guarda que conta o que apagou; E4 a superfície de remoção; E5 o critério fora da prosa — **com recomendação explícita de NÃO criar a lista antes de a E6 provar que medição ao vivo não basta**, porque uma lista de três jogos que parece completa é pior que lista nenhuma; E6 o cartão que responde "para ESTE jogo, ligo ou não?"; E7 os dois textos velhos.

**Os dois achados de texto que confirmei eu mesmo:** o comentário do glade (`:2437-2449`) afirma "nem na linha de comando" — falso desde que `cli/cmd_steam.py` entrou; e `gui/main.glade:2867` ainda manda procurar a exceção por jogo "na aba Emulação", quando ela está na Sistema. O teste `test_guia_das_mascaras_aponta_o_botao_e_a_aba_que_existem` cobre só o `.md`, não o glade — por isso a E7 traz caso novo.

**Portões:** `validar-acentuacao.py --all`, `validar-glifos.py --all`, `validar-referencias-docs.py --all` (192 documentos, zero referência morta) e `check_anonymity.sh` — todos zero, nada a corrigir. Sem MAC, sem emoji, sem o caminho de `userdata` nem número de conta dela em lugar nenhum.
