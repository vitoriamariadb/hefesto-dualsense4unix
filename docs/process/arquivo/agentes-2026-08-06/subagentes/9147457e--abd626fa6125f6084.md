# agente abd626fa6125f6084 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Construir retratar_dialogos.py
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix (branch restauro/inicio-da-sessao, árvore limpa após o commit 10f4818), CRIE a ferramenta que fotografa DIÁLOGOS. Ela não existe: `scripts/gui-captura/retratar_abas.py` só sabe abas, e uma investigação confirmou zero precedente de diálogo fotografado no repositório.

MOTIVO: a regra da casa é "interface só fecha com o olho dela" (PROVA-DE-TELA-01). Três diálogos novos entraram na leva de perfis e nunca foram vistos. As sprints registram isso como aceite em aberto (`2026-08-05-SALVAR-NAO-REBAIXA-02-...md:319` e `2026-08-05-INDICE-a-leva-...md:481-486`).

CRIE `scripts/gui-captura/retratar_dialogos.py`, modelado em `scripts/gui-captura/retratar_abas.py` (leia-o inteiro primeiro).

ROTA TÉCNICA — offscreen reparentando o miolo. Motivo, medido em `docs/process/COMO-OLHAR-A-TELA.md:142-149`: sob Xvfb não há gerenciador de janelas, uma `Gtk.Window` de verdade nunca é mapeada e o filho fica 1x1 para sempre; e `Gtk.OffscreenWindow` não aceita `Gtk.Window` como filho. Então:
- construa o `Gtk.MessageDialog` SEM chamar `.run()` (as três funções chamam `run()` internamente em `gui_dialogs.py:149,193,238` — chamada bloqueante que travaria o script);
- pegue `dialog.get_child()` (content area + action area), reparente para uma `Gtk.OffscreenWindow`;
- aplique a classe de tema `hefesto-dualsense4unix-window` e carregue o CSS — sem isso a foto sai em Adwaita CLARO e não é o produto (`gui_dialogs.py:19-29`). O padrão correto de carregar CSS numa offscreen está em `tests/unit/test_switch_sem_icone_quebrado.py:104-121`;
- drene o laço GTK mais de uma vez antes do `get_pixbuf()` (`retratar_abas.py:120-130` `_assentar()`), senão o widget mede 1x1 e a foto sai vazia.

Você vai precisar construir os diálogos sem `run()`. As funções em `src/hefesto_dualsense4unix/app/gui_dialogs.py` são monolíticas. ESCOLHA e justifique no cabeçalho do script: ou extrair o corpo de construção para funções `_montar_*` reutilizadas pelas públicas (refatoração de produção — preferível, porque garante que a foto é do diálogo REAL e não de uma cópia que envelhece), ou replicar. Se refatorar produção, os testes existentes que usam `inspect.getsource` desses diálogos precisam continuar verdes: `tests/unit/test_salvar_nao_rebaixa_02_...py:600`, `tests/unit/test_ativar_nao_mente_01_...py:332,341`, `tests/unit/test_gui_dialogs_theme.py:76`. Confira ANTES de decidir.

SÃO CINCO FOTOS, não três (dois diálogos têm dois estados cada):
1. `confir

## RESULTADO FINAL

Done. Nothing committed, as instructed.

**Rota escolhida — uma terceira, melhor que as duas propostas.** Em vez de extrair `_montar_*` ou replicar, o script troca `Gtk.MessageDialog.run` por uma função que **fotografa em vez de bloquear** e devolve `CANCEL`. Quem monta o diálogo continua sendo a função de produção inteira — a foto é o produto por construção, sem nem uma função `_montar_*` de onde a pública pudesse divergir.

**Não refatorei produção** (zero linhas alteradas em `gui_dialogs.py`). Conferi antes: a extração derrubaria 4 asserções `inspect.getsource` — `test_gui_dialogs_theme.py:76`, `test_salvar_nao_rebaixa_02:600`, `test_ativar_nao_mente_01:332` e `:341` — porque `_apply_app_theme(` e `set_default_response(` migrariam para as funções novas. Consertá-las seria mudar três arquivos de teste para tirar uma foto.

**Cinco descobertas técnicas que custaram tempo** (todas documentadas no cabeçalho): (1) sem `Gtk.init_check()` o `GtkDialog` nasce com `get_child()` = `None`, porque o miolo vem de *template*; (2) a foto tem de acontecer **dentro** do `run()` — `destroy()` na linha seguinte leva os filhos junto; (3) `set_size_request()` só vale **antes** do `show_all()`, senão a offscreen congela no tamanho mínimo e o texto quebra em coluna estreita; (4) o nó CSS precisa ser `messagedialog` (subclasse com `set_css_name`), senão perde o bloco top-level do `theme.css` e os botões saem sem a borda roxa; (5) a margem de sombra do nó `decoration` desloca o fundo em 28 px numa offscreen — desligada por folha própria, com o motivo escrito.

**Dimensões dos 5 PNGs** em `docs/usage/assets/dialogos/` — todos conferidos visualmente, legíveis e no tema Drácula do produto:

| arquivo | dimensão |
|---|---|
| `dialogo_rebaixa_prioridade.png` | 704x137 |
| `dialogo_vira_sempre_de_programa_especifico.png` | 696x137 |
| `dialogo_vira_sempre_de_so_manual.png` | 696x158 |
| `dialogo_descarta_edicao_pendente.png` | 710x137 |
| `dialogo_descarta_edicao_pendente_sem_nome.png` | 669x137 |

**Portão irmão** em `tests/unit/test_retrato_dos_dialogos_nao_vaza_dado_real.py` (5 testes). O risco aqui não é o MAC, é o **nome de perfil**: os nomes são perfis de fábrica versionados em `assets/profiles_default/`, então publicá-los não conta nada. Arranquei a cura cinco vezes e as cinco reprovaram: chamada ao daemon, leitura de disco, nome fora de fábrica, literal solto na chamada, e — a que exigiu reforço — dois estados virarem a mesma foto (a contagem de chamadas sozinha não via isso).

**Portões:** `pytest` 7017 passed / 1 skipped (7012 + os 5 novos); `ruff check src/ tests/` e `mypy` limpos; acentuação e glifos exit 0. Rodei também referências-docs, anonimato e test_data (novos arquivos) — todos OK.

Uma observação para você decidir: as fotos existem, mas o aceite da PROVA-DE-TELA-01 só fecha com o olho dela — e ainda não há nada em `docs/usage/` que aponte para `dialogos/`, então as imagens estão órfãs da documentação por enquanto.
