# agente ab93810134b94c901 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Implementar herança do match no rodapé
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, IMPLEMENTE a cura REGRA-NAO-SE-PERDE-02, já desenhada e decidida pela dona do projeto em 05/08/2026.

DECISÃO DELA: o match TEM de ser herdado no rodapé. O `MatchAny()` por nome novo deixou de ser contrato deliberado e virou defeito.

DESENHO JÁ APROVADO (siga-o; ele foi validado contra a suíte):

1. A herança mora no RODAPÉ, não no `to_profile`. Motivos medidos: `to_profile` tem só DOIS chamadores em produção — `app/actions/footer_actions.py:418` e `app/actions/profiles_actions.py:2034`; a aba Perfis descarta incondicionalmente o match que o `to_profile` devolve (`base.update({... "match": regra_final...})`), logo é imune; mexer no gate `mesmo_perfil` partiria o gate, que governa 4 campos com a mesma regra (match, priority, mode, suppress); e a mudança no rodapé atinge 1 asserção existente contra 5 se fosse no `to_profile`.

2. Crie em `footer_actions.py` um método irmão de `_prioridade_do_save`, com a mesma silhueta e docstring-escada, `_regra_do_save(existente, draft) -> Match`:
   - degrau 1: `existente.match` (o disco — é o que o remendo atual já faz);
   - degrau 2: `draft.source_match` (a regra do perfil de ORIGEM — a herança nova), MAS só se for regra de verdade. O predicado é ESTRUTURAL, não de tipo: construa o candidato e pergunte `candidato.e_catch_all is False`. Isso cobre de uma vez `MatchAny` e o `MatchCriteria` de campos vazios, sem inventar predicado novo.
   - degrau 3 (órfão, sem origem nenhuma): `MatchManual()`, NÃO `MatchAny()`.

3. Por que `MatchManual()` no ramo órfão (escreva isso na docstring): `MatchAny` não é neutro — é catch-all, perde para qualquer regra na chave de seleção (`profiles/manager.py:817-825`), dispara o veto R-21 (`manager.py:805-813`) e NUNCA ativa dentro do jogo, ao mesmo tempo em que ganha `max(catch-all)+folga` de prioridade e vence o desktop inteiro carregando `suppress_desktop_emulation`. É exatamente a forma do `sackboy_nativo` de 05/08. `MatchManual` nunca é candidato (`matches()` sempre False), `e_catch_all` é False para ele, a `sanidade` o isenta, e é a tradução literal do que o diálogo do rodapé significa: "guarde o que eu tenho agora; quando usar, eu digo". Registre a ressalva de `schema.py:127-129` (binário antigo rejeita `{"type":"manual"}`).

4. Em `_persist_profile_async` (`footer_actions.py:415-421`), o `model_copy(update={"match": ...})` passa a ser INCONDICIONAL, com a regra vinda de `_regra_do_save`. Mantenha `model_copy` e NÃO `mo

## RESULTADO FINAL

Implementation complete. All gates green.

## O que mudou

**`src/hefesto_dualsense4unix/app/actions/footer_actions.py`** — novo método `_regra_do_save(existente, draft) -> Match`, irmão de `_prioridade_do_save` com a mesma silhueta e docstring-escada: degrau 1 `existente.match` (disco), degrau 2 `draft.source_match` com predicado **estrutural** (`Profile(name="sonda", match=origem).e_catch_all is False`, cobrindo `MatchAny` e `MatchCriteria` vazio de uma vez), degrau 3 `MatchManual()`. A docstring registra por que `MatchManual` e não `MatchAny` (chave de seleção, veto R-21, `max(catch-all)+folga`, `suppress_desktop_emulation`) e a ressalva de downgrade de `schema.py:127-129`. Em `_persist_profile_async` o `model_copy(update={"match": ...})` virou incondicional; `model_validate` não foi introduzido (guarda dos `controllers` parciais). `to_profile` e o gate `mesmo_perfil` não foram tocados.

**`src/hefesto_dualsense4unix/app/draft_config.py`** — nota datada de 05/08 no bloco `:546-554`: o "MatchAny() por nome novo" deixou de ser contrato e virou default de conversão; decisão movida para o rodapé. `DraftConfig.default().to_profile("x")` segue devolvendo `MatchAny`, e as duas testemunhas citadas (`test_gui_salvar_perfil_fonte_e_esparsidade.py`, `test_perfil_salva_tudo_rascunho.py`) passaram sem uma linha tocada.

**`tests/unit/test_gravacao_de_perfil_passa_pelo_funil.py`** — `test_perfil_novo_pelo_rodape_continua_nascendo_sempre` reescrito no lugar como `test_perfil_novo_pelo_rodape_herda_a_regra_da_origem`, com nota datada (premissa valeu de 23/07 a 05/08). As duas docstrings caducas (`:216-223` e `:326-331`) corrigidas com nota datada; o código delas não mudou.

**`tests/unit/test_regra_nao_se_perde_02_o_nome_novo_nascia_sem_regra.py`** (novo) — 9 testes, reaproveitando `_janela_fake`/`_salvar_pelo_rodape`/fixture `disco` por import (a fixture é reexportada por atribuição, senão vira cascata de F811).

## A mordida, caso a caso (6 mutações executadas, cada uma revertida)

| mutação arrancada | quem ficou vermelho |
|---|---|
| **A** — degrau 2 (herança da origem) fora | `test_nome_novo_herda_a_regra_do_perfil_de_origem`, `test_o_perfil_que_ela_acabou_de_salvar_vale_dentro_do_jogo`, `test_perfil_novo_pelo_rodape_herda_a_regra_da_origem` |
| **B** — degrau 3 volta a `MatchAny()` | `test_a_origem_catch_all_nao_e_regra_a_herdar`, `test_sem_origem_nasce_so_manual`, `test_o_perfil_do_rodape_nunca_nasce_catch_all`, `test_o_rodape_nao_constroi_catch_all_por_conta_propria` (portão AST) |
| **C** — degrau 1 (disco) fora | `test_o_disco_vence_a_fotografia_quando_os_dois_tem_regra` + `test_salvar_por_cima_de_perfil_que_ja_existe_herda_a_regra` (sprint 01) |
| **D** — `model_copy` volta a ser condicional (estado pré-cura) | 7 vermelhos, incluindo `test_a_sanidade_nao_acusa_depois_de_tres_saves_pelo_rodape` (`catch_all_demais` reaparece) |
| **E** — predicado por tipo (`isinstance(..., MatchAny)`) | só `test_a_origem_catch_all_nao_e_regra_a_herdar` — é o `MatchCriteria` vazio passando, exatamente o caso que motivou o predicado estrutural |
| **F** — `_chamadas_a` devolvendo `[]` | `test_o_portao_enxerga_a_construcao_que_ele_veta` (canário do portão) |

Os 9 casos novos morderam; nenhum passou com a cura arrancada.

## Portões

`git add -A` → **`pytest -q`: 7004 passed, 1 skipped** · `ruff check src/ tests/`: All checks passed · `mypy src/hefesto_dualsense4unix`: Success, 162 arquivos · `validar-acentuacao.py --all`: rc=0 (uma violação minha, "media", foi curada por reescrita) · `validar-glifos.py --all`: rc=0. Nada foi commitado.

**Duas observações sobre a árvore.** (1) A linha de base que você me deu (6987 passed) não bate com a árvore que encontrei: a coleta sem o meu arquivo já dava 6996, e o meu contribui exatamente 9. (2) **Outra sessão está escrevendo neste mesmo working tree durante o meu trabalho** — no meio da execução apareceram `docs/process/estudos/2026-08-05-o-sistema-de-perfis-...md`, `docs/usage/cli.md`, `main.glade` e correções de lint em `profiles_actions.py` e `test_teto_da_prioridade_tem_uma_fonte_so.py` (duas falhas de `ruff` que existiam quando comecei e que eu deliberadamente não toquei — sumiram sozinhas). O `git add -A` final encenou tudo isso junto com o meu.
