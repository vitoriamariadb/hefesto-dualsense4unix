# agente a48ea78b0b42a9356 (sessao 6eac9113)

**tipo:** general-purpose
**tarefa:** Sprints mapeadas e integração
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

Repositório: /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix. LEVA 1 — ENTENDER. NÃO altere nenhum arquivo; só leia e sintetize.

TEMA: **o acervo de sprints já mapeadas sobre PERFIS, e como elas se integram ao trabalho que vamos fazer agora.**

Há ~113 sprints em `docs/process/sprints/` e estudos em `docs/process/estudos/`. A mantenedora pediu: *"veja as sprints que já temos mapeadas e como elas podem ser integradas nisso"*.

O TRABALHO ATUAL (contexto, já provado — não reinvestigue, use como dado):
- perfis da usuária viraram `match={"type":"any"}` com prioridades escaladas (191, 200);
- DEFEITO A: `footer_actions.py:352-368` não chama `DraftConfig.with_profile_identity` após salvar → `source_name` velho → todo save seguinte vira MatchAny + prioridade `max(catch-all)+10`, catraca de +10 por clique;
- DEFEITO B: `on_profile_new` chama `_esquecer_a_fotografia_do_editor()` e as guardas SALVAR-NAO-REBAIXA-01 param de agir → salvar por cima de um perfil existente zera regra e prioridade;
- queixas abertas dela: Steam Input não respeita decisão por jogo; perfis se alteram durante a partida; ativar perfil não aplica imediatamente as abas.

O QUE FAZER:
1. VARRA o acervo e liste TODAS as sprints/estudos que tocam: perfis, prioridade, autoswitch, match/regra de jogo, rascunho/draft, os botões do rodapé, Steam Input, e aplicação de perfil. Para cada uma: nome, data, estado declarado (proposta/curada/caducada) e uma linha do que ela resolve.
2. CRUZE com o código de HOJE: quais dessas sprints declaram entrega que **não está de pé** (o padrão que a `ENTREGA-QUE-NÃO-LIGOU-01` catalogou)? Verifique por `grep` se o símbolo/mecanismo que cada uma promete tem chamador em produção. Este é o item mais valioso do seu relatório.
3. IDENTIFIQUE sobreposição e conflito: sprints que propõem curas incompatíveis entre si, ou que mexem no mesmo campo (ex.: `manual_override_categories` tem pelo menos duas). Diga qual deve vencer e por quê.
4. As "assimetrias que PARECEM defeito e NÃO são" — o índice de 03/08 lista quinze. Extraia as que tocam perfis/autoswitch e destaque-as: são armadilhas para quem for corrigir agora.
5. PROPONHA A CONSOLIDAÇÃO: um plano de execução que integre as sprints existentes com os defeitos A e B e as três queixas abertas dela. Ordem, dependências provadas (não supostas), e o que pode ser feito em paralelo. Diga explicitamente o que NÃO deve ser tocado.

Entregue algo que sirva de índice de trabalho. Cite arquivo e data sempre. Separe o que você VERIFICO

## RESULTADO FINAL

Agent 3 (Steam Input/override batch) is still running; I verified that area in code myself. Here is the synthesis.

---

# LEVA 1 — ENTENDER: o acervo de perfis contra o código de hoje

**Nada foi alterado.** Tudo abaixo separa o que **VERIFIQUEI no código/disco** do que **LI em documento**.

## 0. Três medições ao vivo que mudam a premissa do briefing

Feitas agora, leitura apenas:

| medição | resultado | consequência |
|---|---|---|
| `~/.config/hefesto-dualsense4unix/autoswitch_locked.flag` | **NÃO EXISTE** (só `gamepad_/keyboard_/mouse_emulation.flag`) | **o cadeado caiu.** O lado (a) da `AUTOMATISMO-MORTO-01` (30/07) e a nota dela de 03/08 (`autoswitch_locked: True` há seis dias) **caducaram**. O autoswitch está VIVO — e é por isso que a situação dos catch-all virou aguda |
| `active_profile.txt` + `session.json` | **`sackboy_nativo`**, mtime **05/08 02:51** | o perfil ativo agora É a vítima da catraca |
| os 15 JSONs dela | `sackboy_nativo`: `match={"type":"any"}`, `priority=191`, **`suppress_desktop_emulation=true`**, `mode={gamepad, dualsense, coop:true}` | ver §2.1 — é o achado mais grave |

**A prioridade 200 não está no disco hoje.** Só o **191**. Os demais catch-all: `Pragmata` 5, `meu_perfil` 1, `fallback` 0, `vitoria` 0.

**A aritmética da catraca fecha exatamente:** `_FOLGA_ACIMA_DO_CATCH_ALL = 10` (`/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/src/hefesto_dualsense4unix/app/actions/profiles_actions.py:82`) e `PRIORIDADE_MAXIMA = 200` (`:77`). **191 = 1 + 10×19** — dezenove saves a partir do `meu_perfil` (prioridade 1) como base. O "200" do briefing é o vigésimo, batendo no teto do clamp em `profiles_actions.py:1634`. Daemon vivo agora (pid 1670).

---

## 1. O acervo mapeado — sprints e estudos que tocam perfis/prioridade/autoswitch/match/rascunho/rodapé/Steam Input

**Aviso de método que a própria casa escreveu, e que vale para a tabela inteira:**
`/mnt/.../docs/process/sprints/2026-07-30-INDICE-as-tres-faixas-depois-da-v040.md:14-21` — *"**O campo `Status:` dos documentos não é fonte.** 40 dizem ABERTA e só 3 dizem ENTREGUE"*; e `/mnt/.../2026-08-01-APLICAR-VERDADE-01-o-rodape-nao-mente-mais-a-ponte-ainda-mente.md:198-199` — *"Não confiar no campo `Status:` de nenhum documento citado aqui."*

### Núcleo de perfis (todos em `/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/docs/process/sprints/`)

| sprint | data | estado **declarado** | estado **verificado por mim no código** | resolve |
|---|---|---|---|---|
| `2026-07-25-ABAS-01-as-abas-brigam-pelo-mesmo-estado.md` | 25/07 | ENTREGUE (`d92b544`) | **MEIA** — ver §2.1 | as abas e o rodapé disputam um rascunho só |
| `2026-07-26-PERFIL-JOGO-01-as-configs-somem-ao-abrir-o-jogo.md` | 26/07 | ABERTA (E1,3,4,6 inteiras; E5 parcial) | não reverificado | a config dela some ao abrir o jogo |
| `2026-07-26-PERFIL-NASCE-CERTO-01-o-perfil-do-jogo-que-nunca-vence.md` | 26/07 | E1 entregue | **E1 [OK]** (`profiles_actions.py:1620-1634`, `:1005-1064`), **E4 [X]** (§2.4) | perfil do jogo nascia em 0 e nunca vencia |
| `2026-07-27-EMPATE-01-tres-perfis-empatados-e-quem-ganha-e-o-alfabeto.md` | 27/07 | ABERTA; índice de 30/07 diz "só a E2 falta" | **E1, E2, E3 TODAS [OK]** — doc desatualizado | empate decidido pelo alfabeto do glob |
| `2026-07-29-PERFIL-SALVA-TUDO-01-salvei-todas-as-abas-e-so-parte-ficou.md` | 29/07 | ABERTA, "nenhuma linha escrita" | **E1a [OK], E1b [X], E2 [OK], E3 [OK], E5 [OK]; E4/E6 abertas** | salvar não persiste tudo |
| `2026-07-30-AUTOMATISMO-MORTO-01-o-perfil-do-jogo-nunca-entra.md` | 30/07 | sem campo `Status:`; E0-E4 PROPOSTA | premissa (a) **caducou hoje** (§0) | o autoswitch está morto |
| `2026-08-01-ESCOLHA-DELA-VENCE-01-a-mascara-do-perfil-e-o-tooltip-do-xbox.md` | 01/08 | parcial (`1bec66c`) | E5 (`empatados[0]`) aberta **por decisão** (`profiles/manager.py:820-824`) | a máscara dela virava Xbox sozinha |

### Autoswitch, sinal de jogo e aplicação

| sprint | data | estado declarado | resolve |
|---|---|---|---|
| `2026-07-25-MODO-01-o-modo-jogo-liga-sozinho.md` | 25/07 | ABERTA no cabeçalho; **B2/B3 provadamente entregues** (índice de 25/07 `:101`; AUTOMATISMO-MORTO `:359-365`) | modo jogo nunca liga sozinho |
| `2026-07-28-JANELA-CEGA-01-o-detector-que-nunca-adoece.md` | 28/07 | PARCIALMENTE ENTREGUE | detector de janela cego é indistinguível de sadio |
| `2026-07-31-SINAL-DE-JOGO-01-...md` | 31/07 | ABERTA — medição e plano, MÉDIA | daemon desiste do jogo antes do jogo acabar |
| `2026-07-31-JANELA-FIEL-01-...md` | 31/07 | ABERTA; E1/E4 ALTA | a janela para de reconciliar e edita o perfil anterior |
| `2026-08-01-APLICAR-VERDADE-01-...md` | 01/08 | **ENTREGUE em código, com resto** (E2 pendente) | o rodapé anunciava "aplicado" com sete seções fora |

### Trava manual / posse (`manual_override_categories`)

| sprint | data | estado | verificado |
|---|---|---|---|
| `2026-08-03-AUDIO-QUE-TRANCA-01-...md` | 03/08 | aberta | **confirmado aberto** — nenhum caller limpa `"audio"` |
| `2026-08-03-POSSE-POR-CONTROLE-01-...md` | 03/08 | aberta | **confirmado** — `state_store.py:102` é `set[str]` global, sem chave por `uniq` |
| `2026-08-05-TRAVA-QUE-SOLTA-TARDE-01-...md` | 05/08 | CURADA | **cura presente porém NÃO COMMITADA** (staged; `git status` mostra `MM ipc_handlers.py`, `MM hotkey.py`, `AM test_trava_que_solta_tarde_01.py`) |

### Steam Input

`2026-07-25-AUTO-01` (25/07, ABERTA), `2026-07-26-STEAM-INPUT-01` (26/07), `2026-07-26-DUPLO-REGISTRO-01` (26/07, cura R-D aberta), `2026-07-26-BOTAO-QUE-NAO-MENTE-01` (26/07).

### Estudos indispensáveis

`estudos/2026-08-03-a-sessao-de-quatro-controles-e-o-que-o-journal-provou.md` (as quinze assimetrias), `estudos/2026-08-03-o-backlog-real-conferido-contra-o-codigo.md`, `estudos/2026-07-31-auditoria-geral-o-que-treze-agentes-mediram.md`, `estudos/2026-07-29-mapa-total-o-estudo-de-dezessete-agentes.md`.

### Achado de acervo

**A faixa de perfis está órfã de índice há cinco dias.** Contagem de menções a `PERFIL-SALVA-TUDO|AUTOMATISMO-MORTO|PERFIL-NASCE-CERTO|EMPATE-01|ABAS-01|PERFIL-JOGO-01`:

```
15  2026-07-30-INDICE-as-tres-faixas-depois-da-v040.md   <- último dono
 4  2026-07-31-INDICE-as-ondas-depois-da-auditoria.md
 0  2026-08-01-INDICE-o-controle-inteiro-no-jogo.md
 0  2026-08-03-INDICE-o-bluetooth-de-primeira-classe.md
 0  2026-08-03-ONDAS-a-ordem-de-execucao-da-leva-do-bluetooth.md
```

É a explicação estrutural de por que meias-entregas passaram: **ninguém estava olhando esta faixa.**

---

## 2. O item mais valioso — entregas declaradas que NÃO estão de pé

Padrão catalogado em `/mnt/.../docs/process/sprints/2026-08-03-ENTREGA-QUE-NAO-LIGOU-01-o-codigo-que-existe-e-ninguem-chama.md`: *"cada um destes commits escreveu um artefato, testou o artefato, e nunca testou o encontro dele com o resto do sistema."* **Achei um quarto caso, da mesma família — e é o DEFEITO A.**

### 2.1 ABAS-01 / PERFIL-SALVA-TUDO-01/E1b — `with_profile_identity` tem UM chamador, e não é o rodapé  ← DEFEITO A

**VERIFICADO por grep na árvore inteira:**

```
src/hefesto_dualsense4unix/app/draft_config.py:629      def with_profile_identity(...)   <- definição
src/hefesto_dualsense4unix/app/actions/profiles_actions.py:1719  self.draft = draft.with_profile_identity(profile)  <- ÚNICO chamador
tests/unit/test_perfil_salva_tudo_rascunho.py:275,287   <- testes
```

O único chamador vive em `_reconciliar_rascunho_com_perfil_salvo` (`profiles_actions.py:1675-1721`), acionado só por `on_profile_save` (aba Perfis, `:1247`). **O `on_save_profile` do rodapé (`footer_actions.py:342-368`) não chama.**

E o agravante que fecha o caso: **a docstring do próprio método descreve, textualmente, o caminho do rodapé como um dos dois defeitos que ele cura** — `draft_config.py:644-650`:

> *"rodapé 'Salvar Perfil' como 'MadJack' → `source_name` continuava apontando para o perfil anterior, então o SEGUNDO 'Salvar Perfil' com o mesmo 'MadJack' caía no ramo 'nome novo' de `to_profile` e zerava regra, prioridade e modo do perfil que ela acabara de criar."*

**A cura foi escrita para o rodapé e ligada só na aba Perfis.**

**Cadeia da catraca, verificada linha a linha:**

1. `footer_actions.py:344-347` → `prioridade = _prioridade_acima_dos_catch_all()` = `max(catch-all)+10`;
2. `footer_actions.py:350` → `save_profile(draft.to_profile(nome, priority=prioridade))`;
3. `draft_config.py:562-563` → `mesmo_perfil = source_name is not None and (name == source_name or mesmo_slug(...))` → **False**, porque `source_name` é velho;
4. `draft_config.py:573-574` e `:580-584` → `priority = prioridade` e **`match = MatchAny()`**;
5. `footer_actions.py:355-364` → grava `_active_profile_name`, `_draft_baseline`, e chama `refresh(select_name=nome)` → `_profiles_cache` é reescrito (`profiles_actions.py:1372`). **`draft.source_name` NÃO é atualizado.**
6. Próximo clique: passo 3 falha de novo, e o `max(catch-all)` do passo 1 agora **inclui o próprio perfil no valor anterior** → +10.

**Datação da causa, por `git log -S`:** o commit **`2bbfa22` (30/07 01:41)** landou a metade (a) da E1 (`mesmo_slug` em `draft_config.py`) **e**, no mesmo commit, ligou `_prioridade_acima_dos_catch_all` no rodapé. Antes dele o rodapé usava um default fixo — havia o bug de `MatchAny`, **mas não havia catraca**. As duas metades juntas é que produzem a escalada.

**Por que a suíte não pega:** `tests/unit/test_footer_salvar_nasce_acima_dos_catch_all.py:136-152` afirma o vínculo **lendo o texto-fonte** (`assert "_prioridade_acima_dos_catch_all" in texto`). Nenhum teste exercita **dois saves consecutivos pelo rodapé**. É a mordida na metade errada da cadeia, exatamente como a `ENTREGA-QUE-NÃO-LIGOU-01` descreve.

**O estrago real, medido no disco dela (não é a prioridade):** `sackboy_nativo` era regra por critério e virou catch-all. Como `_chave_de_selecao` = `(not e_catch_all, priority)` (`profiles/manager.py:772-779`), **perder a regra é o que dói** — a prioridade 191 nem é lida pelo veto R-21. Com 191 ele é o catch-all mais alto e **ganha toda janela de desktop**, carregando `suppress_desktop_emulation=true`.

**E aí abre a armadilha de mão única, verificada:** `daemon/lifecycle.py:1564-1567` liga a supressão **sem** o gate `_perfil_tem_opiniao`; só o ramo de LIBERAR tem o gate (`:1574`). Ou seja: **o catch-all liga a supressão de mouse/teclado e nenhum outro catch-all consegue desligar.** As duas fontes já avisavam: `draft_config.py:704-712` e o veto 3 da `PERFIL-SALVA-TUDO-01`. **A catraca produziu exatamente a configuração que dois documentos proibiram por escrito.**

### 2.2 SALVAR-NAO-REBAIXA-01 — as guardas desligam em "Novo perfil"  ← DEFEITO B

**VERIFICADO:** `on_profile_new` (`profiles_actions.py:955`) chama `_esquecer_a_fotografia_do_editor()` (`:1607-1618`), que zera `_regra_do_disco`, `_prioridade_do_disco`, `_assinatura_da_regra_ao_abrir`, `_prioridade_ao_abrir`. Em `_build_profile_from_editor` (`:1878-1886`), as duas guardas são `if <do_disco> is not None and not <foi_mexida>()` — com `None`, **ambas são puladas** e os widgets vencem.

**Dois detalhes que verifiquei e que mudam o desenho da cura:**

- `confirm_downgrade_match_to_any` (`profiles_actions.py:1196-1203`) só dispara se `original.match` for `MatchCriteria`. **Os perfis dela hoje são `MatchAny` → o diálogo NÃO dispara.** A rede está furada justamente no estado atual do disco;
- **não existe diálogo nenhum para rebaixamento de PRIORIDADE.** Só há `prompt_overwrite_existing` e `confirm_downgrade_match_to_any` em `app/gui_dialogs.py`.

### 2.3 Queixa 3 — "ativar perfil não aplica imediatamente as abas": mecanismo verificado

`on_profile_activate` (`profiles_actions.py:1099-1112`) faz `profile.switch` + toast + destaque de linha + `_sync_selection_with_active_profile`. **Não chama `_refresh_all_tabs`.** Os dois únicos chamadores em produção são `app/app.py:796` (bootstrap/reconciliação) e `footer_actions.py:549` (Restaurar Padrão).

Portanto as abas só seguem o perfil ativado pelo tique de 2 Hz — `_reconciliar_draft_com_perfil_ativo` (`app/app.py:827-874`, ligado em `:433`). **E esse caminho tem um portão:** `if self._tem_edicao_pendente(): <toast>; return` (`:866-873`). **Com qualquer edição pendente, as abas nunca acompanham** — ela só recebe um aviso de texto. É a queixa dela, verbatim, e é a `JANELA-FIEL-01/E1` vista de outro ângulo.

### 2.4 PERFIL-NASCE-CERTO-01/E4 — o detector da armadilha não existe

**VERIFICADO:** nada em `src/` nem em `scripts/doctor.sh` detecta "catch-all com prioridade ≥ a de um perfil específico". **É por isso que dezenove saves passaram sem um único aviso.**

### 2.5 STEAM-INPUT / BOTÃO-QUE-NÃO-MENTE-01 — desfazer sem superfície na janela

`remove_appid_from_steam_input_allowlist` tem chamador **só na CLI** (`cli/cmd_steam.py:206,215`). Zero na GUI. A entrega 4 (tornar o tooltip honesto) **foi feita** — `src/hefesto_dualsense4unix/gui/main.glade:2431-2443`. **Mas o comentário desse mesmo bloco ficou velho:** ele afirma *"nem na linha de comando"*, e o caminho de CLI existe hoje.

### 2.6 EMPATE-01 — o documento mente ao contrário

**VERIFICADO entregue, apesar de o índice de 30/07 (`:169-177`, `:354-356`) dizer que a E2 continua aberta:** E1 (log `profile_select_empate_resolvido`, `manager.py:836-845`), E2 (`profiles_actions.py:179-280`, `vencedor_da_disputa` com dois chamadores; entrou em `cd5eaf1`, 31/07 09:47), E3 (desempate por incumbente, `manager.py:828-835`). **Confiar no cabeçalho aqui custaria retrabalho.**

### 2.7 PERFIL-SALVA-TUDO-01 — declarada "nenhuma linha escrita", com quatro entregas de pé

**VERIFICADO:** E1a [OK] (`draft_config.py:562`), E2 [OK] (`priority: int = 5` não existe mais em `src/`), E3 [OK] (`emulation_actions.py:438` → `with_suppress`; `home_actions.py:598` → `with_mode`), E5 [OK] (campos mortos removidos). **E1b [X] — e é o DEFEITO A.** E4 (mic) e E6 (painel) seguem abertas.

---

## 3. Sobreposição e conflito

### 3.1 `manual_override_categories` — três sprints, e elas NÃO se contradizem: empilham

Verificado em `src/hefesto_dualsense4unix/daemon/state_store.py:102,220-253,425-445`:

- é um **`set[str]` global** com quatro categorias (`trigger`|`led`|`rumble`|`audio`);
- armado por `ipc_handlers.py:451,673,769,817,2649,2666`;
- limpo **por inteiro** por `profile.switch` (`:430`), hotkey (`hotkey.py:168`) e a exceção F2 do autoswitch (`profiles/autoswitch.py:508`);
- limpo **por categoria** só para `"trigger"` (`:710`) e `"rumble"` (`:2705`).

| sprint | o que propõe | veredito |
|---|---|---|
| `AUDIO-QUE-TRANCA-01` (03/08) | **nenhum caller limpa `"audio"`** → um toque no volume congela a troca de perfil para sempre | **VERIFIQUEI: aberto.** É o menor e o mais barato |
| `POSSE-POR-CONTROLE-01` (03/08) | o conjunto é global → a trava de um controle congela os quatro; propõe escopo **por `uniq`** | **VERIFIQUEI: aberto.** É o maior — muda a estrutura de dados |
| `TRAVA-QUE-SOLTA-TARDE-01` (05/08) | a trava era limpa **depois** do `activate`; sobe o clear e o lock para antes, com restauro em `except` | **CURADO, não commitado** (`ipc_handlers.py:426-452`, `hotkey.py:166-187`) |

**Quem vence, e por quê:** não há incompatibilidade — há **ordem obrigatória**.

1. **`TRAVA-QUE-SOLTA-TARDE-01` primeiro**, porque já está pronta e as outras duas passam a rodar sobre o código dela. **E o primeiro passo é commitá-la** — enquanto ficar só no índice, qualquer `git stash`/checkout a perde;
2. **`AUDIO-QUE-TRANCA-01` depois** — é uma categoria a mais nos clears já existentes, sobre a ordem já corrigida;
3. **`POSSE-POR-CONTROLE-01` por último** — trocar `set[str]` por mapa `uniq → set[str]` reescreve os dez callsites. Fazê-la antes obrigaria a refazer as outras duas.

### 3.2 Prioridade × regra — o conflito de diagnóstico, e ele é sério

**`AUTOMATISMO-MORTO-01:348-357` refuta explicitamente que prioridade seja a causa:** `_chave_de_selecao` põe especificidade antes de prioridade e o veto R-21 nem lê `priority` — *"subir `Pragmata2` de 5 para 200 não muda absolutamente nada dentro do jogo"*. **Confirmei no código** (`profiles/manager.py:772-779`).

**Consequência direta para o trabalho de agora:** quem for consertar o DEFEITO A vai olhar para o 191 e querer corrigir o número. **O número é sintoma.** O que quebrou o jogo foi `criteria → any`. Uma cura que normalize prioridades e deixe o `match` catch-all não conserta nada.

### 3.3 `JANELA-FIEL-01/E4` × `AUTOMATISMO-MORTO-01/E3` — mesmo arquivo, mesma linha

As duas mexem no `MatchAny()` do rodapé (`draft_config.py`, ramo de nome novo) por motivos diferentes: E4 quer que o rodapé compare por **slug**; E3 quer que ele **nasça com a regra do jogo em foco**. **São complementares**, mas se forem executadas em levas separadas a segunda vai reescrever a primeira. Devem entrar juntas.

---

## 4. As assimetrias que PARECEM defeito e NÃO são — as que tocam perfis/autoswitch

Fonte: `/mnt/.../docs/process/sprints/2026-08-03-INDICE-o-bluetooth-de-primeira-classe.md:230-255`, detalhe em `estudos/2026-08-03-a-sessao-de-quatro-controles-e-o-que-o-journal-provou.md`. `COOP-QUE-NÃO-DESMONTA-01:259` manda explicitamente: *"Não mexer nas quinze assimetrias intencionais."*

As quatro que ficam no caminho de quem corrigir agora:

1. **Debounce assimétrico: 0,5 s para ENTRAR, 12 s para SAIR.** `profiles/autoswitch.py:41-58` (`DEFAULT_DEBOUNCE_SEC = 0.5`, `DEFAULT_DEBOUNCE_SAIDA_SEC = 12.0`). Com 0,5 s dos dois lados o journal mostrava troca de perfil a cada 18-28 s no meio do jogo. **Não simetrizar.**
   *Observação minha, e ela importa para a queixa 2:* `_saida_para_catch_all` (`:379-395`) exige `self._current_especifico`. **Com o perfil corrente sendo catch-all — o caso dela hoje — o lado lento nunca engata, e tudo roda em 0,5 s.** Isto não é a assimetria falhando; é consequência de o perfil ter virado catch-all. Some com o DEFEITO A.
2. **`apply_output_defaults` ignora o seletor da GUI.** `profiles/manager.py:248-255`. É broadcast REAL de propósito: sem isso, ativar perfil com um alvo escolhido aplicava só no alvo. **Não "consertar" para respeitar o seletor.**
3. **`_suppress_leds` nasce `True` e a escrita de LED da pydualsense é suprimida no BT** (`LIGHTBAR-BT-ADOPT-01`/`NEVER-01`).
4. **A camada do co-op fica FORA de `_desired_by_uniq`**; **o rumble não entra em `_desired`** — as duas pagas com defeito medido atrás.

E duas que não estão nas quinze mas se comportam como armadilha na mesma altura:

5. **O veto R-21 é decisão, não bug** (`profiles/manager.py:38-62`, `:620-628`). `AUTOMATISMO-MORTO-01:614-655` lista sete "não faça", e revogar/afrouxar R-21 é o item 4: sem ele volta o ping-pong de 18-28 s. **O caminho certo é dar regra própria ao perfil, não afrouxar o veto.**
6. **`ESCOLHA-DELA-VENCE-01/E5` (`vencedor = empatados[0]`) fica aberta por decisão declarada** — `profiles/manager.py:820-824`: *"mudar isso mudaria comportamento já validado sem que ninguém tenha pedido."* Não é dívida a pagar de passagem.

---

## 5. Plano de consolidação

### Fase 0 — antes de escrever qualquer linha (bloqueante, não paralelizável)

| # | ação | dependência **provada** |
|---|---|---|
| 0.1 | **Commitar `TRAVA-QUE-SOLTA-TARDE-01`.** Está só no índice (`git status`: `MM ipc_handlers.py`, `MM hotkey.py`, `AM test_...py`) | `git status` |
| 0.2 | **Decidir com ela o que fazer com `sackboy_nativo.json`.** É o perfil ATIVO agora, catch-all/191/`suppress=true`. `AUTOMATISMO-MORTO-01:614-655` item 7: **não escrever nos `.json` dela sem autorização explícita** — *"inclusive 'só para normalizar'"* | disco + sprint |
| 0.3 | **Registrar por nota datada que o cadeado caiu.** `AUTOMATISMO-MORTO-01` lado (a) e a nota de 03/08 estão factualmente vencidas; a regra da casa é nota datada, não apagamento | medição de §0 |

### Fase 1 — parar a hemorragia (uma linha e um teste)

| # | entrega | onde | por quê primeiro |
|---|---|---|---|
| 1.1 | **DEFEITO A: `_on_saved` do rodapé reaponta o rascunho.** `self.draft = draft.with_profile_identity(perfil_gravado)` e `_draft_baseline = self.draft` | `footer_actions.py:352-368` | é a `PERFIL-SALVA-TUDO-01/E1b`, já escrita e já testada em unidade — falta **ligar**. Enquanto não ligar, todo save do rodapé destrói mais um perfil |
| 1.2 | **A mordida que faltava.** Um teste de **DOIS saves consecutivos pelo rodapé com o mesmo nome**: o segundo tem de preservar `match` e `priority` | `tests/unit/` | `test_footer_salvar_nasce_acima_dos_catch_all.py:136-152` afirma por texto-fonte. Sem esta, a cura de 1.1 é indistinguível de não-cura |

**Nota de desenho:** `to_profile` precisa devolver/expor o `Profile` gravado ao callback — hoje `_save()` devolve o `Path`. É a única mudança de forma.

### Fase 2 — as três queixas (dependência provada, não suposta)

| # | entrega | queixa | depende de | pode ir em paralelo? |
|---|---|---|---|---|
| 2.1 | **DEFEITO B**: `on_profile_new` marca "perfil novo" **sem** apagar a fotografia — ou o save por cima de arquivo existente reabilita as guardas; e **diálogo para rebaixamento de PRIORIDADE**, que não existe | catraca / rebaixamento | independente de 1.1 | **sim**, com 2.2 e 2.3 |
| 2.2 | **`on_profile_activate` refaz as abas na hora** — chamar `_bootstrap_draft_async` direto em vez de esperar o tique; e decidir com ela o que o gate `_tem_edicao_pendente` faz num gesto **explícito** dela | *"ativar perfil não aplica as abas"* | é a `JANELA-FIEL-01/E1` (prazo no latch), que **precisa vir junto** | sim |
| 2.3 | **`AUDIO-QUE-TRANCA-01`**: `clear_manual_trigger_active("audio")` nos caminhos que já limpam `"trigger"`/`"rumble"` | *"perfis se alteram durante a partida"* | **0.1** (a ordem do clear tem de estar commitada) | sim |
| 2.4 | **`AUTOMATISMO-MORTO-01/E2` — um clique: "usar este perfil sempre neste jogo"** | *"Steam Input não respeita decisão por jogo"* + a raiz das três | **1.1 e 2.1** — sem elas, o perfil promovido volta a catch-all no save seguinte | **não** |

**Por que 2.4 é a cura da queixa 1, e não uma sprint de Steam Input:** com os perfis dela catch-all, o predicado `perfil_e_regra_de_jogo` (`profiles/schema.py:658`) nunca casa; a exceção F2 do autoswitch (`profiles/autoswitch.py:505-518`) nunca cede; e o veto R-21 esvazia o candidato dentro do jogo. **As três queixas dela têm a mesma raiz: perder a regra.** É por isso que 2.4 fecha as três, e é por isso que ela tem de vir **depois** de 1.1.

### Fase 3 — o portão que impede a recaída

| # | entrega | modelo |
|---|---|---|
| 3.1 | **`PERFIL-NASCE-CERTO-01/E4`**: detector de "catch-all com prioridade ≥ à de um perfil específico", na aba Perfis e no `doctor.sh` | não existe hoje (verificado) |
| 3.2 | **`ENTREGA-QUE-NÃO-LIGOU-01/E5`**: portão de símbolo público em `src/` sem referência em `src/`, **com lista de dispensas nomeada e datada** | a sprint exige o aceite: *"rodado contra a árvore de 02/08, acha o `forward_jack` e o `DESLIGADO_OFICIAL`"* — **acrescente `with_profile_identity` ao aceite**, porque ele tem chamador e ainda assim está meio-ligado. Um portão de "zero chamadores" não teria pego o DEFEITO A |

### Fase 4 — higiene documental (barata, paralela a tudo)

Notas datadas em `EMPATE-01` (E1/E2/E3 entregues), `PERFIL-SALVA-TUDO-01` (quatro de seis entregues), `MODO-01` (B2/B3 entregues), `AUTOMATISMO-MORTO-01` (cadeado caiu), e no comentário de `main.glade:2436-2438` (o desfazer existe na CLI hoje). **Um índice novo para a faixa de perfis** — ela está órfã desde 30/07.

### O que NÃO deve ser tocado

1. **Os `.json` dela, sem palavra explícita** (`AUTOMATISMO-MORTO-01:614-655`, item 7) — inclusive `sackboy_nativo.json`, e inclusive "só para normalizar";
2. **O veto R-21** (`profiles/manager.py:620-628`) — nem revogar, nem afrouxar;
3. **`perfil_e_regra_de_jogo` para aceitar regex de título** (`schema.py`) — o segundo consumidor é o furo da trava manual em `_activate`;
4. **O debounce assimétrico 0,5/12 s** e as demais quinze assimetrias intencionais;
5. **`apply_output_defaults` ignorando o seletor** — é a cura do `PERFIL-01`;
6. **`vencedor = empatados[0]`** (`manager.py:829`) — aberto por decisão declarada;
7. **`window_detect_healthy` decaindo** — a `JANELA-CEGA-01:123-148` mede o custo: vaivém na cor do controle dela;
8. **A ordem `apply` → `clear` da trava** — acabou de ser invertida corretamente; reverter reabre `TRAVA-QUE-SOLTA-TARDE-01`;
9. **A estrutura global de `manual_override_categories`** *antes* de 0.1 e 2.3 estarem de pé;
10. **`_esquecer_a_fotografia_do_editor` não deve simplesmente sumir** — ela existe porque perfil novo não tem valor de disco a preservar. A cura do DEFEITO B é de **escopo**, não de remoção.

---

## O que eu NÃO verifiquei

- **Não rodei a suíte, o `ruff`, o `mypy` nem a janela.** Tudo é leitura de código, de disco e de `git`;
- **Não li o journal do daemon**; as medições ao vivo são de arquivos de estado e `pgrep`;
- **Não reverifiquei** `PERFIL-JOGO-01` (seis entregas) nem `JANELA-CEGA-01`/`SINAL-DE-JOGO-01` contra o código — para essas, repasso o que os documentos dizem;
- **A leitura documental do trio Steam Input** (`AUTO-01`, `STEAM-INPUT-01`, `DUPLO-REGISTRO-01`, `BOTÃO-QUE-NÃO-MENTE-01`) **ainda está em voo** num subagente; o que afirmo dessa área acima (`remove_appid_from_steam_input_allowlist` só na CLI, tooltip já honesto, comentário do glade velho) **verifiquei eu mesmo por grep**;
- **O "200" do briefing não está no disco hoje** — só o 191. Não sei se foi corrigido ou se estava noutro lugar; vale confirmar com ela antes de qualquer conta.

## PENULTIMO BLOCO

Agent 2 (autoswitch/aplicação) is still running; I verified those code paths myself. Report below.

---

# LEVA 1 — ENTENDER: o acervo de perfis contra o código de hoje

**Método:** 113 sprints + 22 estudos varridos; toda afirmação marcada como **[MEDIDO]** foi conferida por mim no código ou no disco dela hoje (05/08), **[DOC]** foi lido em documento e não conferido.

---

## 0. O que mudou desde o seu briefing — MEDIÇÃO AO VIVO, e é urgente

Li o disco dela agora. **Dois dados derrubam premissas do briefing:**

**(a) O cadeado do autoswitch SUMIU.** `~/.config/hefesto-dualsense4unix/` tem só `gamepad_emulation.flag`, `keyboard_emulation.flag`, `mouse_emulation.flag`. **Não há `autoswitch_locked.flag`.** [MEDIDO] O lado (a) da `AUTOMATISMO-MORTO-01` (30/07) **caducou**: o autoswitch está VIVO. É exatamente o instante que a `EMPATE-01` (27/07, l.174-176) avisou: *"Assim que o autoswitch voltar a aplicar perfil sozinho, é o alfabeto que decide"*.

**(b) O estrago da catraca já está em disco, e é pior que "prioridade errada".** [MEDIDO]

| perfil | prio | match | suppress | mode | mtime |
|---|---|---|---|---|---|
| **sackboy_nativo** | **191** | **any** | **true** | gamepad/dualsense/coop | **05/08 02:51** |
| vitoria | 0 | any | false | gamepad/dualsense/coop | 05/08 02:49 |
| meu_perfil | 1 | any | false | null | 05/08 02:43 |
| fallback | 0 | any | false | null | 05/08 02:43 |
| Pragmata | 5 | any | false | null | 27/07 23:00 |
| Pragmata2 | 85 | criteria `steam_app_3357650` | — | — | — |
| coop_local 75 · Aventura 70 · Ação 65 · FPS/point_and_click 60 · Corrida/Esportes 55 · Navegação 50 · bow 10 | | criteria | | | |

**Não achei o 200** — só o 191. E **191 = 1 + 10×19**: a base é `meu_perfil` (prioridade 1) e a catraca rodou **19 vezes** sobre o mesmo arquivo. A aritmética fecha exatamente com `_FOLGA_ACIMA_DO_CATCH_ALL = 10` (`profiles_actions.py:82`). [MEDIDO]

**A consequência grave, e ninguém a listou ainda:**

`sackboy_nativo` era um perfil `criteria` (a `PERFIL-SALVA-TUDO-01` o cita como *"um dos dois com `suppress: true` que casam por criteria"*). A catraca o transformou em **catch-all de prioridade 191 — o maior catch-all do disco**, e ele carrega `suppress_desktop_emulation: true`. Como `_chave_de_selecao` é `(not e_catch_all, priority)` (`profiles/manager.py:779`), ele vence **todo** outro catch-all em **toda** janela de desktop. [MEDIDO]

E em `daemon/lifecycle.py:1564-1578` o ramo `if desired:` **não tem** o gate `_perfil_tem_opiniao` — só o ramo de LIBERAR tem (l.1574, R-02). Ou seja: [MEDIDO]

> **`sackboy_nativo` LIGA a supressão de mouse e teclado a cada ativação, e nenhum dos outros catch-all dela consegue desligá-la** (o R-02 barra o ramo de liberar para catch-all). **É uma trava de mão única, ativa agora.**

Isto é literalmente o veto nº 3 da `PERFIL-SALVA-TUDO-01` (*"NÃO persistir `suppress_desktop_emulation: true` em catch-all sem o gate R-02"*) e o aviso da docstring de `draft_config.py:704-712`. **A catraca produziu a configuração que dois documentos proibiam por escrito.**

---

## 1. O acervo — tudo que toca perfis / prioridade / autoswitch / match / rascunho / rodapé / Steam Input

**Aviso de método, e vem da própria casa:** o `INDICE-as-tres-faixas` (30/07, l.14-21) mede: *"**O campo `Status:` dos documentos não é fonte.** 48 arquivos têm o campo, **40 dizem ABERTA** e só 3 dizem ENTREGUE"* — várias das que dizem ABERTA estão fechadas. Por isso a coluna abaixo separa **declarado** de **real (grep)**.

### Núcleo de perfis

| sprint | data | declarado | real [MEDIDO] | resolve |
|---|---|---|---|---|
| `ABAS-01 as abas brigam pelo mesmo estado` | 25/07 | ABERTA | **meia**: `with_profile_identity` existe e tem **1 chamador só** | cada aba se reconstrói de fonte diferente e clicar entre abas perde config |
| `PERFIL-JOGO-01 as configs somem ao abrir o jogo` | 26/07 | ABERTA, E1 obrigatória antes de código | aberta | portão da camada de jogo só filtra `led`; `player_leds` do Steam atravessa |
| `PERFIL-NASCE-CERTO-01 o perfil do jogo que nunca vence` | 26/07 | ABERTA | **E1 [OK], E2-teto [OK] (200), E4 [X]** | perfil de jogo nascia catch-all prio 0 e nunca vencia |
| `EMPATE-01 três perfis empatados` | 27/07 | PARCIAL, "E2 aberta em 31/07" | **E1 [OK] E2 [OK] E3 [OK] E-1 [OK]** — doc desatualizado | catch-all empatados desempatados pelo alfabeto |
| `PERFIL-SALVA-TUDO-01 salvei todas as abas` | 29/07 | **"ABERTA — nenhuma linha de código escrita"** | **E1a [OK] E1b [X] E2 [OK] E3 [OK] E5a [OK] / E4 [X] E6 [X]** | Salvar Perfil reescreve como fábrica as 5 seções que o rascunho só transporta |
| `AUTOMATISMO-MORTO-01 o perfil do jogo nunca entra` | 30/07 | ABERTA | **lado (a) caducou hoje** (flag sumiu) | autoswitch morto na máquina dela |
| `ESCOLHA-DELA-VENCE-01 a máscara do perfil` | 01/08 | E1,E4 ENTREGUES; E2,E3,E5 abertas | confere | máscara escolhida vira Xbox / não sobrevive ao reboot |
| `APLICAR-VERDADE-01 o rodapé não mente mais` | 01/08 | parcial | **[OK] de pé** (`failed`, *"Aplicado, menos:"*) | "Aplicar" dizia ok sem aplicar |
| `JANELA-FIEL-01 a janela para de reconciliar` | 31/07 | — | **E1 [OK] E3 [OK]** (`DRAFT_RELOAD_INFLIGHT_TIMEOUT_S`, `EstadoIndisponivelError`) | latch preso matava a reconciliação em silêncio |

### Autoswitch / match / aplicação

`MODO-01` (25/07) · `JANELA-CEGA-01` (28/07) · `SINAL-DE-JOGO-01` (31/07) · `EMULAÇÃO-NO-JOGO-01` (29/07) · `PAINEL-DA-VERDADE-01` (01/08) — a leitura detalhada deste bloco está no agente ainda em voo; conferi no código os pontos que o plano usa (debounce, R-21, reconciliação de 2 Hz).

### Steam Input

| sprint | data | declarado | real [MEDIDO] |
|---|---|---|---|
| `STEAM-INPUT-01 ela nunca mais precisa decidir` | 26/07 | ABERTA | E7 **caducou** (timer vivo); **E6 entregue** (`steam_input_vpad_suspenso` tem consumidores em `lifecycle.py:1762`, `status_actions.py:175`); **E3 [X] na janela** |
| `DUPLO-REGISTRO-01 dois cadastros` | 26/07 | ABERTA | **intocada**; remendo de 26/07 é o que segura |
| `BOTÃO-QUE-NÃO-MENTE-01` | 26/07 | 1-4 entregues
