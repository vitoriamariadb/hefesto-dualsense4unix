# agente a3967f0c37b817e56 (sessao 9147457e)

**tipo:** general-purpose
**tarefa:** Teste real do campo que não nascia
**profundidade:** 1  **pai:** -

## O QUE FOI PEDIDO

No repositório /mnt/Apate/Desenvolvimento/hefesto-dualsense4unix, escreva o TESTE que morde uma cura que acabei de aplicar. NÃO altere a cura; só escreva teste (e conserte a cura apenas se o teste provar que ela está errada).

O DEFEITO, relatado por ela em 05/08/2026: *"1599660 quando eu clico em jogo da steam não aparece nenhum campo pra digitar"*.

MECANISMO (já diagnosticado, confirme antes de escrever):
- `src/hefesto_dualsense4unix/gui/main.glade`, por volta da linha 2071: o `GtkBox id="profile_game_entry_box"` tem `visible=False` E `no-show-all=True`. Seus dois filhos são um `GtkLabel` ("Nome do jogo:") e o `GtkEntry id="profile_simple_custom_name"`.
- Doutrina do GTK: `no_show_all` faz o `show_all()` da janela IGNORAR o widget — e, por não descer nele, os FILHOS nunca são mostrados.
- `src/hefesto_dualsense4unix/app/actions/profiles_actions.py`, em `_on_aplica_a_changed`: o código fazia `box.show()`, que revela a CAIXA e mais nada. Ela via um vão vazio.
- A CURA aplicada: `box.set_no_show_all(False)` seguido de `box.show_all()`.

POR QUE A SUÍTE NÃO PEGOU — e isto é o coração do teste novo:
Todos os testes existentes usam um dublê `_FakeBox()` com atributo `.visivel`, e afirmam só `ed._get("profile_game_entry_box").visivel` (ver `tests/unit/test_r12_editor_simples_gui.py:213,231`, e os dublês em `test_salvar_nao_rebaixa_02_...py:210`, `test_modo01_...py:925`, `test_empate01_...py:210`, `test_gui_perfil_manual_editor.py:157`, `test_profiles_gui_sync.py:368`). **O dublê responde "estou visível" e ninguém pergunta pelos FILHOS.** Um teste novo com dublê não serve para nada aqui — ele tem de usar GTK REAL e o glade REAL.

O QUE ESCREVER — arquivo novo `tests/unit/test_campo_que_nao_nascia_01_o_jogo_da_steam_sem_onde_digitar.py`:
1. Guarda `exigir_gi_real` (`tests/conftest.py`, por volta de :204-238) — o padrão da casa; veja o uso em `tests/unit/test_launch_wrapper_dialog.py` (classe `TestDialogoGtkReal`) e em `tests/unit/test_switch_sem_icone_quebrado.py`.
2. Carregue o glade REAL com `Gtk.Builder`, pegue `profile_game_entry_box` e `profile_simple_custom_name`. Reparente o que for preciso para uma `Gtk.OffscreenWindow` (armadilha medida em `docs/process/COMO-OLHAR-A-TELA.md:142-149`: sob Xvfb não há gerenciador de janelas e uma `Gtk.Window` de verdade fica 1x1 para sempre) e chame `show_all()` na janela — reproduzindo o que a janela real faz na abertura.
3. **O teste central:** depois de aplicar o mesmo par de chamadas da cura (`set_no_show_all(False)`

## RESULTADO FINAL

Done. Report:

**Casos escritos** — `/mnt/Apate/Desenvolvimento/hefesto-dualsense4unix/tests/unit/test_campo_que_nao_nascia_01_o_jogo_da_steam_sem_onde_digitar.py`, 8 casos com GTK real + `main.glade` real numa `Gtk.OffscreenWindow` (aba Perfis à frente, laço drenado 8x): premissa do `no-show-all`; contraprova `box.show()`; `show_all()` com o `no_show_all` ainda armado (prova que a ORDEM da cura importa); o par da cura medido no ENTRY; e quatro pelo handler REAL `_on_aplica_a_changed` via `SegmentedSelector` de verdade (steam_game, dica do appid, game, e o ciclo mostra→esconde→mostra).

**Com a cura arrancada (`box.show()`) reprovaram 4** — `test_jogo_da_steam_faz_o_campo_nascer_pelo_handler_real`, `test_jogo_da_steam_traz_a_dica_do_appid_junto_com_o_campo`, `test_jogo_especifico_tambem_faz_o_campo_nascer`, `test_qualquer_esconde_o_campo_e_jogo_da_steam_o_traz_de_volta`. Os outros 4 são doutrina do GTK/glade (itens 1-4 do pedido) e por construção não dependem do nosso código: são eles que autorizam as asserções sobre o entry. Confirmei também a lacuna: com o defeito de volta, os 49 testes de dublê existentes seguem VERDES.

**A cura estava certa, mas quebrava a árvore.** A suíte completa acusava 22 reprovações minhas-não-são: `AttributeError: '_FakeBox' object has no attribute 'set_no_show_all'/'show_all'` — seis dublês que se dizem `Gtk.Box` sem duas APIs do `Gtk.Box`. Não toquei em `profiles_actions.py` (o hunk está byte a byte como você aplicou); ensinei a doutrina aos dublês em `test_r12_editor_simples_gui.py`, `test_empate01_*`, `test_salvar_nao_rebaixa_02_*`, `test_modo01_*`, `test_profiles_gui_sync.py` e `test_gui_perfil_manual_editor.py`: nascem com `no_show_all=True` como o glade, `show()` para na caixa, e só `show_all()` desarmado marca `filhos_visiveis`.

**Portões** — `pytest 7055 passed, 1 skipped` (7047 + 8 novos), `ruff check src/ tests/` limpo, `mypy` sem problemas em 163 arquivos, `validar-acentuacao.py --all` e `validar-glifos.py --all` exit 0. Rodei `git add -A` (que também pegou arquivos de outro trabalho em curso na árvore — `cmd_steam.py`, `daemon_actions.py` etc., não são meus). Nada commitado.
