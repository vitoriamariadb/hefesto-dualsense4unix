# HANDOFF 2026-07-19 — estado da onda v1 (co-op 4P misto → produção)

> Documento de passagem entre sessões. Se você é um Claude novo assumindo: LEIA a memória do
> projeto (carregada automaticamente via MEMORY.md) e ESTE arquivo antes de tocar em qualquer coisa.
> Ultracode ON: use o Workflow tool para tarefas substantivas; materialize estudos+memória sempre.

## Onde estamos (uma frase)
Duas ondas grandes ("fim da guerra de escritores" + "plataforma") + os 8 fixes da auditoria estão
APLICADOS NO WORKING TREE (NÃO commitado). Suíte verde. Falta: implementar a leva final
(broker + IMU/touchpad + gyro do DualSense) → auditar → commitar (anônimo) → install SEM FLAGS →
validar ao vivo com os 4 controles.

## Estado técnico exato
- Branch: `sprint/harmonia-uhid`. HEAD = 62483b2. **Working tree tem ~4500 linhas NÃO commitadas**
  (60 arquivos: 58 M, 2 D, 45 untracked). NÃO descarte, NÃO faça reset. É trabalho válido.
- Gate: `pytest tests/` = **3576 passed, 0 failed, 0 skipped**; `ruff check src/ tests/` (pin
  0.15.20 no .venv) limpo; `mypy src/hefesto_dualsense4unix` limpo (145 arquivos).
  check_anonymity.sh é gate duro). NÃO commite antes de pedido/gate — regra do projeto.

## O que já está no working tree (NÃO refazer)
1. **Onda "fim da guerra"** (estudo `docs/process/estudos/2026-07-18-estudo-guerra-de-escritores-hidraw.md`,
   sprints `2026-07-18-sprint-{nucleo,externos-e-gui,infra-kernel-install}.md`):
   - `PROTON_DISABLE_HIDRAW=0x054C/0x0CE6` no lugar de `PROTON_ENABLE_HIDRAW` (morta no Proton 10);
     vpad 0df2 NUNCA na lista. Keepalive neutro (para de zerar rumble de terceiros). Report BT 0x31
     correto (novo `core/ds_output_report.py`, CRC32). Cache no `SysfsLedNode.set_rgb` (fim do flash
     azul de 30s). Replicação do output do jogo→físico (triggers/lightbar/player-LED) — numeração
     dos DualSense passa a ser a do jogo. Externos com identidade persistente (fim do bombardeio que
     matava o 8BitDo). wrapper_used honesto. GUI temada + segmented de modo. udev: vpad na 70, js
     Motion escondido (80), 73/74 removidas. policy=max→1.0. autoswitch GUI-neutra.
2. **Onda "plataforma"** (estudos `2026-07-18-estudo-{kernel-hardening,bt-maximo}.md`, sprint
   `2026-07-18-sprint-plataforma-proton-usb-bt.md`):
   - Proton PINADO (GE-Proton10-34, sha256; tarball já em `~/.cache/hefesto-dualsense4unix/proton/`;
     `integrations/proton_pin.py`; trava jogos via CompatToolMapping). USB power (regras 81 devices+
     hosts xHCI). cmdline via kernelstub com MERGE do token único usbcore.quirks
     (`integrations/kernel_cmdline.py`). btusb autosuspend=0 + FastConnectable idempotente sem
     restart do bluetoothd. kernel-watch (storm_watch evoluído: -71 + joycon subcmd + BT CRC).
     doctor "Energia USB e Rádio". Game Mode no wrapper. Paridade de empacotamento (deb/fedora/
     flatpak/arch) corrigida.
3. **8 fixes da auditoria adversarial** (todos com teste de regressão falha-sem/passa-com):
   #1 numeração DualSense-depois-de-externo (identity reserve provider); #8 camada GAME vaza no
   BT-drop; #4 botão Travar Proton; #6 vdf não-UTF-8; #5 lock antes do estado; #7 unlock bloco vazio;
   #2 wrapper_used janela 120→900s; #3 externo sem MAC GUI×LED.

## O que FALTA (a leva final — próximo trabalho)
Sprint `docs/process/sprints/2026-07-19-sprint-gyro-universal-vpad.md` + specs em
`proximas_ondas_specs_20260718` (memória) e estudos `2026-07-18-estudo-broker-hide-hidraw.md`,
`2026-07-18-estudo-imu-touchpad-vpad.md`, `2026-07-19-estudo-gyro-universal-vpad.md`.

1. **GYRO-01 (P0, DualSense só):** `forward_motion` — `PhysicalReportReader` (thread) lê hidraw cru
   do físico (0x01 USB / 0x31 BT+CRC), copia janela `raw[base+15:base+40]` (gyro/accel/timestamp/
   touch) → `vpad.forward_motion()`; reader é o relógio (não os 60Hz), throttle 250-500Hz; carimbar
   feature 0x05 do físico no blueprint do vpad (senão unidades≠P1 branca drivam). Hook points no
   estudo (uhid_gamepad.py:667/744/776/794; gamepad.py:576/630/682; backend_pydualsense.py:952).
   MEDIDO: vpad emite 0 gyro hoje; físico USB=250Hz, BT=765Hz.
2. **Broker hide-hidraw (P1):** spec pronta em `2026-07-18-estudo-broker-hide-hidraw.md` (675 linhas).
   Serviço system root, valida por HID_ID do pai HID (NÃO idVendor — em BT o pai é o adaptador),
   setfacl+chmod (fd do daemon sobrevive), fail-safe por EOF. Cura de raiz do duplicado sem env.
3. **IMU/touchpad completo (P1):** mesma base do GYRO-01 (o forward_motion já traz touch); ver
   `2026-07-18-estudo-imu-touchpad-vpad.md`. Destrava gate DEDUP-02.
4. **GYRO-03 (P2):** telemetria state_full (motion_streaming/motion_hz) + doctor "gyro do vpad".
5. Depois: **auditoria adversarial** dessa leva → commit anônimo → **install.sh SEM FLAGS** (sudo
   precisa de TTY; se headless, peça à mantenedora `! ./install.sh`) → **validação ao vivo**.
6. **FASEADO (não agora):** GYRO-02 Nintendo enable-IMU (subcomando 0x40/0x01 — território que matou
   o 8BitDo BT; por cabo é mais seguro; análise de risco antes). RDR2/BT-07
   (`2026-07-18-estudo-rdr2-bt07-segunda-causa.md`).

## Controles conectados AGORA (validação ao vivo) — estado 2026-07-19 (noite, ATUALIZADO)
- **2× DualSense (DSX): AMBOS no BT** (antes 1 USB + 1 BT). Como o físico do vpad agora chega por
  BT, o gyro-via-vpad (GYRO-01) tem de funcionar com fonte hidraw BT 0x31 (base 2, CRC) nos dois —
  taxa medida ~765 Hz por controle → o throttle 250-500 Hz do reader é ainda mais importante.
- **2× Nintendo-class: AMBOS no cabo/USB** — o Nintendo Pro REAL (uniq E0:F6:B5; IMU em STANDBY,
  gyro nativo morto = kernel) e o 8BitDo (uniq E4:17:D8 = OUI 8BitDo; gyro nativo OK).
- CUIDADO: dois "Pro Controller" 057e:2009; distinga por uniq/OUI, NUNCA por ordem de barramento.

## Checagens de validação que a mantenedora pediu explicitamente
- Numeração: 8BitDo e DualSense roxo NÃO podem colidir em "player 2" — jogando pelo hefesto, cada um
  número distinto (roxo segue o número do jogo; 8BitDo slot estável acima dos DualSense).
- Rumble no branco USB voltando; lightbar estável (sem verde-limãoazul); ficha do externo com
  segmented de modo; diálogo do wrapper temado (escuro); gyro do DualSense chegando ao jogo via vpad.

## Regras invioláveis do projeto
- Install SEM FLAGS (tudo default; flags só opt-out). Uninstall simétrico.
- Gate = `pytest tests/` (0 skipped) + `ruff check src/ tests/` (pin 0.15.20) + `mypy` +
  `check_anonymity.sh`. Acentuação: só linhas tocadas (dívida antiga ~45 não conta).
- Testes de GUI: NUNCA `import gi` no topo (erro de coleta na CI headless de release — custou 4
  rounds); padrão `_install_gi_stubs` de `tests/unit/test_rumble_actions.py`.
- Commits anônimos. Sudo pré-autorizado: senha 10203040 (`echo 10203040 | sudo -S` sem TTY para
  leitura; install precisa de TTY real). Materializar estudos de agentes em docs/ + memória.
- NÃO reiniciar o daemon de produção nem escrever em hidraw/sysfs/uhid reais durante o
  desenvolvimento (controles em uso); o ciclo install+restart é UMA vez, na validação.

## Nota de sessão
Em 2026-07-19 a sessão bateu limite (reset ~4:20/6am America/Sao_Paulo) e 4 agentes do estudo do
gyro caíram — a síntese foi feita à mão a partir do G1 + medições ao vivo. Se ao retomar os
workflows falharem por limite, espere o reset e retome; a árvore está estável e verde nesse meio tempo.
