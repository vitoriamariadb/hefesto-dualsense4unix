# hefesto-dualsense4unix — drop-in de resiliência do bluetooth.service.
# Instalado em /etc/systemd/system/bluetooth.service.d/10-hefesto-resilience.conf
# (sprint 2026-07-21-sprint-pesquisa-bluez-estabilidade.md, camada 2).
#
# - Restart=on-failure JÁ vem ativo no pacote bluez desta máquina (medido via
#   `systemctl cat bluetooth.service` em 22/07) — reafirmado aqui de propósito,
#   porque o template UPSTREAM traz a linha comentada e um bump futuro do
#   pacote pode regredir isso silenciosamente.
# - WatchdogSec cobre o caso que Restart=on-failure NÃO cobre: bluetoothd VIVO
#   mas travado (busy-loop upstream #784). O bluetoothd suporta sd_notify
#   watchdog (o template upstream traz "#WatchdogSec=10"); 30s é conservador —
#   valores agressivos (1s) têm custo real de CPU documentado no systemd-devel.
# - ExecStopPost fotografa os bonds a cada parada do serviço (inclusive por
#   crash) — par do timer periódico hefesto-bt-bonds-snapshot.timer. O script
#   NUNCA fotografa estado vazio (não sobrescreve backup bom com pós-crash).
#
# Vale a partir do PRÓXIMO (re)start do bluetoothd; o install faz só
# daemon-reload (nunca reinicia o serviço — derrubaria os controles BT).
[Service]
Restart=on-failure
# BT-AGENT-TRAVA-O-RESTART-01: 1 s, a pedido dela ("por mim voltava 1 segundo
# depois"). O gargalo real eram os 90 s do `hefesto-bt-agent` (ver o unit dele),
# não este valor — mas com aquele curado, este passa a ser o tempo que vale.
RestartSec=1
# BLUETOOTHD-MORTO-POR-NOS-01 (08/08/2026). Este valor era 30, e ele MATOU o
# Bluetooth dela no meio de uma sessão de medição:
#
#   00:27:35  bluetooth.service: Watchdog timeout (limit 30s)!
#   00:27:35  bluetooth.service: Killing process 1106 (bluetoothd) com SIGABRT
#   00:27:52  bluetooth.service: Main process exited, code=dumped, status=6/ABRT
#   00:27:54  bluetooth.service: Failed with result 'watchdog'
#
# Ela perdeu os quatro pareamentos e ficou sem controle nenhum. GRAU: MEDIDO —
# e a origem é nossa, sem dúvida: o `bluetooth.service` do pacote entrega
# `#WatchdogSec=10` COMENTADO, e este drop-in era o único presente na máquina.
#
# Por que 30 s é curto demais (SUSPEITA COM MECANISMO, e o mecanismo fecha): o
# laço do bluetoothd bloqueia por motivos LEGÍTIMOS. Na mesma noite, um
# `sdptool browse` estourou 35 s contra um controle que aceitava o ACL e não
# respondia nada acima dele. O upstream entrega a linha comentada exatamente
# por isso.
#
# `0` é EXPLÍCITO de propósito, e não a ausência da linha: assim o arquivo diz
# que a casa DECIDIU desligar, em vez de parecer que esqueceu — e um `systemctl
# show -p WatchdogUSec` mostra a decisão sem precisar deste comentário.
#
# O que cobre o hang de verdade não é o ping do systemd, que só sabe matar: é a
# vigia da casa, que SONDA o D-Bus e distingue "travado" de "ocupado".
WatchdogSec=0
ExecStopPost=-/usr/local/lib/hefesto-dualsense4unix/bt_bonds_snapshot.sh --quiet
# BONDS-QUE-SOBREVIVEM-01 (15/08/2026) — O GATILHO DA VOLTA, e a decisão dela de
# 08/08: "restauro de bonds tem de ser automático; manual com sudo não é
# produto". O salva-vidas gravava sozinho desde 21/07; a volta era um comando
# que ela tinha de digitar, e por isso nunca acontecia.
#
# POR QUE AQUI, e não numa unit própria — medido no journal dela em 15/08:
#
#   06:29:01  snapshot ... (4 bond(s))
#   06:29:41  snapshot ... (1 bond(s))     <- o crash comeu 3 bonds
#   06:29:46  bluetoothd: malloc_consolidate(): unaligned fastbin chunk detected
#   06:29:49  bluetooth.service: Main process exited, code=dumped, status=6/ABRT
#   06:29:50  bluetooth.service: Scheduled restart job, restart counter is at 1
#
# No ExecStopPost o daemon JÁ morreu (o systemd só o roda depois que o processo
# principal saiu): o storage está parado sem que ninguém precise parar nada — e
# parar o BlueZ à mão é o que derrubou o agente de pareamento por oito horas em
# 14/08 (AGENTE-QUE-NAO-VOLTA-01). Um segundo depois o systemd reinicia o
# daemon, e ele LÊ /var/lib/bluetooth no start: o que gravamos aqui já vale na
# volta. E roda como root porque a unit roda como root — sem sudo, sem polkit.
#
# `OnFailure=` NÃO serviria: com Restart=on-failure a unit vai para
# auto-restart, nunca para `failed` — é o "Scheduled restart job" do log acima.
#
# A ORDEM IMPORTA: o snapshot vem antes para fotografar o estrago (valor
# forense, e o acervo nunca aceita foto vazia); a restauração vem depois, sobre
# o disco já fotografado. O "-" de sempre: falha aqui nunca contamina o stop.
ExecStopPost=-/usr/local/lib/hefesto-dualsense4unix/bt_bonds_autorestore.sh --quiet
# O autorestore escreve nos dois lugares. /var/lib/bluetooth já vem do
# StateDirectory=bluetooth da unit do bluez — declarado aqui mesmo assim, com
# "-" (não falha se sumir), para que a dependência esteja ESCRITA e não dependa
# de um detalhe do pacote que um bump futuro pode mudar. Foi exatamente esse
# tipo de silêncio que produziu o BT-SNAPSHOT-SANDBOX-01.
ReadWritePaths=-/var/lib/bluetooth
# BT-SNAPSHOT-SANDBOX-01 (04/08/2026) — MEDIDO no crash de 03/08, às 23:58.
#
# O `bluetooth.service` do bluez roda com `ProtectSystem=strict` e
# `ReadWritePaths=` VAZIO: /var inteiro fica somente-leitura para ele, e o
# `ExecStopPost` acima HERDA esse sandbox. Resultado: o snapshot de bonds
# falhava exatamente na parada por CRASH — o único momento em que ele importa.
#
#   23:58:07  malloc_consolidate(): unaligned fastbin chunk detected
#   23:58:07  bluetooth.service: Main process exited, code=dumped, status=6/ABRT
#   23:58:07  bt_bonds_snapshot.sh:105: .../bt-bonds/.lock:
#             Sistema de arquivos somente para leitura
#
# O salva-vidas FUNCIONAVA pelo timer periódico (que roda fora deste sandbox) e
# FALHAVA no gancho de parada: a rede existia com um furo do tamanho exato do
# evento que ela cobre. Custou para descobrir porque o `-` do ExecStopPost
# engole a falha, e o erro só aparece no journal do `bluetooth.service` — não
# no do daemon.
#
# Abre SÓ o diretório do snapshot; o resto do hardening do bluez fica intacto.
ReadWritePaths=/var/lib/hefesto-dualsense4unix
# BT-NINTENDO-ACTIVE-01: a cada (re)start do bluetoothd, deixa o rádio amigável
# aos controles Nintendo — nome "Nintendo*" (o Pro checa o nome do host p/ sair
# do sniff frágil) + link policy default sem SNIFF (LM recusa LMP_sniff_req).
# Cura de raiz da queda do Pro/8BitDo sob carga (pesquisa 2026-07-22). '-' =
# não-fatal (adaptador pode não estar pronto no start; o watchdog reafirma).
ExecStartPost=-/usr/local/lib/hefesto-dualsense4unix/bt_active_mode.sh --quiet
