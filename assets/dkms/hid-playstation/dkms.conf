# dkms.conf — hefesto-hid-playstation (contenção BT 25/07 + clone no cabo 25/07)
# Módulo hid-playstation patchado (v7.0.11 + patch/0001 + patch/0002): retry
# opcional nos feature reports da probe, para o controle que perde o canal de
# controle Bluetooth quando vários pareiam ao mesmo tempo (0001); e probe que
# sobrevive ao pairing info curto do clone no cabo, em vez de largar o device
# sem driver nenhum (0002). Instala em updates/dkms, que vence o in-tree
# automaticamente (/etc/depmod.d/ubuntu.conf: "search updates ubuntu built-in").
#
# PACKAGE_VERSION fica em 1.0.0 DE PROPÓSITO, pelo mesmo motivo dos pacotes
# vizinhos: o dkms_lib.sh já rebuilda quando o source muda (diff -rq -x patch)
# e um bump deixaria a 1.0.0 antiga registrada e instalada em updates/dkms —
# dois .ko candidatos para o mesmo módulo. Só bumpar junto com um
# uninstall/remove explícito da versão antiga.
PACKAGE_NAME="hefesto-hid-playstation"
PACKAGE_VERSION="1.0.0"
BUILT_MODULE_NAME[0]="hid-playstation"
DEST_MODULE_LOCATION[0]="/updates/dkms"
AUTOINSTALL="yes"
MAKE[0]="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build modules"
CLEAN="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build clean"
