# dkms.conf — hefesto-hid-nintendo (Onda T + clone USB 25/07)
# Módulo hid-nintendo patchado (v7.0.11 + patch/000{1,2,3,4}-*.patch): probe BT
# com retry opcional, sem TX após esgotar o rate-limit, LEDs registrados mesmo
# com SET falho, comando USB no tamanho de report declarado + probe USB que
# degrada em vez de perder o device, parada de espera em controle mudo, e
# limiares como module params. Instala em
# updates/dkms, que vence o in-tree automaticamente
# (/etc/depmod.d/ubuntu.conf: "search updates ubuntu built-in").
#
# PACKAGE_VERSION fica em 1.0.0 DE PROPÓSITO: o dkms_lib.sh já rebuilda quando
# o source muda (diff -rq -x patch) e um bump deixaria a 1.0.0 antiga
# registrada e instalada em updates/dkms — dois .ko candidatos para o mesmo
# módulo. Só bumpar junto com um uninstall/remove explícito da versão antiga.
PACKAGE_NAME="hefesto-hid-nintendo"
PACKAGE_VERSION="1.0.0"
BUILT_MODULE_NAME[0]="hid-nintendo"
DEST_MODULE_LOCATION[0]="/updates/dkms"
AUTOINSTALL="yes"
MAKE[0]="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build modules"
CLEAN="make -C ${kernel_source_dir} M=${dkms_tree}/${PACKAGE_NAME}/${PACKAGE_VERSION}/build clean"
